/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 3);
/******/ })
/************************************************************************/
/******/ ({

/***/ "./node_modules/@babel/runtime/regenerator/index.js":
/*!**********************************************************!*\
  !*** ./node_modules/@babel/runtime/regenerator/index.js ***!
  \**********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(/*! regenerator-runtime */ "./node_modules/regenerator-runtime/runtime.js");


/***/ }),

/***/ "./node_modules/jquery/dist/jquery.js":
/*!********************************************!*\
  !*** ./node_modules/jquery/dist/jquery.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;/*!
 * jQuery JavaScript Library v3.7.1
 * https://jquery.com/
 *
 * Copyright OpenJS Foundation and other contributors
 * Released under the MIT license
 * https://jquery.org/license
 *
 * Date: 2023-08-28T13:37Z
 */
( function( global, factory ) {

	"use strict";

	if (  true && typeof module.exports === "object" ) {

		// For CommonJS and CommonJS-like environments where a proper `window`
		// is present, execute the factory and get jQuery.
		// For environments that do not have a `window` with a `document`
		// (such as Node.js), expose a factory as module.exports.
		// This accentuates the need for the creation of a real `window`.
		// e.g. var jQuery = require("jquery")(window);
		// See ticket trac-14549 for more info.
		module.exports = global.document ?
			factory( global, true ) :
			function( w ) {
				if ( !w.document ) {
					throw new Error( "jQuery requires a window with a document" );
				}
				return factory( w );
			};
	} else {
		factory( global );
	}

// Pass this if window is not defined yet
} )( typeof window !== "undefined" ? window : this, function( window, noGlobal ) {

// Edge <= 12 - 13+, Firefox <=18 - 45+, IE 10 - 11, Safari 5.1 - 9+, iOS 6 - 9.1
// throw exceptions when non-strict code (e.g., ASP.NET 4.5) accesses strict mode
// arguments.callee.caller (trac-13335). But as of jQuery 3.0 (2016), strict mode should be common
// enough that all such attempts are guarded in a try block.
"use strict";

var arr = [];

var getProto = Object.getPrototypeOf;

var slice = arr.slice;

var flat = arr.flat ? function( array ) {
	return arr.flat.call( array );
} : function( array ) {
	return arr.concat.apply( [], array );
};


var push = arr.push;

var indexOf = arr.indexOf;

var class2type = {};

var toString = class2type.toString;

var hasOwn = class2type.hasOwnProperty;

var fnToString = hasOwn.toString;

var ObjectFunctionString = fnToString.call( Object );

var support = {};

var isFunction = function isFunction( obj ) {

		// Support: Chrome <=57, Firefox <=52
		// In some browsers, typeof returns "function" for HTML <object> elements
		// (i.e., `typeof document.createElement( "object" ) === "function"`).
		// We don't want to classify *any* DOM node as a function.
		// Support: QtWeb <=3.8.5, WebKit <=534.34, wkhtmltopdf tool <=0.12.5
		// Plus for old WebKit, typeof returns "function" for HTML collections
		// (e.g., `typeof document.getElementsByTagName("div") === "function"`). (gh-4756)
		return typeof obj === "function" && typeof obj.nodeType !== "number" &&
			typeof obj.item !== "function";
	};


var isWindow = function isWindow( obj ) {
		return obj != null && obj === obj.window;
	};


var document = window.document;



	var preservedScriptAttributes = {
		type: true,
		src: true,
		nonce: true,
		noModule: true
	};

	function DOMEval( code, node, doc ) {
		doc = doc || document;

		var i, val,
			script = doc.createElement( "script" );

		script.text = code;
		if ( node ) {
			for ( i in preservedScriptAttributes ) {

				// Support: Firefox 64+, Edge 18+
				// Some browsers don't support the "nonce" property on scripts.
				// On the other hand, just using `getAttribute` is not enough as
				// the `nonce` attribute is reset to an empty string whenever it
				// becomes browsing-context connected.
				// See https://github.com/whatwg/html/issues/2369
				// See https://html.spec.whatwg.org/#nonce-attributes
				// The `node.getAttribute` check was added for the sake of
				// `jQuery.globalEval` so that it can fake a nonce-containing node
				// via an object.
				val = node[ i ] || node.getAttribute && node.getAttribute( i );
				if ( val ) {
					script.setAttribute( i, val );
				}
			}
		}
		doc.head.appendChild( script ).parentNode.removeChild( script );
	}


function toType( obj ) {
	if ( obj == null ) {
		return obj + "";
	}

	// Support: Android <=2.3 only (functionish RegExp)
	return typeof obj === "object" || typeof obj === "function" ?
		class2type[ toString.call( obj ) ] || "object" :
		typeof obj;
}
/* global Symbol */
// Defining this global in .eslintrc.json would create a danger of using the global
// unguarded in another place, it seems safer to define global only for this module



var version = "3.7.1",

	rhtmlSuffix = /HTML$/i,

	// Define a local copy of jQuery
	jQuery = function( selector, context ) {

		// The jQuery object is actually just the init constructor 'enhanced'
		// Need init if jQuery is called (just allow error to be thrown if not included)
		return new jQuery.fn.init( selector, context );
	};

jQuery.fn = jQuery.prototype = {

	// The current version of jQuery being used
	jquery: version,

	constructor: jQuery,

	// The default length of a jQuery object is 0
	length: 0,

	toArray: function() {
		return slice.call( this );
	},

	// Get the Nth element in the matched element set OR
	// Get the whole matched element set as a clean array
	get: function( num ) {

		// Return all the elements in a clean array
		if ( num == null ) {
			return slice.call( this );
		}

		// Return just the one element from the set
		return num < 0 ? this[ num + this.length ] : this[ num ];
	},

	// Take an array of elements and push it onto the stack
	// (returning the new matched element set)
	pushStack: function( elems ) {

		// Build a new jQuery matched element set
		var ret = jQuery.merge( this.constructor(), elems );

		// Add the old object onto the stack (as a reference)
		ret.prevObject = this;

		// Return the newly-formed element set
		return ret;
	},

	// Execute a callback for every element in the matched set.
	each: function( callback ) {
		return jQuery.each( this, callback );
	},

	map: function( callback ) {
		return this.pushStack( jQuery.map( this, function( elem, i ) {
			return callback.call( elem, i, elem );
		} ) );
	},

	slice: function() {
		return this.pushStack( slice.apply( this, arguments ) );
	},

	first: function() {
		return this.eq( 0 );
	},

	last: function() {
		return this.eq( -1 );
	},

	even: function() {
		return this.pushStack( jQuery.grep( this, function( _elem, i ) {
			return ( i + 1 ) % 2;
		} ) );
	},

	odd: function() {
		return this.pushStack( jQuery.grep( this, function( _elem, i ) {
			return i % 2;
		} ) );
	},

	eq: function( i ) {
		var len = this.length,
			j = +i + ( i < 0 ? len : 0 );
		return this.pushStack( j >= 0 && j < len ? [ this[ j ] ] : [] );
	},

	end: function() {
		return this.prevObject || this.constructor();
	},

	// For internal use only.
	// Behaves like an Array's method, not like a jQuery method.
	push: push,
	sort: arr.sort,
	splice: arr.splice
};

jQuery.extend = jQuery.fn.extend = function() {
	var options, name, src, copy, copyIsArray, clone,
		target = arguments[ 0 ] || {},
		i = 1,
		length = arguments.length,
		deep = false;

	// Handle a deep copy situation
	if ( typeof target === "boolean" ) {
		deep = target;

		// Skip the boolean and the target
		target = arguments[ i ] || {};
		i++;
	}

	// Handle case when target is a string or something (possible in deep copy)
	if ( typeof target !== "object" && !isFunction( target ) ) {
		target = {};
	}

	// Extend jQuery itself if only one argument is passed
	if ( i === length ) {
		target = this;
		i--;
	}

	for ( ; i < length; i++ ) {

		// Only deal with non-null/undefined values
		if ( ( options = arguments[ i ] ) != null ) {

			// Extend the base object
			for ( name in options ) {
				copy = options[ name ];

				// Prevent Object.prototype pollution
				// Prevent never-ending loop
				if ( name === "__proto__" || target === copy ) {
					continue;
				}

				// Recurse if we're merging plain objects or arrays
				if ( deep && copy && ( jQuery.isPlainObject( copy ) ||
					( copyIsArray = Array.isArray( copy ) ) ) ) {
					src = target[ name ];

					// Ensure proper type for the source value
					if ( copyIsArray && !Array.isArray( src ) ) {
						clone = [];
					} else if ( !copyIsArray && !jQuery.isPlainObject( src ) ) {
						clone = {};
					} else {
						clone = src;
					}
					copyIsArray = false;

					// Never move original objects, clone them
					target[ name ] = jQuery.extend( deep, clone, copy );

				// Don't bring in undefined values
				} else if ( copy !== undefined ) {
					target[ name ] = copy;
				}
			}
		}
	}

	// Return the modified object
	return target;
};

jQuery.extend( {

	// Unique for each copy of jQuery on the page
	expando: "jQuery" + ( version + Math.random() ).replace( /\D/g, "" ),

	// Assume jQuery is ready without the ready module
	isReady: true,

	error: function( msg ) {
		throw new Error( msg );
	},

	noop: function() {},

	isPlainObject: function( obj ) {
		var proto, Ctor;

		// Detect obvious negatives
		// Use toString instead of jQuery.type to catch host objects
		if ( !obj || toString.call( obj ) !== "[object Object]" ) {
			return false;
		}

		proto = getProto( obj );

		// Objects with no prototype (e.g., `Object.create( null )`) are plain
		if ( !proto ) {
			return true;
		}

		// Objects with prototype are plain iff they were constructed by a global Object function
		Ctor = hasOwn.call( proto, "constructor" ) && proto.constructor;
		return typeof Ctor === "function" && fnToString.call( Ctor ) === ObjectFunctionString;
	},

	isEmptyObject: function( obj ) {
		var name;

		for ( name in obj ) {
			return false;
		}
		return true;
	},

	// Evaluates a script in a provided context; falls back to the global one
	// if not specified.
	globalEval: function( code, options, doc ) {
		DOMEval( code, { nonce: options && options.nonce }, doc );
	},

	each: function( obj, callback ) {
		var length, i = 0;

		if ( isArrayLike( obj ) ) {
			length = obj.length;
			for ( ; i < length; i++ ) {
				if ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {
					break;
				}
			}
		} else {
			for ( i in obj ) {
				if ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {
					break;
				}
			}
		}

		return obj;
	},


	// Retrieve the text value of an array of DOM nodes
	text: function( elem ) {
		var node,
			ret = "",
			i = 0,
			nodeType = elem.nodeType;

		if ( !nodeType ) {

			// If no nodeType, this is expected to be an array
			while ( ( node = elem[ i++ ] ) ) {

				// Do not traverse comment nodes
				ret += jQuery.text( node );
			}
		}
		if ( nodeType === 1 || nodeType === 11 ) {
			return elem.textContent;
		}
		if ( nodeType === 9 ) {
			return elem.documentElement.textContent;
		}
		if ( nodeType === 3 || nodeType === 4 ) {
			return elem.nodeValue;
		}

		// Do not include comment or processing instruction nodes

		return ret;
	},

	// results is for internal usage only
	makeArray: function( arr, results ) {
		var ret = results || [];

		if ( arr != null ) {
			if ( isArrayLike( Object( arr ) ) ) {
				jQuery.merge( ret,
					typeof arr === "string" ?
						[ arr ] : arr
				);
			} else {
				push.call( ret, arr );
			}
		}

		return ret;
	},

	inArray: function( elem, arr, i ) {
		return arr == null ? -1 : indexOf.call( arr, elem, i );
	},

	isXMLDoc: function( elem ) {
		var namespace = elem && elem.namespaceURI,
			docElem = elem && ( elem.ownerDocument || elem ).documentElement;

		// Assume HTML when documentElement doesn't yet exist, such as inside
		// document fragments.
		return !rhtmlSuffix.test( namespace || docElem && docElem.nodeName || "HTML" );
	},

	// Support: Android <=4.0 only, PhantomJS 1 only
	// push.apply(_, arraylike) throws on ancient WebKit
	merge: function( first, second ) {
		var len = +second.length,
			j = 0,
			i = first.length;

		for ( ; j < len; j++ ) {
			first[ i++ ] = second[ j ];
		}

		first.length = i;

		return first;
	},

	grep: function( elems, callback, invert ) {
		var callbackInverse,
			matches = [],
			i = 0,
			length = elems.length,
			callbackExpect = !invert;

		// Go through the array, only saving the items
		// that pass the validator function
		for ( ; i < length; i++ ) {
			callbackInverse = !callback( elems[ i ], i );
			if ( callbackInverse !== callbackExpect ) {
				matches.push( elems[ i ] );
			}
		}

		return matches;
	},

	// arg is for internal usage only
	map: function( elems, callback, arg ) {
		var length, value,
			i = 0,
			ret = [];

		// Go through the array, translating each of the items to their new values
		if ( isArrayLike( elems ) ) {
			length = elems.length;
			for ( ; i < length; i++ ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret.push( value );
				}
			}

		// Go through every key on the object,
		} else {
			for ( i in elems ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret.push( value );
				}
			}
		}

		// Flatten any nested arrays
		return flat( ret );
	},

	// A global GUID counter for objects
	guid: 1,

	// jQuery.support is not used in Core but other projects attach their
	// properties to it so it needs to exist.
	support: support
} );

if ( typeof Symbol === "function" ) {
	jQuery.fn[ Symbol.iterator ] = arr[ Symbol.iterator ];
}

// Populate the class2type map
jQuery.each( "Boolean Number String Function Array Date RegExp Object Error Symbol".split( " " ),
	function( _i, name ) {
		class2type[ "[object " + name + "]" ] = name.toLowerCase();
	} );

function isArrayLike( obj ) {

	// Support: real iOS 8.2 only (not reproducible in simulator)
	// `in` check used to prevent JIT error (gh-2145)
	// hasOwn isn't used here due to false negatives
	// regarding Nodelist length in IE
	var length = !!obj && "length" in obj && obj.length,
		type = toType( obj );

	if ( isFunction( obj ) || isWindow( obj ) ) {
		return false;
	}

	return type === "array" || length === 0 ||
		typeof length === "number" && length > 0 && ( length - 1 ) in obj;
}


function nodeName( elem, name ) {

	return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();

}
var pop = arr.pop;


var sort = arr.sort;


var splice = arr.splice;


var whitespace = "[\\x20\\t\\r\\n\\f]";


var rtrimCSS = new RegExp(
	"^" + whitespace + "+|((?:^|[^\\\\])(?:\\\\.)*)" + whitespace + "+$",
	"g"
);




// Note: an element does not contain itself
jQuery.contains = function( a, b ) {
	var bup = b && b.parentNode;

	return a === bup || !!( bup && bup.nodeType === 1 && (

		// Support: IE 9 - 11+
		// IE doesn't have `contains` on SVG.
		a.contains ?
			a.contains( bup ) :
			a.compareDocumentPosition && a.compareDocumentPosition( bup ) & 16
	) );
};




// CSS string/identifier serialization
// https://drafts.csswg.org/cssom/#common-serializing-idioms
var rcssescape = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\x80-\uFFFF\w-]/g;

function fcssescape( ch, asCodePoint ) {
	if ( asCodePoint ) {

		// U+0000 NULL becomes U+FFFD REPLACEMENT CHARACTER
		if ( ch === "\0" ) {
			return "\uFFFD";
		}

		// Control characters and (dependent upon position) numbers get escaped as code points
		return ch.slice( 0, -1 ) + "\\" + ch.charCodeAt( ch.length - 1 ).toString( 16 ) + " ";
	}

	// Other potentially-special ASCII characters get backslash-escaped
	return "\\" + ch;
}

jQuery.escapeSelector = function( sel ) {
	return ( sel + "" ).replace( rcssescape, fcssescape );
};




var preferredDoc = document,
	pushNative = push;

( function() {

var i,
	Expr,
	outermostContext,
	sortInput,
	hasDuplicate,
	push = pushNative,

	// Local document vars
	document,
	documentElement,
	documentIsHTML,
	rbuggyQSA,
	matches,

	// Instance-specific data
	expando = jQuery.expando,
	dirruns = 0,
	done = 0,
	classCache = createCache(),
	tokenCache = createCache(),
	compilerCache = createCache(),
	nonnativeSelectorCache = createCache(),
	sortOrder = function( a, b ) {
		if ( a === b ) {
			hasDuplicate = true;
		}
		return 0;
	},

	booleans = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|" +
		"loop|multiple|open|readonly|required|scoped",

	// Regular expressions

	// https://www.w3.org/TR/css-syntax-3/#ident-token-diagram
	identifier = "(?:\\\\[\\da-fA-F]{1,6}" + whitespace +
		"?|\\\\[^\\r\\n\\f]|[\\w-]|[^\0-\\x7f])+",

	// Attribute selectors: https://www.w3.org/TR/selectors/#attribute-selectors
	attributes = "\\[" + whitespace + "*(" + identifier + ")(?:" + whitespace +

		// Operator (capture 2)
		"*([*^$|!~]?=)" + whitespace +

		// "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
		"*(?:'((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\"|(" + identifier + "))|)" +
		whitespace + "*\\]",

	pseudos = ":(" + identifier + ")(?:\\((" +

		// To reduce the number of selectors needing tokenize in the preFilter, prefer arguments:
		// 1. quoted (capture 3; capture 4 or capture 5)
		"('((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\")|" +

		// 2. simple (capture 6)
		"((?:\\\\.|[^\\\\()[\\]]|" + attributes + ")*)|" +

		// 3. anything else (capture 2)
		".*" +
		")\\)|)",

	// Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
	rwhitespace = new RegExp( whitespace + "+", "g" ),

	rcomma = new RegExp( "^" + whitespace + "*," + whitespace + "*" ),
	rleadingCombinator = new RegExp( "^" + whitespace + "*([>+~]|" + whitespace + ")" +
		whitespace + "*" ),
	rdescend = new RegExp( whitespace + "|>" ),

	rpseudo = new RegExp( pseudos ),
	ridentifier = new RegExp( "^" + identifier + "$" ),

	matchExpr = {
		ID: new RegExp( "^#(" + identifier + ")" ),
		CLASS: new RegExp( "^\\.(" + identifier + ")" ),
		TAG: new RegExp( "^(" + identifier + "|[*])" ),
		ATTR: new RegExp( "^" + attributes ),
		PSEUDO: new RegExp( "^" + pseudos ),
		CHILD: new RegExp(
			"^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" +
				whitespace + "*(even|odd|(([+-]|)(\\d*)n|)" + whitespace + "*(?:([+-]|)" +
				whitespace + "*(\\d+)|))" + whitespace + "*\\)|)", "i" ),
		bool: new RegExp( "^(?:" + booleans + ")$", "i" ),

		// For use in libraries implementing .is()
		// We use this for POS matching in `select`
		needsContext: new RegExp( "^" + whitespace +
			"*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + whitespace +
			"*((?:-\\d)?\\d*)" + whitespace + "*\\)|)(?=[^-]|$)", "i" )
	},

	rinputs = /^(?:input|select|textarea|button)$/i,
	rheader = /^h\d$/i,

	// Easily-parseable/retrievable ID or TAG or CLASS selectors
	rquickExpr = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/,

	rsibling = /[+~]/,

	// CSS escapes
	// https://www.w3.org/TR/CSS21/syndata.html#escaped-characters
	runescape = new RegExp( "\\\\[\\da-fA-F]{1,6}" + whitespace +
		"?|\\\\([^\\r\\n\\f])", "g" ),
	funescape = function( escape, nonHex ) {
		var high = "0x" + escape.slice( 1 ) - 0x10000;

		if ( nonHex ) {

			// Strip the backslash prefix from a non-hex escape sequence
			return nonHex;
		}

		// Replace a hexadecimal escape sequence with the encoded Unicode code point
		// Support: IE <=11+
		// For values outside the Basic Multilingual Plane (BMP), manually construct a
		// surrogate pair
		return high < 0 ?
			String.fromCharCode( high + 0x10000 ) :
			String.fromCharCode( high >> 10 | 0xD800, high & 0x3FF | 0xDC00 );
	},

	// Used for iframes; see `setDocument`.
	// Support: IE 9 - 11+, Edge 12 - 18+
	// Removing the function wrapper causes a "Permission Denied"
	// error in IE/Edge.
	unloadHandler = function() {
		setDocument();
	},

	inDisabledFieldset = addCombinator(
		function( elem ) {
			return elem.disabled === true && nodeName( elem, "fieldset" );
		},
		{ dir: "parentNode", next: "legend" }
	);

// Support: IE <=9 only
// Accessing document.activeElement can throw unexpectedly
// https://bugs.jquery.com/ticket/13393
function safeActiveElement() {
	try {
		return document.activeElement;
	} catch ( err ) { }
}

// Optimize for push.apply( _, NodeList )
try {
	push.apply(
		( arr = slice.call( preferredDoc.childNodes ) ),
		preferredDoc.childNodes
	);

	// Support: Android <=4.0
	// Detect silently failing push.apply
	// eslint-disable-next-line no-unused-expressions
	arr[ preferredDoc.childNodes.length ].nodeType;
} catch ( e ) {
	push = {
		apply: function( target, els ) {
			pushNative.apply( target, slice.call( els ) );
		},
		call: function( target ) {
			pushNative.apply( target, slice.call( arguments, 1 ) );
		}
	};
}

function find( selector, context, results, seed ) {
	var m, i, elem, nid, match, groups, newSelector,
		newContext = context && context.ownerDocument,

		// nodeType defaults to 9, since context defaults to document
		nodeType = context ? context.nodeType : 9;

	results = results || [];

	// Return early from calls with invalid selector or context
	if ( typeof selector !== "string" || !selector ||
		nodeType !== 1 && nodeType !== 9 && nodeType !== 11 ) {

		return results;
	}

	// Try to shortcut find operations (as opposed to filters) in HTML documents
	if ( !seed ) {
		setDocument( context );
		context = context || document;

		if ( documentIsHTML ) {

			// If the selector is sufficiently simple, try using a "get*By*" DOM method
			// (excepting DocumentFragment context, where the methods don't exist)
			if ( nodeType !== 11 && ( match = rquickExpr.exec( selector ) ) ) {

				// ID selector
				if ( ( m = match[ 1 ] ) ) {

					// Document context
					if ( nodeType === 9 ) {
						if ( ( elem = context.getElementById( m ) ) ) {

							// Support: IE 9 only
							// getElementById can match elements by name instead of ID
							if ( elem.id === m ) {
								push.call( results, elem );
								return results;
							}
						} else {
							return results;
						}

					// Element context
					} else {

						// Support: IE 9 only
						// getElementById can match elements by name instead of ID
						if ( newContext && ( elem = newContext.getElementById( m ) ) &&
							find.contains( context, elem ) &&
							elem.id === m ) {

							push.call( results, elem );
							return results;
						}
					}

				// Type selector
				} else if ( match[ 2 ] ) {
					push.apply( results, context.getElementsByTagName( selector ) );
					return results;

				// Class selector
				} else if ( ( m = match[ 3 ] ) && context.getElementsByClassName ) {
					push.apply( results, context.getElementsByClassName( m ) );
					return results;
				}
			}

			// Take advantage of querySelectorAll
			if ( !nonnativeSelectorCache[ selector + " " ] &&
				( !rbuggyQSA || !rbuggyQSA.test( selector ) ) ) {

				newSelector = selector;
				newContext = context;

				// qSA considers elements outside a scoping root when evaluating child or
				// descendant combinators, which is not what we want.
				// In such cases, we work around the behavior by prefixing every selector in the
				// list with an ID selector referencing the scope context.
				// The technique has to be used as well when a leading combinator is used
				// as such selectors are not recognized by querySelectorAll.
				// Thanks to Andrew Dupont for this technique.
				if ( nodeType === 1 &&
					( rdescend.test( selector ) || rleadingCombinator.test( selector ) ) ) {

					// Expand context for sibling selectors
					newContext = rsibling.test( selector ) && testContext( context.parentNode ) ||
						context;

					// We can use :scope instead of the ID hack if the browser
					// supports it & if we're not changing the context.
					// Support: IE 11+, Edge 17 - 18+
					// IE/Edge sometimes throw a "Permission denied" error when
					// strict-comparing two documents; shallow comparisons work.
					// eslint-disable-next-line eqeqeq
					if ( newContext != context || !support.scope ) {

						// Capture the context ID, setting it first if necessary
						if ( ( nid = context.getAttribute( "id" ) ) ) {
							nid = jQuery.escapeSelector( nid );
						} else {
							context.setAttribute( "id", ( nid = expando ) );
						}
					}

					// Prefix every selector in the list
					groups = tokenize( selector );
					i = groups.length;
					while ( i-- ) {
						groups[ i ] = ( nid ? "#" + nid : ":scope" ) + " " +
							toSelector( groups[ i ] );
					}
					newSelector = groups.join( "," );
				}

				try {
					push.apply( results,
						newContext.querySelectorAll( newSelector )
					);
					return results;
				} catch ( qsaError ) {
					nonnativeSelectorCache( selector, true );
				} finally {
					if ( nid === expando ) {
						context.removeAttribute( "id" );
					}
				}
			}
		}
	}

	// All others
	return select( selector.replace( rtrimCSS, "$1" ), context, results, seed );
}

/**
 * Create key-value caches of limited size
 * @returns {function(string, object)} Returns the Object data after storing it on itself with
 *	property name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)
 *	deleting the oldest entry
 */
function createCache() {
	var keys = [];

	function cache( key, value ) {

		// Use (key + " ") to avoid collision with native prototype properties
		// (see https://github.com/jquery/sizzle/issues/157)
		if ( keys.push( key + " " ) > Expr.cacheLength ) {

			// Only keep the most recent entries
			delete cache[ keys.shift() ];
		}
		return ( cache[ key + " " ] = value );
	}
	return cache;
}

/**
 * Mark a function for special use by jQuery selector module
 * @param {Function} fn The function to mark
 */
function markFunction( fn ) {
	fn[ expando ] = true;
	return fn;
}

/**
 * Support testing using an element
 * @param {Function} fn Passed the created element and returns a boolean result
 */
function assert( fn ) {
	var el = document.createElement( "fieldset" );

	try {
		return !!fn( el );
	} catch ( e ) {
		return false;
	} finally {

		// Remove from its parent by default
		if ( el.parentNode ) {
			el.parentNode.removeChild( el );
		}

		// release memory in IE
		el = null;
	}
}

/**
 * Returns a function to use in pseudos for input types
 * @param {String} type
 */
function createInputPseudo( type ) {
	return function( elem ) {
		return nodeName( elem, "input" ) && elem.type === type;
	};
}

/**
 * Returns a function to use in pseudos for buttons
 * @param {String} type
 */
function createButtonPseudo( type ) {
	return function( elem ) {
		return ( nodeName( elem, "input" ) || nodeName( elem, "button" ) ) &&
			elem.type === type;
	};
}

/**
 * Returns a function to use in pseudos for :enabled/:disabled
 * @param {Boolean} disabled true for :disabled; false for :enabled
 */
function createDisabledPseudo( disabled ) {

	// Known :disabled false positives: fieldset[disabled] > legend:nth-of-type(n+2) :can-disable
	return function( elem ) {

		// Only certain elements can match :enabled or :disabled
		// https://html.spec.whatwg.org/multipage/scripting.html#selector-enabled
		// https://html.spec.whatwg.org/multipage/scripting.html#selector-disabled
		if ( "form" in elem ) {

			// Check for inherited disabledness on relevant non-disabled elements:
			// * listed form-associated elements in a disabled fieldset
			//   https://html.spec.whatwg.org/multipage/forms.html#category-listed
			//   https://html.spec.whatwg.org/multipage/forms.html#concept-fe-disabled
			// * option elements in a disabled optgroup
			//   https://html.spec.whatwg.org/multipage/forms.html#concept-option-disabled
			// All such elements have a "form" property.
			if ( elem.parentNode && elem.disabled === false ) {

				// Option elements defer to a parent optgroup if present
				if ( "label" in elem ) {
					if ( "label" in elem.parentNode ) {
						return elem.parentNode.disabled === disabled;
					} else {
						return elem.disabled === disabled;
					}
				}

				// Support: IE 6 - 11+
				// Use the isDisabled shortcut property to check for disabled fieldset ancestors
				return elem.isDisabled === disabled ||

					// Where there is no isDisabled, check manually
					elem.isDisabled !== !disabled &&
						inDisabledFieldset( elem ) === disabled;
			}

			return elem.disabled === disabled;

		// Try to winnow out elements that can't be disabled before trusting the disabled property.
		// Some victims get caught in our net (label, legend, menu, track), but it shouldn't
		// even exist on them, let alone have a boolean value.
		} else if ( "label" in elem ) {
			return elem.disabled === disabled;
		}

		// Remaining elements are neither :enabled nor :disabled
		return false;
	};
}

/**
 * Returns a function to use in pseudos for positionals
 * @param {Function} fn
 */
function createPositionalPseudo( fn ) {
	return markFunction( function( argument ) {
		argument = +argument;
		return markFunction( function( seed, matches ) {
			var j,
				matchIndexes = fn( [], seed.length, argument ),
				i = matchIndexes.length;

			// Match elements found at the specified indexes
			while ( i-- ) {
				if ( seed[ ( j = matchIndexes[ i ] ) ] ) {
					seed[ j ] = !( matches[ j ] = seed[ j ] );
				}
			}
		} );
	} );
}

/**
 * Checks a node for validity as a jQuery selector context
 * @param {Element|Object=} context
 * @returns {Element|Object|Boolean} The input node if acceptable, otherwise a falsy value
 */
function testContext( context ) {
	return context && typeof context.getElementsByTagName !== "undefined" && context;
}

/**
 * Sets document-related variables once based on the current document
 * @param {Element|Object} [node] An element or document object to use to set the document
 * @returns {Object} Returns the current document
 */
function setDocument( node ) {
	var subWindow,
		doc = node ? node.ownerDocument || node : preferredDoc;

	// Return early if doc is invalid or already selected
	// Support: IE 11+, Edge 17 - 18+
	// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
	// two documents; shallow comparisons work.
	// eslint-disable-next-line eqeqeq
	if ( doc == document || doc.nodeType !== 9 || !doc.documentElement ) {
		return document;
	}

	// Update global variables
	document = doc;
	documentElement = document.documentElement;
	documentIsHTML = !jQuery.isXMLDoc( document );

	// Support: iOS 7 only, IE 9 - 11+
	// Older browsers didn't support unprefixed `matches`.
	matches = documentElement.matches ||
		documentElement.webkitMatchesSelector ||
		documentElement.msMatchesSelector;

	// Support: IE 9 - 11+, Edge 12 - 18+
	// Accessing iframe documents after unload throws "permission denied" errors
	// (see trac-13936).
	// Limit the fix to IE & Edge Legacy; despite Edge 15+ implementing `matches`,
	// all IE 9+ and Edge Legacy versions implement `msMatchesSelector` as well.
	if ( documentElement.msMatchesSelector &&

		// Support: IE 11+, Edge 17 - 18+
		// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
		// two documents; shallow comparisons work.
		// eslint-disable-next-line eqeqeq
		preferredDoc != document &&
		( subWindow = document.defaultView ) && subWindow.top !== subWindow ) {

		// Support: IE 9 - 11+, Edge 12 - 18+
		subWindow.addEventListener( "unload", unloadHandler );
	}

	// Support: IE <10
	// Check if getElementById returns elements by name
	// The broken getElementById methods don't pick up programmatically-set names,
	// so use a roundabout getElementsByName test
	support.getById = assert( function( el ) {
		documentElement.appendChild( el ).id = jQuery.expando;
		return !document.getElementsByName ||
			!document.getElementsByName( jQuery.expando ).length;
	} );

	// Support: IE 9 only
	// Check to see if it's possible to do matchesSelector
	// on a disconnected node.
	support.disconnectedMatch = assert( function( el ) {
		return matches.call( el, "*" );
	} );

	// Support: IE 9 - 11+, Edge 12 - 18+
	// IE/Edge don't support the :scope pseudo-class.
	support.scope = assert( function() {
		return document.querySelectorAll( ":scope" );
	} );

	// Support: Chrome 105 - 111 only, Safari 15.4 - 16.3 only
	// Make sure the `:has()` argument is parsed unforgivingly.
	// We include `*` in the test to detect buggy implementations that are
	// _selectively_ forgiving (specifically when the list includes at least
	// one valid selector).
	// Note that we treat complete lack of support for `:has()` as if it were
	// spec-compliant support, which is fine because use of `:has()` in such
	// environments will fail in the qSA path and fall back to jQuery traversal
	// anyway.
	support.cssHas = assert( function() {
		try {
			document.querySelector( ":has(*,:jqfake)" );
			return false;
		} catch ( e ) {
			return true;
		}
	} );

	// ID filter and find
	if ( support.getById ) {
		Expr.filter.ID = function( id ) {
			var attrId = id.replace( runescape, funescape );
			return function( elem ) {
				return elem.getAttribute( "id" ) === attrId;
			};
		};
		Expr.find.ID = function( id, context ) {
			if ( typeof context.getElementById !== "undefined" && documentIsHTML ) {
				var elem = context.getElementById( id );
				return elem ? [ elem ] : [];
			}
		};
	} else {
		Expr.filter.ID =  function( id ) {
			var attrId = id.replace( runescape, funescape );
			return function( elem ) {
				var node = typeof elem.getAttributeNode !== "undefined" &&
					elem.getAttributeNode( "id" );
				return node && node.value === attrId;
			};
		};

		// Support: IE 6 - 7 only
		// getElementById is not reliable as a find shortcut
		Expr.find.ID = function( id, context ) {
			if ( typeof context.getElementById !== "undefined" && documentIsHTML ) {
				var node, i, elems,
					elem = context.getElementById( id );

				if ( elem ) {

					// Verify the id attribute
					node = elem.getAttributeNode( "id" );
					if ( node && node.value === id ) {
						return [ elem ];
					}

					// Fall back on getElementsByName
					elems = context.getElementsByName( id );
					i = 0;
					while ( ( elem = elems[ i++ ] ) ) {
						node = elem.getAttributeNode( "id" );
						if ( node && node.value === id ) {
							return [ elem ];
						}
					}
				}

				return [];
			}
		};
	}

	// Tag
	Expr.find.TAG = function( tag, context ) {
		if ( typeof context.getElementsByTagName !== "undefined" ) {
			return context.getElementsByTagName( tag );

		// DocumentFragment nodes don't have gEBTN
		} else {
			return context.querySelectorAll( tag );
		}
	};

	// Class
	Expr.find.CLASS = function( className, context ) {
		if ( typeof context.getElementsByClassName !== "undefined" && documentIsHTML ) {
			return context.getElementsByClassName( className );
		}
	};

	/* QSA/matchesSelector
	---------------------------------------------------------------------- */

	// QSA and matchesSelector support

	rbuggyQSA = [];

	// Build QSA regex
	// Regex strategy adopted from Diego Perini
	assert( function( el ) {

		var input;

		documentElement.appendChild( el ).innerHTML =
			"<a id='" + expando + "' href='' disabled='disabled'></a>" +
			"<select id='" + expando + "-\r\\' disabled='disabled'>" +
			"<option selected=''></option></select>";

		// Support: iOS <=7 - 8 only
		// Boolean attributes and "value" are not treated correctly in some XML documents
		if ( !el.querySelectorAll( "[selected]" ).length ) {
			rbuggyQSA.push( "\\[" + whitespace + "*(?:value|" + booleans + ")" );
		}

		// Support: iOS <=7 - 8 only
		if ( !el.querySelectorAll( "[id~=" + expando + "-]" ).length ) {
			rbuggyQSA.push( "~=" );
		}

		// Support: iOS 8 only
		// https://bugs.webkit.org/show_bug.cgi?id=136851
		// In-page `selector#id sibling-combinator selector` fails
		if ( !el.querySelectorAll( "a#" + expando + "+*" ).length ) {
			rbuggyQSA.push( ".#.+[+~]" );
		}

		// Support: Chrome <=105+, Firefox <=104+, Safari <=15.4+
		// In some of the document kinds, these selectors wouldn't work natively.
		// This is probably OK but for backwards compatibility we want to maintain
		// handling them through jQuery traversal in jQuery 3.x.
		if ( !el.querySelectorAll( ":checked" ).length ) {
			rbuggyQSA.push( ":checked" );
		}

		// Support: Windows 8 Native Apps
		// The type and name attributes are restricted during .innerHTML assignment
		input = document.createElement( "input" );
		input.setAttribute( "type", "hidden" );
		el.appendChild( input ).setAttribute( "name", "D" );

		// Support: IE 9 - 11+
		// IE's :disabled selector does not pick up the children of disabled fieldsets
		// Support: Chrome <=105+, Firefox <=104+, Safari <=15.4+
		// In some of the document kinds, these selectors wouldn't work natively.
		// This is probably OK but for backwards compatibility we want to maintain
		// handling them through jQuery traversal in jQuery 3.x.
		documentElement.appendChild( el ).disabled = true;
		if ( el.querySelectorAll( ":disabled" ).length !== 2 ) {
			rbuggyQSA.push( ":enabled", ":disabled" );
		}

		// Support: IE 11+, Edge 15 - 18+
		// IE 11/Edge don't find elements on a `[name='']` query in some cases.
		// Adding a temporary attribute to the document before the selection works
		// around the issue.
		// Interestingly, IE 10 & older don't seem to have the issue.
		input = document.createElement( "input" );
		input.setAttribute( "name", "" );
		el.appendChild( input );
		if ( !el.querySelectorAll( "[name='']" ).length ) {
			rbuggyQSA.push( "\\[" + whitespace + "*name" + whitespace + "*=" +
				whitespace + "*(?:''|\"\")" );
		}
	} );

	if ( !support.cssHas ) {

		// Support: Chrome 105 - 110+, Safari 15.4 - 16.3+
		// Our regular `try-catch` mechanism fails to detect natively-unsupported
		// pseudo-classes inside `:has()` (such as `:has(:contains("Foo"))`)
		// in browsers that parse the `:has()` argument as a forgiving selector list.
		// https://drafts.csswg.org/selectors/#relational now requires the argument
		// to be parsed unforgivingly, but browsers have not yet fully adjusted.
		rbuggyQSA.push( ":has" );
	}

	rbuggyQSA = rbuggyQSA.length && new RegExp( rbuggyQSA.join( "|" ) );

	/* Sorting
	---------------------------------------------------------------------- */

	// Document order sorting
	sortOrder = function( a, b ) {

		// Flag for duplicate removal
		if ( a === b ) {
			hasDuplicate = true;
			return 0;
		}

		// Sort on method existence if only one input has compareDocumentPosition
		var compare = !a.compareDocumentPosition - !b.compareDocumentPosition;
		if ( compare ) {
			return compare;
		}

		// Calculate position if both inputs belong to the same document
		// Support: IE 11+, Edge 17 - 18+
		// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
		// two documents; shallow comparisons work.
		// eslint-disable-next-line eqeqeq
		compare = ( a.ownerDocument || a ) == ( b.ownerDocument || b ) ?
			a.compareDocumentPosition( b ) :

			// Otherwise we know they are disconnected
			1;

		// Disconnected nodes
		if ( compare & 1 ||
			( !support.sortDetached && b.compareDocumentPosition( a ) === compare ) ) {

			// Choose the first element that is related to our preferred document
			// Support: IE 11+, Edge 17 - 18+
			// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
			// two documents; shallow comparisons work.
			// eslint-disable-next-line eqeqeq
			if ( a === document || a.ownerDocument == preferredDoc &&
				find.contains( preferredDoc, a ) ) {
				return -1;
			}

			// Support: IE 11+, Edge 17 - 18+
			// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
			// two documents; shallow comparisons work.
			// eslint-disable-next-line eqeqeq
			if ( b === document || b.ownerDocument == preferredDoc &&
				find.contains( preferredDoc, b ) ) {
				return 1;
			}

			// Maintain original order
			return sortInput ?
				( indexOf.call( sortInput, a ) - indexOf.call( sortInput, b ) ) :
				0;
		}

		return compare & 4 ? -1 : 1;
	};

	return document;
}

find.matches = function( expr, elements ) {
	return find( expr, null, null, elements );
};

find.matchesSelector = function( elem, expr ) {
	setDocument( elem );

	if ( documentIsHTML &&
		!nonnativeSelectorCache[ expr + " " ] &&
		( !rbuggyQSA || !rbuggyQSA.test( expr ) ) ) {

		try {
			var ret = matches.call( elem, expr );

			// IE 9's matchesSelector returns false on disconnected nodes
			if ( ret || support.disconnectedMatch ||

					// As well, disconnected nodes are said to be in a document
					// fragment in IE 9
					elem.document && elem.document.nodeType !== 11 ) {
				return ret;
			}
		} catch ( e ) {
			nonnativeSelectorCache( expr, true );
		}
	}

	return find( expr, document, null, [ elem ] ).length > 0;
};

find.contains = function( context, elem ) {

	// Set document vars if needed
	// Support: IE 11+, Edge 17 - 18+
	// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
	// two documents; shallow comparisons work.
	// eslint-disable-next-line eqeqeq
	if ( ( context.ownerDocument || context ) != document ) {
		setDocument( context );
	}
	return jQuery.contains( context, elem );
};


find.attr = function( elem, name ) {

	// Set document vars if needed
	// Support: IE 11+, Edge 17 - 18+
	// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
	// two documents; shallow comparisons work.
	// eslint-disable-next-line eqeqeq
	if ( ( elem.ownerDocument || elem ) != document ) {
		setDocument( elem );
	}

	var fn = Expr.attrHandle[ name.toLowerCase() ],

		// Don't get fooled by Object.prototype properties (see trac-13807)
		val = fn && hasOwn.call( Expr.attrHandle, name.toLowerCase() ) ?
			fn( elem, name, !documentIsHTML ) :
			undefined;

	if ( val !== undefined ) {
		return val;
	}

	return elem.getAttribute( name );
};

find.error = function( msg ) {
	throw new Error( "Syntax error, unrecognized expression: " + msg );
};

/**
 * Document sorting and removing duplicates
 * @param {ArrayLike} results
 */
jQuery.uniqueSort = function( results ) {
	var elem,
		duplicates = [],
		j = 0,
		i = 0;

	// Unless we *know* we can detect duplicates, assume their presence
	//
	// Support: Android <=4.0+
	// Testing for detecting duplicates is unpredictable so instead assume we can't
	// depend on duplicate detection in all browsers without a stable sort.
	hasDuplicate = !support.sortStable;
	sortInput = !support.sortStable && slice.call( results, 0 );
	sort.call( results, sortOrder );

	if ( hasDuplicate ) {
		while ( ( elem = results[ i++ ] ) ) {
			if ( elem === results[ i ] ) {
				j = duplicates.push( i );
			}
		}
		while ( j-- ) {
			splice.call( results, duplicates[ j ], 1 );
		}
	}

	// Clear input after sorting to release objects
	// See https://github.com/jquery/sizzle/pull/225
	sortInput = null;

	return results;
};

jQuery.fn.uniqueSort = function() {
	return this.pushStack( jQuery.uniqueSort( slice.apply( this ) ) );
};

Expr = jQuery.expr = {

	// Can be adjusted by the user
	cacheLength: 50,

	createPseudo: markFunction,

	match: matchExpr,

	attrHandle: {},

	find: {},

	relative: {
		">": { dir: "parentNode", first: true },
		" ": { dir: "parentNode" },
		"+": { dir: "previousSibling", first: true },
		"~": { dir: "previousSibling" }
	},

	preFilter: {
		ATTR: function( match ) {
			match[ 1 ] = match[ 1 ].replace( runescape, funescape );

			// Move the given value to match[3] whether quoted or unquoted
			match[ 3 ] = ( match[ 3 ] || match[ 4 ] || match[ 5 ] || "" )
				.replace( runescape, funescape );

			if ( match[ 2 ] === "~=" ) {
				match[ 3 ] = " " + match[ 3 ] + " ";
			}

			return match.slice( 0, 4 );
		},

		CHILD: function( match ) {

			/* matches from matchExpr["CHILD"]
				1 type (only|nth|...)
				2 what (child|of-type)
				3 argument (even|odd|\d*|\d*n([+-]\d+)?|...)
				4 xn-component of xn+y argument ([+-]?\d*n|)
				5 sign of xn-component
				6 x of xn-component
				7 sign of y-component
				8 y of y-component
			*/
			match[ 1 ] = match[ 1 ].toLowerCase();

			if ( match[ 1 ].slice( 0, 3 ) === "nth" ) {

				// nth-* requires argument
				if ( !match[ 3 ] ) {
					find.error( match[ 0 ] );
				}

				// numeric x and y parameters for Expr.filter.CHILD
				// remember that false/true cast respectively to 0/1
				match[ 4 ] = +( match[ 4 ] ?
					match[ 5 ] + ( match[ 6 ] || 1 ) :
					2 * ( match[ 3 ] === "even" || match[ 3 ] === "odd" )
				);
				match[ 5 ] = +( ( match[ 7 ] + match[ 8 ] ) || match[ 3 ] === "odd" );

			// other types prohibit arguments
			} else if ( match[ 3 ] ) {
				find.error( match[ 0 ] );
			}

			return match;
		},

		PSEUDO: function( match ) {
			var excess,
				unquoted = !match[ 6 ] && match[ 2 ];

			if ( matchExpr.CHILD.test( match[ 0 ] ) ) {
				return null;
			}

			// Accept quoted arguments as-is
			if ( match[ 3 ] ) {
				match[ 2 ] = match[ 4 ] || match[ 5 ] || "";

			// Strip excess characters from unquoted arguments
			} else if ( unquoted && rpseudo.test( unquoted ) &&

				// Get excess from tokenize (recursively)
				( excess = tokenize( unquoted, true ) ) &&

				// advance to the next closing parenthesis
				( excess = unquoted.indexOf( ")", unquoted.length - excess ) - unquoted.length ) ) {

				// excess is a negative index
				match[ 0 ] = match[ 0 ].slice( 0, excess );
				match[ 2 ] = unquoted.slice( 0, excess );
			}

			// Return only captures needed by the pseudo filter method (type and argument)
			return match.slice( 0, 3 );
		}
	},

	filter: {

		TAG: function( nodeNameSelector ) {
			var expectedNodeName = nodeNameSelector.replace( runescape, funescape ).toLowerCase();
			return nodeNameSelector === "*" ?
				function() {
					return true;
				} :
				function( elem ) {
					return nodeName( elem, expectedNodeName );
				};
		},

		CLASS: function( className ) {
			var pattern = classCache[ className + " " ];

			return pattern ||
				( pattern = new RegExp( "(^|" + whitespace + ")" + className +
					"(" + whitespace + "|$)" ) ) &&
				classCache( className, function( elem ) {
					return pattern.test(
						typeof elem.className === "string" && elem.className ||
							typeof elem.getAttribute !== "undefined" &&
								elem.getAttribute( "class" ) ||
							""
					);
				} );
		},

		ATTR: function( name, operator, check ) {
			return function( elem ) {
				var result = find.attr( elem, name );

				if ( result == null ) {
					return operator === "!=";
				}
				if ( !operator ) {
					return true;
				}

				result += "";

				if ( operator === "=" ) {
					return result === check;
				}
				if ( operator === "!=" ) {
					return result !== check;
				}
				if ( operator === "^=" ) {
					return check && result.indexOf( check ) === 0;
				}
				if ( operator === "*=" ) {
					return check && result.indexOf( check ) > -1;
				}
				if ( operator === "$=" ) {
					return check && result.slice( -check.length ) === check;
				}
				if ( operator === "~=" ) {
					return ( " " + result.replace( rwhitespace, " " ) + " " )
						.indexOf( check ) > -1;
				}
				if ( operator === "|=" ) {
					return result === check || result.slice( 0, check.length + 1 ) === check + "-";
				}

				return false;
			};
		},

		CHILD: function( type, what, _argument, first, last ) {
			var simple = type.slice( 0, 3 ) !== "nth",
				forward = type.slice( -4 ) !== "last",
				ofType = what === "of-type";

			return first === 1 && last === 0 ?

				// Shortcut for :nth-*(n)
				function( elem ) {
					return !!elem.parentNode;
				} :

				function( elem, _context, xml ) {
					var cache, outerCache, node, nodeIndex, start,
						dir = simple !== forward ? "nextSibling" : "previousSibling",
						parent = elem.parentNode,
						name = ofType && elem.nodeName.toLowerCase(),
						useCache = !xml && !ofType,
						diff = false;

					if ( parent ) {

						// :(first|last|only)-(child|of-type)
						if ( simple ) {
							while ( dir ) {
								node = elem;
								while ( ( node = node[ dir ] ) ) {
									if ( ofType ?
										nodeName( node, name ) :
										node.nodeType === 1 ) {

										return false;
									}
								}

								// Reverse direction for :only-* (if we haven't yet done so)
								start = dir = type === "only" && !start && "nextSibling";
							}
							return true;
						}

						start = [ forward ? parent.firstChild : parent.lastChild ];

						// non-xml :nth-child(...) stores cache data on `parent`
						if ( forward && useCache ) {

							// Seek `elem` from a previously-cached index
							outerCache = parent[ expando ] || ( parent[ expando ] = {} );
							cache = outerCache[ type ] || [];
							nodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];
							diff = nodeIndex && cache[ 2 ];
							node = nodeIndex && parent.childNodes[ nodeIndex ];

							while ( ( node = ++nodeIndex && node && node[ dir ] ||

								// Fallback to seeking `elem` from the start
								( diff = nodeIndex = 0 ) || start.pop() ) ) {

								// When found, cache indexes on `parent` and break
								if ( node.nodeType === 1 && ++diff && node === elem ) {
									outerCache[ type ] = [ dirruns, nodeIndex, diff ];
									break;
								}
							}

						} else {

							// Use previously-cached element index if available
							if ( useCache ) {
								outerCache = elem[ expando ] || ( elem[ expando ] = {} );
								cache = outerCache[ type ] || [];
								nodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];
								diff = nodeIndex;
							}

							// xml :nth-child(...)
							// or :nth-last-child(...) or :nth(-last)?-of-type(...)
							if ( diff === false ) {

								// Use the same loop as above to seek `elem` from the start
								while ( ( node = ++nodeIndex && node && node[ dir ] ||
									( diff = nodeIndex = 0 ) || start.pop() ) ) {

									if ( ( ofType ?
										nodeName( node, name ) :
										node.nodeType === 1 ) &&
										++diff ) {

										// Cache the index of each encountered element
										if ( useCache ) {
											outerCache = node[ expando ] ||
												( node[ expando ] = {} );
											outerCache[ type ] = [ dirruns, diff ];
										}

										if ( node === elem ) {
											break;
										}
									}
								}
							}
						}

						// Incorporate the offset, then check against cycle size
						diff -= last;
						return diff === first || ( diff % first === 0 && diff / first >= 0 );
					}
				};
		},

		PSEUDO: function( pseudo, argument ) {

			// pseudo-class names are case-insensitive
			// https://www.w3.org/TR/selectors/#pseudo-classes
			// Prioritize by case sensitivity in case custom pseudos are added with uppercase letters
			// Remember that setFilters inherits from pseudos
			var args,
				fn = Expr.pseudos[ pseudo ] || Expr.setFilters[ pseudo.toLowerCase() ] ||
					find.error( "unsupported pseudo: " + pseudo );

			// The user may use createPseudo to indicate that
			// arguments are needed to create the filter function
			// just as jQuery does
			if ( fn[ expando ] ) {
				return fn( argument );
			}

			// But maintain support for old signatures
			if ( fn.length > 1 ) {
				args = [ pseudo, pseudo, "", argument ];
				return Expr.setFilters.hasOwnProperty( pseudo.toLowerCase() ) ?
					markFunction( function( seed, matches ) {
						var idx,
							matched = fn( seed, argument ),
							i = matched.length;
						while ( i-- ) {
							idx = indexOf.call( seed, matched[ i ] );
							seed[ idx ] = !( matches[ idx ] = matched[ i ] );
						}
					} ) :
					function( elem ) {
						return fn( elem, 0, args );
					};
			}

			return fn;
		}
	},

	pseudos: {

		// Potentially complex pseudos
		not: markFunction( function( selector ) {

			// Trim the selector passed to compile
			// to avoid treating leading and trailing
			// spaces as combinators
			var input = [],
				results = [],
				matcher = compile( selector.replace( rtrimCSS, "$1" ) );

			return matcher[ expando ] ?
				markFunction( function( seed, matches, _context, xml ) {
					var elem,
						unmatched = matcher( seed, null, xml, [] ),
						i = seed.length;

					// Match elements unmatched by `matcher`
					while ( i-- ) {
						if ( ( elem = unmatched[ i ] ) ) {
							seed[ i ] = !( matches[ i ] = elem );
						}
					}
				} ) :
				function( elem, _context, xml ) {
					input[ 0 ] = elem;
					matcher( input, null, xml, results );

					// Don't keep the element
					// (see https://github.com/jquery/sizzle/issues/299)
					input[ 0 ] = null;
					return !results.pop();
				};
		} ),

		has: markFunction( function( selector ) {
			return function( elem ) {
				return find( selector, elem ).length > 0;
			};
		} ),

		contains: markFunction( function( text ) {
			text = text.replace( runescape, funescape );
			return function( elem ) {
				return ( elem.textContent || jQuery.text( elem ) ).indexOf( text ) > -1;
			};
		} ),

		// "Whether an element is represented by a :lang() selector
		// is based solely on the element's language value
		// being equal to the identifier C,
		// or beginning with the identifier C immediately followed by "-".
		// The matching of C against the element's language value is performed case-insensitively.
		// The identifier C does not have to be a valid language name."
		// https://www.w3.org/TR/selectors/#lang-pseudo
		lang: markFunction( function( lang ) {

			// lang value must be a valid identifier
			if ( !ridentifier.test( lang || "" ) ) {
				find.error( "unsupported lang: " + lang );
			}
			lang = lang.replace( runescape, funescape ).toLowerCase();
			return function( elem ) {
				var elemLang;
				do {
					if ( ( elemLang = documentIsHTML ?
						elem.lang :
						elem.getAttribute( "xml:lang" ) || elem.getAttribute( "lang" ) ) ) {

						elemLang = elemLang.toLowerCase();
						return elemLang === lang || elemLang.indexOf( lang + "-" ) === 0;
					}
				} while ( ( elem = elem.parentNode ) && elem.nodeType === 1 );
				return false;
			};
		} ),

		// Miscellaneous
		target: function( elem ) {
			var hash = window.location && window.location.hash;
			return hash && hash.slice( 1 ) === elem.id;
		},

		root: function( elem ) {
			return elem === documentElement;
		},

		focus: function( elem ) {
			return elem === safeActiveElement() &&
				document.hasFocus() &&
				!!( elem.type || elem.href || ~elem.tabIndex );
		},

		// Boolean properties
		enabled: createDisabledPseudo( false ),
		disabled: createDisabledPseudo( true ),

		checked: function( elem ) {

			// In CSS3, :checked should return both checked and selected elements
			// https://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
			return ( nodeName( elem, "input" ) && !!elem.checked ) ||
				( nodeName( elem, "option" ) && !!elem.selected );
		},

		selected: function( elem ) {

			// Support: IE <=11+
			// Accessing the selectedIndex property
			// forces the browser to treat the default option as
			// selected when in an optgroup.
			if ( elem.parentNode ) {
				// eslint-disable-next-line no-unused-expressions
				elem.parentNode.selectedIndex;
			}

			return elem.selected === true;
		},

		// Contents
		empty: function( elem ) {

			// https://www.w3.org/TR/selectors/#empty-pseudo
			// :empty is negated by element (1) or content nodes (text: 3; cdata: 4; entity ref: 5),
			//   but not by others (comment: 8; processing instruction: 7; etc.)
			// nodeType < 6 works because attributes (2) do not appear as children
			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
				if ( elem.nodeType < 6 ) {
					return false;
				}
			}
			return true;
		},

		parent: function( elem ) {
			return !Expr.pseudos.empty( elem );
		},

		// Element/input types
		header: function( elem ) {
			return rheader.test( elem.nodeName );
		},

		input: function( elem ) {
			return rinputs.test( elem.nodeName );
		},

		button: function( elem ) {
			return nodeName( elem, "input" ) && elem.type === "button" ||
				nodeName( elem, "button" );
		},

		text: function( elem ) {
			var attr;
			return nodeName( elem, "input" ) && elem.type === "text" &&

				// Support: IE <10 only
				// New HTML5 attribute values (e.g., "search") appear
				// with elem.type === "text"
				( ( attr = elem.getAttribute( "type" ) ) == null ||
					attr.toLowerCase() === "text" );
		},

		// Position-in-collection
		first: createPositionalPseudo( function() {
			return [ 0 ];
		} ),

		last: createPositionalPseudo( function( _matchIndexes, length ) {
			return [ length - 1 ];
		} ),

		eq: createPositionalPseudo( function( _matchIndexes, length, argument ) {
			return [ argument < 0 ? argument + length : argument ];
		} ),

		even: createPositionalPseudo( function( matchIndexes, length ) {
			var i = 0;
			for ( ; i < length; i += 2 ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		} ),

		odd: createPositionalPseudo( function( matchIndexes, length ) {
			var i = 1;
			for ( ; i < length; i += 2 ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		} ),

		lt: createPositionalPseudo( function( matchIndexes, length, argument ) {
			var i;

			if ( argument < 0 ) {
				i = argument + length;
			} else if ( argument > length ) {
				i = length;
			} else {
				i = argument;
			}

			for ( ; --i >= 0; ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		} ),

		gt: createPositionalPseudo( function( matchIndexes, length, argument ) {
			var i = argument < 0 ? argument + length : argument;
			for ( ; ++i < length; ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		} )
	}
};

Expr.pseudos.nth = Expr.pseudos.eq;

// Add button/input type pseudos
for ( i in { radio: true, checkbox: true, file: true, password: true, image: true } ) {
	Expr.pseudos[ i ] = createInputPseudo( i );
}
for ( i in { submit: true, reset: true } ) {
	Expr.pseudos[ i ] = createButtonPseudo( i );
}

// Easy API for creating new setFilters
function setFilters() {}
setFilters.prototype = Expr.filters = Expr.pseudos;
Expr.setFilters = new setFilters();

function tokenize( selector, parseOnly ) {
	var matched, match, tokens, type,
		soFar, groups, preFilters,
		cached = tokenCache[ selector + " " ];

	if ( cached ) {
		return parseOnly ? 0 : cached.slice( 0 );
	}

	soFar = selector;
	groups = [];
	preFilters = Expr.preFilter;

	while ( soFar ) {

		// Comma and first run
		if ( !matched || ( match = rcomma.exec( soFar ) ) ) {
			if ( match ) {

				// Don't consume trailing commas as valid
				soFar = soFar.slice( match[ 0 ].length ) || soFar;
			}
			groups.push( ( tokens = [] ) );
		}

		matched = false;

		// Combinators
		if ( ( match = rleadingCombinator.exec( soFar ) ) ) {
			matched = match.shift();
			tokens.push( {
				value: matched,

				// Cast descendant combinators to space
				type: match[ 0 ].replace( rtrimCSS, " " )
			} );
			soFar = soFar.slice( matched.length );
		}

		// Filters
		for ( type in Expr.filter ) {
			if ( ( match = matchExpr[ type ].exec( soFar ) ) && ( !preFilters[ type ] ||
				( match = preFilters[ type ]( match ) ) ) ) {
				matched = match.shift();
				tokens.push( {
					value: matched,
					type: type,
					matches: match
				} );
				soFar = soFar.slice( matched.length );
			}
		}

		if ( !matched ) {
			break;
		}
	}

	// Return the length of the invalid excess
	// if we're just parsing
	// Otherwise, throw an error or return tokens
	if ( parseOnly ) {
		return soFar.length;
	}

	return soFar ?
		find.error( selector ) :

		// Cache the tokens
		tokenCache( selector, groups ).slice( 0 );
}

function toSelector( tokens ) {
	var i = 0,
		len = tokens.length,
		selector = "";
	for ( ; i < len; i++ ) {
		selector += tokens[ i ].value;
	}
	return selector;
}

function addCombinator( matcher, combinator, base ) {
	var dir = combinator.dir,
		skip = combinator.next,
		key = skip || dir,
		checkNonElements = base && key === "parentNode",
		doneName = done++;

	return combinator.first ?

		// Check against closest ancestor/preceding element
		function( elem, context, xml ) {
			while ( ( elem = elem[ dir ] ) ) {
				if ( elem.nodeType === 1 || checkNonElements ) {
					return matcher( elem, context, xml );
				}
			}
			return false;
		} :

		// Check against all ancestor/preceding elements
		function( elem, context, xml ) {
			var oldCache, outerCache,
				newCache = [ dirruns, doneName ];

			// We can't set arbitrary data on XML nodes, so they don't benefit from combinator caching
			if ( xml ) {
				while ( ( elem = elem[ dir ] ) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						if ( matcher( elem, context, xml ) ) {
							return true;
						}
					}
				}
			} else {
				while ( ( elem = elem[ dir ] ) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						outerCache = elem[ expando ] || ( elem[ expando ] = {} );

						if ( skip && nodeName( elem, skip ) ) {
							elem = elem[ dir ] || elem;
						} else if ( ( oldCache = outerCache[ key ] ) &&
							oldCache[ 0 ] === dirruns && oldCache[ 1 ] === doneName ) {

							// Assign to newCache so results back-propagate to previous elements
							return ( newCache[ 2 ] = oldCache[ 2 ] );
						} else {

							// Reuse newcache so results back-propagate to previous elements
							outerCache[ key ] = newCache;

							// A match means we're done; a fail means we have to keep checking
							if ( ( newCache[ 2 ] = matcher( elem, context, xml ) ) ) {
								return true;
							}
						}
					}
				}
			}
			return false;
		};
}

function elementMatcher( matchers ) {
	return matchers.length > 1 ?
		function( elem, context, xml ) {
			var i = matchers.length;
			while ( i-- ) {
				if ( !matchers[ i ]( elem, context, xml ) ) {
					return false;
				}
			}
			return true;
		} :
		matchers[ 0 ];
}

function multipleContexts( selector, contexts, results ) {
	var i = 0,
		len = contexts.length;
	for ( ; i < len; i++ ) {
		find( selector, contexts[ i ], results );
	}
	return results;
}

function condense( unmatched, map, filter, context, xml ) {
	var elem,
		newUnmatched = [],
		i = 0,
		len = unmatched.length,
		mapped = map != null;

	for ( ; i < len; i++ ) {
		if ( ( elem = unmatched[ i ] ) ) {
			if ( !filter || filter( elem, context, xml ) ) {
				newUnmatched.push( elem );
				if ( mapped ) {
					map.push( i );
				}
			}
		}
	}

	return newUnmatched;
}

function setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {
	if ( postFilter && !postFilter[ expando ] ) {
		postFilter = setMatcher( postFilter );
	}
	if ( postFinder && !postFinder[ expando ] ) {
		postFinder = setMatcher( postFinder, postSelector );
	}
	return markFunction( function( seed, results, context, xml ) {
		var temp, i, elem, matcherOut,
			preMap = [],
			postMap = [],
			preexisting = results.length,

			// Get initial elements from seed or context
			elems = seed ||
				multipleContexts( selector || "*",
					context.nodeType ? [ context ] : context, [] ),

			// Prefilter to get matcher input, preserving a map for seed-results synchronization
			matcherIn = preFilter && ( seed || !selector ) ?
				condense( elems, preMap, preFilter, context, xml ) :
				elems;

		if ( matcher ) {

			// If we have a postFinder, or filtered seed, or non-seed postFilter
			// or preexisting results,
			matcherOut = postFinder || ( seed ? preFilter : preexisting || postFilter ) ?

				// ...intermediate processing is necessary
				[] :

				// ...otherwise use results directly
				results;

			// Find primary matches
			matcher( matcherIn, matcherOut, context, xml );
		} else {
			matcherOut = matcherIn;
		}

		// Apply postFilter
		if ( postFilter ) {
			temp = condense( matcherOut, postMap );
			postFilter( temp, [], context, xml );

			// Un-match failing elements by moving them back to matcherIn
			i = temp.length;
			while ( i-- ) {
				if ( ( elem = temp[ i ] ) ) {
					matcherOut[ postMap[ i ] ] = !( matcherIn[ postMap[ i ] ] = elem );
				}
			}
		}

		if ( seed ) {
			if ( postFinder || preFilter ) {
				if ( postFinder ) {

					// Get the final matcherOut by condensing this intermediate into postFinder contexts
					temp = [];
					i = matcherOut.length;
					while ( i-- ) {
						if ( ( elem = matcherOut[ i ] ) ) {

							// Restore matcherIn since elem is not yet a final match
							temp.push( ( matcherIn[ i ] = elem ) );
						}
					}
					postFinder( null, ( matcherOut = [] ), temp, xml );
				}

				// Move matched elements from seed to results to keep them synchronized
				i = matcherOut.length;
				while ( i-- ) {
					if ( ( elem = matcherOut[ i ] ) &&
						( temp = postFinder ? indexOf.call( seed, elem ) : preMap[ i ] ) > -1 ) {

						seed[ temp ] = !( results[ temp ] = elem );
					}
				}
			}

		// Add elements to results, through postFinder if defined
		} else {
			matcherOut = condense(
				matcherOut === results ?
					matcherOut.splice( preexisting, matcherOut.length ) :
					matcherOut
			);
			if ( postFinder ) {
				postFinder( null, results, matcherOut, xml );
			} else {
				push.apply( results, matcherOut );
			}
		}
	} );
}

function matcherFromTokens( tokens ) {
	var checkContext, matcher, j,
		len = tokens.length,
		leadingRelative = Expr.relative[ tokens[ 0 ].type ],
		implicitRelative = leadingRelative || Expr.relative[ " " ],
		i = leadingRelative ? 1 : 0,

		// The foundational matcher ensures that elements are reachable from top-level context(s)
		matchContext = addCombinator( function( elem ) {
			return elem === checkContext;
		}, implicitRelative, true ),
		matchAnyContext = addCombinator( function( elem ) {
			return indexOf.call( checkContext, elem ) > -1;
		}, implicitRelative, true ),
		matchers = [ function( elem, context, xml ) {

			// Support: IE 11+, Edge 17 - 18+
			// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
			// two documents; shallow comparisons work.
			// eslint-disable-next-line eqeqeq
			var ret = ( !leadingRelative && ( xml || context != outermostContext ) ) || (
				( checkContext = context ).nodeType ?
					matchContext( elem, context, xml ) :
					matchAnyContext( elem, context, xml ) );

			// Avoid hanging onto element
			// (see https://github.com/jquery/sizzle/issues/299)
			checkContext = null;
			return ret;
		} ];

	for ( ; i < len; i++ ) {
		if ( ( matcher = Expr.relative[ tokens[ i ].type ] ) ) {
			matchers = [ addCombinator( elementMatcher( matchers ), matcher ) ];
		} else {
			matcher = Expr.filter[ tokens[ i ].type ].apply( null, tokens[ i ].matches );

			// Return special upon seeing a positional matcher
			if ( matcher[ expando ] ) {

				// Find the next relative operator (if any) for proper handling
				j = ++i;
				for ( ; j < len; j++ ) {
					if ( Expr.relative[ tokens[ j ].type ] ) {
						break;
					}
				}
				return setMatcher(
					i > 1 && elementMatcher( matchers ),
					i > 1 && toSelector(

						// If the preceding token was a descendant combinator, insert an implicit any-element `*`
						tokens.slice( 0, i - 1 )
							.concat( { value: tokens[ i - 2 ].type === " " ? "*" : "" } )
					).replace( rtrimCSS, "$1" ),
					matcher,
					i < j && matcherFromTokens( tokens.slice( i, j ) ),
					j < len && matcherFromTokens( ( tokens = tokens.slice( j ) ) ),
					j < len && toSelector( tokens )
				);
			}
			matchers.push( matcher );
		}
	}

	return elementMatcher( matchers );
}

function matcherFromGroupMatchers( elementMatchers, setMatchers ) {
	var bySet = setMatchers.length > 0,
		byElement = elementMatchers.length > 0,
		superMatcher = function( seed, context, xml, results, outermost ) {
			var elem, j, matcher,
				matchedCount = 0,
				i = "0",
				unmatched = seed && [],
				setMatched = [],
				contextBackup = outermostContext,

				// We must always have either seed elements or outermost context
				elems = seed || byElement && Expr.find.TAG( "*", outermost ),

				// Use integer dirruns iff this is the outermost matcher
				dirrunsUnique = ( dirruns += contextBackup == null ? 1 : Math.random() || 0.1 ),
				len = elems.length;

			if ( outermost ) {

				// Support: IE 11+, Edge 17 - 18+
				// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
				// two documents; shallow comparisons work.
				// eslint-disable-next-line eqeqeq
				outermostContext = context == document || context || outermost;
			}

			// Add elements passing elementMatchers directly to results
			// Support: iOS <=7 - 9 only
			// Tolerate NodeList properties (IE: "length"; Safari: <number>) matching
			// elements by id. (see trac-14142)
			for ( ; i !== len && ( elem = elems[ i ] ) != null; i++ ) {
				if ( byElement && elem ) {
					j = 0;

					// Support: IE 11+, Edge 17 - 18+
					// IE/Edge sometimes throw a "Permission denied" error when strict-comparing
					// two documents; shallow comparisons work.
					// eslint-disable-next-line eqeqeq
					if ( !context && elem.ownerDocument != document ) {
						setDocument( elem );
						xml = !documentIsHTML;
					}
					while ( ( matcher = elementMatchers[ j++ ] ) ) {
						if ( matcher( elem, context || document, xml ) ) {
							push.call( results, elem );
							break;
						}
					}
					if ( outermost ) {
						dirruns = dirrunsUnique;
					}
				}

				// Track unmatched elements for set filters
				if ( bySet ) {

					// They will have gone through all possible matchers
					if ( ( elem = !matcher && elem ) ) {
						matchedCount--;
					}

					// Lengthen the array for every element, matched or not
					if ( seed ) {
						unmatched.push( elem );
					}
				}
			}

			// `i` is now the count of elements visited above, and adding it to `matchedCount`
			// makes the latter nonnegative.
			matchedCount += i;

			// Apply set filters to unmatched elements
			// NOTE: This can be skipped if there are no unmatched elements (i.e., `matchedCount`
			// equals `i`), unless we didn't visit _any_ elements in the above loop because we have
			// no element matchers and no seed.
			// Incrementing an initially-string "0" `i` allows `i` to remain a string only in that
			// case, which will result in a "00" `matchedCount` that differs from `i` but is also
			// numerically zero.
			if ( bySet && i !== matchedCount ) {
				j = 0;
				while ( ( matcher = setMatchers[ j++ ] ) ) {
					matcher( unmatched, setMatched, context, xml );
				}

				if ( seed ) {

					// Reintegrate element matches to eliminate the need for sorting
					if ( matchedCount > 0 ) {
						while ( i-- ) {
							if ( !( unmatched[ i ] || setMatched[ i ] ) ) {
								setMatched[ i ] = pop.call( results );
							}
						}
					}

					// Discard index placeholder values to get only actual matches
					setMatched = condense( setMatched );
				}

				// Add matches to results
				push.apply( results, setMatched );

				// Seedless set matches succeeding multiple successful matchers stipulate sorting
				if ( outermost && !seed && setMatched.length > 0 &&
					( matchedCount + setMatchers.length ) > 1 ) {

					jQuery.uniqueSort( results );
				}
			}

			// Override manipulation of globals by nested matchers
			if ( outermost ) {
				dirruns = dirrunsUnique;
				outermostContext = contextBackup;
			}

			return unmatched;
		};

	return bySet ?
		markFunction( superMatcher ) :
		superMatcher;
}

function compile( selector, match /* Internal Use Only */ ) {
	var i,
		setMatchers = [],
		elementMatchers = [],
		cached = compilerCache[ selector + " " ];

	if ( !cached ) {

		// Generate a function of recursive functions that can be used to check each element
		if ( !match ) {
			match = tokenize( selector );
		}
		i = match.length;
		while ( i-- ) {
			cached = matcherFromTokens( match[ i ] );
			if ( cached[ expando ] ) {
				setMatchers.push( cached );
			} else {
				elementMatchers.push( cached );
			}
		}

		// Cache the compiled function
		cached = compilerCache( selector,
			matcherFromGroupMatchers( elementMatchers, setMatchers ) );

		// Save selector and tokenization
		cached.selector = selector;
	}
	return cached;
}

/**
 * A low-level selection function that works with jQuery's compiled
 *  selector functions
 * @param {String|Function} selector A selector or a pre-compiled
 *  selector function built with jQuery selector compile
 * @param {Element} context
 * @param {Array} [results]
 * @param {Array} [seed] A set of elements to match against
 */
function select( selector, context, results, seed ) {
	var i, tokens, token, type, find,
		compiled = typeof selector === "function" && selector,
		match = !seed && tokenize( ( selector = compiled.selector || selector ) );

	results = results || [];

	// Try to minimize operations if there is only one selector in the list and no seed
	// (the latter of which guarantees us context)
	if ( match.length === 1 ) {

		// Reduce context if the leading compound selector is an ID
		tokens = match[ 0 ] = match[ 0 ].slice( 0 );
		if ( tokens.length > 2 && ( token = tokens[ 0 ] ).type === "ID" &&
				context.nodeType === 9 && documentIsHTML && Expr.relative[ tokens[ 1 ].type ] ) {

			context = ( Expr.find.ID(
				token.matches[ 0 ].replace( runescape, funescape ),
				context
			) || [] )[ 0 ];
			if ( !context ) {
				return results;

			// Precompiled matchers will still verify ancestry, so step up a level
			} else if ( compiled ) {
				context = context.parentNode;
			}

			selector = selector.slice( tokens.shift().value.length );
		}

		// Fetch a seed set for right-to-left matching
		i = matchExpr.needsContext.test( selector ) ? 0 : tokens.length;
		while ( i-- ) {
			token = tokens[ i ];

			// Abort if we hit a combinator
			if ( Expr.relative[ ( type = token.type ) ] ) {
				break;
			}
			if ( ( find = Expr.find[ type ] ) ) {

				// Search, expanding context for leading sibling combinators
				if ( ( seed = find(
					token.matches[ 0 ].replace( runescape, funescape ),
					rsibling.test( tokens[ 0 ].type ) &&
						testContext( context.parentNode ) || context
				) ) ) {

					// If seed is empty or no tokens remain, we can return early
					tokens.splice( i, 1 );
					selector = seed.length && toSelector( tokens );
					if ( !selector ) {
						push.apply( results, seed );
						return results;
					}

					break;
				}
			}
		}
	}

	// Compile and execute a filtering function if one is not provided
	// Provide `match` to avoid retokenization if we modified the selector above
	( compiled || compile( selector, match ) )(
		seed,
		context,
		!documentIsHTML,
		results,
		!context || rsibling.test( selector ) && testContext( context.parentNode ) || context
	);
	return results;
}

// One-time assignments

// Support: Android <=4.0 - 4.1+
// Sort stability
support.sortStable = expando.split( "" ).sort( sortOrder ).join( "" ) === expando;

// Initialize against the default document
setDocument();

// Support: Android <=4.0 - 4.1+
// Detached nodes confoundingly follow *each other*
support.sortDetached = assert( function( el ) {

	// Should return 1, but returns 4 (following)
	return el.compareDocumentPosition( document.createElement( "fieldset" ) ) & 1;
} );

jQuery.find = find;

// Deprecated
jQuery.expr[ ":" ] = jQuery.expr.pseudos;
jQuery.unique = jQuery.uniqueSort;

// These have always been private, but they used to be documented as part of
// Sizzle so let's maintain them for now for backwards compatibility purposes.
find.compile = compile;
find.select = select;
find.setDocument = setDocument;
find.tokenize = tokenize;

find.escape = jQuery.escapeSelector;
find.getText = jQuery.text;
find.isXML = jQuery.isXMLDoc;
find.selectors = jQuery.expr;
find.support = jQuery.support;
find.uniqueSort = jQuery.uniqueSort;

	/* eslint-enable */

} )();


var dir = function( elem, dir, until ) {
	var matched = [],
		truncate = until !== undefined;

	while ( ( elem = elem[ dir ] ) && elem.nodeType !== 9 ) {
		if ( elem.nodeType === 1 ) {
			if ( truncate && jQuery( elem ).is( until ) ) {
				break;
			}
			matched.push( elem );
		}
	}
	return matched;
};


var siblings = function( n, elem ) {
	var matched = [];

	for ( ; n; n = n.nextSibling ) {
		if ( n.nodeType === 1 && n !== elem ) {
			matched.push( n );
		}
	}

	return matched;
};


var rneedsContext = jQuery.expr.match.needsContext;

var rsingleTag = ( /^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i );



// Implement the identical functionality for filter and not
function winnow( elements, qualifier, not ) {
	if ( isFunction( qualifier ) ) {
		return jQuery.grep( elements, function( elem, i ) {
			return !!qualifier.call( elem, i, elem ) !== not;
		} );
	}

	// Single element
	if ( qualifier.nodeType ) {
		return jQuery.grep( elements, function( elem ) {
			return ( elem === qualifier ) !== not;
		} );
	}

	// Arraylike of elements (jQuery, arguments, Array)
	if ( typeof qualifier !== "string" ) {
		return jQuery.grep( elements, function( elem ) {
			return ( indexOf.call( qualifier, elem ) > -1 ) !== not;
		} );
	}

	// Filtered directly for both simple and complex selectors
	return jQuery.filter( qualifier, elements, not );
}

jQuery.filter = function( expr, elems, not ) {
	var elem = elems[ 0 ];

	if ( not ) {
		expr = ":not(" + expr + ")";
	}

	if ( elems.length === 1 && elem.nodeType === 1 ) {
		return jQuery.find.matchesSelector( elem, expr ) ? [ elem ] : [];
	}

	return jQuery.find.matches( expr, jQuery.grep( elems, function( elem ) {
		return elem.nodeType === 1;
	} ) );
};

jQuery.fn.extend( {
	find: function( selector ) {
		var i, ret,
			len = this.length,
			self = this;

		if ( typeof selector !== "string" ) {
			return this.pushStack( jQuery( selector ).filter( function() {
				for ( i = 0; i < len; i++ ) {
					if ( jQuery.contains( self[ i ], this ) ) {
						return true;
					}
				}
			} ) );
		}

		ret = this.pushStack( [] );

		for ( i = 0; i < len; i++ ) {
			jQuery.find( selector, self[ i ], ret );
		}

		return len > 1 ? jQuery.uniqueSort( ret ) : ret;
	},
	filter: function( selector ) {
		return this.pushStack( winnow( this, selector || [], false ) );
	},
	not: function( selector ) {
		return this.pushStack( winnow( this, selector || [], true ) );
	},
	is: function( selector ) {
		return !!winnow(
			this,

			// If this is a positional/relative selector, check membership in the returned set
			// so $("p:first").is("p:last") won't return true for a doc with two "p".
			typeof selector === "string" && rneedsContext.test( selector ) ?
				jQuery( selector ) :
				selector || [],
			false
		).length;
	}
} );


// Initialize a jQuery object


// A central reference to the root jQuery(document)
var rootjQuery,

	// A simple way to check for HTML strings
	// Prioritize #id over <tag> to avoid XSS via location.hash (trac-9521)
	// Strict HTML recognition (trac-11290: must start with <)
	// Shortcut simple #id case for speed
	rquickExpr = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/,

	init = jQuery.fn.init = function( selector, context, root ) {
		var match, elem;

		// HANDLE: $(""), $(null), $(undefined), $(false)
		if ( !selector ) {
			return this;
		}

		// Method init() accepts an alternate rootjQuery
		// so migrate can support jQuery.sub (gh-2101)
		root = root || rootjQuery;

		// Handle HTML strings
		if ( typeof selector === "string" ) {
			if ( selector[ 0 ] === "<" &&
				selector[ selector.length - 1 ] === ">" &&
				selector.length >= 3 ) {

				// Assume that strings that start and end with <> are HTML and skip the regex check
				match = [ null, selector, null ];

			} else {
				match = rquickExpr.exec( selector );
			}

			// Match html or make sure no context is specified for #id
			if ( match && ( match[ 1 ] || !context ) ) {

				// HANDLE: $(html) -> $(array)
				if ( match[ 1 ] ) {
					context = context instanceof jQuery ? context[ 0 ] : context;

					// Option to run scripts is true for back-compat
					// Intentionally let the error be thrown if parseHTML is not present
					jQuery.merge( this, jQuery.parseHTML(
						match[ 1 ],
						context && context.nodeType ? context.ownerDocument || context : document,
						true
					) );

					// HANDLE: $(html, props)
					if ( rsingleTag.test( match[ 1 ] ) && jQuery.isPlainObject( context ) ) {
						for ( match in context ) {

							// Properties of context are called as methods if possible
							if ( isFunction( this[ match ] ) ) {
								this[ match ]( context[ match ] );

							// ...and otherwise set as attributes
							} else {
								this.attr( match, context[ match ] );
							}
						}
					}

					return this;

				// HANDLE: $(#id)
				} else {
					elem = document.getElementById( match[ 2 ] );

					if ( elem ) {

						// Inject the element directly into the jQuery object
						this[ 0 ] = elem;
						this.length = 1;
					}
					return this;
				}

			// HANDLE: $(expr, $(...))
			} else if ( !context || context.jquery ) {
				return ( context || root ).find( selector );

			// HANDLE: $(expr, context)
			// (which is just equivalent to: $(context).find(expr)
			} else {
				return this.constructor( context ).find( selector );
			}

		// HANDLE: $(DOMElement)
		} else if ( selector.nodeType ) {
			this[ 0 ] = selector;
			this.length = 1;
			return this;

		// HANDLE: $(function)
		// Shortcut for document ready
		} else if ( isFunction( selector ) ) {
			return root.ready !== undefined ?
				root.ready( selector ) :

				// Execute immediately if ready is not present
				selector( jQuery );
		}

		return jQuery.makeArray( selector, this );
	};

// Give the init function the jQuery prototype for later instantiation
init.prototype = jQuery.fn;

// Initialize central reference
rootjQuery = jQuery( document );


var rparentsprev = /^(?:parents|prev(?:Until|All))/,

	// Methods guaranteed to produce a unique set when starting from a unique set
	guaranteedUnique = {
		children: true,
		contents: true,
		next: true,
		prev: true
	};

jQuery.fn.extend( {
	has: function( target ) {
		var targets = jQuery( target, this ),
			l = targets.length;

		return this.filter( function() {
			var i = 0;
			for ( ; i < l; i++ ) {
				if ( jQuery.contains( this, targets[ i ] ) ) {
					return true;
				}
			}
		} );
	},

	closest: function( selectors, context ) {
		var cur,
			i = 0,
			l = this.length,
			matched = [],
			targets = typeof selectors !== "string" && jQuery( selectors );

		// Positional selectors never match, since there's no _selection_ context
		if ( !rneedsContext.test( selectors ) ) {
			for ( ; i < l; i++ ) {
				for ( cur = this[ i ]; cur && cur !== context; cur = cur.parentNode ) {

					// Always skip document fragments
					if ( cur.nodeType < 11 && ( targets ?
						targets.index( cur ) > -1 :

						// Don't pass non-elements to jQuery#find
						cur.nodeType === 1 &&
							jQuery.find.matchesSelector( cur, selectors ) ) ) {

						matched.push( cur );
						break;
					}
				}
			}
		}

		return this.pushStack( matched.length > 1 ? jQuery.uniqueSort( matched ) : matched );
	},

	// Determine the position of an element within the set
	index: function( elem ) {

		// No argument, return index in parent
		if ( !elem ) {
			return ( this[ 0 ] && this[ 0 ].parentNode ) ? this.first().prevAll().length : -1;
		}

		// Index in selector
		if ( typeof elem === "string" ) {
			return indexOf.call( jQuery( elem ), this[ 0 ] );
		}

		// Locate the position of the desired element
		return indexOf.call( this,

			// If it receives a jQuery object, the first element is used
			elem.jquery ? elem[ 0 ] : elem
		);
	},

	add: function( selector, context ) {
		return this.pushStack(
			jQuery.uniqueSort(
				jQuery.merge( this.get(), jQuery( selector, context ) )
			)
		);
	},

	addBack: function( selector ) {
		return this.add( selector == null ?
			this.prevObject : this.prevObject.filter( selector )
		);
	}
} );

function sibling( cur, dir ) {
	while ( ( cur = cur[ dir ] ) && cur.nodeType !== 1 ) {}
	return cur;
}

jQuery.each( {
	parent: function( elem ) {
		var parent = elem.parentNode;
		return parent && parent.nodeType !== 11 ? parent : null;
	},
	parents: function( elem ) {
		return dir( elem, "parentNode" );
	},
	parentsUntil: function( elem, _i, until ) {
		return dir( elem, "parentNode", until );
	},
	next: function( elem ) {
		return sibling( elem, "nextSibling" );
	},
	prev: function( elem ) {
		return sibling( elem, "previousSibling" );
	},
	nextAll: function( elem ) {
		return dir( elem, "nextSibling" );
	},
	prevAll: function( elem ) {
		return dir( elem, "previousSibling" );
	},
	nextUntil: function( elem, _i, until ) {
		return dir( elem, "nextSibling", until );
	},
	prevUntil: function( elem, _i, until ) {
		return dir( elem, "previousSibling", until );
	},
	siblings: function( elem ) {
		return siblings( ( elem.parentNode || {} ).firstChild, elem );
	},
	children: function( elem ) {
		return siblings( elem.firstChild );
	},
	contents: function( elem ) {
		if ( elem.contentDocument != null &&

			// Support: IE 11+
			// <object> elements with no `data` attribute has an object
			// `contentDocument` with a `null` prototype.
			getProto( elem.contentDocument ) ) {

			return elem.contentDocument;
		}

		// Support: IE 9 - 11 only, iOS 7 only, Android Browser <=4.3 only
		// Treat the template element as a regular one in browsers that
		// don't support it.
		if ( nodeName( elem, "template" ) ) {
			elem = elem.content || elem;
		}

		return jQuery.merge( [], elem.childNodes );
	}
}, function( name, fn ) {
	jQuery.fn[ name ] = function( until, selector ) {
		var matched = jQuery.map( this, fn, until );

		if ( name.slice( -5 ) !== "Until" ) {
			selector = until;
		}

		if ( selector && typeof selector === "string" ) {
			matched = jQuery.filter( selector, matched );
		}

		if ( this.length > 1 ) {

			// Remove duplicates
			if ( !guaranteedUnique[ name ] ) {
				jQuery.uniqueSort( matched );
			}

			// Reverse order for parents* and prev-derivatives
			if ( rparentsprev.test( name ) ) {
				matched.reverse();
			}
		}

		return this.pushStack( matched );
	};
} );
var rnothtmlwhite = ( /[^\x20\t\r\n\f]+/g );



// Convert String-formatted options into Object-formatted ones
function createOptions( options ) {
	var object = {};
	jQuery.each( options.match( rnothtmlwhite ) || [], function( _, flag ) {
		object[ flag ] = true;
	} );
	return object;
}

/*
 * Create a callback list using the following parameters:
 *
 *	options: an optional list of space-separated options that will change how
 *			the callback list behaves or a more traditional option object
 *
 * By default a callback list will act like an event callback list and can be
 * "fired" multiple times.
 *
 * Possible options:
 *
 *	once:			will ensure the callback list can only be fired once (like a Deferred)
 *
 *	memory:			will keep track of previous values and will call any callback added
 *					after the list has been fired right away with the latest "memorized"
 *					values (like a Deferred)
 *
 *	unique:			will ensure a callback can only be added once (no duplicate in the list)
 *
 *	stopOnFalse:	interrupt callings when a callback returns false
 *
 */
jQuery.Callbacks = function( options ) {

	// Convert options from String-formatted to Object-formatted if needed
	// (we check in cache first)
	options = typeof options === "string" ?
		createOptions( options ) :
		jQuery.extend( {}, options );

	var // Flag to know if list is currently firing
		firing,

		// Last fire value for non-forgettable lists
		memory,

		// Flag to know if list was already fired
		fired,

		// Flag to prevent firing
		locked,

		// Actual callback list
		list = [],

		// Queue of execution data for repeatable lists
		queue = [],

		// Index of currently firing callback (modified by add/remove as needed)
		firingIndex = -1,

		// Fire callbacks
		fire = function() {

			// Enforce single-firing
			locked = locked || options.once;

			// Execute callbacks for all pending executions,
			// respecting firingIndex overrides and runtime changes
			fired = firing = true;
			for ( ; queue.length; firingIndex = -1 ) {
				memory = queue.shift();
				while ( ++firingIndex < list.length ) {

					// Run callback and check for early termination
					if ( list[ firingIndex ].apply( memory[ 0 ], memory[ 1 ] ) === false &&
						options.stopOnFalse ) {

						// Jump to end and forget the data so .add doesn't re-fire
						firingIndex = list.length;
						memory = false;
					}
				}
			}

			// Forget the data if we're done with it
			if ( !options.memory ) {
				memory = false;
			}

			firing = false;

			// Clean up if we're done firing for good
			if ( locked ) {

				// Keep an empty list if we have data for future add calls
				if ( memory ) {
					list = [];

				// Otherwise, this object is spent
				} else {
					list = "";
				}
			}
		},

		// Actual Callbacks object
		self = {

			// Add a callback or a collection of callbacks to the list
			add: function() {
				if ( list ) {

					// If we have memory from a past run, we should fire after adding
					if ( memory && !firing ) {
						firingIndex = list.length - 1;
						queue.push( memory );
					}

					( function add( args ) {
						jQuery.each( args, function( _, arg ) {
							if ( isFunction( arg ) ) {
								if ( !options.unique || !self.has( arg ) ) {
									list.push( arg );
								}
							} else if ( arg && arg.length && toType( arg ) !== "string" ) {

								// Inspect recursively
								add( arg );
							}
						} );
					} )( arguments );

					if ( memory && !firing ) {
						fire();
					}
				}
				return this;
			},

			// Remove a callback from the list
			remove: function() {
				jQuery.each( arguments, function( _, arg ) {
					var index;
					while ( ( index = jQuery.inArray( arg, list, index ) ) > -1 ) {
						list.splice( index, 1 );

						// Handle firing indexes
						if ( index <= firingIndex ) {
							firingIndex--;
						}
					}
				} );
				return this;
			},

			// Check if a given callback is in the list.
			// If no argument is given, return whether or not list has callbacks attached.
			has: function( fn ) {
				return fn ?
					jQuery.inArray( fn, list ) > -1 :
					list.length > 0;
			},

			// Remove all callbacks from the list
			empty: function() {
				if ( list ) {
					list = [];
				}
				return this;
			},

			// Disable .fire and .add
			// Abort any current/pending executions
			// Clear all callbacks and values
			disable: function() {
				locked = queue = [];
				list = memory = "";
				return this;
			},
			disabled: function() {
				return !list;
			},

			// Disable .fire
			// Also disable .add unless we have memory (since it would have no effect)
			// Abort any pending executions
			lock: function() {
				locked = queue = [];
				if ( !memory && !firing ) {
					list = memory = "";
				}
				return this;
			},
			locked: function() {
				return !!locked;
			},

			// Call all callbacks with the given context and arguments
			fireWith: function( context, args ) {
				if ( !locked ) {
					args = args || [];
					args = [ context, args.slice ? args.slice() : args ];
					queue.push( args );
					if ( !firing ) {
						fire();
					}
				}
				return this;
			},

			// Call all the callbacks with the given arguments
			fire: function() {
				self.fireWith( this, arguments );
				return this;
			},

			// To know if the callbacks have already been called at least once
			fired: function() {
				return !!fired;
			}
		};

	return self;
};


function Identity( v ) {
	return v;
}
function Thrower( ex ) {
	throw ex;
}

function adoptValue( value, resolve, reject, noValue ) {
	var method;

	try {

		// Check for promise aspect first to privilege synchronous behavior
		if ( value && isFunction( ( method = value.promise ) ) ) {
			method.call( value ).done( resolve ).fail( reject );

		// Other thenables
		} else if ( value && isFunction( ( method = value.then ) ) ) {
			method.call( value, resolve, reject );

		// Other non-thenables
		} else {

			// Control `resolve` arguments by letting Array#slice cast boolean `noValue` to integer:
			// * false: [ value ].slice( 0 ) => resolve( value )
			// * true: [ value ].slice( 1 ) => resolve()
			resolve.apply( undefined, [ value ].slice( noValue ) );
		}

	// For Promises/A+, convert exceptions into rejections
	// Since jQuery.when doesn't unwrap thenables, we can skip the extra checks appearing in
	// Deferred#then to conditionally suppress rejection.
	} catch ( value ) {

		// Support: Android 4.0 only
		// Strict mode functions invoked without .call/.apply get global-object context
		reject.apply( undefined, [ value ] );
	}
}

jQuery.extend( {

	Deferred: function( func ) {
		var tuples = [

				// action, add listener, callbacks,
				// ... .then handlers, argument index, [final state]
				[ "notify", "progress", jQuery.Callbacks( "memory" ),
					jQuery.Callbacks( "memory" ), 2 ],
				[ "resolve", "done", jQuery.Callbacks( "once memory" ),
					jQuery.Callbacks( "once memory" ), 0, "resolved" ],
				[ "reject", "fail", jQuery.Callbacks( "once memory" ),
					jQuery.Callbacks( "once memory" ), 1, "rejected" ]
			],
			state = "pending",
			promise = {
				state: function() {
					return state;
				},
				always: function() {
					deferred.done( arguments ).fail( arguments );
					return this;
				},
				"catch": function( fn ) {
					return promise.then( null, fn );
				},

				// Keep pipe for back-compat
				pipe: function( /* fnDone, fnFail, fnProgress */ ) {
					var fns = arguments;

					return jQuery.Deferred( function( newDefer ) {
						jQuery.each( tuples, function( _i, tuple ) {

							// Map tuples (progress, done, fail) to arguments (done, fail, progress)
							var fn = isFunction( fns[ tuple[ 4 ] ] ) && fns[ tuple[ 4 ] ];

							// deferred.progress(function() { bind to newDefer or newDefer.notify })
							// deferred.done(function() { bind to newDefer or newDefer.resolve })
							// deferred.fail(function() { bind to newDefer or newDefer.reject })
							deferred[ tuple[ 1 ] ]( function() {
								var returned = fn && fn.apply( this, arguments );
								if ( returned && isFunction( returned.promise ) ) {
									returned.promise()
										.progress( newDefer.notify )
										.done( newDefer.resolve )
										.fail( newDefer.reject );
								} else {
									newDefer[ tuple[ 0 ] + "With" ](
										this,
										fn ? [ returned ] : arguments
									);
								}
							} );
						} );
						fns = null;
					} ).promise();
				},
				then: function( onFulfilled, onRejected, onProgress ) {
					var maxDepth = 0;
					function resolve( depth, deferred, handler, special ) {
						return function() {
							var that = this,
								args = arguments,
								mightThrow = function() {
									var returned, then;

									// Support: Promises/A+ section 2.3.3.3.3
									// https://promisesaplus.com/#point-59
									// Ignore double-resolution attempts
									if ( depth < maxDepth ) {
										return;
									}

									returned = handler.apply( that, args );

									// Support: Promises/A+ section 2.3.1
									// https://promisesaplus.com/#point-48
									if ( returned === deferred.promise() ) {
										throw new TypeError( "Thenable self-resolution" );
									}

									// Support: Promises/A+ sections 2.3.3.1, 3.5
									// https://promisesaplus.com/#point-54
									// https://promisesaplus.com/#point-75
									// Retrieve `then` only once
									then = returned &&

										// Support: Promises/A+ section 2.3.4
										// https://promisesaplus.com/#point-64
										// Only check objects and functions for thenability
										( typeof returned === "object" ||
											typeof returned === "function" ) &&
										returned.then;

									// Handle a returned thenable
									if ( isFunction( then ) ) {

										// Special processors (notify) just wait for resolution
										if ( special ) {
											then.call(
												returned,
												resolve( maxDepth, deferred, Identity, special ),
												resolve( maxDepth, deferred, Thrower, special )
											);

										// Normal processors (resolve) also hook into progress
										} else {

											// ...and disregard older resolution values
											maxDepth++;

											then.call(
												returned,
												resolve( maxDepth, deferred, Identity, special ),
												resolve( maxDepth, deferred, Thrower, special ),
												resolve( maxDepth, deferred, Identity,
													deferred.notifyWith )
											);
										}

									// Handle all other returned values
									} else {

										// Only substitute handlers pass on context
										// and multiple values (non-spec behavior)
										if ( handler !== Identity ) {
											that = undefined;
											args = [ returned ];
										}

										// Process the value(s)
										// Default process is resolve
										( special || deferred.resolveWith )( that, args );
									}
								},

								// Only normal processors (resolve) catch and reject exceptions
								process = special ?
									mightThrow :
									function() {
										try {
											mightThrow();
										} catch ( e ) {

											if ( jQuery.Deferred.exceptionHook ) {
												jQuery.Deferred.exceptionHook( e,
													process.error );
											}

											// Support: Promises/A+ section 2.3.3.3.4.1
											// https://promisesaplus.com/#point-61
											// Ignore post-resolution exceptions
											if ( depth + 1 >= maxDepth ) {

												// Only substitute handlers pass on context
												// and multiple values (non-spec behavior)
												if ( handler !== Thrower ) {
													that = undefined;
													args = [ e ];
												}

												deferred.rejectWith( that, args );
											}
										}
									};

							// Support: Promises/A+ section 2.3.3.3.1
							// https://promisesaplus.com/#point-57
							// Re-resolve promises immediately to dodge false rejection from
							// subsequent errors
							if ( depth ) {
								process();
							} else {

								// Call an optional hook to record the error, in case of exception
								// since it's otherwise lost when execution goes async
								if ( jQuery.Deferred.getErrorHook ) {
									process.error = jQuery.Deferred.getErrorHook();

								// The deprecated alias of the above. While the name suggests
								// returning the stack, not an error instance, jQuery just passes
								// it directly to `console.warn` so both will work; an instance
								// just better cooperates with source maps.
								} else if ( jQuery.Deferred.getStackHook ) {
									process.error = jQuery.Deferred.getStackHook();
								}
								window.setTimeout( process );
							}
						};
					}

					return jQuery.Deferred( function( newDefer ) {

						// progress_handlers.add( ... )
						tuples[ 0 ][ 3 ].add(
							resolve(
								0,
								newDefer,
								isFunction( onProgress ) ?
									onProgress :
									Identity,
								newDefer.notifyWith
							)
						);

						// fulfilled_handlers.add( ... )
						tuples[ 1 ][ 3 ].add(
							resolve(
								0,
								newDefer,
								isFunction( onFulfilled ) ?
									onFulfilled :
									Identity
							)
						);

						// rejected_handlers.add( ... )
						tuples[ 2 ][ 3 ].add(
							resolve(
								0,
								newDefer,
								isFunction( onRejected ) ?
									onRejected :
									Thrower
							)
						);
					} ).promise();
				},

				// Get a promise for this deferred
				// If obj is provided, the promise aspect is added to the object
				promise: function( obj ) {
					return obj != null ? jQuery.extend( obj, promise ) : promise;
				}
			},
			deferred = {};

		// Add list-specific methods
		jQuery.each( tuples, function( i, tuple ) {
			var list = tuple[ 2 ],
				stateString = tuple[ 5 ];

			// promise.progress = list.add
			// promise.done = list.add
			// promise.fail = list.add
			promise[ tuple[ 1 ] ] = list.add;

			// Handle state
			if ( stateString ) {
				list.add(
					function() {

						// state = "resolved" (i.e., fulfilled)
						// state = "rejected"
						state = stateString;
					},

					// rejected_callbacks.disable
					// fulfilled_callbacks.disable
					tuples[ 3 - i ][ 2 ].disable,

					// rejected_handlers.disable
					// fulfilled_handlers.disable
					tuples[ 3 - i ][ 3 ].disable,

					// progress_callbacks.lock
					tuples[ 0 ][ 2 ].lock,

					// progress_handlers.lock
					tuples[ 0 ][ 3 ].lock
				);
			}

			// progress_handlers.fire
			// fulfilled_handlers.fire
			// rejected_handlers.fire
			list.add( tuple[ 3 ].fire );

			// deferred.notify = function() { deferred.notifyWith(...) }
			// deferred.resolve = function() { deferred.resolveWith(...) }
			// deferred.reject = function() { deferred.rejectWith(...) }
			deferred[ tuple[ 0 ] ] = function() {
				deferred[ tuple[ 0 ] + "With" ]( this === deferred ? undefined : this, arguments );
				return this;
			};

			// deferred.notifyWith = list.fireWith
			// deferred.resolveWith = list.fireWith
			// deferred.rejectWith = list.fireWith
			deferred[ tuple[ 0 ] + "With" ] = list.fireWith;
		} );

		// Make the deferred a promise
		promise.promise( deferred );

		// Call given func if any
		if ( func ) {
			func.call( deferred, deferred );
		}

		// All done!
		return deferred;
	},

	// Deferred helper
	when: function( singleValue ) {
		var

			// count of uncompleted subordinates
			remaining = arguments.length,

			// count of unprocessed arguments
			i = remaining,

			// subordinate fulfillment data
			resolveContexts = Array( i ),
			resolveValues = slice.call( arguments ),

			// the primary Deferred
			primary = jQuery.Deferred(),

			// subordinate callback factory
			updateFunc = function( i ) {
				return function( value ) {
					resolveContexts[ i ] = this;
					resolveValues[ i ] = arguments.length > 1 ? slice.call( arguments ) : value;
					if ( !( --remaining ) ) {
						primary.resolveWith( resolveContexts, resolveValues );
					}
				};
			};

		// Single- and empty arguments are adopted like Promise.resolve
		if ( remaining <= 1 ) {
			adoptValue( singleValue, primary.done( updateFunc( i ) ).resolve, primary.reject,
				!remaining );

			// Use .then() to unwrap secondary thenables (cf. gh-3000)
			if ( primary.state() === "pending" ||
				isFunction( resolveValues[ i ] && resolveValues[ i ].then ) ) {

				return primary.then();
			}
		}

		// Multiple arguments are aggregated like Promise.all array elements
		while ( i-- ) {
			adoptValue( resolveValues[ i ], updateFunc( i ), primary.reject );
		}

		return primary.promise();
	}
} );


// These usually indicate a programmer mistake during development,
// warn about them ASAP rather than swallowing them by default.
var rerrorNames = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;

// If `jQuery.Deferred.getErrorHook` is defined, `asyncError` is an error
// captured before the async barrier to get the original error cause
// which may otherwise be hidden.
jQuery.Deferred.exceptionHook = function( error, asyncError ) {

	// Support: IE 8 - 9 only
	// Console exists when dev tools are open, which can happen at any time
	if ( window.console && window.console.warn && error && rerrorNames.test( error.name ) ) {
		window.console.warn( "jQuery.Deferred exception: " + error.message,
			error.stack, asyncError );
	}
};




jQuery.readyException = function( error ) {
	window.setTimeout( function() {
		throw error;
	} );
};




// The deferred used on DOM ready
var readyList = jQuery.Deferred();

jQuery.fn.ready = function( fn ) {

	readyList
		.then( fn )

		// Wrap jQuery.readyException in a function so that the lookup
		// happens at the time of error handling instead of callback
		// registration.
		.catch( function( error ) {
			jQuery.readyException( error );
		} );

	return this;
};

jQuery.extend( {

	// Is the DOM ready to be used? Set to true once it occurs.
	isReady: false,

	// A counter to track how many items to wait for before
	// the ready event fires. See trac-6781
	readyWait: 1,

	// Handle when the DOM is ready
	ready: function( wait ) {

		// Abort if there are pending holds or we're already ready
		if ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {
			return;
		}

		// Remember that the DOM is ready
		jQuery.isReady = true;

		// If a normal DOM Ready event fired, decrement, and wait if need be
		if ( wait !== true && --jQuery.readyWait > 0 ) {
			return;
		}

		// If there are functions bound, to execute
		readyList.resolveWith( document, [ jQuery ] );
	}
} );

jQuery.ready.then = readyList.then;

// The ready event handler and self cleanup method
function completed() {
	document.removeEventListener( "DOMContentLoaded", completed );
	window.removeEventListener( "load", completed );
	jQuery.ready();
}

// Catch cases where $(document).ready() is called
// after the browser event has already occurred.
// Support: IE <=9 - 10 only
// Older IE sometimes signals "interactive" too soon
if ( document.readyState === "complete" ||
	( document.readyState !== "loading" && !document.documentElement.doScroll ) ) {

	// Handle it asynchronously to allow scripts the opportunity to delay ready
	window.setTimeout( jQuery.ready );

} else {

	// Use the handy event callback
	document.addEventListener( "DOMContentLoaded", completed );

	// A fallback to window.onload, that will always work
	window.addEventListener( "load", completed );
}




// Multifunctional method to get and set values of a collection
// The value/s can optionally be executed if it's a function
var access = function( elems, fn, key, value, chainable, emptyGet, raw ) {
	var i = 0,
		len = elems.length,
		bulk = key == null;

	// Sets many values
	if ( toType( key ) === "object" ) {
		chainable = true;
		for ( i in key ) {
			access( elems, fn, i, key[ i ], true, emptyGet, raw );
		}

	// Sets one value
	} else if ( value !== undefined ) {
		chainable = true;

		if ( !isFunction( value ) ) {
			raw = true;
		}

		if ( bulk ) {

			// Bulk operations run against the entire set
			if ( raw ) {
				fn.call( elems, value );
				fn = null;

			// ...except when executing function values
			} else {
				bulk = fn;
				fn = function( elem, _key, value ) {
					return bulk.call( jQuery( elem ), value );
				};
			}
		}

		if ( fn ) {
			for ( ; i < len; i++ ) {
				fn(
					elems[ i ], key, raw ?
						value :
						value.call( elems[ i ], i, fn( elems[ i ], key ) )
				);
			}
		}
	}

	if ( chainable ) {
		return elems;
	}

	// Gets
	if ( bulk ) {
		return fn.call( elems );
	}

	return len ? fn( elems[ 0 ], key ) : emptyGet;
};


// Matches dashed string for camelizing
var rmsPrefix = /^-ms-/,
	rdashAlpha = /-([a-z])/g;

// Used by camelCase as callback to replace()
function fcamelCase( _all, letter ) {
	return letter.toUpperCase();
}

// Convert dashed to camelCase; used by the css and data modules
// Support: IE <=9 - 11, Edge 12 - 15
// Microsoft forgot to hump their vendor prefix (trac-9572)
function camelCase( string ) {
	return string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );
}
var acceptData = function( owner ) {

	// Accepts only:
	//  - Node
	//    - Node.ELEMENT_NODE
	//    - Node.DOCUMENT_NODE
	//  - Object
	//    - Any
	return owner.nodeType === 1 || owner.nodeType === 9 || !( +owner.nodeType );
};




function Data() {
	this.expando = jQuery.expando + Data.uid++;
}

Data.uid = 1;

Data.prototype = {

	cache: function( owner ) {

		// Check if the owner object already has a cache
		var value = owner[ this.expando ];

		// If not, create one
		if ( !value ) {
			value = {};

			// We can accept data for non-element nodes in modern browsers,
			// but we should not, see trac-8335.
			// Always return an empty object.
			if ( acceptData( owner ) ) {

				// If it is a node unlikely to be stringify-ed or looped over
				// use plain assignment
				if ( owner.nodeType ) {
					owner[ this.expando ] = value;

				// Otherwise secure it in a non-enumerable property
				// configurable must be true to allow the property to be
				// deleted when data is removed
				} else {
					Object.defineProperty( owner, this.expando, {
						value: value,
						configurable: true
					} );
				}
			}
		}

		return value;
	},
	set: function( owner, data, value ) {
		var prop,
			cache = this.cache( owner );

		// Handle: [ owner, key, value ] args
		// Always use camelCase key (gh-2257)
		if ( typeof data === "string" ) {
			cache[ camelCase( data ) ] = value;

		// Handle: [ owner, { properties } ] args
		} else {

			// Copy the properties one-by-one to the cache object
			for ( prop in data ) {
				cache[ camelCase( prop ) ] = data[ prop ];
			}
		}
		return cache;
	},
	get: function( owner, key ) {
		return key === undefined ?
			this.cache( owner ) :

			// Always use camelCase key (gh-2257)
			owner[ this.expando ] && owner[ this.expando ][ camelCase( key ) ];
	},
	access: function( owner, key, value ) {

		// In cases where either:
		//
		//   1. No key was specified
		//   2. A string key was specified, but no value provided
		//
		// Take the "read" path and allow the get method to determine
		// which value to return, respectively either:
		//
		//   1. The entire cache object
		//   2. The data stored at the key
		//
		if ( key === undefined ||
				( ( key && typeof key === "string" ) && value === undefined ) ) {

			return this.get( owner, key );
		}

		// When the key is not a string, or both a key and value
		// are specified, set or extend (existing objects) with either:
		//
		//   1. An object of properties
		//   2. A key and value
		//
		this.set( owner, key, value );

		// Since the "set" path can have two possible entry points
		// return the expected data based on which path was taken[*]
		return value !== undefined ? value : key;
	},
	remove: function( owner, key ) {
		var i,
			cache = owner[ this.expando ];

		if ( cache === undefined ) {
			return;
		}

		if ( key !== undefined ) {

			// Support array or space separated string of keys
			if ( Array.isArray( key ) ) {

				// If key is an array of keys...
				// We always set camelCase keys, so remove that.
				key = key.map( camelCase );
			} else {
				key = camelCase( key );

				// If a key with the spaces exists, use it.
				// Otherwise, create an array by matching non-whitespace
				key = key in cache ?
					[ key ] :
					( key.match( rnothtmlwhite ) || [] );
			}

			i = key.length;

			while ( i-- ) {
				delete cache[ key[ i ] ];
			}
		}

		// Remove the expando if there's no more data
		if ( key === undefined || jQuery.isEmptyObject( cache ) ) {

			// Support: Chrome <=35 - 45
			// Webkit & Blink performance suffers when deleting properties
			// from DOM nodes, so set to undefined instead
			// https://bugs.chromium.org/p/chromium/issues/detail?id=378607 (bug restricted)
			if ( owner.nodeType ) {
				owner[ this.expando ] = undefined;
			} else {
				delete owner[ this.expando ];
			}
		}
	},
	hasData: function( owner ) {
		var cache = owner[ this.expando ];
		return cache !== undefined && !jQuery.isEmptyObject( cache );
	}
};
var dataPriv = new Data();

var dataUser = new Data();



//	Implementation Summary
//
//	1. Enforce API surface and semantic compatibility with 1.9.x branch
//	2. Improve the module's maintainability by reducing the storage
//		paths to a single mechanism.
//	3. Use the same single mechanism to support "private" and "user" data.
//	4. _Never_ expose "private" data to user code (TODO: Drop _data, _removeData)
//	5. Avoid exposing implementation details on user objects (eg. expando properties)
//	6. Provide a clear path for implementation upgrade to WeakMap in 2014

var rbrace = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/,
	rmultiDash = /[A-Z]/g;

function getData( data ) {
	if ( data === "true" ) {
		return true;
	}

	if ( data === "false" ) {
		return false;
	}

	if ( data === "null" ) {
		return null;
	}

	// Only convert to a number if it doesn't change the string
	if ( data === +data + "" ) {
		return +data;
	}

	if ( rbrace.test( data ) ) {
		return JSON.parse( data );
	}

	return data;
}

function dataAttr( elem, key, data ) {
	var name;

	// If nothing was found internally, try to fetch any
	// data from the HTML5 data-* attribute
	if ( data === undefined && elem.nodeType === 1 ) {
		name = "data-" + key.replace( rmultiDash, "-$&" ).toLowerCase();
		data = elem.getAttribute( name );

		if ( typeof data === "string" ) {
			try {
				data = getData( data );
			} catch ( e ) {}

			// Make sure we set the data so it isn't changed later
			dataUser.set( elem, key, data );
		} else {
			data = undefined;
		}
	}
	return data;
}

jQuery.extend( {
	hasData: function( elem ) {
		return dataUser.hasData( elem ) || dataPriv.hasData( elem );
	},

	data: function( elem, name, data ) {
		return dataUser.access( elem, name, data );
	},

	removeData: function( elem, name ) {
		dataUser.remove( elem, name );
	},

	// TODO: Now that all calls to _data and _removeData have been replaced
	// with direct calls to dataPriv methods, these can be deprecated.
	_data: function( elem, name, data ) {
		return dataPriv.access( elem, name, data );
	},

	_removeData: function( elem, name ) {
		dataPriv.remove( elem, name );
	}
} );

jQuery.fn.extend( {
	data: function( key, value ) {
		var i, name, data,
			elem = this[ 0 ],
			attrs = elem && elem.attributes;

		// Gets all values
		if ( key === undefined ) {
			if ( this.length ) {
				data = dataUser.get( elem );

				if ( elem.nodeType === 1 && !dataPriv.get( elem, "hasDataAttrs" ) ) {
					i = attrs.length;
					while ( i-- ) {

						// Support: IE 11 only
						// The attrs elements can be null (trac-14894)
						if ( attrs[ i ] ) {
							name = attrs[ i ].name;
							if ( name.indexOf( "data-" ) === 0 ) {
								name = camelCase( name.slice( 5 ) );
								dataAttr( elem, name, data[ name ] );
							}
						}
					}
					dataPriv.set( elem, "hasDataAttrs", true );
				}
			}

			return data;
		}

		// Sets multiple values
		if ( typeof key === "object" ) {
			return this.each( function() {
				dataUser.set( this, key );
			} );
		}

		return access( this, function( value ) {
			var data;

			// The calling jQuery object (element matches) is not empty
			// (and therefore has an element appears at this[ 0 ]) and the
			// `value` parameter was not undefined. An empty jQuery object
			// will result in `undefined` for elem = this[ 0 ] which will
			// throw an exception if an attempt to read a data cache is made.
			if ( elem && value === undefined ) {

				// Attempt to get data from the cache
				// The key will always be camelCased in Data
				data = dataUser.get( elem, key );
				if ( data !== undefined ) {
					return data;
				}

				// Attempt to "discover" the data in
				// HTML5 custom data-* attrs
				data = dataAttr( elem, key );
				if ( data !== undefined ) {
					return data;
				}

				// We tried really hard, but the data doesn't exist.
				return;
			}

			// Set the data...
			this.each( function() {

				// We always store the camelCased key
				dataUser.set( this, key, value );
			} );
		}, null, value, arguments.length > 1, null, true );
	},

	removeData: function( key ) {
		return this.each( function() {
			dataUser.remove( this, key );
		} );
	}
} );


jQuery.extend( {
	queue: function( elem, type, data ) {
		var queue;

		if ( elem ) {
			type = ( type || "fx" ) + "queue";
			queue = dataPriv.get( elem, type );

			// Speed up dequeue by getting out quickly if this is just a lookup
			if ( data ) {
				if ( !queue || Array.isArray( data ) ) {
					queue = dataPriv.access( elem, type, jQuery.makeArray( data ) );
				} else {
					queue.push( data );
				}
			}
			return queue || [];
		}
	},

	dequeue: function( elem, type ) {
		type = type || "fx";

		var queue = jQuery.queue( elem, type ),
			startLength = queue.length,
			fn = queue.shift(),
			hooks = jQuery._queueHooks( elem, type ),
			next = function() {
				jQuery.dequeue( elem, type );
			};

		// If the fx queue is dequeued, always remove the progress sentinel
		if ( fn === "inprogress" ) {
			fn = queue.shift();
			startLength--;
		}

		if ( fn ) {

			// Add a progress sentinel to prevent the fx queue from being
			// automatically dequeued
			if ( type === "fx" ) {
				queue.unshift( "inprogress" );
			}

			// Clear up the last queue stop function
			delete hooks.stop;
			fn.call( elem, next, hooks );
		}

		if ( !startLength && hooks ) {
			hooks.empty.fire();
		}
	},

	// Not public - generate a queueHooks object, or return the current one
	_queueHooks: function( elem, type ) {
		var key = type + "queueHooks";
		return dataPriv.get( elem, key ) || dataPriv.access( elem, key, {
			empty: jQuery.Callbacks( "once memory" ).add( function() {
				dataPriv.remove( elem, [ type + "queue", key ] );
			} )
		} );
	}
} );

jQuery.fn.extend( {
	queue: function( type, data ) {
		var setter = 2;

		if ( typeof type !== "string" ) {
			data = type;
			type = "fx";
			setter--;
		}

		if ( arguments.length < setter ) {
			return jQuery.queue( this[ 0 ], type );
		}

		return data === undefined ?
			this :
			this.each( function() {
				var queue = jQuery.queue( this, type, data );

				// Ensure a hooks for this queue
				jQuery._queueHooks( this, type );

				if ( type === "fx" && queue[ 0 ] !== "inprogress" ) {
					jQuery.dequeue( this, type );
				}
			} );
	},
	dequeue: function( type ) {
		return this.each( function() {
			jQuery.dequeue( this, type );
		} );
	},
	clearQueue: function( type ) {
		return this.queue( type || "fx", [] );
	},

	// Get a promise resolved when queues of a certain type
	// are emptied (fx is the type by default)
	promise: function( type, obj ) {
		var tmp,
			count = 1,
			defer = jQuery.Deferred(),
			elements = this,
			i = this.length,
			resolve = function() {
				if ( !( --count ) ) {
					defer.resolveWith( elements, [ elements ] );
				}
			};

		if ( typeof type !== "string" ) {
			obj = type;
			type = undefined;
		}
		type = type || "fx";

		while ( i-- ) {
			tmp = dataPriv.get( elements[ i ], type + "queueHooks" );
			if ( tmp && tmp.empty ) {
				count++;
				tmp.empty.add( resolve );
			}
		}
		resolve();
		return defer.promise( obj );
	}
} );
var pnum = ( /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/ ).source;

var rcssNum = new RegExp( "^(?:([+-])=|)(" + pnum + ")([a-z%]*)$", "i" );


var cssExpand = [ "Top", "Right", "Bottom", "Left" ];

var documentElement = document.documentElement;



	var isAttached = function( elem ) {
			return jQuery.contains( elem.ownerDocument, elem );
		},
		composed = { composed: true };

	// Support: IE 9 - 11+, Edge 12 - 18+, iOS 10.0 - 10.2 only
	// Check attachment across shadow DOM boundaries when possible (gh-3504)
	// Support: iOS 10.0-10.2 only
	// Early iOS 10 versions support `attachShadow` but not `getRootNode`,
	// leading to errors. We need to check for `getRootNode`.
	if ( documentElement.getRootNode ) {
		isAttached = function( elem ) {
			return jQuery.contains( elem.ownerDocument, elem ) ||
				elem.getRootNode( composed ) === elem.ownerDocument;
		};
	}
var isHiddenWithinTree = function( elem, el ) {

		// isHiddenWithinTree might be called from jQuery#filter function;
		// in that case, element will be second argument
		elem = el || elem;

		// Inline style trumps all
		return elem.style.display === "none" ||
			elem.style.display === "" &&

			// Otherwise, check computed style
			// Support: Firefox <=43 - 45
			// Disconnected elements can have computed display: none, so first confirm that elem is
			// in the document.
			isAttached( elem ) &&

			jQuery.css( elem, "display" ) === "none";
	};



function adjustCSS( elem, prop, valueParts, tween ) {
	var adjusted, scale,
		maxIterations = 20,
		currentValue = tween ?
			function() {
				return tween.cur();
			} :
			function() {
				return jQuery.css( elem, prop, "" );
			},
		initial = currentValue(),
		unit = valueParts && valueParts[ 3 ] || ( jQuery.cssNumber[ prop ] ? "" : "px" ),

		// Starting value computation is required for potential unit mismatches
		initialInUnit = elem.nodeType &&
			( jQuery.cssNumber[ prop ] || unit !== "px" && +initial ) &&
			rcssNum.exec( jQuery.css( elem, prop ) );

	if ( initialInUnit && initialInUnit[ 3 ] !== unit ) {

		// Support: Firefox <=54
		// Halve the iteration target value to prevent interference from CSS upper bounds (gh-2144)
		initial = initial / 2;

		// Trust units reported by jQuery.css
		unit = unit || initialInUnit[ 3 ];

		// Iteratively approximate from a nonzero starting point
		initialInUnit = +initial || 1;

		while ( maxIterations-- ) {

			// Evaluate and update our best guess (doubling guesses that zero out).
			// Finish if the scale equals or crosses 1 (making the old*new product non-positive).
			jQuery.style( elem, prop, initialInUnit + unit );
			if ( ( 1 - scale ) * ( 1 - ( scale = currentValue() / initial || 0.5 ) ) <= 0 ) {
				maxIterations = 0;
			}
			initialInUnit = initialInUnit / scale;

		}

		initialInUnit = initialInUnit * 2;
		jQuery.style( elem, prop, initialInUnit + unit );

		// Make sure we update the tween properties later on
		valueParts = valueParts || [];
	}

	if ( valueParts ) {
		initialInUnit = +initialInUnit || +initial || 0;

		// Apply relative offset (+=/-=) if specified
		adjusted = valueParts[ 1 ] ?
			initialInUnit + ( valueParts[ 1 ] + 1 ) * valueParts[ 2 ] :
			+valueParts[ 2 ];
		if ( tween ) {
			tween.unit = unit;
			tween.start = initialInUnit;
			tween.end = adjusted;
		}
	}
	return adjusted;
}


var defaultDisplayMap = {};

function getDefaultDisplay( elem ) {
	var temp,
		doc = elem.ownerDocument,
		nodeName = elem.nodeName,
		display = defaultDisplayMap[ nodeName ];

	if ( display ) {
		return display;
	}

	temp = doc.body.appendChild( doc.createElement( nodeName ) );
	display = jQuery.css( temp, "display" );

	temp.parentNode.removeChild( temp );

	if ( display === "none" ) {
		display = "block";
	}
	defaultDisplayMap[ nodeName ] = display;

	return display;
}

function showHide( elements, show ) {
	var display, elem,
		values = [],
		index = 0,
		length = elements.length;

	// Determine new display value for elements that need to change
	for ( ; index < length; index++ ) {
		elem = elements[ index ];
		if ( !elem.style ) {
			continue;
		}

		display = elem.style.display;
		if ( show ) {

			// Since we force visibility upon cascade-hidden elements, an immediate (and slow)
			// check is required in this first loop unless we have a nonempty display value (either
			// inline or about-to-be-restored)
			if ( display === "none" ) {
				values[ index ] = dataPriv.get( elem, "display" ) || null;
				if ( !values[ index ] ) {
					elem.style.display = "";
				}
			}
			if ( elem.style.display === "" && isHiddenWithinTree( elem ) ) {
				values[ index ] = getDefaultDisplay( elem );
			}
		} else {
			if ( display !== "none" ) {
				values[ index ] = "none";

				// Remember what we're overwriting
				dataPriv.set( elem, "display", display );
			}
		}
	}

	// Set the display of the elements in a second loop to avoid constant reflow
	for ( index = 0; index < length; index++ ) {
		if ( values[ index ] != null ) {
			elements[ index ].style.display = values[ index ];
		}
	}

	return elements;
}

jQuery.fn.extend( {
	show: function() {
		return showHide( this, true );
	},
	hide: function() {
		return showHide( this );
	},
	toggle: function( state ) {
		if ( typeof state === "boolean" ) {
			return state ? this.show() : this.hide();
		}

		return this.each( function() {
			if ( isHiddenWithinTree( this ) ) {
				jQuery( this ).show();
			} else {
				jQuery( this ).hide();
			}
		} );
	}
} );
var rcheckableType = ( /^(?:checkbox|radio)$/i );

var rtagName = ( /<([a-z][^\/\0>\x20\t\r\n\f]*)/i );

var rscriptType = ( /^$|^module$|\/(?:java|ecma)script/i );



( function() {
	var fragment = document.createDocumentFragment(),
		div = fragment.appendChild( document.createElement( "div" ) ),
		input = document.createElement( "input" );

	// Support: Android 4.0 - 4.3 only
	// Check state lost if the name is set (trac-11217)
	// Support: Windows Web Apps (WWA)
	// `name` and `type` must use .setAttribute for WWA (trac-14901)
	input.setAttribute( "type", "radio" );
	input.setAttribute( "checked", "checked" );
	input.setAttribute( "name", "t" );

	div.appendChild( input );

	// Support: Android <=4.1 only
	// Older WebKit doesn't clone checked state correctly in fragments
	support.checkClone = div.cloneNode( true ).cloneNode( true ).lastChild.checked;

	// Support: IE <=11 only
	// Make sure textarea (and checkbox) defaultValue is properly cloned
	div.innerHTML = "<textarea>x</textarea>";
	support.noCloneChecked = !!div.cloneNode( true ).lastChild.defaultValue;

	// Support: IE <=9 only
	// IE <=9 replaces <option> tags with their contents when inserted outside of
	// the select element.
	div.innerHTML = "<option></option>";
	support.option = !!div.lastChild;
} )();


// We have to close these tags to support XHTML (trac-13200)
var wrapMap = {

	// XHTML parsers do not magically insert elements in the
	// same way that tag soup parsers do. So we cannot shorten
	// this by omitting <tbody> or other required elements.
	thead: [ 1, "<table>", "</table>" ],
	col: [ 2, "<table><colgroup>", "</colgroup></table>" ],
	tr: [ 2, "<table><tbody>", "</tbody></table>" ],
	td: [ 3, "<table><tbody><tr>", "</tr></tbody></table>" ],

	_default: [ 0, "", "" ]
};

wrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
wrapMap.th = wrapMap.td;

// Support: IE <=9 only
if ( !support.option ) {
	wrapMap.optgroup = wrapMap.option = [ 1, "<select multiple='multiple'>", "</select>" ];
}


function getAll( context, tag ) {

	// Support: IE <=9 - 11 only
	// Use typeof to avoid zero-argument method invocation on host objects (trac-15151)
	var ret;

	if ( typeof context.getElementsByTagName !== "undefined" ) {
		ret = context.getElementsByTagName( tag || "*" );

	} else if ( typeof context.querySelectorAll !== "undefined" ) {
		ret = context.querySelectorAll( tag || "*" );

	} else {
		ret = [];
	}

	if ( tag === undefined || tag && nodeName( context, tag ) ) {
		return jQuery.merge( [ context ], ret );
	}

	return ret;
}


// Mark scripts as having already been evaluated
function setGlobalEval( elems, refElements ) {
	var i = 0,
		l = elems.length;

	for ( ; i < l; i++ ) {
		dataPriv.set(
			elems[ i ],
			"globalEval",
			!refElements || dataPriv.get( refElements[ i ], "globalEval" )
		);
	}
}


var rhtml = /<|&#?\w+;/;

function buildFragment( elems, context, scripts, selection, ignored ) {
	var elem, tmp, tag, wrap, attached, j,
		fragment = context.createDocumentFragment(),
		nodes = [],
		i = 0,
		l = elems.length;

	for ( ; i < l; i++ ) {
		elem = elems[ i ];

		if ( elem || elem === 0 ) {

			// Add nodes directly
			if ( toType( elem ) === "object" ) {

				// Support: Android <=4.0 only, PhantomJS 1 only
				// push.apply(_, arraylike) throws on ancient WebKit
				jQuery.merge( nodes, elem.nodeType ? [ elem ] : elem );

			// Convert non-html into a text node
			} else if ( !rhtml.test( elem ) ) {
				nodes.push( context.createTextNode( elem ) );

			// Convert html into DOM nodes
			} else {
				tmp = tmp || fragment.appendChild( context.createElement( "div" ) );

				// Deserialize a standard representation
				tag = ( rtagName.exec( elem ) || [ "", "" ] )[ 1 ].toLowerCase();
				wrap = wrapMap[ tag ] || wrapMap._default;
				tmp.innerHTML = wrap[ 1 ] + jQuery.htmlPrefilter( elem ) + wrap[ 2 ];

				// Descend through wrappers to the right content
				j = wrap[ 0 ];
				while ( j-- ) {
					tmp = tmp.lastChild;
				}

				// Support: Android <=4.0 only, PhantomJS 1 only
				// push.apply(_, arraylike) throws on ancient WebKit
				jQuery.merge( nodes, tmp.childNodes );

				// Remember the top-level container
				tmp = fragment.firstChild;

				// Ensure the created nodes are orphaned (trac-12392)
				tmp.textContent = "";
			}
		}
	}

	// Remove wrapper from fragment
	fragment.textContent = "";

	i = 0;
	while ( ( elem = nodes[ i++ ] ) ) {

		// Skip elements already in the context collection (trac-4087)
		if ( selection && jQuery.inArray( elem, selection ) > -1 ) {
			if ( ignored ) {
				ignored.push( elem );
			}
			continue;
		}

		attached = isAttached( elem );

		// Append to fragment
		tmp = getAll( fragment.appendChild( elem ), "script" );

		// Preserve script evaluation history
		if ( attached ) {
			setGlobalEval( tmp );
		}

		// Capture executables
		if ( scripts ) {
			j = 0;
			while ( ( elem = tmp[ j++ ] ) ) {
				if ( rscriptType.test( elem.type || "" ) ) {
					scripts.push( elem );
				}
			}
		}
	}

	return fragment;
}


var rtypenamespace = /^([^.]*)(?:\.(.+)|)/;

function returnTrue() {
	return true;
}

function returnFalse() {
	return false;
}

function on( elem, types, selector, data, fn, one ) {
	var origFn, type;

	// Types can be a map of types/handlers
	if ( typeof types === "object" ) {

		// ( types-Object, selector, data )
		if ( typeof selector !== "string" ) {

			// ( types-Object, data )
			data = data || selector;
			selector = undefined;
		}
		for ( type in types ) {
			on( elem, type, selector, data, types[ type ], one );
		}
		return elem;
	}

	if ( data == null && fn == null ) {

		// ( types, fn )
		fn = selector;
		data = selector = undefined;
	} else if ( fn == null ) {
		if ( typeof selector === "string" ) {

			// ( types, selector, fn )
			fn = data;
			data = undefined;
		} else {

			// ( types, data, fn )
			fn = data;
			data = selector;
			selector = undefined;
		}
	}
	if ( fn === false ) {
		fn = returnFalse;
	} else if ( !fn ) {
		return elem;
	}

	if ( one === 1 ) {
		origFn = fn;
		fn = function( event ) {

			// Can use an empty set, since event contains the info
			jQuery().off( event );
			return origFn.apply( this, arguments );
		};

		// Use same guid so caller can remove using origFn
		fn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );
	}
	return elem.each( function() {
		jQuery.event.add( this, types, fn, data, selector );
	} );
}

/*
 * Helper functions for managing events -- not part of the public interface.
 * Props to Dean Edwards' addEvent library for many of the ideas.
 */
jQuery.event = {

	global: {},

	add: function( elem, types, handler, data, selector ) {

		var handleObjIn, eventHandle, tmp,
			events, t, handleObj,
			special, handlers, type, namespaces, origType,
			elemData = dataPriv.get( elem );

		// Only attach events to objects that accept data
		if ( !acceptData( elem ) ) {
			return;
		}

		// Caller can pass in an object of custom data in lieu of the handler
		if ( handler.handler ) {
			handleObjIn = handler;
			handler = handleObjIn.handler;
			selector = handleObjIn.selector;
		}

		// Ensure that invalid selectors throw exceptions at attach time
		// Evaluate against documentElement in case elem is a non-element node (e.g., document)
		if ( selector ) {
			jQuery.find.matchesSelector( documentElement, selector );
		}

		// Make sure that the handler has a unique ID, used to find/remove it later
		if ( !handler.guid ) {
			handler.guid = jQuery.guid++;
		}

		// Init the element's event structure and main handler, if this is the first
		if ( !( events = elemData.events ) ) {
			events = elemData.events = Object.create( null );
		}
		if ( !( eventHandle = elemData.handle ) ) {
			eventHandle = elemData.handle = function( e ) {

				// Discard the second event of a jQuery.event.trigger() and
				// when an event is called after a page has unloaded
				return typeof jQuery !== "undefined" && jQuery.event.triggered !== e.type ?
					jQuery.event.dispatch.apply( elem, arguments ) : undefined;
			};
		}

		// Handle multiple events separated by a space
		types = ( types || "" ).match( rnothtmlwhite ) || [ "" ];
		t = types.length;
		while ( t-- ) {
			tmp = rtypenamespace.exec( types[ t ] ) || [];
			type = origType = tmp[ 1 ];
			namespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();

			// There *must* be a type, no attaching namespace-only handlers
			if ( !type ) {
				continue;
			}

			// If event changes its type, use the special event handlers for the changed type
			special = jQuery.event.special[ type ] || {};

			// If selector defined, determine special event api type, otherwise given type
			type = ( selector ? special.delegateType : special.bindType ) || type;

			// Update special based on newly reset type
			special = jQuery.event.special[ type ] || {};

			// handleObj is passed to all event handlers
			handleObj = jQuery.extend( {
				type: type,
				origType: origType,
				data: data,
				handler: handler,
				guid: handler.guid,
				selector: selector,
				needsContext: selector && jQuery.expr.match.needsContext.test( selector ),
				namespace: namespaces.join( "." )
			}, handleObjIn );

			// Init the event handler queue if we're the first
			if ( !( handlers = events[ type ] ) ) {
				handlers = events[ type ] = [];
				handlers.delegateCount = 0;

				// Only use addEventListener if the special events handler returns false
				if ( !special.setup ||
					special.setup.call( elem, data, namespaces, eventHandle ) === false ) {

					if ( elem.addEventListener ) {
						elem.addEventListener( type, eventHandle );
					}
				}
			}

			if ( special.add ) {
				special.add.call( elem, handleObj );

				if ( !handleObj.handler.guid ) {
					handleObj.handler.guid = handler.guid;
				}
			}

			// Add to the element's handler list, delegates in front
			if ( selector ) {
				handlers.splice( handlers.delegateCount++, 0, handleObj );
			} else {
				handlers.push( handleObj );
			}

			// Keep track of which events have ever been used, for event optimization
			jQuery.event.global[ type ] = true;
		}

	},

	// Detach an event or set of events from an element
	remove: function( elem, types, handler, selector, mappedTypes ) {

		var j, origCount, tmp,
			events, t, handleObj,
			special, handlers, type, namespaces, origType,
			elemData = dataPriv.hasData( elem ) && dataPriv.get( elem );

		if ( !elemData || !( events = elemData.events ) ) {
			return;
		}

		// Once for each type.namespace in types; type may be omitted
		types = ( types || "" ).match( rnothtmlwhite ) || [ "" ];
		t = types.length;
		while ( t-- ) {
			tmp = rtypenamespace.exec( types[ t ] ) || [];
			type = origType = tmp[ 1 ];
			namespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();

			// Unbind all events (on this namespace, if provided) for the element
			if ( !type ) {
				for ( type in events ) {
					jQuery.event.remove( elem, type + types[ t ], handler, selector, true );
				}
				continue;
			}

			special = jQuery.event.special[ type ] || {};
			type = ( selector ? special.delegateType : special.bindType ) || type;
			handlers = events[ type ] || [];
			tmp = tmp[ 2 ] &&
				new RegExp( "(^|\\.)" + namespaces.join( "\\.(?:.*\\.|)" ) + "(\\.|$)" );

			// Remove matching events
			origCount = j = handlers.length;
			while ( j-- ) {
				handleObj = handlers[ j ];

				if ( ( mappedTypes || origType === handleObj.origType ) &&
					( !handler || handler.guid === handleObj.guid ) &&
					( !tmp || tmp.test( handleObj.namespace ) ) &&
					( !selector || selector === handleObj.selector ||
						selector === "**" && handleObj.selector ) ) {
					handlers.splice( j, 1 );

					if ( handleObj.selector ) {
						handlers.delegateCount--;
					}
					if ( special.remove ) {
						special.remove.call( elem, handleObj );
					}
				}
			}

			// Remove generic event handler if we removed something and no more handlers exist
			// (avoids potential for endless recursion during removal of special event handlers)
			if ( origCount && !handlers.length ) {
				if ( !special.teardown ||
					special.teardown.call( elem, namespaces, elemData.handle ) === false ) {

					jQuery.removeEvent( elem, type, elemData.handle );
				}

				delete events[ type ];
			}
		}

		// Remove data and the expando if it's no longer used
		if ( jQuery.isEmptyObject( events ) ) {
			dataPriv.remove( elem, "handle events" );
		}
	},

	dispatch: function( nativeEvent ) {

		var i, j, ret, matched, handleObj, handlerQueue,
			args = new Array( arguments.length ),

			// Make a writable jQuery.Event from the native event object
			event = jQuery.event.fix( nativeEvent ),

			handlers = (
				dataPriv.get( this, "events" ) || Object.create( null )
			)[ event.type ] || [],
			special = jQuery.event.special[ event.type ] || {};

		// Use the fix-ed jQuery.Event rather than the (read-only) native event
		args[ 0 ] = event;

		for ( i = 1; i < arguments.length; i++ ) {
			args[ i ] = arguments[ i ];
		}

		event.delegateTarget = this;

		// Call the preDispatch hook for the mapped type, and let it bail if desired
		if ( special.preDispatch && special.preDispatch.call( this, event ) === false ) {
			return;
		}

		// Determine handlers
		handlerQueue = jQuery.event.handlers.call( this, event, handlers );

		// Run delegates first; they may want to stop propagation beneath us
		i = 0;
		while ( ( matched = handlerQueue[ i++ ] ) && !event.isPropagationStopped() ) {
			event.currentTarget = matched.elem;

			j = 0;
			while ( ( handleObj = matched.handlers[ j++ ] ) &&
				!event.isImmediatePropagationStopped() ) {

				// If the event is namespaced, then each handler is only invoked if it is
				// specially universal or its namespaces are a superset of the event's.
				if ( !event.rnamespace || handleObj.namespace === false ||
					event.rnamespace.test( handleObj.namespace ) ) {

					event.handleObj = handleObj;
					event.data = handleObj.data;

					ret = ( ( jQuery.event.special[ handleObj.origType ] || {} ).handle ||
						handleObj.handler ).apply( matched.elem, args );

					if ( ret !== undefined ) {
						if ( ( event.result = ret ) === false ) {
							event.preventDefault();
							event.stopPropagation();
						}
					}
				}
			}
		}

		// Call the postDispatch hook for the mapped type
		if ( special.postDispatch ) {
			special.postDispatch.call( this, event );
		}

		return event.result;
	},

	handlers: function( event, handlers ) {
		var i, handleObj, sel, matchedHandlers, matchedSelectors,
			handlerQueue = [],
			delegateCount = handlers.delegateCount,
			cur = event.target;

		// Find delegate handlers
		if ( delegateCount &&

			// Support: IE <=9
			// Black-hole SVG <use> instance trees (trac-13180)
			cur.nodeType &&

			// Support: Firefox <=42
			// Suppress spec-violating clicks indicating a non-primary pointer button (trac-3861)
			// https://www.w3.org/TR/DOM-Level-3-Events/#event-type-click
			// Support: IE 11 only
			// ...but not arrow key "clicks" of radio inputs, which can have `button` -1 (gh-2343)
			!( event.type === "click" && event.button >= 1 ) ) {

			for ( ; cur !== this; cur = cur.parentNode || this ) {

				// Don't check non-elements (trac-13208)
				// Don't process clicks on disabled elements (trac-6911, trac-8165, trac-11382, trac-11764)
				if ( cur.nodeType === 1 && !( event.type === "click" && cur.disabled === true ) ) {
					matchedHandlers = [];
					matchedSelectors = {};
					for ( i = 0; i < delegateCount; i++ ) {
						handleObj = handlers[ i ];

						// Don't conflict with Object.prototype properties (trac-13203)
						sel = handleObj.selector + " ";

						if ( matchedSelectors[ sel ] === undefined ) {
							matchedSelectors[ sel ] = handleObj.needsContext ?
								jQuery( sel, this ).index( cur ) > -1 :
								jQuery.find( sel, this, null, [ cur ] ).length;
						}
						if ( matchedSelectors[ sel ] ) {
							matchedHandlers.push( handleObj );
						}
					}
					if ( matchedHandlers.length ) {
						handlerQueue.push( { elem: cur, handlers: matchedHandlers } );
					}
				}
			}
		}

		// Add the remaining (directly-bound) handlers
		cur = this;
		if ( delegateCount < handlers.length ) {
			handlerQueue.push( { elem: cur, handlers: handlers.slice( delegateCount ) } );
		}

		return handlerQueue;
	},

	addProp: function( name, hook ) {
		Object.defineProperty( jQuery.Event.prototype, name, {
			enumerable: true,
			configurable: true,

			get: isFunction( hook ) ?
				function() {
					if ( this.originalEvent ) {
						return hook( this.originalEvent );
					}
				} :
				function() {
					if ( this.originalEvent ) {
						return this.originalEvent[ name ];
					}
				},

			set: function( value ) {
				Object.defineProperty( this, name, {
					enumerable: true,
					configurable: true,
					writable: true,
					value: value
				} );
			}
		} );
	},

	fix: function( originalEvent ) {
		return originalEvent[ jQuery.expando ] ?
			originalEvent :
			new jQuery.Event( originalEvent );
	},

	special: {
		load: {

			// Prevent triggered image.load events from bubbling to window.load
			noBubble: true
		},
		click: {

			// Utilize native event to ensure correct state for checkable inputs
			setup: function( data ) {

				// For mutual compressibility with _default, replace `this` access with a local var.
				// `|| data` is dead code meant only to preserve the variable through minification.
				var el = this || data;

				// Claim the first handler
				if ( rcheckableType.test( el.type ) &&
					el.click && nodeName( el, "input" ) ) {

					// dataPriv.set( el, "click", ... )
					leverageNative( el, "click", true );
				}

				// Return false to allow normal processing in the caller
				return false;
			},
			trigger: function( data ) {

				// For mutual compressibility with _default, replace `this` access with a local var.
				// `|| data` is dead code meant only to preserve the variable through minification.
				var el = this || data;

				// Force setup before triggering a click
				if ( rcheckableType.test( el.type ) &&
					el.click && nodeName( el, "input" ) ) {

					leverageNative( el, "click" );
				}

				// Return non-false to allow normal event-path propagation
				return true;
			},

			// For cross-browser consistency, suppress native .click() on links
			// Also prevent it if we're currently inside a leveraged native-event stack
			_default: function( event ) {
				var target = event.target;
				return rcheckableType.test( target.type ) &&
					target.click && nodeName( target, "input" ) &&
					dataPriv.get( target, "click" ) ||
					nodeName( target, "a" );
			}
		},

		beforeunload: {
			postDispatch: function( event ) {

				// Support: Firefox 20+
				// Firefox doesn't alert if the returnValue field is not set.
				if ( event.result !== undefined && event.originalEvent ) {
					event.originalEvent.returnValue = event.result;
				}
			}
		}
	}
};

// Ensure the presence of an event listener that handles manually-triggered
// synthetic events by interrupting progress until reinvoked in response to
// *native* events that it fires directly, ensuring that state changes have
// already occurred before other listeners are invoked.
function leverageNative( el, type, isSetup ) {

	// Missing `isSetup` indicates a trigger call, which must force setup through jQuery.event.add
	if ( !isSetup ) {
		if ( dataPriv.get( el, type ) === undefined ) {
			jQuery.event.add( el, type, returnTrue );
		}
		return;
	}

	// Register the controller as a special universal handler for all event namespaces
	dataPriv.set( el, type, false );
	jQuery.event.add( el, type, {
		namespace: false,
		handler: function( event ) {
			var result,
				saved = dataPriv.get( this, type );

			if ( ( event.isTrigger & 1 ) && this[ type ] ) {

				// Interrupt processing of the outer synthetic .trigger()ed event
				if ( !saved ) {

					// Store arguments for use when handling the inner native event
					// There will always be at least one argument (an event object), so this array
					// will not be confused with a leftover capture object.
					saved = slice.call( arguments );
					dataPriv.set( this, type, saved );

					// Trigger the native event and capture its result
					this[ type ]();
					result = dataPriv.get( this, type );
					dataPriv.set( this, type, false );

					if ( saved !== result ) {

						// Cancel the outer synthetic event
						event.stopImmediatePropagation();
						event.preventDefault();

						return result;
					}

				// If this is an inner synthetic event for an event with a bubbling surrogate
				// (focus or blur), assume that the surrogate already propagated from triggering
				// the native event and prevent that from happening again here.
				// This technically gets the ordering wrong w.r.t. to `.trigger()` (in which the
				// bubbling surrogate propagates *after* the non-bubbling base), but that seems
				// less bad than duplication.
				} else if ( ( jQuery.event.special[ type ] || {} ).delegateType ) {
					event.stopPropagation();
				}

			// If this is a native event triggered above, everything is now in order
			// Fire an inner synthetic event with the original arguments
			} else if ( saved ) {

				// ...and capture the result
				dataPriv.set( this, type, jQuery.event.trigger(
					saved[ 0 ],
					saved.slice( 1 ),
					this
				) );

				// Abort handling of the native event by all jQuery handlers while allowing
				// native handlers on the same element to run. On target, this is achieved
				// by stopping immediate propagation just on the jQuery event. However,
				// the native event is re-wrapped by a jQuery one on each level of the
				// propagation so the only way to stop it for jQuery is to stop it for
				// everyone via native `stopPropagation()`. This is not a problem for
				// focus/blur which don't bubble, but it does also stop click on checkboxes
				// and radios. We accept this limitation.
				event.stopPropagation();
				event.isImmediatePropagationStopped = returnTrue;
			}
		}
	} );
}

jQuery.removeEvent = function( elem, type, handle ) {

	// This "if" is needed for plain objects
	if ( elem.removeEventListener ) {
		elem.removeEventListener( type, handle );
	}
};

jQuery.Event = function( src, props ) {

	// Allow instantiation without the 'new' keyword
	if ( !( this instanceof jQuery.Event ) ) {
		return new jQuery.Event( src, props );
	}

	// Event object
	if ( src && src.type ) {
		this.originalEvent = src;
		this.type = src.type;

		// Events bubbling up the document may have been marked as prevented
		// by a handler lower down the tree; reflect the correct value.
		this.isDefaultPrevented = src.defaultPrevented ||
				src.defaultPrevented === undefined &&

				// Support: Android <=2.3 only
				src.returnValue === false ?
			returnTrue :
			returnFalse;

		// Create target properties
		// Support: Safari <=6 - 7 only
		// Target should not be a text node (trac-504, trac-13143)
		this.target = ( src.target && src.target.nodeType === 3 ) ?
			src.target.parentNode :
			src.target;

		this.currentTarget = src.currentTarget;
		this.relatedTarget = src.relatedTarget;

	// Event type
	} else {
		this.type = src;
	}

	// Put explicitly provided properties onto the event object
	if ( props ) {
		jQuery.extend( this, props );
	}

	// Create a timestamp if incoming event doesn't have one
	this.timeStamp = src && src.timeStamp || Date.now();

	// Mark it as fixed
	this[ jQuery.expando ] = true;
};

// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
// https://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
jQuery.Event.prototype = {
	constructor: jQuery.Event,
	isDefaultPrevented: returnFalse,
	isPropagationStopped: returnFalse,
	isImmediatePropagationStopped: returnFalse,
	isSimulated: false,

	preventDefault: function() {
		var e = this.originalEvent;

		this.isDefaultPrevented = returnTrue;

		if ( e && !this.isSimulated ) {
			e.preventDefault();
		}
	},
	stopPropagation: function() {
		var e = this.originalEvent;

		this.isPropagationStopped = returnTrue;

		if ( e && !this.isSimulated ) {
			e.stopPropagation();
		}
	},
	stopImmediatePropagation: function() {
		var e = this.originalEvent;

		this.isImmediatePropagationStopped = returnTrue;

		if ( e && !this.isSimulated ) {
			e.stopImmediatePropagation();
		}

		this.stopPropagation();
	}
};

// Includes all common event props including KeyEvent and MouseEvent specific props
jQuery.each( {
	altKey: true,
	bubbles: true,
	cancelable: true,
	changedTouches: true,
	ctrlKey: true,
	detail: true,
	eventPhase: true,
	metaKey: true,
	pageX: true,
	pageY: true,
	shiftKey: true,
	view: true,
	"char": true,
	code: true,
	charCode: true,
	key: true,
	keyCode: true,
	button: true,
	buttons: true,
	clientX: true,
	clientY: true,
	offsetX: true,
	offsetY: true,
	pointerId: true,
	pointerType: true,
	screenX: true,
	screenY: true,
	targetTouches: true,
	toElement: true,
	touches: true,
	which: true
}, jQuery.event.addProp );

jQuery.each( { focus: "focusin", blur: "focusout" }, function( type, delegateType ) {

	function focusMappedHandler( nativeEvent ) {
		if ( document.documentMode ) {

			// Support: IE 11+
			// Attach a single focusin/focusout handler on the document while someone wants
			// focus/blur. This is because the former are synchronous in IE while the latter
			// are async. In other browsers, all those handlers are invoked synchronously.

			// `handle` from private data would already wrap the event, but we need
			// to change the `type` here.
			var handle = dataPriv.get( this, "handle" ),
				event = jQuery.event.fix( nativeEvent );
			event.type = nativeEvent.type === "focusin" ? "focus" : "blur";
			event.isSimulated = true;

			// First, handle focusin/focusout
			handle( nativeEvent );

			// ...then, handle focus/blur
			//
			// focus/blur don't bubble while focusin/focusout do; simulate the former by only
			// invoking the handler at the lower level.
			if ( event.target === event.currentTarget ) {

				// The setup part calls `leverageNative`, which, in turn, calls
				// `jQuery.event.add`, so event handle will already have been set
				// by this point.
				handle( event );
			}
		} else {

			// For non-IE browsers, attach a single capturing handler on the document
			// while someone wants focusin/focusout.
			jQuery.event.simulate( delegateType, nativeEvent.target,
				jQuery.event.fix( nativeEvent ) );
		}
	}

	jQuery.event.special[ type ] = {

		// Utilize native event if possible so blur/focus sequence is correct
		setup: function() {

			var attaches;

			// Claim the first handler
			// dataPriv.set( this, "focus", ... )
			// dataPriv.set( this, "blur", ... )
			leverageNative( this, type, true );

			if ( document.documentMode ) {

				// Support: IE 9 - 11+
				// We use the same native handler for focusin & focus (and focusout & blur)
				// so we need to coordinate setup & teardown parts between those events.
				// Use `delegateType` as the key as `type` is already used by `leverageNative`.
				attaches = dataPriv.get( this, delegateType );
				if ( !attaches ) {
					this.addEventListener( delegateType, focusMappedHandler );
				}
				dataPriv.set( this, delegateType, ( attaches || 0 ) + 1 );
			} else {

				// Return false to allow normal processing in the caller
				return false;
			}
		},
		trigger: function() {

			// Force setup before trigger
			leverageNative( this, type );

			// Return non-false to allow normal event-path propagation
			return true;
		},

		teardown: function() {
			var attaches;

			if ( document.documentMode ) {
				attaches = dataPriv.get( this, delegateType ) - 1;
				if ( !attaches ) {
					this.removeEventListener( delegateType, focusMappedHandler );
					dataPriv.remove( this, delegateType );
				} else {
					dataPriv.set( this, delegateType, attaches );
				}
			} else {

				// Return false to indicate standard teardown should be applied
				return false;
			}
		},

		// Suppress native focus or blur if we're currently inside
		// a leveraged native-event stack
		_default: function( event ) {
			return dataPriv.get( event.target, type );
		},

		delegateType: delegateType
	};

	// Support: Firefox <=44
	// Firefox doesn't have focus(in | out) events
	// Related ticket - https://bugzilla.mozilla.org/show_bug.cgi?id=687787
	//
	// Support: Chrome <=48 - 49, Safari <=9.0 - 9.1
	// focus(in | out) events fire after focus & blur events,
	// which is spec violation - http://www.w3.org/TR/DOM-Level-3-Events/#events-focusevent-event-order
	// Related ticket - https://bugs.chromium.org/p/chromium/issues/detail?id=449857
	//
	// Support: IE 9 - 11+
	// To preserve relative focusin/focus & focusout/blur event order guaranteed on the 3.x branch,
	// attach a single handler for both events in IE.
	jQuery.event.special[ delegateType ] = {
		setup: function() {

			// Handle: regular nodes (via `this.ownerDocument`), window
			// (via `this.document`) & document (via `this`).
			var doc = this.ownerDocument || this.document || this,
				dataHolder = document.documentMode ? this : doc,
				attaches = dataPriv.get( dataHolder, delegateType );

			// Support: IE 9 - 11+
			// We use the same native handler for focusin & focus (and focusout & blur)
			// so we need to coordinate setup & teardown parts between those events.
			// Use `delegateType` as the key as `type` is already used by `leverageNative`.
			if ( !attaches ) {
				if ( document.documentMode ) {
					this.addEventListener( delegateType, focusMappedHandler );
				} else {
					doc.addEventListener( type, focusMappedHandler, true );
				}
			}
			dataPriv.set( dataHolder, delegateType, ( attaches || 0 ) + 1 );
		},
		teardown: function() {
			var doc = this.ownerDocument || this.document || this,
				dataHolder = document.documentMode ? this : doc,
				attaches = dataPriv.get( dataHolder, delegateType ) - 1;

			if ( !attaches ) {
				if ( document.documentMode ) {
					this.removeEventListener( delegateType, focusMappedHandler );
				} else {
					doc.removeEventListener( type, focusMappedHandler, true );
				}
				dataPriv.remove( dataHolder, delegateType );
			} else {
				dataPriv.set( dataHolder, delegateType, attaches );
			}
		}
	};
} );

// Create mouseenter/leave events using mouseover/out and event-time checks
// so that event delegation works in jQuery.
// Do the same for pointerenter/pointerleave and pointerover/pointerout
//
// Support: Safari 7 only
// Safari sends mouseenter too often; see:
// https://bugs.chromium.org/p/chromium/issues/detail?id=470258
// for the description of the bug (it existed in older Chrome versions as well).
jQuery.each( {
	mouseenter: "mouseover",
	mouseleave: "mouseout",
	pointerenter: "pointerover",
	pointerleave: "pointerout"
}, function( orig, fix ) {
	jQuery.event.special[ orig ] = {
		delegateType: fix,
		bindType: fix,

		handle: function( event ) {
			var ret,
				target = this,
				related = event.relatedTarget,
				handleObj = event.handleObj;

			// For mouseenter/leave call the handler if related is outside the target.
			// NB: No relatedTarget if the mouse left/entered the browser window
			if ( !related || ( related !== target && !jQuery.contains( target, related ) ) ) {
				event.type = handleObj.origType;
				ret = handleObj.handler.apply( this, arguments );
				event.type = fix;
			}
			return ret;
		}
	};
} );

jQuery.fn.extend( {

	on: function( types, selector, data, fn ) {
		return on( this, types, selector, data, fn );
	},
	one: function( types, selector, data, fn ) {
		return on( this, types, selector, data, fn, 1 );
	},
	off: function( types, selector, fn ) {
		var handleObj, type;
		if ( types && types.preventDefault && types.handleObj ) {

			// ( event )  dispatched jQuery.Event
			handleObj = types.handleObj;
			jQuery( types.delegateTarget ).off(
				handleObj.namespace ?
					handleObj.origType + "." + handleObj.namespace :
					handleObj.origType,
				handleObj.selector,
				handleObj.handler
			);
			return this;
		}
		if ( typeof types === "object" ) {

			// ( types-object [, selector] )
			for ( type in types ) {
				this.off( type, selector, types[ type ] );
			}
			return this;
		}
		if ( selector === false || typeof selector === "function" ) {

			// ( types [, fn] )
			fn = selector;
			selector = undefined;
		}
		if ( fn === false ) {
			fn = returnFalse;
		}
		return this.each( function() {
			jQuery.event.remove( this, types, fn, selector );
		} );
	}
} );


var

	// Support: IE <=10 - 11, Edge 12 - 13 only
	// In IE/Edge using regex groups here causes severe slowdowns.
	// See https://connect.microsoft.com/IE/feedback/details/1736512/
	rnoInnerhtml = /<script|<style|<link/i,

	// checked="checked" or checked
	rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i,

	rcleanScript = /^\s*<!\[CDATA\[|\]\]>\s*$/g;

// Prefer a tbody over its parent table for containing new rows
function manipulationTarget( elem, content ) {
	if ( nodeName( elem, "table" ) &&
		nodeName( content.nodeType !== 11 ? content : content.firstChild, "tr" ) ) {

		return jQuery( elem ).children( "tbody" )[ 0 ] || elem;
	}

	return elem;
}

// Replace/restore the type attribute of script elements for safe DOM manipulation
function disableScript( elem ) {
	elem.type = ( elem.getAttribute( "type" ) !== null ) + "/" + elem.type;
	return elem;
}
function restoreScript( elem ) {
	if ( ( elem.type || "" ).slice( 0, 5 ) === "true/" ) {
		elem.type = elem.type.slice( 5 );
	} else {
		elem.removeAttribute( "type" );
	}

	return elem;
}

function cloneCopyEvent( src, dest ) {
	var i, l, type, pdataOld, udataOld, udataCur, events;

	if ( dest.nodeType !== 1 ) {
		return;
	}

	// 1. Copy private data: events, handlers, etc.
	if ( dataPriv.hasData( src ) ) {
		pdataOld = dataPriv.get( src );
		events = pdataOld.events;

		if ( events ) {
			dataPriv.remove( dest, "handle events" );

			for ( type in events ) {
				for ( i = 0, l = events[ type ].length; i < l; i++ ) {
					jQuery.event.add( dest, type, events[ type ][ i ] );
				}
			}
		}
	}

	// 2. Copy user data
	if ( dataUser.hasData( src ) ) {
		udataOld = dataUser.access( src );
		udataCur = jQuery.extend( {}, udataOld );

		dataUser.set( dest, udataCur );
	}
}

// Fix IE bugs, see support tests
function fixInput( src, dest ) {
	var nodeName = dest.nodeName.toLowerCase();

	// Fails to persist the checked state of a cloned checkbox or radio button.
	if ( nodeName === "input" && rcheckableType.test( src.type ) ) {
		dest.checked = src.checked;

	// Fails to return the selected option to the default selected state when cloning options
	} else if ( nodeName === "input" || nodeName === "textarea" ) {
		dest.defaultValue = src.defaultValue;
	}
}

function domManip( collection, args, callback, ignored ) {

	// Flatten any nested arrays
	args = flat( args );

	var fragment, first, scripts, hasScripts, node, doc,
		i = 0,
		l = collection.length,
		iNoClone = l - 1,
		value = args[ 0 ],
		valueIsFunction = isFunction( value );

	// We can't cloneNode fragments that contain checked, in WebKit
	if ( valueIsFunction ||
			( l > 1 && typeof value === "string" &&
				!support.checkClone && rchecked.test( value ) ) ) {
		return collection.each( function( index ) {
			var self = collection.eq( index );
			if ( valueIsFunction ) {
				args[ 0 ] = value.call( this, index, self.html() );
			}
			domManip( self, args, callback, ignored );
		} );
	}

	if ( l ) {
		fragment = buildFragment( args, collection[ 0 ].ownerDocument, false, collection, ignored );
		first = fragment.firstChild;

		if ( fragment.childNodes.length === 1 ) {
			fragment = first;
		}

		// Require either new content or an interest in ignored elements to invoke the callback
		if ( first || ignored ) {
			scripts = jQuery.map( getAll( fragment, "script" ), disableScript );
			hasScripts = scripts.length;

			// Use the original fragment for the last item
			// instead of the first because it can end up
			// being emptied incorrectly in certain situations (trac-8070).
			for ( ; i < l; i++ ) {
				node = fragment;

				if ( i !== iNoClone ) {
					node = jQuery.clone( node, true, true );

					// Keep references to cloned scripts for later restoration
					if ( hasScripts ) {

						// Support: Android <=4.0 only, PhantomJS 1 only
						// push.apply(_, arraylike) throws on ancient WebKit
						jQuery.merge( scripts, getAll( node, "script" ) );
					}
				}

				callback.call( collection[ i ], node, i );
			}

			if ( hasScripts ) {
				doc = scripts[ scripts.length - 1 ].ownerDocument;

				// Re-enable scripts
				jQuery.map( scripts, restoreScript );

				// Evaluate executable scripts on first document insertion
				for ( i = 0; i < hasScripts; i++ ) {
					node = scripts[ i ];
					if ( rscriptType.test( node.type || "" ) &&
						!dataPriv.access( node, "globalEval" ) &&
						jQuery.contains( doc, node ) ) {

						if ( node.src && ( node.type || "" ).toLowerCase()  !== "module" ) {

							// Optional AJAX dependency, but won't run scripts if not present
							if ( jQuery._evalUrl && !node.noModule ) {
								jQuery._evalUrl( node.src, {
									nonce: node.nonce || node.getAttribute( "nonce" )
								}, doc );
							}
						} else {

							// Unwrap a CDATA section containing script contents. This shouldn't be
							// needed as in XML documents they're already not visible when
							// inspecting element contents and in HTML documents they have no
							// meaning but we're preserving that logic for backwards compatibility.
							// This will be removed completely in 4.0. See gh-4904.
							DOMEval( node.textContent.replace( rcleanScript, "" ), node, doc );
						}
					}
				}
			}
		}
	}

	return collection;
}

function remove( elem, selector, keepData ) {
	var node,
		nodes = selector ? jQuery.filter( selector, elem ) : elem,
		i = 0;

	for ( ; ( node = nodes[ i ] ) != null; i++ ) {
		if ( !keepData && node.nodeType === 1 ) {
			jQuery.cleanData( getAll( node ) );
		}

		if ( node.parentNode ) {
			if ( keepData && isAttached( node ) ) {
				setGlobalEval( getAll( node, "script" ) );
			}
			node.parentNode.removeChild( node );
		}
	}

	return elem;
}

jQuery.extend( {
	htmlPrefilter: function( html ) {
		return html;
	},

	clone: function( elem, dataAndEvents, deepDataAndEvents ) {
		var i, l, srcElements, destElements,
			clone = elem.cloneNode( true ),
			inPage = isAttached( elem );

		// Fix IE cloning issues
		if ( !support.noCloneChecked && ( elem.nodeType === 1 || elem.nodeType === 11 ) &&
				!jQuery.isXMLDoc( elem ) ) {

			// We eschew jQuery#find here for performance reasons:
			// https://jsperf.com/getall-vs-sizzle/2
			destElements = getAll( clone );
			srcElements = getAll( elem );

			for ( i = 0, l = srcElements.length; i < l; i++ ) {
				fixInput( srcElements[ i ], destElements[ i ] );
			}
		}

		// Copy the events from the original to the clone
		if ( dataAndEvents ) {
			if ( deepDataAndEvents ) {
				srcElements = srcElements || getAll( elem );
				destElements = destElements || getAll( clone );

				for ( i = 0, l = srcElements.length; i < l; i++ ) {
					cloneCopyEvent( srcElements[ i ], destElements[ i ] );
				}
			} else {
				cloneCopyEvent( elem, clone );
			}
		}

		// Preserve script evaluation history
		destElements = getAll( clone, "script" );
		if ( destElements.length > 0 ) {
			setGlobalEval( destElements, !inPage && getAll( elem, "script" ) );
		}

		// Return the cloned set
		return clone;
	},

	cleanData: function( elems ) {
		var data, elem, type,
			special = jQuery.event.special,
			i = 0;

		for ( ; ( elem = elems[ i ] ) !== undefined; i++ ) {
			if ( acceptData( elem ) ) {
				if ( ( data = elem[ dataPriv.expando ] ) ) {
					if ( data.events ) {
						for ( type in data.events ) {
							if ( special[ type ] ) {
								jQuery.event.remove( elem, type );

							// This is a shortcut to avoid jQuery.event.remove's overhead
							} else {
								jQuery.removeEvent( elem, type, data.handle );
							}
						}
					}

					// Support: Chrome <=35 - 45+
					// Assign undefined instead of using delete, see Data#remove
					elem[ dataPriv.expando ] = undefined;
				}
				if ( elem[ dataUser.expando ] ) {

					// Support: Chrome <=35 - 45+
					// Assign undefined instead of using delete, see Data#remove
					elem[ dataUser.expando ] = undefined;
				}
			}
		}
	}
} );

jQuery.fn.extend( {
	detach: function( selector ) {
		return remove( this, selector, true );
	},

	remove: function( selector ) {
		return remove( this, selector );
	},

	text: function( value ) {
		return access( this, function( value ) {
			return value === undefined ?
				jQuery.text( this ) :
				this.empty().each( function() {
					if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
						this.textContent = value;
					}
				} );
		}, null, value, arguments.length );
	},

	append: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
				var target = manipulationTarget( this, elem );
				target.appendChild( elem );
			}
		} );
	},

	prepend: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
				var target = manipulationTarget( this, elem );
				target.insertBefore( elem, target.firstChild );
			}
		} );
	},

	before: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.parentNode ) {
				this.parentNode.insertBefore( elem, this );
			}
		} );
	},

	after: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.parentNode ) {
				this.parentNode.insertBefore( elem, this.nextSibling );
			}
		} );
	},

	empty: function() {
		var elem,
			i = 0;

		for ( ; ( elem = this[ i ] ) != null; i++ ) {
			if ( elem.nodeType === 1 ) {

				// Prevent memory leaks
				jQuery.cleanData( getAll( elem, false ) );

				// Remove any remaining nodes
				elem.textContent = "";
			}
		}

		return this;
	},

	clone: function( dataAndEvents, deepDataAndEvents ) {
		dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
		deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;

		return this.map( function() {
			return jQuery.clone( this, dataAndEvents, deepDataAndEvents );
		} );
	},

	html: function( value ) {
		return access( this, function( value ) {
			var elem = this[ 0 ] || {},
				i = 0,
				l = this.length;

			if ( value === undefined && elem.nodeType === 1 ) {
				return elem.innerHTML;
			}

			// See if we can take a shortcut and just use innerHTML
			if ( typeof value === "string" && !rnoInnerhtml.test( value ) &&
				!wrapMap[ ( rtagName.exec( value ) || [ "", "" ] )[ 1 ].toLowerCase() ] ) {

				value = jQuery.htmlPrefilter( value );

				try {
					for ( ; i < l; i++ ) {
						elem = this[ i ] || {};

						// Remove element nodes and prevent memory leaks
						if ( elem.nodeType === 1 ) {
							jQuery.cleanData( getAll( elem, false ) );
							elem.innerHTML = value;
						}
					}

					elem = 0;

				// If using innerHTML throws an exception, use the fallback method
				} catch ( e ) {}
			}

			if ( elem ) {
				this.empty().append( value );
			}
		}, null, value, arguments.length );
	},

	replaceWith: function() {
		var ignored = [];

		// Make the changes, replacing each non-ignored context element with the new content
		return domManip( this, arguments, function( elem ) {
			var parent = this.parentNode;

			if ( jQuery.inArray( this, ignored ) < 0 ) {
				jQuery.cleanData( getAll( this ) );
				if ( parent ) {
					parent.replaceChild( elem, this );
				}
			}

		// Force callback invocation
		}, ignored );
	}
} );

jQuery.each( {
	appendTo: "append",
	prependTo: "prepend",
	insertBefore: "before",
	insertAfter: "after",
	replaceAll: "replaceWith"
}, function( name, original ) {
	jQuery.fn[ name ] = function( selector ) {
		var elems,
			ret = [],
			insert = jQuery( selector ),
			last = insert.length - 1,
			i = 0;

		for ( ; i <= last; i++ ) {
			elems = i === last ? this : this.clone( true );
			jQuery( insert[ i ] )[ original ]( elems );

			// Support: Android <=4.0 only, PhantomJS 1 only
			// .get() because push.apply(_, arraylike) throws on ancient WebKit
			push.apply( ret, elems.get() );
		}

		return this.pushStack( ret );
	};
} );
var rnumnonpx = new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );

var rcustomProp = /^--/;


var getStyles = function( elem ) {

		// Support: IE <=11 only, Firefox <=30 (trac-15098, trac-14150)
		// IE throws on elements created in popups
		// FF meanwhile throws on frame elements through "defaultView.getComputedStyle"
		var view = elem.ownerDocument.defaultView;

		if ( !view || !view.opener ) {
			view = window;
		}

		return view.getComputedStyle( elem );
	};

var swap = function( elem, options, callback ) {
	var ret, name,
		old = {};

	// Remember the old values, and insert the new ones
	for ( name in options ) {
		old[ name ] = elem.style[ name ];
		elem.style[ name ] = options[ name ];
	}

	ret = callback.call( elem );

	// Revert the old values
	for ( name in options ) {
		elem.style[ name ] = old[ name ];
	}

	return ret;
};


var rboxStyle = new RegExp( cssExpand.join( "|" ), "i" );



( function() {

	// Executing both pixelPosition & boxSizingReliable tests require only one layout
	// so they're executed at the same time to save the second computation.
	function computeStyleTests() {

		// This is a singleton, we need to execute it only once
		if ( !div ) {
			return;
		}

		container.style.cssText = "position:absolute;left:-11111px;width:60px;" +
			"margin-top:1px;padding:0;border:0";
		div.style.cssText =
			"position:relative;display:block;box-sizing:border-box;overflow:scroll;" +
			"margin:auto;border:1px;padding:1px;" +
			"width:60%;top:1%";
		documentElement.appendChild( container ).appendChild( div );

		var divStyle = window.getComputedStyle( div );
		pixelPositionVal = divStyle.top !== "1%";

		// Support: Android 4.0 - 4.3 only, Firefox <=3 - 44
		reliableMarginLeftVal = roundPixelMeasures( divStyle.marginLeft ) === 12;

		// Support: Android 4.0 - 4.3 only, Safari <=9.1 - 10.1, iOS <=7.0 - 9.3
		// Some styles come back with percentage values, even though they shouldn't
		div.style.right = "60%";
		pixelBoxStylesVal = roundPixelMeasures( divStyle.right ) === 36;

		// Support: IE 9 - 11 only
		// Detect misreporting of content dimensions for box-sizing:border-box elements
		boxSizingReliableVal = roundPixelMeasures( divStyle.width ) === 36;

		// Support: IE 9 only
		// Detect overflow:scroll screwiness (gh-3699)
		// Support: Chrome <=64
		// Don't get tricked when zoom affects offsetWidth (gh-4029)
		div.style.position = "absolute";
		scrollboxSizeVal = roundPixelMeasures( div.offsetWidth / 3 ) === 12;

		documentElement.removeChild( container );

		// Nullify the div so it wouldn't be stored in the memory and
		// it will also be a sign that checks already performed
		div = null;
	}

	function roundPixelMeasures( measure ) {
		return Math.round( parseFloat( measure ) );
	}

	var pixelPositionVal, boxSizingReliableVal, scrollboxSizeVal, pixelBoxStylesVal,
		reliableTrDimensionsVal, reliableMarginLeftVal,
		container = document.createElement( "div" ),
		div = document.createElement( "div" );

	// Finish early in limited (non-browser) environments
	if ( !div.style ) {
		return;
	}

	// Support: IE <=9 - 11 only
	// Style of cloned element affects source element cloned (trac-8908)
	div.style.backgroundClip = "content-box";
	div.cloneNode( true ).style.backgroundClip = "";
	support.clearCloneStyle = div.style.backgroundClip === "content-box";

	jQuery.extend( support, {
		boxSizingReliable: function() {
			computeStyleTests();
			return boxSizingReliableVal;
		},
		pixelBoxStyles: function() {
			computeStyleTests();
			return pixelBoxStylesVal;
		},
		pixelPosition: function() {
			computeStyleTests();
			return pixelPositionVal;
		},
		reliableMarginLeft: function() {
			computeStyleTests();
			return reliableMarginLeftVal;
		},
		scrollboxSize: function() {
			computeStyleTests();
			return scrollboxSizeVal;
		},

		// Support: IE 9 - 11+, Edge 15 - 18+
		// IE/Edge misreport `getComputedStyle` of table rows with width/height
		// set in CSS while `offset*` properties report correct values.
		// Behavior in IE 9 is more subtle than in newer versions & it passes
		// some versions of this test; make sure not to make it pass there!
		//
		// Support: Firefox 70+
		// Only Firefox includes border widths
		// in computed dimensions. (gh-4529)
		reliableTrDimensions: function() {
			var table, tr, trChild, trStyle;
			if ( reliableTrDimensionsVal == null ) {
				table = document.createElement( "table" );
				tr = document.createElement( "tr" );
				trChild = document.createElement( "div" );

				table.style.cssText = "position:absolute;left:-11111px;border-collapse:separate";
				tr.style.cssText = "box-sizing:content-box;border:1px solid";

				// Support: Chrome 86+
				// Height set through cssText does not get applied.
				// Computed height then comes back as 0.
				tr.style.height = "1px";
				trChild.style.height = "9px";

				// Support: Android 8 Chrome 86+
				// In our bodyBackground.html iframe,
				// display for all div elements is set to "inline",
				// which causes a problem only in Android 8 Chrome 86.
				// Ensuring the div is `display: block`
				// gets around this issue.
				trChild.style.display = "block";

				documentElement
					.appendChild( table )
					.appendChild( tr )
					.appendChild( trChild );

				trStyle = window.getComputedStyle( tr );
				reliableTrDimensionsVal = ( parseInt( trStyle.height, 10 ) +
					parseInt( trStyle.borderTopWidth, 10 ) +
					parseInt( trStyle.borderBottomWidth, 10 ) ) === tr.offsetHeight;

				documentElement.removeChild( table );
			}
			return reliableTrDimensionsVal;
		}
	} );
} )();


function curCSS( elem, name, computed ) {
	var width, minWidth, maxWidth, ret,
		isCustomProp = rcustomProp.test( name ),

		// Support: Firefox 51+
		// Retrieving style before computed somehow
		// fixes an issue with getting wrong values
		// on detached elements
		style = elem.style;

	computed = computed || getStyles( elem );

	// getPropertyValue is needed for:
	//   .css('filter') (IE 9 only, trac-12537)
	//   .css('--customProperty) (gh-3144)
	if ( computed ) {

		// Support: IE <=9 - 11+
		// IE only supports `"float"` in `getPropertyValue`; in computed styles
		// it's only available as `"cssFloat"`. We no longer modify properties
		// sent to `.css()` apart from camelCasing, so we need to check both.
		// Normally, this would create difference in behavior: if
		// `getPropertyValue` returns an empty string, the value returned
		// by `.css()` would be `undefined`. This is usually the case for
		// disconnected elements. However, in IE even disconnected elements
		// with no styles return `"none"` for `getPropertyValue( "float" )`
		ret = computed.getPropertyValue( name ) || computed[ name ];

		if ( isCustomProp && ret ) {

			// Support: Firefox 105+, Chrome <=105+
			// Spec requires trimming whitespace for custom properties (gh-4926).
			// Firefox only trims leading whitespace. Chrome just collapses
			// both leading & trailing whitespace to a single space.
			//
			// Fall back to `undefined` if empty string returned.
			// This collapses a missing definition with property defined
			// and set to an empty string but there's no standard API
			// allowing us to differentiate them without a performance penalty
			// and returning `undefined` aligns with older jQuery.
			//
			// rtrimCSS treats U+000D CARRIAGE RETURN and U+000C FORM FEED
			// as whitespace while CSS does not, but this is not a problem
			// because CSS preprocessing replaces them with U+000A LINE FEED
			// (which *is* CSS whitespace)
			// https://www.w3.org/TR/css-syntax-3/#input-preprocessing
			ret = ret.replace( rtrimCSS, "$1" ) || undefined;
		}

		if ( ret === "" && !isAttached( elem ) ) {
			ret = jQuery.style( elem, name );
		}

		// A tribute to the "awesome hack by Dean Edwards"
		// Android Browser returns percentage for some values,
		// but width seems to be reliably pixels.
		// This is against the CSSOM draft spec:
		// https://drafts.csswg.org/cssom/#resolved-values
		if ( !support.pixelBoxStyles() && rnumnonpx.test( ret ) && rboxStyle.test( name ) ) {

			// Remember the original values
			width = style.width;
			minWidth = style.minWidth;
			maxWidth = style.maxWidth;

			// Put in the new values to get a computed value out
			style.minWidth = style.maxWidth = style.width = ret;
			ret = computed.width;

			// Revert the changed values
			style.width = width;
			style.minWidth = minWidth;
			style.maxWidth = maxWidth;
		}
	}

	return ret !== undefined ?

		// Support: IE <=9 - 11 only
		// IE returns zIndex value as an integer.
		ret + "" :
		ret;
}


function addGetHookIf( conditionFn, hookFn ) {

	// Define the hook, we'll check on the first run if it's really needed.
	return {
		get: function() {
			if ( conditionFn() ) {

				// Hook not needed (or it's not possible to use it due
				// to missing dependency), remove it.
				delete this.get;
				return;
			}

			// Hook needed; redefine it so that the support test is not executed again.
			return ( this.get = hookFn ).apply( this, arguments );
		}
	};
}


var cssPrefixes = [ "Webkit", "Moz", "ms" ],
	emptyStyle = document.createElement( "div" ).style,
	vendorProps = {};

// Return a vendor-prefixed property or undefined
function vendorPropName( name ) {

	// Check for vendor prefixed names
	var capName = name[ 0 ].toUpperCase() + name.slice( 1 ),
		i = cssPrefixes.length;

	while ( i-- ) {
		name = cssPrefixes[ i ] + capName;
		if ( name in emptyStyle ) {
			return name;
		}
	}
}

// Return a potentially-mapped jQuery.cssProps or vendor prefixed property
function finalPropName( name ) {
	var final = jQuery.cssProps[ name ] || vendorProps[ name ];

	if ( final ) {
		return final;
	}
	if ( name in emptyStyle ) {
		return name;
	}
	return vendorProps[ name ] = vendorPropName( name ) || name;
}


var

	// Swappable if display is none or starts with table
	// except "table", "table-cell", or "table-caption"
	// See here for display values: https://developer.mozilla.org/en-US/docs/CSS/display
	rdisplayswap = /^(none|table(?!-c[ea]).+)/,
	cssShow = { position: "absolute", visibility: "hidden", display: "block" },
	cssNormalTransform = {
		letterSpacing: "0",
		fontWeight: "400"
	};

function setPositiveNumber( _elem, value, subtract ) {

	// Any relative (+/-) values have already been
	// normalized at this point
	var matches = rcssNum.exec( value );
	return matches ?

		// Guard against undefined "subtract", e.g., when used as in cssHooks
		Math.max( 0, matches[ 2 ] - ( subtract || 0 ) ) + ( matches[ 3 ] || "px" ) :
		value;
}

function boxModelAdjustment( elem, dimension, box, isBorderBox, styles, computedVal ) {
	var i = dimension === "width" ? 1 : 0,
		extra = 0,
		delta = 0,
		marginDelta = 0;

	// Adjustment may not be necessary
	if ( box === ( isBorderBox ? "border" : "content" ) ) {
		return 0;
	}

	for ( ; i < 4; i += 2 ) {

		// Both box models exclude margin
		// Count margin delta separately to only add it after scroll gutter adjustment.
		// This is needed to make negative margins work with `outerHeight( true )` (gh-3982).
		if ( box === "margin" ) {
			marginDelta += jQuery.css( elem, box + cssExpand[ i ], true, styles );
		}

		// If we get here with a content-box, we're seeking "padding" or "border" or "margin"
		if ( !isBorderBox ) {

			// Add padding
			delta += jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );

			// For "border" or "margin", add border
			if ( box !== "padding" ) {
				delta += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );

			// But still keep track of it otherwise
			} else {
				extra += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
			}

		// If we get here with a border-box (content + padding + border), we're seeking "content" or
		// "padding" or "margin"
		} else {

			// For "content", subtract padding
			if ( box === "content" ) {
				delta -= jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );
			}

			// For "content" or "padding", subtract border
			if ( box !== "margin" ) {
				delta -= jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
			}
		}
	}

	// Account for positive content-box scroll gutter when requested by providing computedVal
	if ( !isBorderBox && computedVal >= 0 ) {

		// offsetWidth/offsetHeight is a rounded sum of content, padding, scroll gutter, and border
		// Assuming integer scroll gutter, subtract the rest and round down
		delta += Math.max( 0, Math.ceil(
			elem[ "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 ) ] -
			computedVal -
			delta -
			extra -
			0.5

		// If offsetWidth/offsetHeight is unknown, then we can't determine content-box scroll gutter
		// Use an explicit zero to avoid NaN (gh-3964)
		) ) || 0;
	}

	return delta + marginDelta;
}

function getWidthOrHeight( elem, dimension, extra ) {

	// Start with computed style
	var styles = getStyles( elem ),

		// To avoid forcing a reflow, only fetch boxSizing if we need it (gh-4322).
		// Fake content-box until we know it's needed to know the true value.
		boxSizingNeeded = !support.boxSizingReliable() || extra,
		isBorderBox = boxSizingNeeded &&
			jQuery.css( elem, "boxSizing", false, styles ) === "border-box",
		valueIsBorderBox = isBorderBox,

		val = curCSS( elem, dimension, styles ),
		offsetProp = "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 );

	// Support: Firefox <=54
	// Return a confounding non-pixel value or feign ignorance, as appropriate.
	if ( rnumnonpx.test( val ) ) {
		if ( !extra ) {
			return val;
		}
		val = "auto";
	}


	// Support: IE 9 - 11 only
	// Use offsetWidth/offsetHeight for when box sizing is unreliable.
	// In those cases, the computed value can be trusted to be border-box.
	if ( ( !support.boxSizingReliable() && isBorderBox ||

		// Support: IE 10 - 11+, Edge 15 - 18+
		// IE/Edge misreport `getComputedStyle` of table rows with width/height
		// set in CSS while `offset*` properties report correct values.
		// Interestingly, in some cases IE 9 doesn't suffer from this issue.
		!support.reliableTrDimensions() && nodeName( elem, "tr" ) ||

		// Fall back to offsetWidth/offsetHeight when value is "auto"
		// This happens for inline elements with no explicit setting (gh-3571)
		val === "auto" ||

		// Support: Android <=4.1 - 4.3 only
		// Also use offsetWidth/offsetHeight for misreported inline dimensions (gh-3602)
		!parseFloat( val ) && jQuery.css( elem, "display", false, styles ) === "inline" ) &&

		// Make sure the element is visible & connected
		elem.getClientRects().length ) {

		isBorderBox = jQuery.css( elem, "boxSizing", false, styles ) === "border-box";

		// Where available, offsetWidth/offsetHeight approximate border box dimensions.
		// Where not available (e.g., SVG), assume unreliable box-sizing and interpret the
		// retrieved value as a content box dimension.
		valueIsBorderBox = offsetProp in elem;
		if ( valueIsBorderBox ) {
			val = elem[ offsetProp ];
		}
	}

	// Normalize "" and auto
	val = parseFloat( val ) || 0;

	// Adjust for the element's box model
	return ( val +
		boxModelAdjustment(
			elem,
			dimension,
			extra || ( isBorderBox ? "border" : "content" ),
			valueIsBorderBox,
			styles,

			// Provide the current computed size to request scroll gutter calculation (gh-3589)
			val
		)
	) + "px";
}

jQuery.extend( {

	// Add in style property hooks for overriding the default
	// behavior of getting and setting a style property
	cssHooks: {
		opacity: {
			get: function( elem, computed ) {
				if ( computed ) {

					// We should always get a number back from opacity
					var ret = curCSS( elem, "opacity" );
					return ret === "" ? "1" : ret;
				}
			}
		}
	},

	// Don't automatically add "px" to these possibly-unitless properties
	cssNumber: {
		animationIterationCount: true,
		aspectRatio: true,
		borderImageSlice: true,
		columnCount: true,
		flexGrow: true,
		flexShrink: true,
		fontWeight: true,
		gridArea: true,
		gridColumn: true,
		gridColumnEnd: true,
		gridColumnStart: true,
		gridRow: true,
		gridRowEnd: true,
		gridRowStart: true,
		lineHeight: true,
		opacity: true,
		order: true,
		orphans: true,
		scale: true,
		widows: true,
		zIndex: true,
		zoom: true,

		// SVG-related
		fillOpacity: true,
		floodOpacity: true,
		stopOpacity: true,
		strokeMiterlimit: true,
		strokeOpacity: true
	},

	// Add in properties whose names you wish to fix before
	// setting or getting the value
	cssProps: {},

	// Get and set the style property on a DOM Node
	style: function( elem, name, value, extra ) {

		// Don't set styles on text and comment nodes
		if ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {
			return;
		}

		// Make sure that we're working with the right name
		var ret, type, hooks,
			origName = camelCase( name ),
			isCustomProp = rcustomProp.test( name ),
			style = elem.style;

		// Make sure that we're working with the right name. We don't
		// want to query the value if it is a CSS custom property
		// since they are user-defined.
		if ( !isCustomProp ) {
			name = finalPropName( origName );
		}

		// Gets hook for the prefixed version, then unprefixed version
		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

		// Check if we're setting a value
		if ( value !== undefined ) {
			type = typeof value;

			// Convert "+=" or "-=" to relative numbers (trac-7345)
			if ( type === "string" && ( ret = rcssNum.exec( value ) ) && ret[ 1 ] ) {
				value = adjustCSS( elem, name, ret );

				// Fixes bug trac-9237
				type = "number";
			}

			// Make sure that null and NaN values aren't set (trac-7116)
			if ( value == null || value !== value ) {
				return;
			}

			// If a number was passed in, add the unit (except for certain CSS properties)
			// The isCustomProp check can be removed in jQuery 4.0 when we only auto-append
			// "px" to a few hardcoded values.
			if ( type === "number" && !isCustomProp ) {
				value += ret && ret[ 3 ] || ( jQuery.cssNumber[ origName ] ? "" : "px" );
			}

			// background-* props affect original clone's values
			if ( !support.clearCloneStyle && value === "" && name.indexOf( "background" ) === 0 ) {
				style[ name ] = "inherit";
			}

			// If a hook was provided, use that value, otherwise just set the specified value
			if ( !hooks || !( "set" in hooks ) ||
				( value = hooks.set( elem, value, extra ) ) !== undefined ) {

				if ( isCustomProp ) {
					style.setProperty( name, value );
				} else {
					style[ name ] = value;
				}
			}

		} else {

			// If a hook was provided get the non-computed value from there
			if ( hooks && "get" in hooks &&
				( ret = hooks.get( elem, false, extra ) ) !== undefined ) {

				return ret;
			}

			// Otherwise just get the value from the style object
			return style[ name ];
		}
	},

	css: function( elem, name, extra, styles ) {
		var val, num, hooks,
			origName = camelCase( name ),
			isCustomProp = rcustomProp.test( name );

		// Make sure that we're working with the right name. We don't
		// want to modify the value if it is a CSS custom property
		// since they are user-defined.
		if ( !isCustomProp ) {
			name = finalPropName( origName );
		}

		// Try prefixed name followed by the unprefixed name
		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

		// If a hook was provided get the computed value from there
		if ( hooks && "get" in hooks ) {
			val = hooks.get( elem, true, extra );
		}

		// Otherwise, if a way to get the computed value exists, use that
		if ( val === undefined ) {
			val = curCSS( elem, name, styles );
		}

		// Convert "normal" to computed value
		if ( val === "normal" && name in cssNormalTransform ) {
			val = cssNormalTransform[ name ];
		}

		// Make numeric if forced or a qualifier was provided and val looks numeric
		if ( extra === "" || extra ) {
			num = parseFloat( val );
			return extra === true || isFinite( num ) ? num || 0 : val;
		}

		return val;
	}
} );

jQuery.each( [ "height", "width" ], function( _i, dimension ) {
	jQuery.cssHooks[ dimension ] = {
		get: function( elem, computed, extra ) {
			if ( computed ) {

				// Certain elements can have dimension info if we invisibly show them
				// but it must have a current display style that would benefit
				return rdisplayswap.test( jQuery.css( elem, "display" ) ) &&

					// Support: Safari 8+
					// Table columns in Safari have non-zero offsetWidth & zero
					// getBoundingClientRect().width unless display is changed.
					// Support: IE <=11 only
					// Running getBoundingClientRect on a disconnected node
					// in IE throws an error.
					( !elem.getClientRects().length || !elem.getBoundingClientRect().width ) ?
					swap( elem, cssShow, function() {
						return getWidthOrHeight( elem, dimension, extra );
					} ) :
					getWidthOrHeight( elem, dimension, extra );
			}
		},

		set: function( elem, value, extra ) {
			var matches,
				styles = getStyles( elem ),

				// Only read styles.position if the test has a chance to fail
				// to avoid forcing a reflow.
				scrollboxSizeBuggy = !support.scrollboxSize() &&
					styles.position === "absolute",

				// To avoid forcing a reflow, only fetch boxSizing if we need it (gh-3991)
				boxSizingNeeded = scrollboxSizeBuggy || extra,
				isBorderBox = boxSizingNeeded &&
					jQuery.css( elem, "boxSizing", false, styles ) === "border-box",
				subtract = extra ?
					boxModelAdjustment(
						elem,
						dimension,
						extra,
						isBorderBox,
						styles
					) :
					0;

			// Account for unreliable border-box dimensions by comparing offset* to computed and
			// faking a content-box to get border and padding (gh-3699)
			if ( isBorderBox && scrollboxSizeBuggy ) {
				subtract -= Math.ceil(
					elem[ "offset" + dimension[ 0 ].toUpperCase() + dimension.slice( 1 ) ] -
					parseFloat( styles[ dimension ] ) -
					boxModelAdjustment( elem, dimension, "border", false, styles ) -
					0.5
				);
			}

			// Convert to pixels if value adjustment is needed
			if ( subtract && ( matches = rcssNum.exec( value ) ) &&
				( matches[ 3 ] || "px" ) !== "px" ) {

				elem.style[ dimension ] = value;
				value = jQuery.css( elem, dimension );
			}

			return setPositiveNumber( elem, value, subtract );
		}
	};
} );

jQuery.cssHooks.marginLeft = addGetHookIf( support.reliableMarginLeft,
	function( elem, computed ) {
		if ( computed ) {
			return ( parseFloat( curCSS( elem, "marginLeft" ) ) ||
				elem.getBoundingClientRect().left -
					swap( elem, { marginLeft: 0 }, function() {
						return elem.getBoundingClientRect().left;
					} )
			) + "px";
		}
	}
);

// These hooks are used by animate to expand properties
jQuery.each( {
	margin: "",
	padding: "",
	border: "Width"
}, function( prefix, suffix ) {
	jQuery.cssHooks[ prefix + suffix ] = {
		expand: function( value ) {
			var i = 0,
				expanded = {},

				// Assumes a single number if not a string
				parts = typeof value === "string" ? value.split( " " ) : [ value ];

			for ( ; i < 4; i++ ) {
				expanded[ prefix + cssExpand[ i ] + suffix ] =
					parts[ i ] || parts[ i - 2 ] || parts[ 0 ];
			}

			return expanded;
		}
	};

	if ( prefix !== "margin" ) {
		jQuery.cssHooks[ prefix + suffix ].set = setPositiveNumber;
	}
} );

jQuery.fn.extend( {
	css: function( name, value ) {
		return access( this, function( elem, name, value ) {
			var styles, len,
				map = {},
				i = 0;

			if ( Array.isArray( name ) ) {
				styles = getStyles( elem );
				len = name.length;

				for ( ; i < len; i++ ) {
					map[ name[ i ] ] = jQuery.css( elem, name[ i ], false, styles );
				}

				return map;
			}

			return value !== undefined ?
				jQuery.style( elem, name, value ) :
				jQuery.css( elem, name );
		}, name, value, arguments.length > 1 );
	}
} );


function Tween( elem, options, prop, end, easing ) {
	return new Tween.prototype.init( elem, options, prop, end, easing );
}
jQuery.Tween = Tween;

Tween.prototype = {
	constructor: Tween,
	init: function( elem, options, prop, end, easing, unit ) {
		this.elem = elem;
		this.prop = prop;
		this.easing = easing || jQuery.easing._default;
		this.options = options;
		this.start = this.now = this.cur();
		this.end = end;
		this.unit = unit || ( jQuery.cssNumber[ prop ] ? "" : "px" );
	},
	cur: function() {
		var hooks = Tween.propHooks[ this.prop ];

		return hooks && hooks.get ?
			hooks.get( this ) :
			Tween.propHooks._default.get( this );
	},
	run: function( percent ) {
		var eased,
			hooks = Tween.propHooks[ this.prop ];

		if ( this.options.duration ) {
			this.pos = eased = jQuery.easing[ this.easing ](
				percent, this.options.duration * percent, 0, 1, this.options.duration
			);
		} else {
			this.pos = eased = percent;
		}
		this.now = ( this.end - this.start ) * eased + this.start;

		if ( this.options.step ) {
			this.options.step.call( this.elem, this.now, this );
		}

		if ( hooks && hooks.set ) {
			hooks.set( this );
		} else {
			Tween.propHooks._default.set( this );
		}
		return this;
	}
};

Tween.prototype.init.prototype = Tween.prototype;

Tween.propHooks = {
	_default: {
		get: function( tween ) {
			var result;

			// Use a property on the element directly when it is not a DOM element,
			// or when there is no matching style property that exists.
			if ( tween.elem.nodeType !== 1 ||
				tween.elem[ tween.prop ] != null && tween.elem.style[ tween.prop ] == null ) {
				return tween.elem[ tween.prop ];
			}

			// Passing an empty string as a 3rd parameter to .css will automatically
			// attempt a parseFloat and fallback to a string if the parse fails.
			// Simple values such as "10px" are parsed to Float;
			// complex values such as "rotate(1rad)" are returned as-is.
			result = jQuery.css( tween.elem, tween.prop, "" );

			// Empty strings, null, undefined and "auto" are converted to 0.
			return !result || result === "auto" ? 0 : result;
		},
		set: function( tween ) {

			// Use step hook for back compat.
			// Use cssHook if its there.
			// Use .style if available and use plain properties where available.
			if ( jQuery.fx.step[ tween.prop ] ) {
				jQuery.fx.step[ tween.prop ]( tween );
			} else if ( tween.elem.nodeType === 1 && (
				jQuery.cssHooks[ tween.prop ] ||
					tween.elem.style[ finalPropName( tween.prop ) ] != null ) ) {
				jQuery.style( tween.elem, tween.prop, tween.now + tween.unit );
			} else {
				tween.elem[ tween.prop ] = tween.now;
			}
		}
	}
};

// Support: IE <=9 only
// Panic based approach to setting things on disconnected nodes
Tween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {
	set: function( tween ) {
		if ( tween.elem.nodeType && tween.elem.parentNode ) {
			tween.elem[ tween.prop ] = tween.now;
		}
	}
};

jQuery.easing = {
	linear: function( p ) {
		return p;
	},
	swing: function( p ) {
		return 0.5 - Math.cos( p * Math.PI ) / 2;
	},
	_default: "swing"
};

jQuery.fx = Tween.prototype.init;

// Back compat <1.8 extension point
jQuery.fx.step = {};




var
	fxNow, inProgress,
	rfxtypes = /^(?:toggle|show|hide)$/,
	rrun = /queueHooks$/;

function schedule() {
	if ( inProgress ) {
		if ( document.hidden === false && window.requestAnimationFrame ) {
			window.requestAnimationFrame( schedule );
		} else {
			window.setTimeout( schedule, jQuery.fx.interval );
		}

		jQuery.fx.tick();
	}
}

// Animations created synchronously will run synchronously
function createFxNow() {
	window.setTimeout( function() {
		fxNow = undefined;
	} );
	return ( fxNow = Date.now() );
}

// Generate parameters to create a standard animation
function genFx( type, includeWidth ) {
	var which,
		i = 0,
		attrs = { height: type };

	// If we include width, step value is 1 to do all cssExpand values,
	// otherwise step value is 2 to skip over Left and Right
	includeWidth = includeWidth ? 1 : 0;
	for ( ; i < 4; i += 2 - includeWidth ) {
		which = cssExpand[ i ];
		attrs[ "margin" + which ] = attrs[ "padding" + which ] = type;
	}

	if ( includeWidth ) {
		attrs.opacity = attrs.width = type;
	}

	return attrs;
}

function createTween( value, prop, animation ) {
	var tween,
		collection = ( Animation.tweeners[ prop ] || [] ).concat( Animation.tweeners[ "*" ] ),
		index = 0,
		length = collection.length;
	for ( ; index < length; index++ ) {
		if ( ( tween = collection[ index ].call( animation, prop, value ) ) ) {

			// We're done with this property
			return tween;
		}
	}
}

function defaultPrefilter( elem, props, opts ) {
	var prop, value, toggle, hooks, oldfire, propTween, restoreDisplay, display,
		isBox = "width" in props || "height" in props,
		anim = this,
		orig = {},
		style = elem.style,
		hidden = elem.nodeType && isHiddenWithinTree( elem ),
		dataShow = dataPriv.get( elem, "fxshow" );

	// Queue-skipping animations hijack the fx hooks
	if ( !opts.queue ) {
		hooks = jQuery._queueHooks( elem, "fx" );
		if ( hooks.unqueued == null ) {
			hooks.unqueued = 0;
			oldfire = hooks.empty.fire;
			hooks.empty.fire = function() {
				if ( !hooks.unqueued ) {
					oldfire();
				}
			};
		}
		hooks.unqueued++;

		anim.always( function() {

			// Ensure the complete handler is called before this completes
			anim.always( function() {
				hooks.unqueued--;
				if ( !jQuery.queue( elem, "fx" ).length ) {
					hooks.empty.fire();
				}
			} );
		} );
	}

	// Detect show/hide animations
	for ( prop in props ) {
		value = props[ prop ];
		if ( rfxtypes.test( value ) ) {
			delete props[ prop ];
			toggle = toggle || value === "toggle";
			if ( value === ( hidden ? "hide" : "show" ) ) {

				// Pretend to be hidden if this is a "show" and
				// there is still data from a stopped show/hide
				if ( value === "show" && dataShow && dataShow[ prop ] !== undefined ) {
					hidden = true;

				// Ignore all other no-op show/hide data
				} else {
					continue;
				}
			}
			orig[ prop ] = dataShow && dataShow[ prop ] || jQuery.style( elem, prop );
		}
	}

	// Bail out if this is a no-op like .hide().hide()
	propTween = !jQuery.isEmptyObject( props );
	if ( !propTween && jQuery.isEmptyObject( orig ) ) {
		return;
	}

	// Restrict "overflow" and "display" styles during box animations
	if ( isBox && elem.nodeType === 1 ) {

		// Support: IE <=9 - 11, Edge 12 - 15
		// Record all 3 overflow attributes because IE does not infer the shorthand
		// from identically-valued overflowX and overflowY and Edge just mirrors
		// the overflowX value there.
		opts.overflow = [ style.overflow, style.overflowX, style.overflowY ];

		// Identify a display type, preferring old show/hide data over the CSS cascade
		restoreDisplay = dataShow && dataShow.display;
		if ( restoreDisplay == null ) {
			restoreDisplay = dataPriv.get( elem, "display" );
		}
		display = jQuery.css( elem, "display" );
		if ( display === "none" ) {
			if ( restoreDisplay ) {
				display = restoreDisplay;
			} else {

				// Get nonempty value(s) by temporarily forcing visibility
				showHide( [ elem ], true );
				restoreDisplay = elem.style.display || restoreDisplay;
				display = jQuery.css( elem, "display" );
				showHide( [ elem ] );
			}
		}

		// Animate inline elements as inline-block
		if ( display === "inline" || display === "inline-block" && restoreDisplay != null ) {
			if ( jQuery.css( elem, "float" ) === "none" ) {

				// Restore the original display value at the end of pure show/hide animations
				if ( !propTween ) {
					anim.done( function() {
						style.display = restoreDisplay;
					} );
					if ( restoreDisplay == null ) {
						display = style.display;
						restoreDisplay = display === "none" ? "" : display;
					}
				}
				style.display = "inline-block";
			}
		}
	}

	if ( opts.overflow ) {
		style.overflow = "hidden";
		anim.always( function() {
			style.overflow = opts.overflow[ 0 ];
			style.overflowX = opts.overflow[ 1 ];
			style.overflowY = opts.overflow[ 2 ];
		} );
	}

	// Implement show/hide animations
	propTween = false;
	for ( prop in orig ) {

		// General show/hide setup for this element animation
		if ( !propTween ) {
			if ( dataShow ) {
				if ( "hidden" in dataShow ) {
					hidden = dataShow.hidden;
				}
			} else {
				dataShow = dataPriv.access( elem, "fxshow", { display: restoreDisplay } );
			}

			// Store hidden/visible for toggle so `.stop().toggle()` "reverses"
			if ( toggle ) {
				dataShow.hidden = !hidden;
			}

			// Show elements before animating them
			if ( hidden ) {
				showHide( [ elem ], true );
			}

			/* eslint-disable no-loop-func */

			anim.done( function() {

				/* eslint-enable no-loop-func */

				// The final step of a "hide" animation is actually hiding the element
				if ( !hidden ) {
					showHide( [ elem ] );
				}
				dataPriv.remove( elem, "fxshow" );
				for ( prop in orig ) {
					jQuery.style( elem, prop, orig[ prop ] );
				}
			} );
		}

		// Per-property setup
		propTween = createTween( hidden ? dataShow[ prop ] : 0, prop, anim );
		if ( !( prop in dataShow ) ) {
			dataShow[ prop ] = propTween.start;
			if ( hidden ) {
				propTween.end = propTween.start;
				propTween.start = 0;
			}
		}
	}
}

function propFilter( props, specialEasing ) {
	var index, name, easing, value, hooks;

	// camelCase, specialEasing and expand cssHook pass
	for ( index in props ) {
		name = camelCase( index );
		easing = specialEasing[ name ];
		value = props[ index ];
		if ( Array.isArray( value ) ) {
			easing = value[ 1 ];
			value = props[ index ] = value[ 0 ];
		}

		if ( index !== name ) {
			props[ name ] = value;
			delete props[ index ];
		}

		hooks = jQuery.cssHooks[ name ];
		if ( hooks && "expand" in hooks ) {
			value = hooks.expand( value );
			delete props[ name ];

			// Not quite $.extend, this won't overwrite existing keys.
			// Reusing 'index' because we have the correct "name"
			for ( index in value ) {
				if ( !( index in props ) ) {
					props[ index ] = value[ index ];
					specialEasing[ index ] = easing;
				}
			}
		} else {
			specialEasing[ name ] = easing;
		}
	}
}

function Animation( elem, properties, options ) {
	var result,
		stopped,
		index = 0,
		length = Animation.prefilters.length,
		deferred = jQuery.Deferred().always( function() {

			// Don't match elem in the :animated selector
			delete tick.elem;
		} ),
		tick = function() {
			if ( stopped ) {
				return false;
			}
			var currentTime = fxNow || createFxNow(),
				remaining = Math.max( 0, animation.startTime + animation.duration - currentTime ),

				// Support: Android 2.3 only
				// Archaic crash bug won't allow us to use `1 - ( 0.5 || 0 )` (trac-12497)
				temp = remaining / animation.duration || 0,
				percent = 1 - temp,
				index = 0,
				length = animation.tweens.length;

			for ( ; index < length; index++ ) {
				animation.tweens[ index ].run( percent );
			}

			deferred.notifyWith( elem, [ animation, percent, remaining ] );

			// If there's more to do, yield
			if ( percent < 1 && length ) {
				return remaining;
			}

			// If this was an empty animation, synthesize a final progress notification
			if ( !length ) {
				deferred.notifyWith( elem, [ animation, 1, 0 ] );
			}

			// Resolve the animation and report its conclusion
			deferred.resolveWith( elem, [ animation ] );
			return false;
		},
		animation = deferred.promise( {
			elem: elem,
			props: jQuery.extend( {}, properties ),
			opts: jQuery.extend( true, {
				specialEasing: {},
				easing: jQuery.easing._default
			}, options ),
			originalProperties: properties,
			originalOptions: options,
			startTime: fxNow || createFxNow(),
			duration: options.duration,
			tweens: [],
			createTween: function( prop, end ) {
				var tween = jQuery.Tween( elem, animation.opts, prop, end,
					animation.opts.specialEasing[ prop ] || animation.opts.easing );
				animation.tweens.push( tween );
				return tween;
			},
			stop: function( gotoEnd ) {
				var index = 0,

					// If we are going to the end, we want to run all the tweens
					// otherwise we skip this part
					length = gotoEnd ? animation.tweens.length : 0;
				if ( stopped ) {
					return this;
				}
				stopped = true;
				for ( ; index < length; index++ ) {
					animation.tweens[ index ].run( 1 );
				}

				// Resolve when we played the last frame; otherwise, reject
				if ( gotoEnd ) {
					deferred.notifyWith( elem, [ animation, 1, 0 ] );
					deferred.resolveWith( elem, [ animation, gotoEnd ] );
				} else {
					deferred.rejectWith( elem, [ animation, gotoEnd ] );
				}
				return this;
			}
		} ),
		props = animation.props;

	propFilter( props, animation.opts.specialEasing );

	for ( ; index < length; index++ ) {
		result = Animation.prefilters[ index ].call( animation, elem, props, animation.opts );
		if ( result ) {
			if ( isFunction( result.stop ) ) {
				jQuery._queueHooks( animation.elem, animation.opts.queue ).stop =
					result.stop.bind( result );
			}
			return result;
		}
	}

	jQuery.map( props, createTween, animation );

	if ( isFunction( animation.opts.start ) ) {
		animation.opts.start.call( elem, animation );
	}

	// Attach callbacks from options
	animation
		.progress( animation.opts.progress )
		.done( animation.opts.done, animation.opts.complete )
		.fail( animation.opts.fail )
		.always( animation.opts.always );

	jQuery.fx.timer(
		jQuery.extend( tick, {
			elem: elem,
			anim: animation,
			queue: animation.opts.queue
		} )
	);

	return animation;
}

jQuery.Animation = jQuery.extend( Animation, {

	tweeners: {
		"*": [ function( prop, value ) {
			var tween = this.createTween( prop, value );
			adjustCSS( tween.elem, prop, rcssNum.exec( value ), tween );
			return tween;
		} ]
	},

	tweener: function( props, callback ) {
		if ( isFunction( props ) ) {
			callback = props;
			props = [ "*" ];
		} else {
			props = props.match( rnothtmlwhite );
		}

		var prop,
			index = 0,
			length = props.length;

		for ( ; index < length; index++ ) {
			prop = props[ index ];
			Animation.tweeners[ prop ] = Animation.tweeners[ prop ] || [];
			Animation.tweeners[ prop ].unshift( callback );
		}
	},

	prefilters: [ defaultPrefilter ],

	prefilter: function( callback, prepend ) {
		if ( prepend ) {
			Animation.prefilters.unshift( callback );
		} else {
			Animation.prefilters.push( callback );
		}
	}
} );

jQuery.speed = function( speed, easing, fn ) {
	var opt = speed && typeof speed === "object" ? jQuery.extend( {}, speed ) : {
		complete: fn || !fn && easing ||
			isFunction( speed ) && speed,
		duration: speed,
		easing: fn && easing || easing && !isFunction( easing ) && easing
	};

	// Go to the end state if fx are off
	if ( jQuery.fx.off ) {
		opt.duration = 0;

	} else {
		if ( typeof opt.duration !== "number" ) {
			if ( opt.duration in jQuery.fx.speeds ) {
				opt.duration = jQuery.fx.speeds[ opt.duration ];

			} else {
				opt.duration = jQuery.fx.speeds._default;
			}
		}
	}

	// Normalize opt.queue - true/undefined/null -> "fx"
	if ( opt.queue == null || opt.queue === true ) {
		opt.queue = "fx";
	}

	// Queueing
	opt.old = opt.complete;

	opt.complete = function() {
		if ( isFunction( opt.old ) ) {
			opt.old.call( this );
		}

		if ( opt.queue ) {
			jQuery.dequeue( this, opt.queue );
		}
	};

	return opt;
};

jQuery.fn.extend( {
	fadeTo: function( speed, to, easing, callback ) {

		// Show any hidden elements after setting opacity to 0
		return this.filter( isHiddenWithinTree ).css( "opacity", 0 ).show()

			// Animate to the value specified
			.end().animate( { opacity: to }, speed, easing, callback );
	},
	animate: function( prop, speed, easing, callback ) {
		var empty = jQuery.isEmptyObject( prop ),
			optall = jQuery.speed( speed, easing, callback ),
			doAnimation = function() {

				// Operate on a copy of prop so per-property easing won't be lost
				var anim = Animation( this, jQuery.extend( {}, prop ), optall );

				// Empty animations, or finishing resolves immediately
				if ( empty || dataPriv.get( this, "finish" ) ) {
					anim.stop( true );
				}
			};

		doAnimation.finish = doAnimation;

		return empty || optall.queue === false ?
			this.each( doAnimation ) :
			this.queue( optall.queue, doAnimation );
	},
	stop: function( type, clearQueue, gotoEnd ) {
		var stopQueue = function( hooks ) {
			var stop = hooks.stop;
			delete hooks.stop;
			stop( gotoEnd );
		};

		if ( typeof type !== "string" ) {
			gotoEnd = clearQueue;
			clearQueue = type;
			type = undefined;
		}
		if ( clearQueue ) {
			this.queue( type || "fx", [] );
		}

		return this.each( function() {
			var dequeue = true,
				index = type != null && type + "queueHooks",
				timers = jQuery.timers,
				data = dataPriv.get( this );

			if ( index ) {
				if ( data[ index ] && data[ index ].stop ) {
					stopQueue( data[ index ] );
				}
			} else {
				for ( index in data ) {
					if ( data[ index ] && data[ index ].stop && rrun.test( index ) ) {
						stopQueue( data[ index ] );
					}
				}
			}

			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this &&
					( type == null || timers[ index ].queue === type ) ) {

					timers[ index ].anim.stop( gotoEnd );
					dequeue = false;
					timers.splice( index, 1 );
				}
			}

			// Start the next in the queue if the last step wasn't forced.
			// Timers currently will call their complete callbacks, which
			// will dequeue but only if they were gotoEnd.
			if ( dequeue || !gotoEnd ) {
				jQuery.dequeue( this, type );
			}
		} );
	},
	finish: function( type ) {
		if ( type !== false ) {
			type = type || "fx";
		}
		return this.each( function() {
			var index,
				data = dataPriv.get( this ),
				queue = data[ type + "queue" ],
				hooks = data[ type + "queueHooks" ],
				timers = jQuery.timers,
				length = queue ? queue.length : 0;

			// Enable finishing flag on private data
			data.finish = true;

			// Empty the queue first
			jQuery.queue( this, type, [] );

			if ( hooks && hooks.stop ) {
				hooks.stop.call( this, true );
			}

			// Look for any active animations, and finish them
			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this && timers[ index ].queue === type ) {
					timers[ index ].anim.stop( true );
					timers.splice( index, 1 );
				}
			}

			// Look for any animations in the old queue and finish them
			for ( index = 0; index < length; index++ ) {
				if ( queue[ index ] && queue[ index ].finish ) {
					queue[ index ].finish.call( this );
				}
			}

			// Turn off finishing flag
			delete data.finish;
		} );
	}
} );

jQuery.each( [ "toggle", "show", "hide" ], function( _i, name ) {
	var cssFn = jQuery.fn[ name ];
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return speed == null || typeof speed === "boolean" ?
			cssFn.apply( this, arguments ) :
			this.animate( genFx( name, true ), speed, easing, callback );
	};
} );

// Generate shortcuts for custom animations
jQuery.each( {
	slideDown: genFx( "show" ),
	slideUp: genFx( "hide" ),
	slideToggle: genFx( "toggle" ),
	fadeIn: { opacity: "show" },
	fadeOut: { opacity: "hide" },
	fadeToggle: { opacity: "toggle" }
}, function( name, props ) {
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return this.animate( props, speed, easing, callback );
	};
} );

jQuery.timers = [];
jQuery.fx.tick = function() {
	var timer,
		i = 0,
		timers = jQuery.timers;

	fxNow = Date.now();

	for ( ; i < timers.length; i++ ) {
		timer = timers[ i ];

		// Run the timer and safely remove it when done (allowing for external removal)
		if ( !timer() && timers[ i ] === timer ) {
			timers.splice( i--, 1 );
		}
	}

	if ( !timers.length ) {
		jQuery.fx.stop();
	}
	fxNow = undefined;
};

jQuery.fx.timer = function( timer ) {
	jQuery.timers.push( timer );
	jQuery.fx.start();
};

jQuery.fx.interval = 13;
jQuery.fx.start = function() {
	if ( inProgress ) {
		return;
	}

	inProgress = true;
	schedule();
};

jQuery.fx.stop = function() {
	inProgress = null;
};

jQuery.fx.speeds = {
	slow: 600,
	fast: 200,

	// Default speed
	_default: 400
};


// Based off of the plugin by Clint Helfers, with permission.
jQuery.fn.delay = function( time, type ) {
	time = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;
	type = type || "fx";

	return this.queue( type, function( next, hooks ) {
		var timeout = window.setTimeout( next, time );
		hooks.stop = function() {
			window.clearTimeout( timeout );
		};
	} );
};


( function() {
	var input = document.createElement( "input" ),
		select = document.createElement( "select" ),
		opt = select.appendChild( document.createElement( "option" ) );

	input.type = "checkbox";

	// Support: Android <=4.3 only
	// Default value for a checkbox should be "on"
	support.checkOn = input.value !== "";

	// Support: IE <=11 only
	// Must access selectedIndex to make default options select
	support.optSelected = opt.selected;

	// Support: IE <=11 only
	// An input loses its value after becoming a radio
	input = document.createElement( "input" );
	input.value = "t";
	input.type = "radio";
	support.radioValue = input.value === "t";
} )();


var boolHook,
	attrHandle = jQuery.expr.attrHandle;

jQuery.fn.extend( {
	attr: function( name, value ) {
		return access( this, jQuery.attr, name, value, arguments.length > 1 );
	},

	removeAttr: function( name ) {
		return this.each( function() {
			jQuery.removeAttr( this, name );
		} );
	}
} );

jQuery.extend( {
	attr: function( elem, name, value ) {
		var ret, hooks,
			nType = elem.nodeType;

		// Don't get/set attributes on text, comment and attribute nodes
		if ( nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		// Fallback to prop when attributes are not supported
		if ( typeof elem.getAttribute === "undefined" ) {
			return jQuery.prop( elem, name, value );
		}

		// Attribute hooks are determined by the lowercase version
		// Grab necessary hook if one is defined
		if ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {
			hooks = jQuery.attrHooks[ name.toLowerCase() ] ||
				( jQuery.expr.match.bool.test( name ) ? boolHook : undefined );
		}

		if ( value !== undefined ) {
			if ( value === null ) {
				jQuery.removeAttr( elem, name );
				return;
			}

			if ( hooks && "set" in hooks &&
				( ret = hooks.set( elem, value, name ) ) !== undefined ) {
				return ret;
			}

			elem.setAttribute( name, value + "" );
			return value;
		}

		if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {
			return ret;
		}

		ret = jQuery.find.attr( elem, name );

		// Non-existent attributes return null, we normalize to undefined
		return ret == null ? undefined : ret;
	},

	attrHooks: {
		type: {
			set: function( elem, value ) {
				if ( !support.radioValue && value === "radio" &&
					nodeName( elem, "input" ) ) {
					var val = elem.value;
					elem.setAttribute( "type", value );
					if ( val ) {
						elem.value = val;
					}
					return value;
				}
			}
		}
	},

	removeAttr: function( elem, value ) {
		var name,
			i = 0,

			// Attribute names can contain non-HTML whitespace characters
			// https://html.spec.whatwg.org/multipage/syntax.html#attributes-2
			attrNames = value && value.match( rnothtmlwhite );

		if ( attrNames && elem.nodeType === 1 ) {
			while ( ( name = attrNames[ i++ ] ) ) {
				elem.removeAttribute( name );
			}
		}
	}
} );

// Hooks for boolean attributes
boolHook = {
	set: function( elem, value, name ) {
		if ( value === false ) {

			// Remove boolean attributes when set to false
			jQuery.removeAttr( elem, name );
		} else {
			elem.setAttribute( name, name );
		}
		return name;
	}
};

jQuery.each( jQuery.expr.match.bool.source.match( /\w+/g ), function( _i, name ) {
	var getter = attrHandle[ name ] || jQuery.find.attr;

	attrHandle[ name ] = function( elem, name, isXML ) {
		var ret, handle,
			lowercaseName = name.toLowerCase();

		if ( !isXML ) {

			// Avoid an infinite loop by temporarily removing this function from the getter
			handle = attrHandle[ lowercaseName ];
			attrHandle[ lowercaseName ] = ret;
			ret = getter( elem, name, isXML ) != null ?
				lowercaseName :
				null;
			attrHandle[ lowercaseName ] = handle;
		}
		return ret;
	};
} );




var rfocusable = /^(?:input|select|textarea|button)$/i,
	rclickable = /^(?:a|area)$/i;

jQuery.fn.extend( {
	prop: function( name, value ) {
		return access( this, jQuery.prop, name, value, arguments.length > 1 );
	},

	removeProp: function( name ) {
		return this.each( function() {
			delete this[ jQuery.propFix[ name ] || name ];
		} );
	}
} );

jQuery.extend( {
	prop: function( elem, name, value ) {
		var ret, hooks,
			nType = elem.nodeType;

		// Don't get/set properties on text, comment and attribute nodes
		if ( nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		if ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {

			// Fix name and attach hooks
			name = jQuery.propFix[ name ] || name;
			hooks = jQuery.propHooks[ name ];
		}

		if ( value !== undefined ) {
			if ( hooks && "set" in hooks &&
				( ret = hooks.set( elem, value, name ) ) !== undefined ) {
				return ret;
			}

			return ( elem[ name ] = value );
		}

		if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {
			return ret;
		}

		return elem[ name ];
	},

	propHooks: {
		tabIndex: {
			get: function( elem ) {

				// Support: IE <=9 - 11 only
				// elem.tabIndex doesn't always return the
				// correct value when it hasn't been explicitly set
				// Use proper attribute retrieval (trac-12072)
				var tabindex = jQuery.find.attr( elem, "tabindex" );

				if ( tabindex ) {
					return parseInt( tabindex, 10 );
				}

				if (
					rfocusable.test( elem.nodeName ) ||
					rclickable.test( elem.nodeName ) &&
					elem.href
				) {
					return 0;
				}

				return -1;
			}
		}
	},

	propFix: {
		"for": "htmlFor",
		"class": "className"
	}
} );

// Support: IE <=11 only
// Accessing the selectedIndex property
// forces the browser to respect setting selected
// on the option
// The getter ensures a default option is selected
// when in an optgroup
// eslint rule "no-unused-expressions" is disabled for this code
// since it considers such accessions noop
if ( !support.optSelected ) {
	jQuery.propHooks.selected = {
		get: function( elem ) {

			/* eslint no-unused-expressions: "off" */

			var parent = elem.parentNode;
			if ( parent && parent.parentNode ) {
				parent.parentNode.selectedIndex;
			}
			return null;
		},
		set: function( elem ) {

			/* eslint no-unused-expressions: "off" */

			var parent = elem.parentNode;
			if ( parent ) {
				parent.selectedIndex;

				if ( parent.parentNode ) {
					parent.parentNode.selectedIndex;
				}
			}
		}
	};
}

jQuery.each( [
	"tabIndex",
	"readOnly",
	"maxLength",
	"cellSpacing",
	"cellPadding",
	"rowSpan",
	"colSpan",
	"useMap",
	"frameBorder",
	"contentEditable"
], function() {
	jQuery.propFix[ this.toLowerCase() ] = this;
} );




	// Strip and collapse whitespace according to HTML spec
	// https://infra.spec.whatwg.org/#strip-and-collapse-ascii-whitespace
	function stripAndCollapse( value ) {
		var tokens = value.match( rnothtmlwhite ) || [];
		return tokens.join( " " );
	}


function getClass( elem ) {
	return elem.getAttribute && elem.getAttribute( "class" ) || "";
}

function classesToArray( value ) {
	if ( Array.isArray( value ) ) {
		return value;
	}
	if ( typeof value === "string" ) {
		return value.match( rnothtmlwhite ) || [];
	}
	return [];
}

jQuery.fn.extend( {
	addClass: function( value ) {
		var classNames, cur, curValue, className, i, finalValue;

		if ( isFunction( value ) ) {
			return this.each( function( j ) {
				jQuery( this ).addClass( value.call( this, j, getClass( this ) ) );
			} );
		}

		classNames = classesToArray( value );

		if ( classNames.length ) {
			return this.each( function() {
				curValue = getClass( this );
				cur = this.nodeType === 1 && ( " " + stripAndCollapse( curValue ) + " " );

				if ( cur ) {
					for ( i = 0; i < classNames.length; i++ ) {
						className = classNames[ i ];
						if ( cur.indexOf( " " + className + " " ) < 0 ) {
							cur += className + " ";
						}
					}

					// Only assign if different to avoid unneeded rendering.
					finalValue = stripAndCollapse( cur );
					if ( curValue !== finalValue ) {
						this.setAttribute( "class", finalValue );
					}
				}
			} );
		}

		return this;
	},

	removeClass: function( value ) {
		var classNames, cur, curValue, className, i, finalValue;

		if ( isFunction( value ) ) {
			return this.each( function( j ) {
				jQuery( this ).removeClass( value.call( this, j, getClass( this ) ) );
			} );
		}

		if ( !arguments.length ) {
			return this.attr( "class", "" );
		}

		classNames = classesToArray( value );

		if ( classNames.length ) {
			return this.each( function() {
				curValue = getClass( this );

				// This expression is here for better compressibility (see addClass)
				cur = this.nodeType === 1 && ( " " + stripAndCollapse( curValue ) + " " );

				if ( cur ) {
					for ( i = 0; i < classNames.length; i++ ) {
						className = classNames[ i ];

						// Remove *all* instances
						while ( cur.indexOf( " " + className + " " ) > -1 ) {
							cur = cur.replace( " " + className + " ", " " );
						}
					}

					// Only assign if different to avoid unneeded rendering.
					finalValue = stripAndCollapse( cur );
					if ( curValue !== finalValue ) {
						this.setAttribute( "class", finalValue );
					}
				}
			} );
		}

		return this;
	},

	toggleClass: function( value, stateVal ) {
		var classNames, className, i, self,
			type = typeof value,
			isValidValue = type === "string" || Array.isArray( value );

		if ( isFunction( value ) ) {
			return this.each( function( i ) {
				jQuery( this ).toggleClass(
					value.call( this, i, getClass( this ), stateVal ),
					stateVal
				);
			} );
		}

		if ( typeof stateVal === "boolean" && isValidValue ) {
			return stateVal ? this.addClass( value ) : this.removeClass( value );
		}

		classNames = classesToArray( value );

		return this.each( function() {
			if ( isValidValue ) {

				// Toggle individual class names
				self = jQuery( this );

				for ( i = 0; i < classNames.length; i++ ) {
					className = classNames[ i ];

					// Check each className given, space separated list
					if ( self.hasClass( className ) ) {
						self.removeClass( className );
					} else {
						self.addClass( className );
					}
				}

			// Toggle whole class name
			} else if ( value === undefined || type === "boolean" ) {
				className = getClass( this );
				if ( className ) {

					// Store className if set
					dataPriv.set( this, "__className__", className );
				}

				// If the element has a class name or if we're passed `false`,
				// then remove the whole classname (if there was one, the above saved it).
				// Otherwise bring back whatever was previously saved (if anything),
				// falling back to the empty string if nothing was stored.
				if ( this.setAttribute ) {
					this.setAttribute( "class",
						className || value === false ?
							"" :
							dataPriv.get( this, "__className__" ) || ""
					);
				}
			}
		} );
	},

	hasClass: function( selector ) {
		var className, elem,
			i = 0;

		className = " " + selector + " ";
		while ( ( elem = this[ i++ ] ) ) {
			if ( elem.nodeType === 1 &&
				( " " + stripAndCollapse( getClass( elem ) ) + " " ).indexOf( className ) > -1 ) {
				return true;
			}
		}

		return false;
	}
} );




var rreturn = /\r/g;

jQuery.fn.extend( {
	val: function( value ) {
		var hooks, ret, valueIsFunction,
			elem = this[ 0 ];

		if ( !arguments.length ) {
			if ( elem ) {
				hooks = jQuery.valHooks[ elem.type ] ||
					jQuery.valHooks[ elem.nodeName.toLowerCase() ];

				if ( hooks &&
					"get" in hooks &&
					( ret = hooks.get( elem, "value" ) ) !== undefined
				) {
					return ret;
				}

				ret = elem.value;

				// Handle most common string cases
				if ( typeof ret === "string" ) {
					return ret.replace( rreturn, "" );
				}

				// Handle cases where value is null/undef or number
				return ret == null ? "" : ret;
			}

			return;
		}

		valueIsFunction = isFunction( value );

		return this.each( function( i ) {
			var val;

			if ( this.nodeType !== 1 ) {
				return;
			}

			if ( valueIsFunction ) {
				val = value.call( this, i, jQuery( this ).val() );
			} else {
				val = value;
			}

			// Treat null/undefined as ""; convert numbers to string
			if ( val == null ) {
				val = "";

			} else if ( typeof val === "number" ) {
				val += "";

			} else if ( Array.isArray( val ) ) {
				val = jQuery.map( val, function( value ) {
					return value == null ? "" : value + "";
				} );
			}

			hooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];

			// If set returns undefined, fall back to normal setting
			if ( !hooks || !( "set" in hooks ) || hooks.set( this, val, "value" ) === undefined ) {
				this.value = val;
			}
		} );
	}
} );

jQuery.extend( {
	valHooks: {
		option: {
			get: function( elem ) {

				var val = jQuery.find.attr( elem, "value" );
				return val != null ?
					val :

					// Support: IE <=10 - 11 only
					// option.text throws exceptions (trac-14686, trac-14858)
					// Strip and collapse whitespace
					// https://html.spec.whatwg.org/#strip-and-collapse-whitespace
					stripAndCollapse( jQuery.text( elem ) );
			}
		},
		select: {
			get: function( elem ) {
				var value, option, i,
					options = elem.options,
					index = elem.selectedIndex,
					one = elem.type === "select-one",
					values = one ? null : [],
					max = one ? index + 1 : options.length;

				if ( index < 0 ) {
					i = max;

				} else {
					i = one ? index : 0;
				}

				// Loop through all the selected options
				for ( ; i < max; i++ ) {
					option = options[ i ];

					// Support: IE <=9 only
					// IE8-9 doesn't update selected after form reset (trac-2551)
					if ( ( option.selected || i === index ) &&

							// Don't return options that are disabled or in a disabled optgroup
							!option.disabled &&
							( !option.parentNode.disabled ||
								!nodeName( option.parentNode, "optgroup" ) ) ) {

						// Get the specific value for the option
						value = jQuery( option ).val();

						// We don't need an array for one selects
						if ( one ) {
							return value;
						}

						// Multi-Selects return an array
						values.push( value );
					}
				}

				return values;
			},

			set: function( elem, value ) {
				var optionSet, option,
					options = elem.options,
					values = jQuery.makeArray( value ),
					i = options.length;

				while ( i-- ) {
					option = options[ i ];

					/* eslint-disable no-cond-assign */

					if ( option.selected =
						jQuery.inArray( jQuery.valHooks.option.get( option ), values ) > -1
					) {
						optionSet = true;
					}

					/* eslint-enable no-cond-assign */
				}

				// Force browsers to behave consistently when non-matching value is set
				if ( !optionSet ) {
					elem.selectedIndex = -1;
				}
				return values;
			}
		}
	}
} );

// Radios and checkboxes getter/setter
jQuery.each( [ "radio", "checkbox" ], function() {
	jQuery.valHooks[ this ] = {
		set: function( elem, value ) {
			if ( Array.isArray( value ) ) {
				return ( elem.checked = jQuery.inArray( jQuery( elem ).val(), value ) > -1 );
			}
		}
	};
	if ( !support.checkOn ) {
		jQuery.valHooks[ this ].get = function( elem ) {
			return elem.getAttribute( "value" ) === null ? "on" : elem.value;
		};
	}
} );




// Return jQuery for attributes-only inclusion
var location = window.location;

var nonce = { guid: Date.now() };

var rquery = ( /\?/ );



// Cross-browser xml parsing
jQuery.parseXML = function( data ) {
	var xml, parserErrorElem;
	if ( !data || typeof data !== "string" ) {
		return null;
	}

	// Support: IE 9 - 11 only
	// IE throws on parseFromString with invalid input.
	try {
		xml = ( new window.DOMParser() ).parseFromString( data, "text/xml" );
	} catch ( e ) {}

	parserErrorElem = xml && xml.getElementsByTagName( "parsererror" )[ 0 ];
	if ( !xml || parserErrorElem ) {
		jQuery.error( "Invalid XML: " + (
			parserErrorElem ?
				jQuery.map( parserErrorElem.childNodes, function( el ) {
					return el.textContent;
				} ).join( "\n" ) :
				data
		) );
	}
	return xml;
};


var rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,
	stopPropagationCallback = function( e ) {
		e.stopPropagation();
	};

jQuery.extend( jQuery.event, {

	trigger: function( event, data, elem, onlyHandlers ) {

		var i, cur, tmp, bubbleType, ontype, handle, special, lastElement,
			eventPath = [ elem || document ],
			type = hasOwn.call( event, "type" ) ? event.type : event,
			namespaces = hasOwn.call( event, "namespace" ) ? event.namespace.split( "." ) : [];

		cur = lastElement = tmp = elem = elem || document;

		// Don't do events on text and comment nodes
		if ( elem.nodeType === 3 || elem.nodeType === 8 ) {
			return;
		}

		// focus/blur morphs to focusin/out; ensure we're not firing them right now
		if ( rfocusMorph.test( type + jQuery.event.triggered ) ) {
			return;
		}

		if ( type.indexOf( "." ) > -1 ) {

			// Namespaced trigger; create a regexp to match event type in handle()
			namespaces = type.split( "." );
			type = namespaces.shift();
			namespaces.sort();
		}
		ontype = type.indexOf( ":" ) < 0 && "on" + type;

		// Caller can pass in a jQuery.Event object, Object, or just an event type string
		event = event[ jQuery.expando ] ?
			event :
			new jQuery.Event( type, typeof event === "object" && event );

		// Trigger bitmask: & 1 for native handlers; & 2 for jQuery (always true)
		event.isTrigger = onlyHandlers ? 2 : 3;
		event.namespace = namespaces.join( "." );
		event.rnamespace = event.namespace ?
			new RegExp( "(^|\\.)" + namespaces.join( "\\.(?:.*\\.|)" ) + "(\\.|$)" ) :
			null;

		// Clean up the event in case it is being reused
		event.result = undefined;
		if ( !event.target ) {
			event.target = elem;
		}

		// Clone any incoming data and prepend the event, creating the handler arg list
		data = data == null ?
			[ event ] :
			jQuery.makeArray( data, [ event ] );

		// Allow special events to draw outside the lines
		special = jQuery.event.special[ type ] || {};
		if ( !onlyHandlers && special.trigger && special.trigger.apply( elem, data ) === false ) {
			return;
		}

		// Determine event propagation path in advance, per W3C events spec (trac-9951)
		// Bubble up to document, then to window; watch for a global ownerDocument var (trac-9724)
		if ( !onlyHandlers && !special.noBubble && !isWindow( elem ) ) {

			bubbleType = special.delegateType || type;
			if ( !rfocusMorph.test( bubbleType + type ) ) {
				cur = cur.parentNode;
			}
			for ( ; cur; cur = cur.parentNode ) {
				eventPath.push( cur );
				tmp = cur;
			}

			// Only add window if we got to document (e.g., not plain obj or detached DOM)
			if ( tmp === ( elem.ownerDocument || document ) ) {
				eventPath.push( tmp.defaultView || tmp.parentWindow || window );
			}
		}

		// Fire handlers on the event path
		i = 0;
		while ( ( cur = eventPath[ i++ ] ) && !event.isPropagationStopped() ) {
			lastElement = cur;
			event.type = i > 1 ?
				bubbleType :
				special.bindType || type;

			// jQuery handler
			handle = ( dataPriv.get( cur, "events" ) || Object.create( null ) )[ event.type ] &&
				dataPriv.get( cur, "handle" );
			if ( handle ) {
				handle.apply( cur, data );
			}

			// Native handler
			handle = ontype && cur[ ontype ];
			if ( handle && handle.apply && acceptData( cur ) ) {
				event.result = handle.apply( cur, data );
				if ( event.result === false ) {
					event.preventDefault();
				}
			}
		}
		event.type = type;

		// If nobody prevented the default action, do it now
		if ( !onlyHandlers && !event.isDefaultPrevented() ) {

			if ( ( !special._default ||
				special._default.apply( eventPath.pop(), data ) === false ) &&
				acceptData( elem ) ) {

				// Call a native DOM method on the target with the same name as the event.
				// Don't do default actions on window, that's where global variables be (trac-6170)
				if ( ontype && isFunction( elem[ type ] ) && !isWindow( elem ) ) {

					// Don't re-trigger an onFOO event when we call its FOO() method
					tmp = elem[ ontype ];

					if ( tmp ) {
						elem[ ontype ] = null;
					}

					// Prevent re-triggering of the same event, since we already bubbled it above
					jQuery.event.triggered = type;

					if ( event.isPropagationStopped() ) {
						lastElement.addEventListener( type, stopPropagationCallback );
					}

					elem[ type ]();

					if ( event.isPropagationStopped() ) {
						lastElement.removeEventListener( type, stopPropagationCallback );
					}

					jQuery.event.triggered = undefined;

					if ( tmp ) {
						elem[ ontype ] = tmp;
					}
				}
			}
		}

		return event.result;
	},

	// Piggyback on a donor event to simulate a different one
	// Used only for `focus(in | out)` events
	simulate: function( type, elem, event ) {
		var e = jQuery.extend(
			new jQuery.Event(),
			event,
			{
				type: type,
				isSimulated: true
			}
		);

		jQuery.event.trigger( e, null, elem );
	}

} );

jQuery.fn.extend( {

	trigger: function( type, data ) {
		return this.each( function() {
			jQuery.event.trigger( type, data, this );
		} );
	},
	triggerHandler: function( type, data ) {
		var elem = this[ 0 ];
		if ( elem ) {
			return jQuery.event.trigger( type, data, elem, true );
		}
	}
} );


var
	rbracket = /\[\]$/,
	rCRLF = /\r?\n/g,
	rsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,
	rsubmittable = /^(?:input|select|textarea|keygen)/i;

function buildParams( prefix, obj, traditional, add ) {
	var name;

	if ( Array.isArray( obj ) ) {

		// Serialize array item.
		jQuery.each( obj, function( i, v ) {
			if ( traditional || rbracket.test( prefix ) ) {

				// Treat each array item as a scalar.
				add( prefix, v );

			} else {

				// Item is non-scalar (array or object), encode its numeric index.
				buildParams(
					prefix + "[" + ( typeof v === "object" && v != null ? i : "" ) + "]",
					v,
					traditional,
					add
				);
			}
		} );

	} else if ( !traditional && toType( obj ) === "object" ) {

		// Serialize object item.
		for ( name in obj ) {
			buildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );
		}

	} else {

		// Serialize scalar item.
		add( prefix, obj );
	}
}

// Serialize an array of form elements or a set of
// key/values into a query string
jQuery.param = function( a, traditional ) {
	var prefix,
		s = [],
		add = function( key, valueOrFunction ) {

			// If value is a function, invoke it and use its return value
			var value = isFunction( valueOrFunction ) ?
				valueOrFunction() :
				valueOrFunction;

			s[ s.length ] = encodeURIComponent( key ) + "=" +
				encodeURIComponent( value == null ? "" : value );
		};

	if ( a == null ) {
		return "";
	}

	// If an array was passed in, assume that it is an array of form elements.
	if ( Array.isArray( a ) || ( a.jquery && !jQuery.isPlainObject( a ) ) ) {

		// Serialize the form elements
		jQuery.each( a, function() {
			add( this.name, this.value );
		} );

	} else {

		// If traditional, encode the "old" way (the way 1.3.2 or older
		// did it), otherwise encode params recursively.
		for ( prefix in a ) {
			buildParams( prefix, a[ prefix ], traditional, add );
		}
	}

	// Return the resulting serialization
	return s.join( "&" );
};

jQuery.fn.extend( {
	serialize: function() {
		return jQuery.param( this.serializeArray() );
	},
	serializeArray: function() {
		return this.map( function() {

			// Can add propHook for "elements" to filter or add form elements
			var elements = jQuery.prop( this, "elements" );
			return elements ? jQuery.makeArray( elements ) : this;
		} ).filter( function() {
			var type = this.type;

			// Use .is( ":disabled" ) so that fieldset[disabled] works
			return this.name && !jQuery( this ).is( ":disabled" ) &&
				rsubmittable.test( this.nodeName ) && !rsubmitterTypes.test( type ) &&
				( this.checked || !rcheckableType.test( type ) );
		} ).map( function( _i, elem ) {
			var val = jQuery( this ).val();

			if ( val == null ) {
				return null;
			}

			if ( Array.isArray( val ) ) {
				return jQuery.map( val, function( val ) {
					return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
				} );
			}

			return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
		} ).get();
	}
} );


var
	r20 = /%20/g,
	rhash = /#.*$/,
	rantiCache = /([?&])_=[^&]*/,
	rheaders = /^(.*?):[ \t]*([^\r\n]*)$/mg,

	// trac-7653, trac-8125, trac-8152: local protocol detection
	rlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,
	rnoContent = /^(?:GET|HEAD)$/,
	rprotocol = /^\/\//,

	/* Prefilters
	 * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
	 * 2) These are called:
	 *    - BEFORE asking for a transport
	 *    - AFTER param serialization (s.data is a string if s.processData is true)
	 * 3) key is the dataType
	 * 4) the catchall symbol "*" can be used
	 * 5) execution will start with transport dataType and THEN continue down to "*" if needed
	 */
	prefilters = {},

	/* Transports bindings
	 * 1) key is the dataType
	 * 2) the catchall symbol "*" can be used
	 * 3) selection will start with transport dataType and THEN go to "*" if needed
	 */
	transports = {},

	// Avoid comment-prolog char sequence (trac-10098); must appease lint and evade compression
	allTypes = "*/".concat( "*" ),

	// Anchor tag for parsing the document origin
	originAnchor = document.createElement( "a" );

originAnchor.href = location.href;

// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
function addToPrefiltersOrTransports( structure ) {

	// dataTypeExpression is optional and defaults to "*"
	return function( dataTypeExpression, func ) {

		if ( typeof dataTypeExpression !== "string" ) {
			func = dataTypeExpression;
			dataTypeExpression = "*";
		}

		var dataType,
			i = 0,
			dataTypes = dataTypeExpression.toLowerCase().match( rnothtmlwhite ) || [];

		if ( isFunction( func ) ) {

			// For each dataType in the dataTypeExpression
			while ( ( dataType = dataTypes[ i++ ] ) ) {

				// Prepend if requested
				if ( dataType[ 0 ] === "+" ) {
					dataType = dataType.slice( 1 ) || "*";
					( structure[ dataType ] = structure[ dataType ] || [] ).unshift( func );

				// Otherwise append
				} else {
					( structure[ dataType ] = structure[ dataType ] || [] ).push( func );
				}
			}
		}
	};
}

// Base inspection function for prefilters and transports
function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {

	var inspected = {},
		seekingTransport = ( structure === transports );

	function inspect( dataType ) {
		var selected;
		inspected[ dataType ] = true;
		jQuery.each( structure[ dataType ] || [], function( _, prefilterOrFactory ) {
			var dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );
			if ( typeof dataTypeOrTransport === "string" &&
				!seekingTransport && !inspected[ dataTypeOrTransport ] ) {

				options.dataTypes.unshift( dataTypeOrTransport );
				inspect( dataTypeOrTransport );
				return false;
			} else if ( seekingTransport ) {
				return !( selected = dataTypeOrTransport );
			}
		} );
		return selected;
	}

	return inspect( options.dataTypes[ 0 ] ) || !inspected[ "*" ] && inspect( "*" );
}

// A special extend for ajax options
// that takes "flat" options (not to be deep extended)
// Fixes trac-9887
function ajaxExtend( target, src ) {
	var key, deep,
		flatOptions = jQuery.ajaxSettings.flatOptions || {};

	for ( key in src ) {
		if ( src[ key ] !== undefined ) {
			( flatOptions[ key ] ? target : ( deep || ( deep = {} ) ) )[ key ] = src[ key ];
		}
	}
	if ( deep ) {
		jQuery.extend( true, target, deep );
	}

	return target;
}

/* Handles responses to an ajax request:
 * - finds the right dataType (mediates between content-type and expected dataType)
 * - returns the corresponding response
 */
function ajaxHandleResponses( s, jqXHR, responses ) {

	var ct, type, finalDataType, firstDataType,
		contents = s.contents,
		dataTypes = s.dataTypes;

	// Remove auto dataType and get content-type in the process
	while ( dataTypes[ 0 ] === "*" ) {
		dataTypes.shift();
		if ( ct === undefined ) {
			ct = s.mimeType || jqXHR.getResponseHeader( "Content-Type" );
		}
	}

	// Check if we're dealing with a known content-type
	if ( ct ) {
		for ( type in contents ) {
			if ( contents[ type ] && contents[ type ].test( ct ) ) {
				dataTypes.unshift( type );
				break;
			}
		}
	}

	// Check to see if we have a response for the expected dataType
	if ( dataTypes[ 0 ] in responses ) {
		finalDataType = dataTypes[ 0 ];
	} else {

		// Try convertible dataTypes
		for ( type in responses ) {
			if ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[ 0 ] ] ) {
				finalDataType = type;
				break;
			}
			if ( !firstDataType ) {
				firstDataType = type;
			}
		}

		// Or just use first one
		finalDataType = finalDataType || firstDataType;
	}

	// If we found a dataType
	// We add the dataType to the list if needed
	// and return the corresponding response
	if ( finalDataType ) {
		if ( finalDataType !== dataTypes[ 0 ] ) {
			dataTypes.unshift( finalDataType );
		}
		return responses[ finalDataType ];
	}
}

/* Chain conversions given the request and the original response
 * Also sets the responseXXX fields on the jqXHR instance
 */
function ajaxConvert( s, response, jqXHR, isSuccess ) {
	var conv2, current, conv, tmp, prev,
		converters = {},

		// Work with a copy of dataTypes in case we need to modify it for conversion
		dataTypes = s.dataTypes.slice();

	// Create converters map with lowercased keys
	if ( dataTypes[ 1 ] ) {
		for ( conv in s.converters ) {
			converters[ conv.toLowerCase() ] = s.converters[ conv ];
		}
	}

	current = dataTypes.shift();

	// Convert to each sequential dataType
	while ( current ) {

		if ( s.responseFields[ current ] ) {
			jqXHR[ s.responseFields[ current ] ] = response;
		}

		// Apply the dataFilter if provided
		if ( !prev && isSuccess && s.dataFilter ) {
			response = s.dataFilter( response, s.dataType );
		}

		prev = current;
		current = dataTypes.shift();

		if ( current ) {

			// There's only work to do if current dataType is non-auto
			if ( current === "*" ) {

				current = prev;

			// Convert response if prev dataType is non-auto and differs from current
			} else if ( prev !== "*" && prev !== current ) {

				// Seek a direct converter
				conv = converters[ prev + " " + current ] || converters[ "* " + current ];

				// If none found, seek a pair
				if ( !conv ) {
					for ( conv2 in converters ) {

						// If conv2 outputs current
						tmp = conv2.split( " " );
						if ( tmp[ 1 ] === current ) {

							// If prev can be converted to accepted input
							conv = converters[ prev + " " + tmp[ 0 ] ] ||
								converters[ "* " + tmp[ 0 ] ];
							if ( conv ) {

								// Condense equivalence converters
								if ( conv === true ) {
									conv = converters[ conv2 ];

								// Otherwise, insert the intermediate dataType
								} else if ( converters[ conv2 ] !== true ) {
									current = tmp[ 0 ];
									dataTypes.unshift( tmp[ 1 ] );
								}
								break;
							}
						}
					}
				}

				// Apply converter (if not an equivalence)
				if ( conv !== true ) {

					// Unless errors are allowed to bubble, catch and return them
					if ( conv && s.throws ) {
						response = conv( response );
					} else {
						try {
							response = conv( response );
						} catch ( e ) {
							return {
								state: "parsererror",
								error: conv ? e : "No conversion from " + prev + " to " + current
							};
						}
					}
				}
			}
		}
	}

	return { state: "success", data: response };
}

jQuery.extend( {

	// Counter for holding the number of active queries
	active: 0,

	// Last-Modified header cache for next request
	lastModified: {},
	etag: {},

	ajaxSettings: {
		url: location.href,
		type: "GET",
		isLocal: rlocalProtocol.test( location.protocol ),
		global: true,
		processData: true,
		async: true,
		contentType: "application/x-www-form-urlencoded; charset=UTF-8",

		/*
		timeout: 0,
		data: null,
		dataType: null,
		username: null,
		password: null,
		cache: null,
		throws: false,
		traditional: false,
		headers: {},
		*/

		accepts: {
			"*": allTypes,
			text: "text/plain",
			html: "text/html",
			xml: "application/xml, text/xml",
			json: "application/json, text/javascript"
		},

		contents: {
			xml: /\bxml\b/,
			html: /\bhtml/,
			json: /\bjson\b/
		},

		responseFields: {
			xml: "responseXML",
			text: "responseText",
			json: "responseJSON"
		},

		// Data converters
		// Keys separate source (or catchall "*") and destination types with a single space
		converters: {

			// Convert anything to text
			"* text": String,

			// Text to html (true = no transformation)
			"text html": true,

			// Evaluate text as a json expression
			"text json": JSON.parse,

			// Parse text as xml
			"text xml": jQuery.parseXML
		},

		// For options that shouldn't be deep extended:
		// you can add your own custom options here if
		// and when you create one that shouldn't be
		// deep extended (see ajaxExtend)
		flatOptions: {
			url: true,
			context: true
		}
	},

	// Creates a full fledged settings object into target
	// with both ajaxSettings and settings fields.
	// If target is omitted, writes into ajaxSettings.
	ajaxSetup: function( target, settings ) {
		return settings ?

			// Building a settings object
			ajaxExtend( ajaxExtend( target, jQuery.ajaxSettings ), settings ) :

			// Extending ajaxSettings
			ajaxExtend( jQuery.ajaxSettings, target );
	},

	ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
	ajaxTransport: addToPrefiltersOrTransports( transports ),

	// Main method
	ajax: function( url, options ) {

		// If url is an object, simulate pre-1.5 signature
		if ( typeof url === "object" ) {
			options = url;
			url = undefined;
		}

		// Force options to be an object
		options = options || {};

		var transport,

			// URL without anti-cache param
			cacheURL,

			// Response headers
			responseHeadersString,
			responseHeaders,

			// timeout handle
			timeoutTimer,

			// Url cleanup var
			urlAnchor,

			// Request state (becomes false upon send and true upon completion)
			completed,

			// To know if global events are to be dispatched
			fireGlobals,

			// Loop variable
			i,

			// uncached part of the url
			uncached,

			// Create the final options object
			s = jQuery.ajaxSetup( {}, options ),

			// Callbacks context
			callbackContext = s.context || s,

			// Context for global events is callbackContext if it is a DOM node or jQuery collection
			globalEventContext = s.context &&
				( callbackContext.nodeType || callbackContext.jquery ) ?
				jQuery( callbackContext ) :
				jQuery.event,

			// Deferreds
			deferred = jQuery.Deferred(),
			completeDeferred = jQuery.Callbacks( "once memory" ),

			// Status-dependent callbacks
			statusCode = s.statusCode || {},

			// Headers (they are sent all at once)
			requestHeaders = {},
			requestHeadersNames = {},

			// Default abort message
			strAbort = "canceled",

			// Fake xhr
			jqXHR = {
				readyState: 0,

				// Builds headers hashtable if needed
				getResponseHeader: function( key ) {
					var match;
					if ( completed ) {
						if ( !responseHeaders ) {
							responseHeaders = {};
							while ( ( match = rheaders.exec( responseHeadersString ) ) ) {
								responseHeaders[ match[ 1 ].toLowerCase() + " " ] =
									( responseHeaders[ match[ 1 ].toLowerCase() + " " ] || [] )
										.concat( match[ 2 ] );
							}
						}
						match = responseHeaders[ key.toLowerCase() + " " ];
					}
					return match == null ? null : match.join( ", " );
				},

				// Raw string
				getAllResponseHeaders: function() {
					return completed ? responseHeadersString : null;
				},

				// Caches the header
				setRequestHeader: function( name, value ) {
					if ( completed == null ) {
						name = requestHeadersNames[ name.toLowerCase() ] =
							requestHeadersNames[ name.toLowerCase() ] || name;
						requestHeaders[ name ] = value;
					}
					return this;
				},

				// Overrides response content-type header
				overrideMimeType: function( type ) {
					if ( completed == null ) {
						s.mimeType = type;
					}
					return this;
				},

				// Status-dependent callbacks
				statusCode: function( map ) {
					var code;
					if ( map ) {
						if ( completed ) {

							// Execute the appropriate callbacks
							jqXHR.always( map[ jqXHR.status ] );
						} else {

							// Lazy-add the new callbacks in a way that preserves old ones
							for ( code in map ) {
								statusCode[ code ] = [ statusCode[ code ], map[ code ] ];
							}
						}
					}
					return this;
				},

				// Cancel the request
				abort: function( statusText ) {
					var finalText = statusText || strAbort;
					if ( transport ) {
						transport.abort( finalText );
					}
					done( 0, finalText );
					return this;
				}
			};

		// Attach deferreds
		deferred.promise( jqXHR );

		// Add protocol if not provided (prefilters might expect it)
		// Handle falsy url in the settings object (trac-10093: consistency with old signature)
		// We also use the url parameter if available
		s.url = ( ( url || s.url || location.href ) + "" )
			.replace( rprotocol, location.protocol + "//" );

		// Alias method option to type as per ticket trac-12004
		s.type = options.method || options.type || s.method || s.type;

		// Extract dataTypes list
		s.dataTypes = ( s.dataType || "*" ).toLowerCase().match( rnothtmlwhite ) || [ "" ];

		// A cross-domain request is in order when the origin doesn't match the current origin.
		if ( s.crossDomain == null ) {
			urlAnchor = document.createElement( "a" );

			// Support: IE <=8 - 11, Edge 12 - 15
			// IE throws exception on accessing the href property if url is malformed,
			// e.g. http://example.com:80x/
			try {
				urlAnchor.href = s.url;

				// Support: IE <=8 - 11 only
				// Anchor's host property isn't correctly set when s.url is relative
				urlAnchor.href = urlAnchor.href;
				s.crossDomain = originAnchor.protocol + "//" + originAnchor.host !==
					urlAnchor.protocol + "//" + urlAnchor.host;
			} catch ( e ) {

				// If there is an error parsing the URL, assume it is crossDomain,
				// it can be rejected by the transport if it is invalid
				s.crossDomain = true;
			}
		}

		// Convert data if not already a string
		if ( s.data && s.processData && typeof s.data !== "string" ) {
			s.data = jQuery.param( s.data, s.traditional );
		}

		// Apply prefilters
		inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

		// If request was aborted inside a prefilter, stop there
		if ( completed ) {
			return jqXHR;
		}

		// We can fire global events as of now if asked to
		// Don't fire events if jQuery.event is undefined in an AMD-usage scenario (trac-15118)
		fireGlobals = jQuery.event && s.global;

		// Watch for a new set of requests
		if ( fireGlobals && jQuery.active++ === 0 ) {
			jQuery.event.trigger( "ajaxStart" );
		}

		// Uppercase the type
		s.type = s.type.toUpperCase();

		// Determine if request has content
		s.hasContent = !rnoContent.test( s.type );

		// Save the URL in case we're toying with the If-Modified-Since
		// and/or If-None-Match header later on
		// Remove hash to simplify url manipulation
		cacheURL = s.url.replace( rhash, "" );

		// More options handling for requests with no content
		if ( !s.hasContent ) {

			// Remember the hash so we can put it back
			uncached = s.url.slice( cacheURL.length );

			// If data is available and should be processed, append data to url
			if ( s.data && ( s.processData || typeof s.data === "string" ) ) {
				cacheURL += ( rquery.test( cacheURL ) ? "&" : "?" ) + s.data;

				// trac-9682: remove data so that it's not used in an eventual retry
				delete s.data;
			}

			// Add or update anti-cache param if needed
			if ( s.cache === false ) {
				cacheURL = cacheURL.replace( rantiCache, "$1" );
				uncached = ( rquery.test( cacheURL ) ? "&" : "?" ) + "_=" + ( nonce.guid++ ) +
					uncached;
			}

			// Put hash and anti-cache on the URL that will be requested (gh-1732)
			s.url = cacheURL + uncached;

		// Change '%20' to '+' if this is encoded form body content (gh-2658)
		} else if ( s.data && s.processData &&
			( s.contentType || "" ).indexOf( "application/x-www-form-urlencoded" ) === 0 ) {
			s.data = s.data.replace( r20, "+" );
		}

		// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
		if ( s.ifModified ) {
			if ( jQuery.lastModified[ cacheURL ] ) {
				jqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ cacheURL ] );
			}
			if ( jQuery.etag[ cacheURL ] ) {
				jqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ cacheURL ] );
			}
		}

		// Set the correct header, if data is being sent
		if ( s.data && s.hasContent && s.contentType !== false || options.contentType ) {
			jqXHR.setRequestHeader( "Content-Type", s.contentType );
		}

		// Set the Accepts header for the server, depending on the dataType
		jqXHR.setRequestHeader(
			"Accept",
			s.dataTypes[ 0 ] && s.accepts[ s.dataTypes[ 0 ] ] ?
				s.accepts[ s.dataTypes[ 0 ] ] +
					( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :
				s.accepts[ "*" ]
		);

		// Check for headers option
		for ( i in s.headers ) {
			jqXHR.setRequestHeader( i, s.headers[ i ] );
		}

		// Allow custom headers/mimetypes and early abort
		if ( s.beforeSend &&
			( s.beforeSend.call( callbackContext, jqXHR, s ) === false || completed ) ) {

			// Abort if not done already and return
			return jqXHR.abort();
		}

		// Aborting is no longer a cancellation
		strAbort = "abort";

		// Install callbacks on deferreds
		completeDeferred.add( s.complete );
		jqXHR.done( s.success );
		jqXHR.fail( s.error );

		// Get transport
		transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

		// If no transport, we auto-abort
		if ( !transport ) {
			done( -1, "No Transport" );
		} else {
			jqXHR.readyState = 1;

			// Send global event
			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );
			}

			// If request was aborted inside ajaxSend, stop there
			if ( completed ) {
				return jqXHR;
			}

			// Timeout
			if ( s.async && s.timeout > 0 ) {
				timeoutTimer = window.setTimeout( function() {
					jqXHR.abort( "timeout" );
				}, s.timeout );
			}

			try {
				completed = false;
				transport.send( requestHeaders, done );
			} catch ( e ) {

				// Rethrow post-completion exceptions
				if ( completed ) {
					throw e;
				}

				// Propagate others as results
				done( -1, e );
			}
		}

		// Callback for when everything is done
		function done( status, nativeStatusText, responses, headers ) {
			var isSuccess, success, error, response, modified,
				statusText = nativeStatusText;

			// Ignore repeat invocations
			if ( completed ) {
				return;
			}

			completed = true;

			// Clear timeout if it exists
			if ( timeoutTimer ) {
				window.clearTimeout( timeoutTimer );
			}

			// Dereference transport for early garbage collection
			// (no matter how long the jqXHR object will be used)
			transport = undefined;

			// Cache response headers
			responseHeadersString = headers || "";

			// Set readyState
			jqXHR.readyState = status > 0 ? 4 : 0;

			// Determine if successful
			isSuccess = status >= 200 && status < 300 || status === 304;

			// Get response data
			if ( responses ) {
				response = ajaxHandleResponses( s, jqXHR, responses );
			}

			// Use a noop converter for missing script but not if jsonp
			if ( !isSuccess &&
				jQuery.inArray( "script", s.dataTypes ) > -1 &&
				jQuery.inArray( "json", s.dataTypes ) < 0 ) {
				s.converters[ "text script" ] = function() {};
			}

			// Convert no matter what (that way responseXXX fields are always set)
			response = ajaxConvert( s, response, jqXHR, isSuccess );

			// If successful, handle type chaining
			if ( isSuccess ) {

				// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
				if ( s.ifModified ) {
					modified = jqXHR.getResponseHeader( "Last-Modified" );
					if ( modified ) {
						jQuery.lastModified[ cacheURL ] = modified;
					}
					modified = jqXHR.getResponseHeader( "etag" );
					if ( modified ) {
						jQuery.etag[ cacheURL ] = modified;
					}
				}

				// if no content
				if ( status === 204 || s.type === "HEAD" ) {
					statusText = "nocontent";

				// if not modified
				} else if ( status === 304 ) {
					statusText = "notmodified";

				// If we have data, let's convert it
				} else {
					statusText = response.state;
					success = response.data;
					error = response.error;
					isSuccess = !error;
				}
			} else {

				// Extract error from statusText and normalize for non-aborts
				error = statusText;
				if ( status || !statusText ) {
					statusText = "error";
					if ( status < 0 ) {
						status = 0;
					}
				}
			}

			// Set data for the fake xhr object
			jqXHR.status = status;
			jqXHR.statusText = ( nativeStatusText || statusText ) + "";

			// Success/Error
			if ( isSuccess ) {
				deferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );
			} else {
				deferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );
			}

			// Status-dependent callbacks
			jqXHR.statusCode( statusCode );
			statusCode = undefined;

			if ( fireGlobals ) {
				globalEventContext.trigger( isSuccess ? "ajaxSuccess" : "ajaxError",
					[ jqXHR, s, isSuccess ? success : error ] );
			}

			// Complete
			completeDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );

			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );

				// Handle the global AJAX counter
				if ( !( --jQuery.active ) ) {
					jQuery.event.trigger( "ajaxStop" );
				}
			}
		}

		return jqXHR;
	},

	getJSON: function( url, data, callback ) {
		return jQuery.get( url, data, callback, "json" );
	},

	getScript: function( url, callback ) {
		return jQuery.get( url, undefined, callback, "script" );
	}
} );

jQuery.each( [ "get", "post" ], function( _i, method ) {
	jQuery[ method ] = function( url, data, callback, type ) {

		// Shift arguments if data argument was omitted
		if ( isFunction( data ) ) {
			type = type || callback;
			callback = data;
			data = undefined;
		}

		// The url can be an options object (which then must have .url)
		return jQuery.ajax( jQuery.extend( {
			url: url,
			type: method,
			dataType: type,
			data: data,
			success: callback
		}, jQuery.isPlainObject( url ) && url ) );
	};
} );

jQuery.ajaxPrefilter( function( s ) {
	var i;
	for ( i in s.headers ) {
		if ( i.toLowerCase() === "content-type" ) {
			s.contentType = s.headers[ i ] || "";
		}
	}
} );


jQuery._evalUrl = function( url, options, doc ) {
	return jQuery.ajax( {
		url: url,

		// Make this explicit, since user can override this through ajaxSetup (trac-11264)
		type: "GET",
		dataType: "script",
		cache: true,
		async: false,
		global: false,

		// Only evaluate the response if it is successful (gh-4126)
		// dataFilter is not invoked for failure responses, so using it instead
		// of the default converter is kludgy but it works.
		converters: {
			"text script": function() {}
		},
		dataFilter: function( response ) {
			jQuery.globalEval( response, options, doc );
		}
	} );
};


jQuery.fn.extend( {
	wrapAll: function( html ) {
		var wrap;

		if ( this[ 0 ] ) {
			if ( isFunction( html ) ) {
				html = html.call( this[ 0 ] );
			}

			// The elements to wrap the target around
			wrap = jQuery( html, this[ 0 ].ownerDocument ).eq( 0 ).clone( true );

			if ( this[ 0 ].parentNode ) {
				wrap.insertBefore( this[ 0 ] );
			}

			wrap.map( function() {
				var elem = this;

				while ( elem.firstElementChild ) {
					elem = elem.firstElementChild;
				}

				return elem;
			} ).append( this );
		}

		return this;
	},

	wrapInner: function( html ) {
		if ( isFunction( html ) ) {
			return this.each( function( i ) {
				jQuery( this ).wrapInner( html.call( this, i ) );
			} );
		}

		return this.each( function() {
			var self = jQuery( this ),
				contents = self.contents();

			if ( contents.length ) {
				contents.wrapAll( html );

			} else {
				self.append( html );
			}
		} );
	},

	wrap: function( html ) {
		var htmlIsFunction = isFunction( html );

		return this.each( function( i ) {
			jQuery( this ).wrapAll( htmlIsFunction ? html.call( this, i ) : html );
		} );
	},

	unwrap: function( selector ) {
		this.parent( selector ).not( "body" ).each( function() {
			jQuery( this ).replaceWith( this.childNodes );
		} );
		return this;
	}
} );


jQuery.expr.pseudos.hidden = function( elem ) {
	return !jQuery.expr.pseudos.visible( elem );
};
jQuery.expr.pseudos.visible = function( elem ) {
	return !!( elem.offsetWidth || elem.offsetHeight || elem.getClientRects().length );
};




jQuery.ajaxSettings.xhr = function() {
	try {
		return new window.XMLHttpRequest();
	} catch ( e ) {}
};

var xhrSuccessStatus = {

		// File protocol always yields status code 0, assume 200
		0: 200,

		// Support: IE <=9 only
		// trac-1450: sometimes IE returns 1223 when it should be 204
		1223: 204
	},
	xhrSupported = jQuery.ajaxSettings.xhr();

support.cors = !!xhrSupported && ( "withCredentials" in xhrSupported );
support.ajax = xhrSupported = !!xhrSupported;

jQuery.ajaxTransport( function( options ) {
	var callback, errorCallback;

	// Cross domain only allowed if supported through XMLHttpRequest
	if ( support.cors || xhrSupported && !options.crossDomain ) {
		return {
			send: function( headers, complete ) {
				var i,
					xhr = options.xhr();

				xhr.open(
					options.type,
					options.url,
					options.async,
					options.username,
					options.password
				);

				// Apply custom fields if provided
				if ( options.xhrFields ) {
					for ( i in options.xhrFields ) {
						xhr[ i ] = options.xhrFields[ i ];
					}
				}

				// Override mime type if needed
				if ( options.mimeType && xhr.overrideMimeType ) {
					xhr.overrideMimeType( options.mimeType );
				}

				// X-Requested-With header
				// For cross-domain requests, seeing as conditions for a preflight are
				// akin to a jigsaw puzzle, we simply never set it to be sure.
				// (it can always be set on a per-request basis or even using ajaxSetup)
				// For same-domain requests, won't change header if already provided.
				if ( !options.crossDomain && !headers[ "X-Requested-With" ] ) {
					headers[ "X-Requested-With" ] = "XMLHttpRequest";
				}

				// Set headers
				for ( i in headers ) {
					xhr.setRequestHeader( i, headers[ i ] );
				}

				// Callback
				callback = function( type ) {
					return function() {
						if ( callback ) {
							callback = errorCallback = xhr.onload =
								xhr.onerror = xhr.onabort = xhr.ontimeout =
									xhr.onreadystatechange = null;

							if ( type === "abort" ) {
								xhr.abort();
							} else if ( type === "error" ) {

								// Support: IE <=9 only
								// On a manual native abort, IE9 throws
								// errors on any property access that is not readyState
								if ( typeof xhr.status !== "number" ) {
									complete( 0, "error" );
								} else {
									complete(

										// File: protocol always yields status 0; see trac-8605, trac-14207
										xhr.status,
										xhr.statusText
									);
								}
							} else {
								complete(
									xhrSuccessStatus[ xhr.status ] || xhr.status,
									xhr.statusText,

									// Support: IE <=9 only
									// IE9 has no XHR2 but throws on binary (trac-11426)
									// For XHR2 non-text, let the caller handle it (gh-2498)
									( xhr.responseType || "text" ) !== "text"  ||
									typeof xhr.responseText !== "string" ?
										{ binary: xhr.response } :
										{ text: xhr.responseText },
									xhr.getAllResponseHeaders()
								);
							}
						}
					};
				};

				// Listen to events
				xhr.onload = callback();
				errorCallback = xhr.onerror = xhr.ontimeout = callback( "error" );

				// Support: IE 9 only
				// Use onreadystatechange to replace onabort
				// to handle uncaught aborts
				if ( xhr.onabort !== undefined ) {
					xhr.onabort = errorCallback;
				} else {
					xhr.onreadystatechange = function() {

						// Check readyState before timeout as it changes
						if ( xhr.readyState === 4 ) {

							// Allow onerror to be called first,
							// but that will not handle a native abort
							// Also, save errorCallback to a variable
							// as xhr.onerror cannot be accessed
							window.setTimeout( function() {
								if ( callback ) {
									errorCallback();
								}
							} );
						}
					};
				}

				// Create the abort callback
				callback = callback( "abort" );

				try {

					// Do send the request (this may raise an exception)
					xhr.send( options.hasContent && options.data || null );
				} catch ( e ) {

					// trac-14683: Only rethrow if this hasn't been notified as an error yet
					if ( callback ) {
						throw e;
					}
				}
			},

			abort: function() {
				if ( callback ) {
					callback();
				}
			}
		};
	}
} );




// Prevent auto-execution of scripts when no explicit dataType was provided (See gh-2432)
jQuery.ajaxPrefilter( function( s ) {
	if ( s.crossDomain ) {
		s.contents.script = false;
	}
} );

// Install script dataType
jQuery.ajaxSetup( {
	accepts: {
		script: "text/javascript, application/javascript, " +
			"application/ecmascript, application/x-ecmascript"
	},
	contents: {
		script: /\b(?:java|ecma)script\b/
	},
	converters: {
		"text script": function( text ) {
			jQuery.globalEval( text );
			return text;
		}
	}
} );

// Handle cache's special case and crossDomain
jQuery.ajaxPrefilter( "script", function( s ) {
	if ( s.cache === undefined ) {
		s.cache = false;
	}
	if ( s.crossDomain ) {
		s.type = "GET";
	}
} );

// Bind script tag hack transport
jQuery.ajaxTransport( "script", function( s ) {

	// This transport only deals with cross domain or forced-by-attrs requests
	if ( s.crossDomain || s.scriptAttrs ) {
		var script, callback;
		return {
			send: function( _, complete ) {
				script = jQuery( "<script>" )
					.attr( s.scriptAttrs || {} )
					.prop( { charset: s.scriptCharset, src: s.url } )
					.on( "load error", callback = function( evt ) {
						script.remove();
						callback = null;
						if ( evt ) {
							complete( evt.type === "error" ? 404 : 200, evt.type );
						}
					} );

				// Use native DOM manipulation to avoid our domManip AJAX trickery
				document.head.appendChild( script[ 0 ] );
			},
			abort: function() {
				if ( callback ) {
					callback();
				}
			}
		};
	}
} );




var oldCallbacks = [],
	rjsonp = /(=)\?(?=&|$)|\?\?/;

// Default jsonp settings
jQuery.ajaxSetup( {
	jsonp: "callback",
	jsonpCallback: function() {
		var callback = oldCallbacks.pop() || ( jQuery.expando + "_" + ( nonce.guid++ ) );
		this[ callback ] = true;
		return callback;
	}
} );

// Detect, normalize options and install callbacks for jsonp requests
jQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {

	var callbackName, overwritten, responseContainer,
		jsonProp = s.jsonp !== false && ( rjsonp.test( s.url ) ?
			"url" :
			typeof s.data === "string" &&
				( s.contentType || "" )
					.indexOf( "application/x-www-form-urlencoded" ) === 0 &&
				rjsonp.test( s.data ) && "data"
		);

	// Handle iff the expected data type is "jsonp" or we have a parameter to set
	if ( jsonProp || s.dataTypes[ 0 ] === "jsonp" ) {

		// Get callback name, remembering preexisting value associated with it
		callbackName = s.jsonpCallback = isFunction( s.jsonpCallback ) ?
			s.jsonpCallback() :
			s.jsonpCallback;

		// Insert callback into url or form data
		if ( jsonProp ) {
			s[ jsonProp ] = s[ jsonProp ].replace( rjsonp, "$1" + callbackName );
		} else if ( s.jsonp !== false ) {
			s.url += ( rquery.test( s.url ) ? "&" : "?" ) + s.jsonp + "=" + callbackName;
		}

		// Use data converter to retrieve json after script execution
		s.converters[ "script json" ] = function() {
			if ( !responseContainer ) {
				jQuery.error( callbackName + " was not called" );
			}
			return responseContainer[ 0 ];
		};

		// Force json dataType
		s.dataTypes[ 0 ] = "json";

		// Install callback
		overwritten = window[ callbackName ];
		window[ callbackName ] = function() {
			responseContainer = arguments;
		};

		// Clean-up function (fires after converters)
		jqXHR.always( function() {

			// If previous value didn't exist - remove it
			if ( overwritten === undefined ) {
				jQuery( window ).removeProp( callbackName );

			// Otherwise restore preexisting value
			} else {
				window[ callbackName ] = overwritten;
			}

			// Save back as free
			if ( s[ callbackName ] ) {

				// Make sure that re-using the options doesn't screw things around
				s.jsonpCallback = originalSettings.jsonpCallback;

				// Save the callback name for future use
				oldCallbacks.push( callbackName );
			}

			// Call if it was a function and we have a response
			if ( responseContainer && isFunction( overwritten ) ) {
				overwritten( responseContainer[ 0 ] );
			}

			responseContainer = overwritten = undefined;
		} );

		// Delegate to script
		return "script";
	}
} );




// Support: Safari 8 only
// In Safari 8 documents created via document.implementation.createHTMLDocument
// collapse sibling forms: the second one becomes a child of the first one.
// Because of that, this security measure has to be disabled in Safari 8.
// https://bugs.webkit.org/show_bug.cgi?id=137337
support.createHTMLDocument = ( function() {
	var body = document.implementation.createHTMLDocument( "" ).body;
	body.innerHTML = "<form></form><form></form>";
	return body.childNodes.length === 2;
} )();


// Argument "data" should be string of html
// context (optional): If specified, the fragment will be created in this context,
// defaults to document
// keepScripts (optional): If true, will include scripts passed in the html string
jQuery.parseHTML = function( data, context, keepScripts ) {
	if ( typeof data !== "string" ) {
		return [];
	}
	if ( typeof context === "boolean" ) {
		keepScripts = context;
		context = false;
	}

	var base, parsed, scripts;

	if ( !context ) {

		// Stop scripts or inline event handlers from being executed immediately
		// by using document.implementation
		if ( support.createHTMLDocument ) {
			context = document.implementation.createHTMLDocument( "" );

			// Set the base href for the created document
			// so any parsed elements with URLs
			// are based on the document's URL (gh-2965)
			base = context.createElement( "base" );
			base.href = document.location.href;
			context.head.appendChild( base );
		} else {
			context = document;
		}
	}

	parsed = rsingleTag.exec( data );
	scripts = !keepScripts && [];

	// Single tag
	if ( parsed ) {
		return [ context.createElement( parsed[ 1 ] ) ];
	}

	parsed = buildFragment( [ data ], context, scripts );

	if ( scripts && scripts.length ) {
		jQuery( scripts ).remove();
	}

	return jQuery.merge( [], parsed.childNodes );
};


/**
 * Load a url into a page
 */
jQuery.fn.load = function( url, params, callback ) {
	var selector, type, response,
		self = this,
		off = url.indexOf( " " );

	if ( off > -1 ) {
		selector = stripAndCollapse( url.slice( off ) );
		url = url.slice( 0, off );
	}

	// If it's a function
	if ( isFunction( params ) ) {

		// We assume that it's the callback
		callback = params;
		params = undefined;

	// Otherwise, build a param string
	} else if ( params && typeof params === "object" ) {
		type = "POST";
	}

	// If we have elements to modify, make the request
	if ( self.length > 0 ) {
		jQuery.ajax( {
			url: url,

			// If "type" variable is undefined, then "GET" method will be used.
			// Make value of this field explicit since
			// user can override it through ajaxSetup method
			type: type || "GET",
			dataType: "html",
			data: params
		} ).done( function( responseText ) {

			// Save response for use in complete callback
			response = arguments;

			self.html( selector ?

				// If a selector was specified, locate the right elements in a dummy div
				// Exclude scripts to avoid IE 'Permission Denied' errors
				jQuery( "<div>" ).append( jQuery.parseHTML( responseText ) ).find( selector ) :

				// Otherwise use the full result
				responseText );

		// If the request succeeds, this function gets "data", "status", "jqXHR"
		// but they are ignored because response was set above.
		// If it fails, this function gets "jqXHR", "status", "error"
		} ).always( callback && function( jqXHR, status ) {
			self.each( function() {
				callback.apply( this, response || [ jqXHR.responseText, status, jqXHR ] );
			} );
		} );
	}

	return this;
};




jQuery.expr.pseudos.animated = function( elem ) {
	return jQuery.grep( jQuery.timers, function( fn ) {
		return elem === fn.elem;
	} ).length;
};




jQuery.offset = {
	setOffset: function( elem, options, i ) {
		var curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition,
			position = jQuery.css( elem, "position" ),
			curElem = jQuery( elem ),
			props = {};

		// Set position first, in-case top/left are set even on static elem
		if ( position === "static" ) {
			elem.style.position = "relative";
		}

		curOffset = curElem.offset();
		curCSSTop = jQuery.css( elem, "top" );
		curCSSLeft = jQuery.css( elem, "left" );
		calculatePosition = ( position === "absolute" || position === "fixed" ) &&
			( curCSSTop + curCSSLeft ).indexOf( "auto" ) > -1;

		// Need to be able to calculate position if either
		// top or left is auto and position is either absolute or fixed
		if ( calculatePosition ) {
			curPosition = curElem.position();
			curTop = curPosition.top;
			curLeft = curPosition.left;

		} else {
			curTop = parseFloat( curCSSTop ) || 0;
			curLeft = parseFloat( curCSSLeft ) || 0;
		}

		if ( isFunction( options ) ) {

			// Use jQuery.extend here to allow modification of coordinates argument (gh-1848)
			options = options.call( elem, i, jQuery.extend( {}, curOffset ) );
		}

		if ( options.top != null ) {
			props.top = ( options.top - curOffset.top ) + curTop;
		}
		if ( options.left != null ) {
			props.left = ( options.left - curOffset.left ) + curLeft;
		}

		if ( "using" in options ) {
			options.using.call( elem, props );

		} else {
			curElem.css( props );
		}
	}
};

jQuery.fn.extend( {

	// offset() relates an element's border box to the document origin
	offset: function( options ) {

		// Preserve chaining for setter
		if ( arguments.length ) {
			return options === undefined ?
				this :
				this.each( function( i ) {
					jQuery.offset.setOffset( this, options, i );
				} );
		}

		var rect, win,
			elem = this[ 0 ];

		if ( !elem ) {
			return;
		}

		// Return zeros for disconnected and hidden (display: none) elements (gh-2310)
		// Support: IE <=11 only
		// Running getBoundingClientRect on a
		// disconnected node in IE throws an error
		if ( !elem.getClientRects().length ) {
			return { top: 0, left: 0 };
		}

		// Get document-relative position by adding viewport scroll to viewport-relative gBCR
		rect = elem.getBoundingClientRect();
		win = elem.ownerDocument.defaultView;
		return {
			top: rect.top + win.pageYOffset,
			left: rect.left + win.pageXOffset
		};
	},

	// position() relates an element's margin box to its offset parent's padding box
	// This corresponds to the behavior of CSS absolute positioning
	position: function() {
		if ( !this[ 0 ] ) {
			return;
		}

		var offsetParent, offset, doc,
			elem = this[ 0 ],
			parentOffset = { top: 0, left: 0 };

		// position:fixed elements are offset from the viewport, which itself always has zero offset
		if ( jQuery.css( elem, "position" ) === "fixed" ) {

			// Assume position:fixed implies availability of getBoundingClientRect
			offset = elem.getBoundingClientRect();

		} else {
			offset = this.offset();

			// Account for the *real* offset parent, which can be the document or its root element
			// when a statically positioned element is identified
			doc = elem.ownerDocument;
			offsetParent = elem.offsetParent || doc.documentElement;
			while ( offsetParent &&
				( offsetParent === doc.body || offsetParent === doc.documentElement ) &&
				jQuery.css( offsetParent, "position" ) === "static" ) {

				offsetParent = offsetParent.parentNode;
			}
			if ( offsetParent && offsetParent !== elem && offsetParent.nodeType === 1 ) {

				// Incorporate borders into its offset, since they are outside its content origin
				parentOffset = jQuery( offsetParent ).offset();
				parentOffset.top += jQuery.css( offsetParent, "borderTopWidth", true );
				parentOffset.left += jQuery.css( offsetParent, "borderLeftWidth", true );
			}
		}

		// Subtract parent offsets and element margins
		return {
			top: offset.top - parentOffset.top - jQuery.css( elem, "marginTop", true ),
			left: offset.left - parentOffset.left - jQuery.css( elem, "marginLeft", true )
		};
	},

	// This method will return documentElement in the following cases:
	// 1) For the element inside the iframe without offsetParent, this method will return
	//    documentElement of the parent window
	// 2) For the hidden or detached element
	// 3) For body or html element, i.e. in case of the html node - it will return itself
	//
	// but those exceptions were never presented as a real life use-cases
	// and might be considered as more preferable results.
	//
	// This logic, however, is not guaranteed and can change at any point in the future
	offsetParent: function() {
		return this.map( function() {
			var offsetParent = this.offsetParent;

			while ( offsetParent && jQuery.css( offsetParent, "position" ) === "static" ) {
				offsetParent = offsetParent.offsetParent;
			}

			return offsetParent || documentElement;
		} );
	}
} );

// Create scrollLeft and scrollTop methods
jQuery.each( { scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function( method, prop ) {
	var top = "pageYOffset" === prop;

	jQuery.fn[ method ] = function( val ) {
		return access( this, function( elem, method, val ) {

			// Coalesce documents and windows
			var win;
			if ( isWindow( elem ) ) {
				win = elem;
			} else if ( elem.nodeType === 9 ) {
				win = elem.defaultView;
			}

			if ( val === undefined ) {
				return win ? win[ prop ] : elem[ method ];
			}

			if ( win ) {
				win.scrollTo(
					!top ? val : win.pageXOffset,
					top ? val : win.pageYOffset
				);

			} else {
				elem[ method ] = val;
			}
		}, method, val, arguments.length );
	};
} );

// Support: Safari <=7 - 9.1, Chrome <=37 - 49
// Add the top/left cssHooks using jQuery.fn.position
// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084
// Blink bug: https://bugs.chromium.org/p/chromium/issues/detail?id=589347
// getComputedStyle returns percent when specified for top/left/bottom/right;
// rather than make the css module depend on the offset module, just check for it here
jQuery.each( [ "top", "left" ], function( _i, prop ) {
	jQuery.cssHooks[ prop ] = addGetHookIf( support.pixelPosition,
		function( elem, computed ) {
			if ( computed ) {
				computed = curCSS( elem, prop );

				// If curCSS returns percentage, fallback to offset
				return rnumnonpx.test( computed ) ?
					jQuery( elem ).position()[ prop ] + "px" :
					computed;
			}
		}
	);
} );


// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods
jQuery.each( { Height: "height", Width: "width" }, function( name, type ) {
	jQuery.each( {
		padding: "inner" + name,
		content: type,
		"": "outer" + name
	}, function( defaultExtra, funcName ) {

		// Margin is only for outerHeight, outerWidth
		jQuery.fn[ funcName ] = function( margin, value ) {
			var chainable = arguments.length && ( defaultExtra || typeof margin !== "boolean" ),
				extra = defaultExtra || ( margin === true || value === true ? "margin" : "border" );

			return access( this, function( elem, type, value ) {
				var doc;

				if ( isWindow( elem ) ) {

					// $( window ).outerWidth/Height return w/h including scrollbars (gh-1729)
					return funcName.indexOf( "outer" ) === 0 ?
						elem[ "inner" + name ] :
						elem.document.documentElement[ "client" + name ];
				}

				// Get document width or height
				if ( elem.nodeType === 9 ) {
					doc = elem.documentElement;

					// Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height],
					// whichever is greatest
					return Math.max(
						elem.body[ "scroll" + name ], doc[ "scroll" + name ],
						elem.body[ "offset" + name ], doc[ "offset" + name ],
						doc[ "client" + name ]
					);
				}

				return value === undefined ?

					// Get width or height on the element, requesting but not forcing parseFloat
					jQuery.css( elem, type, extra ) :

					// Set width or height on the element
					jQuery.style( elem, type, value, extra );
			}, type, chainable ? margin : undefined, chainable );
		};
	} );
} );


jQuery.each( [
	"ajaxStart",
	"ajaxStop",
	"ajaxComplete",
	"ajaxError",
	"ajaxSuccess",
	"ajaxSend"
], function( _i, type ) {
	jQuery.fn[ type ] = function( fn ) {
		return this.on( type, fn );
	};
} );




jQuery.fn.extend( {

	bind: function( types, data, fn ) {
		return this.on( types, null, data, fn );
	},
	unbind: function( types, fn ) {
		return this.off( types, null, fn );
	},

	delegate: function( selector, types, data, fn ) {
		return this.on( types, selector, data, fn );
	},
	undelegate: function( selector, types, fn ) {

		// ( namespace ) or ( selector, types [, fn] )
		return arguments.length === 1 ?
			this.off( selector, "**" ) :
			this.off( types, selector || "**", fn );
	},

	hover: function( fnOver, fnOut ) {
		return this
			.on( "mouseenter", fnOver )
			.on( "mouseleave", fnOut || fnOver );
	}
} );

jQuery.each(
	( "blur focus focusin focusout resize scroll click dblclick " +
	"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +
	"change select submit keydown keypress keyup contextmenu" ).split( " " ),
	function( _i, name ) {

		// Handle event binding
		jQuery.fn[ name ] = function( data, fn ) {
			return arguments.length > 0 ?
				this.on( name, null, data, fn ) :
				this.trigger( name );
		};
	}
);




// Support: Android <=4.0 only
// Make sure we trim BOM and NBSP
// Require that the "whitespace run" starts from a non-whitespace
// to avoid O(N^2) behavior when the engine would try matching "\s+$" at each space position.
var rtrim = /^[\s\uFEFF\xA0]+|([^\s\uFEFF\xA0])[\s\uFEFF\xA0]+$/g;

// Bind a function to a context, optionally partially applying any
// arguments.
// jQuery.proxy is deprecated to promote standards (specifically Function#bind)
// However, it is not slated for removal any time soon
jQuery.proxy = function( fn, context ) {
	var tmp, args, proxy;

	if ( typeof context === "string" ) {
		tmp = fn[ context ];
		context = fn;
		fn = tmp;
	}

	// Quick check to determine if target is callable, in the spec
	// this throws a TypeError, but we will just return undefined.
	if ( !isFunction( fn ) ) {
		return undefined;
	}

	// Simulated bind
	args = slice.call( arguments, 2 );
	proxy = function() {
		return fn.apply( context || this, args.concat( slice.call( arguments ) ) );
	};

	// Set the guid of unique handler to the same of original handler, so it can be removed
	proxy.guid = fn.guid = fn.guid || jQuery.guid++;

	return proxy;
};

jQuery.holdReady = function( hold ) {
	if ( hold ) {
		jQuery.readyWait++;
	} else {
		jQuery.ready( true );
	}
};
jQuery.isArray = Array.isArray;
jQuery.parseJSON = JSON.parse;
jQuery.nodeName = nodeName;
jQuery.isFunction = isFunction;
jQuery.isWindow = isWindow;
jQuery.camelCase = camelCase;
jQuery.type = toType;

jQuery.now = Date.now;

jQuery.isNumeric = function( obj ) {

	// As of jQuery 3.0, isNumeric is limited to
	// strings and numbers (primitives or objects)
	// that can be coerced to finite numbers (gh-2662)
	var type = jQuery.type( obj );
	return ( type === "number" || type === "string" ) &&

		// parseFloat NaNs numeric-cast false positives ("")
		// ...but misinterprets leading-number strings, particularly hex literals ("0x...")
		// subtraction forces infinities to NaN
		!isNaN( obj - parseFloat( obj ) );
};

jQuery.trim = function( text ) {
	return text == null ?
		"" :
		( text + "" ).replace( rtrim, "$1" );
};



// Register as a named AMD module, since jQuery can be concatenated with other
// files that may use define, but not via a proper concatenation script that
// understands anonymous AMD modules. A named AMD is safest and most robust
// way to register. Lowercase jquery is used because AMD module names are
// derived from file names, and jQuery is normally delivered in a lowercase
// file name. Do this after creating the global so that if an AMD module wants
// to call noConflict to hide this version of jQuery, it will work.

// Note that for maximum portability, libraries that are not jQuery should
// declare themselves as anonymous modules, and avoid setting a global if an
// AMD loader is present. jQuery is a special case. For more information, see
// https://github.com/jrburke/requirejs/wiki/Updating-existing-libraries#wiki-anon

if ( true ) {
	!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = (function() {
		return jQuery;
	}).apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__),
				__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
}




var

	// Map over jQuery in case of overwrite
	_jQuery = window.jQuery,

	// Map over the $ in case of overwrite
	_$ = window.$;

jQuery.noConflict = function( deep ) {
	if ( window.$ === jQuery ) {
		window.$ = _$;
	}

	if ( deep && window.jQuery === jQuery ) {
		window.jQuery = _jQuery;
	}

	return jQuery;
};

// Expose jQuery and $ identifiers, even in AMD
// (trac-7102#comment:10, https://github.com/jquery/jquery/pull/557)
// and CommonJS for browser emulators (trac-13566)
if ( typeof noGlobal === "undefined" ) {
	window.jQuery = window.$ = jQuery;
}




return jQuery;
} );


/***/ }),

/***/ "./node_modules/process/browser.js":
/*!*****************************************!*\
  !*** ./node_modules/process/browser.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout () {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
} ())
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch(e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch(e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }


}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }



}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while(len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) { return [] }

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() { return 0; };


/***/ }),

/***/ "./node_modules/regenerator-runtime/runtime.js":
/*!*****************************************************!*\
  !*** ./node_modules/regenerator-runtime/runtime.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

var runtime = (function (exports) {
  "use strict";

  var Op = Object.prototype;
  var hasOwn = Op.hasOwnProperty;
  var undefined; // More compressible than void 0.
  var $Symbol = typeof Symbol === "function" ? Symbol : {};
  var iteratorSymbol = $Symbol.iterator || "@@iterator";
  var asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator";
  var toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag";

  function define(obj, key, value) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
    return obj[key];
  }
  try {
    // IE 8 has a broken Object.defineProperty that only works on DOM objects.
    define({}, "");
  } catch (err) {
    define = function(obj, key, value) {
      return obj[key] = value;
    };
  }

  function wrap(innerFn, outerFn, self, tryLocsList) {
    // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.
    var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;
    var generator = Object.create(protoGenerator.prototype);
    var context = new Context(tryLocsList || []);

    // The ._invoke method unifies the implementations of the .next,
    // .throw, and .return methods.
    generator._invoke = makeInvokeMethod(innerFn, self, context);

    return generator;
  }
  exports.wrap = wrap;

  // Try/catch helper to minimize deoptimizations. Returns a completion
  // record like context.tryEntries[i].completion. This interface could
  // have been (and was previously) designed to take a closure to be
  // invoked without arguments, but in all the cases we care about we
  // already have an existing method we want to call, so there's no need
  // to create a new function object. We can even get away with assuming
  // the method takes exactly one argument, since that happens to be true
  // in every case, so we don't have to touch the arguments object. The
  // only additional allocation required is the completion record, which
  // has a stable shape and so hopefully should be cheap to allocate.
  function tryCatch(fn, obj, arg) {
    try {
      return { type: "normal", arg: fn.call(obj, arg) };
    } catch (err) {
      return { type: "throw", arg: err };
    }
  }

  var GenStateSuspendedStart = "suspendedStart";
  var GenStateSuspendedYield = "suspendedYield";
  var GenStateExecuting = "executing";
  var GenStateCompleted = "completed";

  // Returning this object from the innerFn has the same effect as
  // breaking out of the dispatch switch statement.
  var ContinueSentinel = {};

  // Dummy constructor functions that we use as the .constructor and
  // .constructor.prototype properties for functions that return Generator
  // objects. For full spec compliance, you may wish to configure your
  // minifier not to mangle the names of these two functions.
  function Generator() {}
  function GeneratorFunction() {}
  function GeneratorFunctionPrototype() {}

  // This is a polyfill for %IteratorPrototype% for environments that
  // don't natively support it.
  var IteratorPrototype = {};
  define(IteratorPrototype, iteratorSymbol, function () {
    return this;
  });

  var getProto = Object.getPrototypeOf;
  var NativeIteratorPrototype = getProto && getProto(getProto(values([])));
  if (NativeIteratorPrototype &&
      NativeIteratorPrototype !== Op &&
      hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) {
    // This environment has a native %IteratorPrototype%; use it instead
    // of the polyfill.
    IteratorPrototype = NativeIteratorPrototype;
  }

  var Gp = GeneratorFunctionPrototype.prototype =
    Generator.prototype = Object.create(IteratorPrototype);
  GeneratorFunction.prototype = GeneratorFunctionPrototype;
  define(Gp, "constructor", GeneratorFunctionPrototype);
  define(GeneratorFunctionPrototype, "constructor", GeneratorFunction);
  GeneratorFunction.displayName = define(
    GeneratorFunctionPrototype,
    toStringTagSymbol,
    "GeneratorFunction"
  );

  // Helper for defining the .next, .throw, and .return methods of the
  // Iterator interface in terms of a single ._invoke method.
  function defineIteratorMethods(prototype) {
    ["next", "throw", "return"].forEach(function(method) {
      define(prototype, method, function(arg) {
        return this._invoke(method, arg);
      });
    });
  }

  exports.isGeneratorFunction = function(genFun) {
    var ctor = typeof genFun === "function" && genFun.constructor;
    return ctor
      ? ctor === GeneratorFunction ||
        // For the native GeneratorFunction constructor, the best we can
        // do is to check its .name property.
        (ctor.displayName || ctor.name) === "GeneratorFunction"
      : false;
  };

  exports.mark = function(genFun) {
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);
    } else {
      genFun.__proto__ = GeneratorFunctionPrototype;
      define(genFun, toStringTagSymbol, "GeneratorFunction");
    }
    genFun.prototype = Object.create(Gp);
    return genFun;
  };

  // Within the body of any async function, `await x` is transformed to
  // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test
  // `hasOwn.call(value, "__await")` to determine if the yielded value is
  // meant to be awaited.
  exports.awrap = function(arg) {
    return { __await: arg };
  };

  function AsyncIterator(generator, PromiseImpl) {
    function invoke(method, arg, resolve, reject) {
      var record = tryCatch(generator[method], generator, arg);
      if (record.type === "throw") {
        reject(record.arg);
      } else {
        var result = record.arg;
        var value = result.value;
        if (value &&
            typeof value === "object" &&
            hasOwn.call(value, "__await")) {
          return PromiseImpl.resolve(value.__await).then(function(value) {
            invoke("next", value, resolve, reject);
          }, function(err) {
            invoke("throw", err, resolve, reject);
          });
        }

        return PromiseImpl.resolve(value).then(function(unwrapped) {
          // When a yielded Promise is resolved, its final value becomes
          // the .value of the Promise<{value,done}> result for the
          // current iteration.
          result.value = unwrapped;
          resolve(result);
        }, function(error) {
          // If a rejected Promise was yielded, throw the rejection back
          // into the async generator function so it can be handled there.
          return invoke("throw", error, resolve, reject);
        });
      }
    }

    var previousPromise;

    function enqueue(method, arg) {
      function callInvokeWithMethodAndArg() {
        return new PromiseImpl(function(resolve, reject) {
          invoke(method, arg, resolve, reject);
        });
      }

      return previousPromise =
        // If enqueue has been called before, then we want to wait until
        // all previous Promises have been resolved before calling invoke,
        // so that results are always delivered in the correct order. If
        // enqueue has not been called before, then it is important to
        // call invoke immediately, without waiting on a callback to fire,
        // so that the async generator function has the opportunity to do
        // any necessary setup in a predictable way. This predictability
        // is why the Promise constructor synchronously invokes its
        // executor callback, and why async functions synchronously
        // execute code before the first await. Since we implement simple
        // async functions in terms of async generators, it is especially
        // important to get this right, even though it requires care.
        previousPromise ? previousPromise.then(
          callInvokeWithMethodAndArg,
          // Avoid propagating failures to Promises returned by later
          // invocations of the iterator.
          callInvokeWithMethodAndArg
        ) : callInvokeWithMethodAndArg();
    }

    // Define the unified helper method that is used to implement .next,
    // .throw, and .return (see defineIteratorMethods).
    this._invoke = enqueue;
  }

  defineIteratorMethods(AsyncIterator.prototype);
  define(AsyncIterator.prototype, asyncIteratorSymbol, function () {
    return this;
  });
  exports.AsyncIterator = AsyncIterator;

  // Note that simple async functions are implemented on top of
  // AsyncIterator objects; they just return a Promise for the value of
  // the final result produced by the iterator.
  exports.async = function(innerFn, outerFn, self, tryLocsList, PromiseImpl) {
    if (PromiseImpl === void 0) PromiseImpl = Promise;

    var iter = new AsyncIterator(
      wrap(innerFn, outerFn, self, tryLocsList),
      PromiseImpl
    );

    return exports.isGeneratorFunction(outerFn)
      ? iter // If outerFn is a generator, return the full iterator.
      : iter.next().then(function(result) {
          return result.done ? result.value : iter.next();
        });
  };

  function makeInvokeMethod(innerFn, self, context) {
    var state = GenStateSuspendedStart;

    return function invoke(method, arg) {
      if (state === GenStateExecuting) {
        throw new Error("Generator is already running");
      }

      if (state === GenStateCompleted) {
        if (method === "throw") {
          throw arg;
        }

        // Be forgiving, per 25.3.3.3.3 of the spec:
        // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume
        return doneResult();
      }

      context.method = method;
      context.arg = arg;

      while (true) {
        var delegate = context.delegate;
        if (delegate) {
          var delegateResult = maybeInvokeDelegate(delegate, context);
          if (delegateResult) {
            if (delegateResult === ContinueSentinel) continue;
            return delegateResult;
          }
        }

        if (context.method === "next") {
          // Setting context._sent for legacy support of Babel's
          // function.sent implementation.
          context.sent = context._sent = context.arg;

        } else if (context.method === "throw") {
          if (state === GenStateSuspendedStart) {
            state = GenStateCompleted;
            throw context.arg;
          }

          context.dispatchException(context.arg);

        } else if (context.method === "return") {
          context.abrupt("return", context.arg);
        }

        state = GenStateExecuting;

        var record = tryCatch(innerFn, self, context);
        if (record.type === "normal") {
          // If an exception is thrown from innerFn, we leave state ===
          // GenStateExecuting and loop back for another invocation.
          state = context.done
            ? GenStateCompleted
            : GenStateSuspendedYield;

          if (record.arg === ContinueSentinel) {
            continue;
          }

          return {
            value: record.arg,
            done: context.done
          };

        } else if (record.type === "throw") {
          state = GenStateCompleted;
          // Dispatch the exception by looping back around to the
          // context.dispatchException(context.arg) call above.
          context.method = "throw";
          context.arg = record.arg;
        }
      }
    };
  }

  // Call delegate.iterator[context.method](context.arg) and handle the
  // result, either by returning a { value, done } result from the
  // delegate iterator, or by modifying context.method and context.arg,
  // setting context.delegate to null, and returning the ContinueSentinel.
  function maybeInvokeDelegate(delegate, context) {
    var method = delegate.iterator[context.method];
    if (method === undefined) {
      // A .throw or .return when the delegate iterator has no .throw
      // method always terminates the yield* loop.
      context.delegate = null;

      if (context.method === "throw") {
        // Note: ["return"] must be used for ES3 parsing compatibility.
        if (delegate.iterator["return"]) {
          // If the delegate iterator has a return method, give it a
          // chance to clean up.
          context.method = "return";
          context.arg = undefined;
          maybeInvokeDelegate(delegate, context);

          if (context.method === "throw") {
            // If maybeInvokeDelegate(context) changed context.method from
            // "return" to "throw", let that override the TypeError below.
            return ContinueSentinel;
          }
        }

        context.method = "throw";
        context.arg = new TypeError(
          "The iterator does not provide a 'throw' method");
      }

      return ContinueSentinel;
    }

    var record = tryCatch(method, delegate.iterator, context.arg);

    if (record.type === "throw") {
      context.method = "throw";
      context.arg = record.arg;
      context.delegate = null;
      return ContinueSentinel;
    }

    var info = record.arg;

    if (! info) {
      context.method = "throw";
      context.arg = new TypeError("iterator result is not an object");
      context.delegate = null;
      return ContinueSentinel;
    }

    if (info.done) {
      // Assign the result of the finished delegate to the temporary
      // variable specified by delegate.resultName (see delegateYield).
      context[delegate.resultName] = info.value;

      // Resume execution at the desired location (see delegateYield).
      context.next = delegate.nextLoc;

      // If context.method was "throw" but the delegate handled the
      // exception, let the outer generator proceed normally. If
      // context.method was "next", forget context.arg since it has been
      // "consumed" by the delegate iterator. If context.method was
      // "return", allow the original .return call to continue in the
      // outer generator.
      if (context.method !== "return") {
        context.method = "next";
        context.arg = undefined;
      }

    } else {
      // Re-yield the result returned by the delegate method.
      return info;
    }

    // The delegate iterator is finished, so forget it and continue with
    // the outer generator.
    context.delegate = null;
    return ContinueSentinel;
  }

  // Define Generator.prototype.{next,throw,return} in terms of the
  // unified ._invoke helper method.
  defineIteratorMethods(Gp);

  define(Gp, toStringTagSymbol, "Generator");

  // A Generator should always return itself as the iterator object when the
  // @@iterator function is called on it. Some browsers' implementations of the
  // iterator prototype chain incorrectly implement this, causing the Generator
  // object to not be returned from this call. This ensures that doesn't happen.
  // See https://github.com/facebook/regenerator/issues/274 for more details.
  define(Gp, iteratorSymbol, function() {
    return this;
  });

  define(Gp, "toString", function() {
    return "[object Generator]";
  });

  function pushTryEntry(locs) {
    var entry = { tryLoc: locs[0] };

    if (1 in locs) {
      entry.catchLoc = locs[1];
    }

    if (2 in locs) {
      entry.finallyLoc = locs[2];
      entry.afterLoc = locs[3];
    }

    this.tryEntries.push(entry);
  }

  function resetTryEntry(entry) {
    var record = entry.completion || {};
    record.type = "normal";
    delete record.arg;
    entry.completion = record;
  }

  function Context(tryLocsList) {
    // The root entry object (effectively a try statement without a catch
    // or a finally block) gives us a place to store values thrown from
    // locations where there is no enclosing try statement.
    this.tryEntries = [{ tryLoc: "root" }];
    tryLocsList.forEach(pushTryEntry, this);
    this.reset(true);
  }

  exports.keys = function(object) {
    var keys = [];
    for (var key in object) {
      keys.push(key);
    }
    keys.reverse();

    // Rather than returning an object with a next method, we keep
    // things simple and return the next function itself.
    return function next() {
      while (keys.length) {
        var key = keys.pop();
        if (key in object) {
          next.value = key;
          next.done = false;
          return next;
        }
      }

      // To avoid creating an additional object, we just hang the .value
      // and .done properties off the next function object itself. This
      // also ensures that the minifier will not anonymize the function.
      next.done = true;
      return next;
    };
  };

  function values(iterable) {
    if (iterable) {
      var iteratorMethod = iterable[iteratorSymbol];
      if (iteratorMethod) {
        return iteratorMethod.call(iterable);
      }

      if (typeof iterable.next === "function") {
        return iterable;
      }

      if (!isNaN(iterable.length)) {
        var i = -1, next = function next() {
          while (++i < iterable.length) {
            if (hasOwn.call(iterable, i)) {
              next.value = iterable[i];
              next.done = false;
              return next;
            }
          }

          next.value = undefined;
          next.done = true;

          return next;
        };

        return next.next = next;
      }
    }

    // Return an iterator with no values.
    return { next: doneResult };
  }
  exports.values = values;

  function doneResult() {
    return { value: undefined, done: true };
  }

  Context.prototype = {
    constructor: Context,

    reset: function(skipTempReset) {
      this.prev = 0;
      this.next = 0;
      // Resetting context._sent for legacy support of Babel's
      // function.sent implementation.
      this.sent = this._sent = undefined;
      this.done = false;
      this.delegate = null;

      this.method = "next";
      this.arg = undefined;

      this.tryEntries.forEach(resetTryEntry);

      if (!skipTempReset) {
        for (var name in this) {
          // Not sure about the optimal order of these conditions:
          if (name.charAt(0) === "t" &&
              hasOwn.call(this, name) &&
              !isNaN(+name.slice(1))) {
            this[name] = undefined;
          }
        }
      }
    },

    stop: function() {
      this.done = true;

      var rootEntry = this.tryEntries[0];
      var rootRecord = rootEntry.completion;
      if (rootRecord.type === "throw") {
        throw rootRecord.arg;
      }

      return this.rval;
    },

    dispatchException: function(exception) {
      if (this.done) {
        throw exception;
      }

      var context = this;
      function handle(loc, caught) {
        record.type = "throw";
        record.arg = exception;
        context.next = loc;

        if (caught) {
          // If the dispatched exception was caught by a catch block,
          // then let that catch block handle the exception normally.
          context.method = "next";
          context.arg = undefined;
        }

        return !! caught;
      }

      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        var record = entry.completion;

        if (entry.tryLoc === "root") {
          // Exception thrown outside of any try block that could handle
          // it, so set the completion value of the entire function to
          // throw the exception.
          return handle("end");
        }

        if (entry.tryLoc <= this.prev) {
          var hasCatch = hasOwn.call(entry, "catchLoc");
          var hasFinally = hasOwn.call(entry, "finallyLoc");

          if (hasCatch && hasFinally) {
            if (this.prev < entry.catchLoc) {
              return handle(entry.catchLoc, true);
            } else if (this.prev < entry.finallyLoc) {
              return handle(entry.finallyLoc);
            }

          } else if (hasCatch) {
            if (this.prev < entry.catchLoc) {
              return handle(entry.catchLoc, true);
            }

          } else if (hasFinally) {
            if (this.prev < entry.finallyLoc) {
              return handle(entry.finallyLoc);
            }

          } else {
            throw new Error("try statement without catch or finally");
          }
        }
      }
    },

    abrupt: function(type, arg) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.tryLoc <= this.prev &&
            hasOwn.call(entry, "finallyLoc") &&
            this.prev < entry.finallyLoc) {
          var finallyEntry = entry;
          break;
        }
      }

      if (finallyEntry &&
          (type === "break" ||
           type === "continue") &&
          finallyEntry.tryLoc <= arg &&
          arg <= finallyEntry.finallyLoc) {
        // Ignore the finally entry if control is not jumping to a
        // location outside the try/catch block.
        finallyEntry = null;
      }

      var record = finallyEntry ? finallyEntry.completion : {};
      record.type = type;
      record.arg = arg;

      if (finallyEntry) {
        this.method = "next";
        this.next = finallyEntry.finallyLoc;
        return ContinueSentinel;
      }

      return this.complete(record);
    },

    complete: function(record, afterLoc) {
      if (record.type === "throw") {
        throw record.arg;
      }

      if (record.type === "break" ||
          record.type === "continue") {
        this.next = record.arg;
      } else if (record.type === "return") {
        this.rval = this.arg = record.arg;
        this.method = "return";
        this.next = "end";
      } else if (record.type === "normal" && afterLoc) {
        this.next = afterLoc;
      }

      return ContinueSentinel;
    },

    finish: function(finallyLoc) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.finallyLoc === finallyLoc) {
          this.complete(entry.completion, entry.afterLoc);
          resetTryEntry(entry);
          return ContinueSentinel;
        }
      }
    },

    "catch": function(tryLoc) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.tryLoc === tryLoc) {
          var record = entry.completion;
          if (record.type === "throw") {
            var thrown = record.arg;
            resetTryEntry(entry);
          }
          return thrown;
        }
      }

      // The context.catch method must only be called with a location
      // argument that corresponds to a known catch block.
      throw new Error("illegal catch attempt");
    },

    delegateYield: function(iterable, resultName, nextLoc) {
      this.delegate = {
        iterator: values(iterable),
        resultName: resultName,
        nextLoc: nextLoc
      };

      if (this.method === "next") {
        // Deliberately forget the last sent value so that we don't
        // accidentally pass it on to the delegate.
        this.arg = undefined;
      }

      return ContinueSentinel;
    }
  };

  // Regardless of whether this script is executing as a CommonJS module
  // or not, return the runtime object so that we can declare the variable
  // regeneratorRuntime in the outer scope, which allows this module to be
  // injected easily by `bin/regenerator --include-runtime script.js`.
  return exports;

}(
  // If this script is executing as a CommonJS module, use module.exports
  // as the regeneratorRuntime namespace. Otherwise create a new empty
  // object. Either way, the resulting object will be used to initialize
  // the regeneratorRuntime variable at the top of this file.
   true ? module.exports : undefined
));

try {
  regeneratorRuntime = runtime;
} catch (accidentalStrictMode) {
  // This module should not be running in strict mode, so the above
  // assignment should always work unless something is misconfigured. Just
  // in case runtime.js accidentally runs in strict mode, in modern engines
  // we can explicitly access globalThis. In older engines we can escape
  // strict mode using a global Function call. This could conceivably fail
  // if a Content Security Policy forbids using Function, but in that case
  // the proper solution is to fix the accidental strict mode problem. If
  // you've misconfigured your bundler to force strict mode and applied a
  // CSP to forbid Function, and you're not willing to fix either of those
  // problems, please detail your unique predicament in a GitHub issue.
  if (typeof globalThis === "object") {
    globalThis.regeneratorRuntime = runtime;
  } else {
    Function("r", "regeneratorRuntime = r")(runtime);
  }
}


/***/ }),

/***/ "./node_modules/setimmediate/setImmediate.js":
/*!***************************************************!*\
  !*** ./node_modules/setimmediate/setImmediate.js ***!
  \***************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global, process) {(function (global, undefined) {
    "use strict";

    if (global.setImmediate) {
        return;
    }

    var nextHandle = 1; // Spec says greater than zero
    var tasksByHandle = {};
    var currentlyRunningATask = false;
    var doc = global.document;
    var registerImmediate;

    function setImmediate(callback) {
      // Callback can either be a function or a string
      if (typeof callback !== "function") {
        callback = new Function("" + callback);
      }
      // Copy function arguments
      var args = new Array(arguments.length - 1);
      for (var i = 0; i < args.length; i++) {
          args[i] = arguments[i + 1];
      }
      // Store and register the task
      var task = { callback: callback, args: args };
      tasksByHandle[nextHandle] = task;
      registerImmediate(nextHandle);
      return nextHandle++;
    }

    function clearImmediate(handle) {
        delete tasksByHandle[handle];
    }

    function run(task) {
        var callback = task.callback;
        var args = task.args;
        switch (args.length) {
        case 0:
            callback();
            break;
        case 1:
            callback(args[0]);
            break;
        case 2:
            callback(args[0], args[1]);
            break;
        case 3:
            callback(args[0], args[1], args[2]);
            break;
        default:
            callback.apply(undefined, args);
            break;
        }
    }

    function runIfPresent(handle) {
        // From the spec: "Wait until any invocations of this algorithm started before this one have completed."
        // So if we're currently running a task, we'll need to delay this invocation.
        if (currentlyRunningATask) {
            // Delay by doing a setTimeout. setImmediate was tried instead, but in Firefox 7 it generated a
            // "too much recursion" error.
            setTimeout(runIfPresent, 0, handle);
        } else {
            var task = tasksByHandle[handle];
            if (task) {
                currentlyRunningATask = true;
                try {
                    run(task);
                } finally {
                    clearImmediate(handle);
                    currentlyRunningATask = false;
                }
            }
        }
    }

    function installNextTickImplementation() {
        registerImmediate = function(handle) {
            process.nextTick(function () { runIfPresent(handle); });
        };
    }

    function canUsePostMessage() {
        // The test against `importScripts` prevents this implementation from being installed inside a web worker,
        // where `global.postMessage` means something completely different and can't be used for this purpose.
        if (global.postMessage && !global.importScripts) {
            var postMessageIsAsynchronous = true;
            var oldOnMessage = global.onmessage;
            global.onmessage = function() {
                postMessageIsAsynchronous = false;
            };
            global.postMessage("", "*");
            global.onmessage = oldOnMessage;
            return postMessageIsAsynchronous;
        }
    }

    function installPostMessageImplementation() {
        // Installs an event handler on `global` for the `message` event: see
        // * https://developer.mozilla.org/en/DOM/window.postMessage
        // * http://www.whatwg.org/specs/web-apps/current-work/multipage/comms.html#crossDocumentMessages

        var messagePrefix = "setImmediate$" + Math.random() + "$";
        var onGlobalMessage = function(event) {
            if (event.source === global &&
                typeof event.data === "string" &&
                event.data.indexOf(messagePrefix) === 0) {
                runIfPresent(+event.data.slice(messagePrefix.length));
            }
        };

        if (global.addEventListener) {
            global.addEventListener("message", onGlobalMessage, false);
        } else {
            global.attachEvent("onmessage", onGlobalMessage);
        }

        registerImmediate = function(handle) {
            global.postMessage(messagePrefix + handle, "*");
        };
    }

    function installMessageChannelImplementation() {
        var channel = new MessageChannel();
        channel.port1.onmessage = function(event) {
            var handle = event.data;
            runIfPresent(handle);
        };

        registerImmediate = function(handle) {
            channel.port2.postMessage(handle);
        };
    }

    function installReadyStateChangeImplementation() {
        var html = doc.documentElement;
        registerImmediate = function(handle) {
            // Create a <script> element; its readystatechange event will be fired asynchronously once it is inserted
            // into the document. Do so, thus queuing up the task. Remember to clean up once it's been called.
            var script = doc.createElement("script");
            script.onreadystatechange = function () {
                runIfPresent(handle);
                script.onreadystatechange = null;
                html.removeChild(script);
                script = null;
            };
            html.appendChild(script);
        };
    }

    function installSetTimeoutImplementation() {
        registerImmediate = function(handle) {
            setTimeout(runIfPresent, 0, handle);
        };
    }

    // If supported, we should attach to the prototype of global, since that is where setTimeout et al. live.
    var attachTo = Object.getPrototypeOf && Object.getPrototypeOf(global);
    attachTo = attachTo && attachTo.setTimeout ? attachTo : global;

    // Don't get fooled by e.g. browserify environments.
    if ({}.toString.call(global.process) === "[object process]") {
        // For Node.js before 0.9
        installNextTickImplementation();

    } else if (canUsePostMessage()) {
        // For non-IE10 modern browsers
        installPostMessageImplementation();

    } else if (global.MessageChannel) {
        // For web workers, where supported
        installMessageChannelImplementation();

    } else if (doc && "onreadystatechange" in doc.createElement("script")) {
        // For IE 6–8
        installReadyStateChangeImplementation();

    } else {
        // For older browsers
        installSetTimeoutImplementation();
    }

    attachTo.setImmediate = setImmediate;
    attachTo.clearImmediate = clearImmediate;
}(typeof self === "undefined" ? typeof global === "undefined" ? this : global : self));

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js"), __webpack_require__(/*! ./../process/browser.js */ "./node_modules/process/browser.js")))

/***/ }),

/***/ "./node_modules/timers-browserify/main.js":
/*!************************************************!*\
  !*** ./node_modules/timers-browserify/main.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {var scope = (typeof global !== "undefined" && global) ||
            (typeof self !== "undefined" && self) ||
            window;
var apply = Function.prototype.apply;

// DOM APIs, for completeness

exports.setTimeout = function() {
  return new Timeout(apply.call(setTimeout, scope, arguments), clearTimeout);
};
exports.setInterval = function() {
  return new Timeout(apply.call(setInterval, scope, arguments), clearInterval);
};
exports.clearTimeout =
exports.clearInterval = function(timeout) {
  if (timeout) {
    timeout.close();
  }
};

function Timeout(id, clearFn) {
  this._id = id;
  this._clearFn = clearFn;
}
Timeout.prototype.unref = Timeout.prototype.ref = function() {};
Timeout.prototype.close = function() {
  this._clearFn.call(scope, this._id);
};

// Does not start the time, just sets up the members needed.
exports.enroll = function(item, msecs) {
  clearTimeout(item._idleTimeoutId);
  item._idleTimeout = msecs;
};

exports.unenroll = function(item) {
  clearTimeout(item._idleTimeoutId);
  item._idleTimeout = -1;
};

exports._unrefActive = exports.active = function(item) {
  clearTimeout(item._idleTimeoutId);

  var msecs = item._idleTimeout;
  if (msecs >= 0) {
    item._idleTimeoutId = setTimeout(function onTimeout() {
      if (item._onTimeout)
        item._onTimeout();
    }, msecs);
  }
};

// setimmediate attaches itself to the global object
__webpack_require__(/*! setimmediate */ "./node_modules/setimmediate/setImmediate.js");
// On some exotic environments, it's not clear which object `setimmediate` was
// able to install onto.  Search each possibility in the same order as the
// `setimmediate` library.
exports.setImmediate = (typeof self !== "undefined" && self.setImmediate) ||
                       (typeof global !== "undefined" && global.setImmediate) ||
                       (this && this.setImmediate);
exports.clearImmediate = (typeof self !== "undefined" && self.clearImmediate) ||
                         (typeof global !== "undefined" && global.clearImmediate) ||
                         (this && this.clearImmediate);

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "./node_modules/vue/dist/vue.common.dev.js":
/*!*************************************************!*\
  !*** ./node_modules/vue/dist/vue.common.dev.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global, setImmediate) {/*!
 * Vue.js v2.7.14
 * (c) 2014-2022 Evan You
 * Released under the MIT License.
 */


const emptyObject = Object.freeze({});
const isArray = Array.isArray;
// These helpers produce better VM code in JS engines due to their
// explicitness and function inlining.
function isUndef(v) {
    return v === undefined || v === null;
}
function isDef(v) {
    return v !== undefined && v !== null;
}
function isTrue(v) {
    return v === true;
}
function isFalse(v) {
    return v === false;
}
/**
 * Check if value is primitive.
 */
function isPrimitive(value) {
    return (typeof value === 'string' ||
        typeof value === 'number' ||
        // $flow-disable-line
        typeof value === 'symbol' ||
        typeof value === 'boolean');
}
function isFunction(value) {
    return typeof value === 'function';
}
/**
 * Quick object check - this is primarily used to tell
 * objects from primitive values when we know the value
 * is a JSON-compliant type.
 */
function isObject(obj) {
    return obj !== null && typeof obj === 'object';
}
/**
 * Get the raw type string of a value, e.g., [object Object].
 */
const _toString = Object.prototype.toString;
function toRawType(value) {
    return _toString.call(value).slice(8, -1);
}
/**
 * Strict object type check. Only returns true
 * for plain JavaScript objects.
 */
function isPlainObject(obj) {
    return _toString.call(obj) === '[object Object]';
}
function isRegExp(v) {
    return _toString.call(v) === '[object RegExp]';
}
/**
 * Check if val is a valid array index.
 */
function isValidArrayIndex(val) {
    const n = parseFloat(String(val));
    return n >= 0 && Math.floor(n) === n && isFinite(val);
}
function isPromise(val) {
    return (isDef(val) &&
        typeof val.then === 'function' &&
        typeof val.catch === 'function');
}
/**
 * Convert a value to a string that is actually rendered.
 */
function toString(val) {
    return val == null
        ? ''
        : Array.isArray(val) || (isPlainObject(val) && val.toString === _toString)
            ? JSON.stringify(val, null, 2)
            : String(val);
}
/**
 * Convert an input value to a number for persistence.
 * If the conversion fails, return original string.
 */
function toNumber(val) {
    const n = parseFloat(val);
    return isNaN(n) ? val : n;
}
/**
 * Make a map and return a function for checking if a key
 * is in that map.
 */
function makeMap(str, expectsLowerCase) {
    const map = Object.create(null);
    const list = str.split(',');
    for (let i = 0; i < list.length; i++) {
        map[list[i]] = true;
    }
    return expectsLowerCase ? val => map[val.toLowerCase()] : val => map[val];
}
/**
 * Check if a tag is a built-in tag.
 */
const isBuiltInTag = makeMap('slot,component', true);
/**
 * Check if an attribute is a reserved attribute.
 */
const isReservedAttribute = makeMap('key,ref,slot,slot-scope,is');
/**
 * Remove an item from an array.
 */
function remove$2(arr, item) {
    const len = arr.length;
    if (len) {
        // fast path for the only / last item
        if (item === arr[len - 1]) {
            arr.length = len - 1;
            return;
        }
        const index = arr.indexOf(item);
        if (index > -1) {
            return arr.splice(index, 1);
        }
    }
}
/**
 * Check whether an object has the property.
 */
const hasOwnProperty = Object.prototype.hasOwnProperty;
function hasOwn(obj, key) {
    return hasOwnProperty.call(obj, key);
}
/**
 * Create a cached version of a pure function.
 */
function cached(fn) {
    const cache = Object.create(null);
    return function cachedFn(str) {
        const hit = cache[str];
        return hit || (cache[str] = fn(str));
    };
}
/**
 * Camelize a hyphen-delimited string.
 */
const camelizeRE = /-(\w)/g;
const camelize = cached((str) => {
    return str.replace(camelizeRE, (_, c) => (c ? c.toUpperCase() : ''));
});
/**
 * Capitalize a string.
 */
const capitalize = cached((str) => {
    return str.charAt(0).toUpperCase() + str.slice(1);
});
/**
 * Hyphenate a camelCase string.
 */
const hyphenateRE = /\B([A-Z])/g;
const hyphenate = cached((str) => {
    return str.replace(hyphenateRE, '-$1').toLowerCase();
});
/**
 * Simple bind polyfill for environments that do not support it,
 * e.g., PhantomJS 1.x. Technically, we don't need this anymore
 * since native bind is now performant enough in most browsers.
 * But removing it would mean breaking code that was able to run in
 * PhantomJS 1.x, so this must be kept for backward compatibility.
 */
/* istanbul ignore next */
function polyfillBind(fn, ctx) {
    function boundFn(a) {
        const l = arguments.length;
        return l
            ? l > 1
                ? fn.apply(ctx, arguments)
                : fn.call(ctx, a)
            : fn.call(ctx);
    }
    boundFn._length = fn.length;
    return boundFn;
}
function nativeBind(fn, ctx) {
    return fn.bind(ctx);
}
// @ts-expect-error bind cannot be `undefined`
const bind$1 = Function.prototype.bind ? nativeBind : polyfillBind;
/**
 * Convert an Array-like object to a real Array.
 */
function toArray(list, start) {
    start = start || 0;
    let i = list.length - start;
    const ret = new Array(i);
    while (i--) {
        ret[i] = list[i + start];
    }
    return ret;
}
/**
 * Mix properties into target object.
 */
function extend(to, _from) {
    for (const key in _from) {
        to[key] = _from[key];
    }
    return to;
}
/**
 * Merge an Array of Objects into a single Object.
 */
function toObject(arr) {
    const res = {};
    for (let i = 0; i < arr.length; i++) {
        if (arr[i]) {
            extend(res, arr[i]);
        }
    }
    return res;
}
/* eslint-disable no-unused-vars */
/**
 * Perform no operation.
 * Stubbing args to make Flow happy without leaving useless transpiled code
 * with ...rest (https://flow.org/blog/2017/05/07/Strict-Function-Call-Arity/).
 */
function noop(a, b, c) { }
/**
 * Always return false.
 */
const no = (a, b, c) => false;
/* eslint-enable no-unused-vars */
/**
 * Return the same value.
 */
const identity = (_) => _;
/**
 * Generate a string containing static keys from compiler modules.
 */
function genStaticKeys$1(modules) {
    return modules
        .reduce((keys, m) => {
        return keys.concat(m.staticKeys || []);
    }, [])
        .join(',');
}
/**
 * Check if two values are loosely equal - that is,
 * if they are plain objects, do they have the same shape?
 */
function looseEqual(a, b) {
    if (a === b)
        return true;
    const isObjectA = isObject(a);
    const isObjectB = isObject(b);
    if (isObjectA && isObjectB) {
        try {
            const isArrayA = Array.isArray(a);
            const isArrayB = Array.isArray(b);
            if (isArrayA && isArrayB) {
                return (a.length === b.length &&
                    a.every((e, i) => {
                        return looseEqual(e, b[i]);
                    }));
            }
            else if (a instanceof Date && b instanceof Date) {
                return a.getTime() === b.getTime();
            }
            else if (!isArrayA && !isArrayB) {
                const keysA = Object.keys(a);
                const keysB = Object.keys(b);
                return (keysA.length === keysB.length &&
                    keysA.every(key => {
                        return looseEqual(a[key], b[key]);
                    }));
            }
            else {
                /* istanbul ignore next */
                return false;
            }
        }
        catch (e) {
            /* istanbul ignore next */
            return false;
        }
    }
    else if (!isObjectA && !isObjectB) {
        return String(a) === String(b);
    }
    else {
        return false;
    }
}
/**
 * Return the first index at which a loosely equal value can be
 * found in the array (if value is a plain object, the array must
 * contain an object of the same shape), or -1 if it is not present.
 */
function looseIndexOf(arr, val) {
    for (let i = 0; i < arr.length; i++) {
        if (looseEqual(arr[i], val))
            return i;
    }
    return -1;
}
/**
 * Ensure a function is called only once.
 */
function once(fn) {
    let called = false;
    return function () {
        if (!called) {
            called = true;
            fn.apply(this, arguments);
        }
    };
}
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is#polyfill
function hasChanged(x, y) {
    if (x === y) {
        return x === 0 && 1 / x !== 1 / y;
    }
    else {
        return x === x || y === y;
    }
}

const SSR_ATTR = 'data-server-rendered';
const ASSET_TYPES = ['component', 'directive', 'filter'];
const LIFECYCLE_HOOKS = [
    'beforeCreate',
    'created',
    'beforeMount',
    'mounted',
    'beforeUpdate',
    'updated',
    'beforeDestroy',
    'destroyed',
    'activated',
    'deactivated',
    'errorCaptured',
    'serverPrefetch',
    'renderTracked',
    'renderTriggered'
];

var config = {
    /**
     * Option merge strategies (used in core/util/options)
     */
    // $flow-disable-line
    optionMergeStrategies: Object.create(null),
    /**
     * Whether to suppress warnings.
     */
    silent: false,
    /**
     * Show production mode tip message on boot?
     */
    productionTip: true,
    /**
     * Whether to enable devtools
     */
    devtools: true,
    /**
     * Whether to record perf
     */
    performance: false,
    /**
     * Error handler for watcher errors
     */
    errorHandler: null,
    /**
     * Warn handler for watcher warns
     */
    warnHandler: null,
    /**
     * Ignore certain custom elements
     */
    ignoredElements: [],
    /**
     * Custom user key aliases for v-on
     */
    // $flow-disable-line
    keyCodes: Object.create(null),
    /**
     * Check if a tag is reserved so that it cannot be registered as a
     * component. This is platform-dependent and may be overwritten.
     */
    isReservedTag: no,
    /**
     * Check if an attribute is reserved so that it cannot be used as a component
     * prop. This is platform-dependent and may be overwritten.
     */
    isReservedAttr: no,
    /**
     * Check if a tag is an unknown element.
     * Platform-dependent.
     */
    isUnknownElement: no,
    /**
     * Get the namespace of an element
     */
    getTagNamespace: noop,
    /**
     * Parse the real tag name for the specific platform.
     */
    parsePlatformTagName: identity,
    /**
     * Check if an attribute must be bound using property, e.g. value
     * Platform-dependent.
     */
    mustUseProp: no,
    /**
     * Perform updates asynchronously. Intended to be used by Vue Test Utils
     * This will significantly reduce performance if set to false.
     */
    async: true,
    /**
     * Exposed for legacy reasons
     */
    _lifecycleHooks: LIFECYCLE_HOOKS
};

/**
 * unicode letters used for parsing html tags, component names and property paths.
 * using https://www.w3.org/TR/html53/semantics-scripting.html#potentialcustomelementname
 * skipping \u10000-\uEFFFF due to it freezing up PhantomJS
 */
const unicodeRegExp = /a-zA-Z\u00B7\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u037D\u037F-\u1FFF\u200C-\u200D\u203F-\u2040\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD/;
/**
 * Check if a string starts with $ or _
 */
function isReserved(str) {
    const c = (str + '').charCodeAt(0);
    return c === 0x24 || c === 0x5f;
}
/**
 * Define a property.
 */
function def(obj, key, val, enumerable) {
    Object.defineProperty(obj, key, {
        value: val,
        enumerable: !!enumerable,
        writable: true,
        configurable: true
    });
}
/**
 * Parse simple path.
 */
const bailRE = new RegExp(`[^${unicodeRegExp.source}.$_\\d]`);
function parsePath(path) {
    if (bailRE.test(path)) {
        return;
    }
    const segments = path.split('.');
    return function (obj) {
        for (let i = 0; i < segments.length; i++) {
            if (!obj)
                return;
            obj = obj[segments[i]];
        }
        return obj;
    };
}

// can we use __proto__?
const hasProto = '__proto__' in {};
// Browser environment sniffing
const inBrowser = typeof window !== 'undefined';
const UA = inBrowser && window.navigator.userAgent.toLowerCase();
const isIE = UA && /msie|trident/.test(UA);
const isIE9 = UA && UA.indexOf('msie 9.0') > 0;
const isEdge = UA && UA.indexOf('edge/') > 0;
UA && UA.indexOf('android') > 0;
const isIOS = UA && /iphone|ipad|ipod|ios/.test(UA);
UA && /chrome\/\d+/.test(UA) && !isEdge;
UA && /phantomjs/.test(UA);
const isFF = UA && UA.match(/firefox\/(\d+)/);
// Firefox has a "watch" function on Object.prototype...
// @ts-expect-error firebox support
const nativeWatch = {}.watch;
let supportsPassive = false;
if (inBrowser) {
    try {
        const opts = {};
        Object.defineProperty(opts, 'passive', {
            get() {
                /* istanbul ignore next */
                supportsPassive = true;
            }
        }); // https://github.com/facebook/flow/issues/285
        window.addEventListener('test-passive', null, opts);
    }
    catch (e) { }
}
// this needs to be lazy-evaled because vue may be required before
// vue-server-renderer can set VUE_ENV
let _isServer;
const isServerRendering = () => {
    if (_isServer === undefined) {
        /* istanbul ignore if */
        if (!inBrowser && typeof global !== 'undefined') {
            // detect presence of vue-server-renderer and avoid
            // Webpack shimming the process
            _isServer =
                global['process'] && global['process'].env.VUE_ENV === 'server';
        }
        else {
            _isServer = false;
        }
    }
    return _isServer;
};
// detect devtools
const devtools = inBrowser && window.__VUE_DEVTOOLS_GLOBAL_HOOK__;
/* istanbul ignore next */
function isNative(Ctor) {
    return typeof Ctor === 'function' && /native code/.test(Ctor.toString());
}
const hasSymbol = typeof Symbol !== 'undefined' &&
    isNative(Symbol) &&
    typeof Reflect !== 'undefined' &&
    isNative(Reflect.ownKeys);
let _Set; // $flow-disable-line
/* istanbul ignore if */ if (typeof Set !== 'undefined' && isNative(Set)) {
    // use native Set when available.
    _Set = Set;
}
else {
    // a non-standard Set polyfill that only works with primitive keys.
    _Set = class Set {
        constructor() {
            this.set = Object.create(null);
        }
        has(key) {
            return this.set[key] === true;
        }
        add(key) {
            this.set[key] = true;
        }
        clear() {
            this.set = Object.create(null);
        }
    };
}

let currentInstance = null;
/**
 * This is exposed for compatibility with v3 (e.g. some functions in VueUse
 * relies on it). Do not use this internally, just use `currentInstance`.
 *
 * @internal this function needs manual type declaration because it relies
 * on previously manually authored types from Vue 2
 */
function getCurrentInstance() {
    return currentInstance && { proxy: currentInstance };
}
/**
 * @internal
 */
function setCurrentInstance(vm = null) {
    if (!vm)
        currentInstance && currentInstance._scope.off();
    currentInstance = vm;
    vm && vm._scope.on();
}

/**
 * @internal
 */
class VNode {
    constructor(tag, data, children, text, elm, context, componentOptions, asyncFactory) {
        this.tag = tag;
        this.data = data;
        this.children = children;
        this.text = text;
        this.elm = elm;
        this.ns = undefined;
        this.context = context;
        this.fnContext = undefined;
        this.fnOptions = undefined;
        this.fnScopeId = undefined;
        this.key = data && data.key;
        this.componentOptions = componentOptions;
        this.componentInstance = undefined;
        this.parent = undefined;
        this.raw = false;
        this.isStatic = false;
        this.isRootInsert = true;
        this.isComment = false;
        this.isCloned = false;
        this.isOnce = false;
        this.asyncFactory = asyncFactory;
        this.asyncMeta = undefined;
        this.isAsyncPlaceholder = false;
    }
    // DEPRECATED: alias for componentInstance for backwards compat.
    /* istanbul ignore next */
    get child() {
        return this.componentInstance;
    }
}
const createEmptyVNode = (text = '') => {
    const node = new VNode();
    node.text = text;
    node.isComment = true;
    return node;
};
function createTextVNode(val) {
    return new VNode(undefined, undefined, undefined, String(val));
}
// optimized shallow clone
// used for static nodes and slot nodes because they may be reused across
// multiple renders, cloning them avoids errors when DOM manipulations rely
// on their elm reference.
function cloneVNode(vnode) {
    const cloned = new VNode(vnode.tag, vnode.data, 
    // #7975
    // clone children array to avoid mutating original in case of cloning
    // a child.
    vnode.children && vnode.children.slice(), vnode.text, vnode.elm, vnode.context, vnode.componentOptions, vnode.asyncFactory);
    cloned.ns = vnode.ns;
    cloned.isStatic = vnode.isStatic;
    cloned.key = vnode.key;
    cloned.isComment = vnode.isComment;
    cloned.fnContext = vnode.fnContext;
    cloned.fnOptions = vnode.fnOptions;
    cloned.fnScopeId = vnode.fnScopeId;
    cloned.asyncMeta = vnode.asyncMeta;
    cloned.isCloned = true;
    return cloned;
}

/* not type checking this file because flow doesn't play well with Proxy */
let initProxy;
{
    const allowedGlobals = makeMap('Infinity,undefined,NaN,isFinite,isNaN,' +
        'parseFloat,parseInt,decodeURI,decodeURIComponent,encodeURI,encodeURIComponent,' +
        'Math,Number,Date,Array,Object,Boolean,String,RegExp,Map,Set,JSON,Intl,BigInt,' +
        'require' // for Webpack/Browserify
    );
    const warnNonPresent = (target, key) => {
        warn$2(`Property or method "${key}" is not defined on the instance but ` +
            'referenced during render. Make sure that this property is reactive, ' +
            'either in the data option, or for class-based components, by ' +
            'initializing the property. ' +
            'See: https://v2.vuejs.org/v2/guide/reactivity.html#Declaring-Reactive-Properties.', target);
    };
    const warnReservedPrefix = (target, key) => {
        warn$2(`Property "${key}" must be accessed with "$data.${key}" because ` +
            'properties starting with "$" or "_" are not proxied in the Vue instance to ' +
            'prevent conflicts with Vue internals. ' +
            'See: https://v2.vuejs.org/v2/api/#data', target);
    };
    const hasProxy = typeof Proxy !== 'undefined' && isNative(Proxy);
    if (hasProxy) {
        const isBuiltInModifier = makeMap('stop,prevent,self,ctrl,shift,alt,meta,exact');
        config.keyCodes = new Proxy(config.keyCodes, {
            set(target, key, value) {
                if (isBuiltInModifier(key)) {
                    warn$2(`Avoid overwriting built-in modifier in config.keyCodes: .${key}`);
                    return false;
                }
                else {
                    target[key] = value;
                    return true;
                }
            }
        });
    }
    const hasHandler = {
        has(target, key) {
            const has = key in target;
            const isAllowed = allowedGlobals(key) ||
                (typeof key === 'string' &&
                    key.charAt(0) === '_' &&
                    !(key in target.$data));
            if (!has && !isAllowed) {
                if (key in target.$data)
                    warnReservedPrefix(target, key);
                else
                    warnNonPresent(target, key);
            }
            return has || !isAllowed;
        }
    };
    const getHandler = {
        get(target, key) {
            if (typeof key === 'string' && !(key in target)) {
                if (key in target.$data)
                    warnReservedPrefix(target, key);
                else
                    warnNonPresent(target, key);
            }
            return target[key];
        }
    };
    initProxy = function initProxy(vm) {
        if (hasProxy) {
            // determine which proxy handler to use
            const options = vm.$options;
            const handlers = options.render && options.render._withStripped ? getHandler : hasHandler;
            vm._renderProxy = new Proxy(vm, handlers);
        }
        else {
            vm._renderProxy = vm;
        }
    };
}

let uid$2 = 0;
const pendingCleanupDeps = [];
const cleanupDeps = () => {
    for (let i = 0; i < pendingCleanupDeps.length; i++) {
        const dep = pendingCleanupDeps[i];
        dep.subs = dep.subs.filter(s => s);
        dep._pending = false;
    }
    pendingCleanupDeps.length = 0;
};
/**
 * A dep is an observable that can have multiple
 * directives subscribing to it.
 * @internal
 */
class Dep {
    constructor() {
        // pending subs cleanup
        this._pending = false;
        this.id = uid$2++;
        this.subs = [];
    }
    addSub(sub) {
        this.subs.push(sub);
    }
    removeSub(sub) {
        // #12696 deps with massive amount of subscribers are extremely slow to
        // clean up in Chromium
        // to workaround this, we unset the sub for now, and clear them on
        // next scheduler flush.
        this.subs[this.subs.indexOf(sub)] = null;
        if (!this._pending) {
            this._pending = true;
            pendingCleanupDeps.push(this);
        }
    }
    depend(info) {
        if (Dep.target) {
            Dep.target.addDep(this);
            if (info && Dep.target.onTrack) {
                Dep.target.onTrack(Object.assign({ effect: Dep.target }, info));
            }
        }
    }
    notify(info) {
        // stabilize the subscriber list first
        const subs = this.subs.filter(s => s);
        if (!config.async) {
            // subs aren't sorted in scheduler if not running async
            // we need to sort them now to make sure they fire in correct
            // order
            subs.sort((a, b) => a.id - b.id);
        }
        for (let i = 0, l = subs.length; i < l; i++) {
            const sub = subs[i];
            if (info) {
                sub.onTrigger &&
                    sub.onTrigger(Object.assign({ effect: subs[i] }, info));
            }
            sub.update();
        }
    }
}
// The current target watcher being evaluated.
// This is globally unique because only one watcher
// can be evaluated at a time.
Dep.target = null;
const targetStack = [];
function pushTarget(target) {
    targetStack.push(target);
    Dep.target = target;
}
function popTarget() {
    targetStack.pop();
    Dep.target = targetStack[targetStack.length - 1];
}

/*
 * not type checking this file because flow doesn't play well with
 * dynamically accessing methods on Array prototype
 */
const arrayProto = Array.prototype;
const arrayMethods = Object.create(arrayProto);
const methodsToPatch = [
    'push',
    'pop',
    'shift',
    'unshift',
    'splice',
    'sort',
    'reverse'
];
/**
 * Intercept mutating methods and emit events
 */
methodsToPatch.forEach(function (method) {
    // cache original method
    const original = arrayProto[method];
    def(arrayMethods, method, function mutator(...args) {
        const result = original.apply(this, args);
        const ob = this.__ob__;
        let inserted;
        switch (method) {
            case 'push':
            case 'unshift':
                inserted = args;
                break;
            case 'splice':
                inserted = args.slice(2);
                break;
        }
        if (inserted)
            ob.observeArray(inserted);
        // notify change
        {
            ob.dep.notify({
                type: "array mutation" /* TriggerOpTypes.ARRAY_MUTATION */,
                target: this,
                key: method
            });
        }
        return result;
    });
});

const arrayKeys = Object.getOwnPropertyNames(arrayMethods);
const NO_INIITIAL_VALUE = {};
/**
 * In some cases we may want to disable observation inside a component's
 * update computation.
 */
let shouldObserve = true;
function toggleObserving(value) {
    shouldObserve = value;
}
// ssr mock dep
const mockDep = {
    notify: noop,
    depend: noop,
    addSub: noop,
    removeSub: noop
};
/**
 * Observer class that is attached to each observed
 * object. Once attached, the observer converts the target
 * object's property keys into getter/setters that
 * collect dependencies and dispatch updates.
 */
class Observer {
    constructor(value, shallow = false, mock = false) {
        this.value = value;
        this.shallow = shallow;
        this.mock = mock;
        // this.value = value
        this.dep = mock ? mockDep : new Dep();
        this.vmCount = 0;
        def(value, '__ob__', this);
        if (isArray(value)) {
            if (!mock) {
                if (hasProto) {
                    value.__proto__ = arrayMethods;
                    /* eslint-enable no-proto */
                }
                else {
                    for (let i = 0, l = arrayKeys.length; i < l; i++) {
                        const key = arrayKeys[i];
                        def(value, key, arrayMethods[key]);
                    }
                }
            }
            if (!shallow) {
                this.observeArray(value);
            }
        }
        else {
            /**
             * Walk through all properties and convert them into
             * getter/setters. This method should only be called when
             * value type is Object.
             */
            const keys = Object.keys(value);
            for (let i = 0; i < keys.length; i++) {
                const key = keys[i];
                defineReactive(value, key, NO_INIITIAL_VALUE, undefined, shallow, mock);
            }
        }
    }
    /**
     * Observe a list of Array items.
     */
    observeArray(value) {
        for (let i = 0, l = value.length; i < l; i++) {
            observe(value[i], false, this.mock);
        }
    }
}
// helpers
/**
 * Attempt to create an observer instance for a value,
 * returns the new observer if successfully observed,
 * or the existing observer if the value already has one.
 */
function observe(value, shallow, ssrMockReactivity) {
    if (value && hasOwn(value, '__ob__') && value.__ob__ instanceof Observer) {
        return value.__ob__;
    }
    if (shouldObserve &&
        (ssrMockReactivity || !isServerRendering()) &&
        (isArray(value) || isPlainObject(value)) &&
        Object.isExtensible(value) &&
        !value.__v_skip /* ReactiveFlags.SKIP */ &&
        !isRef(value) &&
        !(value instanceof VNode)) {
        return new Observer(value, shallow, ssrMockReactivity);
    }
}
/**
 * Define a reactive property on an Object.
 */
function defineReactive(obj, key, val, customSetter, shallow, mock) {
    const dep = new Dep();
    const property = Object.getOwnPropertyDescriptor(obj, key);
    if (property && property.configurable === false) {
        return;
    }
    // cater for pre-defined getter/setters
    const getter = property && property.get;
    const setter = property && property.set;
    if ((!getter || setter) &&
        (val === NO_INIITIAL_VALUE || arguments.length === 2)) {
        val = obj[key];
    }
    let childOb = !shallow && observe(val, false, mock);
    Object.defineProperty(obj, key, {
        enumerable: true,
        configurable: true,
        get: function reactiveGetter() {
            const value = getter ? getter.call(obj) : val;
            if (Dep.target) {
                {
                    dep.depend({
                        target: obj,
                        type: "get" /* TrackOpTypes.GET */,
                        key
                    });
                }
                if (childOb) {
                    childOb.dep.depend();
                    if (isArray(value)) {
                        dependArray(value);
                    }
                }
            }
            return isRef(value) && !shallow ? value.value : value;
        },
        set: function reactiveSetter(newVal) {
            const value = getter ? getter.call(obj) : val;
            if (!hasChanged(value, newVal)) {
                return;
            }
            if (customSetter) {
                customSetter();
            }
            if (setter) {
                setter.call(obj, newVal);
            }
            else if (getter) {
                // #7981: for accessor properties without setter
                return;
            }
            else if (!shallow && isRef(value) && !isRef(newVal)) {
                value.value = newVal;
                return;
            }
            else {
                val = newVal;
            }
            childOb = !shallow && observe(newVal, false, mock);
            {
                dep.notify({
                    type: "set" /* TriggerOpTypes.SET */,
                    target: obj,
                    key,
                    newValue: newVal,
                    oldValue: value
                });
            }
        }
    });
    return dep;
}
function set(target, key, val) {
    if ((isUndef(target) || isPrimitive(target))) {
        warn$2(`Cannot set reactive property on undefined, null, or primitive value: ${target}`);
    }
    if (isReadonly(target)) {
        warn$2(`Set operation on key "${key}" failed: target is readonly.`);
        return;
    }
    const ob = target.__ob__;
    if (isArray(target) && isValidArrayIndex(key)) {
        target.length = Math.max(target.length, key);
        target.splice(key, 1, val);
        // when mocking for SSR, array methods are not hijacked
        if (ob && !ob.shallow && ob.mock) {
            observe(val, false, true);
        }
        return val;
    }
    if (key in target && !(key in Object.prototype)) {
        target[key] = val;
        return val;
    }
    if (target._isVue || (ob && ob.vmCount)) {
        warn$2('Avoid adding reactive properties to a Vue instance or its root $data ' +
                'at runtime - declare it upfront in the data option.');
        return val;
    }
    if (!ob) {
        target[key] = val;
        return val;
    }
    defineReactive(ob.value, key, val, undefined, ob.shallow, ob.mock);
    {
        ob.dep.notify({
            type: "add" /* TriggerOpTypes.ADD */,
            target: target,
            key,
            newValue: val,
            oldValue: undefined
        });
    }
    return val;
}
function del(target, key) {
    if ((isUndef(target) || isPrimitive(target))) {
        warn$2(`Cannot delete reactive property on undefined, null, or primitive value: ${target}`);
    }
    if (isArray(target) && isValidArrayIndex(key)) {
        target.splice(key, 1);
        return;
    }
    const ob = target.__ob__;
    if (target._isVue || (ob && ob.vmCount)) {
        warn$2('Avoid deleting properties on a Vue instance or its root $data ' +
                '- just set it to null.');
        return;
    }
    if (isReadonly(target)) {
        warn$2(`Delete operation on key "${key}" failed: target is readonly.`);
        return;
    }
    if (!hasOwn(target, key)) {
        return;
    }
    delete target[key];
    if (!ob) {
        return;
    }
    {
        ob.dep.notify({
            type: "delete" /* TriggerOpTypes.DELETE */,
            target: target,
            key
        });
    }
}
/**
 * Collect dependencies on array elements when the array is touched, since
 * we cannot intercept array element access like property getters.
 */
function dependArray(value) {
    for (let e, i = 0, l = value.length; i < l; i++) {
        e = value[i];
        if (e && e.__ob__) {
            e.__ob__.dep.depend();
        }
        if (isArray(e)) {
            dependArray(e);
        }
    }
}

function reactive(target) {
    makeReactive(target, false);
    return target;
}
/**
 * Return a shallowly-reactive copy of the original object, where only the root
 * level properties are reactive. It also does not auto-unwrap refs (even at the
 * root level).
 */
function shallowReactive(target) {
    makeReactive(target, true);
    def(target, "__v_isShallow" /* ReactiveFlags.IS_SHALLOW */, true);
    return target;
}
function makeReactive(target, shallow) {
    // if trying to observe a readonly proxy, return the readonly version.
    if (!isReadonly(target)) {
        {
            if (isArray(target)) {
                warn$2(`Avoid using Array as root value for ${shallow ? `shallowReactive()` : `reactive()`} as it cannot be tracked in watch() or watchEffect(). Use ${shallow ? `shallowRef()` : `ref()`} instead. This is a Vue-2-only limitation.`);
            }
            const existingOb = target && target.__ob__;
            if (existingOb && existingOb.shallow !== shallow) {
                warn$2(`Target is already a ${existingOb.shallow ? `` : `non-`}shallow reactive object, and cannot be converted to ${shallow ? `` : `non-`}shallow.`);
            }
        }
        const ob = observe(target, shallow, isServerRendering() /* ssr mock reactivity */);
        if (!ob) {
            if (target == null || isPrimitive(target)) {
                warn$2(`value cannot be made reactive: ${String(target)}`);
            }
            if (isCollectionType(target)) {
                warn$2(`Vue 2 does not support reactive collection types such as Map or Set.`);
            }
        }
    }
}
function isReactive(value) {
    if (isReadonly(value)) {
        return isReactive(value["__v_raw" /* ReactiveFlags.RAW */]);
    }
    return !!(value && value.__ob__);
}
function isShallow(value) {
    return !!(value && value.__v_isShallow);
}
function isReadonly(value) {
    return !!(value && value.__v_isReadonly);
}
function isProxy(value) {
    return isReactive(value) || isReadonly(value);
}
function toRaw(observed) {
    const raw = observed && observed["__v_raw" /* ReactiveFlags.RAW */];
    return raw ? toRaw(raw) : observed;
}
function markRaw(value) {
    // non-extensible objects won't be observed anyway
    if (Object.isExtensible(value)) {
        def(value, "__v_skip" /* ReactiveFlags.SKIP */, true);
    }
    return value;
}
/**
 * @internal
 */
function isCollectionType(value) {
    const type = toRawType(value);
    return (type === 'Map' || type === 'WeakMap' || type === 'Set' || type === 'WeakSet');
}

/**
 * @internal
 */
const RefFlag = `__v_isRef`;
function isRef(r) {
    return !!(r && r.__v_isRef === true);
}
function ref$1(value) {
    return createRef(value, false);
}
function shallowRef(value) {
    return createRef(value, true);
}
function createRef(rawValue, shallow) {
    if (isRef(rawValue)) {
        return rawValue;
    }
    const ref = {};
    def(ref, RefFlag, true);
    def(ref, "__v_isShallow" /* ReactiveFlags.IS_SHALLOW */, shallow);
    def(ref, 'dep', defineReactive(ref, 'value', rawValue, null, shallow, isServerRendering()));
    return ref;
}
function triggerRef(ref) {
    if (!ref.dep) {
        warn$2(`received object is not a triggerable ref.`);
    }
    {
        ref.dep &&
            ref.dep.notify({
                type: "set" /* TriggerOpTypes.SET */,
                target: ref,
                key: 'value'
            });
    }
}
function unref(ref) {
    return isRef(ref) ? ref.value : ref;
}
function proxyRefs(objectWithRefs) {
    if (isReactive(objectWithRefs)) {
        return objectWithRefs;
    }
    const proxy = {};
    const keys = Object.keys(objectWithRefs);
    for (let i = 0; i < keys.length; i++) {
        proxyWithRefUnwrap(proxy, objectWithRefs, keys[i]);
    }
    return proxy;
}
function proxyWithRefUnwrap(target, source, key) {
    Object.defineProperty(target, key, {
        enumerable: true,
        configurable: true,
        get: () => {
            const val = source[key];
            if (isRef(val)) {
                return val.value;
            }
            else {
                const ob = val && val.__ob__;
                if (ob)
                    ob.dep.depend();
                return val;
            }
        },
        set: value => {
            const oldValue = source[key];
            if (isRef(oldValue) && !isRef(value)) {
                oldValue.value = value;
            }
            else {
                source[key] = value;
            }
        }
    });
}
function customRef(factory) {
    const dep = new Dep();
    const { get, set } = factory(() => {
        {
            dep.depend({
                target: ref,
                type: "get" /* TrackOpTypes.GET */,
                key: 'value'
            });
        }
    }, () => {
        {
            dep.notify({
                target: ref,
                type: "set" /* TriggerOpTypes.SET */,
                key: 'value'
            });
        }
    });
    const ref = {
        get value() {
            return get();
        },
        set value(newVal) {
            set(newVal);
        }
    };
    def(ref, RefFlag, true);
    return ref;
}
function toRefs(object) {
    if (!isReactive(object)) {
        warn$2(`toRefs() expects a reactive object but received a plain one.`);
    }
    const ret = isArray(object) ? new Array(object.length) : {};
    for (const key in object) {
        ret[key] = toRef(object, key);
    }
    return ret;
}
function toRef(object, key, defaultValue) {
    const val = object[key];
    if (isRef(val)) {
        return val;
    }
    const ref = {
        get value() {
            const val = object[key];
            return val === undefined ? defaultValue : val;
        },
        set value(newVal) {
            object[key] = newVal;
        }
    };
    def(ref, RefFlag, true);
    return ref;
}

const rawToReadonlyFlag = `__v_rawToReadonly`;
const rawToShallowReadonlyFlag = `__v_rawToShallowReadonly`;
function readonly(target) {
    return createReadonly(target, false);
}
function createReadonly(target, shallow) {
    if (!isPlainObject(target)) {
        {
            if (isArray(target)) {
                warn$2(`Vue 2 does not support readonly arrays.`);
            }
            else if (isCollectionType(target)) {
                warn$2(`Vue 2 does not support readonly collection types such as Map or Set.`);
            }
            else {
                warn$2(`value cannot be made readonly: ${typeof target}`);
            }
        }
        return target;
    }
    if (!Object.isExtensible(target)) {
        warn$2(`Vue 2 does not support creating readonly proxy for non-extensible object.`);
    }
    // already a readonly object
    if (isReadonly(target)) {
        return target;
    }
    // already has a readonly proxy
    const existingFlag = shallow ? rawToShallowReadonlyFlag : rawToReadonlyFlag;
    const existingProxy = target[existingFlag];
    if (existingProxy) {
        return existingProxy;
    }
    const proxy = Object.create(Object.getPrototypeOf(target));
    def(target, existingFlag, proxy);
    def(proxy, "__v_isReadonly" /* ReactiveFlags.IS_READONLY */, true);
    def(proxy, "__v_raw" /* ReactiveFlags.RAW */, target);
    if (isRef(target)) {
        def(proxy, RefFlag, true);
    }
    if (shallow || isShallow(target)) {
        def(proxy, "__v_isShallow" /* ReactiveFlags.IS_SHALLOW */, true);
    }
    const keys = Object.keys(target);
    for (let i = 0; i < keys.length; i++) {
        defineReadonlyProperty(proxy, target, keys[i], shallow);
    }
    return proxy;
}
function defineReadonlyProperty(proxy, target, key, shallow) {
    Object.defineProperty(proxy, key, {
        enumerable: true,
        configurable: true,
        get() {
            const val = target[key];
            return shallow || !isPlainObject(val) ? val : readonly(val);
        },
        set() {
            warn$2(`Set operation on key "${key}" failed: target is readonly.`);
        }
    });
}
/**
 * Returns a reactive-copy of the original object, where only the root level
 * properties are readonly, and does NOT unwrap refs nor recursively convert
 * returned properties.
 * This is used for creating the props proxy object for stateful components.
 */
function shallowReadonly(target) {
    return createReadonly(target, true);
}

function computed(getterOrOptions, debugOptions) {
    let getter;
    let setter;
    const onlyGetter = isFunction(getterOrOptions);
    if (onlyGetter) {
        getter = getterOrOptions;
        setter = () => {
                warn$2('Write operation failed: computed value is readonly');
            }
            ;
    }
    else {
        getter = getterOrOptions.get;
        setter = getterOrOptions.set;
    }
    const watcher = isServerRendering()
        ? null
        : new Watcher(currentInstance, getter, noop, { lazy: true });
    if (watcher && debugOptions) {
        watcher.onTrack = debugOptions.onTrack;
        watcher.onTrigger = debugOptions.onTrigger;
    }
    const ref = {
        // some libs rely on the presence effect for checking computed refs
        // from normal refs, but the implementation doesn't matter
        effect: watcher,
        get value() {
            if (watcher) {
                if (watcher.dirty) {
                    watcher.evaluate();
                }
                if (Dep.target) {
                    if (Dep.target.onTrack) {
                        Dep.target.onTrack({
                            effect: Dep.target,
                            target: ref,
                            type: "get" /* TrackOpTypes.GET */,
                            key: 'value'
                        });
                    }
                    watcher.depend();
                }
                return watcher.value;
            }
            else {
                return getter();
            }
        },
        set value(newVal) {
            setter(newVal);
        }
    };
    def(ref, RefFlag, true);
    def(ref, "__v_isReadonly" /* ReactiveFlags.IS_READONLY */, onlyGetter);
    return ref;
}

let mark;
let measure;
{
    const perf = inBrowser && window.performance;
    /* istanbul ignore if */
    if (perf &&
        // @ts-ignore
        perf.mark &&
        // @ts-ignore
        perf.measure &&
        // @ts-ignore
        perf.clearMarks &&
        // @ts-ignore
        perf.clearMeasures) {
        mark = tag => perf.mark(tag);
        measure = (name, startTag, endTag) => {
            perf.measure(name, startTag, endTag);
            perf.clearMarks(startTag);
            perf.clearMarks(endTag);
            // perf.clearMeasures(name)
        };
    }
}

const normalizeEvent = cached((name) => {
    const passive = name.charAt(0) === '&';
    name = passive ? name.slice(1) : name;
    const once = name.charAt(0) === '~'; // Prefixed last, checked first
    name = once ? name.slice(1) : name;
    const capture = name.charAt(0) === '!';
    name = capture ? name.slice(1) : name;
    return {
        name,
        once,
        capture,
        passive
    };
});
function createFnInvoker(fns, vm) {
    function invoker() {
        const fns = invoker.fns;
        if (isArray(fns)) {
            const cloned = fns.slice();
            for (let i = 0; i < cloned.length; i++) {
                invokeWithErrorHandling(cloned[i], null, arguments, vm, `v-on handler`);
            }
        }
        else {
            // return handler return value for single handlers
            return invokeWithErrorHandling(fns, null, arguments, vm, `v-on handler`);
        }
    }
    invoker.fns = fns;
    return invoker;
}
function updateListeners(on, oldOn, add, remove, createOnceHandler, vm) {
    let name, cur, old, event;
    for (name in on) {
        cur = on[name];
        old = oldOn[name];
        event = normalizeEvent(name);
        if (isUndef(cur)) {
            warn$2(`Invalid handler for event "${event.name}": got ` + String(cur), vm);
        }
        else if (isUndef(old)) {
            if (isUndef(cur.fns)) {
                cur = on[name] = createFnInvoker(cur, vm);
            }
            if (isTrue(event.once)) {
                cur = on[name] = createOnceHandler(event.name, cur, event.capture);
            }
            add(event.name, cur, event.capture, event.passive, event.params);
        }
        else if (cur !== old) {
            old.fns = cur;
            on[name] = old;
        }
    }
    for (name in oldOn) {
        if (isUndef(on[name])) {
            event = normalizeEvent(name);
            remove(event.name, oldOn[name], event.capture);
        }
    }
}

function mergeVNodeHook(def, hookKey, hook) {
    if (def instanceof VNode) {
        def = def.data.hook || (def.data.hook = {});
    }
    let invoker;
    const oldHook = def[hookKey];
    function wrappedHook() {
        hook.apply(this, arguments);
        // important: remove merged hook to ensure it's called only once
        // and prevent memory leak
        remove$2(invoker.fns, wrappedHook);
    }
    if (isUndef(oldHook)) {
        // no existing hook
        invoker = createFnInvoker([wrappedHook]);
    }
    else {
        /* istanbul ignore if */
        if (isDef(oldHook.fns) && isTrue(oldHook.merged)) {
            // already a merged invoker
            invoker = oldHook;
            invoker.fns.push(wrappedHook);
        }
        else {
            // existing plain hook
            invoker = createFnInvoker([oldHook, wrappedHook]);
        }
    }
    invoker.merged = true;
    def[hookKey] = invoker;
}

function extractPropsFromVNodeData(data, Ctor, tag) {
    // we are only extracting raw values here.
    // validation and default values are handled in the child
    // component itself.
    const propOptions = Ctor.options.props;
    if (isUndef(propOptions)) {
        return;
    }
    const res = {};
    const { attrs, props } = data;
    if (isDef(attrs) || isDef(props)) {
        for (const key in propOptions) {
            const altKey = hyphenate(key);
            {
                const keyInLowerCase = key.toLowerCase();
                if (key !== keyInLowerCase && attrs && hasOwn(attrs, keyInLowerCase)) {
                    tip(`Prop "${keyInLowerCase}" is passed to component ` +
                        `${formatComponentName(
                        // @ts-expect-error tag is string
                        tag || Ctor)}, but the declared prop name is` +
                        ` "${key}". ` +
                        `Note that HTML attributes are case-insensitive and camelCased ` +
                        `props need to use their kebab-case equivalents when using in-DOM ` +
                        `templates. You should probably use "${altKey}" instead of "${key}".`);
                }
            }
            checkProp(res, props, key, altKey, true) ||
                checkProp(res, attrs, key, altKey, false);
        }
    }
    return res;
}
function checkProp(res, hash, key, altKey, preserve) {
    if (isDef(hash)) {
        if (hasOwn(hash, key)) {
            res[key] = hash[key];
            if (!preserve) {
                delete hash[key];
            }
            return true;
        }
        else if (hasOwn(hash, altKey)) {
            res[key] = hash[altKey];
            if (!preserve) {
                delete hash[altKey];
            }
            return true;
        }
    }
    return false;
}

// The template compiler attempts to minimize the need for normalization by
// statically analyzing the template at compile time.
//
// For plain HTML markup, normalization can be completely skipped because the
// generated render function is guaranteed to return Array<VNode>. There are
// two cases where extra normalization is needed:
// 1. When the children contains components - because a functional component
// may return an Array instead of a single root. In this case, just a simple
// normalization is needed - if any child is an Array, we flatten the whole
// thing with Array.prototype.concat. It is guaranteed to be only 1-level deep
// because functional components already normalize their own children.
function simpleNormalizeChildren(children) {
    for (let i = 0; i < children.length; i++) {
        if (isArray(children[i])) {
            return Array.prototype.concat.apply([], children);
        }
    }
    return children;
}
// 2. When the children contains constructs that always generated nested Arrays,
// e.g. <template>, <slot>, v-for, or when the children is provided by user
// with hand-written render functions / JSX. In such cases a full normalization
// is needed to cater to all possible types of children values.
function normalizeChildren(children) {
    return isPrimitive(children)
        ? [createTextVNode(children)]
        : isArray(children)
            ? normalizeArrayChildren(children)
            : undefined;
}
function isTextNode(node) {
    return isDef(node) && isDef(node.text) && isFalse(node.isComment);
}
function normalizeArrayChildren(children, nestedIndex) {
    const res = [];
    let i, c, lastIndex, last;
    for (i = 0; i < children.length; i++) {
        c = children[i];
        if (isUndef(c) || typeof c === 'boolean')
            continue;
        lastIndex = res.length - 1;
        last = res[lastIndex];
        //  nested
        if (isArray(c)) {
            if (c.length > 0) {
                c = normalizeArrayChildren(c, `${nestedIndex || ''}_${i}`);
                // merge adjacent text nodes
                if (isTextNode(c[0]) && isTextNode(last)) {
                    res[lastIndex] = createTextVNode(last.text + c[0].text);
                    c.shift();
                }
                res.push.apply(res, c);
            }
        }
        else if (isPrimitive(c)) {
            if (isTextNode(last)) {
                // merge adjacent text nodes
                // this is necessary for SSR hydration because text nodes are
                // essentially merged when rendered to HTML strings
                res[lastIndex] = createTextVNode(last.text + c);
            }
            else if (c !== '') {
                // convert primitive to vnode
                res.push(createTextVNode(c));
            }
        }
        else {
            if (isTextNode(c) && isTextNode(last)) {
                // merge adjacent text nodes
                res[lastIndex] = createTextVNode(last.text + c.text);
            }
            else {
                // default key for nested array children (likely generated by v-for)
                if (isTrue(children._isVList) &&
                    isDef(c.tag) &&
                    isUndef(c.key) &&
                    isDef(nestedIndex)) {
                    c.key = `__vlist${nestedIndex}_${i}__`;
                }
                res.push(c);
            }
        }
    }
    return res;
}

const SIMPLE_NORMALIZE = 1;
const ALWAYS_NORMALIZE = 2;
// wrapper function for providing a more flexible interface
// without getting yelled at by flow
function createElement$1(context, tag, data, children, normalizationType, alwaysNormalize) {
    if (isArray(data) || isPrimitive(data)) {
        normalizationType = children;
        children = data;
        data = undefined;
    }
    if (isTrue(alwaysNormalize)) {
        normalizationType = ALWAYS_NORMALIZE;
    }
    return _createElement(context, tag, data, children, normalizationType);
}
function _createElement(context, tag, data, children, normalizationType) {
    if (isDef(data) && isDef(data.__ob__)) {
        warn$2(`Avoid using observed data object as vnode data: ${JSON.stringify(data)}\n` + 'Always create fresh vnode data objects in each render!', context);
        return createEmptyVNode();
    }
    // object syntax in v-bind
    if (isDef(data) && isDef(data.is)) {
        tag = data.is;
    }
    if (!tag) {
        // in case of component :is set to falsy value
        return createEmptyVNode();
    }
    // warn against non-primitive key
    if (isDef(data) && isDef(data.key) && !isPrimitive(data.key)) {
        warn$2('Avoid using non-primitive value as key, ' +
            'use string/number value instead.', context);
    }
    // support single function children as default scoped slot
    if (isArray(children) && isFunction(children[0])) {
        data = data || {};
        data.scopedSlots = { default: children[0] };
        children.length = 0;
    }
    if (normalizationType === ALWAYS_NORMALIZE) {
        children = normalizeChildren(children);
    }
    else if (normalizationType === SIMPLE_NORMALIZE) {
        children = simpleNormalizeChildren(children);
    }
    let vnode, ns;
    if (typeof tag === 'string') {
        let Ctor;
        ns = (context.$vnode && context.$vnode.ns) || config.getTagNamespace(tag);
        if (config.isReservedTag(tag)) {
            // platform built-in elements
            if (isDef(data) &&
                isDef(data.nativeOn) &&
                data.tag !== 'component') {
                warn$2(`The .native modifier for v-on is only valid on components but it was used on <${tag}>.`, context);
            }
            vnode = new VNode(config.parsePlatformTagName(tag), data, children, undefined, undefined, context);
        }
        else if ((!data || !data.pre) &&
            isDef((Ctor = resolveAsset(context.$options, 'components', tag)))) {
            // component
            vnode = createComponent(Ctor, data, context, children, tag);
        }
        else {
            // unknown or unlisted namespaced elements
            // check at runtime because it may get assigned a namespace when its
            // parent normalizes children
            vnode = new VNode(tag, data, children, undefined, undefined, context);
        }
    }
    else {
        // direct component options / constructor
        vnode = createComponent(tag, data, context, children);
    }
    if (isArray(vnode)) {
        return vnode;
    }
    else if (isDef(vnode)) {
        if (isDef(ns))
            applyNS(vnode, ns);
        if (isDef(data))
            registerDeepBindings(data);
        return vnode;
    }
    else {
        return createEmptyVNode();
    }
}
function applyNS(vnode, ns, force) {
    vnode.ns = ns;
    if (vnode.tag === 'foreignObject') {
        // use default namespace inside foreignObject
        ns = undefined;
        force = true;
    }
    if (isDef(vnode.children)) {
        for (let i = 0, l = vnode.children.length; i < l; i++) {
            const child = vnode.children[i];
            if (isDef(child.tag) &&
                (isUndef(child.ns) || (isTrue(force) && child.tag !== 'svg'))) {
                applyNS(child, ns, force);
            }
        }
    }
}
// ref #5318
// necessary to ensure parent re-render when deep bindings like :style and
// :class are used on slot nodes
function registerDeepBindings(data) {
    if (isObject(data.style)) {
        traverse(data.style);
    }
    if (isObject(data.class)) {
        traverse(data.class);
    }
}

/**
 * Runtime helper for rendering v-for lists.
 */
function renderList(val, render) {
    let ret = null, i, l, keys, key;
    if (isArray(val) || typeof val === 'string') {
        ret = new Array(val.length);
        for (i = 0, l = val.length; i < l; i++) {
            ret[i] = render(val[i], i);
        }
    }
    else if (typeof val === 'number') {
        ret = new Array(val);
        for (i = 0; i < val; i++) {
            ret[i] = render(i + 1, i);
        }
    }
    else if (isObject(val)) {
        if (hasSymbol && val[Symbol.iterator]) {
            ret = [];
            const iterator = val[Symbol.iterator]();
            let result = iterator.next();
            while (!result.done) {
                ret.push(render(result.value, ret.length));
                result = iterator.next();
            }
        }
        else {
            keys = Object.keys(val);
            ret = new Array(keys.length);
            for (i = 0, l = keys.length; i < l; i++) {
                key = keys[i];
                ret[i] = render(val[key], key, i);
            }
        }
    }
    if (!isDef(ret)) {
        ret = [];
    }
    ret._isVList = true;
    return ret;
}

/**
 * Runtime helper for rendering <slot>
 */
function renderSlot(name, fallbackRender, props, bindObject) {
    const scopedSlotFn = this.$scopedSlots[name];
    let nodes;
    if (scopedSlotFn) {
        // scoped slot
        props = props || {};
        if (bindObject) {
            if (!isObject(bindObject)) {
                warn$2('slot v-bind without argument expects an Object', this);
            }
            props = extend(extend({}, bindObject), props);
        }
        nodes =
            scopedSlotFn(props) ||
                (isFunction(fallbackRender) ? fallbackRender() : fallbackRender);
    }
    else {
        nodes =
            this.$slots[name] ||
                (isFunction(fallbackRender) ? fallbackRender() : fallbackRender);
    }
    const target = props && props.slot;
    if (target) {
        return this.$createElement('template', { slot: target }, nodes);
    }
    else {
        return nodes;
    }
}

/**
 * Runtime helper for resolving filters
 */
function resolveFilter(id) {
    return resolveAsset(this.$options, 'filters', id, true) || identity;
}

function isKeyNotMatch(expect, actual) {
    if (isArray(expect)) {
        return expect.indexOf(actual) === -1;
    }
    else {
        return expect !== actual;
    }
}
/**
 * Runtime helper for checking keyCodes from config.
 * exposed as Vue.prototype._k
 * passing in eventKeyName as last argument separately for backwards compat
 */
function checkKeyCodes(eventKeyCode, key, builtInKeyCode, eventKeyName, builtInKeyName) {
    const mappedKeyCode = config.keyCodes[key] || builtInKeyCode;
    if (builtInKeyName && eventKeyName && !config.keyCodes[key]) {
        return isKeyNotMatch(builtInKeyName, eventKeyName);
    }
    else if (mappedKeyCode) {
        return isKeyNotMatch(mappedKeyCode, eventKeyCode);
    }
    else if (eventKeyName) {
        return hyphenate(eventKeyName) !== key;
    }
    return eventKeyCode === undefined;
}

/**
 * Runtime helper for merging v-bind="object" into a VNode's data.
 */
function bindObjectProps(data, tag, value, asProp, isSync) {
    if (value) {
        if (!isObject(value)) {
            warn$2('v-bind without argument expects an Object or Array value', this);
        }
        else {
            if (isArray(value)) {
                value = toObject(value);
            }
            let hash;
            for (const key in value) {
                if (key === 'class' || key === 'style' || isReservedAttribute(key)) {
                    hash = data;
                }
                else {
                    const type = data.attrs && data.attrs.type;
                    hash =
                        asProp || config.mustUseProp(tag, type, key)
                            ? data.domProps || (data.domProps = {})
                            : data.attrs || (data.attrs = {});
                }
                const camelizedKey = camelize(key);
                const hyphenatedKey = hyphenate(key);
                if (!(camelizedKey in hash) && !(hyphenatedKey in hash)) {
                    hash[key] = value[key];
                    if (isSync) {
                        const on = data.on || (data.on = {});
                        on[`update:${key}`] = function ($event) {
                            value[key] = $event;
                        };
                    }
                }
            }
        }
    }
    return data;
}

/**
 * Runtime helper for rendering static trees.
 */
function renderStatic(index, isInFor) {
    const cached = this._staticTrees || (this._staticTrees = []);
    let tree = cached[index];
    // if has already-rendered static tree and not inside v-for,
    // we can reuse the same tree.
    if (tree && !isInFor) {
        return tree;
    }
    // otherwise, render a fresh tree.
    tree = cached[index] = this.$options.staticRenderFns[index].call(this._renderProxy, this._c, this // for render fns generated for functional component templates
    );
    markStatic$1(tree, `__static__${index}`, false);
    return tree;
}
/**
 * Runtime helper for v-once.
 * Effectively it means marking the node as static with a unique key.
 */
function markOnce(tree, index, key) {
    markStatic$1(tree, `__once__${index}${key ? `_${key}` : ``}`, true);
    return tree;
}
function markStatic$1(tree, key, isOnce) {
    if (isArray(tree)) {
        for (let i = 0; i < tree.length; i++) {
            if (tree[i] && typeof tree[i] !== 'string') {
                markStaticNode(tree[i], `${key}_${i}`, isOnce);
            }
        }
    }
    else {
        markStaticNode(tree, key, isOnce);
    }
}
function markStaticNode(node, key, isOnce) {
    node.isStatic = true;
    node.key = key;
    node.isOnce = isOnce;
}

function bindObjectListeners(data, value) {
    if (value) {
        if (!isPlainObject(value)) {
            warn$2('v-on without argument expects an Object value', this);
        }
        else {
            const on = (data.on = data.on ? extend({}, data.on) : {});
            for (const key in value) {
                const existing = on[key];
                const ours = value[key];
                on[key] = existing ? [].concat(existing, ours) : ours;
            }
        }
    }
    return data;
}

function resolveScopedSlots(fns, res, 
// the following are added in 2.6
hasDynamicKeys, contentHashKey) {
    res = res || { $stable: !hasDynamicKeys };
    for (let i = 0; i < fns.length; i++) {
        const slot = fns[i];
        if (isArray(slot)) {
            resolveScopedSlots(slot, res, hasDynamicKeys);
        }
        else if (slot) {
            // marker for reverse proxying v-slot without scope on this.$slots
            // @ts-expect-error
            if (slot.proxy) {
                // @ts-expect-error
                slot.fn.proxy = true;
            }
            res[slot.key] = slot.fn;
        }
    }
    if (contentHashKey) {
        res.$key = contentHashKey;
    }
    return res;
}

// helper to process dynamic keys for dynamic arguments in v-bind and v-on.
function bindDynamicKeys(baseObj, values) {
    for (let i = 0; i < values.length; i += 2) {
        const key = values[i];
        if (typeof key === 'string' && key) {
            baseObj[values[i]] = values[i + 1];
        }
        else if (key !== '' && key !== null) {
            // null is a special value for explicitly removing a binding
            warn$2(`Invalid value for dynamic directive argument (expected string or null): ${key}`, this);
        }
    }
    return baseObj;
}
// helper to dynamically append modifier runtime markers to event names.
// ensure only append when value is already string, otherwise it will be cast
// to string and cause the type check to miss.
function prependModifier(value, symbol) {
    return typeof value === 'string' ? symbol + value : value;
}

function installRenderHelpers(target) {
    target._o = markOnce;
    target._n = toNumber;
    target._s = toString;
    target._l = renderList;
    target._t = renderSlot;
    target._q = looseEqual;
    target._i = looseIndexOf;
    target._m = renderStatic;
    target._f = resolveFilter;
    target._k = checkKeyCodes;
    target._b = bindObjectProps;
    target._v = createTextVNode;
    target._e = createEmptyVNode;
    target._u = resolveScopedSlots;
    target._g = bindObjectListeners;
    target._d = bindDynamicKeys;
    target._p = prependModifier;
}

/**
 * Runtime helper for resolving raw children VNodes into a slot object.
 */
function resolveSlots(children, context) {
    if (!children || !children.length) {
        return {};
    }
    const slots = {};
    for (let i = 0, l = children.length; i < l; i++) {
        const child = children[i];
        const data = child.data;
        // remove slot attribute if the node is resolved as a Vue slot node
        if (data && data.attrs && data.attrs.slot) {
            delete data.attrs.slot;
        }
        // named slots should only be respected if the vnode was rendered in the
        // same context.
        if ((child.context === context || child.fnContext === context) &&
            data &&
            data.slot != null) {
            const name = data.slot;
            const slot = slots[name] || (slots[name] = []);
            if (child.tag === 'template') {
                slot.push.apply(slot, child.children || []);
            }
            else {
                slot.push(child);
            }
        }
        else {
            (slots.default || (slots.default = [])).push(child);
        }
    }
    // ignore slots that contains only whitespace
    for (const name in slots) {
        if (slots[name].every(isWhitespace)) {
            delete slots[name];
        }
    }
    return slots;
}
function isWhitespace(node) {
    return (node.isComment && !node.asyncFactory) || node.text === ' ';
}

function isAsyncPlaceholder(node) {
    // @ts-expect-error not really boolean type
    return node.isComment && node.asyncFactory;
}

function normalizeScopedSlots(ownerVm, scopedSlots, normalSlots, prevScopedSlots) {
    let res;
    const hasNormalSlots = Object.keys(normalSlots).length > 0;
    const isStable = scopedSlots ? !!scopedSlots.$stable : !hasNormalSlots;
    const key = scopedSlots && scopedSlots.$key;
    if (!scopedSlots) {
        res = {};
    }
    else if (scopedSlots._normalized) {
        // fast path 1: child component re-render only, parent did not change
        return scopedSlots._normalized;
    }
    else if (isStable &&
        prevScopedSlots &&
        prevScopedSlots !== emptyObject &&
        key === prevScopedSlots.$key &&
        !hasNormalSlots &&
        !prevScopedSlots.$hasNormal) {
        // fast path 2: stable scoped slots w/ no normal slots to proxy,
        // only need to normalize once
        return prevScopedSlots;
    }
    else {
        res = {};
        for (const key in scopedSlots) {
            if (scopedSlots[key] && key[0] !== '$') {
                res[key] = normalizeScopedSlot(ownerVm, normalSlots, key, scopedSlots[key]);
            }
        }
    }
    // expose normal slots on scopedSlots
    for (const key in normalSlots) {
        if (!(key in res)) {
            res[key] = proxyNormalSlot(normalSlots, key);
        }
    }
    // avoriaz seems to mock a non-extensible $scopedSlots object
    // and when that is passed down this would cause an error
    if (scopedSlots && Object.isExtensible(scopedSlots)) {
        scopedSlots._normalized = res;
    }
    def(res, '$stable', isStable);
    def(res, '$key', key);
    def(res, '$hasNormal', hasNormalSlots);
    return res;
}
function normalizeScopedSlot(vm, normalSlots, key, fn) {
    const normalized = function () {
        const cur = currentInstance;
        setCurrentInstance(vm);
        let res = arguments.length ? fn.apply(null, arguments) : fn({});
        res =
            res && typeof res === 'object' && !isArray(res)
                ? [res] // single vnode
                : normalizeChildren(res);
        const vnode = res && res[0];
        setCurrentInstance(cur);
        return res &&
            (!vnode ||
                (res.length === 1 && vnode.isComment && !isAsyncPlaceholder(vnode))) // #9658, #10391
            ? undefined
            : res;
    };
    // this is a slot using the new v-slot syntax without scope. although it is
    // compiled as a scoped slot, render fn users would expect it to be present
    // on this.$slots because the usage is semantically a normal slot.
    if (fn.proxy) {
        Object.defineProperty(normalSlots, key, {
            get: normalized,
            enumerable: true,
            configurable: true
        });
    }
    return normalized;
}
function proxyNormalSlot(slots, key) {
    return () => slots[key];
}

function initSetup(vm) {
    const options = vm.$options;
    const setup = options.setup;
    if (setup) {
        const ctx = (vm._setupContext = createSetupContext(vm));
        setCurrentInstance(vm);
        pushTarget();
        const setupResult = invokeWithErrorHandling(setup, null, [vm._props || shallowReactive({}), ctx], vm, `setup`);
        popTarget();
        setCurrentInstance();
        if (isFunction(setupResult)) {
            // render function
            // @ts-ignore
            options.render = setupResult;
        }
        else if (isObject(setupResult)) {
            // bindings
            if (setupResult instanceof VNode) {
                warn$2(`setup() should not return VNodes directly - ` +
                    `return a render function instead.`);
            }
            vm._setupState = setupResult;
            // __sfc indicates compiled bindings from <script setup>
            if (!setupResult.__sfc) {
                for (const key in setupResult) {
                    if (!isReserved(key)) {
                        proxyWithRefUnwrap(vm, setupResult, key);
                    }
                    else {
                        warn$2(`Avoid using variables that start with _ or $ in setup().`);
                    }
                }
            }
            else {
                // exposed for compiled render fn
                const proxy = (vm._setupProxy = {});
                for (const key in setupResult) {
                    if (key !== '__sfc') {
                        proxyWithRefUnwrap(proxy, setupResult, key);
                    }
                }
            }
        }
        else if (setupResult !== undefined) {
            warn$2(`setup() should return an object. Received: ${setupResult === null ? 'null' : typeof setupResult}`);
        }
    }
}
function createSetupContext(vm) {
    let exposeCalled = false;
    return {
        get attrs() {
            if (!vm._attrsProxy) {
                const proxy = (vm._attrsProxy = {});
                def(proxy, '_v_attr_proxy', true);
                syncSetupProxy(proxy, vm.$attrs, emptyObject, vm, '$attrs');
            }
            return vm._attrsProxy;
        },
        get listeners() {
            if (!vm._listenersProxy) {
                const proxy = (vm._listenersProxy = {});
                syncSetupProxy(proxy, vm.$listeners, emptyObject, vm, '$listeners');
            }
            return vm._listenersProxy;
        },
        get slots() {
            return initSlotsProxy(vm);
        },
        emit: bind$1(vm.$emit, vm),
        expose(exposed) {
            {
                if (exposeCalled) {
                    warn$2(`expose() should be called only once per setup().`, vm);
                }
                exposeCalled = true;
            }
            if (exposed) {
                Object.keys(exposed).forEach(key => proxyWithRefUnwrap(vm, exposed, key));
            }
        }
    };
}
function syncSetupProxy(to, from, prev, instance, type) {
    let changed = false;
    for (const key in from) {
        if (!(key in to)) {
            changed = true;
            defineProxyAttr(to, key, instance, type);
        }
        else if (from[key] !== prev[key]) {
            changed = true;
        }
    }
    for (const key in to) {
        if (!(key in from)) {
            changed = true;
            delete to[key];
        }
    }
    return changed;
}
function defineProxyAttr(proxy, key, instance, type) {
    Object.defineProperty(proxy, key, {
        enumerable: true,
        configurable: true,
        get() {
            return instance[type][key];
        }
    });
}
function initSlotsProxy(vm) {
    if (!vm._slotsProxy) {
        syncSetupSlots((vm._slotsProxy = {}), vm.$scopedSlots);
    }
    return vm._slotsProxy;
}
function syncSetupSlots(to, from) {
    for (const key in from) {
        to[key] = from[key];
    }
    for (const key in to) {
        if (!(key in from)) {
            delete to[key];
        }
    }
}
/**
 * @internal use manual type def because public setup context type relies on
 * legacy VNode types
 */
function useSlots() {
    return getContext().slots;
}
/**
 * @internal use manual type def because public setup context type relies on
 * legacy VNode types
 */
function useAttrs() {
    return getContext().attrs;
}
/**
 * Vue 2 only
 * @internal use manual type def because public setup context type relies on
 * legacy VNode types
 */
function useListeners() {
    return getContext().listeners;
}
function getContext() {
    if (!currentInstance) {
        warn$2(`useContext() called without active instance.`);
    }
    const vm = currentInstance;
    return vm._setupContext || (vm._setupContext = createSetupContext(vm));
}
/**
 * Runtime helper for merging default declarations. Imported by compiled code
 * only.
 * @internal
 */
function mergeDefaults(raw, defaults) {
    const props = isArray(raw)
        ? raw.reduce((normalized, p) => ((normalized[p] = {}), normalized), {})
        : raw;
    for (const key in defaults) {
        const opt = props[key];
        if (opt) {
            if (isArray(opt) || isFunction(opt)) {
                props[key] = { type: opt, default: defaults[key] };
            }
            else {
                opt.default = defaults[key];
            }
        }
        else if (opt === null) {
            props[key] = { default: defaults[key] };
        }
        else {
            warn$2(`props default key "${key}" has no corresponding declaration.`);
        }
    }
    return props;
}

function initRender(vm) {
    vm._vnode = null; // the root of the child tree
    vm._staticTrees = null; // v-once cached trees
    const options = vm.$options;
    const parentVnode = (vm.$vnode = options._parentVnode); // the placeholder node in parent tree
    const renderContext = parentVnode && parentVnode.context;
    vm.$slots = resolveSlots(options._renderChildren, renderContext);
    vm.$scopedSlots = parentVnode
        ? normalizeScopedSlots(vm.$parent, parentVnode.data.scopedSlots, vm.$slots)
        : emptyObject;
    // bind the createElement fn to this instance
    // so that we get proper render context inside it.
    // args order: tag, data, children, normalizationType, alwaysNormalize
    // internal version is used by render functions compiled from templates
    // @ts-expect-error
    vm._c = (a, b, c, d) => createElement$1(vm, a, b, c, d, false);
    // normalization is always applied for the public version, used in
    // user-written render functions.
    // @ts-expect-error
    vm.$createElement = (a, b, c, d) => createElement$1(vm, a, b, c, d, true);
    // $attrs & $listeners are exposed for easier HOC creation.
    // they need to be reactive so that HOCs using them are always updated
    const parentData = parentVnode && parentVnode.data;
    /* istanbul ignore else */
    {
        defineReactive(vm, '$attrs', (parentData && parentData.attrs) || emptyObject, () => {
            !isUpdatingChildComponent && warn$2(`$attrs is readonly.`, vm);
        }, true);
        defineReactive(vm, '$listeners', options._parentListeners || emptyObject, () => {
            !isUpdatingChildComponent && warn$2(`$listeners is readonly.`, vm);
        }, true);
    }
}
let currentRenderingInstance = null;
function renderMixin(Vue) {
    // install runtime convenience helpers
    installRenderHelpers(Vue.prototype);
    Vue.prototype.$nextTick = function (fn) {
        return nextTick(fn, this);
    };
    Vue.prototype._render = function () {
        const vm = this;
        const { render, _parentVnode } = vm.$options;
        if (_parentVnode && vm._isMounted) {
            vm.$scopedSlots = normalizeScopedSlots(vm.$parent, _parentVnode.data.scopedSlots, vm.$slots, vm.$scopedSlots);
            if (vm._slotsProxy) {
                syncSetupSlots(vm._slotsProxy, vm.$scopedSlots);
            }
        }
        // set parent vnode. this allows render functions to have access
        // to the data on the placeholder node.
        vm.$vnode = _parentVnode;
        // render self
        let vnode;
        try {
            // There's no need to maintain a stack because all render fns are called
            // separately from one another. Nested component's render fns are called
            // when parent component is patched.
            setCurrentInstance(vm);
            currentRenderingInstance = vm;
            vnode = render.call(vm._renderProxy, vm.$createElement);
        }
        catch (e) {
            handleError(e, vm, `render`);
            // return error render result,
            // or previous vnode to prevent render error causing blank component
            /* istanbul ignore else */
            if (vm.$options.renderError) {
                try {
                    vnode = vm.$options.renderError.call(vm._renderProxy, vm.$createElement, e);
                }
                catch (e) {
                    handleError(e, vm, `renderError`);
                    vnode = vm._vnode;
                }
            }
            else {
                vnode = vm._vnode;
            }
        }
        finally {
            currentRenderingInstance = null;
            setCurrentInstance();
        }
        // if the returned array contains only a single node, allow it
        if (isArray(vnode) && vnode.length === 1) {
            vnode = vnode[0];
        }
        // return empty vnode in case the render function errored out
        if (!(vnode instanceof VNode)) {
            if (isArray(vnode)) {
                warn$2('Multiple root nodes returned from render function. Render function ' +
                    'should return a single root node.', vm);
            }
            vnode = createEmptyVNode();
        }
        // set parent
        vnode.parent = _parentVnode;
        return vnode;
    };
}

function ensureCtor(comp, base) {
    if (comp.__esModule || (hasSymbol && comp[Symbol.toStringTag] === 'Module')) {
        comp = comp.default;
    }
    return isObject(comp) ? base.extend(comp) : comp;
}
function createAsyncPlaceholder(factory, data, context, children, tag) {
    const node = createEmptyVNode();
    node.asyncFactory = factory;
    node.asyncMeta = { data, context, children, tag };
    return node;
}
function resolveAsyncComponent(factory, baseCtor) {
    if (isTrue(factory.error) && isDef(factory.errorComp)) {
        return factory.errorComp;
    }
    if (isDef(factory.resolved)) {
        return factory.resolved;
    }
    const owner = currentRenderingInstance;
    if (owner && isDef(factory.owners) && factory.owners.indexOf(owner) === -1) {
        // already pending
        factory.owners.push(owner);
    }
    if (isTrue(factory.loading) && isDef(factory.loadingComp)) {
        return factory.loadingComp;
    }
    if (owner && !isDef(factory.owners)) {
        const owners = (factory.owners = [owner]);
        let sync = true;
        let timerLoading = null;
        let timerTimeout = null;
        owner.$on('hook:destroyed', () => remove$2(owners, owner));
        const forceRender = (renderCompleted) => {
            for (let i = 0, l = owners.length; i < l; i++) {
                owners[i].$forceUpdate();
            }
            if (renderCompleted) {
                owners.length = 0;
                if (timerLoading !== null) {
                    clearTimeout(timerLoading);
                    timerLoading = null;
                }
                if (timerTimeout !== null) {
                    clearTimeout(timerTimeout);
                    timerTimeout = null;
                }
            }
        };
        const resolve = once((res) => {
            // cache resolved
            factory.resolved = ensureCtor(res, baseCtor);
            // invoke callbacks only if this is not a synchronous resolve
            // (async resolves are shimmed as synchronous during SSR)
            if (!sync) {
                forceRender(true);
            }
            else {
                owners.length = 0;
            }
        });
        const reject = once(reason => {
            warn$2(`Failed to resolve async component: ${String(factory)}` +
                    (reason ? `\nReason: ${reason}` : ''));
            if (isDef(factory.errorComp)) {
                factory.error = true;
                forceRender(true);
            }
        });
        const res = factory(resolve, reject);
        if (isObject(res)) {
            if (isPromise(res)) {
                // () => Promise
                if (isUndef(factory.resolved)) {
                    res.then(resolve, reject);
                }
            }
            else if (isPromise(res.component)) {
                res.component.then(resolve, reject);
                if (isDef(res.error)) {
                    factory.errorComp = ensureCtor(res.error, baseCtor);
                }
                if (isDef(res.loading)) {
                    factory.loadingComp = ensureCtor(res.loading, baseCtor);
                    if (res.delay === 0) {
                        factory.loading = true;
                    }
                    else {
                        // @ts-expect-error NodeJS timeout type
                        timerLoading = setTimeout(() => {
                            timerLoading = null;
                            if (isUndef(factory.resolved) && isUndef(factory.error)) {
                                factory.loading = true;
                                forceRender(false);
                            }
                        }, res.delay || 200);
                    }
                }
                if (isDef(res.timeout)) {
                    // @ts-expect-error NodeJS timeout type
                    timerTimeout = setTimeout(() => {
                        timerTimeout = null;
                        if (isUndef(factory.resolved)) {
                            reject(`timeout (${res.timeout}ms)` );
                        }
                    }, res.timeout);
                }
            }
        }
        sync = false;
        // return in case resolved synchronously
        return factory.loading ? factory.loadingComp : factory.resolved;
    }
}

function getFirstComponentChild(children) {
    if (isArray(children)) {
        for (let i = 0; i < children.length; i++) {
            const c = children[i];
            if (isDef(c) && (isDef(c.componentOptions) || isAsyncPlaceholder(c))) {
                return c;
            }
        }
    }
}

function initEvents(vm) {
    vm._events = Object.create(null);
    vm._hasHookEvent = false;
    // init parent attached events
    const listeners = vm.$options._parentListeners;
    if (listeners) {
        updateComponentListeners(vm, listeners);
    }
}
let target$1;
function add$1(event, fn) {
    target$1.$on(event, fn);
}
function remove$1(event, fn) {
    target$1.$off(event, fn);
}
function createOnceHandler$1(event, fn) {
    const _target = target$1;
    return function onceHandler() {
        const res = fn.apply(null, arguments);
        if (res !== null) {
            _target.$off(event, onceHandler);
        }
    };
}
function updateComponentListeners(vm, listeners, oldListeners) {
    target$1 = vm;
    updateListeners(listeners, oldListeners || {}, add$1, remove$1, createOnceHandler$1, vm);
    target$1 = undefined;
}
function eventsMixin(Vue) {
    const hookRE = /^hook:/;
    Vue.prototype.$on = function (event, fn) {
        const vm = this;
        if (isArray(event)) {
            for (let i = 0, l = event.length; i < l; i++) {
                vm.$on(event[i], fn);
            }
        }
        else {
            (vm._events[event] || (vm._events[event] = [])).push(fn);
            // optimize hook:event cost by using a boolean flag marked at registration
            // instead of a hash lookup
            if (hookRE.test(event)) {
                vm._hasHookEvent = true;
            }
        }
        return vm;
    };
    Vue.prototype.$once = function (event, fn) {
        const vm = this;
        function on() {
            vm.$off(event, on);
            fn.apply(vm, arguments);
        }
        on.fn = fn;
        vm.$on(event, on);
        return vm;
    };
    Vue.prototype.$off = function (event, fn) {
        const vm = this;
        // all
        if (!arguments.length) {
            vm._events = Object.create(null);
            return vm;
        }
        // array of events
        if (isArray(event)) {
            for (let i = 0, l = event.length; i < l; i++) {
                vm.$off(event[i], fn);
            }
            return vm;
        }
        // specific event
        const cbs = vm._events[event];
        if (!cbs) {
            return vm;
        }
        if (!fn) {
            vm._events[event] = null;
            return vm;
        }
        // specific handler
        let cb;
        let i = cbs.length;
        while (i--) {
            cb = cbs[i];
            if (cb === fn || cb.fn === fn) {
                cbs.splice(i, 1);
                break;
            }
        }
        return vm;
    };
    Vue.prototype.$emit = function (event) {
        const vm = this;
        {
            const lowerCaseEvent = event.toLowerCase();
            if (lowerCaseEvent !== event && vm._events[lowerCaseEvent]) {
                tip(`Event "${lowerCaseEvent}" is emitted in component ` +
                    `${formatComponentName(vm)} but the handler is registered for "${event}". ` +
                    `Note that HTML attributes are case-insensitive and you cannot use ` +
                    `v-on to listen to camelCase events when using in-DOM templates. ` +
                    `You should probably use "${hyphenate(event)}" instead of "${event}".`);
            }
        }
        let cbs = vm._events[event];
        if (cbs) {
            cbs = cbs.length > 1 ? toArray(cbs) : cbs;
            const args = toArray(arguments, 1);
            const info = `event handler for "${event}"`;
            for (let i = 0, l = cbs.length; i < l; i++) {
                invokeWithErrorHandling(cbs[i], vm, args, vm, info);
            }
        }
        return vm;
    };
}

let activeInstance = null;
let isUpdatingChildComponent = false;
function setActiveInstance(vm) {
    const prevActiveInstance = activeInstance;
    activeInstance = vm;
    return () => {
        activeInstance = prevActiveInstance;
    };
}
function initLifecycle(vm) {
    const options = vm.$options;
    // locate first non-abstract parent
    let parent = options.parent;
    if (parent && !options.abstract) {
        while (parent.$options.abstract && parent.$parent) {
            parent = parent.$parent;
        }
        parent.$children.push(vm);
    }
    vm.$parent = parent;
    vm.$root = parent ? parent.$root : vm;
    vm.$children = [];
    vm.$refs = {};
    vm._provided = parent ? parent._provided : Object.create(null);
    vm._watcher = null;
    vm._inactive = null;
    vm._directInactive = false;
    vm._isMounted = false;
    vm._isDestroyed = false;
    vm._isBeingDestroyed = false;
}
function lifecycleMixin(Vue) {
    Vue.prototype._update = function (vnode, hydrating) {
        const vm = this;
        const prevEl = vm.$el;
        const prevVnode = vm._vnode;
        const restoreActiveInstance = setActiveInstance(vm);
        vm._vnode = vnode;
        // Vue.prototype.__patch__ is injected in entry points
        // based on the rendering backend used.
        if (!prevVnode) {
            // initial render
            vm.$el = vm.__patch__(vm.$el, vnode, hydrating, false /* removeOnly */);
        }
        else {
            // updates
            vm.$el = vm.__patch__(prevVnode, vnode);
        }
        restoreActiveInstance();
        // update __vue__ reference
        if (prevEl) {
            prevEl.__vue__ = null;
        }
        if (vm.$el) {
            vm.$el.__vue__ = vm;
        }
        // if parent is an HOC, update its $el as well
        let wrapper = vm;
        while (wrapper &&
            wrapper.$vnode &&
            wrapper.$parent &&
            wrapper.$vnode === wrapper.$parent._vnode) {
            wrapper.$parent.$el = wrapper.$el;
            wrapper = wrapper.$parent;
        }
        // updated hook is called by the scheduler to ensure that children are
        // updated in a parent's updated hook.
    };
    Vue.prototype.$forceUpdate = function () {
        const vm = this;
        if (vm._watcher) {
            vm._watcher.update();
        }
    };
    Vue.prototype.$destroy = function () {
        const vm = this;
        if (vm._isBeingDestroyed) {
            return;
        }
        callHook$1(vm, 'beforeDestroy');
        vm._isBeingDestroyed = true;
        // remove self from parent
        const parent = vm.$parent;
        if (parent && !parent._isBeingDestroyed && !vm.$options.abstract) {
            remove$2(parent.$children, vm);
        }
        // teardown scope. this includes both the render watcher and other
        // watchers created
        vm._scope.stop();
        // remove reference from data ob
        // frozen object may not have observer.
        if (vm._data.__ob__) {
            vm._data.__ob__.vmCount--;
        }
        // call the last hook...
        vm._isDestroyed = true;
        // invoke destroy hooks on current rendered tree
        vm.__patch__(vm._vnode, null);
        // fire destroyed hook
        callHook$1(vm, 'destroyed');
        // turn off all instance listeners.
        vm.$off();
        // remove __vue__ reference
        if (vm.$el) {
            vm.$el.__vue__ = null;
        }
        // release circular reference (#6759)
        if (vm.$vnode) {
            vm.$vnode.parent = null;
        }
    };
}
function mountComponent(vm, el, hydrating) {
    vm.$el = el;
    if (!vm.$options.render) {
        // @ts-expect-error invalid type
        vm.$options.render = createEmptyVNode;
        {
            /* istanbul ignore if */
            if ((vm.$options.template && vm.$options.template.charAt(0) !== '#') ||
                vm.$options.el ||
                el) {
                warn$2('You are using the runtime-only build of Vue where the template ' +
                    'compiler is not available. Either pre-compile the templates into ' +
                    'render functions, or use the compiler-included build.', vm);
            }
            else {
                warn$2('Failed to mount component: template or render function not defined.', vm);
            }
        }
    }
    callHook$1(vm, 'beforeMount');
    let updateComponent;
    /* istanbul ignore if */
    if (config.performance && mark) {
        updateComponent = () => {
            const name = vm._name;
            const id = vm._uid;
            const startTag = `vue-perf-start:${id}`;
            const endTag = `vue-perf-end:${id}`;
            mark(startTag);
            const vnode = vm._render();
            mark(endTag);
            measure(`vue ${name} render`, startTag, endTag);
            mark(startTag);
            vm._update(vnode, hydrating);
            mark(endTag);
            measure(`vue ${name} patch`, startTag, endTag);
        };
    }
    else {
        updateComponent = () => {
            vm._update(vm._render(), hydrating);
        };
    }
    const watcherOptions = {
        before() {
            if (vm._isMounted && !vm._isDestroyed) {
                callHook$1(vm, 'beforeUpdate');
            }
        }
    };
    {
        watcherOptions.onTrack = e => callHook$1(vm, 'renderTracked', [e]);
        watcherOptions.onTrigger = e => callHook$1(vm, 'renderTriggered', [e]);
    }
    // we set this to vm._watcher inside the watcher's constructor
    // since the watcher's initial patch may call $forceUpdate (e.g. inside child
    // component's mounted hook), which relies on vm._watcher being already defined
    new Watcher(vm, updateComponent, noop, watcherOptions, true /* isRenderWatcher */);
    hydrating = false;
    // flush buffer for flush: "pre" watchers queued in setup()
    const preWatchers = vm._preWatchers;
    if (preWatchers) {
        for (let i = 0; i < preWatchers.length; i++) {
            preWatchers[i].run();
        }
    }
    // manually mounted instance, call mounted on self
    // mounted is called for render-created child components in its inserted hook
    if (vm.$vnode == null) {
        vm._isMounted = true;
        callHook$1(vm, 'mounted');
    }
    return vm;
}
function updateChildComponent(vm, propsData, listeners, parentVnode, renderChildren) {
    {
        isUpdatingChildComponent = true;
    }
    // determine whether component has slot children
    // we need to do this before overwriting $options._renderChildren.
    // check if there are dynamic scopedSlots (hand-written or compiled but with
    // dynamic slot names). Static scoped slots compiled from template has the
    // "$stable" marker.
    const newScopedSlots = parentVnode.data.scopedSlots;
    const oldScopedSlots = vm.$scopedSlots;
    const hasDynamicScopedSlot = !!((newScopedSlots && !newScopedSlots.$stable) ||
        (oldScopedSlots !== emptyObject && !oldScopedSlots.$stable) ||
        (newScopedSlots && vm.$scopedSlots.$key !== newScopedSlots.$key) ||
        (!newScopedSlots && vm.$scopedSlots.$key));
    // Any static slot children from the parent may have changed during parent's
    // update. Dynamic scoped slots may also have changed. In such cases, a forced
    // update is necessary to ensure correctness.
    let needsForceUpdate = !!(renderChildren || // has new static slots
        vm.$options._renderChildren || // has old static slots
        hasDynamicScopedSlot);
    const prevVNode = vm.$vnode;
    vm.$options._parentVnode = parentVnode;
    vm.$vnode = parentVnode; // update vm's placeholder node without re-render
    if (vm._vnode) {
        // update child tree's parent
        vm._vnode.parent = parentVnode;
    }
    vm.$options._renderChildren = renderChildren;
    // update $attrs and $listeners hash
    // these are also reactive so they may trigger child update if the child
    // used them during render
    const attrs = parentVnode.data.attrs || emptyObject;
    if (vm._attrsProxy) {
        // force update if attrs are accessed and has changed since it may be
        // passed to a child component.
        if (syncSetupProxy(vm._attrsProxy, attrs, (prevVNode.data && prevVNode.data.attrs) || emptyObject, vm, '$attrs')) {
            needsForceUpdate = true;
        }
    }
    vm.$attrs = attrs;
    // update listeners
    listeners = listeners || emptyObject;
    const prevListeners = vm.$options._parentListeners;
    if (vm._listenersProxy) {
        syncSetupProxy(vm._listenersProxy, listeners, prevListeners || emptyObject, vm, '$listeners');
    }
    vm.$listeners = vm.$options._parentListeners = listeners;
    updateComponentListeners(vm, listeners, prevListeners);
    // update props
    if (propsData && vm.$options.props) {
        toggleObserving(false);
        const props = vm._props;
        const propKeys = vm.$options._propKeys || [];
        for (let i = 0; i < propKeys.length; i++) {
            const key = propKeys[i];
            const propOptions = vm.$options.props; // wtf flow?
            props[key] = validateProp(key, propOptions, propsData, vm);
        }
        toggleObserving(true);
        // keep a copy of raw propsData
        vm.$options.propsData = propsData;
    }
    // resolve slots + force update if has children
    if (needsForceUpdate) {
        vm.$slots = resolveSlots(renderChildren, parentVnode.context);
        vm.$forceUpdate();
    }
    {
        isUpdatingChildComponent = false;
    }
}
function isInInactiveTree(vm) {
    while (vm && (vm = vm.$parent)) {
        if (vm._inactive)
            return true;
    }
    return false;
}
function activateChildComponent(vm, direct) {
    if (direct) {
        vm._directInactive = false;
        if (isInInactiveTree(vm)) {
            return;
        }
    }
    else if (vm._directInactive) {
        return;
    }
    if (vm._inactive || vm._inactive === null) {
        vm._inactive = false;
        for (let i = 0; i < vm.$children.length; i++) {
            activateChildComponent(vm.$children[i]);
        }
        callHook$1(vm, 'activated');
    }
}
function deactivateChildComponent(vm, direct) {
    if (direct) {
        vm._directInactive = true;
        if (isInInactiveTree(vm)) {
            return;
        }
    }
    if (!vm._inactive) {
        vm._inactive = true;
        for (let i = 0; i < vm.$children.length; i++) {
            deactivateChildComponent(vm.$children[i]);
        }
        callHook$1(vm, 'deactivated');
    }
}
function callHook$1(vm, hook, args, setContext = true) {
    // #7573 disable dep collection when invoking lifecycle hooks
    pushTarget();
    const prev = currentInstance;
    setContext && setCurrentInstance(vm);
    const handlers = vm.$options[hook];
    const info = `${hook} hook`;
    if (handlers) {
        for (let i = 0, j = handlers.length; i < j; i++) {
            invokeWithErrorHandling(handlers[i], vm, args || null, vm, info);
        }
    }
    if (vm._hasHookEvent) {
        vm.$emit('hook:' + hook);
    }
    setContext && setCurrentInstance(prev);
    popTarget();
}

const MAX_UPDATE_COUNT = 100;
const queue = [];
const activatedChildren = [];
let has = {};
let circular = {};
let waiting = false;
let flushing = false;
let index$1 = 0;
/**
 * Reset the scheduler's state.
 */
function resetSchedulerState() {
    index$1 = queue.length = activatedChildren.length = 0;
    has = {};
    {
        circular = {};
    }
    waiting = flushing = false;
}
// Async edge case #6566 requires saving the timestamp when event listeners are
// attached. However, calling performance.now() has a perf overhead especially
// if the page has thousands of event listeners. Instead, we take a timestamp
// every time the scheduler flushes and use that for all event listeners
// attached during that flush.
let currentFlushTimestamp = 0;
// Async edge case fix requires storing an event listener's attach timestamp.
let getNow = Date.now;
// Determine what event timestamp the browser is using. Annoyingly, the
// timestamp can either be hi-res (relative to page load) or low-res
// (relative to UNIX epoch), so in order to compare time we have to use the
// same timestamp type when saving the flush timestamp.
// All IE versions use low-res event timestamps, and have problematic clock
// implementations (#9632)
if (inBrowser && !isIE) {
    const performance = window.performance;
    if (performance &&
        typeof performance.now === 'function' &&
        getNow() > document.createEvent('Event').timeStamp) {
        // if the event timestamp, although evaluated AFTER the Date.now(), is
        // smaller than it, it means the event is using a hi-res timestamp,
        // and we need to use the hi-res version for event listener timestamps as
        // well.
        getNow = () => performance.now();
    }
}
const sortCompareFn = (a, b) => {
    if (a.post) {
        if (!b.post)
            return 1;
    }
    else if (b.post) {
        return -1;
    }
    return a.id - b.id;
};
/**
 * Flush both queues and run the watchers.
 */
function flushSchedulerQueue() {
    currentFlushTimestamp = getNow();
    flushing = true;
    let watcher, id;
    // Sort queue before flush.
    // This ensures that:
    // 1. Components are updated from parent to child. (because parent is always
    //    created before the child)
    // 2. A component's user watchers are run before its render watcher (because
    //    user watchers are created before the render watcher)
    // 3. If a component is destroyed during a parent component's watcher run,
    //    its watchers can be skipped.
    queue.sort(sortCompareFn);
    // do not cache length because more watchers might be pushed
    // as we run existing watchers
    for (index$1 = 0; index$1 < queue.length; index$1++) {
        watcher = queue[index$1];
        if (watcher.before) {
            watcher.before();
        }
        id = watcher.id;
        has[id] = null;
        watcher.run();
        // in dev build, check and stop circular updates.
        if (has[id] != null) {
            circular[id] = (circular[id] || 0) + 1;
            if (circular[id] > MAX_UPDATE_COUNT) {
                warn$2('You may have an infinite update loop ' +
                    (watcher.user
                        ? `in watcher with expression "${watcher.expression}"`
                        : `in a component render function.`), watcher.vm);
                break;
            }
        }
    }
    // keep copies of post queues before resetting state
    const activatedQueue = activatedChildren.slice();
    const updatedQueue = queue.slice();
    resetSchedulerState();
    // call component updated and activated hooks
    callActivatedHooks(activatedQueue);
    callUpdatedHooks(updatedQueue);
    cleanupDeps();
    // devtool hook
    /* istanbul ignore if */
    if (devtools && config.devtools) {
        devtools.emit('flush');
    }
}
function callUpdatedHooks(queue) {
    let i = queue.length;
    while (i--) {
        const watcher = queue[i];
        const vm = watcher.vm;
        if (vm && vm._watcher === watcher && vm._isMounted && !vm._isDestroyed) {
            callHook$1(vm, 'updated');
        }
    }
}
/**
 * Queue a kept-alive component that was activated during patch.
 * The queue will be processed after the entire tree has been patched.
 */
function queueActivatedComponent(vm) {
    // setting _inactive to false here so that a render function can
    // rely on checking whether it's in an inactive tree (e.g. router-view)
    vm._inactive = false;
    activatedChildren.push(vm);
}
function callActivatedHooks(queue) {
    for (let i = 0; i < queue.length; i++) {
        queue[i]._inactive = true;
        activateChildComponent(queue[i], true /* true */);
    }
}
/**
 * Push a watcher into the watcher queue.
 * Jobs with duplicate IDs will be skipped unless it's
 * pushed when the queue is being flushed.
 */
function queueWatcher(watcher) {
    const id = watcher.id;
    if (has[id] != null) {
        return;
    }
    if (watcher === Dep.target && watcher.noRecurse) {
        return;
    }
    has[id] = true;
    if (!flushing) {
        queue.push(watcher);
    }
    else {
        // if already flushing, splice the watcher based on its id
        // if already past its id, it will be run next immediately.
        let i = queue.length - 1;
        while (i > index$1 && queue[i].id > watcher.id) {
            i--;
        }
        queue.splice(i + 1, 0, watcher);
    }
    // queue the flush
    if (!waiting) {
        waiting = true;
        if (!config.async) {
            flushSchedulerQueue();
            return;
        }
        nextTick(flushSchedulerQueue);
    }
}

const WATCHER = `watcher`;
const WATCHER_CB = `${WATCHER} callback`;
const WATCHER_GETTER = `${WATCHER} getter`;
const WATCHER_CLEANUP = `${WATCHER} cleanup`;
// Simple effect.
function watchEffect(effect, options) {
    return doWatch(effect, null, options);
}
function watchPostEffect(effect, options) {
    return doWatch(effect, null, (Object.assign(Object.assign({}, options), { flush: 'post' }) ));
}
function watchSyncEffect(effect, options) {
    return doWatch(effect, null, (Object.assign(Object.assign({}, options), { flush: 'sync' }) ));
}
// initial value for watchers to trigger on undefined initial values
const INITIAL_WATCHER_VALUE = {};
// implementation
function watch(source, cb, options) {
    if (typeof cb !== 'function') {
        warn$2(`\`watch(fn, options?)\` signature has been moved to a separate API. ` +
            `Use \`watchEffect(fn, options?)\` instead. \`watch\` now only ` +
            `supports \`watch(source, cb, options?) signature.`);
    }
    return doWatch(source, cb, options);
}
function doWatch(source, cb, { immediate, deep, flush = 'pre', onTrack, onTrigger } = emptyObject) {
    if (!cb) {
        if (immediate !== undefined) {
            warn$2(`watch() "immediate" option is only respected when using the ` +
                `watch(source, callback, options?) signature.`);
        }
        if (deep !== undefined) {
            warn$2(`watch() "deep" option is only respected when using the ` +
                `watch(source, callback, options?) signature.`);
        }
    }
    const warnInvalidSource = (s) => {
        warn$2(`Invalid watch source: ${s}. A watch source can only be a getter/effect ` +
            `function, a ref, a reactive object, or an array of these types.`);
    };
    const instance = currentInstance;
    const call = (fn, type, args = null) => invokeWithErrorHandling(fn, null, args, instance, type);
    let getter;
    let forceTrigger = false;
    let isMultiSource = false;
    if (isRef(source)) {
        getter = () => source.value;
        forceTrigger = isShallow(source);
    }
    else if (isReactive(source)) {
        getter = () => {
            source.__ob__.dep.depend();
            return source;
        };
        deep = true;
    }
    else if (isArray(source)) {
        isMultiSource = true;
        forceTrigger = source.some(s => isReactive(s) || isShallow(s));
        getter = () => source.map(s => {
            if (isRef(s)) {
                return s.value;
            }
            else if (isReactive(s)) {
                return traverse(s);
            }
            else if (isFunction(s)) {
                return call(s, WATCHER_GETTER);
            }
            else {
                warnInvalidSource(s);
            }
        });
    }
    else if (isFunction(source)) {
        if (cb) {
            // getter with cb
            getter = () => call(source, WATCHER_GETTER);
        }
        else {
            // no cb -> simple effect
            getter = () => {
                if (instance && instance._isDestroyed) {
                    return;
                }
                if (cleanup) {
                    cleanup();
                }
                return call(source, WATCHER, [onCleanup]);
            };
        }
    }
    else {
        getter = noop;
        warnInvalidSource(source);
    }
    if (cb && deep) {
        const baseGetter = getter;
        getter = () => traverse(baseGetter());
    }
    let cleanup;
    let onCleanup = (fn) => {
        cleanup = watcher.onStop = () => {
            call(fn, WATCHER_CLEANUP);
        };
    };
    // in SSR there is no need to setup an actual effect, and it should be noop
    // unless it's eager
    if (isServerRendering()) {
        // we will also not call the invalidate callback (+ runner is not set up)
        onCleanup = noop;
        if (!cb) {
            getter();
        }
        else if (immediate) {
            call(cb, WATCHER_CB, [
                getter(),
                isMultiSource ? [] : undefined,
                onCleanup
            ]);
        }
        return noop;
    }
    const watcher = new Watcher(currentInstance, getter, noop, {
        lazy: true
    });
    watcher.noRecurse = !cb;
    let oldValue = isMultiSource ? [] : INITIAL_WATCHER_VALUE;
    // overwrite default run
    watcher.run = () => {
        if (!watcher.active) {
            return;
        }
        if (cb) {
            // watch(source, cb)
            const newValue = watcher.get();
            if (deep ||
                forceTrigger ||
                (isMultiSource
                    ? newValue.some((v, i) => hasChanged(v, oldValue[i]))
                    : hasChanged(newValue, oldValue))) {
                // cleanup before running cb again
                if (cleanup) {
                    cleanup();
                }
                call(cb, WATCHER_CB, [
                    newValue,
                    // pass undefined as the old value when it's changed for the first time
                    oldValue === INITIAL_WATCHER_VALUE ? undefined : oldValue,
                    onCleanup
                ]);
                oldValue = newValue;
            }
        }
        else {
            // watchEffect
            watcher.get();
        }
    };
    if (flush === 'sync') {
        watcher.update = watcher.run;
    }
    else if (flush === 'post') {
        watcher.post = true;
        watcher.update = () => queueWatcher(watcher);
    }
    else {
        // pre
        watcher.update = () => {
            if (instance && instance === currentInstance && !instance._isMounted) {
                // pre-watcher triggered before
                const buffer = instance._preWatchers || (instance._preWatchers = []);
                if (buffer.indexOf(watcher) < 0)
                    buffer.push(watcher);
            }
            else {
                queueWatcher(watcher);
            }
        };
    }
    {
        watcher.onTrack = onTrack;
        watcher.onTrigger = onTrigger;
    }
    // initial run
    if (cb) {
        if (immediate) {
            watcher.run();
        }
        else {
            oldValue = watcher.get();
        }
    }
    else if (flush === 'post' && instance) {
        instance.$once('hook:mounted', () => watcher.get());
    }
    else {
        watcher.get();
    }
    return () => {
        watcher.teardown();
    };
}

let activeEffectScope;
class EffectScope {
    constructor(detached = false) {
        this.detached = detached;
        /**
         * @internal
         */
        this.active = true;
        /**
         * @internal
         */
        this.effects = [];
        /**
         * @internal
         */
        this.cleanups = [];
        this.parent = activeEffectScope;
        if (!detached && activeEffectScope) {
            this.index =
                (activeEffectScope.scopes || (activeEffectScope.scopes = [])).push(this) - 1;
        }
    }
    run(fn) {
        if (this.active) {
            const currentEffectScope = activeEffectScope;
            try {
                activeEffectScope = this;
                return fn();
            }
            finally {
                activeEffectScope = currentEffectScope;
            }
        }
        else {
            warn$2(`cannot run an inactive effect scope.`);
        }
    }
    /**
     * This should only be called on non-detached scopes
     * @internal
     */
    on() {
        activeEffectScope = this;
    }
    /**
     * This should only be called on non-detached scopes
     * @internal
     */
    off() {
        activeEffectScope = this.parent;
    }
    stop(fromParent) {
        if (this.active) {
            let i, l;
            for (i = 0, l = this.effects.length; i < l; i++) {
                this.effects[i].teardown();
            }
            for (i = 0, l = this.cleanups.length; i < l; i++) {
                this.cleanups[i]();
            }
            if (this.scopes) {
                for (i = 0, l = this.scopes.length; i < l; i++) {
                    this.scopes[i].stop(true);
                }
            }
            // nested scope, dereference from parent to avoid memory leaks
            if (!this.detached && this.parent && !fromParent) {
                // optimized O(1) removal
                const last = this.parent.scopes.pop();
                if (last && last !== this) {
                    this.parent.scopes[this.index] = last;
                    last.index = this.index;
                }
            }
            this.parent = undefined;
            this.active = false;
        }
    }
}
function effectScope(detached) {
    return new EffectScope(detached);
}
/**
 * @internal
 */
function recordEffectScope(effect, scope = activeEffectScope) {
    if (scope && scope.active) {
        scope.effects.push(effect);
    }
}
function getCurrentScope() {
    return activeEffectScope;
}
function onScopeDispose(fn) {
    if (activeEffectScope) {
        activeEffectScope.cleanups.push(fn);
    }
    else {
        warn$2(`onScopeDispose() is called when there is no active effect scope` +
            ` to be associated with.`);
    }
}

function provide(key, value) {
    if (!currentInstance) {
        {
            warn$2(`provide() can only be used inside setup().`);
        }
    }
    else {
        // TS doesn't allow symbol as index type
        resolveProvided(currentInstance)[key] = value;
    }
}
function resolveProvided(vm) {
    // by default an instance inherits its parent's provides object
    // but when it needs to provide values of its own, it creates its
    // own provides object using parent provides object as prototype.
    // this way in `inject` we can simply look up injections from direct
    // parent and let the prototype chain do the work.
    const existing = vm._provided;
    const parentProvides = vm.$parent && vm.$parent._provided;
    if (parentProvides === existing) {
        return (vm._provided = Object.create(parentProvides));
    }
    else {
        return existing;
    }
}
function inject(key, defaultValue, treatDefaultAsFactory = false) {
    // fallback to `currentRenderingInstance` so that this can be called in
    // a functional component
    const instance = currentInstance;
    if (instance) {
        // #2400
        // to support `app.use` plugins,
        // fallback to appContext's `provides` if the instance is at root
        const provides = instance.$parent && instance.$parent._provided;
        if (provides && key in provides) {
            // TS doesn't allow symbol as index type
            return provides[key];
        }
        else if (arguments.length > 1) {
            return treatDefaultAsFactory && isFunction(defaultValue)
                ? defaultValue.call(instance)
                : defaultValue;
        }
        else {
            warn$2(`injection "${String(key)}" not found.`);
        }
    }
    else {
        warn$2(`inject() can only be used inside setup() or functional components.`);
    }
}

/**
 * @internal this function needs manual public type declaration because it relies
 * on previously manually authored types from Vue 2
 */
function h(type, props, children) {
    if (!currentInstance) {
        warn$2(`globally imported h() can only be invoked when there is an active ` +
                `component instance, e.g. synchronously in a component's render or setup function.`);
    }
    return createElement$1(currentInstance, type, props, children, 2, true);
}

function handleError(err, vm, info) {
    // Deactivate deps tracking while processing error handler to avoid possible infinite rendering.
    // See: https://github.com/vuejs/vuex/issues/1505
    pushTarget();
    try {
        if (vm) {
            let cur = vm;
            while ((cur = cur.$parent)) {
                const hooks = cur.$options.errorCaptured;
                if (hooks) {
                    for (let i = 0; i < hooks.length; i++) {
                        try {
                            const capture = hooks[i].call(cur, err, vm, info) === false;
                            if (capture)
                                return;
                        }
                        catch (e) {
                            globalHandleError(e, cur, 'errorCaptured hook');
                        }
                    }
                }
            }
        }
        globalHandleError(err, vm, info);
    }
    finally {
        popTarget();
    }
}
function invokeWithErrorHandling(handler, context, args, vm, info) {
    let res;
    try {
        res = args ? handler.apply(context, args) : handler.call(context);
        if (res && !res._isVue && isPromise(res) && !res._handled) {
            res.catch(e => handleError(e, vm, info + ` (Promise/async)`));
            res._handled = true;
        }
    }
    catch (e) {
        handleError(e, vm, info);
    }
    return res;
}
function globalHandleError(err, vm, info) {
    if (config.errorHandler) {
        try {
            return config.errorHandler.call(null, err, vm, info);
        }
        catch (e) {
            // if the user intentionally throws the original error in the handler,
            // do not log it twice
            if (e !== err) {
                logError(e, null, 'config.errorHandler');
            }
        }
    }
    logError(err, vm, info);
}
function logError(err, vm, info) {
    {
        warn$2(`Error in ${info}: "${err.toString()}"`, vm);
    }
    /* istanbul ignore else */
    if (inBrowser && typeof console !== 'undefined') {
        console.error(err);
    }
    else {
        throw err;
    }
}

/* globals MutationObserver */
let isUsingMicroTask = false;
const callbacks = [];
let pending = false;
function flushCallbacks() {
    pending = false;
    const copies = callbacks.slice(0);
    callbacks.length = 0;
    for (let i = 0; i < copies.length; i++) {
        copies[i]();
    }
}
// Here we have async deferring wrappers using microtasks.
// In 2.5 we used (macro) tasks (in combination with microtasks).
// However, it has subtle problems when state is changed right before repaint
// (e.g. #6813, out-in transitions).
// Also, using (macro) tasks in event handler would cause some weird behaviors
// that cannot be circumvented (e.g. #7109, #7153, #7546, #7834, #8109).
// So we now use microtasks everywhere, again.
// A major drawback of this tradeoff is that there are some scenarios
// where microtasks have too high a priority and fire in between supposedly
// sequential events (e.g. #4521, #6690, which have workarounds)
// or even between bubbling of the same event (#6566).
let timerFunc;
// The nextTick behavior leverages the microtask queue, which can be accessed
// via either native Promise.then or MutationObserver.
// MutationObserver has wider support, however it is seriously bugged in
// UIWebView in iOS >= 9.3.3 when triggered in touch event handlers. It
// completely stops working after triggering a few times... so, if native
// Promise is available, we will use it:
/* istanbul ignore next, $flow-disable-line */
if (typeof Promise !== 'undefined' && isNative(Promise)) {
    const p = Promise.resolve();
    timerFunc = () => {
        p.then(flushCallbacks);
        // In problematic UIWebViews, Promise.then doesn't completely break, but
        // it can get stuck in a weird state where callbacks are pushed into the
        // microtask queue but the queue isn't being flushed, until the browser
        // needs to do some other work, e.g. handle a timer. Therefore we can
        // "force" the microtask queue to be flushed by adding an empty timer.
        if (isIOS)
            setTimeout(noop);
    };
    isUsingMicroTask = true;
}
else if (!isIE &&
    typeof MutationObserver !== 'undefined' &&
    (isNative(MutationObserver) ||
        // PhantomJS and iOS 7.x
        MutationObserver.toString() === '[object MutationObserverConstructor]')) {
    // Use MutationObserver where native Promise is not available,
    // e.g. PhantomJS, iOS7, Android 4.4
    // (#6466 MutationObserver is unreliable in IE11)
    let counter = 1;
    const observer = new MutationObserver(flushCallbacks);
    const textNode = document.createTextNode(String(counter));
    observer.observe(textNode, {
        characterData: true
    });
    timerFunc = () => {
        counter = (counter + 1) % 2;
        textNode.data = String(counter);
    };
    isUsingMicroTask = true;
}
else if (typeof setImmediate !== 'undefined' && isNative(setImmediate)) {
    // Fallback to setImmediate.
    // Technically it leverages the (macro) task queue,
    // but it is still a better choice than setTimeout.
    timerFunc = () => {
        setImmediate(flushCallbacks);
    };
}
else {
    // Fallback to setTimeout.
    timerFunc = () => {
        setTimeout(flushCallbacks, 0);
    };
}
/**
 * @internal
 */
function nextTick(cb, ctx) {
    let _resolve;
    callbacks.push(() => {
        if (cb) {
            try {
                cb.call(ctx);
            }
            catch (e) {
                handleError(e, ctx, 'nextTick');
            }
        }
        else if (_resolve) {
            _resolve(ctx);
        }
    });
    if (!pending) {
        pending = true;
        timerFunc();
    }
    // $flow-disable-line
    if (!cb && typeof Promise !== 'undefined') {
        return new Promise(resolve => {
            _resolve = resolve;
        });
    }
}

function useCssModule(name = '$style') {
    /* istanbul ignore else */
    {
        if (!currentInstance) {
            warn$2(`useCssModule must be called inside setup()`);
            return emptyObject;
        }
        const mod = currentInstance[name];
        if (!mod) {
            warn$2(`Current instance does not have CSS module named "${name}".`);
            return emptyObject;
        }
        return mod;
    }
}

/**
 * Runtime helper for SFC's CSS variable injection feature.
 * @private
 */
function useCssVars(getter) {
    if (!inBrowser && !false)
        return;
    const instance = currentInstance;
    if (!instance) {
        warn$2(`useCssVars is called without current active component instance.`);
        return;
    }
    watchPostEffect(() => {
        const el = instance.$el;
        const vars = getter(instance, instance._setupProxy);
        if (el && el.nodeType === 1) {
            const style = el.style;
            for (const key in vars) {
                style.setProperty(`--${key}`, vars[key]);
            }
        }
    });
}

/**
 * v3-compatible async component API.
 * @internal the type is manually declared in <root>/types/v3-define-async-component.d.ts
 * because it relies on existing manual types
 */
function defineAsyncComponent(source) {
    if (isFunction(source)) {
        source = { loader: source };
    }
    const { loader, loadingComponent, errorComponent, delay = 200, timeout, // undefined = never times out
    suspensible = false, // in Vue 3 default is true
    onError: userOnError } = source;
    if (suspensible) {
        warn$2(`The suspensiblbe option for async components is not supported in Vue2. It is ignored.`);
    }
    let pendingRequest = null;
    let retries = 0;
    const retry = () => {
        retries++;
        pendingRequest = null;
        return load();
    };
    const load = () => {
        let thisRequest;
        return (pendingRequest ||
            (thisRequest = pendingRequest =
                loader()
                    .catch(err => {
                    err = err instanceof Error ? err : new Error(String(err));
                    if (userOnError) {
                        return new Promise((resolve, reject) => {
                            const userRetry = () => resolve(retry());
                            const userFail = () => reject(err);
                            userOnError(err, userRetry, userFail, retries + 1);
                        });
                    }
                    else {
                        throw err;
                    }
                })
                    .then((comp) => {
                    if (thisRequest !== pendingRequest && pendingRequest) {
                        return pendingRequest;
                    }
                    if (!comp) {
                        warn$2(`Async component loader resolved to undefined. ` +
                            `If you are using retry(), make sure to return its return value.`);
                    }
                    // interop module default
                    if (comp &&
                        (comp.__esModule || comp[Symbol.toStringTag] === 'Module')) {
                        comp = comp.default;
                    }
                    if (comp && !isObject(comp) && !isFunction(comp)) {
                        throw new Error(`Invalid async component load result: ${comp}`);
                    }
                    return comp;
                })));
    };
    return () => {
        const component = load();
        return {
            component,
            delay,
            timeout,
            error: errorComponent,
            loading: loadingComponent
        };
    };
}

function createLifeCycle(hookName) {
    return (fn, target = currentInstance) => {
        if (!target) {
            warn$2(`${formatName(hookName)} is called when there is no active component instance to be ` +
                    `associated with. ` +
                    `Lifecycle injection APIs can only be used during execution of setup().`);
            return;
        }
        return injectHook(target, hookName, fn);
    };
}
function formatName(name) {
    if (name === 'beforeDestroy') {
        name = 'beforeUnmount';
    }
    else if (name === 'destroyed') {
        name = 'unmounted';
    }
    return `on${name[0].toUpperCase() + name.slice(1)}`;
}
function injectHook(instance, hookName, fn) {
    const options = instance.$options;
    options[hookName] = mergeLifecycleHook(options[hookName], fn);
}
const onBeforeMount = createLifeCycle('beforeMount');
const onMounted = createLifeCycle('mounted');
const onBeforeUpdate = createLifeCycle('beforeUpdate');
const onUpdated = createLifeCycle('updated');
const onBeforeUnmount = createLifeCycle('beforeDestroy');
const onUnmounted = createLifeCycle('destroyed');
const onActivated = createLifeCycle('activated');
const onDeactivated = createLifeCycle('deactivated');
const onServerPrefetch = createLifeCycle('serverPrefetch');
const onRenderTracked = createLifeCycle('renderTracked');
const onRenderTriggered = createLifeCycle('renderTriggered');
const injectErrorCapturedHook = createLifeCycle('errorCaptured');
function onErrorCaptured(hook, target = currentInstance) {
    injectErrorCapturedHook(hook, target);
}

/**
 * Note: also update dist/vue.runtime.mjs when adding new exports to this file.
 */
const version = '2.7.14';
/**
 * @internal type is manually declared in <root>/types/v3-define-component.d.ts
 */
function defineComponent(options) {
    return options;
}

var vca = /*#__PURE__*/Object.freeze({
  __proto__: null,
  version: version,
  defineComponent: defineComponent,
  ref: ref$1,
  shallowRef: shallowRef,
  isRef: isRef,
  toRef: toRef,
  toRefs: toRefs,
  unref: unref,
  proxyRefs: proxyRefs,
  customRef: customRef,
  triggerRef: triggerRef,
  reactive: reactive,
  isReactive: isReactive,
  isReadonly: isReadonly,
  isShallow: isShallow,
  isProxy: isProxy,
  shallowReactive: shallowReactive,
  markRaw: markRaw,
  toRaw: toRaw,
  readonly: readonly,
  shallowReadonly: shallowReadonly,
  computed: computed,
  watch: watch,
  watchEffect: watchEffect,
  watchPostEffect: watchPostEffect,
  watchSyncEffect: watchSyncEffect,
  EffectScope: EffectScope,
  effectScope: effectScope,
  onScopeDispose: onScopeDispose,
  getCurrentScope: getCurrentScope,
  provide: provide,
  inject: inject,
  h: h,
  getCurrentInstance: getCurrentInstance,
  useSlots: useSlots,
  useAttrs: useAttrs,
  useListeners: useListeners,
  mergeDefaults: mergeDefaults,
  nextTick: nextTick,
  set: set,
  del: del,
  useCssModule: useCssModule,
  useCssVars: useCssVars,
  defineAsyncComponent: defineAsyncComponent,
  onBeforeMount: onBeforeMount,
  onMounted: onMounted,
  onBeforeUpdate: onBeforeUpdate,
  onUpdated: onUpdated,
  onBeforeUnmount: onBeforeUnmount,
  onUnmounted: onUnmounted,
  onActivated: onActivated,
  onDeactivated: onDeactivated,
  onServerPrefetch: onServerPrefetch,
  onRenderTracked: onRenderTracked,
  onRenderTriggered: onRenderTriggered,
  onErrorCaptured: onErrorCaptured
});

const seenObjects = new _Set();
/**
 * Recursively traverse an object to evoke all converted
 * getters, so that every nested property inside the object
 * is collected as a "deep" dependency.
 */
function traverse(val) {
    _traverse(val, seenObjects);
    seenObjects.clear();
    return val;
}
function _traverse(val, seen) {
    let i, keys;
    const isA = isArray(val);
    if ((!isA && !isObject(val)) ||
        val.__v_skip /* ReactiveFlags.SKIP */ ||
        Object.isFrozen(val) ||
        val instanceof VNode) {
        return;
    }
    if (val.__ob__) {
        const depId = val.__ob__.dep.id;
        if (seen.has(depId)) {
            return;
        }
        seen.add(depId);
    }
    if (isA) {
        i = val.length;
        while (i--)
            _traverse(val[i], seen);
    }
    else if (isRef(val)) {
        _traverse(val.value, seen);
    }
    else {
        keys = Object.keys(val);
        i = keys.length;
        while (i--)
            _traverse(val[keys[i]], seen);
    }
}

let uid$1 = 0;
/**
 * A watcher parses an expression, collects dependencies,
 * and fires callback when the expression value changes.
 * This is used for both the $watch() api and directives.
 * @internal
 */
class Watcher {
    constructor(vm, expOrFn, cb, options, isRenderWatcher) {
        recordEffectScope(this, 
        // if the active effect scope is manually created (not a component scope),
        // prioritize it
        activeEffectScope && !activeEffectScope._vm
            ? activeEffectScope
            : vm
                ? vm._scope
                : undefined);
        if ((this.vm = vm) && isRenderWatcher) {
            vm._watcher = this;
        }
        // options
        if (options) {
            this.deep = !!options.deep;
            this.user = !!options.user;
            this.lazy = !!options.lazy;
            this.sync = !!options.sync;
            this.before = options.before;
            {
                this.onTrack = options.onTrack;
                this.onTrigger = options.onTrigger;
            }
        }
        else {
            this.deep = this.user = this.lazy = this.sync = false;
        }
        this.cb = cb;
        this.id = ++uid$1; // uid for batching
        this.active = true;
        this.post = false;
        this.dirty = this.lazy; // for lazy watchers
        this.deps = [];
        this.newDeps = [];
        this.depIds = new _Set();
        this.newDepIds = new _Set();
        this.expression = expOrFn.toString() ;
        // parse expression for getter
        if (isFunction(expOrFn)) {
            this.getter = expOrFn;
        }
        else {
            this.getter = parsePath(expOrFn);
            if (!this.getter) {
                this.getter = noop;
                warn$2(`Failed watching path: "${expOrFn}" ` +
                        'Watcher only accepts simple dot-delimited paths. ' +
                        'For full control, use a function instead.', vm);
            }
        }
        this.value = this.lazy ? undefined : this.get();
    }
    /**
     * Evaluate the getter, and re-collect dependencies.
     */
    get() {
        pushTarget(this);
        let value;
        const vm = this.vm;
        try {
            value = this.getter.call(vm, vm);
        }
        catch (e) {
            if (this.user) {
                handleError(e, vm, `getter for watcher "${this.expression}"`);
            }
            else {
                throw e;
            }
        }
        finally {
            // "touch" every property so they are all tracked as
            // dependencies for deep watching
            if (this.deep) {
                traverse(value);
            }
            popTarget();
            this.cleanupDeps();
        }
        return value;
    }
    /**
     * Add a dependency to this directive.
     */
    addDep(dep) {
        const id = dep.id;
        if (!this.newDepIds.has(id)) {
            this.newDepIds.add(id);
            this.newDeps.push(dep);
            if (!this.depIds.has(id)) {
                dep.addSub(this);
            }
        }
    }
    /**
     * Clean up for dependency collection.
     */
    cleanupDeps() {
        let i = this.deps.length;
        while (i--) {
            const dep = this.deps[i];
            if (!this.newDepIds.has(dep.id)) {
                dep.removeSub(this);
            }
        }
        let tmp = this.depIds;
        this.depIds = this.newDepIds;
        this.newDepIds = tmp;
        this.newDepIds.clear();
        tmp = this.deps;
        this.deps = this.newDeps;
        this.newDeps = tmp;
        this.newDeps.length = 0;
    }
    /**
     * Subscriber interface.
     * Will be called when a dependency changes.
     */
    update() {
        /* istanbul ignore else */
        if (this.lazy) {
            this.dirty = true;
        }
        else if (this.sync) {
            this.run();
        }
        else {
            queueWatcher(this);
        }
    }
    /**
     * Scheduler job interface.
     * Will be called by the scheduler.
     */
    run() {
        if (this.active) {
            const value = this.get();
            if (value !== this.value ||
                // Deep watchers and watchers on Object/Arrays should fire even
                // when the value is the same, because the value may
                // have mutated.
                isObject(value) ||
                this.deep) {
                // set new value
                const oldValue = this.value;
                this.value = value;
                if (this.user) {
                    const info = `callback for watcher "${this.expression}"`;
                    invokeWithErrorHandling(this.cb, this.vm, [value, oldValue], this.vm, info);
                }
                else {
                    this.cb.call(this.vm, value, oldValue);
                }
            }
        }
    }
    /**
     * Evaluate the value of the watcher.
     * This only gets called for lazy watchers.
     */
    evaluate() {
        this.value = this.get();
        this.dirty = false;
    }
    /**
     * Depend on all deps collected by this watcher.
     */
    depend() {
        let i = this.deps.length;
        while (i--) {
            this.deps[i].depend();
        }
    }
    /**
     * Remove self from all dependencies' subscriber list.
     */
    teardown() {
        if (this.vm && !this.vm._isBeingDestroyed) {
            remove$2(this.vm._scope.effects, this);
        }
        if (this.active) {
            let i = this.deps.length;
            while (i--) {
                this.deps[i].removeSub(this);
            }
            this.active = false;
            if (this.onStop) {
                this.onStop();
            }
        }
    }
}

const sharedPropertyDefinition = {
    enumerable: true,
    configurable: true,
    get: noop,
    set: noop
};
function proxy(target, sourceKey, key) {
    sharedPropertyDefinition.get = function proxyGetter() {
        return this[sourceKey][key];
    };
    sharedPropertyDefinition.set = function proxySetter(val) {
        this[sourceKey][key] = val;
    };
    Object.defineProperty(target, key, sharedPropertyDefinition);
}
function initState(vm) {
    const opts = vm.$options;
    if (opts.props)
        initProps$1(vm, opts.props);
    // Composition API
    initSetup(vm);
    if (opts.methods)
        initMethods(vm, opts.methods);
    if (opts.data) {
        initData(vm);
    }
    else {
        const ob = observe((vm._data = {}));
        ob && ob.vmCount++;
    }
    if (opts.computed)
        initComputed$1(vm, opts.computed);
    if (opts.watch && opts.watch !== nativeWatch) {
        initWatch(vm, opts.watch);
    }
}
function initProps$1(vm, propsOptions) {
    const propsData = vm.$options.propsData || {};
    const props = (vm._props = shallowReactive({}));
    // cache prop keys so that future props updates can iterate using Array
    // instead of dynamic object key enumeration.
    const keys = (vm.$options._propKeys = []);
    const isRoot = !vm.$parent;
    // root instance props should be converted
    if (!isRoot) {
        toggleObserving(false);
    }
    for (const key in propsOptions) {
        keys.push(key);
        const value = validateProp(key, propsOptions, propsData, vm);
        /* istanbul ignore else */
        {
            const hyphenatedKey = hyphenate(key);
            if (isReservedAttribute(hyphenatedKey) ||
                config.isReservedAttr(hyphenatedKey)) {
                warn$2(`"${hyphenatedKey}" is a reserved attribute and cannot be used as component prop.`, vm);
            }
            defineReactive(props, key, value, () => {
                if (!isRoot && !isUpdatingChildComponent) {
                    warn$2(`Avoid mutating a prop directly since the value will be ` +
                        `overwritten whenever the parent component re-renders. ` +
                        `Instead, use a data or computed property based on the prop's ` +
                        `value. Prop being mutated: "${key}"`, vm);
                }
            });
        }
        // static props are already proxied on the component's prototype
        // during Vue.extend(). We only need to proxy props defined at
        // instantiation here.
        if (!(key in vm)) {
            proxy(vm, `_props`, key);
        }
    }
    toggleObserving(true);
}
function initData(vm) {
    let data = vm.$options.data;
    data = vm._data = isFunction(data) ? getData(data, vm) : data || {};
    if (!isPlainObject(data)) {
        data = {};
        warn$2('data functions should return an object:\n' +
                'https://v2.vuejs.org/v2/guide/components.html#data-Must-Be-a-Function', vm);
    }
    // proxy data on instance
    const keys = Object.keys(data);
    const props = vm.$options.props;
    const methods = vm.$options.methods;
    let i = keys.length;
    while (i--) {
        const key = keys[i];
        {
            if (methods && hasOwn(methods, key)) {
                warn$2(`Method "${key}" has already been defined as a data property.`, vm);
            }
        }
        if (props && hasOwn(props, key)) {
            warn$2(`The data property "${key}" is already declared as a prop. ` +
                    `Use prop default value instead.`, vm);
        }
        else if (!isReserved(key)) {
            proxy(vm, `_data`, key);
        }
    }
    // observe data
    const ob = observe(data);
    ob && ob.vmCount++;
}
function getData(data, vm) {
    // #7573 disable dep collection when invoking data getters
    pushTarget();
    try {
        return data.call(vm, vm);
    }
    catch (e) {
        handleError(e, vm, `data()`);
        return {};
    }
    finally {
        popTarget();
    }
}
const computedWatcherOptions = { lazy: true };
function initComputed$1(vm, computed) {
    // $flow-disable-line
    const watchers = (vm._computedWatchers = Object.create(null));
    // computed properties are just getters during SSR
    const isSSR = isServerRendering();
    for (const key in computed) {
        const userDef = computed[key];
        const getter = isFunction(userDef) ? userDef : userDef.get;
        if (getter == null) {
            warn$2(`Getter is missing for computed property "${key}".`, vm);
        }
        if (!isSSR) {
            // create internal watcher for the computed property.
            watchers[key] = new Watcher(vm, getter || noop, noop, computedWatcherOptions);
        }
        // component-defined computed properties are already defined on the
        // component prototype. We only need to define computed properties defined
        // at instantiation here.
        if (!(key in vm)) {
            defineComputed(vm, key, userDef);
        }
        else {
            if (key in vm.$data) {
                warn$2(`The computed property "${key}" is already defined in data.`, vm);
            }
            else if (vm.$options.props && key in vm.$options.props) {
                warn$2(`The computed property "${key}" is already defined as a prop.`, vm);
            }
            else if (vm.$options.methods && key in vm.$options.methods) {
                warn$2(`The computed property "${key}" is already defined as a method.`, vm);
            }
        }
    }
}
function defineComputed(target, key, userDef) {
    const shouldCache = !isServerRendering();
    if (isFunction(userDef)) {
        sharedPropertyDefinition.get = shouldCache
            ? createComputedGetter(key)
            : createGetterInvoker(userDef);
        sharedPropertyDefinition.set = noop;
    }
    else {
        sharedPropertyDefinition.get = userDef.get
            ? shouldCache && userDef.cache !== false
                ? createComputedGetter(key)
                : createGetterInvoker(userDef.get)
            : noop;
        sharedPropertyDefinition.set = userDef.set || noop;
    }
    if (sharedPropertyDefinition.set === noop) {
        sharedPropertyDefinition.set = function () {
            warn$2(`Computed property "${key}" was assigned to but it has no setter.`, this);
        };
    }
    Object.defineProperty(target, key, sharedPropertyDefinition);
}
function createComputedGetter(key) {
    return function computedGetter() {
        const watcher = this._computedWatchers && this._computedWatchers[key];
        if (watcher) {
            if (watcher.dirty) {
                watcher.evaluate();
            }
            if (Dep.target) {
                if (Dep.target.onTrack) {
                    Dep.target.onTrack({
                        effect: Dep.target,
                        target: this,
                        type: "get" /* TrackOpTypes.GET */,
                        key
                    });
                }
                watcher.depend();
            }
            return watcher.value;
        }
    };
}
function createGetterInvoker(fn) {
    return function computedGetter() {
        return fn.call(this, this);
    };
}
function initMethods(vm, methods) {
    const props = vm.$options.props;
    for (const key in methods) {
        {
            if (typeof methods[key] !== 'function') {
                warn$2(`Method "${key}" has type "${typeof methods[key]}" in the component definition. ` +
                    `Did you reference the function correctly?`, vm);
            }
            if (props && hasOwn(props, key)) {
                warn$2(`Method "${key}" has already been defined as a prop.`, vm);
            }
            if (key in vm && isReserved(key)) {
                warn$2(`Method "${key}" conflicts with an existing Vue instance method. ` +
                    `Avoid defining component methods that start with _ or $.`);
            }
        }
        vm[key] = typeof methods[key] !== 'function' ? noop : bind$1(methods[key], vm);
    }
}
function initWatch(vm, watch) {
    for (const key in watch) {
        const handler = watch[key];
        if (isArray(handler)) {
            for (let i = 0; i < handler.length; i++) {
                createWatcher(vm, key, handler[i]);
            }
        }
        else {
            createWatcher(vm, key, handler);
        }
    }
}
function createWatcher(vm, expOrFn, handler, options) {
    if (isPlainObject(handler)) {
        options = handler;
        handler = handler.handler;
    }
    if (typeof handler === 'string') {
        handler = vm[handler];
    }
    return vm.$watch(expOrFn, handler, options);
}
function stateMixin(Vue) {
    // flow somehow has problems with directly declared definition object
    // when using Object.defineProperty, so we have to procedurally build up
    // the object here.
    const dataDef = {};
    dataDef.get = function () {
        return this._data;
    };
    const propsDef = {};
    propsDef.get = function () {
        return this._props;
    };
    {
        dataDef.set = function () {
            warn$2('Avoid replacing instance root $data. ' +
                'Use nested data properties instead.', this);
        };
        propsDef.set = function () {
            warn$2(`$props is readonly.`, this);
        };
    }
    Object.defineProperty(Vue.prototype, '$data', dataDef);
    Object.defineProperty(Vue.prototype, '$props', propsDef);
    Vue.prototype.$set = set;
    Vue.prototype.$delete = del;
    Vue.prototype.$watch = function (expOrFn, cb, options) {
        const vm = this;
        if (isPlainObject(cb)) {
            return createWatcher(vm, expOrFn, cb, options);
        }
        options = options || {};
        options.user = true;
        const watcher = new Watcher(vm, expOrFn, cb, options);
        if (options.immediate) {
            const info = `callback for immediate watcher "${watcher.expression}"`;
            pushTarget();
            invokeWithErrorHandling(cb, vm, [watcher.value], vm, info);
            popTarget();
        }
        return function unwatchFn() {
            watcher.teardown();
        };
    };
}

function initProvide(vm) {
    const provideOption = vm.$options.provide;
    if (provideOption) {
        const provided = isFunction(provideOption)
            ? provideOption.call(vm)
            : provideOption;
        if (!isObject(provided)) {
            return;
        }
        const source = resolveProvided(vm);
        // IE9 doesn't support Object.getOwnPropertyDescriptors so we have to
        // iterate the keys ourselves.
        const keys = hasSymbol ? Reflect.ownKeys(provided) : Object.keys(provided);
        for (let i = 0; i < keys.length; i++) {
            const key = keys[i];
            Object.defineProperty(source, key, Object.getOwnPropertyDescriptor(provided, key));
        }
    }
}
function initInjections(vm) {
    const result = resolveInject(vm.$options.inject, vm);
    if (result) {
        toggleObserving(false);
        Object.keys(result).forEach(key => {
            /* istanbul ignore else */
            {
                defineReactive(vm, key, result[key], () => {
                    warn$2(`Avoid mutating an injected value directly since the changes will be ` +
                        `overwritten whenever the provided component re-renders. ` +
                        `injection being mutated: "${key}"`, vm);
                });
            }
        });
        toggleObserving(true);
    }
}
function resolveInject(inject, vm) {
    if (inject) {
        // inject is :any because flow is not smart enough to figure out cached
        const result = Object.create(null);
        const keys = hasSymbol ? Reflect.ownKeys(inject) : Object.keys(inject);
        for (let i = 0; i < keys.length; i++) {
            const key = keys[i];
            // #6574 in case the inject object is observed...
            if (key === '__ob__')
                continue;
            const provideKey = inject[key].from;
            if (provideKey in vm._provided) {
                result[key] = vm._provided[provideKey];
            }
            else if ('default' in inject[key]) {
                const provideDefault = inject[key].default;
                result[key] = isFunction(provideDefault)
                    ? provideDefault.call(vm)
                    : provideDefault;
            }
            else {
                warn$2(`Injection "${key}" not found`, vm);
            }
        }
        return result;
    }
}

let uid = 0;
function initMixin$1(Vue) {
    Vue.prototype._init = function (options) {
        const vm = this;
        // a uid
        vm._uid = uid++;
        let startTag, endTag;
        /* istanbul ignore if */
        if (config.performance && mark) {
            startTag = `vue-perf-start:${vm._uid}`;
            endTag = `vue-perf-end:${vm._uid}`;
            mark(startTag);
        }
        // a flag to mark this as a Vue instance without having to do instanceof
        // check
        vm._isVue = true;
        // avoid instances from being observed
        vm.__v_skip = true;
        // effect scope
        vm._scope = new EffectScope(true /* detached */);
        vm._scope._vm = true;
        // merge options
        if (options && options._isComponent) {
            // optimize internal component instantiation
            // since dynamic options merging is pretty slow, and none of the
            // internal component options needs special treatment.
            initInternalComponent(vm, options);
        }
        else {
            vm.$options = mergeOptions(resolveConstructorOptions(vm.constructor), options || {}, vm);
        }
        /* istanbul ignore else */
        {
            initProxy(vm);
        }
        // expose real self
        vm._self = vm;
        initLifecycle(vm);
        initEvents(vm);
        initRender(vm);
        callHook$1(vm, 'beforeCreate', undefined, false /* setContext */);
        initInjections(vm); // resolve injections before data/props
        initState(vm);
        initProvide(vm); // resolve provide after data/props
        callHook$1(vm, 'created');
        /* istanbul ignore if */
        if (config.performance && mark) {
            vm._name = formatComponentName(vm, false);
            mark(endTag);
            measure(`vue ${vm._name} init`, startTag, endTag);
        }
        if (vm.$options.el) {
            vm.$mount(vm.$options.el);
        }
    };
}
function initInternalComponent(vm, options) {
    const opts = (vm.$options = Object.create(vm.constructor.options));
    // doing this because it's faster than dynamic enumeration.
    const parentVnode = options._parentVnode;
    opts.parent = options.parent;
    opts._parentVnode = parentVnode;
    const vnodeComponentOptions = parentVnode.componentOptions;
    opts.propsData = vnodeComponentOptions.propsData;
    opts._parentListeners = vnodeComponentOptions.listeners;
    opts._renderChildren = vnodeComponentOptions.children;
    opts._componentTag = vnodeComponentOptions.tag;
    if (options.render) {
        opts.render = options.render;
        opts.staticRenderFns = options.staticRenderFns;
    }
}
function resolveConstructorOptions(Ctor) {
    let options = Ctor.options;
    if (Ctor.super) {
        const superOptions = resolveConstructorOptions(Ctor.super);
        const cachedSuperOptions = Ctor.superOptions;
        if (superOptions !== cachedSuperOptions) {
            // super option changed,
            // need to resolve new options.
            Ctor.superOptions = superOptions;
            // check if there are any late-modified/attached options (#4976)
            const modifiedOptions = resolveModifiedOptions(Ctor);
            // update base extend options
            if (modifiedOptions) {
                extend(Ctor.extendOptions, modifiedOptions);
            }
            options = Ctor.options = mergeOptions(superOptions, Ctor.extendOptions);
            if (options.name) {
                options.components[options.name] = Ctor;
            }
        }
    }
    return options;
}
function resolveModifiedOptions(Ctor) {
    let modified;
    const latest = Ctor.options;
    const sealed = Ctor.sealedOptions;
    for (const key in latest) {
        if (latest[key] !== sealed[key]) {
            if (!modified)
                modified = {};
            modified[key] = latest[key];
        }
    }
    return modified;
}

function FunctionalRenderContext(data, props, children, parent, Ctor) {
    const options = Ctor.options;
    // ensure the createElement function in functional components
    // gets a unique context - this is necessary for correct named slot check
    let contextVm;
    if (hasOwn(parent, '_uid')) {
        contextVm = Object.create(parent);
        contextVm._original = parent;
    }
    else {
        // the context vm passed in is a functional context as well.
        // in this case we want to make sure we are able to get a hold to the
        // real context instance.
        contextVm = parent;
        // @ts-ignore
        parent = parent._original;
    }
    const isCompiled = isTrue(options._compiled);
    const needNormalization = !isCompiled;
    this.data = data;
    this.props = props;
    this.children = children;
    this.parent = parent;
    this.listeners = data.on || emptyObject;
    this.injections = resolveInject(options.inject, parent);
    this.slots = () => {
        if (!this.$slots) {
            normalizeScopedSlots(parent, data.scopedSlots, (this.$slots = resolveSlots(children, parent)));
        }
        return this.$slots;
    };
    Object.defineProperty(this, 'scopedSlots', {
        enumerable: true,
        get() {
            return normalizeScopedSlots(parent, data.scopedSlots, this.slots());
        }
    });
    // support for compiled functional template
    if (isCompiled) {
        // exposing $options for renderStatic()
        this.$options = options;
        // pre-resolve slots for renderSlot()
        this.$slots = this.slots();
        this.$scopedSlots = normalizeScopedSlots(parent, data.scopedSlots, this.$slots);
    }
    if (options._scopeId) {
        this._c = (a, b, c, d) => {
            const vnode = createElement$1(contextVm, a, b, c, d, needNormalization);
            if (vnode && !isArray(vnode)) {
                vnode.fnScopeId = options._scopeId;
                vnode.fnContext = parent;
            }
            return vnode;
        };
    }
    else {
        this._c = (a, b, c, d) => createElement$1(contextVm, a, b, c, d, needNormalization);
    }
}
installRenderHelpers(FunctionalRenderContext.prototype);
function createFunctionalComponent(Ctor, propsData, data, contextVm, children) {
    const options = Ctor.options;
    const props = {};
    const propOptions = options.props;
    if (isDef(propOptions)) {
        for (const key in propOptions) {
            props[key] = validateProp(key, propOptions, propsData || emptyObject);
        }
    }
    else {
        if (isDef(data.attrs))
            mergeProps(props, data.attrs);
        if (isDef(data.props))
            mergeProps(props, data.props);
    }
    const renderContext = new FunctionalRenderContext(data, props, children, contextVm, Ctor);
    const vnode = options.render.call(null, renderContext._c, renderContext);
    if (vnode instanceof VNode) {
        return cloneAndMarkFunctionalResult(vnode, data, renderContext.parent, options, renderContext);
    }
    else if (isArray(vnode)) {
        const vnodes = normalizeChildren(vnode) || [];
        const res = new Array(vnodes.length);
        for (let i = 0; i < vnodes.length; i++) {
            res[i] = cloneAndMarkFunctionalResult(vnodes[i], data, renderContext.parent, options, renderContext);
        }
        return res;
    }
}
function cloneAndMarkFunctionalResult(vnode, data, contextVm, options, renderContext) {
    // #7817 clone node before setting fnContext, otherwise if the node is reused
    // (e.g. it was from a cached normal slot) the fnContext causes named slots
    // that should not be matched to match.
    const clone = cloneVNode(vnode);
    clone.fnContext = contextVm;
    clone.fnOptions = options;
    {
        (clone.devtoolsMeta = clone.devtoolsMeta || {}).renderContext =
            renderContext;
    }
    if (data.slot) {
        (clone.data || (clone.data = {})).slot = data.slot;
    }
    return clone;
}
function mergeProps(to, from) {
    for (const key in from) {
        to[camelize(key)] = from[key];
    }
}

function getComponentName(options) {
    return options.name || options.__name || options._componentTag;
}
// inline hooks to be invoked on component VNodes during patch
const componentVNodeHooks = {
    init(vnode, hydrating) {
        if (vnode.componentInstance &&
            !vnode.componentInstance._isDestroyed &&
            vnode.data.keepAlive) {
            // kept-alive components, treat as a patch
            const mountedNode = vnode; // work around flow
            componentVNodeHooks.prepatch(mountedNode, mountedNode);
        }
        else {
            const child = (vnode.componentInstance = createComponentInstanceForVnode(vnode, activeInstance));
            child.$mount(hydrating ? vnode.elm : undefined, hydrating);
        }
    },
    prepatch(oldVnode, vnode) {
        const options = vnode.componentOptions;
        const child = (vnode.componentInstance = oldVnode.componentInstance);
        updateChildComponent(child, options.propsData, // updated props
        options.listeners, // updated listeners
        vnode, // new parent vnode
        options.children // new children
        );
    },
    insert(vnode) {
        const { context, componentInstance } = vnode;
        if (!componentInstance._isMounted) {
            componentInstance._isMounted = true;
            callHook$1(componentInstance, 'mounted');
        }
        if (vnode.data.keepAlive) {
            if (context._isMounted) {
                // vue-router#1212
                // During updates, a kept-alive component's child components may
                // change, so directly walking the tree here may call activated hooks
                // on incorrect children. Instead we push them into a queue which will
                // be processed after the whole patch process ended.
                queueActivatedComponent(componentInstance);
            }
            else {
                activateChildComponent(componentInstance, true /* direct */);
            }
        }
    },
    destroy(vnode) {
        const { componentInstance } = vnode;
        if (!componentInstance._isDestroyed) {
            if (!vnode.data.keepAlive) {
                componentInstance.$destroy();
            }
            else {
                deactivateChildComponent(componentInstance, true /* direct */);
            }
        }
    }
};
const hooksToMerge = Object.keys(componentVNodeHooks);
function createComponent(Ctor, data, context, children, tag) {
    if (isUndef(Ctor)) {
        return;
    }
    const baseCtor = context.$options._base;
    // plain options object: turn it into a constructor
    if (isObject(Ctor)) {
        Ctor = baseCtor.extend(Ctor);
    }
    // if at this stage it's not a constructor or an async component factory,
    // reject.
    if (typeof Ctor !== 'function') {
        {
            warn$2(`Invalid Component definition: ${String(Ctor)}`, context);
        }
        return;
    }
    // async component
    let asyncFactory;
    // @ts-expect-error
    if (isUndef(Ctor.cid)) {
        asyncFactory = Ctor;
        Ctor = resolveAsyncComponent(asyncFactory, baseCtor);
        if (Ctor === undefined) {
            // return a placeholder node for async component, which is rendered
            // as a comment node but preserves all the raw information for the node.
            // the information will be used for async server-rendering and hydration.
            return createAsyncPlaceholder(asyncFactory, data, context, children, tag);
        }
    }
    data = data || {};
    // resolve constructor options in case global mixins are applied after
    // component constructor creation
    resolveConstructorOptions(Ctor);
    // transform component v-model data into props & events
    if (isDef(data.model)) {
        // @ts-expect-error
        transformModel(Ctor.options, data);
    }
    // extract props
    // @ts-expect-error
    const propsData = extractPropsFromVNodeData(data, Ctor, tag);
    // functional component
    // @ts-expect-error
    if (isTrue(Ctor.options.functional)) {
        return createFunctionalComponent(Ctor, propsData, data, context, children);
    }
    // extract listeners, since these needs to be treated as
    // child component listeners instead of DOM listeners
    const listeners = data.on;
    // replace with listeners with .native modifier
    // so it gets processed during parent component patch.
    data.on = data.nativeOn;
    // @ts-expect-error
    if (isTrue(Ctor.options.abstract)) {
        // abstract components do not keep anything
        // other than props & listeners & slot
        // work around flow
        const slot = data.slot;
        data = {};
        if (slot) {
            data.slot = slot;
        }
    }
    // install component management hooks onto the placeholder node
    installComponentHooks(data);
    // return a placeholder vnode
    // @ts-expect-error
    const name = getComponentName(Ctor.options) || tag;
    const vnode = new VNode(
    // @ts-expect-error
    `vue-component-${Ctor.cid}${name ? `-${name}` : ''}`, data, undefined, undefined, undefined, context, 
    // @ts-expect-error
    { Ctor, propsData, listeners, tag, children }, asyncFactory);
    return vnode;
}
function createComponentInstanceForVnode(
// we know it's MountedComponentVNode but flow doesn't
vnode, 
// activeInstance in lifecycle state
parent) {
    const options = {
        _isComponent: true,
        _parentVnode: vnode,
        parent
    };
    // check inline-template render functions
    const inlineTemplate = vnode.data.inlineTemplate;
    if (isDef(inlineTemplate)) {
        options.render = inlineTemplate.render;
        options.staticRenderFns = inlineTemplate.staticRenderFns;
    }
    return new vnode.componentOptions.Ctor(options);
}
function installComponentHooks(data) {
    const hooks = data.hook || (data.hook = {});
    for (let i = 0; i < hooksToMerge.length; i++) {
        const key = hooksToMerge[i];
        const existing = hooks[key];
        const toMerge = componentVNodeHooks[key];
        // @ts-expect-error
        if (existing !== toMerge && !(existing && existing._merged)) {
            hooks[key] = existing ? mergeHook(toMerge, existing) : toMerge;
        }
    }
}
function mergeHook(f1, f2) {
    const merged = (a, b) => {
        // flow complains about extra args which is why we use any
        f1(a, b);
        f2(a, b);
    };
    merged._merged = true;
    return merged;
}
// transform component v-model info (value and callback) into
// prop and event handler respectively.
function transformModel(options, data) {
    const prop = (options.model && options.model.prop) || 'value';
    const event = (options.model && options.model.event) || 'input';
    (data.attrs || (data.attrs = {}))[prop] = data.model.value;
    const on = data.on || (data.on = {});
    const existing = on[event];
    const callback = data.model.callback;
    if (isDef(existing)) {
        if (isArray(existing)
            ? existing.indexOf(callback) === -1
            : existing !== callback) {
            on[event] = [callback].concat(existing);
        }
    }
    else {
        on[event] = callback;
    }
}

let warn$2 = noop;
let tip = noop;
let generateComponentTrace; // work around flow check
let formatComponentName;
{
    const hasConsole = typeof console !== 'undefined';
    const classifyRE = /(?:^|[-_])(\w)/g;
    const classify = str => str.replace(classifyRE, c => c.toUpperCase()).replace(/[-_]/g, '');
    warn$2 = (msg, vm = currentInstance) => {
        const trace = vm ? generateComponentTrace(vm) : '';
        if (config.warnHandler) {
            config.warnHandler.call(null, msg, vm, trace);
        }
        else if (hasConsole && !config.silent) {
            console.error(`[Vue warn]: ${msg}${trace}`);
        }
    };
    tip = (msg, vm) => {
        if (hasConsole && !config.silent) {
            console.warn(`[Vue tip]: ${msg}` + (vm ? generateComponentTrace(vm) : ''));
        }
    };
    formatComponentName = (vm, includeFile) => {
        if (vm.$root === vm) {
            return '<Root>';
        }
        const options = isFunction(vm) && vm.cid != null
            ? vm.options
            : vm._isVue
                ? vm.$options || vm.constructor.options
                : vm;
        let name = getComponentName(options);
        const file = options.__file;
        if (!name && file) {
            const match = file.match(/([^/\\]+)\.vue$/);
            name = match && match[1];
        }
        return ((name ? `<${classify(name)}>` : `<Anonymous>`) +
            (file && includeFile !== false ? ` at ${file}` : ''));
    };
    const repeat = (str, n) => {
        let res = '';
        while (n) {
            if (n % 2 === 1)
                res += str;
            if (n > 1)
                str += str;
            n >>= 1;
        }
        return res;
    };
    generateComponentTrace = (vm) => {
        if (vm._isVue && vm.$parent) {
            const tree = [];
            let currentRecursiveSequence = 0;
            while (vm) {
                if (tree.length > 0) {
                    const last = tree[tree.length - 1];
                    if (last.constructor === vm.constructor) {
                        currentRecursiveSequence++;
                        vm = vm.$parent;
                        continue;
                    }
                    else if (currentRecursiveSequence > 0) {
                        tree[tree.length - 1] = [last, currentRecursiveSequence];
                        currentRecursiveSequence = 0;
                    }
                }
                tree.push(vm);
                vm = vm.$parent;
            }
            return ('\n\nfound in\n\n' +
                tree
                    .map((vm, i) => `${i === 0 ? '---> ' : repeat(' ', 5 + i * 2)}${isArray(vm)
                    ? `${formatComponentName(vm[0])}... (${vm[1]} recursive calls)`
                    : formatComponentName(vm)}`)
                    .join('\n'));
        }
        else {
            return `\n\n(found in ${formatComponentName(vm)})`;
        }
    };
}

/**
 * Option overwriting strategies are functions that handle
 * how to merge a parent option value and a child option
 * value into the final value.
 */
const strats = config.optionMergeStrategies;
/**
 * Options with restrictions
 */
{
    strats.el = strats.propsData = function (parent, child, vm, key) {
        if (!vm) {
            warn$2(`option "${key}" can only be used during instance ` +
                'creation with the `new` keyword.');
        }
        return defaultStrat(parent, child);
    };
}
/**
 * Helper that recursively merges two data objects together.
 */
function mergeData(to, from, recursive = true) {
    if (!from)
        return to;
    let key, toVal, fromVal;
    const keys = hasSymbol
        ? Reflect.ownKeys(from)
        : Object.keys(from);
    for (let i = 0; i < keys.length; i++) {
        key = keys[i];
        // in case the object is already observed...
        if (key === '__ob__')
            continue;
        toVal = to[key];
        fromVal = from[key];
        if (!recursive || !hasOwn(to, key)) {
            set(to, key, fromVal);
        }
        else if (toVal !== fromVal &&
            isPlainObject(toVal) &&
            isPlainObject(fromVal)) {
            mergeData(toVal, fromVal);
        }
    }
    return to;
}
/**
 * Data
 */
function mergeDataOrFn(parentVal, childVal, vm) {
    if (!vm) {
        // in a Vue.extend merge, both should be functions
        if (!childVal) {
            return parentVal;
        }
        if (!parentVal) {
            return childVal;
        }
        // when parentVal & childVal are both present,
        // we need to return a function that returns the
        // merged result of both functions... no need to
        // check if parentVal is a function here because
        // it has to be a function to pass previous merges.
        return function mergedDataFn() {
            return mergeData(isFunction(childVal) ? childVal.call(this, this) : childVal, isFunction(parentVal) ? parentVal.call(this, this) : parentVal);
        };
    }
    else {
        return function mergedInstanceDataFn() {
            // instance merge
            const instanceData = isFunction(childVal)
                ? childVal.call(vm, vm)
                : childVal;
            const defaultData = isFunction(parentVal)
                ? parentVal.call(vm, vm)
                : parentVal;
            if (instanceData) {
                return mergeData(instanceData, defaultData);
            }
            else {
                return defaultData;
            }
        };
    }
}
strats.data = function (parentVal, childVal, vm) {
    if (!vm) {
        if (childVal && typeof childVal !== 'function') {
            warn$2('The "data" option should be a function ' +
                    'that returns a per-instance value in component ' +
                    'definitions.', vm);
            return parentVal;
        }
        return mergeDataOrFn(parentVal, childVal);
    }
    return mergeDataOrFn(parentVal, childVal, vm);
};
/**
 * Hooks and props are merged as arrays.
 */
function mergeLifecycleHook(parentVal, childVal) {
    const res = childVal
        ? parentVal
            ? parentVal.concat(childVal)
            : isArray(childVal)
                ? childVal
                : [childVal]
        : parentVal;
    return res ? dedupeHooks(res) : res;
}
function dedupeHooks(hooks) {
    const res = [];
    for (let i = 0; i < hooks.length; i++) {
        if (res.indexOf(hooks[i]) === -1) {
            res.push(hooks[i]);
        }
    }
    return res;
}
LIFECYCLE_HOOKS.forEach(hook => {
    strats[hook] = mergeLifecycleHook;
});
/**
 * Assets
 *
 * When a vm is present (instance creation), we need to do
 * a three-way merge between constructor options, instance
 * options and parent options.
 */
function mergeAssets(parentVal, childVal, vm, key) {
    const res = Object.create(parentVal || null);
    if (childVal) {
        assertObjectType(key, childVal, vm);
        return extend(res, childVal);
    }
    else {
        return res;
    }
}
ASSET_TYPES.forEach(function (type) {
    strats[type + 's'] = mergeAssets;
});
/**
 * Watchers.
 *
 * Watchers hashes should not overwrite one
 * another, so we merge them as arrays.
 */
strats.watch = function (parentVal, childVal, vm, key) {
    // work around Firefox's Object.prototype.watch...
    //@ts-expect-error work around
    if (parentVal === nativeWatch)
        parentVal = undefined;
    //@ts-expect-error work around
    if (childVal === nativeWatch)
        childVal = undefined;
    /* istanbul ignore if */
    if (!childVal)
        return Object.create(parentVal || null);
    {
        assertObjectType(key, childVal, vm);
    }
    if (!parentVal)
        return childVal;
    const ret = {};
    extend(ret, parentVal);
    for (const key in childVal) {
        let parent = ret[key];
        const child = childVal[key];
        if (parent && !isArray(parent)) {
            parent = [parent];
        }
        ret[key] = parent ? parent.concat(child) : isArray(child) ? child : [child];
    }
    return ret;
};
/**
 * Other object hashes.
 */
strats.props =
    strats.methods =
        strats.inject =
            strats.computed =
                function (parentVal, childVal, vm, key) {
                    if (childVal && true) {
                        assertObjectType(key, childVal, vm);
                    }
                    if (!parentVal)
                        return childVal;
                    const ret = Object.create(null);
                    extend(ret, parentVal);
                    if (childVal)
                        extend(ret, childVal);
                    return ret;
                };
strats.provide = function (parentVal, childVal) {
    if (!parentVal)
        return childVal;
    return function () {
        const ret = Object.create(null);
        mergeData(ret, isFunction(parentVal) ? parentVal.call(this) : parentVal);
        if (childVal) {
            mergeData(ret, isFunction(childVal) ? childVal.call(this) : childVal, false // non-recursive
            );
        }
        return ret;
    };
};
/**
 * Default strategy.
 */
const defaultStrat = function (parentVal, childVal) {
    return childVal === undefined ? parentVal : childVal;
};
/**
 * Validate component names
 */
function checkComponents(options) {
    for (const key in options.components) {
        validateComponentName(key);
    }
}
function validateComponentName(name) {
    if (!new RegExp(`^[a-zA-Z][\\-\\.0-9_${unicodeRegExp.source}]*$`).test(name)) {
        warn$2('Invalid component name: "' +
            name +
            '". Component names ' +
            'should conform to valid custom element name in html5 specification.');
    }
    if (isBuiltInTag(name) || config.isReservedTag(name)) {
        warn$2('Do not use built-in or reserved HTML elements as component ' +
            'id: ' +
            name);
    }
}
/**
 * Ensure all props option syntax are normalized into the
 * Object-based format.
 */
function normalizeProps(options, vm) {
    const props = options.props;
    if (!props)
        return;
    const res = {};
    let i, val, name;
    if (isArray(props)) {
        i = props.length;
        while (i--) {
            val = props[i];
            if (typeof val === 'string') {
                name = camelize(val);
                res[name] = { type: null };
            }
            else {
                warn$2('props must be strings when using array syntax.');
            }
        }
    }
    else if (isPlainObject(props)) {
        for (const key in props) {
            val = props[key];
            name = camelize(key);
            res[name] = isPlainObject(val) ? val : { type: val };
        }
    }
    else {
        warn$2(`Invalid value for option "props": expected an Array or an Object, ` +
            `but got ${toRawType(props)}.`, vm);
    }
    options.props = res;
}
/**
 * Normalize all injections into Object-based format
 */
function normalizeInject(options, vm) {
    const inject = options.inject;
    if (!inject)
        return;
    const normalized = (options.inject = {});
    if (isArray(inject)) {
        for (let i = 0; i < inject.length; i++) {
            normalized[inject[i]] = { from: inject[i] };
        }
    }
    else if (isPlainObject(inject)) {
        for (const key in inject) {
            const val = inject[key];
            normalized[key] = isPlainObject(val)
                ? extend({ from: key }, val)
                : { from: val };
        }
    }
    else {
        warn$2(`Invalid value for option "inject": expected an Array or an Object, ` +
            `but got ${toRawType(inject)}.`, vm);
    }
}
/**
 * Normalize raw function directives into object format.
 */
function normalizeDirectives$1(options) {
    const dirs = options.directives;
    if (dirs) {
        for (const key in dirs) {
            const def = dirs[key];
            if (isFunction(def)) {
                dirs[key] = { bind: def, update: def };
            }
        }
    }
}
function assertObjectType(name, value, vm) {
    if (!isPlainObject(value)) {
        warn$2(`Invalid value for option "${name}": expected an Object, ` +
            `but got ${toRawType(value)}.`, vm);
    }
}
/**
 * Merge two option objects into a new one.
 * Core utility used in both instantiation and inheritance.
 */
function mergeOptions(parent, child, vm) {
    {
        checkComponents(child);
    }
    if (isFunction(child)) {
        // @ts-expect-error
        child = child.options;
    }
    normalizeProps(child, vm);
    normalizeInject(child, vm);
    normalizeDirectives$1(child);
    // Apply extends and mixins on the child options,
    // but only if it is a raw options object that isn't
    // the result of another mergeOptions call.
    // Only merged options has the _base property.
    if (!child._base) {
        if (child.extends) {
            parent = mergeOptions(parent, child.extends, vm);
        }
        if (child.mixins) {
            for (let i = 0, l = child.mixins.length; i < l; i++) {
                parent = mergeOptions(parent, child.mixins[i], vm);
            }
        }
    }
    const options = {};
    let key;
    for (key in parent) {
        mergeField(key);
    }
    for (key in child) {
        if (!hasOwn(parent, key)) {
            mergeField(key);
        }
    }
    function mergeField(key) {
        const strat = strats[key] || defaultStrat;
        options[key] = strat(parent[key], child[key], vm, key);
    }
    return options;
}
/**
 * Resolve an asset.
 * This function is used because child instances need access
 * to assets defined in its ancestor chain.
 */
function resolveAsset(options, type, id, warnMissing) {
    /* istanbul ignore if */
    if (typeof id !== 'string') {
        return;
    }
    const assets = options[type];
    // check local registration variations first
    if (hasOwn(assets, id))
        return assets[id];
    const camelizedId = camelize(id);
    if (hasOwn(assets, camelizedId))
        return assets[camelizedId];
    const PascalCaseId = capitalize(camelizedId);
    if (hasOwn(assets, PascalCaseId))
        return assets[PascalCaseId];
    // fallback to prototype chain
    const res = assets[id] || assets[camelizedId] || assets[PascalCaseId];
    if (warnMissing && !res) {
        warn$2('Failed to resolve ' + type.slice(0, -1) + ': ' + id);
    }
    return res;
}

function validateProp(key, propOptions, propsData, vm) {
    const prop = propOptions[key];
    const absent = !hasOwn(propsData, key);
    let value = propsData[key];
    // boolean casting
    const booleanIndex = getTypeIndex(Boolean, prop.type);
    if (booleanIndex > -1) {
        if (absent && !hasOwn(prop, 'default')) {
            value = false;
        }
        else if (value === '' || value === hyphenate(key)) {
            // only cast empty string / same name to boolean if
            // boolean has higher priority
            const stringIndex = getTypeIndex(String, prop.type);
            if (stringIndex < 0 || booleanIndex < stringIndex) {
                value = true;
            }
        }
    }
    // check default value
    if (value === undefined) {
        value = getPropDefaultValue(vm, prop, key);
        // since the default value is a fresh copy,
        // make sure to observe it.
        const prevShouldObserve = shouldObserve;
        toggleObserving(true);
        observe(value);
        toggleObserving(prevShouldObserve);
    }
    {
        assertProp(prop, key, value, vm, absent);
    }
    return value;
}
/**
 * Get the default value of a prop.
 */
function getPropDefaultValue(vm, prop, key) {
    // no default, return undefined
    if (!hasOwn(prop, 'default')) {
        return undefined;
    }
    const def = prop.default;
    // warn against non-factory defaults for Object & Array
    if (isObject(def)) {
        warn$2('Invalid default value for prop "' +
            key +
            '": ' +
            'Props with type Object/Array must use a factory function ' +
            'to return the default value.', vm);
    }
    // the raw prop value was also undefined from previous render,
    // return previous default value to avoid unnecessary watcher trigger
    if (vm &&
        vm.$options.propsData &&
        vm.$options.propsData[key] === undefined &&
        vm._props[key] !== undefined) {
        return vm._props[key];
    }
    // call factory function for non-Function types
    // a value is Function if its prototype is function even across different execution context
    return isFunction(def) && getType(prop.type) !== 'Function'
        ? def.call(vm)
        : def;
}
/**
 * Assert whether a prop is valid.
 */
function assertProp(prop, name, value, vm, absent) {
    if (prop.required && absent) {
        warn$2('Missing required prop: "' + name + '"', vm);
        return;
    }
    if (value == null && !prop.required) {
        return;
    }
    let type = prop.type;
    let valid = !type || type === true;
    const expectedTypes = [];
    if (type) {
        if (!isArray(type)) {
            type = [type];
        }
        for (let i = 0; i < type.length && !valid; i++) {
            const assertedType = assertType(value, type[i], vm);
            expectedTypes.push(assertedType.expectedType || '');
            valid = assertedType.valid;
        }
    }
    const haveExpectedTypes = expectedTypes.some(t => t);
    if (!valid && haveExpectedTypes) {
        warn$2(getInvalidTypeMessage(name, value, expectedTypes), vm);
        return;
    }
    const validator = prop.validator;
    if (validator) {
        if (!validator(value)) {
            warn$2('Invalid prop: custom validator check failed for prop "' + name + '".', vm);
        }
    }
}
const simpleCheckRE = /^(String|Number|Boolean|Function|Symbol|BigInt)$/;
function assertType(value, type, vm) {
    let valid;
    const expectedType = getType(type);
    if (simpleCheckRE.test(expectedType)) {
        const t = typeof value;
        valid = t === expectedType.toLowerCase();
        // for primitive wrapper objects
        if (!valid && t === 'object') {
            valid = value instanceof type;
        }
    }
    else if (expectedType === 'Object') {
        valid = isPlainObject(value);
    }
    else if (expectedType === 'Array') {
        valid = isArray(value);
    }
    else {
        try {
            valid = value instanceof type;
        }
        catch (e) {
            warn$2('Invalid prop type: "' + String(type) + '" is not a constructor', vm);
            valid = false;
        }
    }
    return {
        valid,
        expectedType
    };
}
const functionTypeCheckRE = /^\s*function (\w+)/;
/**
 * Use function string name to check built-in types,
 * because a simple equality check will fail when running
 * across different vms / iframes.
 */
function getType(fn) {
    const match = fn && fn.toString().match(functionTypeCheckRE);
    return match ? match[1] : '';
}
function isSameType(a, b) {
    return getType(a) === getType(b);
}
function getTypeIndex(type, expectedTypes) {
    if (!isArray(expectedTypes)) {
        return isSameType(expectedTypes, type) ? 0 : -1;
    }
    for (let i = 0, len = expectedTypes.length; i < len; i++) {
        if (isSameType(expectedTypes[i], type)) {
            return i;
        }
    }
    return -1;
}
function getInvalidTypeMessage(name, value, expectedTypes) {
    let message = `Invalid prop: type check failed for prop "${name}".` +
        ` Expected ${expectedTypes.map(capitalize).join(', ')}`;
    const expectedType = expectedTypes[0];
    const receivedType = toRawType(value);
    // check if we need to specify expected value
    if (expectedTypes.length === 1 &&
        isExplicable(expectedType) &&
        isExplicable(typeof value) &&
        !isBoolean(expectedType, receivedType)) {
        message += ` with value ${styleValue(value, expectedType)}`;
    }
    message += `, got ${receivedType} `;
    // check if we need to specify received value
    if (isExplicable(receivedType)) {
        message += `with value ${styleValue(value, receivedType)}.`;
    }
    return message;
}
function styleValue(value, type) {
    if (type === 'String') {
        return `"${value}"`;
    }
    else if (type === 'Number') {
        return `${Number(value)}`;
    }
    else {
        return `${value}`;
    }
}
const EXPLICABLE_TYPES = ['string', 'number', 'boolean'];
function isExplicable(value) {
    return EXPLICABLE_TYPES.some(elem => value.toLowerCase() === elem);
}
function isBoolean(...args) {
    return args.some(elem => elem.toLowerCase() === 'boolean');
}

function Vue(options) {
    if (!(this instanceof Vue)) {
        warn$2('Vue is a constructor and should be called with the `new` keyword');
    }
    this._init(options);
}
//@ts-expect-error Vue has function type
initMixin$1(Vue);
//@ts-expect-error Vue has function type
stateMixin(Vue);
//@ts-expect-error Vue has function type
eventsMixin(Vue);
//@ts-expect-error Vue has function type
lifecycleMixin(Vue);
//@ts-expect-error Vue has function type
renderMixin(Vue);

function initUse(Vue) {
    Vue.use = function (plugin) {
        const installedPlugins = this._installedPlugins || (this._installedPlugins = []);
        if (installedPlugins.indexOf(plugin) > -1) {
            return this;
        }
        // additional parameters
        const args = toArray(arguments, 1);
        args.unshift(this);
        if (isFunction(plugin.install)) {
            plugin.install.apply(plugin, args);
        }
        else if (isFunction(plugin)) {
            plugin.apply(null, args);
        }
        installedPlugins.push(plugin);
        return this;
    };
}

function initMixin(Vue) {
    Vue.mixin = function (mixin) {
        this.options = mergeOptions(this.options, mixin);
        return this;
    };
}

function initExtend(Vue) {
    /**
     * Each instance constructor, including Vue, has a unique
     * cid. This enables us to create wrapped "child
     * constructors" for prototypal inheritance and cache them.
     */
    Vue.cid = 0;
    let cid = 1;
    /**
     * Class inheritance
     */
    Vue.extend = function (extendOptions) {
        extendOptions = extendOptions || {};
        const Super = this;
        const SuperId = Super.cid;
        const cachedCtors = extendOptions._Ctor || (extendOptions._Ctor = {});
        if (cachedCtors[SuperId]) {
            return cachedCtors[SuperId];
        }
        const name = getComponentName(extendOptions) || getComponentName(Super.options);
        if (name) {
            validateComponentName(name);
        }
        const Sub = function VueComponent(options) {
            this._init(options);
        };
        Sub.prototype = Object.create(Super.prototype);
        Sub.prototype.constructor = Sub;
        Sub.cid = cid++;
        Sub.options = mergeOptions(Super.options, extendOptions);
        Sub['super'] = Super;
        // For props and computed properties, we define the proxy getters on
        // the Vue instances at extension time, on the extended prototype. This
        // avoids Object.defineProperty calls for each instance created.
        if (Sub.options.props) {
            initProps(Sub);
        }
        if (Sub.options.computed) {
            initComputed(Sub);
        }
        // allow further extension/mixin/plugin usage
        Sub.extend = Super.extend;
        Sub.mixin = Super.mixin;
        Sub.use = Super.use;
        // create asset registers, so extended classes
        // can have their private assets too.
        ASSET_TYPES.forEach(function (type) {
            Sub[type] = Super[type];
        });
        // enable recursive self-lookup
        if (name) {
            Sub.options.components[name] = Sub;
        }
        // keep a reference to the super options at extension time.
        // later at instantiation we can check if Super's options have
        // been updated.
        Sub.superOptions = Super.options;
        Sub.extendOptions = extendOptions;
        Sub.sealedOptions = extend({}, Sub.options);
        // cache constructor
        cachedCtors[SuperId] = Sub;
        return Sub;
    };
}
function initProps(Comp) {
    const props = Comp.options.props;
    for (const key in props) {
        proxy(Comp.prototype, `_props`, key);
    }
}
function initComputed(Comp) {
    const computed = Comp.options.computed;
    for (const key in computed) {
        defineComputed(Comp.prototype, key, computed[key]);
    }
}

function initAssetRegisters(Vue) {
    /**
     * Create asset registration methods.
     */
    ASSET_TYPES.forEach(type => {
        // @ts-expect-error function is not exact same type
        Vue[type] = function (id, definition) {
            if (!definition) {
                return this.options[type + 's'][id];
            }
            else {
                /* istanbul ignore if */
                if (type === 'component') {
                    validateComponentName(id);
                }
                if (type === 'component' && isPlainObject(definition)) {
                    // @ts-expect-error
                    definition.name = definition.name || id;
                    definition = this.options._base.extend(definition);
                }
                if (type === 'directive' && isFunction(definition)) {
                    definition = { bind: definition, update: definition };
                }
                this.options[type + 's'][id] = definition;
                return definition;
            }
        };
    });
}

function _getComponentName(opts) {
    return opts && (getComponentName(opts.Ctor.options) || opts.tag);
}
function matches(pattern, name) {
    if (isArray(pattern)) {
        return pattern.indexOf(name) > -1;
    }
    else if (typeof pattern === 'string') {
        return pattern.split(',').indexOf(name) > -1;
    }
    else if (isRegExp(pattern)) {
        return pattern.test(name);
    }
    /* istanbul ignore next */
    return false;
}
function pruneCache(keepAliveInstance, filter) {
    const { cache, keys, _vnode } = keepAliveInstance;
    for (const key in cache) {
        const entry = cache[key];
        if (entry) {
            const name = entry.name;
            if (name && !filter(name)) {
                pruneCacheEntry(cache, key, keys, _vnode);
            }
        }
    }
}
function pruneCacheEntry(cache, key, keys, current) {
    const entry = cache[key];
    if (entry && (!current || entry.tag !== current.tag)) {
        // @ts-expect-error can be undefined
        entry.componentInstance.$destroy();
    }
    cache[key] = null;
    remove$2(keys, key);
}
const patternTypes = [String, RegExp, Array];
// TODO defineComponent
var KeepAlive = {
    name: 'keep-alive',
    abstract: true,
    props: {
        include: patternTypes,
        exclude: patternTypes,
        max: [String, Number]
    },
    methods: {
        cacheVNode() {
            const { cache, keys, vnodeToCache, keyToCache } = this;
            if (vnodeToCache) {
                const { tag, componentInstance, componentOptions } = vnodeToCache;
                cache[keyToCache] = {
                    name: _getComponentName(componentOptions),
                    tag,
                    componentInstance
                };
                keys.push(keyToCache);
                // prune oldest entry
                if (this.max && keys.length > parseInt(this.max)) {
                    pruneCacheEntry(cache, keys[0], keys, this._vnode);
                }
                this.vnodeToCache = null;
            }
        }
    },
    created() {
        this.cache = Object.create(null);
        this.keys = [];
    },
    destroyed() {
        for (const key in this.cache) {
            pruneCacheEntry(this.cache, key, this.keys);
        }
    },
    mounted() {
        this.cacheVNode();
        this.$watch('include', val => {
            pruneCache(this, name => matches(val, name));
        });
        this.$watch('exclude', val => {
            pruneCache(this, name => !matches(val, name));
        });
    },
    updated() {
        this.cacheVNode();
    },
    render() {
        const slot = this.$slots.default;
        const vnode = getFirstComponentChild(slot);
        const componentOptions = vnode && vnode.componentOptions;
        if (componentOptions) {
            // check pattern
            const name = _getComponentName(componentOptions);
            const { include, exclude } = this;
            if (
            // not included
            (include && (!name || !matches(include, name))) ||
                // excluded
                (exclude && name && matches(exclude, name))) {
                return vnode;
            }
            const { cache, keys } = this;
            const key = vnode.key == null
                ? // same constructor may get registered as different local components
                    // so cid alone is not enough (#3269)
                    componentOptions.Ctor.cid +
                        (componentOptions.tag ? `::${componentOptions.tag}` : '')
                : vnode.key;
            if (cache[key]) {
                vnode.componentInstance = cache[key].componentInstance;
                // make current key freshest
                remove$2(keys, key);
                keys.push(key);
            }
            else {
                // delay setting the cache until update
                this.vnodeToCache = vnode;
                this.keyToCache = key;
            }
            // @ts-expect-error can vnode.data can be undefined
            vnode.data.keepAlive = true;
        }
        return vnode || (slot && slot[0]);
    }
};

var builtInComponents = {
    KeepAlive
};

function initGlobalAPI(Vue) {
    // config
    const configDef = {};
    configDef.get = () => config;
    {
        configDef.set = () => {
            warn$2('Do not replace the Vue.config object, set individual fields instead.');
        };
    }
    Object.defineProperty(Vue, 'config', configDef);
    // exposed util methods.
    // NOTE: these are not considered part of the public API - avoid relying on
    // them unless you are aware of the risk.
    Vue.util = {
        warn: warn$2,
        extend,
        mergeOptions,
        defineReactive
    };
    Vue.set = set;
    Vue.delete = del;
    Vue.nextTick = nextTick;
    // 2.6 explicit observable API
    Vue.observable = (obj) => {
        observe(obj);
        return obj;
    };
    Vue.options = Object.create(null);
    ASSET_TYPES.forEach(type => {
        Vue.options[type + 's'] = Object.create(null);
    });
    // this is used to identify the "base" constructor to extend all plain-object
    // components with in Weex's multi-instance scenarios.
    Vue.options._base = Vue;
    extend(Vue.options.components, builtInComponents);
    initUse(Vue);
    initMixin(Vue);
    initExtend(Vue);
    initAssetRegisters(Vue);
}

initGlobalAPI(Vue);
Object.defineProperty(Vue.prototype, '$isServer', {
    get: isServerRendering
});
Object.defineProperty(Vue.prototype, '$ssrContext', {
    get() {
        /* istanbul ignore next */
        return this.$vnode && this.$vnode.ssrContext;
    }
});
// expose FunctionalRenderContext for ssr runtime helper installation
Object.defineProperty(Vue, 'FunctionalRenderContext', {
    value: FunctionalRenderContext
});
Vue.version = version;

// these are reserved for web because they are directly compiled away
// during template compilation
const isReservedAttr = makeMap('style,class');
// attributes that should be using props for binding
const acceptValue = makeMap('input,textarea,option,select,progress');
const mustUseProp = (tag, type, attr) => {
    return ((attr === 'value' && acceptValue(tag) && type !== 'button') ||
        (attr === 'selected' && tag === 'option') ||
        (attr === 'checked' && tag === 'input') ||
        (attr === 'muted' && tag === 'video'));
};
const isEnumeratedAttr = makeMap('contenteditable,draggable,spellcheck');
const isValidContentEditableValue = makeMap('events,caret,typing,plaintext-only');
const convertEnumeratedValue = (key, value) => {
    return isFalsyAttrValue(value) || value === 'false'
        ? 'false'
        : // allow arbitrary string value for contenteditable
            key === 'contenteditable' && isValidContentEditableValue(value)
                ? value
                : 'true';
};
const isBooleanAttr = makeMap('allowfullscreen,async,autofocus,autoplay,checked,compact,controls,declare,' +
    'default,defaultchecked,defaultmuted,defaultselected,defer,disabled,' +
    'enabled,formnovalidate,hidden,indeterminate,inert,ismap,itemscope,loop,multiple,' +
    'muted,nohref,noresize,noshade,novalidate,nowrap,open,pauseonexit,readonly,' +
    'required,reversed,scoped,seamless,selected,sortable,' +
    'truespeed,typemustmatch,visible');
const xlinkNS = 'http://www.w3.org/1999/xlink';
const isXlink = (name) => {
    return name.charAt(5) === ':' && name.slice(0, 5) === 'xlink';
};
const getXlinkProp = (name) => {
    return isXlink(name) ? name.slice(6, name.length) : '';
};
const isFalsyAttrValue = (val) => {
    return val == null || val === false;
};

function genClassForVnode(vnode) {
    let data = vnode.data;
    let parentNode = vnode;
    let childNode = vnode;
    while (isDef(childNode.componentInstance)) {
        childNode = childNode.componentInstance._vnode;
        if (childNode && childNode.data) {
            data = mergeClassData(childNode.data, data);
        }
    }
    // @ts-expect-error parentNode.parent not VNodeWithData
    while (isDef((parentNode = parentNode.parent))) {
        if (parentNode && parentNode.data) {
            data = mergeClassData(data, parentNode.data);
        }
    }
    return renderClass(data.staticClass, data.class);
}
function mergeClassData(child, parent) {
    return {
        staticClass: concat(child.staticClass, parent.staticClass),
        class: isDef(child.class) ? [child.class, parent.class] : parent.class
    };
}
function renderClass(staticClass, dynamicClass) {
    if (isDef(staticClass) || isDef(dynamicClass)) {
        return concat(staticClass, stringifyClass(dynamicClass));
    }
    /* istanbul ignore next */
    return '';
}
function concat(a, b) {
    return a ? (b ? a + ' ' + b : a) : b || '';
}
function stringifyClass(value) {
    if (Array.isArray(value)) {
        return stringifyArray(value);
    }
    if (isObject(value)) {
        return stringifyObject(value);
    }
    if (typeof value === 'string') {
        return value;
    }
    /* istanbul ignore next */
    return '';
}
function stringifyArray(value) {
    let res = '';
    let stringified;
    for (let i = 0, l = value.length; i < l; i++) {
        if (isDef((stringified = stringifyClass(value[i]))) && stringified !== '') {
            if (res)
                res += ' ';
            res += stringified;
        }
    }
    return res;
}
function stringifyObject(value) {
    let res = '';
    for (const key in value) {
        if (value[key]) {
            if (res)
                res += ' ';
            res += key;
        }
    }
    return res;
}

const namespaceMap = {
    svg: 'http://www.w3.org/2000/svg',
    math: 'http://www.w3.org/1998/Math/MathML'
};
const isHTMLTag = makeMap('html,body,base,head,link,meta,style,title,' +
    'address,article,aside,footer,header,h1,h2,h3,h4,h5,h6,hgroup,nav,section,' +
    'div,dd,dl,dt,figcaption,figure,picture,hr,img,li,main,ol,p,pre,ul,' +
    'a,b,abbr,bdi,bdo,br,cite,code,data,dfn,em,i,kbd,mark,q,rp,rt,rtc,ruby,' +
    's,samp,small,span,strong,sub,sup,time,u,var,wbr,area,audio,map,track,video,' +
    'embed,object,param,source,canvas,script,noscript,del,ins,' +
    'caption,col,colgroup,table,thead,tbody,td,th,tr,' +
    'button,datalist,fieldset,form,input,label,legend,meter,optgroup,option,' +
    'output,progress,select,textarea,' +
    'details,dialog,menu,menuitem,summary,' +
    'content,element,shadow,template,blockquote,iframe,tfoot');
// this map is intentionally selective, only covering SVG elements that may
// contain child elements.
const isSVG = makeMap('svg,animate,circle,clippath,cursor,defs,desc,ellipse,filter,font-face,' +
    'foreignobject,g,glyph,image,line,marker,mask,missing-glyph,path,pattern,' +
    'polygon,polyline,rect,switch,symbol,text,textpath,tspan,use,view', true);
const isPreTag = (tag) => tag === 'pre';
const isReservedTag = (tag) => {
    return isHTMLTag(tag) || isSVG(tag);
};
function getTagNamespace(tag) {
    if (isSVG(tag)) {
        return 'svg';
    }
    // basic support for MathML
    // note it doesn't support other MathML elements being component roots
    if (tag === 'math') {
        return 'math';
    }
}
const unknownElementCache = Object.create(null);
function isUnknownElement(tag) {
    /* istanbul ignore if */
    if (!inBrowser) {
        return true;
    }
    if (isReservedTag(tag)) {
        return false;
    }
    tag = tag.toLowerCase();
    /* istanbul ignore if */
    if (unknownElementCache[tag] != null) {
        return unknownElementCache[tag];
    }
    const el = document.createElement(tag);
    if (tag.indexOf('-') > -1) {
        // http://stackoverflow.com/a/28210364/1070244
        return (unknownElementCache[tag] =
            el.constructor === window.HTMLUnknownElement ||
                el.constructor === window.HTMLElement);
    }
    else {
        return (unknownElementCache[tag] = /HTMLUnknownElement/.test(el.toString()));
    }
}
const isTextInputType = makeMap('text,number,password,search,email,tel,url');

/**
 * Query an element selector if it's not an element already.
 */
function query(el) {
    if (typeof el === 'string') {
        const selected = document.querySelector(el);
        if (!selected) {
            warn$2('Cannot find element: ' + el);
            return document.createElement('div');
        }
        return selected;
    }
    else {
        return el;
    }
}

function createElement(tagName, vnode) {
    const elm = document.createElement(tagName);
    if (tagName !== 'select') {
        return elm;
    }
    // false or null will remove the attribute but undefined will not
    if (vnode.data &&
        vnode.data.attrs &&
        vnode.data.attrs.multiple !== undefined) {
        elm.setAttribute('multiple', 'multiple');
    }
    return elm;
}
function createElementNS(namespace, tagName) {
    return document.createElementNS(namespaceMap[namespace], tagName);
}
function createTextNode(text) {
    return document.createTextNode(text);
}
function createComment(text) {
    return document.createComment(text);
}
function insertBefore(parentNode, newNode, referenceNode) {
    parentNode.insertBefore(newNode, referenceNode);
}
function removeChild(node, child) {
    node.removeChild(child);
}
function appendChild(node, child) {
    node.appendChild(child);
}
function parentNode(node) {
    return node.parentNode;
}
function nextSibling(node) {
    return node.nextSibling;
}
function tagName(node) {
    return node.tagName;
}
function setTextContent(node, text) {
    node.textContent = text;
}
function setStyleScope(node, scopeId) {
    node.setAttribute(scopeId, '');
}

var nodeOps = /*#__PURE__*/Object.freeze({
  __proto__: null,
  createElement: createElement,
  createElementNS: createElementNS,
  createTextNode: createTextNode,
  createComment: createComment,
  insertBefore: insertBefore,
  removeChild: removeChild,
  appendChild: appendChild,
  parentNode: parentNode,
  nextSibling: nextSibling,
  tagName: tagName,
  setTextContent: setTextContent,
  setStyleScope: setStyleScope
});

var ref = {
    create(_, vnode) {
        registerRef(vnode);
    },
    update(oldVnode, vnode) {
        if (oldVnode.data.ref !== vnode.data.ref) {
            registerRef(oldVnode, true);
            registerRef(vnode);
        }
    },
    destroy(vnode) {
        registerRef(vnode, true);
    }
};
function registerRef(vnode, isRemoval) {
    const ref = vnode.data.ref;
    if (!isDef(ref))
        return;
    const vm = vnode.context;
    const refValue = vnode.componentInstance || vnode.elm;
    const value = isRemoval ? null : refValue;
    const $refsValue = isRemoval ? undefined : refValue;
    if (isFunction(ref)) {
        invokeWithErrorHandling(ref, vm, [value], vm, `template ref function`);
        return;
    }
    const isFor = vnode.data.refInFor;
    const _isString = typeof ref === 'string' || typeof ref === 'number';
    const _isRef = isRef(ref);
    const refs = vm.$refs;
    if (_isString || _isRef) {
        if (isFor) {
            const existing = _isString ? refs[ref] : ref.value;
            if (isRemoval) {
                isArray(existing) && remove$2(existing, refValue);
            }
            else {
                if (!isArray(existing)) {
                    if (_isString) {
                        refs[ref] = [refValue];
                        setSetupRef(vm, ref, refs[ref]);
                    }
                    else {
                        ref.value = [refValue];
                    }
                }
                else if (!existing.includes(refValue)) {
                    existing.push(refValue);
                }
            }
        }
        else if (_isString) {
            if (isRemoval && refs[ref] !== refValue) {
                return;
            }
            refs[ref] = $refsValue;
            setSetupRef(vm, ref, value);
        }
        else if (_isRef) {
            if (isRemoval && ref.value !== refValue) {
                return;
            }
            ref.value = value;
        }
        else {
            warn$2(`Invalid template ref type: ${typeof ref}`);
        }
    }
}
function setSetupRef({ _setupState }, key, val) {
    if (_setupState && hasOwn(_setupState, key)) {
        if (isRef(_setupState[key])) {
            _setupState[key].value = val;
        }
        else {
            _setupState[key] = val;
        }
    }
}

/**
 * Virtual DOM patching algorithm based on Snabbdom by
 * Simon Friis Vindum (@paldepind)
 * Licensed under the MIT License
 * https://github.com/paldepind/snabbdom/blob/master/LICENSE
 *
 * modified by Evan You (@yyx990803)
 *
 * Not type-checking this because this file is perf-critical and the cost
 * of making flow understand it is not worth it.
 */
const emptyNode = new VNode('', {}, []);
const hooks = ['create', 'activate', 'update', 'remove', 'destroy'];
function sameVnode(a, b) {
    return (a.key === b.key &&
        a.asyncFactory === b.asyncFactory &&
        ((a.tag === b.tag &&
            a.isComment === b.isComment &&
            isDef(a.data) === isDef(b.data) &&
            sameInputType(a, b)) ||
            (isTrue(a.isAsyncPlaceholder) && isUndef(b.asyncFactory.error))));
}
function sameInputType(a, b) {
    if (a.tag !== 'input')
        return true;
    let i;
    const typeA = isDef((i = a.data)) && isDef((i = i.attrs)) && i.type;
    const typeB = isDef((i = b.data)) && isDef((i = i.attrs)) && i.type;
    return typeA === typeB || (isTextInputType(typeA) && isTextInputType(typeB));
}
function createKeyToOldIdx(children, beginIdx, endIdx) {
    let i, key;
    const map = {};
    for (i = beginIdx; i <= endIdx; ++i) {
        key = children[i].key;
        if (isDef(key))
            map[key] = i;
    }
    return map;
}
function createPatchFunction(backend) {
    let i, j;
    const cbs = {};
    const { modules, nodeOps } = backend;
    for (i = 0; i < hooks.length; ++i) {
        cbs[hooks[i]] = [];
        for (j = 0; j < modules.length; ++j) {
            if (isDef(modules[j][hooks[i]])) {
                cbs[hooks[i]].push(modules[j][hooks[i]]);
            }
        }
    }
    function emptyNodeAt(elm) {
        return new VNode(nodeOps.tagName(elm).toLowerCase(), {}, [], undefined, elm);
    }
    function createRmCb(childElm, listeners) {
        function remove() {
            if (--remove.listeners === 0) {
                removeNode(childElm);
            }
        }
        remove.listeners = listeners;
        return remove;
    }
    function removeNode(el) {
        const parent = nodeOps.parentNode(el);
        // element may have already been removed due to v-html / v-text
        if (isDef(parent)) {
            nodeOps.removeChild(parent, el);
        }
    }
    function isUnknownElement(vnode, inVPre) {
        return (!inVPre &&
            !vnode.ns &&
            !(config.ignoredElements.length &&
                config.ignoredElements.some(ignore => {
                    return isRegExp(ignore)
                        ? ignore.test(vnode.tag)
                        : ignore === vnode.tag;
                })) &&
            config.isUnknownElement(vnode.tag));
    }
    let creatingElmInVPre = 0;
    function createElm(vnode, insertedVnodeQueue, parentElm, refElm, nested, ownerArray, index) {
        if (isDef(vnode.elm) && isDef(ownerArray)) {
            // This vnode was used in a previous render!
            // now it's used as a new node, overwriting its elm would cause
            // potential patch errors down the road when it's used as an insertion
            // reference node. Instead, we clone the node on-demand before creating
            // associated DOM element for it.
            vnode = ownerArray[index] = cloneVNode(vnode);
        }
        vnode.isRootInsert = !nested; // for transition enter check
        if (createComponent(vnode, insertedVnodeQueue, parentElm, refElm)) {
            return;
        }
        const data = vnode.data;
        const children = vnode.children;
        const tag = vnode.tag;
        if (isDef(tag)) {
            {
                if (data && data.pre) {
                    creatingElmInVPre++;
                }
                if (isUnknownElement(vnode, creatingElmInVPre)) {
                    warn$2('Unknown custom element: <' +
                        tag +
                        '> - did you ' +
                        'register the component correctly? For recursive components, ' +
                        'make sure to provide the "name" option.', vnode.context);
                }
            }
            vnode.elm = vnode.ns
                ? nodeOps.createElementNS(vnode.ns, tag)
                : nodeOps.createElement(tag, vnode);
            setScope(vnode);
            createChildren(vnode, children, insertedVnodeQueue);
            if (isDef(data)) {
                invokeCreateHooks(vnode, insertedVnodeQueue);
            }
            insert(parentElm, vnode.elm, refElm);
            if (data && data.pre) {
                creatingElmInVPre--;
            }
        }
        else if (isTrue(vnode.isComment)) {
            vnode.elm = nodeOps.createComment(vnode.text);
            insert(parentElm, vnode.elm, refElm);
        }
        else {
            vnode.elm = nodeOps.createTextNode(vnode.text);
            insert(parentElm, vnode.elm, refElm);
        }
    }
    function createComponent(vnode, insertedVnodeQueue, parentElm, refElm) {
        let i = vnode.data;
        if (isDef(i)) {
            const isReactivated = isDef(vnode.componentInstance) && i.keepAlive;
            if (isDef((i = i.hook)) && isDef((i = i.init))) {
                i(vnode, false /* hydrating */);
            }
            // after calling the init hook, if the vnode is a child component
            // it should've created a child instance and mounted it. the child
            // component also has set the placeholder vnode's elm.
            // in that case we can just return the element and be done.
            if (isDef(vnode.componentInstance)) {
                initComponent(vnode, insertedVnodeQueue);
                insert(parentElm, vnode.elm, refElm);
                if (isTrue(isReactivated)) {
                    reactivateComponent(vnode, insertedVnodeQueue, parentElm, refElm);
                }
                return true;
            }
        }
    }
    function initComponent(vnode, insertedVnodeQueue) {
        if (isDef(vnode.data.pendingInsert)) {
            insertedVnodeQueue.push.apply(insertedVnodeQueue, vnode.data.pendingInsert);
            vnode.data.pendingInsert = null;
        }
        vnode.elm = vnode.componentInstance.$el;
        if (isPatchable(vnode)) {
            invokeCreateHooks(vnode, insertedVnodeQueue);
            setScope(vnode);
        }
        else {
            // empty component root.
            // skip all element-related modules except for ref (#3455)
            registerRef(vnode);
            // make sure to invoke the insert hook
            insertedVnodeQueue.push(vnode);
        }
    }
    function reactivateComponent(vnode, insertedVnodeQueue, parentElm, refElm) {
        let i;
        // hack for #4339: a reactivated component with inner transition
        // does not trigger because the inner node's created hooks are not called
        // again. It's not ideal to involve module-specific logic in here but
        // there doesn't seem to be a better way to do it.
        let innerNode = vnode;
        while (innerNode.componentInstance) {
            innerNode = innerNode.componentInstance._vnode;
            if (isDef((i = innerNode.data)) && isDef((i = i.transition))) {
                for (i = 0; i < cbs.activate.length; ++i) {
                    cbs.activate[i](emptyNode, innerNode);
                }
                insertedVnodeQueue.push(innerNode);
                break;
            }
        }
        // unlike a newly created component,
        // a reactivated keep-alive component doesn't insert itself
        insert(parentElm, vnode.elm, refElm);
    }
    function insert(parent, elm, ref) {
        if (isDef(parent)) {
            if (isDef(ref)) {
                if (nodeOps.parentNode(ref) === parent) {
                    nodeOps.insertBefore(parent, elm, ref);
                }
            }
            else {
                nodeOps.appendChild(parent, elm);
            }
        }
    }
    function createChildren(vnode, children, insertedVnodeQueue) {
        if (isArray(children)) {
            {
                checkDuplicateKeys(children);
            }
            for (let i = 0; i < children.length; ++i) {
                createElm(children[i], insertedVnodeQueue, vnode.elm, null, true, children, i);
            }
        }
        else if (isPrimitive(vnode.text)) {
            nodeOps.appendChild(vnode.elm, nodeOps.createTextNode(String(vnode.text)));
        }
    }
    function isPatchable(vnode) {
        while (vnode.componentInstance) {
            vnode = vnode.componentInstance._vnode;
        }
        return isDef(vnode.tag);
    }
    function invokeCreateHooks(vnode, insertedVnodeQueue) {
        for (let i = 0; i < cbs.create.length; ++i) {
            cbs.create[i](emptyNode, vnode);
        }
        i = vnode.data.hook; // Reuse variable
        if (isDef(i)) {
            if (isDef(i.create))
                i.create(emptyNode, vnode);
            if (isDef(i.insert))
                insertedVnodeQueue.push(vnode);
        }
    }
    // set scope id attribute for scoped CSS.
    // this is implemented as a special case to avoid the overhead
    // of going through the normal attribute patching process.
    function setScope(vnode) {
        let i;
        if (isDef((i = vnode.fnScopeId))) {
            nodeOps.setStyleScope(vnode.elm, i);
        }
        else {
            let ancestor = vnode;
            while (ancestor) {
                if (isDef((i = ancestor.context)) && isDef((i = i.$options._scopeId))) {
                    nodeOps.setStyleScope(vnode.elm, i);
                }
                ancestor = ancestor.parent;
            }
        }
        // for slot content they should also get the scopeId from the host instance.
        if (isDef((i = activeInstance)) &&
            i !== vnode.context &&
            i !== vnode.fnContext &&
            isDef((i = i.$options._scopeId))) {
            nodeOps.setStyleScope(vnode.elm, i);
        }
    }
    function addVnodes(parentElm, refElm, vnodes, startIdx, endIdx, insertedVnodeQueue) {
        for (; startIdx <= endIdx; ++startIdx) {
            createElm(vnodes[startIdx], insertedVnodeQueue, parentElm, refElm, false, vnodes, startIdx);
        }
    }
    function invokeDestroyHook(vnode) {
        let i, j;
        const data = vnode.data;
        if (isDef(data)) {
            if (isDef((i = data.hook)) && isDef((i = i.destroy)))
                i(vnode);
            for (i = 0; i < cbs.destroy.length; ++i)
                cbs.destroy[i](vnode);
        }
        if (isDef((i = vnode.children))) {
            for (j = 0; j < vnode.children.length; ++j) {
                invokeDestroyHook(vnode.children[j]);
            }
        }
    }
    function removeVnodes(vnodes, startIdx, endIdx) {
        for (; startIdx <= endIdx; ++startIdx) {
            const ch = vnodes[startIdx];
            if (isDef(ch)) {
                if (isDef(ch.tag)) {
                    removeAndInvokeRemoveHook(ch);
                    invokeDestroyHook(ch);
                }
                else {
                    // Text node
                    removeNode(ch.elm);
                }
            }
        }
    }
    function removeAndInvokeRemoveHook(vnode, rm) {
        if (isDef(rm) || isDef(vnode.data)) {
            let i;
            const listeners = cbs.remove.length + 1;
            if (isDef(rm)) {
                // we have a recursively passed down rm callback
                // increase the listeners count
                rm.listeners += listeners;
            }
            else {
                // directly removing
                rm = createRmCb(vnode.elm, listeners);
            }
            // recursively invoke hooks on child component root node
            if (isDef((i = vnode.componentInstance)) &&
                isDef((i = i._vnode)) &&
                isDef(i.data)) {
                removeAndInvokeRemoveHook(i, rm);
            }
            for (i = 0; i < cbs.remove.length; ++i) {
                cbs.remove[i](vnode, rm);
            }
            if (isDef((i = vnode.data.hook)) && isDef((i = i.remove))) {
                i(vnode, rm);
            }
            else {
                rm();
            }
        }
        else {
            removeNode(vnode.elm);
        }
    }
    function updateChildren(parentElm, oldCh, newCh, insertedVnodeQueue, removeOnly) {
        let oldStartIdx = 0;
        let newStartIdx = 0;
        let oldEndIdx = oldCh.length - 1;
        let oldStartVnode = oldCh[0];
        let oldEndVnode = oldCh[oldEndIdx];
        let newEndIdx = newCh.length - 1;
        let newStartVnode = newCh[0];
        let newEndVnode = newCh[newEndIdx];
        let oldKeyToIdx, idxInOld, vnodeToMove, refElm;
        // removeOnly is a special flag used only by <transition-group>
        // to ensure removed elements stay in correct relative positions
        // during leaving transitions
        const canMove = !removeOnly;
        {
            checkDuplicateKeys(newCh);
        }
        while (oldStartIdx <= oldEndIdx && newStartIdx <= newEndIdx) {
            if (isUndef(oldStartVnode)) {
                oldStartVnode = oldCh[++oldStartIdx]; // Vnode has been moved left
            }
            else if (isUndef(oldEndVnode)) {
                oldEndVnode = oldCh[--oldEndIdx];
            }
            else if (sameVnode(oldStartVnode, newStartVnode)) {
                patchVnode(oldStartVnode, newStartVnode, insertedVnodeQueue, newCh, newStartIdx);
                oldStartVnode = oldCh[++oldStartIdx];
                newStartVnode = newCh[++newStartIdx];
            }
            else if (sameVnode(oldEndVnode, newEndVnode)) {
                patchVnode(oldEndVnode, newEndVnode, insertedVnodeQueue, newCh, newEndIdx);
                oldEndVnode = oldCh[--oldEndIdx];
                newEndVnode = newCh[--newEndIdx];
            }
            else if (sameVnode(oldStartVnode, newEndVnode)) {
                // Vnode moved right
                patchVnode(oldStartVnode, newEndVnode, insertedVnodeQueue, newCh, newEndIdx);
                canMove &&
                    nodeOps.insertBefore(parentElm, oldStartVnode.elm, nodeOps.nextSibling(oldEndVnode.elm));
                oldStartVnode = oldCh[++oldStartIdx];
                newEndVnode = newCh[--newEndIdx];
            }
            else if (sameVnode(oldEndVnode, newStartVnode)) {
                // Vnode moved left
                patchVnode(oldEndVnode, newStartVnode, insertedVnodeQueue, newCh, newStartIdx);
                canMove &&
                    nodeOps.insertBefore(parentElm, oldEndVnode.elm, oldStartVnode.elm);
                oldEndVnode = oldCh[--oldEndIdx];
                newStartVnode = newCh[++newStartIdx];
            }
            else {
                if (isUndef(oldKeyToIdx))
                    oldKeyToIdx = createKeyToOldIdx(oldCh, oldStartIdx, oldEndIdx);
                idxInOld = isDef(newStartVnode.key)
                    ? oldKeyToIdx[newStartVnode.key]
                    : findIdxInOld(newStartVnode, oldCh, oldStartIdx, oldEndIdx);
                if (isUndef(idxInOld)) {
                    // New element
                    createElm(newStartVnode, insertedVnodeQueue, parentElm, oldStartVnode.elm, false, newCh, newStartIdx);
                }
                else {
                    vnodeToMove = oldCh[idxInOld];
                    if (sameVnode(vnodeToMove, newStartVnode)) {
                        patchVnode(vnodeToMove, newStartVnode, insertedVnodeQueue, newCh, newStartIdx);
                        oldCh[idxInOld] = undefined;
                        canMove &&
                            nodeOps.insertBefore(parentElm, vnodeToMove.elm, oldStartVnode.elm);
                    }
                    else {
                        // same key but different element. treat as new element
                        createElm(newStartVnode, insertedVnodeQueue, parentElm, oldStartVnode.elm, false, newCh, newStartIdx);
                    }
                }
                newStartVnode = newCh[++newStartIdx];
            }
        }
        if (oldStartIdx > oldEndIdx) {
            refElm = isUndef(newCh[newEndIdx + 1]) ? null : newCh[newEndIdx + 1].elm;
            addVnodes(parentElm, refElm, newCh, newStartIdx, newEndIdx, insertedVnodeQueue);
        }
        else if (newStartIdx > newEndIdx) {
            removeVnodes(oldCh, oldStartIdx, oldEndIdx);
        }
    }
    function checkDuplicateKeys(children) {
        const seenKeys = {};
        for (let i = 0; i < children.length; i++) {
            const vnode = children[i];
            const key = vnode.key;
            if (isDef(key)) {
                if (seenKeys[key]) {
                    warn$2(`Duplicate keys detected: '${key}'. This may cause an update error.`, vnode.context);
                }
                else {
                    seenKeys[key] = true;
                }
            }
        }
    }
    function findIdxInOld(node, oldCh, start, end) {
        for (let i = start; i < end; i++) {
            const c = oldCh[i];
            if (isDef(c) && sameVnode(node, c))
                return i;
        }
    }
    function patchVnode(oldVnode, vnode, insertedVnodeQueue, ownerArray, index, removeOnly) {
        if (oldVnode === vnode) {
            return;
        }
        if (isDef(vnode.elm) && isDef(ownerArray)) {
            // clone reused vnode
            vnode = ownerArray[index] = cloneVNode(vnode);
        }
        const elm = (vnode.elm = oldVnode.elm);
        if (isTrue(oldVnode.isAsyncPlaceholder)) {
            if (isDef(vnode.asyncFactory.resolved)) {
                hydrate(oldVnode.elm, vnode, insertedVnodeQueue);
            }
            else {
                vnode.isAsyncPlaceholder = true;
            }
            return;
        }
        // reuse element for static trees.
        // note we only do this if the vnode is cloned -
        // if the new node is not cloned it means the render functions have been
        // reset by the hot-reload-api and we need to do a proper re-render.
        if (isTrue(vnode.isStatic) &&
            isTrue(oldVnode.isStatic) &&
            vnode.key === oldVnode.key &&
            (isTrue(vnode.isCloned) || isTrue(vnode.isOnce))) {
            vnode.componentInstance = oldVnode.componentInstance;
            return;
        }
        let i;
        const data = vnode.data;
        if (isDef(data) && isDef((i = data.hook)) && isDef((i = i.prepatch))) {
            i(oldVnode, vnode);
        }
        const oldCh = oldVnode.children;
        const ch = vnode.children;
        if (isDef(data) && isPatchable(vnode)) {
            for (i = 0; i < cbs.update.length; ++i)
                cbs.update[i](oldVnode, vnode);
            if (isDef((i = data.hook)) && isDef((i = i.update)))
                i(oldVnode, vnode);
        }
        if (isUndef(vnode.text)) {
            if (isDef(oldCh) && isDef(ch)) {
                if (oldCh !== ch)
                    updateChildren(elm, oldCh, ch, insertedVnodeQueue, removeOnly);
            }
            else if (isDef(ch)) {
                {
                    checkDuplicateKeys(ch);
                }
                if (isDef(oldVnode.text))
                    nodeOps.setTextContent(elm, '');
                addVnodes(elm, null, ch, 0, ch.length - 1, insertedVnodeQueue);
            }
            else if (isDef(oldCh)) {
                removeVnodes(oldCh, 0, oldCh.length - 1);
            }
            else if (isDef(oldVnode.text)) {
                nodeOps.setTextContent(elm, '');
            }
        }
        else if (oldVnode.text !== vnode.text) {
            nodeOps.setTextContent(elm, vnode.text);
        }
        if (isDef(data)) {
            if (isDef((i = data.hook)) && isDef((i = i.postpatch)))
                i(oldVnode, vnode);
        }
    }
    function invokeInsertHook(vnode, queue, initial) {
        // delay insert hooks for component root nodes, invoke them after the
        // element is really inserted
        if (isTrue(initial) && isDef(vnode.parent)) {
            vnode.parent.data.pendingInsert = queue;
        }
        else {
            for (let i = 0; i < queue.length; ++i) {
                queue[i].data.hook.insert(queue[i]);
            }
        }
    }
    let hydrationBailed = false;
    // list of modules that can skip create hook during hydration because they
    // are already rendered on the client or has no need for initialization
    // Note: style is excluded because it relies on initial clone for future
    // deep updates (#7063).
    const isRenderedModule = makeMap('attrs,class,staticClass,staticStyle,key');
    // Note: this is a browser-only function so we can assume elms are DOM nodes.
    function hydrate(elm, vnode, insertedVnodeQueue, inVPre) {
        let i;
        const { tag, data, children } = vnode;
        inVPre = inVPre || (data && data.pre);
        vnode.elm = elm;
        if (isTrue(vnode.isComment) && isDef(vnode.asyncFactory)) {
            vnode.isAsyncPlaceholder = true;
            return true;
        }
        // assert node match
        {
            if (!assertNodeMatch(elm, vnode, inVPre)) {
                return false;
            }
        }
        if (isDef(data)) {
            if (isDef((i = data.hook)) && isDef((i = i.init)))
                i(vnode, true /* hydrating */);
            if (isDef((i = vnode.componentInstance))) {
                // child component. it should have hydrated its own tree.
                initComponent(vnode, insertedVnodeQueue);
                return true;
            }
        }
        if (isDef(tag)) {
            if (isDef(children)) {
                // empty element, allow client to pick up and populate children
                if (!elm.hasChildNodes()) {
                    createChildren(vnode, children, insertedVnodeQueue);
                }
                else {
                    // v-html and domProps: innerHTML
                    if (isDef((i = data)) &&
                        isDef((i = i.domProps)) &&
                        isDef((i = i.innerHTML))) {
                        if (i !== elm.innerHTML) {
                            /* istanbul ignore if */
                            if (typeof console !== 'undefined' &&
                                !hydrationBailed) {
                                hydrationBailed = true;
                                console.warn('Parent: ', elm);
                                console.warn('server innerHTML: ', i);
                                console.warn('client innerHTML: ', elm.innerHTML);
                            }
                            return false;
                        }
                    }
                    else {
                        // iterate and compare children lists
                        let childrenMatch = true;
                        let childNode = elm.firstChild;
                        for (let i = 0; i < children.length; i++) {
                            if (!childNode ||
                                !hydrate(childNode, children[i], insertedVnodeQueue, inVPre)) {
                                childrenMatch = false;
                                break;
                            }
                            childNode = childNode.nextSibling;
                        }
                        // if childNode is not null, it means the actual childNodes list is
                        // longer than the virtual children list.
                        if (!childrenMatch || childNode) {
                            /* istanbul ignore if */
                            if (typeof console !== 'undefined' &&
                                !hydrationBailed) {
                                hydrationBailed = true;
                                console.warn('Parent: ', elm);
                                console.warn('Mismatching childNodes vs. VNodes: ', elm.childNodes, children);
                            }
                            return false;
                        }
                    }
                }
            }
            if (isDef(data)) {
                let fullInvoke = false;
                for (const key in data) {
                    if (!isRenderedModule(key)) {
                        fullInvoke = true;
                        invokeCreateHooks(vnode, insertedVnodeQueue);
                        break;
                    }
                }
                if (!fullInvoke && data['class']) {
                    // ensure collecting deps for deep class bindings for future updates
                    traverse(data['class']);
                }
            }
        }
        else if (elm.data !== vnode.text) {
            elm.data = vnode.text;
        }
        return true;
    }
    function assertNodeMatch(node, vnode, inVPre) {
        if (isDef(vnode.tag)) {
            return (vnode.tag.indexOf('vue-component') === 0 ||
                (!isUnknownElement(vnode, inVPre) &&
                    vnode.tag.toLowerCase() ===
                        (node.tagName && node.tagName.toLowerCase())));
        }
        else {
            return node.nodeType === (vnode.isComment ? 8 : 3);
        }
    }
    return function patch(oldVnode, vnode, hydrating, removeOnly) {
        if (isUndef(vnode)) {
            if (isDef(oldVnode))
                invokeDestroyHook(oldVnode);
            return;
        }
        let isInitialPatch = false;
        const insertedVnodeQueue = [];
        if (isUndef(oldVnode)) {
            // empty mount (likely as component), create new root element
            isInitialPatch = true;
            createElm(vnode, insertedVnodeQueue);
        }
        else {
            const isRealElement = isDef(oldVnode.nodeType);
            if (!isRealElement && sameVnode(oldVnode, vnode)) {
                // patch existing root node
                patchVnode(oldVnode, vnode, insertedVnodeQueue, null, null, removeOnly);
            }
            else {
                if (isRealElement) {
                    // mounting to a real element
                    // check if this is server-rendered content and if we can perform
                    // a successful hydration.
                    if (oldVnode.nodeType === 1 && oldVnode.hasAttribute(SSR_ATTR)) {
                        oldVnode.removeAttribute(SSR_ATTR);
                        hydrating = true;
                    }
                    if (isTrue(hydrating)) {
                        if (hydrate(oldVnode, vnode, insertedVnodeQueue)) {
                            invokeInsertHook(vnode, insertedVnodeQueue, true);
                            return oldVnode;
                        }
                        else {
                            warn$2('The client-side rendered virtual DOM tree is not matching ' +
                                'server-rendered content. This is likely caused by incorrect ' +
                                'HTML markup, for example nesting block-level elements inside ' +
                                '<p>, or missing <tbody>. Bailing hydration and performing ' +
                                'full client-side render.');
                        }
                    }
                    // either not server-rendered, or hydration failed.
                    // create an empty node and replace it
                    oldVnode = emptyNodeAt(oldVnode);
                }
                // replacing existing element
                const oldElm = oldVnode.elm;
                const parentElm = nodeOps.parentNode(oldElm);
                // create new node
                createElm(vnode, insertedVnodeQueue, 
                // extremely rare edge case: do not insert if old element is in a
                // leaving transition. Only happens when combining transition +
                // keep-alive + HOCs. (#4590)
                oldElm._leaveCb ? null : parentElm, nodeOps.nextSibling(oldElm));
                // update parent placeholder node element, recursively
                if (isDef(vnode.parent)) {
                    let ancestor = vnode.parent;
                    const patchable = isPatchable(vnode);
                    while (ancestor) {
                        for (let i = 0; i < cbs.destroy.length; ++i) {
                            cbs.destroy[i](ancestor);
                        }
                        ancestor.elm = vnode.elm;
                        if (patchable) {
                            for (let i = 0; i < cbs.create.length; ++i) {
                                cbs.create[i](emptyNode, ancestor);
                            }
                            // #6513
                            // invoke insert hooks that may have been merged by create hooks.
                            // e.g. for directives that uses the "inserted" hook.
                            const insert = ancestor.data.hook.insert;
                            if (insert.merged) {
                                // start at index 1 to avoid re-invoking component mounted hook
                                for (let i = 1; i < insert.fns.length; i++) {
                                    insert.fns[i]();
                                }
                            }
                        }
                        else {
                            registerRef(ancestor);
                        }
                        ancestor = ancestor.parent;
                    }
                }
                // destroy old node
                if (isDef(parentElm)) {
                    removeVnodes([oldVnode], 0, 0);
                }
                else if (isDef(oldVnode.tag)) {
                    invokeDestroyHook(oldVnode);
                }
            }
        }
        invokeInsertHook(vnode, insertedVnodeQueue, isInitialPatch);
        return vnode.elm;
    };
}

var directives$1 = {
    create: updateDirectives,
    update: updateDirectives,
    destroy: function unbindDirectives(vnode) {
        // @ts-expect-error emptyNode is not VNodeWithData
        updateDirectives(vnode, emptyNode);
    }
};
function updateDirectives(oldVnode, vnode) {
    if (oldVnode.data.directives || vnode.data.directives) {
        _update(oldVnode, vnode);
    }
}
function _update(oldVnode, vnode) {
    const isCreate = oldVnode === emptyNode;
    const isDestroy = vnode === emptyNode;
    const oldDirs = normalizeDirectives(oldVnode.data.directives, oldVnode.context);
    const newDirs = normalizeDirectives(vnode.data.directives, vnode.context);
    const dirsWithInsert = [];
    const dirsWithPostpatch = [];
    let key, oldDir, dir;
    for (key in newDirs) {
        oldDir = oldDirs[key];
        dir = newDirs[key];
        if (!oldDir) {
            // new directive, bind
            callHook(dir, 'bind', vnode, oldVnode);
            if (dir.def && dir.def.inserted) {
                dirsWithInsert.push(dir);
            }
        }
        else {
            // existing directive, update
            dir.oldValue = oldDir.value;
            dir.oldArg = oldDir.arg;
            callHook(dir, 'update', vnode, oldVnode);
            if (dir.def && dir.def.componentUpdated) {
                dirsWithPostpatch.push(dir);
            }
        }
    }
    if (dirsWithInsert.length) {
        const callInsert = () => {
            for (let i = 0; i < dirsWithInsert.length; i++) {
                callHook(dirsWithInsert[i], 'inserted', vnode, oldVnode);
            }
        };
        if (isCreate) {
            mergeVNodeHook(vnode, 'insert', callInsert);
        }
        else {
            callInsert();
        }
    }
    if (dirsWithPostpatch.length) {
        mergeVNodeHook(vnode, 'postpatch', () => {
            for (let i = 0; i < dirsWithPostpatch.length; i++) {
                callHook(dirsWithPostpatch[i], 'componentUpdated', vnode, oldVnode);
            }
        });
    }
    if (!isCreate) {
        for (key in oldDirs) {
            if (!newDirs[key]) {
                // no longer present, unbind
                callHook(oldDirs[key], 'unbind', oldVnode, oldVnode, isDestroy);
            }
        }
    }
}
const emptyModifiers = Object.create(null);
function normalizeDirectives(dirs, vm) {
    const res = Object.create(null);
    if (!dirs) {
        // $flow-disable-line
        return res;
    }
    let i, dir;
    for (i = 0; i < dirs.length; i++) {
        dir = dirs[i];
        if (!dir.modifiers) {
            // $flow-disable-line
            dir.modifiers = emptyModifiers;
        }
        res[getRawDirName(dir)] = dir;
        if (vm._setupState && vm._setupState.__sfc) {
            const setupDef = dir.def || resolveAsset(vm, '_setupState', 'v-' + dir.name);
            if (typeof setupDef === 'function') {
                dir.def = {
                    bind: setupDef,
                    update: setupDef,
                };
            }
            else {
                dir.def = setupDef;
            }
        }
        dir.def = dir.def || resolveAsset(vm.$options, 'directives', dir.name, true);
    }
    // $flow-disable-line
    return res;
}
function getRawDirName(dir) {
    return (dir.rawName || `${dir.name}.${Object.keys(dir.modifiers || {}).join('.')}`);
}
function callHook(dir, hook, vnode, oldVnode, isDestroy) {
    const fn = dir.def && dir.def[hook];
    if (fn) {
        try {
            fn(vnode.elm, dir, vnode, oldVnode, isDestroy);
        }
        catch (e) {
            handleError(e, vnode.context, `directive ${dir.name} ${hook} hook`);
        }
    }
}

var baseModules = [ref, directives$1];

function updateAttrs(oldVnode, vnode) {
    const opts = vnode.componentOptions;
    if (isDef(opts) && opts.Ctor.options.inheritAttrs === false) {
        return;
    }
    if (isUndef(oldVnode.data.attrs) && isUndef(vnode.data.attrs)) {
        return;
    }
    let key, cur, old;
    const elm = vnode.elm;
    const oldAttrs = oldVnode.data.attrs || {};
    let attrs = vnode.data.attrs || {};
    // clone observed objects, as the user probably wants to mutate it
    if (isDef(attrs.__ob__) || isTrue(attrs._v_attr_proxy)) {
        attrs = vnode.data.attrs = extend({}, attrs);
    }
    for (key in attrs) {
        cur = attrs[key];
        old = oldAttrs[key];
        if (old !== cur) {
            setAttr(elm, key, cur, vnode.data.pre);
        }
    }
    // #4391: in IE9, setting type can reset value for input[type=radio]
    // #6666: IE/Edge forces progress value down to 1 before setting a max
    /* istanbul ignore if */
    if ((isIE || isEdge) && attrs.value !== oldAttrs.value) {
        setAttr(elm, 'value', attrs.value);
    }
    for (key in oldAttrs) {
        if (isUndef(attrs[key])) {
            if (isXlink(key)) {
                elm.removeAttributeNS(xlinkNS, getXlinkProp(key));
            }
            else if (!isEnumeratedAttr(key)) {
                elm.removeAttribute(key);
            }
        }
    }
}
function setAttr(el, key, value, isInPre) {
    if (isInPre || el.tagName.indexOf('-') > -1) {
        baseSetAttr(el, key, value);
    }
    else if (isBooleanAttr(key)) {
        // set attribute for blank value
        // e.g. <option disabled>Select one</option>
        if (isFalsyAttrValue(value)) {
            el.removeAttribute(key);
        }
        else {
            // technically allowfullscreen is a boolean attribute for <iframe>,
            // but Flash expects a value of "true" when used on <embed> tag
            value = key === 'allowfullscreen' && el.tagName === 'EMBED' ? 'true' : key;
            el.setAttribute(key, value);
        }
    }
    else if (isEnumeratedAttr(key)) {
        el.setAttribute(key, convertEnumeratedValue(key, value));
    }
    else if (isXlink(key)) {
        if (isFalsyAttrValue(value)) {
            el.removeAttributeNS(xlinkNS, getXlinkProp(key));
        }
        else {
            el.setAttributeNS(xlinkNS, key, value);
        }
    }
    else {
        baseSetAttr(el, key, value);
    }
}
function baseSetAttr(el, key, value) {
    if (isFalsyAttrValue(value)) {
        el.removeAttribute(key);
    }
    else {
        // #7138: IE10 & 11 fires input event when setting placeholder on
        // <textarea>... block the first input event and remove the blocker
        // immediately.
        /* istanbul ignore if */
        if (isIE &&
            !isIE9 &&
            el.tagName === 'TEXTAREA' &&
            key === 'placeholder' &&
            value !== '' &&
            !el.__ieph) {
            const blocker = e => {
                e.stopImmediatePropagation();
                el.removeEventListener('input', blocker);
            };
            el.addEventListener('input', blocker);
            // $flow-disable-line
            el.__ieph = true; /* IE placeholder patched */
        }
        el.setAttribute(key, value);
    }
}
var attrs = {
    create: updateAttrs,
    update: updateAttrs
};

function updateClass(oldVnode, vnode) {
    const el = vnode.elm;
    const data = vnode.data;
    const oldData = oldVnode.data;
    if (isUndef(data.staticClass) &&
        isUndef(data.class) &&
        (isUndef(oldData) ||
            (isUndef(oldData.staticClass) && isUndef(oldData.class)))) {
        return;
    }
    let cls = genClassForVnode(vnode);
    // handle transition classes
    const transitionClass = el._transitionClasses;
    if (isDef(transitionClass)) {
        cls = concat(cls, stringifyClass(transitionClass));
    }
    // set the class
    if (cls !== el._prevClass) {
        el.setAttribute('class', cls);
        el._prevClass = cls;
    }
}
var klass$1 = {
    create: updateClass,
    update: updateClass
};

const validDivisionCharRE = /[\w).+\-_$\]]/;
function parseFilters(exp) {
    let inSingle = false;
    let inDouble = false;
    let inTemplateString = false;
    let inRegex = false;
    let curly = 0;
    let square = 0;
    let paren = 0;
    let lastFilterIndex = 0;
    let c, prev, i, expression, filters;
    for (i = 0; i < exp.length; i++) {
        prev = c;
        c = exp.charCodeAt(i);
        if (inSingle) {
            if (c === 0x27 && prev !== 0x5c)
                inSingle = false;
        }
        else if (inDouble) {
            if (c === 0x22 && prev !== 0x5c)
                inDouble = false;
        }
        else if (inTemplateString) {
            if (c === 0x60 && prev !== 0x5c)
                inTemplateString = false;
        }
        else if (inRegex) {
            if (c === 0x2f && prev !== 0x5c)
                inRegex = false;
        }
        else if (c === 0x7c && // pipe
            exp.charCodeAt(i + 1) !== 0x7c &&
            exp.charCodeAt(i - 1) !== 0x7c &&
            !curly &&
            !square &&
            !paren) {
            if (expression === undefined) {
                // first filter, end of expression
                lastFilterIndex = i + 1;
                expression = exp.slice(0, i).trim();
            }
            else {
                pushFilter();
            }
        }
        else {
            switch (c) {
                case 0x22:
                    inDouble = true;
                    break; // "
                case 0x27:
                    inSingle = true;
                    break; // '
                case 0x60:
                    inTemplateString = true;
                    break; // `
                case 0x28:
                    paren++;
                    break; // (
                case 0x29:
                    paren--;
                    break; // )
                case 0x5b:
                    square++;
                    break; // [
                case 0x5d:
                    square--;
                    break; // ]
                case 0x7b:
                    curly++;
                    break; // {
                case 0x7d:
                    curly--;
                    break; // }
            }
            if (c === 0x2f) {
                // /
                let j = i - 1;
                let p;
                // find first non-whitespace prev char
                for (; j >= 0; j--) {
                    p = exp.charAt(j);
                    if (p !== ' ')
                        break;
                }
                if (!p || !validDivisionCharRE.test(p)) {
                    inRegex = true;
                }
            }
        }
    }
    if (expression === undefined) {
        expression = exp.slice(0, i).trim();
    }
    else if (lastFilterIndex !== 0) {
        pushFilter();
    }
    function pushFilter() {
        (filters || (filters = [])).push(exp.slice(lastFilterIndex, i).trim());
        lastFilterIndex = i + 1;
    }
    if (filters) {
        for (i = 0; i < filters.length; i++) {
            expression = wrapFilter(expression, filters[i]);
        }
    }
    return expression;
}
function wrapFilter(exp, filter) {
    const i = filter.indexOf('(');
    if (i < 0) {
        // _f: resolveFilter
        return `_f("${filter}")(${exp})`;
    }
    else {
        const name = filter.slice(0, i);
        const args = filter.slice(i + 1);
        return `_f("${name}")(${exp}${args !== ')' ? ',' + args : args}`;
    }
}

/* eslint-disable no-unused-vars */
function baseWarn(msg, range) {
    console.error(`[Vue compiler]: ${msg}`);
}
/* eslint-enable no-unused-vars */
function pluckModuleFunction(modules, key) {
    return modules ? modules.map(m => m[key]).filter(_ => _) : [];
}
function addProp(el, name, value, range, dynamic) {
    (el.props || (el.props = [])).push(rangeSetItem({ name, value, dynamic }, range));
    el.plain = false;
}
function addAttr(el, name, value, range, dynamic) {
    const attrs = dynamic
        ? el.dynamicAttrs || (el.dynamicAttrs = [])
        : el.attrs || (el.attrs = []);
    attrs.push(rangeSetItem({ name, value, dynamic }, range));
    el.plain = false;
}
// add a raw attr (use this in preTransforms)
function addRawAttr(el, name, value, range) {
    el.attrsMap[name] = value;
    el.attrsList.push(rangeSetItem({ name, value }, range));
}
function addDirective(el, name, rawName, value, arg, isDynamicArg, modifiers, range) {
    (el.directives || (el.directives = [])).push(rangeSetItem({
        name,
        rawName,
        value,
        arg,
        isDynamicArg,
        modifiers
    }, range));
    el.plain = false;
}
function prependModifierMarker(symbol, name, dynamic) {
    return dynamic ? `_p(${name},"${symbol}")` : symbol + name; // mark the event as captured
}
function addHandler(el, name, value, modifiers, important, warn, range, dynamic) {
    modifiers = modifiers || emptyObject;
    // warn prevent and passive modifier
    /* istanbul ignore if */
    if (warn && modifiers.prevent && modifiers.passive) {
        warn("passive and prevent can't be used together. " +
            "Passive handler can't prevent default event.", range);
    }
    // normalize click.right and click.middle since they don't actually fire
    // this is technically browser-specific, but at least for now browsers are
    // the only target envs that have right/middle clicks.
    if (modifiers.right) {
        if (dynamic) {
            name = `(${name})==='click'?'contextmenu':(${name})`;
        }
        else if (name === 'click') {
            name = 'contextmenu';
            delete modifiers.right;
        }
    }
    else if (modifiers.middle) {
        if (dynamic) {
            name = `(${name})==='click'?'mouseup':(${name})`;
        }
        else if (name === 'click') {
            name = 'mouseup';
        }
    }
    // check capture modifier
    if (modifiers.capture) {
        delete modifiers.capture;
        name = prependModifierMarker('!', name, dynamic);
    }
    if (modifiers.once) {
        delete modifiers.once;
        name = prependModifierMarker('~', name, dynamic);
    }
    /* istanbul ignore if */
    if (modifiers.passive) {
        delete modifiers.passive;
        name = prependModifierMarker('&', name, dynamic);
    }
    let events;
    if (modifiers.native) {
        delete modifiers.native;
        events = el.nativeEvents || (el.nativeEvents = {});
    }
    else {
        events = el.events || (el.events = {});
    }
    const newHandler = rangeSetItem({ value: value.trim(), dynamic }, range);
    if (modifiers !== emptyObject) {
        newHandler.modifiers = modifiers;
    }
    const handlers = events[name];
    /* istanbul ignore if */
    if (Array.isArray(handlers)) {
        important ? handlers.unshift(newHandler) : handlers.push(newHandler);
    }
    else if (handlers) {
        events[name] = important ? [newHandler, handlers] : [handlers, newHandler];
    }
    else {
        events[name] = newHandler;
    }
    el.plain = false;
}
function getRawBindingAttr(el, name) {
    return (el.rawAttrsMap[':' + name] ||
        el.rawAttrsMap['v-bind:' + name] ||
        el.rawAttrsMap[name]);
}
function getBindingAttr(el, name, getStatic) {
    const dynamicValue = getAndRemoveAttr(el, ':' + name) || getAndRemoveAttr(el, 'v-bind:' + name);
    if (dynamicValue != null) {
        return parseFilters(dynamicValue);
    }
    else if (getStatic !== false) {
        const staticValue = getAndRemoveAttr(el, name);
        if (staticValue != null) {
            return JSON.stringify(staticValue);
        }
    }
}
// note: this only removes the attr from the Array (attrsList) so that it
// doesn't get processed by processAttrs.
// By default it does NOT remove it from the map (attrsMap) because the map is
// needed during codegen.
function getAndRemoveAttr(el, name, removeFromMap) {
    let val;
    if ((val = el.attrsMap[name]) != null) {
        const list = el.attrsList;
        for (let i = 0, l = list.length; i < l; i++) {
            if (list[i].name === name) {
                list.splice(i, 1);
                break;
            }
        }
    }
    if (removeFromMap) {
        delete el.attrsMap[name];
    }
    return val;
}
function getAndRemoveAttrByRegex(el, name) {
    const list = el.attrsList;
    for (let i = 0, l = list.length; i < l; i++) {
        const attr = list[i];
        if (name.test(attr.name)) {
            list.splice(i, 1);
            return attr;
        }
    }
}
function rangeSetItem(item, range) {
    if (range) {
        if (range.start != null) {
            item.start = range.start;
        }
        if (range.end != null) {
            item.end = range.end;
        }
    }
    return item;
}

/**
 * Cross-platform code generation for component v-model
 */
function genComponentModel(el, value, modifiers) {
    const { number, trim } = modifiers || {};
    const baseValueExpression = '$$v';
    let valueExpression = baseValueExpression;
    if (trim) {
        valueExpression =
            `(typeof ${baseValueExpression} === 'string'` +
                `? ${baseValueExpression}.trim()` +
                `: ${baseValueExpression})`;
    }
    if (number) {
        valueExpression = `_n(${valueExpression})`;
    }
    const assignment = genAssignmentCode(value, valueExpression);
    el.model = {
        value: `(${value})`,
        expression: JSON.stringify(value),
        callback: `function (${baseValueExpression}) {${assignment}}`
    };
}
/**
 * Cross-platform codegen helper for generating v-model value assignment code.
 */
function genAssignmentCode(value, assignment) {
    const res = parseModel(value);
    if (res.key === null) {
        return `${value}=${assignment}`;
    }
    else {
        return `$set(${res.exp}, ${res.key}, ${assignment})`;
    }
}
/**
 * Parse a v-model expression into a base path and a final key segment.
 * Handles both dot-path and possible square brackets.
 *
 * Possible cases:
 *
 * - test
 * - test[key]
 * - test[test1[key]]
 * - test["a"][key]
 * - xxx.test[a[a].test1[key]]
 * - test.xxx.a["asa"][test1[key]]
 *
 */
let len, str, chr, index, expressionPos, expressionEndPos;
function parseModel(val) {
    // Fix https://github.com/vuejs/vue/pull/7730
    // allow v-model="obj.val " (trailing whitespace)
    val = val.trim();
    len = val.length;
    if (val.indexOf('[') < 0 || val.lastIndexOf(']') < len - 1) {
        index = val.lastIndexOf('.');
        if (index > -1) {
            return {
                exp: val.slice(0, index),
                key: '"' + val.slice(index + 1) + '"'
            };
        }
        else {
            return {
                exp: val,
                key: null
            };
        }
    }
    str = val;
    index = expressionPos = expressionEndPos = 0;
    while (!eof()) {
        chr = next();
        /* istanbul ignore if */
        if (isStringStart(chr)) {
            parseString(chr);
        }
        else if (chr === 0x5b) {
            parseBracket(chr);
        }
    }
    return {
        exp: val.slice(0, expressionPos),
        key: val.slice(expressionPos + 1, expressionEndPos)
    };
}
function next() {
    return str.charCodeAt(++index);
}
function eof() {
    return index >= len;
}
function isStringStart(chr) {
    return chr === 0x22 || chr === 0x27;
}
function parseBracket(chr) {
    let inBracket = 1;
    expressionPos = index;
    while (!eof()) {
        chr = next();
        if (isStringStart(chr)) {
            parseString(chr);
            continue;
        }
        if (chr === 0x5b)
            inBracket++;
        if (chr === 0x5d)
            inBracket--;
        if (inBracket === 0) {
            expressionEndPos = index;
            break;
        }
    }
}
function parseString(chr) {
    const stringQuote = chr;
    while (!eof()) {
        chr = next();
        if (chr === stringQuote) {
            break;
        }
    }
}

let warn$1;
// in some cases, the event used has to be determined at runtime
// so we used some reserved tokens during compile.
const RANGE_TOKEN = '__r';
const CHECKBOX_RADIO_TOKEN = '__c';
function model$1(el, dir, _warn) {
    warn$1 = _warn;
    const value = dir.value;
    const modifiers = dir.modifiers;
    const tag = el.tag;
    const type = el.attrsMap.type;
    {
        // inputs with type="file" are read only and setting the input's
        // value will throw an error.
        if (tag === 'input' && type === 'file') {
            warn$1(`<${el.tag} v-model="${value}" type="file">:\n` +
                `File inputs are read only. Use a v-on:change listener instead.`, el.rawAttrsMap['v-model']);
        }
    }
    if (el.component) {
        genComponentModel(el, value, modifiers);
        // component v-model doesn't need extra runtime
        return false;
    }
    else if (tag === 'select') {
        genSelect(el, value, modifiers);
    }
    else if (tag === 'input' && type === 'checkbox') {
        genCheckboxModel(el, value, modifiers);
    }
    else if (tag === 'input' && type === 'radio') {
        genRadioModel(el, value, modifiers);
    }
    else if (tag === 'input' || tag === 'textarea') {
        genDefaultModel(el, value, modifiers);
    }
    else if (!config.isReservedTag(tag)) {
        genComponentModel(el, value, modifiers);
        // component v-model doesn't need extra runtime
        return false;
    }
    else {
        warn$1(`<${el.tag} v-model="${value}">: ` +
            `v-model is not supported on this element type. ` +
            "If you are working with contenteditable, it's recommended to " +
            'wrap a library dedicated for that purpose inside a custom component.', el.rawAttrsMap['v-model']);
    }
    // ensure runtime directive metadata
    return true;
}
function genCheckboxModel(el, value, modifiers) {
    const number = modifiers && modifiers.number;
    const valueBinding = getBindingAttr(el, 'value') || 'null';
    const trueValueBinding = getBindingAttr(el, 'true-value') || 'true';
    const falseValueBinding = getBindingAttr(el, 'false-value') || 'false';
    addProp(el, 'checked', `Array.isArray(${value})` +
        `?_i(${value},${valueBinding})>-1` +
        (trueValueBinding === 'true'
            ? `:(${value})`
            : `:_q(${value},${trueValueBinding})`));
    addHandler(el, 'change', `var $$a=${value},` +
        '$$el=$event.target,' +
        `$$c=$$el.checked?(${trueValueBinding}):(${falseValueBinding});` +
        'if(Array.isArray($$a)){' +
        `var $$v=${number ? '_n(' + valueBinding + ')' : valueBinding},` +
        '$$i=_i($$a,$$v);' +
        `if($$el.checked){$$i<0&&(${genAssignmentCode(value, '$$a.concat([$$v])')})}` +
        `else{$$i>-1&&(${genAssignmentCode(value, '$$a.slice(0,$$i).concat($$a.slice($$i+1))')})}` +
        `}else{${genAssignmentCode(value, '$$c')}}`, null, true);
}
function genRadioModel(el, value, modifiers) {
    const number = modifiers && modifiers.number;
    let valueBinding = getBindingAttr(el, 'value') || 'null';
    valueBinding = number ? `_n(${valueBinding})` : valueBinding;
    addProp(el, 'checked', `_q(${value},${valueBinding})`);
    addHandler(el, 'change', genAssignmentCode(value, valueBinding), null, true);
}
function genSelect(el, value, modifiers) {
    const number = modifiers && modifiers.number;
    const selectedVal = `Array.prototype.filter` +
        `.call($event.target.options,function(o){return o.selected})` +
        `.map(function(o){var val = "_value" in o ? o._value : o.value;` +
        `return ${number ? '_n(val)' : 'val'}})`;
    const assignment = '$event.target.multiple ? $$selectedVal : $$selectedVal[0]';
    let code = `var $$selectedVal = ${selectedVal};`;
    code = `${code} ${genAssignmentCode(value, assignment)}`;
    addHandler(el, 'change', code, null, true);
}
function genDefaultModel(el, value, modifiers) {
    const type = el.attrsMap.type;
    // warn if v-bind:value conflicts with v-model
    // except for inputs with v-bind:type
    {
        const value = el.attrsMap['v-bind:value'] || el.attrsMap[':value'];
        const typeBinding = el.attrsMap['v-bind:type'] || el.attrsMap[':type'];
        if (value && !typeBinding) {
            const binding = el.attrsMap['v-bind:value'] ? 'v-bind:value' : ':value';
            warn$1(`${binding}="${value}" conflicts with v-model on the same element ` +
                'because the latter already expands to a value binding internally', el.rawAttrsMap[binding]);
        }
    }
    const { lazy, number, trim } = modifiers || {};
    const needCompositionGuard = !lazy && type !== 'range';
    const event = lazy ? 'change' : type === 'range' ? RANGE_TOKEN : 'input';
    let valueExpression = '$event.target.value';
    if (trim) {
        valueExpression = `$event.target.value.trim()`;
    }
    if (number) {
        valueExpression = `_n(${valueExpression})`;
    }
    let code = genAssignmentCode(value, valueExpression);
    if (needCompositionGuard) {
        code = `if($event.target.composing)return;${code}`;
    }
    addProp(el, 'value', `(${value})`);
    addHandler(el, event, code, null, true);
    if (trim || number) {
        addHandler(el, 'blur', '$forceUpdate()');
    }
}

// normalize v-model event tokens that can only be determined at runtime.
// it's important to place the event as the first in the array because
// the whole point is ensuring the v-model callback gets called before
// user-attached handlers.
function normalizeEvents(on) {
    /* istanbul ignore if */
    if (isDef(on[RANGE_TOKEN])) {
        // IE input[type=range] only supports `change` event
        const event = isIE ? 'change' : 'input';
        on[event] = [].concat(on[RANGE_TOKEN], on[event] || []);
        delete on[RANGE_TOKEN];
    }
    // This was originally intended to fix #4521 but no longer necessary
    // after 2.5. Keeping it for backwards compat with generated code from < 2.4
    /* istanbul ignore if */
    if (isDef(on[CHECKBOX_RADIO_TOKEN])) {
        on.change = [].concat(on[CHECKBOX_RADIO_TOKEN], on.change || []);
        delete on[CHECKBOX_RADIO_TOKEN];
    }
}
let target;
function createOnceHandler(event, handler, capture) {
    const _target = target; // save current target element in closure
    return function onceHandler() {
        const res = handler.apply(null, arguments);
        if (res !== null) {
            remove(event, onceHandler, capture, _target);
        }
    };
}
// #9446: Firefox <= 53 (in particular, ESR 52) has incorrect Event.timeStamp
// implementation and does not fire microtasks in between event propagation, so
// safe to exclude.
const useMicrotaskFix = isUsingMicroTask && !(isFF && Number(isFF[1]) <= 53);
function add(name, handler, capture, passive) {
    // async edge case #6566: inner click event triggers patch, event handler
    // attached to outer element during patch, and triggered again. This
    // happens because browsers fire microtask ticks between event propagation.
    // the solution is simple: we save the timestamp when a handler is attached,
    // and the handler would only fire if the event passed to it was fired
    // AFTER it was attached.
    if (useMicrotaskFix) {
        const attachedTimestamp = currentFlushTimestamp;
        const original = handler;
        //@ts-expect-error
        handler = original._wrapper = function (e) {
            if (
            // no bubbling, should always fire.
            // this is just a safety net in case event.timeStamp is unreliable in
            // certain weird environments...
            e.target === e.currentTarget ||
                // event is fired after handler attachment
                e.timeStamp >= attachedTimestamp ||
                // bail for environments that have buggy event.timeStamp implementations
                // #9462 iOS 9 bug: event.timeStamp is 0 after history.pushState
                // #9681 QtWebEngine event.timeStamp is negative value
                e.timeStamp <= 0 ||
                // #9448 bail if event is fired in another document in a multi-page
                // electron/nw.js app, since event.timeStamp will be using a different
                // starting reference
                e.target.ownerDocument !== document) {
                return original.apply(this, arguments);
            }
        };
    }
    target.addEventListener(name, handler, supportsPassive ? { capture, passive } : capture);
}
function remove(name, handler, capture, _target) {
    (_target || target).removeEventListener(name, 
    //@ts-expect-error
    handler._wrapper || handler, capture);
}
function updateDOMListeners(oldVnode, vnode) {
    if (isUndef(oldVnode.data.on) && isUndef(vnode.data.on)) {
        return;
    }
    const on = vnode.data.on || {};
    const oldOn = oldVnode.data.on || {};
    // vnode is empty when removing all listeners,
    // and use old vnode dom element
    target = vnode.elm || oldVnode.elm;
    normalizeEvents(on);
    updateListeners(on, oldOn, add, remove, createOnceHandler, vnode.context);
    target = undefined;
}
var events = {
    create: updateDOMListeners,
    update: updateDOMListeners,
    // @ts-expect-error emptyNode has actually data
    destroy: (vnode) => updateDOMListeners(vnode, emptyNode)
};

let svgContainer;
function updateDOMProps(oldVnode, vnode) {
    if (isUndef(oldVnode.data.domProps) && isUndef(vnode.data.domProps)) {
        return;
    }
    let key, cur;
    const elm = vnode.elm;
    const oldProps = oldVnode.data.domProps || {};
    let props = vnode.data.domProps || {};
    // clone observed objects, as the user probably wants to mutate it
    if (isDef(props.__ob__) || isTrue(props._v_attr_proxy)) {
        props = vnode.data.domProps = extend({}, props);
    }
    for (key in oldProps) {
        if (!(key in props)) {
            elm[key] = '';
        }
    }
    for (key in props) {
        cur = props[key];
        // ignore children if the node has textContent or innerHTML,
        // as these will throw away existing DOM nodes and cause removal errors
        // on subsequent patches (#3360)
        if (key === 'textContent' || key === 'innerHTML') {
            if (vnode.children)
                vnode.children.length = 0;
            if (cur === oldProps[key])
                continue;
            // #6601 work around Chrome version <= 55 bug where single textNode
            // replaced by innerHTML/textContent retains its parentNode property
            if (elm.childNodes.length === 1) {
                elm.removeChild(elm.childNodes[0]);
            }
        }
        if (key === 'value' && elm.tagName !== 'PROGRESS') {
            // store value as _value as well since
            // non-string values will be stringified
            elm._value = cur;
            // avoid resetting cursor position when value is the same
            const strCur = isUndef(cur) ? '' : String(cur);
            if (shouldUpdateValue(elm, strCur)) {
                elm.value = strCur;
            }
        }
        else if (key === 'innerHTML' &&
            isSVG(elm.tagName) &&
            isUndef(elm.innerHTML)) {
            // IE doesn't support innerHTML for SVG elements
            svgContainer = svgContainer || document.createElement('div');
            svgContainer.innerHTML = `<svg>${cur}</svg>`;
            const svg = svgContainer.firstChild;
            while (elm.firstChild) {
                elm.removeChild(elm.firstChild);
            }
            while (svg.firstChild) {
                elm.appendChild(svg.firstChild);
            }
        }
        else if (
        // skip the update if old and new VDOM state is the same.
        // `value` is handled separately because the DOM value may be temporarily
        // out of sync with VDOM state due to focus, composition and modifiers.
        // This  #4521 by skipping the unnecessary `checked` update.
        cur !== oldProps[key]) {
            // some property updates can throw
            // e.g. `value` on <progress> w/ non-finite value
            try {
                elm[key] = cur;
            }
            catch (e) { }
        }
    }
}
function shouldUpdateValue(elm, checkVal) {
    return (
    //@ts-expect-error
    !elm.composing &&
        (elm.tagName === 'OPTION' ||
            isNotInFocusAndDirty(elm, checkVal) ||
            isDirtyWithModifiers(elm, checkVal)));
}
function isNotInFocusAndDirty(elm, checkVal) {
    // return true when textbox (.number and .trim) loses focus and its value is
    // not equal to the updated value
    let notInFocus = true;
    // #6157
    // work around IE bug when accessing document.activeElement in an iframe
    try {
        notInFocus = document.activeElement !== elm;
    }
    catch (e) { }
    return notInFocus && elm.value !== checkVal;
}
function isDirtyWithModifiers(elm, newVal) {
    const value = elm.value;
    const modifiers = elm._vModifiers; // injected by v-model runtime
    if (isDef(modifiers)) {
        if (modifiers.number) {
            return toNumber(value) !== toNumber(newVal);
        }
        if (modifiers.trim) {
            return value.trim() !== newVal.trim();
        }
    }
    return value !== newVal;
}
var domProps = {
    create: updateDOMProps,
    update: updateDOMProps
};

const parseStyleText = cached(function (cssText) {
    const res = {};
    const listDelimiter = /;(?![^(]*\))/g;
    const propertyDelimiter = /:(.+)/;
    cssText.split(listDelimiter).forEach(function (item) {
        if (item) {
            const tmp = item.split(propertyDelimiter);
            tmp.length > 1 && (res[tmp[0].trim()] = tmp[1].trim());
        }
    });
    return res;
});
// merge static and dynamic style data on the same vnode
function normalizeStyleData(data) {
    const style = normalizeStyleBinding(data.style);
    // static style is pre-processed into an object during compilation
    // and is always a fresh object, so it's safe to merge into it
    return data.staticStyle ? extend(data.staticStyle, style) : style;
}
// normalize possible array / string values into Object
function normalizeStyleBinding(bindingStyle) {
    if (Array.isArray(bindingStyle)) {
        return toObject(bindingStyle);
    }
    if (typeof bindingStyle === 'string') {
        return parseStyleText(bindingStyle);
    }
    return bindingStyle;
}
/**
 * parent component style should be after child's
 * so that parent component's style could override it
 */
function getStyle(vnode, checkChild) {
    const res = {};
    let styleData;
    if (checkChild) {
        let childNode = vnode;
        while (childNode.componentInstance) {
            childNode = childNode.componentInstance._vnode;
            if (childNode &&
                childNode.data &&
                (styleData = normalizeStyleData(childNode.data))) {
                extend(res, styleData);
            }
        }
    }
    if ((styleData = normalizeStyleData(vnode.data))) {
        extend(res, styleData);
    }
    let parentNode = vnode;
    // @ts-expect-error parentNode.parent not VNodeWithData
    while ((parentNode = parentNode.parent)) {
        if (parentNode.data && (styleData = normalizeStyleData(parentNode.data))) {
            extend(res, styleData);
        }
    }
    return res;
}

const cssVarRE = /^--/;
const importantRE = /\s*!important$/;
const setProp = (el, name, val) => {
    /* istanbul ignore if */
    if (cssVarRE.test(name)) {
        el.style.setProperty(name, val);
    }
    else if (importantRE.test(val)) {
        el.style.setProperty(hyphenate(name), val.replace(importantRE, ''), 'important');
    }
    else {
        const normalizedName = normalize(name);
        if (Array.isArray(val)) {
            // Support values array created by autoprefixer, e.g.
            // {display: ["-webkit-box", "-ms-flexbox", "flex"]}
            // Set them one by one, and the browser will only set those it can recognize
            for (let i = 0, len = val.length; i < len; i++) {
                el.style[normalizedName] = val[i];
            }
        }
        else {
            el.style[normalizedName] = val;
        }
    }
};
const vendorNames = ['Webkit', 'Moz', 'ms'];
let emptyStyle;
const normalize = cached(function (prop) {
    emptyStyle = emptyStyle || document.createElement('div').style;
    prop = camelize(prop);
    if (prop !== 'filter' && prop in emptyStyle) {
        return prop;
    }
    const capName = prop.charAt(0).toUpperCase() + prop.slice(1);
    for (let i = 0; i < vendorNames.length; i++) {
        const name = vendorNames[i] + capName;
        if (name in emptyStyle) {
            return name;
        }
    }
});
function updateStyle(oldVnode, vnode) {
    const data = vnode.data;
    const oldData = oldVnode.data;
    if (isUndef(data.staticStyle) &&
        isUndef(data.style) &&
        isUndef(oldData.staticStyle) &&
        isUndef(oldData.style)) {
        return;
    }
    let cur, name;
    const el = vnode.elm;
    const oldStaticStyle = oldData.staticStyle;
    const oldStyleBinding = oldData.normalizedStyle || oldData.style || {};
    // if static style exists, stylebinding already merged into it when doing normalizeStyleData
    const oldStyle = oldStaticStyle || oldStyleBinding;
    const style = normalizeStyleBinding(vnode.data.style) || {};
    // store normalized style under a different key for next diff
    // make sure to clone it if it's reactive, since the user likely wants
    // to mutate it.
    vnode.data.normalizedStyle = isDef(style.__ob__) ? extend({}, style) : style;
    const newStyle = getStyle(vnode, true);
    for (name in oldStyle) {
        if (isUndef(newStyle[name])) {
            setProp(el, name, '');
        }
    }
    for (name in newStyle) {
        cur = newStyle[name];
        if (cur !== oldStyle[name]) {
            // ie9 setting to null has no effect, must use empty string
            setProp(el, name, cur == null ? '' : cur);
        }
    }
}
var style$1 = {
    create: updateStyle,
    update: updateStyle
};

const whitespaceRE$1 = /\s+/;
/**
 * Add class with compatibility for SVG since classList is not supported on
 * SVG elements in IE
 */
function addClass(el, cls) {
    /* istanbul ignore if */
    if (!cls || !(cls = cls.trim())) {
        return;
    }
    /* istanbul ignore else */
    if (el.classList) {
        if (cls.indexOf(' ') > -1) {
            cls.split(whitespaceRE$1).forEach(c => el.classList.add(c));
        }
        else {
            el.classList.add(cls);
        }
    }
    else {
        const cur = ` ${el.getAttribute('class') || ''} `;
        if (cur.indexOf(' ' + cls + ' ') < 0) {
            el.setAttribute('class', (cur + cls).trim());
        }
    }
}
/**
 * Remove class with compatibility for SVG since classList is not supported on
 * SVG elements in IE
 */
function removeClass(el, cls) {
    /* istanbul ignore if */
    if (!cls || !(cls = cls.trim())) {
        return;
    }
    /* istanbul ignore else */
    if (el.classList) {
        if (cls.indexOf(' ') > -1) {
            cls.split(whitespaceRE$1).forEach(c => el.classList.remove(c));
        }
        else {
            el.classList.remove(cls);
        }
        if (!el.classList.length) {
            el.removeAttribute('class');
        }
    }
    else {
        let cur = ` ${el.getAttribute('class') || ''} `;
        const tar = ' ' + cls + ' ';
        while (cur.indexOf(tar) >= 0) {
            cur = cur.replace(tar, ' ');
        }
        cur = cur.trim();
        if (cur) {
            el.setAttribute('class', cur);
        }
        else {
            el.removeAttribute('class');
        }
    }
}

function resolveTransition(def) {
    if (!def) {
        return;
    }
    /* istanbul ignore else */
    if (typeof def === 'object') {
        const res = {};
        if (def.css !== false) {
            extend(res, autoCssTransition(def.name || 'v'));
        }
        extend(res, def);
        return res;
    }
    else if (typeof def === 'string') {
        return autoCssTransition(def);
    }
}
const autoCssTransition = cached(name => {
    return {
        enterClass: `${name}-enter`,
        enterToClass: `${name}-enter-to`,
        enterActiveClass: `${name}-enter-active`,
        leaveClass: `${name}-leave`,
        leaveToClass: `${name}-leave-to`,
        leaveActiveClass: `${name}-leave-active`
    };
});
const hasTransition = inBrowser && !isIE9;
const TRANSITION = 'transition';
const ANIMATION = 'animation';
// Transition property/event sniffing
let transitionProp = 'transition';
let transitionEndEvent = 'transitionend';
let animationProp = 'animation';
let animationEndEvent = 'animationend';
if (hasTransition) {
    /* istanbul ignore if */
    if (window.ontransitionend === undefined &&
        window.onwebkittransitionend !== undefined) {
        transitionProp = 'WebkitTransition';
        transitionEndEvent = 'webkitTransitionEnd';
    }
    if (window.onanimationend === undefined &&
        window.onwebkitanimationend !== undefined) {
        animationProp = 'WebkitAnimation';
        animationEndEvent = 'webkitAnimationEnd';
    }
}
// binding to window is necessary to make hot reload work in IE in strict mode
const raf = inBrowser
    ? window.requestAnimationFrame
        ? window.requestAnimationFrame.bind(window)
        : setTimeout
    : /* istanbul ignore next */ /* istanbul ignore next */ fn => fn();
function nextFrame(fn) {
    raf(() => {
        // @ts-expect-error
        raf(fn);
    });
}
function addTransitionClass(el, cls) {
    const transitionClasses = el._transitionClasses || (el._transitionClasses = []);
    if (transitionClasses.indexOf(cls) < 0) {
        transitionClasses.push(cls);
        addClass(el, cls);
    }
}
function removeTransitionClass(el, cls) {
    if (el._transitionClasses) {
        remove$2(el._transitionClasses, cls);
    }
    removeClass(el, cls);
}
function whenTransitionEnds(el, expectedType, cb) {
    const { type, timeout, propCount } = getTransitionInfo(el, expectedType);
    if (!type)
        return cb();
    const event = type === TRANSITION ? transitionEndEvent : animationEndEvent;
    let ended = 0;
    const end = () => {
        el.removeEventListener(event, onEnd);
        cb();
    };
    const onEnd = e => {
        if (e.target === el) {
            if (++ended >= propCount) {
                end();
            }
        }
    };
    setTimeout(() => {
        if (ended < propCount) {
            end();
        }
    }, timeout + 1);
    el.addEventListener(event, onEnd);
}
const transformRE = /\b(transform|all)(,|$)/;
function getTransitionInfo(el, expectedType) {
    const styles = window.getComputedStyle(el);
    // JSDOM may return undefined for transition properties
    const transitionDelays = (styles[transitionProp + 'Delay'] || '').split(', ');
    const transitionDurations = (styles[transitionProp + 'Duration'] || '').split(', ');
    const transitionTimeout = getTimeout(transitionDelays, transitionDurations);
    const animationDelays = (styles[animationProp + 'Delay'] || '').split(', ');
    const animationDurations = (styles[animationProp + 'Duration'] || '').split(', ');
    const animationTimeout = getTimeout(animationDelays, animationDurations);
    let type;
    let timeout = 0;
    let propCount = 0;
    /* istanbul ignore if */
    if (expectedType === TRANSITION) {
        if (transitionTimeout > 0) {
            type = TRANSITION;
            timeout = transitionTimeout;
            propCount = transitionDurations.length;
        }
    }
    else if (expectedType === ANIMATION) {
        if (animationTimeout > 0) {
            type = ANIMATION;
            timeout = animationTimeout;
            propCount = animationDurations.length;
        }
    }
    else {
        timeout = Math.max(transitionTimeout, animationTimeout);
        type =
            timeout > 0
                ? transitionTimeout > animationTimeout
                    ? TRANSITION
                    : ANIMATION
                : null;
        propCount = type
            ? type === TRANSITION
                ? transitionDurations.length
                : animationDurations.length
            : 0;
    }
    const hasTransform = type === TRANSITION && transformRE.test(styles[transitionProp + 'Property']);
    return {
        type,
        timeout,
        propCount,
        hasTransform
    };
}
function getTimeout(delays, durations) {
    /* istanbul ignore next */
    while (delays.length < durations.length) {
        delays = delays.concat(delays);
    }
    return Math.max.apply(null, durations.map((d, i) => {
        return toMs(d) + toMs(delays[i]);
    }));
}
// Old versions of Chromium (below 61.0.3163.100) formats floating pointer numbers
// in a locale-dependent way, using a comma instead of a dot.
// If comma is not replaced with a dot, the input will be rounded down (i.e. acting
// as a floor function) causing unexpected behaviors
function toMs(s) {
    return Number(s.slice(0, -1).replace(',', '.')) * 1000;
}

function enter(vnode, toggleDisplay) {
    const el = vnode.elm;
    // call leave callback now
    if (isDef(el._leaveCb)) {
        el._leaveCb.cancelled = true;
        el._leaveCb();
    }
    const data = resolveTransition(vnode.data.transition);
    if (isUndef(data)) {
        return;
    }
    /* istanbul ignore if */
    if (isDef(el._enterCb) || el.nodeType !== 1) {
        return;
    }
    const { css, type, enterClass, enterToClass, enterActiveClass, appearClass, appearToClass, appearActiveClass, beforeEnter, enter, afterEnter, enterCancelled, beforeAppear, appear, afterAppear, appearCancelled, duration } = data;
    // activeInstance will always be the <transition> component managing this
    // transition. One edge case to check is when the <transition> is placed
    // as the root node of a child component. In that case we need to check
    // <transition>'s parent for appear check.
    let context = activeInstance;
    let transitionNode = activeInstance.$vnode;
    while (transitionNode && transitionNode.parent) {
        context = transitionNode.context;
        transitionNode = transitionNode.parent;
    }
    const isAppear = !context._isMounted || !vnode.isRootInsert;
    if (isAppear && !appear && appear !== '') {
        return;
    }
    const startClass = isAppear && appearClass ? appearClass : enterClass;
    const activeClass = isAppear && appearActiveClass ? appearActiveClass : enterActiveClass;
    const toClass = isAppear && appearToClass ? appearToClass : enterToClass;
    const beforeEnterHook = isAppear ? beforeAppear || beforeEnter : beforeEnter;
    const enterHook = isAppear ? (isFunction(appear) ? appear : enter) : enter;
    const afterEnterHook = isAppear ? afterAppear || afterEnter : afterEnter;
    const enterCancelledHook = isAppear
        ? appearCancelled || enterCancelled
        : enterCancelled;
    const explicitEnterDuration = toNumber(isObject(duration) ? duration.enter : duration);
    if (explicitEnterDuration != null) {
        checkDuration(explicitEnterDuration, 'enter', vnode);
    }
    const expectsCSS = css !== false && !isIE9;
    const userWantsControl = getHookArgumentsLength(enterHook);
    const cb = (el._enterCb = once(() => {
        if (expectsCSS) {
            removeTransitionClass(el, toClass);
            removeTransitionClass(el, activeClass);
        }
        // @ts-expect-error
        if (cb.cancelled) {
            if (expectsCSS) {
                removeTransitionClass(el, startClass);
            }
            enterCancelledHook && enterCancelledHook(el);
        }
        else {
            afterEnterHook && afterEnterHook(el);
        }
        el._enterCb = null;
    }));
    if (!vnode.data.show) {
        // remove pending leave element on enter by injecting an insert hook
        mergeVNodeHook(vnode, 'insert', () => {
            const parent = el.parentNode;
            const pendingNode = parent && parent._pending && parent._pending[vnode.key];
            if (pendingNode &&
                pendingNode.tag === vnode.tag &&
                pendingNode.elm._leaveCb) {
                pendingNode.elm._leaveCb();
            }
            enterHook && enterHook(el, cb);
        });
    }
    // start enter transition
    beforeEnterHook && beforeEnterHook(el);
    if (expectsCSS) {
        addTransitionClass(el, startClass);
        addTransitionClass(el, activeClass);
        nextFrame(() => {
            removeTransitionClass(el, startClass);
            // @ts-expect-error
            if (!cb.cancelled) {
                addTransitionClass(el, toClass);
                if (!userWantsControl) {
                    if (isValidDuration(explicitEnterDuration)) {
                        setTimeout(cb, explicitEnterDuration);
                    }
                    else {
                        whenTransitionEnds(el, type, cb);
                    }
                }
            }
        });
    }
    if (vnode.data.show) {
        toggleDisplay && toggleDisplay();
        enterHook && enterHook(el, cb);
    }
    if (!expectsCSS && !userWantsControl) {
        cb();
    }
}
function leave(vnode, rm) {
    const el = vnode.elm;
    // call enter callback now
    if (isDef(el._enterCb)) {
        el._enterCb.cancelled = true;
        el._enterCb();
    }
    const data = resolveTransition(vnode.data.transition);
    if (isUndef(data) || el.nodeType !== 1) {
        return rm();
    }
    /* istanbul ignore if */
    if (isDef(el._leaveCb)) {
        return;
    }
    const { css, type, leaveClass, leaveToClass, leaveActiveClass, beforeLeave, leave, afterLeave, leaveCancelled, delayLeave, duration } = data;
    const expectsCSS = css !== false && !isIE9;
    const userWantsControl = getHookArgumentsLength(leave);
    const explicitLeaveDuration = toNumber(isObject(duration) ? duration.leave : duration);
    if (isDef(explicitLeaveDuration)) {
        checkDuration(explicitLeaveDuration, 'leave', vnode);
    }
    const cb = (el._leaveCb = once(() => {
        if (el.parentNode && el.parentNode._pending) {
            el.parentNode._pending[vnode.key] = null;
        }
        if (expectsCSS) {
            removeTransitionClass(el, leaveToClass);
            removeTransitionClass(el, leaveActiveClass);
        }
        // @ts-expect-error
        if (cb.cancelled) {
            if (expectsCSS) {
                removeTransitionClass(el, leaveClass);
            }
            leaveCancelled && leaveCancelled(el);
        }
        else {
            rm();
            afterLeave && afterLeave(el);
        }
        el._leaveCb = null;
    }));
    if (delayLeave) {
        delayLeave(performLeave);
    }
    else {
        performLeave();
    }
    function performLeave() {
        // the delayed leave may have already been cancelled
        // @ts-expect-error
        if (cb.cancelled) {
            return;
        }
        // record leaving element
        if (!vnode.data.show && el.parentNode) {
            (el.parentNode._pending || (el.parentNode._pending = {}))[vnode.key] =
                vnode;
        }
        beforeLeave && beforeLeave(el);
        if (expectsCSS) {
            addTransitionClass(el, leaveClass);
            addTransitionClass(el, leaveActiveClass);
            nextFrame(() => {
                removeTransitionClass(el, leaveClass);
                // @ts-expect-error
                if (!cb.cancelled) {
                    addTransitionClass(el, leaveToClass);
                    if (!userWantsControl) {
                        if (isValidDuration(explicitLeaveDuration)) {
                            setTimeout(cb, explicitLeaveDuration);
                        }
                        else {
                            whenTransitionEnds(el, type, cb);
                        }
                    }
                }
            });
        }
        leave && leave(el, cb);
        if (!expectsCSS && !userWantsControl) {
            cb();
        }
    }
}
// only used in dev mode
function checkDuration(val, name, vnode) {
    if (typeof val !== 'number') {
        warn$2(`<transition> explicit ${name} duration is not a valid number - ` +
            `got ${JSON.stringify(val)}.`, vnode.context);
    }
    else if (isNaN(val)) {
        warn$2(`<transition> explicit ${name} duration is NaN - ` +
            'the duration expression might be incorrect.', vnode.context);
    }
}
function isValidDuration(val) {
    return typeof val === 'number' && !isNaN(val);
}
/**
 * Normalize a transition hook's argument length. The hook may be:
 * - a merged hook (invoker) with the original in .fns
 * - a wrapped component method (check ._length)
 * - a plain function (.length)
 */
function getHookArgumentsLength(fn) {
    if (isUndef(fn)) {
        return false;
    }
    // @ts-expect-error
    const invokerFns = fn.fns;
    if (isDef(invokerFns)) {
        // invoker
        return getHookArgumentsLength(Array.isArray(invokerFns) ? invokerFns[0] : invokerFns);
    }
    else {
        // @ts-expect-error
        return (fn._length || fn.length) > 1;
    }
}
function _enter(_, vnode) {
    if (vnode.data.show !== true) {
        enter(vnode);
    }
}
var transition = inBrowser
    ? {
        create: _enter,
        activate: _enter,
        remove(vnode, rm) {
            /* istanbul ignore else */
            if (vnode.data.show !== true) {
                // @ts-expect-error
                leave(vnode, rm);
            }
            else {
                rm();
            }
        }
    }
    : {};

var platformModules = [attrs, klass$1, events, domProps, style$1, transition];

// the directive module should be applied last, after all
// built-in modules have been applied.
const modules$1 = platformModules.concat(baseModules);
const patch = createPatchFunction({ nodeOps, modules: modules$1 });

/**
 * Not type checking this file because flow doesn't like attaching
 * properties to Elements.
 */
/* istanbul ignore if */
if (isIE9) {
    // http://www.matts411.com/post/internet-explorer-9-oninput/
    document.addEventListener('selectionchange', () => {
        const el = document.activeElement;
        // @ts-expect-error
        if (el && el.vmodel) {
            trigger(el, 'input');
        }
    });
}
const directive = {
    inserted(el, binding, vnode, oldVnode) {
        if (vnode.tag === 'select') {
            // #6903
            if (oldVnode.elm && !oldVnode.elm._vOptions) {
                mergeVNodeHook(vnode, 'postpatch', () => {
                    directive.componentUpdated(el, binding, vnode);
                });
            }
            else {
                setSelected(el, binding, vnode.context);
            }
            el._vOptions = [].map.call(el.options, getValue);
        }
        else if (vnode.tag === 'textarea' || isTextInputType(el.type)) {
            el._vModifiers = binding.modifiers;
            if (!binding.modifiers.lazy) {
                el.addEventListener('compositionstart', onCompositionStart);
                el.addEventListener('compositionend', onCompositionEnd);
                // Safari < 10.2 & UIWebView doesn't fire compositionend when
                // switching focus before confirming composition choice
                // this also fixes the issue where some browsers e.g. iOS Chrome
                // fires "change" instead of "input" on autocomplete.
                el.addEventListener('change', onCompositionEnd);
                /* istanbul ignore if */
                if (isIE9) {
                    el.vmodel = true;
                }
            }
        }
    },
    componentUpdated(el, binding, vnode) {
        if (vnode.tag === 'select') {
            setSelected(el, binding, vnode.context);
            // in case the options rendered by v-for have changed,
            // it's possible that the value is out-of-sync with the rendered options.
            // detect such cases and filter out values that no longer has a matching
            // option in the DOM.
            const prevOptions = el._vOptions;
            const curOptions = (el._vOptions = [].map.call(el.options, getValue));
            if (curOptions.some((o, i) => !looseEqual(o, prevOptions[i]))) {
                // trigger change event if
                // no matching option found for at least one value
                const needReset = el.multiple
                    ? binding.value.some(v => hasNoMatchingOption(v, curOptions))
                    : binding.value !== binding.oldValue &&
                        hasNoMatchingOption(binding.value, curOptions);
                if (needReset) {
                    trigger(el, 'change');
                }
            }
        }
    }
};
function setSelected(el, binding, vm) {
    actuallySetSelected(el, binding, vm);
    /* istanbul ignore if */
    if (isIE || isEdge) {
        setTimeout(() => {
            actuallySetSelected(el, binding, vm);
        }, 0);
    }
}
function actuallySetSelected(el, binding, vm) {
    const value = binding.value;
    const isMultiple = el.multiple;
    if (isMultiple && !Array.isArray(value)) {
        warn$2(`<select multiple v-model="${binding.expression}"> ` +
                `expects an Array value for its binding, but got ${Object.prototype.toString
                    .call(value)
                    .slice(8, -1)}`, vm);
        return;
    }
    let selected, option;
    for (let i = 0, l = el.options.length; i < l; i++) {
        option = el.options[i];
        if (isMultiple) {
            selected = looseIndexOf(value, getValue(option)) > -1;
            if (option.selected !== selected) {
                option.selected = selected;
            }
        }
        else {
            if (looseEqual(getValue(option), value)) {
                if (el.selectedIndex !== i) {
                    el.selectedIndex = i;
                }
                return;
            }
        }
    }
    if (!isMultiple) {
        el.selectedIndex = -1;
    }
}
function hasNoMatchingOption(value, options) {
    return options.every(o => !looseEqual(o, value));
}
function getValue(option) {
    return '_value' in option ? option._value : option.value;
}
function onCompositionStart(e) {
    e.target.composing = true;
}
function onCompositionEnd(e) {
    // prevent triggering an input event for no reason
    if (!e.target.composing)
        return;
    e.target.composing = false;
    trigger(e.target, 'input');
}
function trigger(el, type) {
    const e = document.createEvent('HTMLEvents');
    e.initEvent(type, true, true);
    el.dispatchEvent(e);
}

// recursively search for possible transition defined inside the component root
function locateNode(vnode) {
    // @ts-expect-error
    return vnode.componentInstance && (!vnode.data || !vnode.data.transition)
        ? locateNode(vnode.componentInstance._vnode)
        : vnode;
}
var show = {
    bind(el, { value }, vnode) {
        vnode = locateNode(vnode);
        const transition = vnode.data && vnode.data.transition;
        const originalDisplay = (el.__vOriginalDisplay =
            el.style.display === 'none' ? '' : el.style.display);
        if (value && transition) {
            vnode.data.show = true;
            enter(vnode, () => {
                el.style.display = originalDisplay;
            });
        }
        else {
            el.style.display = value ? originalDisplay : 'none';
        }
    },
    update(el, { value, oldValue }, vnode) {
        /* istanbul ignore if */
        if (!value === !oldValue)
            return;
        vnode = locateNode(vnode);
        const transition = vnode.data && vnode.data.transition;
        if (transition) {
            vnode.data.show = true;
            if (value) {
                enter(vnode, () => {
                    el.style.display = el.__vOriginalDisplay;
                });
            }
            else {
                leave(vnode, () => {
                    el.style.display = 'none';
                });
            }
        }
        else {
            el.style.display = value ? el.__vOriginalDisplay : 'none';
        }
    },
    unbind(el, binding, vnode, oldVnode, isDestroy) {
        if (!isDestroy) {
            el.style.display = el.__vOriginalDisplay;
        }
    }
};

var platformDirectives = {
    model: directive,
    show
};

// Provides transition support for a single element/component.
const transitionProps = {
    name: String,
    appear: Boolean,
    css: Boolean,
    mode: String,
    type: String,
    enterClass: String,
    leaveClass: String,
    enterToClass: String,
    leaveToClass: String,
    enterActiveClass: String,
    leaveActiveClass: String,
    appearClass: String,
    appearActiveClass: String,
    appearToClass: String,
    duration: [Number, String, Object]
};
// in case the child is also an abstract component, e.g. <keep-alive>
// we want to recursively retrieve the real component to be rendered
function getRealChild(vnode) {
    const compOptions = vnode && vnode.componentOptions;
    if (compOptions && compOptions.Ctor.options.abstract) {
        return getRealChild(getFirstComponentChild(compOptions.children));
    }
    else {
        return vnode;
    }
}
function extractTransitionData(comp) {
    const data = {};
    const options = comp.$options;
    // props
    for (const key in options.propsData) {
        data[key] = comp[key];
    }
    // events.
    // extract listeners and pass them directly to the transition methods
    const listeners = options._parentListeners;
    for (const key in listeners) {
        data[camelize(key)] = listeners[key];
    }
    return data;
}
function placeholder(h, rawChild) {
    // @ts-expect-error
    if (/\d-keep-alive$/.test(rawChild.tag)) {
        return h('keep-alive', {
            props: rawChild.componentOptions.propsData
        });
    }
}
function hasParentTransition(vnode) {
    while ((vnode = vnode.parent)) {
        if (vnode.data.transition) {
            return true;
        }
    }
}
function isSameChild(child, oldChild) {
    return oldChild.key === child.key && oldChild.tag === child.tag;
}
const isNotTextNode = (c) => c.tag || isAsyncPlaceholder(c);
const isVShowDirective = d => d.name === 'show';
var Transition = {
    name: 'transition',
    props: transitionProps,
    abstract: true,
    render(h) {
        let children = this.$slots.default;
        if (!children) {
            return;
        }
        // filter out text nodes (possible whitespaces)
        children = children.filter(isNotTextNode);
        /* istanbul ignore if */
        if (!children.length) {
            return;
        }
        // warn multiple elements
        if (children.length > 1) {
            warn$2('<transition> can only be used on a single element. Use ' +
                '<transition-group> for lists.', this.$parent);
        }
        const mode = this.mode;
        // warn invalid mode
        if (mode && mode !== 'in-out' && mode !== 'out-in') {
            warn$2('invalid <transition> mode: ' + mode, this.$parent);
        }
        const rawChild = children[0];
        // if this is a component root node and the component's
        // parent container node also has transition, skip.
        if (hasParentTransition(this.$vnode)) {
            return rawChild;
        }
        // apply transition data to child
        // use getRealChild() to ignore abstract components e.g. keep-alive
        const child = getRealChild(rawChild);
        /* istanbul ignore if */
        if (!child) {
            return rawChild;
        }
        if (this._leaving) {
            return placeholder(h, rawChild);
        }
        // ensure a key that is unique to the vnode type and to this transition
        // component instance. This key will be used to remove pending leaving nodes
        // during entering.
        const id = `__transition-${this._uid}-`;
        child.key =
            child.key == null
                ? child.isComment
                    ? id + 'comment'
                    : id + child.tag
                : isPrimitive(child.key)
                    ? String(child.key).indexOf(id) === 0
                        ? child.key
                        : id + child.key
                    : child.key;
        const data = ((child.data || (child.data = {})).transition =
            extractTransitionData(this));
        const oldRawChild = this._vnode;
        const oldChild = getRealChild(oldRawChild);
        // mark v-show
        // so that the transition module can hand over the control to the directive
        if (child.data.directives && child.data.directives.some(isVShowDirective)) {
            child.data.show = true;
        }
        if (oldChild &&
            oldChild.data &&
            !isSameChild(child, oldChild) &&
            !isAsyncPlaceholder(oldChild) &&
            // #6687 component root is a comment node
            !(oldChild.componentInstance &&
                oldChild.componentInstance._vnode.isComment)) {
            // replace old child transition data with fresh one
            // important for dynamic transitions!
            const oldData = (oldChild.data.transition = extend({}, data));
            // handle transition mode
            if (mode === 'out-in') {
                // return placeholder node and queue update when leave finishes
                this._leaving = true;
                mergeVNodeHook(oldData, 'afterLeave', () => {
                    this._leaving = false;
                    this.$forceUpdate();
                });
                return placeholder(h, rawChild);
            }
            else if (mode === 'in-out') {
                if (isAsyncPlaceholder(child)) {
                    return oldRawChild;
                }
                let delayedLeave;
                const performLeave = () => {
                    delayedLeave();
                };
                mergeVNodeHook(data, 'afterEnter', performLeave);
                mergeVNodeHook(data, 'enterCancelled', performLeave);
                mergeVNodeHook(oldData, 'delayLeave', leave => {
                    delayedLeave = leave;
                });
            }
        }
        return rawChild;
    }
};

// Provides transition support for list items.
const props = extend({
    tag: String,
    moveClass: String
}, transitionProps);
delete props.mode;
var TransitionGroup = {
    props,
    beforeMount() {
        const update = this._update;
        this._update = (vnode, hydrating) => {
            const restoreActiveInstance = setActiveInstance(this);
            // force removing pass
            this.__patch__(this._vnode, this.kept, false, // hydrating
            true // removeOnly (!important, avoids unnecessary moves)
            );
            this._vnode = this.kept;
            restoreActiveInstance();
            update.call(this, vnode, hydrating);
        };
    },
    render(h) {
        const tag = this.tag || this.$vnode.data.tag || 'span';
        const map = Object.create(null);
        const prevChildren = (this.prevChildren = this.children);
        const rawChildren = this.$slots.default || [];
        const children = (this.children = []);
        const transitionData = extractTransitionData(this);
        for (let i = 0; i < rawChildren.length; i++) {
            const c = rawChildren[i];
            if (c.tag) {
                if (c.key != null && String(c.key).indexOf('__vlist') !== 0) {
                    children.push(c);
                    map[c.key] = c;
                    (c.data || (c.data = {})).transition = transitionData;
                }
                else {
                    const opts = c.componentOptions;
                    const name = opts
                        ? getComponentName(opts.Ctor.options) || opts.tag || ''
                        : c.tag;
                    warn$2(`<transition-group> children must be keyed: <${name}>`);
                }
            }
        }
        if (prevChildren) {
            const kept = [];
            const removed = [];
            for (let i = 0; i < prevChildren.length; i++) {
                const c = prevChildren[i];
                c.data.transition = transitionData;
                // @ts-expect-error .getBoundingClientRect is not typed in Node
                c.data.pos = c.elm.getBoundingClientRect();
                if (map[c.key]) {
                    kept.push(c);
                }
                else {
                    removed.push(c);
                }
            }
            this.kept = h(tag, null, kept);
            this.removed = removed;
        }
        return h(tag, null, children);
    },
    updated() {
        const children = this.prevChildren;
        const moveClass = this.moveClass || (this.name || 'v') + '-move';
        if (!children.length || !this.hasMove(children[0].elm, moveClass)) {
            return;
        }
        // we divide the work into three loops to avoid mixing DOM reads and writes
        // in each iteration - which helps prevent layout thrashing.
        children.forEach(callPendingCbs);
        children.forEach(recordPosition);
        children.forEach(applyTranslation);
        // force reflow to put everything in position
        // assign to this to avoid being removed in tree-shaking
        // $flow-disable-line
        this._reflow = document.body.offsetHeight;
        children.forEach((c) => {
            if (c.data.moved) {
                const el = c.elm;
                const s = el.style;
                addTransitionClass(el, moveClass);
                s.transform = s.WebkitTransform = s.transitionDuration = '';
                el.addEventListener(transitionEndEvent, (el._moveCb = function cb(e) {
                    if (e && e.target !== el) {
                        return;
                    }
                    if (!e || /transform$/.test(e.propertyName)) {
                        el.removeEventListener(transitionEndEvent, cb);
                        el._moveCb = null;
                        removeTransitionClass(el, moveClass);
                    }
                }));
            }
        });
    },
    methods: {
        hasMove(el, moveClass) {
            /* istanbul ignore if */
            if (!hasTransition) {
                return false;
            }
            /* istanbul ignore if */
            if (this._hasMove) {
                return this._hasMove;
            }
            // Detect whether an element with the move class applied has
            // CSS transitions. Since the element may be inside an entering
            // transition at this very moment, we make a clone of it and remove
            // all other transition classes applied to ensure only the move class
            // is applied.
            const clone = el.cloneNode();
            if (el._transitionClasses) {
                el._transitionClasses.forEach((cls) => {
                    removeClass(clone, cls);
                });
            }
            addClass(clone, moveClass);
            clone.style.display = 'none';
            this.$el.appendChild(clone);
            const info = getTransitionInfo(clone);
            this.$el.removeChild(clone);
            return (this._hasMove = info.hasTransform);
        }
    }
};
function callPendingCbs(c) {
    /* istanbul ignore if */
    if (c.elm._moveCb) {
        c.elm._moveCb();
    }
    /* istanbul ignore if */
    if (c.elm._enterCb) {
        c.elm._enterCb();
    }
}
function recordPosition(c) {
    c.data.newPos = c.elm.getBoundingClientRect();
}
function applyTranslation(c) {
    const oldPos = c.data.pos;
    const newPos = c.data.newPos;
    const dx = oldPos.left - newPos.left;
    const dy = oldPos.top - newPos.top;
    if (dx || dy) {
        c.data.moved = true;
        const s = c.elm.style;
        s.transform = s.WebkitTransform = `translate(${dx}px,${dy}px)`;
        s.transitionDuration = '0s';
    }
}

var platformComponents = {
    Transition,
    TransitionGroup
};

// install platform specific utils
Vue.config.mustUseProp = mustUseProp;
Vue.config.isReservedTag = isReservedTag;
Vue.config.isReservedAttr = isReservedAttr;
Vue.config.getTagNamespace = getTagNamespace;
Vue.config.isUnknownElement = isUnknownElement;
// install platform runtime directives & components
extend(Vue.options.directives, platformDirectives);
extend(Vue.options.components, platformComponents);
// install platform patch function
Vue.prototype.__patch__ = inBrowser ? patch : noop;
// public mount method
Vue.prototype.$mount = function (el, hydrating) {
    el = el && inBrowser ? query(el) : undefined;
    return mountComponent(this, el, hydrating);
};
// devtools global hook
/* istanbul ignore next */
if (inBrowser) {
    setTimeout(() => {
        if (config.devtools) {
            if (devtools) {
                devtools.emit('init', Vue);
            }
            else {
                // @ts-expect-error
                console[console.info ? 'info' : 'log']('Download the Vue Devtools extension for a better development experience:\n' +
                    'https://github.com/vuejs/vue-devtools');
            }
        }
        if (config.productionTip !== false &&
            typeof console !== 'undefined') {
            // @ts-expect-error
            console[console.info ? 'info' : 'log'](`You are running Vue in development mode.\n` +
                `Make sure to turn on production mode when deploying for production.\n` +
                `See more tips at https://vuejs.org/guide/deployment.html`);
        }
    }, 0);
}

const defaultTagRE = /\{\{((?:.|\r?\n)+?)\}\}/g;
const regexEscapeRE = /[-.*+?^${}()|[\]\/\\]/g;
const buildRegex = cached(delimiters => {
    const open = delimiters[0].replace(regexEscapeRE, '\\$&');
    const close = delimiters[1].replace(regexEscapeRE, '\\$&');
    return new RegExp(open + '((?:.|\\n)+?)' + close, 'g');
});
function parseText(text, delimiters) {
    //@ts-expect-error
    const tagRE = delimiters ? buildRegex(delimiters) : defaultTagRE;
    if (!tagRE.test(text)) {
        return;
    }
    const tokens = [];
    const rawTokens = [];
    let lastIndex = (tagRE.lastIndex = 0);
    let match, index, tokenValue;
    while ((match = tagRE.exec(text))) {
        index = match.index;
        // push text token
        if (index > lastIndex) {
            rawTokens.push((tokenValue = text.slice(lastIndex, index)));
            tokens.push(JSON.stringify(tokenValue));
        }
        // tag token
        const exp = parseFilters(match[1].trim());
        tokens.push(`_s(${exp})`);
        rawTokens.push({ '@binding': exp });
        lastIndex = index + match[0].length;
    }
    if (lastIndex < text.length) {
        rawTokens.push((tokenValue = text.slice(lastIndex)));
        tokens.push(JSON.stringify(tokenValue));
    }
    return {
        expression: tokens.join('+'),
        tokens: rawTokens
    };
}

function transformNode$1(el, options) {
    const warn = options.warn || baseWarn;
    const staticClass = getAndRemoveAttr(el, 'class');
    if (staticClass) {
        const res = parseText(staticClass, options.delimiters);
        if (res) {
            warn(`class="${staticClass}": ` +
                'Interpolation inside attributes has been removed. ' +
                'Use v-bind or the colon shorthand instead. For example, ' +
                'instead of <div class="{{ val }}">, use <div :class="val">.', el.rawAttrsMap['class']);
        }
    }
    if (staticClass) {
        el.staticClass = JSON.stringify(staticClass.replace(/\s+/g, ' ').trim());
    }
    const classBinding = getBindingAttr(el, 'class', false /* getStatic */);
    if (classBinding) {
        el.classBinding = classBinding;
    }
}
function genData$2(el) {
    let data = '';
    if (el.staticClass) {
        data += `staticClass:${el.staticClass},`;
    }
    if (el.classBinding) {
        data += `class:${el.classBinding},`;
    }
    return data;
}
var klass = {
    staticKeys: ['staticClass'],
    transformNode: transformNode$1,
    genData: genData$2
};

function transformNode(el, options) {
    const warn = options.warn || baseWarn;
    const staticStyle = getAndRemoveAttr(el, 'style');
    if (staticStyle) {
        /* istanbul ignore if */
        {
            const res = parseText(staticStyle, options.delimiters);
            if (res) {
                warn(`style="${staticStyle}": ` +
                    'Interpolation inside attributes has been removed. ' +
                    'Use v-bind or the colon shorthand instead. For example, ' +
                    'instead of <div style="{{ val }}">, use <div :style="val">.', el.rawAttrsMap['style']);
            }
        }
        el.staticStyle = JSON.stringify(parseStyleText(staticStyle));
    }
    const styleBinding = getBindingAttr(el, 'style', false /* getStatic */);
    if (styleBinding) {
        el.styleBinding = styleBinding;
    }
}
function genData$1(el) {
    let data = '';
    if (el.staticStyle) {
        data += `staticStyle:${el.staticStyle},`;
    }
    if (el.styleBinding) {
        data += `style:(${el.styleBinding}),`;
    }
    return data;
}
var style = {
    staticKeys: ['staticStyle'],
    transformNode,
    genData: genData$1
};

let decoder;
var he = {
    decode(html) {
        decoder = decoder || document.createElement('div');
        decoder.innerHTML = html;
        return decoder.textContent;
    }
};

const isUnaryTag = makeMap('area,base,br,col,embed,frame,hr,img,input,isindex,keygen,' +
    'link,meta,param,source,track,wbr');
// Elements that you can, intentionally, leave open
// (and which close themselves)
const canBeLeftOpenTag = makeMap('colgroup,dd,dt,li,options,p,td,tfoot,th,thead,tr,source');
// HTML5 tags https://html.spec.whatwg.org/multipage/indices.html#elements-3
// Phrasing Content https://html.spec.whatwg.org/multipage/dom.html#phrasing-content
const isNonPhrasingTag = makeMap('address,article,aside,base,blockquote,body,caption,col,colgroup,dd,' +
    'details,dialog,div,dl,dt,fieldset,figcaption,figure,footer,form,' +
    'h1,h2,h3,h4,h5,h6,head,header,hgroup,hr,html,legend,li,menuitem,meta,' +
    'optgroup,option,param,rp,rt,source,style,summary,tbody,td,tfoot,th,thead,' +
    'title,tr,track');

/**
 * Not type-checking this file because it's mostly vendor code.
 */
// Regular Expressions for parsing tags and attributes
const attribute = /^\s*([^\s"'<>\/=]+)(?:\s*(=)\s*(?:"([^"]*)"+|'([^']*)'+|([^\s"'=<>`]+)))?/;
const dynamicArgAttribute = /^\s*((?:v-[\w-]+:|@|:|#)\[[^=]+?\][^\s"'<>\/=]*)(?:\s*(=)\s*(?:"([^"]*)"+|'([^']*)'+|([^\s"'=<>`]+)))?/;
const ncname = `[a-zA-Z_][\\-\\.0-9_a-zA-Z${unicodeRegExp.source}]*`;
const qnameCapture = `((?:${ncname}\\:)?${ncname})`;
const startTagOpen = new RegExp(`^<${qnameCapture}`);
const startTagClose = /^\s*(\/?)>/;
const endTag = new RegExp(`^<\\/${qnameCapture}[^>]*>`);
const doctype = /^<!DOCTYPE [^>]+>/i;
// #7298: escape - to avoid being passed as HTML comment when inlined in page
const comment = /^<!\--/;
const conditionalComment = /^<!\[/;
// Special Elements (can contain anything)
const isPlainTextElement = makeMap('script,style,textarea', true);
const reCache = {};
const decodingMap = {
    '&lt;': '<',
    '&gt;': '>',
    '&quot;': '"',
    '&amp;': '&',
    '&#10;': '\n',
    '&#9;': '\t',
    '&#39;': "'"
};
const encodedAttr = /&(?:lt|gt|quot|amp|#39);/g;
const encodedAttrWithNewLines = /&(?:lt|gt|quot|amp|#39|#10|#9);/g;
// #5992
const isIgnoreNewlineTag = makeMap('pre,textarea', true);
const shouldIgnoreFirstNewline = (tag, html) => tag && isIgnoreNewlineTag(tag) && html[0] === '\n';
function decodeAttr(value, shouldDecodeNewlines) {
    const re = shouldDecodeNewlines ? encodedAttrWithNewLines : encodedAttr;
    return value.replace(re, match => decodingMap[match]);
}
function parseHTML(html, options) {
    const stack = [];
    const expectHTML = options.expectHTML;
    const isUnaryTag = options.isUnaryTag || no;
    const canBeLeftOpenTag = options.canBeLeftOpenTag || no;
    let index = 0;
    let last, lastTag;
    while (html) {
        last = html;
        // Make sure we're not in a plaintext content element like script/style
        if (!lastTag || !isPlainTextElement(lastTag)) {
            let textEnd = html.indexOf('<');
            if (textEnd === 0) {
                // Comment:
                if (comment.test(html)) {
                    const commentEnd = html.indexOf('-->');
                    if (commentEnd >= 0) {
                        if (options.shouldKeepComment && options.comment) {
                            options.comment(html.substring(4, commentEnd), index, index + commentEnd + 3);
                        }
                        advance(commentEnd + 3);
                        continue;
                    }
                }
                // http://en.wikipedia.org/wiki/Conditional_comment#Downlevel-revealed_conditional_comment
                if (conditionalComment.test(html)) {
                    const conditionalEnd = html.indexOf(']>');
                    if (conditionalEnd >= 0) {
                        advance(conditionalEnd + 2);
                        continue;
                    }
                }
                // Doctype:
                const doctypeMatch = html.match(doctype);
                if (doctypeMatch) {
                    advance(doctypeMatch[0].length);
                    continue;
                }
                // End tag:
                const endTagMatch = html.match(endTag);
                if (endTagMatch) {
                    const curIndex = index;
                    advance(endTagMatch[0].length);
                    parseEndTag(endTagMatch[1], curIndex, index);
                    continue;
                }
                // Start tag:
                const startTagMatch = parseStartTag();
                if (startTagMatch) {
                    handleStartTag(startTagMatch);
                    if (shouldIgnoreFirstNewline(startTagMatch.tagName, html)) {
                        advance(1);
                    }
                    continue;
                }
            }
            let text, rest, next;
            if (textEnd >= 0) {
                rest = html.slice(textEnd);
                while (!endTag.test(rest) &&
                    !startTagOpen.test(rest) &&
                    !comment.test(rest) &&
                    !conditionalComment.test(rest)) {
                    // < in plain text, be forgiving and treat it as text
                    next = rest.indexOf('<', 1);
                    if (next < 0)
                        break;
                    textEnd += next;
                    rest = html.slice(textEnd);
                }
                text = html.substring(0, textEnd);
            }
            if (textEnd < 0) {
                text = html;
            }
            if (text) {
                advance(text.length);
            }
            if (options.chars && text) {
                options.chars(text, index - text.length, index);
            }
        }
        else {
            let endTagLength = 0;
            const stackedTag = lastTag.toLowerCase();
            const reStackedTag = reCache[stackedTag] ||
                (reCache[stackedTag] = new RegExp('([\\s\\S]*?)(</' + stackedTag + '[^>]*>)', 'i'));
            const rest = html.replace(reStackedTag, function (all, text, endTag) {
                endTagLength = endTag.length;
                if (!isPlainTextElement(stackedTag) && stackedTag !== 'noscript') {
                    text = text
                        .replace(/<!\--([\s\S]*?)-->/g, '$1') // #7298
                        .replace(/<!\[CDATA\[([\s\S]*?)]]>/g, '$1');
                }
                if (shouldIgnoreFirstNewline(stackedTag, text)) {
                    text = text.slice(1);
                }
                if (options.chars) {
                    options.chars(text);
                }
                return '';
            });
            index += html.length - rest.length;
            html = rest;
            parseEndTag(stackedTag, index - endTagLength, index);
        }
        if (html === last) {
            options.chars && options.chars(html);
            if (!stack.length && options.warn) {
                options.warn(`Mal-formatted tag at end of template: "${html}"`, {
                    start: index + html.length
                });
            }
            break;
        }
    }
    // Clean up any remaining tags
    parseEndTag();
    function advance(n) {
        index += n;
        html = html.substring(n);
    }
    function parseStartTag() {
        const start = html.match(startTagOpen);
        if (start) {
            const match = {
                tagName: start[1],
                attrs: [],
                start: index
            };
            advance(start[0].length);
            let end, attr;
            while (!(end = html.match(startTagClose)) &&
                (attr = html.match(dynamicArgAttribute) || html.match(attribute))) {
                attr.start = index;
                advance(attr[0].length);
                attr.end = index;
                match.attrs.push(attr);
            }
            if (end) {
                match.unarySlash = end[1];
                advance(end[0].length);
                match.end = index;
                return match;
            }
        }
    }
    function handleStartTag(match) {
        const tagName = match.tagName;
        const unarySlash = match.unarySlash;
        if (expectHTML) {
            if (lastTag === 'p' && isNonPhrasingTag(tagName)) {
                parseEndTag(lastTag);
            }
            if (canBeLeftOpenTag(tagName) && lastTag === tagName) {
                parseEndTag(tagName);
            }
        }
        const unary = isUnaryTag(tagName) || !!unarySlash;
        const l = match.attrs.length;
        const attrs = new Array(l);
        for (let i = 0; i < l; i++) {
            const args = match.attrs[i];
            const value = args[3] || args[4] || args[5] || '';
            const shouldDecodeNewlines = tagName === 'a' && args[1] === 'href'
                ? options.shouldDecodeNewlinesForHref
                : options.shouldDecodeNewlines;
            attrs[i] = {
                name: args[1],
                value: decodeAttr(value, shouldDecodeNewlines)
            };
            if (options.outputSourceRange) {
                attrs[i].start = args.start + args[0].match(/^\s*/).length;
                attrs[i].end = args.end;
            }
        }
        if (!unary) {
            stack.push({
                tag: tagName,
                lowerCasedTag: tagName.toLowerCase(),
                attrs: attrs,
                start: match.start,
                end: match.end
            });
            lastTag = tagName;
        }
        if (options.start) {
            options.start(tagName, attrs, unary, match.start, match.end);
        }
    }
    function parseEndTag(tagName, start, end) {
        let pos, lowerCasedTagName;
        if (start == null)
            start = index;
        if (end == null)
            end = index;
        // Find the closest opened tag of the same type
        if (tagName) {
            lowerCasedTagName = tagName.toLowerCase();
            for (pos = stack.length - 1; pos >= 0; pos--) {
                if (stack[pos].lowerCasedTag === lowerCasedTagName) {
                    break;
                }
            }
        }
        else {
            // If no tag name is provided, clean shop
            pos = 0;
        }
        if (pos >= 0) {
            // Close all the open elements, up the stack
            for (let i = stack.length - 1; i >= pos; i--) {
                if ((i > pos || !tagName) && options.warn) {
                    options.warn(`tag <${stack[i].tag}> has no matching end tag.`, {
                        start: stack[i].start,
                        end: stack[i].end
                    });
                }
                if (options.end) {
                    options.end(stack[i].tag, start, end);
                }
            }
            // Remove the open elements from the stack
            stack.length = pos;
            lastTag = pos && stack[pos - 1].tag;
        }
        else if (lowerCasedTagName === 'br') {
            if (options.start) {
                options.start(tagName, [], true, start, end);
            }
        }
        else if (lowerCasedTagName === 'p') {
            if (options.start) {
                options.start(tagName, [], false, start, end);
            }
            if (options.end) {
                options.end(tagName, start, end);
            }
        }
    }
}

const onRE = /^@|^v-on:/;
const dirRE = /^v-|^@|^:|^#/;
const forAliasRE = /([\s\S]*?)\s+(?:in|of)\s+([\s\S]*)/;
const forIteratorRE = /,([^,\}\]]*)(?:,([^,\}\]]*))?$/;
const stripParensRE = /^\(|\)$/g;
const dynamicArgRE = /^\[.*\]$/;
const argRE = /:(.*)$/;
const bindRE = /^:|^\.|^v-bind:/;
const modifierRE = /\.[^.\]]+(?=[^\]]*$)/g;
const slotRE = /^v-slot(:|$)|^#/;
const lineBreakRE = /[\r\n]/;
const whitespaceRE = /[ \f\t\r\n]+/g;
const invalidAttributeRE = /[\s"'<>\/=]/;
const decodeHTMLCached = cached(he.decode);
const emptySlotScopeToken = `_empty_`;
// configurable state
let warn;
let delimiters;
let transforms;
let preTransforms;
let postTransforms;
let platformIsPreTag;
let platformMustUseProp;
let platformGetTagNamespace;
let maybeComponent;
function createASTElement(tag, attrs, parent) {
    return {
        type: 1,
        tag,
        attrsList: attrs,
        attrsMap: makeAttrsMap(attrs),
        rawAttrsMap: {},
        parent,
        children: []
    };
}
/**
 * Convert HTML string to AST.
 */
function parse(template, options) {
    warn = options.warn || baseWarn;
    platformIsPreTag = options.isPreTag || no;
    platformMustUseProp = options.mustUseProp || no;
    platformGetTagNamespace = options.getTagNamespace || no;
    const isReservedTag = options.isReservedTag || no;
    maybeComponent = (el) => !!(el.component ||
        el.attrsMap[':is'] ||
        el.attrsMap['v-bind:is'] ||
        !(el.attrsMap.is ? isReservedTag(el.attrsMap.is) : isReservedTag(el.tag)));
    transforms = pluckModuleFunction(options.modules, 'transformNode');
    preTransforms = pluckModuleFunction(options.modules, 'preTransformNode');
    postTransforms = pluckModuleFunction(options.modules, 'postTransformNode');
    delimiters = options.delimiters;
    const stack = [];
    const preserveWhitespace = options.preserveWhitespace !== false;
    const whitespaceOption = options.whitespace;
    let root;
    let currentParent;
    let inVPre = false;
    let inPre = false;
    let warned = false;
    function warnOnce(msg, range) {
        if (!warned) {
            warned = true;
            warn(msg, range);
        }
    }
    function closeElement(element) {
        trimEndingWhitespace(element);
        if (!inVPre && !element.processed) {
            element = processElement(element, options);
        }
        // tree management
        if (!stack.length && element !== root) {
            // allow root elements with v-if, v-else-if and v-else
            if (root.if && (element.elseif || element.else)) {
                {
                    checkRootConstraints(element);
                }
                addIfCondition(root, {
                    exp: element.elseif,
                    block: element
                });
            }
            else {
                warnOnce(`Component template should contain exactly one root element. ` +
                    `If you are using v-if on multiple elements, ` +
                    `use v-else-if to chain them instead.`, { start: element.start });
            }
        }
        if (currentParent && !element.forbidden) {
            if (element.elseif || element.else) {
                processIfConditions(element, currentParent);
            }
            else {
                if (element.slotScope) {
                    // scoped slot
                    // keep it in the children list so that v-else(-if) conditions can
                    // find it as the prev node.
                    const name = element.slotTarget || '"default"';
                    (currentParent.scopedSlots || (currentParent.scopedSlots = {}))[name] = element;
                }
                currentParent.children.push(element);
                element.parent = currentParent;
            }
        }
        // final children cleanup
        // filter out scoped slots
        element.children = element.children.filter(c => !c.slotScope);
        // remove trailing whitespace node again
        trimEndingWhitespace(element);
        // check pre state
        if (element.pre) {
            inVPre = false;
        }
        if (platformIsPreTag(element.tag)) {
            inPre = false;
        }
        // apply post-transforms
        for (let i = 0; i < postTransforms.length; i++) {
            postTransforms[i](element, options);
        }
    }
    function trimEndingWhitespace(el) {
        // remove trailing whitespace node
        if (!inPre) {
            let lastNode;
            while ((lastNode = el.children[el.children.length - 1]) &&
                lastNode.type === 3 &&
                lastNode.text === ' ') {
                el.children.pop();
            }
        }
    }
    function checkRootConstraints(el) {
        if (el.tag === 'slot' || el.tag === 'template') {
            warnOnce(`Cannot use <${el.tag}> as component root element because it may ` +
                'contain multiple nodes.', { start: el.start });
        }
        if (el.attrsMap.hasOwnProperty('v-for')) {
            warnOnce('Cannot use v-for on stateful component root element because ' +
                'it renders multiple elements.', el.rawAttrsMap['v-for']);
        }
    }
    parseHTML(template, {
        warn,
        expectHTML: options.expectHTML,
        isUnaryTag: options.isUnaryTag,
        canBeLeftOpenTag: options.canBeLeftOpenTag,
        shouldDecodeNewlines: options.shouldDecodeNewlines,
        shouldDecodeNewlinesForHref: options.shouldDecodeNewlinesForHref,
        shouldKeepComment: options.comments,
        outputSourceRange: options.outputSourceRange,
        start(tag, attrs, unary, start, end) {
            // check namespace.
            // inherit parent ns if there is one
            const ns = (currentParent && currentParent.ns) || platformGetTagNamespace(tag);
            // handle IE svg bug
            /* istanbul ignore if */
            if (isIE && ns === 'svg') {
                attrs = guardIESVGBug(attrs);
            }
            let element = createASTElement(tag, attrs, currentParent);
            if (ns) {
                element.ns = ns;
            }
            {
                if (options.outputSourceRange) {
                    element.start = start;
                    element.end = end;
                    element.rawAttrsMap = element.attrsList.reduce((cumulated, attr) => {
                        cumulated[attr.name] = attr;
                        return cumulated;
                    }, {});
                }
                attrs.forEach(attr => {
                    if (invalidAttributeRE.test(attr.name)) {
                        warn(`Invalid dynamic argument expression: attribute names cannot contain ` +
                            `spaces, quotes, <, >, / or =.`, options.outputSourceRange
                            ? {
                                start: attr.start + attr.name.indexOf(`[`),
                                end: attr.start + attr.name.length
                            }
                            : undefined);
                    }
                });
            }
            if (isForbiddenTag(element) && !isServerRendering()) {
                element.forbidden = true;
                warn('Templates should only be responsible for mapping the state to the ' +
                        'UI. Avoid placing tags with side-effects in your templates, such as ' +
                        `<${tag}>` +
                        ', as they will not be parsed.', { start: element.start });
            }
            // apply pre-transforms
            for (let i = 0; i < preTransforms.length; i++) {
                element = preTransforms[i](element, options) || element;
            }
            if (!inVPre) {
                processPre(element);
                if (element.pre) {
                    inVPre = true;
                }
            }
            if (platformIsPreTag(element.tag)) {
                inPre = true;
            }
            if (inVPre) {
                processRawAttrs(element);
            }
            else if (!element.processed) {
                // structural directives
                processFor(element);
                processIf(element);
                processOnce(element);
            }
            if (!root) {
                root = element;
                {
                    checkRootConstraints(root);
                }
            }
            if (!unary) {
                currentParent = element;
                stack.push(element);
            }
            else {
                closeElement(element);
            }
        },
        end(tag, start, end) {
            const element = stack[stack.length - 1];
            // pop stack
            stack.length -= 1;
            currentParent = stack[stack.length - 1];
            if (options.outputSourceRange) {
                element.end = end;
            }
            closeElement(element);
        },
        chars(text, start, end) {
            if (!currentParent) {
                {
                    if (text === template) {
                        warnOnce('Component template requires a root element, rather than just text.', { start });
                    }
                    else if ((text = text.trim())) {
                        warnOnce(`text "${text}" outside root element will be ignored.`, {
                            start
                        });
                    }
                }
                return;
            }
            // IE textarea placeholder bug
            /* istanbul ignore if */
            if (isIE &&
                currentParent.tag === 'textarea' &&
                currentParent.attrsMap.placeholder === text) {
                return;
            }
            const children = currentParent.children;
            if (inPre || text.trim()) {
                text = isTextTag(currentParent)
                    ? text
                    : decodeHTMLCached(text);
            }
            else if (!children.length) {
                // remove the whitespace-only node right after an opening tag
                text = '';
            }
            else if (whitespaceOption) {
                if (whitespaceOption === 'condense') {
                    // in condense mode, remove the whitespace node if it contains
                    // line break, otherwise condense to a single space
                    text = lineBreakRE.test(text) ? '' : ' ';
                }
                else {
                    text = ' ';
                }
            }
            else {
                text = preserveWhitespace ? ' ' : '';
            }
            if (text) {
                if (!inPre && whitespaceOption === 'condense') {
                    // condense consecutive whitespaces into single space
                    text = text.replace(whitespaceRE, ' ');
                }
                let res;
                let child;
                if (!inVPre && text !== ' ' && (res = parseText(text, delimiters))) {
                    child = {
                        type: 2,
                        expression: res.expression,
                        tokens: res.tokens,
                        text
                    };
                }
                else if (text !== ' ' ||
                    !children.length ||
                    children[children.length - 1].text !== ' ') {
                    child = {
                        type: 3,
                        text
                    };
                }
                if (child) {
                    if (options.outputSourceRange) {
                        child.start = start;
                        child.end = end;
                    }
                    children.push(child);
                }
            }
        },
        comment(text, start, end) {
            // adding anything as a sibling to the root node is forbidden
            // comments should still be allowed, but ignored
            if (currentParent) {
                const child = {
                    type: 3,
                    text,
                    isComment: true
                };
                if (options.outputSourceRange) {
                    child.start = start;
                    child.end = end;
                }
                currentParent.children.push(child);
            }
        }
    });
    return root;
}
function processPre(el) {
    if (getAndRemoveAttr(el, 'v-pre') != null) {
        el.pre = true;
    }
}
function processRawAttrs(el) {
    const list = el.attrsList;
    const len = list.length;
    if (len) {
        const attrs = (el.attrs = new Array(len));
        for (let i = 0; i < len; i++) {
            attrs[i] = {
                name: list[i].name,
                value: JSON.stringify(list[i].value)
            };
            if (list[i].start != null) {
                attrs[i].start = list[i].start;
                attrs[i].end = list[i].end;
            }
        }
    }
    else if (!el.pre) {
        // non root node in pre blocks with no attributes
        el.plain = true;
    }
}
function processElement(element, options) {
    processKey(element);
    // determine whether this is a plain element after
    // removing structural attributes
    element.plain =
        !element.key && !element.scopedSlots && !element.attrsList.length;
    processRef(element);
    processSlotContent(element);
    processSlotOutlet(element);
    processComponent(element);
    for (let i = 0; i < transforms.length; i++) {
        element = transforms[i](element, options) || element;
    }
    processAttrs(element);
    return element;
}
function processKey(el) {
    const exp = getBindingAttr(el, 'key');
    if (exp) {
        {
            if (el.tag === 'template') {
                warn(`<template> cannot be keyed. Place the key on real elements instead.`, getRawBindingAttr(el, 'key'));
            }
            if (el.for) {
                const iterator = el.iterator2 || el.iterator1;
                const parent = el.parent;
                if (iterator &&
                    iterator === exp &&
                    parent &&
                    parent.tag === 'transition-group') {
                    warn(`Do not use v-for index as key on <transition-group> children, ` +
                        `this is the same as not using keys.`, getRawBindingAttr(el, 'key'), true /* tip */);
                }
            }
        }
        el.key = exp;
    }
}
function processRef(el) {
    const ref = getBindingAttr(el, 'ref');
    if (ref) {
        el.ref = ref;
        el.refInFor = checkInFor(el);
    }
}
function processFor(el) {
    let exp;
    if ((exp = getAndRemoveAttr(el, 'v-for'))) {
        const res = parseFor(exp);
        if (res) {
            extend(el, res);
        }
        else {
            warn(`Invalid v-for expression: ${exp}`, el.rawAttrsMap['v-for']);
        }
    }
}
function parseFor(exp) {
    const inMatch = exp.match(forAliasRE);
    if (!inMatch)
        return;
    const res = {};
    res.for = inMatch[2].trim();
    const alias = inMatch[1].trim().replace(stripParensRE, '');
    const iteratorMatch = alias.match(forIteratorRE);
    if (iteratorMatch) {
        res.alias = alias.replace(forIteratorRE, '').trim();
        res.iterator1 = iteratorMatch[1].trim();
        if (iteratorMatch[2]) {
            res.iterator2 = iteratorMatch[2].trim();
        }
    }
    else {
        res.alias = alias;
    }
    return res;
}
function processIf(el) {
    const exp = getAndRemoveAttr(el, 'v-if');
    if (exp) {
        el.if = exp;
        addIfCondition(el, {
            exp: exp,
            block: el
        });
    }
    else {
        if (getAndRemoveAttr(el, 'v-else') != null) {
            el.else = true;
        }
        const elseif = getAndRemoveAttr(el, 'v-else-if');
        if (elseif) {
            el.elseif = elseif;
        }
    }
}
function processIfConditions(el, parent) {
    const prev = findPrevElement(parent.children);
    if (prev && prev.if) {
        addIfCondition(prev, {
            exp: el.elseif,
            block: el
        });
    }
    else {
        warn(`v-${el.elseif ? 'else-if="' + el.elseif + '"' : 'else'} ` +
            `used on element <${el.tag}> without corresponding v-if.`, el.rawAttrsMap[el.elseif ? 'v-else-if' : 'v-else']);
    }
}
function findPrevElement(children) {
    let i = children.length;
    while (i--) {
        if (children[i].type === 1) {
            return children[i];
        }
        else {
            if (children[i].text !== ' ') {
                warn(`text "${children[i].text.trim()}" between v-if and v-else(-if) ` +
                    `will be ignored.`, children[i]);
            }
            children.pop();
        }
    }
}
function addIfCondition(el, condition) {
    if (!el.ifConditions) {
        el.ifConditions = [];
    }
    el.ifConditions.push(condition);
}
function processOnce(el) {
    const once = getAndRemoveAttr(el, 'v-once');
    if (once != null) {
        el.once = true;
    }
}
// handle content being passed to a component as slot,
// e.g. <template slot="xxx">, <div slot-scope="xxx">
function processSlotContent(el) {
    let slotScope;
    if (el.tag === 'template') {
        slotScope = getAndRemoveAttr(el, 'scope');
        /* istanbul ignore if */
        if (slotScope) {
            warn(`the "scope" attribute for scoped slots have been deprecated and ` +
                `replaced by "slot-scope" since 2.5. The new "slot-scope" attribute ` +
                `can also be used on plain elements in addition to <template> to ` +
                `denote scoped slots.`, el.rawAttrsMap['scope'], true);
        }
        el.slotScope = slotScope || getAndRemoveAttr(el, 'slot-scope');
    }
    else if ((slotScope = getAndRemoveAttr(el, 'slot-scope'))) {
        /* istanbul ignore if */
        if (el.attrsMap['v-for']) {
            warn(`Ambiguous combined usage of slot-scope and v-for on <${el.tag}> ` +
                `(v-for takes higher priority). Use a wrapper <template> for the ` +
                `scoped slot to make it clearer.`, el.rawAttrsMap['slot-scope'], true);
        }
        el.slotScope = slotScope;
    }
    // slot="xxx"
    const slotTarget = getBindingAttr(el, 'slot');
    if (slotTarget) {
        el.slotTarget = slotTarget === '""' ? '"default"' : slotTarget;
        el.slotTargetDynamic = !!(el.attrsMap[':slot'] || el.attrsMap['v-bind:slot']);
        // preserve slot as an attribute for native shadow DOM compat
        // only for non-scoped slots.
        if (el.tag !== 'template' && !el.slotScope) {
            addAttr(el, 'slot', slotTarget, getRawBindingAttr(el, 'slot'));
        }
    }
    // 2.6 v-slot syntax
    {
        if (el.tag === 'template') {
            // v-slot on <template>
            const slotBinding = getAndRemoveAttrByRegex(el, slotRE);
            if (slotBinding) {
                {
                    if (el.slotTarget || el.slotScope) {
                        warn(`Unexpected mixed usage of different slot syntaxes.`, el);
                    }
                    if (el.parent && !maybeComponent(el.parent)) {
                        warn(`<template v-slot> can only appear at the root level inside ` +
                            `the receiving component`, el);
                    }
                }
                const { name, dynamic } = getSlotName(slotBinding);
                el.slotTarget = name;
                el.slotTargetDynamic = dynamic;
                el.slotScope = slotBinding.value || emptySlotScopeToken; // force it into a scoped slot for perf
            }
        }
        else {
            // v-slot on component, denotes default slot
            const slotBinding = getAndRemoveAttrByRegex(el, slotRE);
            if (slotBinding) {
                {
                    if (!maybeComponent(el)) {
                        warn(`v-slot can only be used on components or <template>.`, slotBinding);
                    }
                    if (el.slotScope || el.slotTarget) {
                        warn(`Unexpected mixed usage of different slot syntaxes.`, el);
                    }
                    if (el.scopedSlots) {
                        warn(`To avoid scope ambiguity, the default slot should also use ` +
                            `<template> syntax when there are other named slots.`, slotBinding);
                    }
                }
                // add the component's children to its default slot
                const slots = el.scopedSlots || (el.scopedSlots = {});
                const { name, dynamic } = getSlotName(slotBinding);
                const slotContainer = (slots[name] = createASTElement('template', [], el));
                slotContainer.slotTarget = name;
                slotContainer.slotTargetDynamic = dynamic;
                slotContainer.children = el.children.filter((c) => {
                    if (!c.slotScope) {
                        c.parent = slotContainer;
                        return true;
                    }
                });
                slotContainer.slotScope = slotBinding.value || emptySlotScopeToken;
                // remove children as they are returned from scopedSlots now
                el.children = [];
                // mark el non-plain so data gets generated
                el.plain = false;
            }
        }
    }
}
function getSlotName(binding) {
    let name = binding.name.replace(slotRE, '');
    if (!name) {
        if (binding.name[0] !== '#') {
            name = 'default';
        }
        else {
            warn(`v-slot shorthand syntax requires a slot name.`, binding);
        }
    }
    return dynamicArgRE.test(name)
        ? // dynamic [name]
            { name: name.slice(1, -1), dynamic: true }
        : // static name
            { name: `"${name}"`, dynamic: false };
}
// handle <slot/> outlets
function processSlotOutlet(el) {
    if (el.tag === 'slot') {
        el.slotName = getBindingAttr(el, 'name');
        if (el.key) {
            warn(`\`key\` does not work on <slot> because slots are abstract outlets ` +
                `and can possibly expand into multiple elements. ` +
                `Use the key on a wrapping element instead.`, getRawBindingAttr(el, 'key'));
        }
    }
}
function processComponent(el) {
    let binding;
    if ((binding = getBindingAttr(el, 'is'))) {
        el.component = binding;
    }
    if (getAndRemoveAttr(el, 'inline-template') != null) {
        el.inlineTemplate = true;
    }
}
function processAttrs(el) {
    const list = el.attrsList;
    let i, l, name, rawName, value, modifiers, syncGen, isDynamic;
    for (i = 0, l = list.length; i < l; i++) {
        name = rawName = list[i].name;
        value = list[i].value;
        if (dirRE.test(name)) {
            // mark element as dynamic
            el.hasBindings = true;
            // modifiers
            modifiers = parseModifiers(name.replace(dirRE, ''));
            // support .foo shorthand syntax for the .prop modifier
            if (modifiers) {
                name = name.replace(modifierRE, '');
            }
            if (bindRE.test(name)) {
                // v-bind
                name = name.replace(bindRE, '');
                value = parseFilters(value);
                isDynamic = dynamicArgRE.test(name);
                if (isDynamic) {
                    name = name.slice(1, -1);
                }
                if (value.trim().length === 0) {
                    warn(`The value for a v-bind expression cannot be empty. Found in "v-bind:${name}"`);
                }
                if (modifiers) {
                    if (modifiers.prop && !isDynamic) {
                        name = camelize(name);
                        if (name === 'innerHtml')
                            name = 'innerHTML';
                    }
                    if (modifiers.camel && !isDynamic) {
                        name = camelize(name);
                    }
                    if (modifiers.sync) {
                        syncGen = genAssignmentCode(value, `$event`);
                        if (!isDynamic) {
                            addHandler(el, `update:${camelize(name)}`, syncGen, null, false, warn, list[i]);
                            if (hyphenate(name) !== camelize(name)) {
                                addHandler(el, `update:${hyphenate(name)}`, syncGen, null, false, warn, list[i]);
                            }
                        }
                        else {
                            // handler w/ dynamic event name
                            addHandler(el, `"update:"+(${name})`, syncGen, null, false, warn, list[i], true // dynamic
                            );
                        }
                    }
                }
                if ((modifiers && modifiers.prop) ||
                    (!el.component && platformMustUseProp(el.tag, el.attrsMap.type, name))) {
                    addProp(el, name, value, list[i], isDynamic);
                }
                else {
                    addAttr(el, name, value, list[i], isDynamic);
                }
            }
            else if (onRE.test(name)) {
                // v-on
                name = name.replace(onRE, '');
                isDynamic = dynamicArgRE.test(name);
                if (isDynamic) {
                    name = name.slice(1, -1);
                }
                addHandler(el, name, value, modifiers, false, warn, list[i], isDynamic);
            }
            else {
                // normal directives
                name = name.replace(dirRE, '');
                // parse arg
                const argMatch = name.match(argRE);
                let arg = argMatch && argMatch[1];
                isDynamic = false;
                if (arg) {
                    name = name.slice(0, -(arg.length + 1));
                    if (dynamicArgRE.test(arg)) {
                        arg = arg.slice(1, -1);
                        isDynamic = true;
                    }
                }
                addDirective(el, name, rawName, value, arg, isDynamic, modifiers, list[i]);
                if (name === 'model') {
                    checkForAliasModel(el, value);
                }
            }
        }
        else {
            // literal attribute
            {
                const res = parseText(value, delimiters);
                if (res) {
                    warn(`${name}="${value}": ` +
                        'Interpolation inside attributes has been removed. ' +
                        'Use v-bind or the colon shorthand instead. For example, ' +
                        'instead of <div id="{{ val }}">, use <div :id="val">.', list[i]);
                }
            }
            addAttr(el, name, JSON.stringify(value), list[i]);
            // #6887 firefox doesn't update muted state if set via attribute
            // even immediately after element creation
            if (!el.component &&
                name === 'muted' &&
                platformMustUseProp(el.tag, el.attrsMap.type, name)) {
                addProp(el, name, 'true', list[i]);
            }
        }
    }
}
function checkInFor(el) {
    let parent = el;
    while (parent) {
        if (parent.for !== undefined) {
            return true;
        }
        parent = parent.parent;
    }
    return false;
}
function parseModifiers(name) {
    const match = name.match(modifierRE);
    if (match) {
        const ret = {};
        match.forEach(m => {
            ret[m.slice(1)] = true;
        });
        return ret;
    }
}
function makeAttrsMap(attrs) {
    const map = {};
    for (let i = 0, l = attrs.length; i < l; i++) {
        if (map[attrs[i].name] && !isIE && !isEdge) {
            warn('duplicate attribute: ' + attrs[i].name, attrs[i]);
        }
        map[attrs[i].name] = attrs[i].value;
    }
    return map;
}
// for script (e.g. type="x/template") or style, do not decode content
function isTextTag(el) {
    return el.tag === 'script' || el.tag === 'style';
}
function isForbiddenTag(el) {
    return (el.tag === 'style' ||
        (el.tag === 'script' &&
            (!el.attrsMap.type || el.attrsMap.type === 'text/javascript')));
}
const ieNSBug = /^xmlns:NS\d+/;
const ieNSPrefix = /^NS\d+:/;
/* istanbul ignore next */
function guardIESVGBug(attrs) {
    const res = [];
    for (let i = 0; i < attrs.length; i++) {
        const attr = attrs[i];
        if (!ieNSBug.test(attr.name)) {
            attr.name = attr.name.replace(ieNSPrefix, '');
            res.push(attr);
        }
    }
    return res;
}
function checkForAliasModel(el, value) {
    let _el = el;
    while (_el) {
        if (_el.for && _el.alias === value) {
            warn(`<${el.tag} v-model="${value}">: ` +
                `You are binding v-model directly to a v-for iteration alias. ` +
                `This will not be able to modify the v-for source array because ` +
                `writing to the alias is like modifying a function local variable. ` +
                `Consider using an array of objects and use v-model on an object property instead.`, el.rawAttrsMap['v-model']);
        }
        _el = _el.parent;
    }
}

/**
 * Expand input[v-model] with dynamic type bindings into v-if-else chains
 * Turn this:
 *   <input v-model="data[type]" :type="type">
 * into this:
 *   <input v-if="type === 'checkbox'" type="checkbox" v-model="data[type]">
 *   <input v-else-if="type === 'radio'" type="radio" v-model="data[type]">
 *   <input v-else :type="type" v-model="data[type]">
 */
function preTransformNode(el, options) {
    if (el.tag === 'input') {
        const map = el.attrsMap;
        if (!map['v-model']) {
            return;
        }
        let typeBinding;
        if (map[':type'] || map['v-bind:type']) {
            typeBinding = getBindingAttr(el, 'type');
        }
        if (!map.type && !typeBinding && map['v-bind']) {
            typeBinding = `(${map['v-bind']}).type`;
        }
        if (typeBinding) {
            const ifCondition = getAndRemoveAttr(el, 'v-if', true);
            const ifConditionExtra = ifCondition ? `&&(${ifCondition})` : ``;
            const hasElse = getAndRemoveAttr(el, 'v-else', true) != null;
            const elseIfCondition = getAndRemoveAttr(el, 'v-else-if', true);
            // 1. checkbox
            const branch0 = cloneASTElement(el);
            // process for on the main node
            processFor(branch0);
            addRawAttr(branch0, 'type', 'checkbox');
            processElement(branch0, options);
            branch0.processed = true; // prevent it from double-processed
            branch0.if = `(${typeBinding})==='checkbox'` + ifConditionExtra;
            addIfCondition(branch0, {
                exp: branch0.if,
                block: branch0
            });
            // 2. add radio else-if condition
            const branch1 = cloneASTElement(el);
            getAndRemoveAttr(branch1, 'v-for', true);
            addRawAttr(branch1, 'type', 'radio');
            processElement(branch1, options);
            addIfCondition(branch0, {
                exp: `(${typeBinding})==='radio'` + ifConditionExtra,
                block: branch1
            });
            // 3. other
            const branch2 = cloneASTElement(el);
            getAndRemoveAttr(branch2, 'v-for', true);
            addRawAttr(branch2, ':type', typeBinding);
            processElement(branch2, options);
            addIfCondition(branch0, {
                exp: ifCondition,
                block: branch2
            });
            if (hasElse) {
                branch0.else = true;
            }
            else if (elseIfCondition) {
                branch0.elseif = elseIfCondition;
            }
            return branch0;
        }
    }
}
function cloneASTElement(el) {
    return createASTElement(el.tag, el.attrsList.slice(), el.parent);
}
var model = {
    preTransformNode
};

var modules = [klass, style, model];

function text(el, dir) {
    if (dir.value) {
        addProp(el, 'textContent', `_s(${dir.value})`, dir);
    }
}

function html(el, dir) {
    if (dir.value) {
        addProp(el, 'innerHTML', `_s(${dir.value})`, dir);
    }
}

var directives = {
    model: model$1,
    text,
    html
};

const baseOptions = {
    expectHTML: true,
    modules,
    directives,
    isPreTag,
    isUnaryTag,
    mustUseProp,
    canBeLeftOpenTag,
    isReservedTag,
    getTagNamespace,
    staticKeys: genStaticKeys$1(modules)
};

let isStaticKey;
let isPlatformReservedTag;
const genStaticKeysCached = cached(genStaticKeys);
/**
 * Goal of the optimizer: walk the generated template AST tree
 * and detect sub-trees that are purely static, i.e. parts of
 * the DOM that never needs to change.
 *
 * Once we detect these sub-trees, we can:
 *
 * 1. Hoist them into constants, so that we no longer need to
 *    create fresh nodes for them on each re-render;
 * 2. Completely skip them in the patching process.
 */
function optimize(root, options) {
    if (!root)
        return;
    isStaticKey = genStaticKeysCached(options.staticKeys || '');
    isPlatformReservedTag = options.isReservedTag || no;
    // first pass: mark all non-static nodes.
    markStatic(root);
    // second pass: mark static roots.
    markStaticRoots(root, false);
}
function genStaticKeys(keys) {
    return makeMap('type,tag,attrsList,attrsMap,plain,parent,children,attrs,start,end,rawAttrsMap' +
        (keys ? ',' + keys : ''));
}
function markStatic(node) {
    node.static = isStatic(node);
    if (node.type === 1) {
        // do not make component slot content static. this avoids
        // 1. components not able to mutate slot nodes
        // 2. static slot content fails for hot-reloading
        if (!isPlatformReservedTag(node.tag) &&
            node.tag !== 'slot' &&
            node.attrsMap['inline-template'] == null) {
            return;
        }
        for (let i = 0, l = node.children.length; i < l; i++) {
            const child = node.children[i];
            markStatic(child);
            if (!child.static) {
                node.static = false;
            }
        }
        if (node.ifConditions) {
            for (let i = 1, l = node.ifConditions.length; i < l; i++) {
                const block = node.ifConditions[i].block;
                markStatic(block);
                if (!block.static) {
                    node.static = false;
                }
            }
        }
    }
}
function markStaticRoots(node, isInFor) {
    if (node.type === 1) {
        if (node.static || node.once) {
            node.staticInFor = isInFor;
        }
        // For a node to qualify as a static root, it should have children that
        // are not just static text. Otherwise the cost of hoisting out will
        // outweigh the benefits and it's better off to just always render it fresh.
        if (node.static &&
            node.children.length &&
            !(node.children.length === 1 && node.children[0].type === 3)) {
            node.staticRoot = true;
            return;
        }
        else {
            node.staticRoot = false;
        }
        if (node.children) {
            for (let i = 0, l = node.children.length; i < l; i++) {
                markStaticRoots(node.children[i], isInFor || !!node.for);
            }
        }
        if (node.ifConditions) {
            for (let i = 1, l = node.ifConditions.length; i < l; i++) {
                markStaticRoots(node.ifConditions[i].block, isInFor);
            }
        }
    }
}
function isStatic(node) {
    if (node.type === 2) {
        // expression
        return false;
    }
    if (node.type === 3) {
        // text
        return true;
    }
    return !!(node.pre ||
        (!node.hasBindings && // no dynamic bindings
            !node.if &&
            !node.for && // not v-if or v-for or v-else
            !isBuiltInTag(node.tag) && // not a built-in
            isPlatformReservedTag(node.tag) && // not a component
            !isDirectChildOfTemplateFor(node) &&
            Object.keys(node).every(isStaticKey)));
}
function isDirectChildOfTemplateFor(node) {
    while (node.parent) {
        node = node.parent;
        if (node.tag !== 'template') {
            return false;
        }
        if (node.for) {
            return true;
        }
    }
    return false;
}

const fnExpRE = /^([\w$_]+|\([^)]*?\))\s*=>|^function(?:\s+[\w$]+)?\s*\(/;
const fnInvokeRE = /\([^)]*?\);*$/;
const simplePathRE = /^[A-Za-z_$][\w$]*(?:\.[A-Za-z_$][\w$]*|\['[^']*?']|\["[^"]*?"]|\[\d+]|\[[A-Za-z_$][\w$]*])*$/;
// KeyboardEvent.keyCode aliases
const keyCodes = {
    esc: 27,
    tab: 9,
    enter: 13,
    space: 32,
    up: 38,
    left: 37,
    right: 39,
    down: 40,
    delete: [8, 46]
};
// KeyboardEvent.key aliases
const keyNames = {
    // #7880: IE11 and Edge use `Esc` for Escape key name.
    esc: ['Esc', 'Escape'],
    tab: 'Tab',
    enter: 'Enter',
    // #9112: IE11 uses `Spacebar` for Space key name.
    space: [' ', 'Spacebar'],
    // #7806: IE11 uses key names without `Arrow` prefix for arrow keys.
    up: ['Up', 'ArrowUp'],
    left: ['Left', 'ArrowLeft'],
    right: ['Right', 'ArrowRight'],
    down: ['Down', 'ArrowDown'],
    // #9112: IE11 uses `Del` for Delete key name.
    delete: ['Backspace', 'Delete', 'Del']
};
// #4868: modifiers that prevent the execution of the listener
// need to explicitly return null so that we can determine whether to remove
// the listener for .once
const genGuard = condition => `if(${condition})return null;`;
const modifierCode = {
    stop: '$event.stopPropagation();',
    prevent: '$event.preventDefault();',
    self: genGuard(`$event.target !== $event.currentTarget`),
    ctrl: genGuard(`!$event.ctrlKey`),
    shift: genGuard(`!$event.shiftKey`),
    alt: genGuard(`!$event.altKey`),
    meta: genGuard(`!$event.metaKey`),
    left: genGuard(`'button' in $event && $event.button !== 0`),
    middle: genGuard(`'button' in $event && $event.button !== 1`),
    right: genGuard(`'button' in $event && $event.button !== 2`)
};
function genHandlers(events, isNative) {
    const prefix = isNative ? 'nativeOn:' : 'on:';
    let staticHandlers = ``;
    let dynamicHandlers = ``;
    for (const name in events) {
        const handlerCode = genHandler(events[name]);
        //@ts-expect-error
        if (events[name] && events[name].dynamic) {
            dynamicHandlers += `${name},${handlerCode},`;
        }
        else {
            staticHandlers += `"${name}":${handlerCode},`;
        }
    }
    staticHandlers = `{${staticHandlers.slice(0, -1)}}`;
    if (dynamicHandlers) {
        return prefix + `_d(${staticHandlers},[${dynamicHandlers.slice(0, -1)}])`;
    }
    else {
        return prefix + staticHandlers;
    }
}
function genHandler(handler) {
    if (!handler) {
        return 'function(){}';
    }
    if (Array.isArray(handler)) {
        return `[${handler.map(handler => genHandler(handler)).join(',')}]`;
    }
    const isMethodPath = simplePathRE.test(handler.value);
    const isFunctionExpression = fnExpRE.test(handler.value);
    const isFunctionInvocation = simplePathRE.test(handler.value.replace(fnInvokeRE, ''));
    if (!handler.modifiers) {
        if (isMethodPath || isFunctionExpression) {
            return handler.value;
        }
        return `function($event){${isFunctionInvocation ? `return ${handler.value}` : handler.value}}`; // inline statement
    }
    else {
        let code = '';
        let genModifierCode = '';
        const keys = [];
        for (const key in handler.modifiers) {
            if (modifierCode[key]) {
                genModifierCode += modifierCode[key];
                // left/right
                if (keyCodes[key]) {
                    keys.push(key);
                }
            }
            else if (key === 'exact') {
                const modifiers = handler.modifiers;
                genModifierCode += genGuard(['ctrl', 'shift', 'alt', 'meta']
                    .filter(keyModifier => !modifiers[keyModifier])
                    .map(keyModifier => `$event.${keyModifier}Key`)
                    .join('||'));
            }
            else {
                keys.push(key);
            }
        }
        if (keys.length) {
            code += genKeyFilter(keys);
        }
        // Make sure modifiers like prevent and stop get executed after key filtering
        if (genModifierCode) {
            code += genModifierCode;
        }
        const handlerCode = isMethodPath
            ? `return ${handler.value}.apply(null, arguments)`
            : isFunctionExpression
                ? `return (${handler.value}).apply(null, arguments)`
                : isFunctionInvocation
                    ? `return ${handler.value}`
                    : handler.value;
        return `function($event){${code}${handlerCode}}`;
    }
}
function genKeyFilter(keys) {
    return (
    // make sure the key filters only apply to KeyboardEvents
    // #9441: can't use 'keyCode' in $event because Chrome autofill fires fake
    // key events that do not have keyCode property...
    `if(!$event.type.indexOf('key')&&` +
        `${keys.map(genFilterCode).join('&&')})return null;`);
}
function genFilterCode(key) {
    const keyVal = parseInt(key, 10);
    if (keyVal) {
        return `$event.keyCode!==${keyVal}`;
    }
    const keyCode = keyCodes[key];
    const keyName = keyNames[key];
    return (`_k($event.keyCode,` +
        `${JSON.stringify(key)},` +
        `${JSON.stringify(keyCode)},` +
        `$event.key,` +
        `${JSON.stringify(keyName)}` +
        `)`);
}

function on(el, dir) {
    if (dir.modifiers) {
        warn$2(`v-on without argument does not support modifiers.`);
    }
    el.wrapListeners = (code) => `_g(${code},${dir.value})`;
}

function bind(el, dir) {
    el.wrapData = (code) => {
        return `_b(${code},'${el.tag}',${dir.value},${dir.modifiers && dir.modifiers.prop ? 'true' : 'false'}${dir.modifiers && dir.modifiers.sync ? ',true' : ''})`;
    };
}

var baseDirectives = {
    on,
    bind,
    cloak: noop
};

class CodegenState {
    constructor(options) {
        this.options = options;
        this.warn = options.warn || baseWarn;
        this.transforms = pluckModuleFunction(options.modules, 'transformCode');
        this.dataGenFns = pluckModuleFunction(options.modules, 'genData');
        this.directives = extend(extend({}, baseDirectives), options.directives);
        const isReservedTag = options.isReservedTag || no;
        this.maybeComponent = (el) => !!el.component || !isReservedTag(el.tag);
        this.onceId = 0;
        this.staticRenderFns = [];
        this.pre = false;
    }
}
function generate(ast, options) {
    const state = new CodegenState(options);
    // fix #11483, Root level <script> tags should not be rendered.
    const code = ast
        ? ast.tag === 'script'
            ? 'null'
            : genElement(ast, state)
        : '_c("div")';
    return {
        render: `with(this){return ${code}}`,
        staticRenderFns: state.staticRenderFns
    };
}
function genElement(el, state) {
    if (el.parent) {
        el.pre = el.pre || el.parent.pre;
    }
    if (el.staticRoot && !el.staticProcessed) {
        return genStatic(el, state);
    }
    else if (el.once && !el.onceProcessed) {
        return genOnce(el, state);
    }
    else if (el.for && !el.forProcessed) {
        return genFor(el, state);
    }
    else if (el.if && !el.ifProcessed) {
        return genIf(el, state);
    }
    else if (el.tag === 'template' && !el.slotTarget && !state.pre) {
        return genChildren(el, state) || 'void 0';
    }
    else if (el.tag === 'slot') {
        return genSlot(el, state);
    }
    else {
        // component or element
        let code;
        if (el.component) {
            code = genComponent(el.component, el, state);
        }
        else {
            let data;
            const maybeComponent = state.maybeComponent(el);
            if (!el.plain || (el.pre && maybeComponent)) {
                data = genData(el, state);
            }
            let tag;
            // check if this is a component in <script setup>
            const bindings = state.options.bindings;
            if (maybeComponent && bindings && bindings.__isScriptSetup !== false) {
                tag = checkBindingType(bindings, el.tag);
            }
            if (!tag)
                tag = `'${el.tag}'`;
            const children = el.inlineTemplate ? null : genChildren(el, state, true);
            code = `_c(${tag}${data ? `,${data}` : '' // data
            }${children ? `,${children}` : '' // children
            })`;
        }
        // module transforms
        for (let i = 0; i < state.transforms.length; i++) {
            code = state.transforms[i](el, code);
        }
        return code;
    }
}
function checkBindingType(bindings, key) {
    const camelName = camelize(key);
    const PascalName = capitalize(camelName);
    const checkType = (type) => {
        if (bindings[key] === type) {
            return key;
        }
        if (bindings[camelName] === type) {
            return camelName;
        }
        if (bindings[PascalName] === type) {
            return PascalName;
        }
    };
    const fromConst = checkType("setup-const" /* BindingTypes.SETUP_CONST */) ||
        checkType("setup-reactive-const" /* BindingTypes.SETUP_REACTIVE_CONST */);
    if (fromConst) {
        return fromConst;
    }
    const fromMaybeRef = checkType("setup-let" /* BindingTypes.SETUP_LET */) ||
        checkType("setup-ref" /* BindingTypes.SETUP_REF */) ||
        checkType("setup-maybe-ref" /* BindingTypes.SETUP_MAYBE_REF */);
    if (fromMaybeRef) {
        return fromMaybeRef;
    }
}
// hoist static sub-trees out
function genStatic(el, state) {
    el.staticProcessed = true;
    // Some elements (templates) need to behave differently inside of a v-pre
    // node.  All pre nodes are static roots, so we can use this as a location to
    // wrap a state change and reset it upon exiting the pre node.
    const originalPreState = state.pre;
    if (el.pre) {
        state.pre = el.pre;
    }
    state.staticRenderFns.push(`with(this){return ${genElement(el, state)}}`);
    state.pre = originalPreState;
    return `_m(${state.staticRenderFns.length - 1}${el.staticInFor ? ',true' : ''})`;
}
// v-once
function genOnce(el, state) {
    el.onceProcessed = true;
    if (el.if && !el.ifProcessed) {
        return genIf(el, state);
    }
    else if (el.staticInFor) {
        let key = '';
        let parent = el.parent;
        while (parent) {
            if (parent.for) {
                key = parent.key;
                break;
            }
            parent = parent.parent;
        }
        if (!key) {
            state.warn(`v-once can only be used inside v-for that is keyed. `, el.rawAttrsMap['v-once']);
            return genElement(el, state);
        }
        return `_o(${genElement(el, state)},${state.onceId++},${key})`;
    }
    else {
        return genStatic(el, state);
    }
}
function genIf(el, state, altGen, altEmpty) {
    el.ifProcessed = true; // avoid recursion
    return genIfConditions(el.ifConditions.slice(), state, altGen, altEmpty);
}
function genIfConditions(conditions, state, altGen, altEmpty) {
    if (!conditions.length) {
        return altEmpty || '_e()';
    }
    const condition = conditions.shift();
    if (condition.exp) {
        return `(${condition.exp})?${genTernaryExp(condition.block)}:${genIfConditions(conditions, state, altGen, altEmpty)}`;
    }
    else {
        return `${genTernaryExp(condition.block)}`;
    }
    // v-if with v-once should generate code like (a)?_m(0):_m(1)
    function genTernaryExp(el) {
        return altGen
            ? altGen(el, state)
            : el.once
                ? genOnce(el, state)
                : genElement(el, state);
    }
}
function genFor(el, state, altGen, altHelper) {
    const exp = el.for;
    const alias = el.alias;
    const iterator1 = el.iterator1 ? `,${el.iterator1}` : '';
    const iterator2 = el.iterator2 ? `,${el.iterator2}` : '';
    if (state.maybeComponent(el) &&
        el.tag !== 'slot' &&
        el.tag !== 'template' &&
        !el.key) {
        state.warn(`<${el.tag} v-for="${alias} in ${exp}">: component lists rendered with ` +
            `v-for should have explicit keys. ` +
            `See https://v2.vuejs.org/v2/guide/list.html#key for more info.`, el.rawAttrsMap['v-for'], true /* tip */);
    }
    el.forProcessed = true; // avoid recursion
    return (`${altHelper || '_l'}((${exp}),` +
        `function(${alias}${iterator1}${iterator2}){` +
        `return ${(altGen || genElement)(el, state)}` +
        '})');
}
function genData(el, state) {
    let data = '{';
    // directives first.
    // directives may mutate the el's other properties before they are generated.
    const dirs = genDirectives(el, state);
    if (dirs)
        data += dirs + ',';
    // key
    if (el.key) {
        data += `key:${el.key},`;
    }
    // ref
    if (el.ref) {
        data += `ref:${el.ref},`;
    }
    if (el.refInFor) {
        data += `refInFor:true,`;
    }
    // pre
    if (el.pre) {
        data += `pre:true,`;
    }
    // record original tag name for components using "is" attribute
    if (el.component) {
        data += `tag:"${el.tag}",`;
    }
    // module data generation functions
    for (let i = 0; i < state.dataGenFns.length; i++) {
        data += state.dataGenFns[i](el);
    }
    // attributes
    if (el.attrs) {
        data += `attrs:${genProps(el.attrs)},`;
    }
    // DOM props
    if (el.props) {
        data += `domProps:${genProps(el.props)},`;
    }
    // event handlers
    if (el.events) {
        data += `${genHandlers(el.events, false)},`;
    }
    if (el.nativeEvents) {
        data += `${genHandlers(el.nativeEvents, true)},`;
    }
    // slot target
    // only for non-scoped slots
    if (el.slotTarget && !el.slotScope) {
        data += `slot:${el.slotTarget},`;
    }
    // scoped slots
    if (el.scopedSlots) {
        data += `${genScopedSlots(el, el.scopedSlots, state)},`;
    }
    // component v-model
    if (el.model) {
        data += `model:{value:${el.model.value},callback:${el.model.callback},expression:${el.model.expression}},`;
    }
    // inline-template
    if (el.inlineTemplate) {
        const inlineTemplate = genInlineTemplate(el, state);
        if (inlineTemplate) {
            data += `${inlineTemplate},`;
        }
    }
    data = data.replace(/,$/, '') + '}';
    // v-bind dynamic argument wrap
    // v-bind with dynamic arguments must be applied using the same v-bind object
    // merge helper so that class/style/mustUseProp attrs are handled correctly.
    if (el.dynamicAttrs) {
        data = `_b(${data},"${el.tag}",${genProps(el.dynamicAttrs)})`;
    }
    // v-bind data wrap
    if (el.wrapData) {
        data = el.wrapData(data);
    }
    // v-on data wrap
    if (el.wrapListeners) {
        data = el.wrapListeners(data);
    }
    return data;
}
function genDirectives(el, state) {
    const dirs = el.directives;
    if (!dirs)
        return;
    let res = 'directives:[';
    let hasRuntime = false;
    let i, l, dir, needRuntime;
    for (i = 0, l = dirs.length; i < l; i++) {
        dir = dirs[i];
        needRuntime = true;
        const gen = state.directives[dir.name];
        if (gen) {
            // compile-time directive that manipulates AST.
            // returns true if it also needs a runtime counterpart.
            needRuntime = !!gen(el, dir, state.warn);
        }
        if (needRuntime) {
            hasRuntime = true;
            res += `{name:"${dir.name}",rawName:"${dir.rawName}"${dir.value
                ? `,value:(${dir.value}),expression:${JSON.stringify(dir.value)}`
                : ''}${dir.arg ? `,arg:${dir.isDynamicArg ? dir.arg : `"${dir.arg}"`}` : ''}${dir.modifiers ? `,modifiers:${JSON.stringify(dir.modifiers)}` : ''}},`;
        }
    }
    if (hasRuntime) {
        return res.slice(0, -1) + ']';
    }
}
function genInlineTemplate(el, state) {
    const ast = el.children[0];
    if ((el.children.length !== 1 || ast.type !== 1)) {
        state.warn('Inline-template components must have exactly one child element.', { start: el.start });
    }
    if (ast && ast.type === 1) {
        const inlineRenderFns = generate(ast, state.options);
        return `inlineTemplate:{render:function(){${inlineRenderFns.render}},staticRenderFns:[${inlineRenderFns.staticRenderFns
            .map(code => `function(){${code}}`)
            .join(',')}]}`;
    }
}
function genScopedSlots(el, slots, state) {
    // by default scoped slots are considered "stable", this allows child
    // components with only scoped slots to skip forced updates from parent.
    // but in some cases we have to bail-out of this optimization
    // for example if the slot contains dynamic names, has v-if or v-for on them...
    let needsForceUpdate = el.for ||
        Object.keys(slots).some(key => {
            const slot = slots[key];
            return (slot.slotTargetDynamic || slot.if || slot.for || containsSlotChild(slot) // is passing down slot from parent which may be dynamic
            );
        });
    // #9534: if a component with scoped slots is inside a conditional branch,
    // it's possible for the same component to be reused but with different
    // compiled slot content. To avoid that, we generate a unique key based on
    // the generated code of all the slot contents.
    let needsKey = !!el.if;
    // OR when it is inside another scoped slot or v-for (the reactivity may be
    // disconnected due to the intermediate scope variable)
    // #9438, #9506
    // TODO: this can be further optimized by properly analyzing in-scope bindings
    // and skip force updating ones that do not actually use scope variables.
    if (!needsForceUpdate) {
        let parent = el.parent;
        while (parent) {
            if ((parent.slotScope && parent.slotScope !== emptySlotScopeToken) ||
                parent.for) {
                needsForceUpdate = true;
                break;
            }
            if (parent.if) {
                needsKey = true;
            }
            parent = parent.parent;
        }
    }
    const generatedSlots = Object.keys(slots)
        .map(key => genScopedSlot(slots[key], state))
        .join(',');
    return `scopedSlots:_u([${generatedSlots}]${needsForceUpdate ? `,null,true` : ``}${!needsForceUpdate && needsKey ? `,null,false,${hash(generatedSlots)}` : ``})`;
}
function hash(str) {
    let hash = 5381;
    let i = str.length;
    while (i) {
        hash = (hash * 33) ^ str.charCodeAt(--i);
    }
    return hash >>> 0;
}
function containsSlotChild(el) {
    if (el.type === 1) {
        if (el.tag === 'slot') {
            return true;
        }
        return el.children.some(containsSlotChild);
    }
    return false;
}
function genScopedSlot(el, state) {
    const isLegacySyntax = el.attrsMap['slot-scope'];
    if (el.if && !el.ifProcessed && !isLegacySyntax) {
        return genIf(el, state, genScopedSlot, `null`);
    }
    if (el.for && !el.forProcessed) {
        return genFor(el, state, genScopedSlot);
    }
    const slotScope = el.slotScope === emptySlotScopeToken ? `` : String(el.slotScope);
    const fn = `function(${slotScope}){` +
        `return ${el.tag === 'template'
            ? el.if && isLegacySyntax
                ? `(${el.if})?${genChildren(el, state) || 'undefined'}:undefined`
                : genChildren(el, state) || 'undefined'
            : genElement(el, state)}}`;
    // reverse proxy v-slot without scope on this.$slots
    const reverseProxy = slotScope ? `` : `,proxy:true`;
    return `{key:${el.slotTarget || `"default"`},fn:${fn}${reverseProxy}}`;
}
function genChildren(el, state, checkSkip, altGenElement, altGenNode) {
    const children = el.children;
    if (children.length) {
        const el = children[0];
        // optimize single v-for
        if (children.length === 1 &&
            el.for &&
            el.tag !== 'template' &&
            el.tag !== 'slot') {
            const normalizationType = checkSkip
                ? state.maybeComponent(el)
                    ? `,1`
                    : `,0`
                : ``;
            return `${(altGenElement || genElement)(el, state)}${normalizationType}`;
        }
        const normalizationType = checkSkip
            ? getNormalizationType(children, state.maybeComponent)
            : 0;
        const gen = altGenNode || genNode;
        return `[${children.map(c => gen(c, state)).join(',')}]${normalizationType ? `,${normalizationType}` : ''}`;
    }
}
// determine the normalization needed for the children array.
// 0: no normalization needed
// 1: simple normalization needed (possible 1-level deep nested array)
// 2: full normalization needed
function getNormalizationType(children, maybeComponent) {
    let res = 0;
    for (let i = 0; i < children.length; i++) {
        const el = children[i];
        if (el.type !== 1) {
            continue;
        }
        if (needsNormalization(el) ||
            (el.ifConditions &&
                el.ifConditions.some(c => needsNormalization(c.block)))) {
            res = 2;
            break;
        }
        if (maybeComponent(el) ||
            (el.ifConditions && el.ifConditions.some(c => maybeComponent(c.block)))) {
            res = 1;
        }
    }
    return res;
}
function needsNormalization(el) {
    return el.for !== undefined || el.tag === 'template' || el.tag === 'slot';
}
function genNode(node, state) {
    if (node.type === 1) {
        return genElement(node, state);
    }
    else if (node.type === 3 && node.isComment) {
        return genComment(node);
    }
    else {
        return genText(node);
    }
}
function genText(text) {
    return `_v(${text.type === 2
        ? text.expression // no need for () because already wrapped in _s()
        : transformSpecialNewlines(JSON.stringify(text.text))})`;
}
function genComment(comment) {
    return `_e(${JSON.stringify(comment.text)})`;
}
function genSlot(el, state) {
    const slotName = el.slotName || '"default"';
    const children = genChildren(el, state);
    let res = `_t(${slotName}${children ? `,function(){return ${children}}` : ''}`;
    const attrs = el.attrs || el.dynamicAttrs
        ? genProps((el.attrs || []).concat(el.dynamicAttrs || []).map(attr => ({
            // slot props are camelized
            name: camelize(attr.name),
            value: attr.value,
            dynamic: attr.dynamic
        })))
        : null;
    const bind = el.attrsMap['v-bind'];
    if ((attrs || bind) && !children) {
        res += `,null`;
    }
    if (attrs) {
        res += `,${attrs}`;
    }
    if (bind) {
        res += `${attrs ? '' : ',null'},${bind}`;
    }
    return res + ')';
}
// componentName is el.component, take it as argument to shun flow's pessimistic refinement
function genComponent(componentName, el, state) {
    const children = el.inlineTemplate ? null : genChildren(el, state, true);
    return `_c(${componentName},${genData(el, state)}${children ? `,${children}` : ''})`;
}
function genProps(props) {
    let staticProps = ``;
    let dynamicProps = ``;
    for (let i = 0; i < props.length; i++) {
        const prop = props[i];
        const value = transformSpecialNewlines(prop.value);
        if (prop.dynamic) {
            dynamicProps += `${prop.name},${value},`;
        }
        else {
            staticProps += `"${prop.name}":${value},`;
        }
    }
    staticProps = `{${staticProps.slice(0, -1)}}`;
    if (dynamicProps) {
        return `_d(${staticProps},[${dynamicProps.slice(0, -1)}])`;
    }
    else {
        return staticProps;
    }
}
// #3895, #4268
function transformSpecialNewlines(text) {
    return text.replace(/\u2028/g, '\\u2028').replace(/\u2029/g, '\\u2029');
}

// these keywords should not appear inside expressions, but operators like
// typeof, instanceof and in are allowed
const prohibitedKeywordRE = new RegExp('\\b' +
    ('do,if,for,let,new,try,var,case,else,with,await,break,catch,class,const,' +
        'super,throw,while,yield,delete,export,import,return,switch,default,' +
        'extends,finally,continue,debugger,function,arguments')
        .split(',')
        .join('\\b|\\b') +
    '\\b');
// these unary operators should not be used as property/method names
const unaryOperatorsRE = new RegExp('\\b' +
    'delete,typeof,void'.split(',').join('\\s*\\([^\\)]*\\)|\\b') +
    '\\s*\\([^\\)]*\\)');
// strip strings in expressions
const stripStringRE = /'(?:[^'\\]|\\.)*'|"(?:[^"\\]|\\.)*"|`(?:[^`\\]|\\.)*\$\{|\}(?:[^`\\]|\\.)*`|`(?:[^`\\]|\\.)*`/g;
// detect problematic expressions in a template
function detectErrors(ast, warn) {
    if (ast) {
        checkNode(ast, warn);
    }
}
function checkNode(node, warn) {
    if (node.type === 1) {
        for (const name in node.attrsMap) {
            if (dirRE.test(name)) {
                const value = node.attrsMap[name];
                if (value) {
                    const range = node.rawAttrsMap[name];
                    if (name === 'v-for') {
                        checkFor(node, `v-for="${value}"`, warn, range);
                    }
                    else if (name === 'v-slot' || name[0] === '#') {
                        checkFunctionParameterExpression(value, `${name}="${value}"`, warn, range);
                    }
                    else if (onRE.test(name)) {
                        checkEvent(value, `${name}="${value}"`, warn, range);
                    }
                    else {
                        checkExpression(value, `${name}="${value}"`, warn, range);
                    }
                }
            }
        }
        if (node.children) {
            for (let i = 0; i < node.children.length; i++) {
                checkNode(node.children[i], warn);
            }
        }
    }
    else if (node.type === 2) {
        checkExpression(node.expression, node.text, warn, node);
    }
}
function checkEvent(exp, text, warn, range) {
    const stripped = exp.replace(stripStringRE, '');
    const keywordMatch = stripped.match(unaryOperatorsRE);
    if (keywordMatch && stripped.charAt(keywordMatch.index - 1) !== '$') {
        warn(`avoid using JavaScript unary operator as property name: ` +
            `"${keywordMatch[0]}" in expression ${text.trim()}`, range);
    }
    checkExpression(exp, text, warn, range);
}
function checkFor(node, text, warn, range) {
    checkExpression(node.for || '', text, warn, range);
    checkIdentifier(node.alias, 'v-for alias', text, warn, range);
    checkIdentifier(node.iterator1, 'v-for iterator', text, warn, range);
    checkIdentifier(node.iterator2, 'v-for iterator', text, warn, range);
}
function checkIdentifier(ident, type, text, warn, range) {
    if (typeof ident === 'string') {
        try {
            new Function(`var ${ident}=_`);
        }
        catch (e) {
            warn(`invalid ${type} "${ident}" in expression: ${text.trim()}`, range);
        }
    }
}
function checkExpression(exp, text, warn, range) {
    try {
        new Function(`return ${exp}`);
    }
    catch (e) {
        const keywordMatch = exp
            .replace(stripStringRE, '')
            .match(prohibitedKeywordRE);
        if (keywordMatch) {
            warn(`avoid using JavaScript keyword as property name: ` +
                `"${keywordMatch[0]}"\n  Raw expression: ${text.trim()}`, range);
        }
        else {
            warn(`invalid expression: ${e.message} in\n\n` +
                `    ${exp}\n\n` +
                `  Raw expression: ${text.trim()}\n`, range);
        }
    }
}
function checkFunctionParameterExpression(exp, text, warn, range) {
    try {
        new Function(exp, '');
    }
    catch (e) {
        warn(`invalid function parameter expression: ${e.message} in\n\n` +
            `    ${exp}\n\n` +
            `  Raw expression: ${text.trim()}\n`, range);
    }
}

const range = 2;
function generateCodeFrame(source, start = 0, end = source.length) {
    const lines = source.split(/\r?\n/);
    let count = 0;
    const res = [];
    for (let i = 0; i < lines.length; i++) {
        count += lines[i].length + 1;
        if (count >= start) {
            for (let j = i - range; j <= i + range || end > count; j++) {
                if (j < 0 || j >= lines.length)
                    continue;
                res.push(`${j + 1}${repeat(` `, 3 - String(j + 1).length)}|  ${lines[j]}`);
                const lineLength = lines[j].length;
                if (j === i) {
                    // push underline
                    const pad = start - (count - lineLength) + 1;
                    const length = end > count ? lineLength - pad : end - start;
                    res.push(`   |  ` + repeat(` `, pad) + repeat(`^`, length));
                }
                else if (j > i) {
                    if (end > count) {
                        const length = Math.min(end - count, lineLength);
                        res.push(`   |  ` + repeat(`^`, length));
                    }
                    count += lineLength + 1;
                }
            }
            break;
        }
    }
    return res.join('\n');
}
function repeat(str, n) {
    let result = '';
    if (n > 0) {
        // eslint-disable-next-line no-constant-condition
        while (true) {
            // eslint-disable-line
            if (n & 1)
                result += str;
            n >>>= 1;
            if (n <= 0)
                break;
            str += str;
        }
    }
    return result;
}

function createFunction(code, errors) {
    try {
        return new Function(code);
    }
    catch (err) {
        errors.push({ err, code });
        return noop;
    }
}
function createCompileToFunctionFn(compile) {
    const cache = Object.create(null);
    return function compileToFunctions(template, options, vm) {
        options = extend({}, options);
        const warn = options.warn || warn$2;
        delete options.warn;
        /* istanbul ignore if */
        {
            // detect possible CSP restriction
            try {
                new Function('return 1');
            }
            catch (e) {
                if (e.toString().match(/unsafe-eval|CSP/)) {
                    warn('It seems you are using the standalone build of Vue.js in an ' +
                        'environment with Content Security Policy that prohibits unsafe-eval. ' +
                        'The template compiler cannot work in this environment. Consider ' +
                        'relaxing the policy to allow unsafe-eval or pre-compiling your ' +
                        'templates into render functions.');
                }
            }
        }
        // check cache
        const key = options.delimiters
            ? String(options.delimiters) + template
            : template;
        if (cache[key]) {
            return cache[key];
        }
        // compile
        const compiled = compile(template, options);
        // check compilation errors/tips
        {
            if (compiled.errors && compiled.errors.length) {
                if (options.outputSourceRange) {
                    compiled.errors.forEach(e => {
                        warn(`Error compiling template:\n\n${e.msg}\n\n` +
                            generateCodeFrame(template, e.start, e.end), vm);
                    });
                }
                else {
                    warn(`Error compiling template:\n\n${template}\n\n` +
                        compiled.errors.map(e => `- ${e}`).join('\n') +
                        '\n', vm);
                }
            }
            if (compiled.tips && compiled.tips.length) {
                if (options.outputSourceRange) {
                    compiled.tips.forEach(e => tip(e.msg, vm));
                }
                else {
                    compiled.tips.forEach(msg => tip(msg, vm));
                }
            }
        }
        // turn code into functions
        const res = {};
        const fnGenErrors = [];
        res.render = createFunction(compiled.render, fnGenErrors);
        res.staticRenderFns = compiled.staticRenderFns.map(code => {
            return createFunction(code, fnGenErrors);
        });
        // check function generation errors.
        // this should only happen if there is a bug in the compiler itself.
        // mostly for codegen development use
        /* istanbul ignore if */
        {
            if ((!compiled.errors || !compiled.errors.length) && fnGenErrors.length) {
                warn(`Failed to generate render function:\n\n` +
                    fnGenErrors
                        .map(({ err, code }) => `${err.toString()} in\n\n${code}\n`)
                        .join('\n'), vm);
            }
        }
        return (cache[key] = res);
    };
}

function createCompilerCreator(baseCompile) {
    return function createCompiler(baseOptions) {
        function compile(template, options) {
            const finalOptions = Object.create(baseOptions);
            const errors = [];
            const tips = [];
            let warn = (msg, range, tip) => {
                (tip ? tips : errors).push(msg);
            };
            if (options) {
                if (options.outputSourceRange) {
                    // $flow-disable-line
                    const leadingSpaceLength = template.match(/^\s*/)[0].length;
                    warn = (msg, range, tip) => {
                        const data = typeof msg === 'string' ? { msg } : msg;
                        if (range) {
                            if (range.start != null) {
                                data.start = range.start + leadingSpaceLength;
                            }
                            if (range.end != null) {
                                data.end = range.end + leadingSpaceLength;
                            }
                        }
                        (tip ? tips : errors).push(data);
                    };
                }
                // merge custom modules
                if (options.modules) {
                    finalOptions.modules = (baseOptions.modules || []).concat(options.modules);
                }
                // merge custom directives
                if (options.directives) {
                    finalOptions.directives = extend(Object.create(baseOptions.directives || null), options.directives);
                }
                // copy other options
                for (const key in options) {
                    if (key !== 'modules' && key !== 'directives') {
                        finalOptions[key] = options[key];
                    }
                }
            }
            finalOptions.warn = warn;
            const compiled = baseCompile(template.trim(), finalOptions);
            {
                detectErrors(compiled.ast, warn);
            }
            compiled.errors = errors;
            compiled.tips = tips;
            return compiled;
        }
        return {
            compile,
            compileToFunctions: createCompileToFunctionFn(compile)
        };
    };
}

// `createCompilerCreator` allows creating compilers that use alternative
// parser/optimizer/codegen, e.g the SSR optimizing compiler.
// Here we just export a default compiler using the default parts.
const createCompiler = createCompilerCreator(function baseCompile(template, options) {
    const ast = parse(template.trim(), options);
    if (options.optimize !== false) {
        optimize(ast, options);
    }
    const code = generate(ast, options);
    return {
        ast,
        render: code.render,
        staticRenderFns: code.staticRenderFns
    };
});

const { compile, compileToFunctions } = createCompiler(baseOptions);

// check whether current browser encodes a char inside attribute values
let div;
function getShouldDecode(href) {
    div = div || document.createElement('div');
    div.innerHTML = href ? `<a href="\n"/>` : `<div a="\n"/>`;
    return div.innerHTML.indexOf('&#10;') > 0;
}
// #3663: IE encodes newlines inside attribute values while other browsers don't
const shouldDecodeNewlines = inBrowser ? getShouldDecode(false) : false;
// #6828: chrome encodes content in a[href]
const shouldDecodeNewlinesForHref = inBrowser
    ? getShouldDecode(true)
    : false;

const idToTemplate = cached(id => {
    const el = query(id);
    return el && el.innerHTML;
});
const mount = Vue.prototype.$mount;
Vue.prototype.$mount = function (el, hydrating) {
    el = el && query(el);
    /* istanbul ignore if */
    if (el === document.body || el === document.documentElement) {
        warn$2(`Do not mount Vue to <html> or <body> - mount to normal elements instead.`);
        return this;
    }
    const options = this.$options;
    // resolve template/el and convert to render function
    if (!options.render) {
        let template = options.template;
        if (template) {
            if (typeof template === 'string') {
                if (template.charAt(0) === '#') {
                    template = idToTemplate(template);
                    /* istanbul ignore if */
                    if (!template) {
                        warn$2(`Template element not found or is empty: ${options.template}`, this);
                    }
                }
            }
            else if (template.nodeType) {
                template = template.innerHTML;
            }
            else {
                {
                    warn$2('invalid template option:' + template, this);
                }
                return this;
            }
        }
        else if (el) {
            // @ts-expect-error
            template = getOuterHTML(el);
        }
        if (template) {
            /* istanbul ignore if */
            if (config.performance && mark) {
                mark('compile');
            }
            const { render, staticRenderFns } = compileToFunctions(template, {
                outputSourceRange: true,
                shouldDecodeNewlines,
                shouldDecodeNewlinesForHref,
                delimiters: options.delimiters,
                comments: options.comments
            }, this);
            options.render = render;
            options.staticRenderFns = staticRenderFns;
            /* istanbul ignore if */
            if (config.performance && mark) {
                mark('compile end');
                measure(`vue ${this._name} compile`, 'compile', 'compile end');
            }
        }
    }
    return mount.call(this, el, hydrating);
};
/**
 * Get outerHTML of elements, taking care
 * of SVG elements in IE as well.
 */
function getOuterHTML(el) {
    if (el.outerHTML) {
        return el.outerHTML;
    }
    else {
        const container = document.createElement('div');
        container.appendChild(el.cloneNode(true));
        return container.innerHTML;
    }
}
Vue.compile = compileToFunctions;

// export type EffectScheduler = (...args: any[]) => any
/**
 * @internal since we are not exposing this in Vue 2, it's used only for
 * internal testing.
 */
function effect(fn, scheduler) {
    const watcher = new Watcher(currentInstance, fn, noop, {
        sync: true
    });
    if (scheduler) {
        watcher.update = () => {
            scheduler(() => watcher.run());
        };
    }
}

extend(Vue, vca);
Vue.effect = effect;

module.exports = Vue;

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js"), __webpack_require__(/*! ./../../timers-browserify/main.js */ "./node_modules/timers-browserify/main.js").setImmediate))

/***/ }),

/***/ "./node_modules/vue/dist/vue.common.js":
/*!*********************************************!*\
  !*** ./node_modules/vue/dist/vue.common.js ***!
  \*********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

if (false) {} else {
  module.exports = __webpack_require__(/*! ./vue.common.dev.js */ "./node_modules/vue/dist/vue.common.dev.js")
}


/***/ }),

/***/ "./node_modules/webpack/buildin/global.js":
/*!***********************************!*\
  !*** (webpack)/buildin/global.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || new Function("return this")();
} catch (e) {
	// This works if the window reference is available
	if (typeof window === "object") g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ }),

/***/ "./public/class_js/CatUserEspecialidad.js":
/*!************************************************!*\
  !*** ./public/class_js/CatUserEspecialidad.js ***!
  \************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return CatUserEspecialidad; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var CatUserEspecialidad = /*#__PURE__*/function () {
  function CatUserEspecialidad() {
    _classCallCheck(this, CatUserEspecialidad);
  }

  _createClass(CatUserEspecialidad, null, [{
    key: "index",
    value: function index(selector) {
      $(selector).html();
    }
  }, {
    key: "create",
    value: function create() {}
  }, {
    key: "store",
    value: function store() {}
  }, {
    key: "show",
    value: function show(model_id) {
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_especialidad/show/" + model_id);
    }
  }, {
    key: "update",
    value: function update() {}
  }, {
    key: "destroy",
    value: function destroy() {}
  }, {
    key: "create_form",
    value: function create_form() {}
  }, {
    key: "getIndex",
    value: function () {
      var _getIndex = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                _context.next = 2;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_especialidad/index");

              case 2:
                return _context.abrupt("return", _context.sent);

              case 3:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function getIndex() {
        return _getIndex.apply(this, arguments);
      }

      return getIndex;
    }()
  }, {
    key: "menuEspecialidad",
    value: function menuEspecialidad(selector) {
      $("#messageModal").modal("show");
      CatUserEspecialidad.getIndex2().then(function (response) {
        var html = "";
        $.each(response, function (indexInArray, valueOfElement) {
          if (valueOfElement['total_pacientes'] > 0) {
            html += "\n                        <a data-id=\"".concat(valueOfElement['id'], "\" data-description=\"").concat(valueOfElement['description'], "\" class=\"menu-especialidad cursor-pointer list-group-item list-group-item-action\">\n\n                                <img onerror=\"reemplaza_imagen(this)\" src=\"").concat(location.origin, "/image/system/especialidades/").concat(valueOfElement['image'], ".svg\" style=\"width:2em;height:2em;\">\n                                ").concat(valueOfElement['description'], " ").concat(valueOfElement['total_pacientes'] > 0 ? '<span class="badge rounded badge-secondary">' + valueOfElement['total_pacientes'] + '</span>' : '', "\n\n                        </a>\n                    ");
          }
        });
        $(selector).html("\n\n                <div class=\"list-group\">\n                    <span class=\"text-primary font-weight-bold menu-pad-title\"\">\n                        Pacientes por Especialidad\n                    </span>\n                    <div id=\"menuPatiencare\" style=\"max-height:75vh;overflow:auto;\">\n                    ".concat(html, "\n                    </div>\n                </div>\n\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 pl-3 pr-3 pt-1 pb-1\">\n                        <button id=\"regresar\"  data-dismiss=\"modal\" aria-label=\"Close\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                    </div>\n                </div>\n            "));
        $(".menu-especialidad").on("click", function () {
          Component.viewByEspecialidad({
            'especialidad': $(this).data("description"),
            'cat_especialidad_id': $(this).data("id")
          });
        });
      });
    }
  }, {
    key: "getIndex2",
    value: function () {
      var _getIndex2 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
          while (1) {
            switch (_context2.prev = _context2.next) {
              case 0:
                return _context2.abrupt("return", [{
                  "id": 1,
                  "description": "Alergia, Asma e Inmunología",
                  "image": "inmunologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 2,
                  "description": "Anestesiología",
                  "image": "anestesiologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 3,
                  "description": "Cardiología",
                  "image": "cardiologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 2
                }, {
                  "id": 4,
                  "description": "Cirugía Bucal y Maxilofacial",
                  "image": "cirugia_bucal",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 5,
                  "description": "Cirugía Cardiovascular",
                  "image": "cirugia_cardiovascular",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 6,
                  "description": "Cirugía de Columna",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 2
                }, {
                  "id": 7,
                  "description": "Cirugía de la Mano",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 2
                }, {
                  "id": 8,
                  "description": "Cirugía del  Tórax",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 9,
                  "description": "Cirugía Dermatológica",
                  "image": "cirugia_dermatologica",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 10,
                  "description": "Cirugía General",
                  "image": "cirugia_general",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 8
                }, {
                  "id": 11,
                  "description": "Cirugía Oncológica",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 3
                }, {
                  "id": 12,
                  "description": "Cirugía Pediátrica ",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 13,
                  "description": "Cirugía Plástica",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 1
                }, {
                  "id": 14,
                  "description": "Clinica del Dolor ",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 15,
                  "description": "Coloproctología",
                  "image": "coloproctologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 2
                }, {
                  "id": 65,
                  "description": "Comité de control de infecciones",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2022-02-11 21:23:21",
                  "updated_at": "2022-02-11 21:23:21",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 16,
                  "description": "COVID-19 Adultos",
                  "image": "\t\r\ncovid_adulto",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 16
                }, {
                  "id": 17,
                  "description": "COVID-19 Pediatría",
                  "image": "\t\r\ncovid_pediatrico",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 18,
                  "description": "Cuello y Cabeza ",
                  "image": "\t\r\ncuello_cabeza",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 19,
                  "description": "Dermatología",
                  "image": "dermatologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 20,
                  "description": "Dermatopatología",
                  "image": "dermopatologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 21,
                  "description": "Endocrinología",
                  "image": "endocrinologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 22,
                  "description": "Fisiatria",
                  "image": "fisiatria",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 23,
                  "description": "Fonoaudiología",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 25,
                  "description": "Gastroenterología",
                  "image": "gastroenterologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 8
                }, {
                  "id": 24,
                  "description": "Gastroenterología Pediátrica ",
                  "image": "gastroenterologia_pediatrica",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 26,
                  "description": "Ginecologia Oncologica",
                  "image": "ginecologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 27,
                  "description": "Ginecología y Obstetricia",
                  "image": "obstetricia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 29,
                  "description": "Infectología",
                  "image": "infectologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 6
                }, {
                  "id": 28,
                  "description": "Infectología Pediátrica",
                  "image": "infectologia_pediatrica",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 62,
                  "description": "Innovación",
                  "image": "innovacion",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 8
                }, {
                  "id": 61,
                  "description": "Medicina de Emergencia",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 30,
                  "description": "Medicina General ",
                  "image": "medicina_general",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 29
                }, {
                  "id": 31,
                  "description": "Medicina Interna ",
                  "image": "medicina_interna",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 16
                }, {
                  "id": 32,
                  "description": "Medicina Nuclear ",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 63,
                  "description": "Médico Intensivista",
                  "image": "medico_intensivista",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2021-06-04 14:07:16",
                  "updated_at": "2021-06-04 14:07:16",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 34,
                  "description": "Nefrología",
                  "image": "nefrologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 33,
                  "description": "Nefrología Pediátrica",
                  "image": "nefrologia_pediatrica",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 64,
                  "description": "Neonatologia",
                  "image": "neonatologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2021-06-04 14:07:16",
                  "updated_at": "2021-06-04 14:07:16",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 35,
                  "description": "Neumonología",
                  "image": "neumonologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 10
                }, {
                  "id": 36,
                  "description": "Neurocirugía",
                  "image": "neurocirugia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 9
                }, {
                  "id": 38,
                  "description": "Neurología",
                  "image": "neurologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 3
                }, {
                  "id": 37,
                  "description": "Neurología Pediatrica",
                  "image": "neurologia_pediatrica",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 39,
                  "description": "Nutrición y Dietética",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 40,
                  "description": "Obstetricia ",
                  "image": "obstetricia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 5
                }, {
                  "id": 41,
                  "description": "Odontología",
                  "image": "odontologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 43,
                  "description": "Odontopediatría",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 42,
                  "description": "Odontopediatría y Ortodoncia",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 44,
                  "description": "Oftalmología",
                  "image": "oftalmologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 45,
                  "description": "Oncología",
                  "image": "oncologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 7
                }, {
                  "id": 46,
                  "description": "Orientación Emergencia Adultos ",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 47,
                  "description": "Ortopedia Infantil ",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 48,
                  "description": "Otorrinolaringología",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 1
                }, {
                  "id": 49,
                  "description": "Pediatría",
                  "image": "pediatria",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 5
                }, {
                  "id": 50,
                  "description": "Prueba del Sistema",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 51,
                  "description": "Psicología Clínica",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 52,
                  "description": "Psicopedagogía",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 54,
                  "description": "Psiquiatría",
                  "image": "psiquiatria",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 53,
                  "description": "Psiquiatria Infantil ",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 55,
                  "description": "Radiología e Imágenes ",
                  "image": "radiologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 56,
                  "description": "Radioterapia Oncológica",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 57,
                  "description": "Terapia Ocupacional",
                  "image": null,
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }, {
                  "id": 58,
                  "description": "Traumatología",
                  "image": "traumatologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 9
                }, {
                  "id": 60,
                  "description": "Urología",
                  "image": "urologia",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 2
                }, {
                  "id": 59,
                  "description": "Urología Pediátrica",
                  "image": "urologia_pediatrica",
                  "active": "1",
                  "user_id_medico": null,
                  "created_at": "2020-09-16 03:37:42",
                  "updated_at": "2020-09-16 03:37:42",
                  "deleted_at": null,
                  "total_pacientes": 0
                }]);

              case 1:
              case "end":
                return _context2.stop();
            }
          }
        }, _callee2);
      }));

      function getIndex2() {
        return _getIndex2.apply(this, arguments);
      }

      return getIndex2;
    }()
  }]);

  return CatUserEspecialidad;
}();



/***/ }),

/***/ "./public/class_js/CatUserEstado.js":
/*!******************************************!*\
  !*** ./public/class_js/CatUserEstado.js ***!
  \******************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return CatUserEstado; });
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }

var CatUserEstado = /*#__PURE__*/function () {
  function CatUserEstado() {
    _classCallCheck(this, CatUserEstado);
  }

  _createClass(CatUserEstado, [{
    key: "getAll",
    value: function getAll() {
      //return await get( location.origin+"/cat_user_estado/index")
      return [{
        "id": 1,
        "description": "Amazonas",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 2,
        "description": "Anzoategui",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 3,
        "description": "Apure",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 4,
        "description": "Aragua",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 5,
        "description": "Barinas",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 6,
        "description": "Bolívar",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 7,
        "description": "Carabobo",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 8,
        "description": "Cojedes",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 9,
        "description": "Delta Amacuro",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 10,
        "description": "Falcón",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 11,
        "description": "Guárico",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 12,
        "description": "Lara",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 13,
        "description": "Mérida",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 14,
        "description": "Miranda",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 15,
        "description": "Monagas",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 16,
        "description": "Nueva Esparta",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 17,
        "description": "Portuguesa",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 18,
        "description": "Sucre",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 19,
        "description": "Táchira",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 20,
        "description": "Trujillo",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 21,
        "description": "Yaracuy",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 22,
        "description": "Zulia",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 23,
        "description": "Dependencias Federales",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 24,
        "description": "Distrito Capital",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 25,
        "description": "La Guaira",
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }];
    }
  }]);

  return CatUserEstado;
}();



/***/ }),

/***/ "./public/class_js/CatUserMunicipio.js":
/*!*********************************************!*\
  !*** ./public/class_js/CatUserMunicipio.js ***!
  \*********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return CatUserMunicipio; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var CatUserMunicipio = /*#__PURE__*/function () {
  function CatUserMunicipio() {
    _classCallCheck(this, CatUserMunicipio);
  }

  _createClass(CatUserMunicipio, [{
    key: "show",
    value: function show(id) {
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["get"])(location.origin + "/cat_user_municipio/show/" + id);
    }
  }, {
    key: "getAll",
    value: function getAll() {
      //return await get( location.origin+"/cat_user_municipio/getAll")
      return [{
        "id": 1,
        "description": "Alto Orinoco",
        "cat_estado_id": 1,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 2,
        "description": "Atabapo",
        "cat_estado_id": 1,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 3,
        "description": "Atures",
        "cat_estado_id": 1,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 4,
        "description": "Autana",
        "cat_estado_id": 1,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 5,
        "description": "Maroa",
        "cat_estado_id": 1,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 6,
        "description": "Manapiare",
        "cat_estado_id": 1,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 7,
        "description": "Rio Negro",
        "cat_estado_id": 1,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 8,
        "description": "Fernando De Peñalver",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 9,
        "description": "Francisco Del Carmen Carvajal",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 10,
        "description": "Francisco De Miranda",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 11,
        "description": "Guanta",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 12,
        "description": "Independencia",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 13,
        "description": "Juan Antonio Sotillo",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 14,
        "description": "Juan Manuel Cajigal",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 15,
        "description": "Jose Gregorio Monagas",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 16,
        "description": "Libertad",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 17,
        "description": "Manuel Ezequiel Bruzual",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 18,
        "description": "Pedro Maria Freites",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 19,
        "description": "Piritu",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 20,
        "description": "San Jose De Guanipa",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 21,
        "description": "San Juan De Capistrano",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 22,
        "description": "Santa Ana",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 23,
        "description": "Simon Bolivar",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 24,
        "description": "Simon Rodriguez",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 25,
        "description": "Sir Arthur Mc Gregor",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 26,
        "description": "Diego Bautista Urbaneja",
        "cat_estado_id": 2,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 27,
        "description": "Achaguas",
        "cat_estado_id": 3,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 28,
        "description": "Biruaca",
        "cat_estado_id": 3,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 29,
        "description": "Muñoz",
        "cat_estado_id": 3,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 30,
        "description": "Paez",
        "cat_estado_id": 3,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 31,
        "description": "Pedro Camejo",
        "cat_estado_id": 3,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 32,
        "description": "Romulo Gallegos",
        "cat_estado_id": 3,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 33,
        "description": "San Fernando",
        "cat_estado_id": 3,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 34,
        "description": "Bolivar",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 35,
        "description": "Camatagua",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 36,
        "description": "Girardot",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 37,
        "description": "Jose Angel Lamas",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 38,
        "description": "Jose Felix Ribas",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 39,
        "description": "Jose Rafael Revenga",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 40,
        "description": "Libertador",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 41,
        "description": "Mario Briceño Iragorry",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 42,
        "description": "San Casimiro",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 43,
        "description": "San Sebastian",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 44,
        "description": "Santiago Mariño",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 45,
        "description": "Santos Michelena",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 46,
        "description": "Sucre",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 47,
        "description": "Tovar",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 48,
        "description": "Urdaneta",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 49,
        "description": "Zamora",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 50,
        "description": "Francisco Linares Alcantara",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 51,
        "description": "Ocumare De La Costa De Oro",
        "cat_estado_id": 4,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 52,
        "description": "Alberto Arvelo Torrealba",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 53,
        "description": "Antonio Jose De Sucre",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 54,
        "description": "Arismendi",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 55,
        "description": "Barinas",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 56,
        "description": "Bolivar",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 57,
        "description": "Cruz Paredes",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 58,
        "description": "Ezequiel Zamora",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 59,
        "description": "Obispos",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 60,
        "description": "Pedraza",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 61,
        "description": "Rojas",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 62,
        "description": "Sosa",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 63,
        "description": "Andres Eloy Blanco",
        "cat_estado_id": 5,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 64,
        "description": "Caroni",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 65,
        "description": "Cedeño",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 66,
        "description": "El Callao",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 67,
        "description": "Gran Sabana",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 68,
        "description": "Heres",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 69,
        "description": "Piar",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 70,
        "description": "Raul Leoni",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 71,
        "description": "Roscio",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 72,
        "description": "Sifontes",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 73,
        "description": "Sucre",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 74,
        "description": "Padre Pedro Chien",
        "cat_estado_id": 6,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 75,
        "description": "Bejuma",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 76,
        "description": "Carlos Arvelo",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 77,
        "description": "Diego Ibarra",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 78,
        "description": "Guacara",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 79,
        "description": "Juan Jose Mora",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 80,
        "description": "Libertador",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 81,
        "description": "Los Guayos",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 82,
        "description": "Miranda",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 83,
        "description": "Montalban",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 84,
        "description": "Naguanagua",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 85,
        "description": "Puerto Cabello",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 86,
        "description": "San Diego",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 87,
        "description": "San Joaquin",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 88,
        "description": "Valencia",
        "cat_estado_id": 7,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 89,
        "description": "Anzoategui",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 90,
        "description": "Falcon",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 91,
        "description": "Girardot",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 92,
        "description": "Lima Blanco",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 93,
        "description": "Pao De San Juan Bautista",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 94,
        "description": "Ricaurte",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 95,
        "description": "Romulo Gallegos",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 96,
        "description": "San Carlos",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 97,
        "description": "Tinaco",
        "cat_estado_id": 8,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 98,
        "description": "Antonio Diaz",
        "cat_estado_id": 9,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 99,
        "description": "Casacoima",
        "cat_estado_id": 9,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 100,
        "description": "Pedernales",
        "cat_estado_id": 9,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 101,
        "description": "Tucupita",
        "cat_estado_id": 9,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 102,
        "description": "Acosta",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 103,
        "description": "Bolivar",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 104,
        "description": "Buchivacoa",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 105,
        "description": "Cacique Manaure",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 106,
        "description": "Carirubana",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 107,
        "description": "Colina",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 108,
        "description": "Dabajuro",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 109,
        "description": "Democracia",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 110,
        "description": "Falcon",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 111,
        "description": "Federacion",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 112,
        "description": "Jacura",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 113,
        "description": "Los Taques",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 114,
        "description": "Mauroa",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 115,
        "description": "Miranda",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 116,
        "description": "Monseñor Iturriza",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 117,
        "description": "Palmasola",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 118,
        "description": "Petit",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 119,
        "description": "Piritu",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 120,
        "description": "San Francisco",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 121,
        "description": "Silva",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 122,
        "description": "Sucre",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 123,
        "description": "Tocopero",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:41",
        "updated_at": "2020-09-16 03:37:41",
        "deleted_at": null
      }, {
        "id": 124,
        "description": "Union",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 125,
        "description": "Urumaco",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 126,
        "description": "Zamora",
        "cat_estado_id": 10,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 127,
        "description": "Camaguan",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 128,
        "description": "Chaguaramas",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 129,
        "description": "El Socorro",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 130,
        "description": "San Geronimo De Guayabal",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 131,
        "description": "Leonardo Infante",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 132,
        "description": "Las Mercedes",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 133,
        "description": "Julian Mellado",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 134,
        "description": "Francisco De Miranda",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 135,
        "description": "Jose Tadeo Monagas",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 136,
        "description": "Ortiz",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 137,
        "description": "Jose Felix Ribas",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 138,
        "description": "Juan German Roscio",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 139,
        "description": "San Jose De Guaribe",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 140,
        "description": "Santa Maria De Ipire",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 141,
        "description": "Pedro Zaraza",
        "cat_estado_id": 11,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 142,
        "description": "Andres Eloy Blanco",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 143,
        "description": "Crespo",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 144,
        "description": "Iribarren",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 145,
        "description": "Jimenez",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 146,
        "description": "Moran",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 147,
        "description": "Palavecino",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 148,
        "description": "Simon Planas",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 149,
        "description": "Torres",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 150,
        "description": "Urdaneta",
        "cat_estado_id": 12,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 151,
        "description": "Alberto Adriani",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 152,
        "description": "Andres Bello",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 153,
        "description": "Antonio Pinto Salinas",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 154,
        "description": "Aricagua",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 155,
        "description": "Arzobispo Chacon",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 156,
        "description": "Campo Elias",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 157,
        "description": "Caracciolo Parra Olmedo",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 158,
        "description": "Cardenal Quintero",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 159,
        "description": "Guaraque",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 160,
        "description": "Julio Cesar Salas",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 161,
        "description": "Justo Briceño",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 162,
        "description": "Libertador",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 163,
        "description": "Miranda",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 164,
        "description": "Obispo Ramos De Lora",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 165,
        "description": "Padre Noguera",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 166,
        "description": "Pueblo Llano",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 167,
        "description": "Rangel",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 168,
        "description": "Rivas Davila",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 169,
        "description": "Santos Marquina",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 170,
        "description": "Sucre",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 171,
        "description": "Tovar",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 172,
        "description": "Tulio Febres Cordero",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 173,
        "description": "Zea",
        "cat_estado_id": 13,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 174,
        "description": "Acevedo",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 175,
        "description": "Andres Bello",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 176,
        "description": "Baruta",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 177,
        "description": "Brion",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 178,
        "description": "Buroz",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 179,
        "description": "Carrizal",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 180,
        "description": "Chacao",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 181,
        "description": "Cristobal Rojas",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 182,
        "description": "El Hatillo",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 183,
        "description": "Guaicaipuro",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 184,
        "description": "Independencia",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 185,
        "description": "Lander",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 186,
        "description": "Los Salias",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 187,
        "description": "Paez",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 188,
        "description": "Paz Castillo",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 189,
        "description": "Pedro Gual",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 190,
        "description": "Plaza",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 191,
        "description": "Simon Bolivar",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 192,
        "description": "Sucre",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 193,
        "description": "Urdaneta",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 194,
        "description": "Zamora",
        "cat_estado_id": 14,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 195,
        "description": "Acosta",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 196,
        "description": "Aguasay",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 197,
        "description": "Bolivar",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 198,
        "description": "Caripe",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 199,
        "description": "Cedeño",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 200,
        "description": "Ezequiel Zamora",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 201,
        "description": "Libertador",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 202,
        "description": "Maturin",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 203,
        "description": "Piar",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 204,
        "description": "Punceres",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 205,
        "description": "Santa Barbara",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 206,
        "description": "Sotillo",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 207,
        "description": "Uracoa",
        "cat_estado_id": 15,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 208,
        "description": "Antolin Del Campo",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 209,
        "description": "Arismendi",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 210,
        "description": "Diaz",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 211,
        "description": "Garcia",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 212,
        "description": "Gomez",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 213,
        "description": "Maneiro",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 214,
        "description": "Marcano",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 215,
        "description": "Mariño",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 216,
        "description": "Peninsula De Macanao",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 217,
        "description": "Tubores",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 218,
        "description": "Villalba",
        "cat_estado_id": 16,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 219,
        "description": "Agua Blanca",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 220,
        "description": "Araure",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 221,
        "description": "Esteller",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 222,
        "description": "Guanare",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 223,
        "description": "Guanarito",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 224,
        "description": "Monseñor Jose Vicente De Unda",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 225,
        "description": "Ospino",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 226,
        "description": "Paez",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 227,
        "description": "Papelon",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 228,
        "description": "San Genaro De Boconoito",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 229,
        "description": "San Rafael De Onoto",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 230,
        "description": "Santa Rosalia",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 231,
        "description": "Sucre",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 232,
        "description": "Turen",
        "cat_estado_id": 17,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 233,
        "description": "Andres Eloy Blanco",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 234,
        "description": "Andres Mata",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 235,
        "description": "Arismendi",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 236,
        "description": "Benitez",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 237,
        "description": "Bermudez",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 238,
        "description": "Bolivar",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 239,
        "description": "Cajigal",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 240,
        "description": "Cruz Salmeron Acosta",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 241,
        "description": "Libertador",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 242,
        "description": "Mariño",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 243,
        "description": "Mejia",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 244,
        "description": "Montes",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 245,
        "description": "Ribero",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 246,
        "description": "Sucre",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 247,
        "description": "Valdez",
        "cat_estado_id": 18,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 248,
        "description": "Andres Bello",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 249,
        "description": "Antonio Romulo Costa",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 250,
        "description": "Ayacucho",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 251,
        "description": "Bolivar",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 252,
        "description": "Cardenas",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 253,
        "description": "Cordoba",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 254,
        "description": "Fernandez Feo",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 255,
        "description": "Francisco De Miranda",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 256,
        "description": "Garcia De Hevia",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 257,
        "description": "Guasimos",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 258,
        "description": "Independencia",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 259,
        "description": "Jauregui",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 260,
        "description": "Jose Maria Vargas",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 261,
        "description": "Junin",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 262,
        "description": "Libertad",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 263,
        "description": "Libertador",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 264,
        "description": "Lobatera",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 265,
        "description": "Michelena",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 266,
        "description": "Panamericano",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 267,
        "description": "Pedro Maria Ureña",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 268,
        "description": "Rafael Urdaneta",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 269,
        "description": "Samuel Dario Maldonado",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 270,
        "description": "San Cristobal",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 271,
        "description": "Seboruco",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 272,
        "description": "Simon Rodriguez",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 273,
        "description": "Sucre",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 274,
        "description": "Torbes",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 275,
        "description": "Uribante",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 276,
        "description": "San Judas Tadeo",
        "cat_estado_id": 19,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 277,
        "description": "Andres Bello",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 278,
        "description": "Bocono",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 279,
        "description": "Bolivar",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 280,
        "description": "Candelaria",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 281,
        "description": "Carache",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 282,
        "description": "Escuque",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 283,
        "description": "Jose Felipe Marquez Cañizalez",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 284,
        "description": "Juan Vicente Campo Elias",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 285,
        "description": "La Ceiba",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 286,
        "description": "Miranda",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 287,
        "description": "Monte Carmelo",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 288,
        "description": "Motatan",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 289,
        "description": "Pampan",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 290,
        "description": "Pampanito",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 291,
        "description": "Rafael Rangel",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 292,
        "description": "San Rafael De Carvajal",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 293,
        "description": "Sucre",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 294,
        "description": "Trujillo",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 295,
        "description": "Urdaneta",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 296,
        "description": "Valera",
        "cat_estado_id": 20,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 297,
        "description": "Aristides Bastidas",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 298,
        "description": "Bolivar",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 299,
        "description": "Bruzual",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 300,
        "description": "Cocorote",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 301,
        "description": "Independencia",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 302,
        "description": "Jose Antonio Paez",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 303,
        "description": "La Trinidad",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 304,
        "description": "Manuel Monge",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 305,
        "description": "Nirgua",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 306,
        "description": "Peña",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 307,
        "description": "San Felipe",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 308,
        "description": "Sucre",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 309,
        "description": "Urachiche",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 310,
        "description": "Veroes",
        "cat_estado_id": 21,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 311,
        "description": "Almirante Padilla",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 312,
        "description": "Baralt",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 313,
        "description": "Cabimas",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 314,
        "description": "Catatumbo",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 315,
        "description": "Colon",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 316,
        "description": "Francisco Javier Pulgar",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 317,
        "description": "Jesus Enrique Lossada",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 318,
        "description": "Jesus Maria Semprun",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 319,
        "description": "La Cañada De Urdaneta",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 320,
        "description": "Lagunillas",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 321,
        "description": "Machiques De Perija",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 322,
        "description": "Mara",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 323,
        "description": "Maracaibo",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 324,
        "description": "Miranda",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 325,
        "description": "Paez",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 326,
        "description": "Rosario De Perija",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 327,
        "description": "San Francisco",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 328,
        "description": "Santa Rita",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 329,
        "description": "Simon Bolivar",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 330,
        "description": "Sucre",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 331,
        "description": "Valmore Rodriguez",
        "cat_estado_id": 22,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 332,
        "description": "Libertador",
        "cat_estado_id": 24,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }, {
        "id": 333,
        "description": "Vargas",
        "cat_estado_id": 25,
        "active": "1",
        "user_id_medico": null,
        "created_at": "2020-09-16 03:37:42",
        "updated_at": "2020-09-16 03:37:42",
        "deleted_at": null
      }];
    }
  }]);

  return CatUserMunicipio;
}();



/***/ }),

/***/ "./public/class_js/CatUserUbicacion.js":
/*!*********************************************!*\
  !*** ./public/class_js/CatUserUbicacion.js ***!
  \*********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return CatUserUbicacion; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var CatUserUbicacion = /*#__PURE__*/function () {
  function CatUserUbicacion() {
    _classCallCheck(this, CatUserUbicacion);
  }

  _createClass(CatUserUbicacion, [{
    key: "getIndex",
    value: function getIndex() {
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["get"])(location.origin + "/cat_user_ubicacion/index");
    }
  }, {
    key: "showMenu",
    value: function showMenu() {
      var formdata = new FormData();
      formdata.append("cat_user_ubicacion_id", 1);
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/cat_user_ubicacion/show/menu", formdata);
    }
  }, {
    key: "show",
    value: function show(parent_cat_user_ubicacion_id) {
      //let formdata = new FormData();
      //formdata.append("parent_cat_user_ubicacion_id", parent_cat_user_ubicacion_id)
      //formdata.append("_token", $("#_token").val())
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["get"])(location.origin + '/cat_user_ubicacion/show/' + parent_cat_user_ubicacion_id);
    }
  }]);

  return CatUserUbicacion;
}();



/***/ }),

/***/ "./public/class_js/Component.js":
/*!**************************************!*\
  !*** ./public/class_js/Component.js ***!
  \**************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return Component; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./UserCuestUbicacion.js */ "./public/class_js/UserCuestUbicacion.js");
/* harmony import */ var _UserCuestTemperatura_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./UserCuestTemperatura.js */ "./public/class_js/UserCuestTemperatura.js");
/* harmony import */ var _UserCuestFr_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./UserCuestFr.js */ "./public/class_js/UserCuestFr.js");
/* harmony import */ var _UserCuestTa_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./UserCuestTa.js */ "./public/class_js/UserCuestTa.js");
/* harmony import */ var _UserCuestFc_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./UserCuestFc.js */ "./public/class_js/UserCuestFc.js");
/* harmony import */ var _UserCuestOximetria_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./UserCuestOximetria.js */ "./public/class_js/UserCuestOximetria.js");
/* harmony import */ var _UserCuestGl_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./UserCuestGl.js */ "./public/class_js/UserCuestGl.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
/* harmony import */ var _UserCuestAdministracion_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./UserCuestAdministracion.js */ "./public/class_js/UserCuestAdministracion.js");
/* harmony import */ var _UserCuestEpisodio_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./UserCuestEpisodio.js */ "./public/class_js/UserCuestEpisodio.js");
/* harmony import */ var _UserCuestPendiente_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./UserCuestPendiente.js */ "./public/class_js/UserCuestPendiente.js");
/* harmony import */ var _UserCuestChat_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./UserCuestChat.js */ "./public/class_js/UserCuestChat.js");
/* harmony import */ var _UserCuestHistoria_js__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./UserCuestHistoria.js */ "./public/class_js/UserCuestHistoria.js");
/* harmony import */ var jquery__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! jquery */ "./node_modules/jquery/dist/jquery.js");
/* harmony import */ var jquery__WEBPACK_IMPORTED_MODULE_15___default = /*#__PURE__*/__webpack_require__.n(jquery__WEBPACK_IMPORTED_MODULE_15__);
function _objectDestructuringEmpty(obj) { if (obj == null) throw new TypeError("Cannot destructure undefined"); }



function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
















var message;
var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_9__["default"]();
var userCuestHistoria = new _UserCuestHistoria_js__WEBPACK_IMPORTED_MODULE_14__["default"]();
var userCuestPendiente = new _UserCuestPendiente_js__WEBPACK_IMPORTED_MODULE_12__["default"]();

var Component = /*#__PURE__*/function () {
  function Component() {
    _classCallCheck(this, Component);
  }

  _createClass(Component, [{
    key: "modalTemplate_1",
    value: function modalTemplate_1(_ref, fn) {
      var _ref$title = _ref.title,
          title = _ref$title === void 0 ? "Sin titulo" : _ref$title,
          user_id = _ref.user_id,
          _ref$modal = _ref.modal,
          modal = _ref$modal === void 0 ? '#modelId' : _ref$modal,
          _ref$selector = _ref.selector,
          selector = _ref$selector === void 0 ? '.modal-body' : _ref$selector,
          _ref$btn_title = _ref.btn_title,
          btn_title = _ref$btn_title === void 0 ? 'Nuevo valor' : _ref$btn_title,
          _ref$btn_create = _ref.btn_create,
          btn_create = _ref$btn_create === void 0 ? true : _ref$btn_create,
          _ref$eventModelFooter = _ref.eventModelFooter,
          eventModelFooter = _ref$eventModelFooter === void 0 ? '' : _ref$eventModelFooter,
          _ref$headerPaciente = _ref.headerPaciente,
          headerPaciente = _ref$headerPaciente === void 0 ? true : _ref$headerPaciente;
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n                <div id=\"infoPaciente\"></div>\n\n                <div class=\"d-flex flex-wrap justify-content-between\" >\n                    <div class=\"h1 text-primary\">\n                        ".concat(title, "\n                    </div>\n                    <span id=\"model_create\" class=\"btn text-shadow-drop-tl text-primary mr-2\" style=\"").concat(btn_create === true ? 'display:block;' : 'display:none;', "float:right;font-size: 2rem !important; cursor:pointer;\">\n                        ").concat(btn_title, "\n                        <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                    </span>\n                </div>\n                <div id=\"modalTemplate_1_body\" class=\"flex-fill mb-1\">\n                </div>\n                <div class=\"input-group my-1 d-none\">\n                    <div class=\"input-group-prepend\">\n                        <span class=\"input-group-text\" id=\"inputGroupFileAddon01\">\n                            <img style=\"width: 2em;height: 2em;\" src=\"/image/system/carga1-success.svg\">\n                        </span>\n                    </div>\n                    <div class=\"custom-file\">\n                        <input type=\"file\" class=\"custom-file-input\" id=\"file\" aria-describedby=\"file\">\n                        <label class=\"custom-file-label\" style=\"height: calc(2.7rem + 3px);\" for=\"inputGroupFile01\">Pulsa aqu\xED para subir archivos</label>\n                    </div>\n                </div>\n                <div class=\"row d-none\" id=\"file_temp\">\n\n                </div>\n                <div class=\"d-flex justify-content-center\">\n                    <div id=\"modalTemplate_1_footer\"></div>\n                    <div id=\"modalTemplate_2_footer\"></div>\n                </div>\n\n            ")); //eval( eventModelContent  );

      eval(eventModelFooter);

      if (headerPaciente) {
        _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_9__["default"].infoPaciente("#infoPaciente", user_id);
      }

      fn();
    }
  }, {
    key: "modalTemplate_3",
    value: function modalTemplate_3(_ref2, fn) {
      var _ref2$title = _ref2.title,
          title = _ref2$title === void 0 ? "Sin titulo" : _ref2$title,
          _ref2$modal = _ref2.modal,
          modal = _ref2$modal === void 0 ? '#messageModal' : _ref2$modal,
          _ref2$selector = _ref2.selector,
          selector = _ref2$selector === void 0 ? '.modal-body' : _ref2$selector;
      $(modal).modal("show");
      fn();
    }
    /**
     * Modulos que usan modalTemplate_2:
     *
     * SystemIncidencia
     *
     */

  }, {
    key: "modalTemplate_2",
    value: function modalTemplate_2(_ref3, fn) {
      var _ref3$title = _ref3.title,
          title = _ref3$title === void 0 ? "Sin titulo" : _ref3$title,
          user_id = _ref3.user_id,
          _ref3$modal = _ref3.modal,
          modal = _ref3$modal === void 0 ? '#modelId' : _ref3$modal,
          _ref3$selector = _ref3.selector,
          selector = _ref3$selector === void 0 ? '.modal-body' : _ref3$selector,
          _ref3$btn_title = _ref3.btn_title,
          btn_title = _ref3$btn_title === void 0 ? 'Nuevo valor' : _ref3$btn_title,
          _ref3$btn_create = _ref3.btn_create,
          btn_create = _ref3$btn_create === void 0 ? true : _ref3$btn_create;
      $(modal).modal("show");
      $(selector).html("\n                <div class=\"row mt-1\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 position-inherit\">\n                        <div>\n                            <h1 class=\"text-primary\">\n                                <span id=\"modalTemplate_1_title\">\n                                    ".concat(title, "\n                                </span>\n                            </h1>\n                        </div>\n                        <span id=\"model_create\" class=\"text-shadow-drop-tl text-primary mr-2\" style=\"").concat(btn_create === true ? 'display:block;' : 'display:none;', "float:right;font-size: 2rem !important; cursor:pointer;\">\n                            ").concat(btn_title, "\n                            <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                        </span>\n                        <div id=\"modalTemplate_1_body\"></div>\n                    </div>\n                </div>\n                <div class=\"botones-bottom-modal\">\n                    <div id=\"modalTemplate_1_footer\"></div>\n                    <div id=\"modalTemplate_2_footer\"></div>\n                </div>\n            "));
      fn();
    }
  }, {
    key: "formTemplate_1",
    value: function formTemplate_1(_ref4) {
      var _ref4$selector = _ref4.selector,
          selector = _ref4$selector === void 0 ? '#modalTemplate_1_body' : _ref4$selector,
          eventModelContent = _ref4.eventModelContent,
          _ref4$btn_agregar = _ref4.btn_agregar,
          btn_agregar = _ref4$btn_agregar === void 0 ? 'Agregar' : _ref4$btn_agregar;
      $(selector).html("\n            <div class=\"container-fluid\">\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <input type=\"number\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"value\" id=\"value\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <input type=\"datetime-local\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <textarea class=\"form-control form-control-lg bg-gray-footer-parodi mb-3\" name=\"coment\" id=\"coment\" rows=\"2\" placeholder=\"Observaci\xF3n\"></textarea>\n                    </div>\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <button id=\"btn_modalTemplate_agregar\" class=\"font-weight-bold btn btn-success mb-1 btn-block btn-lg\">".concat(btn_agregar, "</button>\n                    </div>\n                </div>\n            </div>\n        "));
      $("btn_modalTemplate_agregar").on("click", function () {
        eval(eventModelContent);
      });
    }
  }, {
    key: "formTemplate_2",
    value: function formTemplate_2(_ref5) {
      var _ref5$placeholder = _ref5.placeholder,
          placeholder = _ref5$placeholder === void 0 ? "Escriba un texto..." : _ref5$placeholder,
          _ref5$fieldName = _ref5.fieldName,
          fieldName = _ref5$fieldName === void 0 ? "description" : _ref5$fieldName,
          _ref5$selector = _ref5.selector,
          selector = _ref5$selector === void 0 ? '#modalTemplate_1_body' : _ref5$selector,
          _ref5$btn_agregar = _ref5.btn_agregar,
          btn_agregar = _ref5$btn_agregar === void 0 ? 'Agregar' : _ref5$btn_agregar;
      $(selector).html("\n            <textarea class=\"form-control bg-gray-footer-parodi \" name=\"".concat(fieldName, "\" id=\"").concat(fieldName, "\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n        "));
    }
  }, {
    key: "btn_regresar",
    value: function btn_regresar(_ref6) {
      var _ref6$selector = _ref6.selector,
          selector = _ref6$selector === void 0 ? '#modalTemplate_2_footer' : _ref6$selector,
          _ref6$title = _ref6.title,
          title = _ref6$title === void 0 ? 'Regresar' : _ref6$title,
          eventModel = _ref6.eventModel;
      //console.log(eventModel)
      $(selector).html("\n            <button id=\"btn_regresar\" class=\"font-weight-bold btn btn-primary btn-block btn-lg\">".concat(title, "</button>\n        "));
      $("#btn_regresar").on("click", function () {
        eval(eventModel);
      });
    }
  }, {
    key: "btn_group_store",
    value: function btn_group_store(_ref7) {
      var _ref7$selector = _ref7.selector,
          selector = _ref7$selector === void 0 ? '#modalTemplate_2_footer' : _ref7$selector,
          _ref7$title = _ref7.title,
          title = _ref7$title === void 0 ? 'Regresar' : _ref7$title,
          eventModel = _ref7.eventModel;
      //console.log(eventModel)
      $(selector).html("\n            <button id=\"btn_modalTemplate_store\" class=\"font-weight-bold btn btn-success mb-1 btn-block btn-lg\">Agregar</button>\n            <button id=\"btn_regresar\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">".concat(title, "</button>\n        "));
      $("#btn_regresar").on("click", function () {
        eval(eventModel);
      });
    }
  }, {
    key: "btn_agregar",
    value: function btn_agregar(_ref8) {
      var _ref8$selector = _ref8.selector,
          selector = _ref8$selector === void 0 ? '#modalTemplate_1_footer' : _ref8$selector,
          _ref8$title = _ref8.title,
          title = _ref8$title === void 0 ? 'Agregar' : _ref8$title,
          eventModel = _ref8.eventModel;
      //console.log(eventModel)
      $(selector).html("\n            <button id=\"btn_agregar\" class=\"font-weight-bold btn btn-success mb-1 btn-block btn-lg\">".concat(title, "</button>\n        "));
      $("#btn_agregar").on("click", function () {
        eval(eventModel);
      });
    }
  }, {
    key: "btn_closeModal",
    value: function btn_closeModal(_ref9) {
      var _ref9$selector = _ref9.selector,
          selector = _ref9$selector === void 0 ? "#modalTemplate_2_footer" : _ref9$selector,
          _ref9$title = _ref9.title,
          title = _ref9$title === void 0 ? "Cerrar" : _ref9$title;
      $(selector).html("\n            <a class=\"btn bg-primary text-primary btn-block btn-lg\" data-dismiss=\"modal\" aria-label=\"Close\">".concat(title, "</a>\n        "));
    }
  }, {
    key: "btn_delete",
    value: function btn_delete(_ref10) {
      var _ref10$data = _ref10.data,
          data = _ref10$data === void 0 ? "#" : _ref10$data,
          _ref10$tooltip = _ref10.tooltip,
          tooltip = _ref10$tooltip === void 0 ? "Eliminar Valor" : _ref10$tooltip,
          _ref10$className = _ref10.className,
          className = _ref10$className === void 0 ? "delete-row btn btn-danger" : _ref10$className,
          _ref10$html = _ref10.html,
          html = _ref10$html === void 0 ? "<i class='fa fa-minus' aria-hidden='true'></i>" : _ref10$html;
      return "\n            <button\n                data-tippy-content=\"".concat(tooltip, "\"\n                class=\"").concat(className, "\"\n                data-id=\"").concat(data, "\"\n            >\n                ").concat(html, "\n            </button>\n        ");
    }
  }, {
    key: "modelIndex_1",
    value: function modelIndex_1(_ref11) {
      var _ref11$selector = _ref11.selector,
          selector = _ref11$selector === void 0 ? '#modalTemplate_1_body' : _ref11$selector,
          _ref11$columName = _ref11.columName,
          columName = _ref11$columName === void 0 ? ['Valor', "Observación"] : _ref11$columName,
          _ref11$classModel = _ref11.classModel,
          classModel = _ref11$classModel === void 0 ? null : _ref11$classModel,
          user_id = _ref11.user_id;
      var tempColumn = "";
      $.each(columName, function (indexOfArray, valueOfElement) {
        tempColumn += "<th class=\"text-primary text-center\">".concat(valueOfElement, "</th>");
      });
      $(selector).html("\n            <div class=\"table-responsive\">\n                <table class=\"table table-bordered mb-3\">\n                    <tr>\n                        <th class=\"text-primary text-center\">\n                            #\n                        </th>\n                        ".concat(tempColumn, "\n                        <th class=\"text-primary text-center\">Acci\xF3n</th>\n\n                    </tr>\n                    <tbody id=\"filas\">\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center\">\n                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                    <span class=\"sr-only\">Loading...</span>\n                                </div>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n            </div>\n        "));
      eval(classModel + ".show({user_id:" + user_id + "})").then(function (response) {
        var filas = "";

        if (Object.keys(response).length > 0) {
          var tempRow = "";
          $(response).each(function (index, element) {
            tempRow = "\n                        <tr id=\"temp_".concat(element.id, "\">\n                            <td style=\"width: 20px;\" class=\"text-right text-secondary\" id=\"modal-").concat(element.id, "\">\n                                ").concat(index + 1, "\n                            </td>\n                            <td class=\"text-secondary\" id=\"modal-").concat(element.id, "\">\n                                ").concat(element.description != null ? element.description : '', "\n                            </td>\n                            <td style=\"width: 60px;\" align=\"center\">\n                                <button data-tippy-content=\"Eliminar valor\" class=\"delete-row btn btn-danger\" data-id=\"").concat(element.id, "\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button>\n                            </td>\n                        </tr>\n                    ");
            filas = filas + tempRow;
          });
        } else {
          filas = "\n                    <tr>\n                        <td colspan=\"5\" class=\"text-center text-primary\">Sin registros</td>\n                    </tr>\n                ";
        }

        $("#filas").html(filas);
        $(".delete-row").on("click", function () {
          var c = confirm("¿Seguro que desea eliminar este registro?");

          if (c) {
            var id = $(this).data('id');
            eval(classModel + ".eliminar({id:" + id + ",user_id:" + user_id + "})");
          }
        });
        tippy('[data-tippy-content]', {
          theme: 'tooltip-cmdlt-primary',
          zIndex: 200000
        });
      });
    }
  }, {
    key: "modelIndex_2",
    value: function modelIndex_2(_ref12, fn) {
      var _ref12$tableId = _ref12.tableId,
          tableId = _ref12$tableId === void 0 ? "" : _ref12$tableId,
          _ref12$selector = _ref12.selector,
          selector = _ref12$selector === void 0 ? '#modalTemplate_1_body' : _ref12$selector,
          _ref12$columName = _ref12.columName,
          columName = _ref12$columName === void 0 ? ['Valor', "Observación"] : _ref12$columName,
          _ref12$datatable = _ref12.datatable,
          datatable = _ref12$datatable === void 0 ? true : _ref12$datatable,
          _ref12$datatableOptio = _ref12.datatableOptions,
          datatableOptions = _ref12$datatableOptio === void 0 ? [] : _ref12$datatableOptio,
          _ref12$datatablerepor = _ref12.datatablereportes,
          datatablereportes = _ref12$datatablerepor === void 0 ? false : _ref12$datatablerepor,
          _ref12$classModel = _ref12.classModel,
          classModel = _ref12$classModel === void 0 ? null : _ref12$classModel,
          _ref12$user_id = _ref12.user_id,
          user_id = _ref12$user_id === void 0 ? null : _ref12$user_id;
      var tempColumn = "";
      $.each(columName, function (indexOfArray, valueOfElement) {
        tempColumn += "<th class=\"text-primary text-center\">".concat(valueOfElement, "</th>");
      });
      $(selector).html("\n            <div class=\"table-responsive\" style=\"max-height: 60vh;\">\n                <table id=\"".concat(tableId, "\" class=\"table table-bordered mb-3\">\n                    <thead>\n                        <tr>\n\n                            ").concat(tempColumn, "\n                            <th class=\"text-primary text-center\">Acci\xF3n</th>\n\n                        </tr>\n                    </thead>\n                    <tbody id=\"filas\">\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center\">\n                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                    <span class=\"sr-only\">Loading...</span>\n                                </div>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n            </div>\n        "));
      fn();
    }
  }, {
    key: "formatArray",
    value: function formatArray(_ref13) {
      var response = _ref13.response,
          _ref13$columnName = _ref13.columnName,
          columnName = _ref13$columnName === void 0 ? "column-" : _ref13$columnName;
      var resultSet = new Array();
      $.each(response, function (indexInArray, valueOfElement) {
        resultSet[indexInArray] = {};
        var i = 0;
        $.each(valueOfElement, function (indexInArray2, valueOfElement2) {
          resultSet[indexInArray][columnName + i] = valueOfElement2;
          i++;
        });
      });
      return resultSet;
    }
  }, {
    key: "formatArray_2",
    value: function formatArray_2(_ref14) {
      var response = _ref14.response,
          _ref14$columnName = _ref14.columnName,
          columnName = _ref14$columnName === void 0 ? ['id', 'value', 'coments', 'active', 'cat_user_cuestionario_id', 'user_id', 'created_at'] : _ref14$columnName;
      var resultSet = new Array();
      $.each(response, function (indexInArray, valueOfElement) {
        resultSet[indexInArray] = {};
        $.each(valueOfElement, function (indexInArray2, valueOfElement2) {
          var buscar = columnName.find(function (element) {
            return element === indexInArray2;
          });

          if (buscar == indexInArray2) {
            resultSet[indexInArray][indexInArray2] = valueOfElement2;
          }
        });
      });
      return resultSet;
    }
  }, {
    key: "modelTableResult_1",
    value: function modelTableResult_1(_ref15) {
      var _ref15$selector = _ref15.selector,
          selector = _ref15$selector === void 0 ? '#modalTemplate_1_body' : _ref15$selector,
          _ref15$response = _ref15.response,
          response = _ref15$response === void 0 ? [{
        v1: "v1",
        v2: "v2",
        v3: "v3"
      }, {
        v4: "v4",
        v5: "v5",
        v6: "v6"
      }] : _ref15$response,
          _ref15$columnName = _ref15.columnName,
          columnName = _ref15$columnName === void 0 ? ['Title1', "Title2"] : _ref15$columnName,
          _ref15$className = _ref15.className,
          className = _ref15$className === void 0 ? "table table-borderless" : _ref15$className,
          _ref15$messageNoData = _ref15.messageNoData,
          messageNoData = _ref15$messageNoData === void 0 ? "Sin resultados" : _ref15$messageNoData,
          _ref15$eventDelete = _ref15.eventDelete,
          eventDelete = _ref15$eventDelete === void 0 ? null : _ref15$eventDelete;
      var totalColumn = Object.keys(columnName).length;

      var thead = function thead() {
        var row = "<tr>";
        $.each(columnName, function (indexInArray, valueOfElement) {
          row += "\n                            <th class='head-".concat(indexInArray, " text-center text-primary'>").concat(valueOfElement, "</th>\n                        ");
        });
        row += "</tr>";
        return row;
      };

      var tbody = function tbody() {
        var resultSet = "";

        if (Object.keys(response).length > 0) {
          $.each(response, function (indexInArray, valueOfElement) {
            resultSet += "<tr>";
            $.each(valueOfElement, function (indexInArray2, valueOfElement2) {
              resultSet += "<td>";
              resultSet += valueOfElement2;
              resultSet += "</td>";
            });
            resultSet += "</tr>";
          });
        } else {
          resultSet += "<tr>";
          resultSet += "<td class='text-secondary' colspan='" + totalColumn + "'>";
          resultSet += messageNoData;
          resultSet += "</td>";
          resultSet += "</tr>";
        }

        return resultSet;
      };

      var table = "\n            <table class=\"".concat(className, "\">\n                <thead>\n                    ").concat(thead(), "\n                </thead>\n                <tbody>\n                    ").concat(tbody(), "\n                </tbody>\n            </table>\n        ");
      $(selector).html(table);
      $(".delete-row").on("click", function () {
        eval(eventDelete.replace("#", $(this).data('id')));
      });
      tippy('[data-tippy-content]', {
        theme: 'tooltip-cmdlt-primary'
      });
    }
  }, {
    key: "headerTable",
    value: function headerTable(_ref16) {
      var _ref16$option = _ref16.option,
          option = _ref16$option === void 0 ? 1 : _ref16$option;

      switch (option) {
        case 1:
          return ["Fecha", "Valor", "Observación", "Acción"];
          break;
      }
    }
  }, {
    key: "formatDate",
    value: function formatDate(_ref17) {
      var _ref17$date = _ref17.date,
          date = _ref17$date === void 0 ? "2000-01-01 00:00:00 AM" : _ref17$date;
      var d = new Date(date);
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(d.getMonth()).toUpperCase() + " / " + d.getDate() + " <b>|</b> " + formatAMPM(d);
    }
  }, {
    key: "viewResumenPaciente",
    value: function () {
      var _viewResumenPaciente = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(selector) {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                $(selector).html("\n            <div class=\"container-fluid  p-0 sticky-top\" style=\"z-index: 1040 !important;\">\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 \">\n                        <div class=\"table-responsive-md\">\n                            <table id=\"tablapacientes\" class=\"w-100\">\n                                <thead>\n                                    <tr>\n                                        <th class=\"bg-white sticky-top p-1 menu-nivel-1 text-uppercase\" colspan=\"13\">\n                                            <div id=\"cat_user_ubicacion_menu\"></div>\n                                        </th>\n                                        <th class=\"bg-white sticky-top p-1 menu-nivel-1\" colspan=\"5\" align=\"middle\">\n                                            <button id=\"btn_paciente_create\" onclick=\"window.location='/medico/create_paciente'\"  class=\"btn btn-success font-weight-bold btn-block\" style=\"\" >Nuevo Paciente</button>\n                                        </th>\n                                    </tr>\n                                    <tr>\n\n                                        <th class=\"menu-nivel-2 sticky-top align-top align-top pt-2\" colspan=\"18\">\n                                            <table class=\"w-100\">\n                                                <tr>\n                                                    <td align=\"middle\" style=\"width: 100px;overflow: hidden;\">\n                                                        <div id=\"titleArea\" style=\"width:180px;\"  class=\"text-uppercase\">\n                                                        </div>\n                                                    </td>\n                                                    <td>\n                                                        <div class=\"flex-fill\" id=\"btnNotificaciones\" style=\"\n                                                        display: flex;\n\n                                                        justify-content: flex-end;\n                                                    \">\n                                                        </div>\n                                                    </td>\n                                                    <td align=\"middle\" style=\"width: 26%;overflow: hidden;\">\n                                                        <input type=\"search\" class=\"form-control\" id=\"busquedapaciente\" placeholder=\"Buscar paciente...\" aria-label=\"Buscar paciente...\" aria-describedby=\"button-addon2\">\n\n                                                    </td>\n                                                </tr>\n                                            </table>\n\n                                        </th>\n                                    </tr>\n                                    <tr>\n                                        <td class=\"menu-nivel-3 sticky-top\" data-tippy-content=\"Monitoreo de Pacientes Priorizados\" colspan=\"18\">\n                                            <div id=\"signosP\"  class='marquee-with-options'></div>\n                                        </td>\n                                    </tr>\n\n                                </thead>\n                                <tbody id=\"row_paciente\">\n\n                                </tbody>\n                            </table>\n                        </div>\n                    </div>\n                </div>\n            </div>\n        ");
                filtroTabla();
                tippy('[data-tippy-content]', {
                  theme: 'tooltip-cmdlt-primary'
                });

              case 3:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function viewResumenPaciente(_x) {
        return _viewResumenPaciente.apply(this, arguments);
      }

      return viewResumenPaciente;
    }()
    /*************** */

  }, {
    key: "AppMenuPad",
    value: function AppMenuPad() {
      var that = this;
      return {
        el: "#AppMenuPad",
        template:
        /*html */
        "\n\n                <div>\n                    <div id=\"menuPatiencare\" class=\"list-group\">\n                        <span class=\"text-primary font-weight-bold menu-pad-title\">\n                            Men\xFA PAD\n                        </span>\n                        <div v-for=\"(item,index) in items\" :key=\"index\">\n                            <a\n                                v-if=\"!item.hasEvent\"\n                                data-toggle=\"collapse\"\n                                :href=\"'#collapse_pad'+index\"\n                                role=\"button\"\n                                aria-expanded=\"false\"\n                                :aria-controls=\"'#collapse'+index\"\n                                class=\"d-flex align-items-center list-group-item list-group-item-action\">\n                                <i :class=\"[item.icon,'font-weight-bold text-info mr-1 ml-2 mb-0 h3']\"></i>\n                                {{item.description}}\n                            </a>\n                            <a\n                                v-else\n                                @click=\"handleAreaOption(item.param_route)\"\n                                :href=\"'#collapse'+index\"\n                                role=\"button\"\n                                class=\"d-flex align-items-center list-group-item list-group-item-action\">\n                                <i :class=\"[item.icon,'font-weight-bold text-info mr-1 ml-2 mb-0 h3']\"></i>\n                                {{item.description}}\n                            </a>\n                            <div v-if=\"!item.hasEvent\" class=\"collapse\" :id=\"'collapse_pad'+index\">\n                                <div class=\"card mb-0 card-body py-0\">\n                                    <a\n                                        v-for=\"(item2,index2) in item.subItems\" :key=\"index2\"\n                                        @click=\"handleAreaOption(item2.param_route)\"\n                                        href=\"#\"\n                                        class=\"border-0 list-group-item p-2 list-group-item-action\"\n                                    >\n                                        <i :class=\"[item2.icon,'text-info']\"></i> {{item2.description}}\n                                    </a>\n                                </div>\n                            </div>\n                        </div>\n\n\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 pl-3 pr-3 pt-1 pb-1\">\n                            <button id=\"regresar\"  data-dismiss=\"modal\" aria-label=\"Close\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                        </div>\n                    </div>\n                </div>\n            ",
        data: function data() {
          return {
            items: [{
              id: 1,
              description: "Pad Emergencia",
              icon: "pc-pad_emergencia",
              param_route: "",
              hasEvent: false,
              subItems: [{
                id: 1,
                description: "Adulto",
                icon: "pc-pad_emergencia",
                param_route: "pad_emergencia_adulto",
                hasEvent: false,
                subItems: []
              }, {
                id: 1,
                description: "Pediátrica",
                icon: "pc-pad_emergencia",
                param_route: "pad_emergencia_pediatrica",
                hasEvent: false,
                subItems: []
              }]
            }, {
              id: 1,
              description: "Pad PostQuirúrgico",
              icon: "pc-light-pad-quiru",
              param_route: "",
              hasEvent: false,
              subItems: [{
                id: 1,
                description: "Ambulatorio",
                icon: "pc-pad_emergencia",
                param_route: "pad_postquirurgico_amb",
                hasEvent: false,
                subItems: []
              }, {
                id: 1,
                description: "Hospitalización",
                icon: "pc-pad_emergencia",
                param_route: "pad_postquirurgico_hosp",
                hasEvent: false,
                subItems: []
              }]
            }, {
              id: 1,
              description: "Pad Médico",
              icon: "pc-light-pad-medico",
              param_route: "pad_medico",
              hasEvent: true,
              subItems: []
            }],
            loading: document.getElementById('loadingScreen')
          };
        },
        props: {
          areaSiglas: String,
          FnAreaSiglas: Function
        },
        methods: {
          getAreaData: function getAreaData() {
            var _this = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2() {
              var areaSiglas;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
                while (1) {
                  switch (_context2.prev = _context2.next) {
                    case 0:
                      areaSiglas = [null, undefined, 'undefined'].includes(localStorage.getItem('area')) ? 'tp' : localStorage.getItem('area');

                      _this.FnAreaSiglas(areaSiglas); //this.selectorAreaName.textContent = this.items.find(x=>x.siglas === areaSiglas).name


                      _this.loading.classList.remove("d-none");

                      _context2.next = 5;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/vistas/" + areaSiglas);

                    case 5:
                      pacientes_datos = _context2.sent;
                      EventBus.$emit('externalVarChanged', pacientes_datos);
                      that.AppBtnTotales(pacientes_datos);

                      _this.loading.classList.add("d-none");

                    case 9:
                    case "end":
                      return _context2.stop();
                  }
                }
              }, _callee2);
            }))();
          },
          handleAreaOption: function handleAreaOption(areaSiglas) {
            console.log(areaSiglas);
            this.FnAreaSiglas(areaSiglas);
            localStorage.setItem("area", areaSiglas);
            this.getAreaData();
            $("#messageModal").modal("hide");
          }
        },
        mounted: function mounted() {}
      };
    }
  }, {
    key: "AppMenuAQ",
    value: function AppMenuAQ() {
      var that = this;
      console.log(pacientes_datos);
      return {
        el: "#AppMenuAQ",
        template:
        /*html */
        "\n\n                <div>\n                    <div id=\"menuPatiencare\" class=\"list-group\">\n                        <span class=\"text-primary font-weight-bold menu-pad-title\">\n                            Men\xFA \xC1rea Quir\xFArgica\n                        </span>\n                        <div v-for=\"(item,index) in items\" :key=\"index\">\n                            <a\n                                v-if=\"!item.hasEvent\"\n                                @click=\"handleAreaOption(item.param_route)\"\n                                data-toggle=\"collapse\"\n                                :href=\"'#collapse_pad'+index\"\n                                role=\"button\"\n                                aria-expanded=\"false\"\n                                :aria-controls=\"'#collapse'+index\"\n                                class=\"d-flex align-items-center list-group-item list-group-item-action\">\n                                <i :class=\"[item.icon,'font-weight-bold text-info mr-1 ml-2 mb-0 h3']\"></i>\n                                {{item.description}} <!--<span class=\"ml-1 badge badge-gray text-info\">{{item.total_pacientes}}</span>-->\n                            </a>\n\n                        </div>\n\n\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 pl-3 pr-3 pt-1 pb-1\">\n                            <button id=\"regresar\"  data-dismiss=\"modal\" aria-label=\"Close\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                        </div>\n                    </div>\n                </div>\n            ",
        data: function data() {
          return {
            items: [{
              id: 1,
              description: "Torre Hospitalización",
              icon: "pc-light-hospital",
              param_route: "aq_torre_hosp",
              hasEvent: false,
              subItems: [] //total_pacientes: pacientes_datos.filter(x=>[418,422].includes( Number(x.cat_user_ubicacion_id) )).length

            }, {
              id: 1,
              description: "MAPM",
              icon: "pc-ambulatorio",
              param_route: "aq_mapm",
              hasEvent: false,
              subItems: [] //total_pacientes: pacientes_datos.filter(x=>[424,425].includes( Number(x.cat_user_ubicacion_id) )).length

            }],
            loading: document.getElementById('loadingScreen')
          };
        },
        props: {
          areaSiglas: String,
          FnAreaSiglas: Function
        },
        methods: {
          getAreaData: function getAreaData() {
            var _this2 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3() {
              var areaSiglas;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
                while (1) {
                  switch (_context3.prev = _context3.next) {
                    case 0:
                      areaSiglas = [null, undefined, 'undefined'].includes(localStorage.getItem('area')) ? 'tp' : localStorage.getItem('area');

                      _this2.FnAreaSiglas(areaSiglas); //this.selectorAreaName.textContent = this.items.find(x=>x.siglas === areaSiglas).name


                      _this2.loading.classList.remove("d-none");

                      _context3.next = 5;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/vistas/" + areaSiglas);

                    case 5:
                      pacientes_datos = _context3.sent;
                      EventBus.$emit('externalVarChanged', pacientes_datos);
                      that.AppBtnTotales(pacientes_datos);

                      _this2.loading.classList.add("d-none");

                    case 9:
                    case "end":
                      return _context3.stop();
                  }
                }
              }, _callee3);
            }))();
          },
          handleAreaOption: function handleAreaOption(areaSiglas) {
            console.log(areaSiglas);
            this.FnAreaSiglas(areaSiglas);
            localStorage.setItem("area", areaSiglas);
            this.getAreaData();
            $("#messageModal").modal("hide");
          }
        },
        mounted: function mounted() {}
      };
    }
  }, {
    key: "AppMenuEgresos",
    value: function AppMenuEgresos() {
      return {
        el: "#AppMenuEgresos",
        template:
        /*html */
        "\n                <div id=\"AppMenuEgresos\">\n                    <div id=\"menuPatiencare\" class=\"list-group\">\n                        <span class=\"text-primary font-weight-bold menu-pad-title\">\n                            Men\xFA Egresos\n                        </span>\n                        <div v-for=\"(item,index) in items\" :key=\"index\">\n                            <a\n                                v-if=\"!item.hasEvent\"\n                                data-toggle=\"collapse\"\n                                :href=\"'#collapse_pad'+index\"\n                                role=\"button\"\n                                aria-expanded=\"false\"\n                                :aria-controls=\"'#collapse'+index\"\n                                class=\"d-flex align-items-center list-group-item list-group-item-action\">\n                                <i :class=\"[item.icon,'font-weight-bold text-info mr-1 ml-2 mb-0 h3']\"></i>\n                                {{item.description}}\n                            </a>\n                            <a\n                                v-else\n                                @click=\"handleAreaOption(item.param_route)\"\n                                :href=\"'#collapse'+index\"\n                                role=\"button\"\n                                class=\"d-flex align-items-center list-group-item list-group-item-action\">\n                                <i :class=\"[item.icon,'font-weight-bold text-info mr-1 ml-2 mb-0 h3']\"></i>\n                                {{item.description}}\n                            </a>\n                            <div v-if=\"!item.hasEvent\" class=\"collapse\" :id=\"'collapse_pad'+index\">\n                                <div class=\"card mb-0 card-body py-0\">\n                                    <a\n                                        v-for=\"(item2,index2) in item.subItems\" :key=\"index2\"\n                                        @click=\"handleAreaOption(item2.param_route)\"\n                                        href=\"#\"\n                                        class=\"border-0 list-group-item p-2 list-group-item-action\"\n                                    >\n                                        <i :class=\"[item2.icon,'text-info']\"></i> {{item2.description}}\n                                    </a>\n                                </div>\n                            </div>\n                        </div>\n\n\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 pl-3 pr-3 pt-1 pb-1\">\n                        <button id=\"regresar\"  data-dismiss=\"modal\" aria-label=\"Close\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                    </div>\n                </div>\n            ",
        data: function data() {
          return {
            loading: document.getElementById('loadingScreen'),
            items: [{
              id: 1,
              description: "Altas",
              icon: "",
              param_route: "alta",
              hasEvent: true,
              subItems: []
            }, {
              id: 1,
              description: "Traslado a otra institución",
              icon: "",
              param_route: "traslado",
              hasEvent: true,
              subItems: []
            }, {
              id: 1,
              description: "Contraopinión médica",
              icon: "",
              param_route: "contraopinion",
              hasEvent: true,
              subItems: []
            }, {
              id: 1,
              description: "Fallecido",
              icon: "",
              param_route: "fallecido",
              hasEvent: true,
              subItems: []
            }, {
              id: 1,
              description: "Otro",
              icon: "",
              param_route: "otro",
              hasEvent: true,
              subItems: []
            }]
          };
        },
        props: {
          areaSiglas: String,
          FnAreaSiglas: Function
        },
        methods: {
          getAreaData: function getAreaData() {
            var _this3 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4() {
              var areaSiglas;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
                while (1) {
                  switch (_context4.prev = _context4.next) {
                    case 0:
                      areaSiglas = [null, undefined, 'undefined'].includes(localStorage.getItem('area')) ? 'tp' : localStorage.getItem('area');

                      _this3.FnAreaSiglas(areaSiglas); //this.selectorAreaName.textContent = this.items.find(x=>x.siglas === areaSiglas).name
                      //this.loading.classList.remove("d-none")


                      _this3.loading.classList.remove("d-none");

                      _context4.next = 5;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/vistas/" + areaSiglas);

                    case 5:
                      pacientes_datos = _context4.sent;
                      EventBus.$emit('externalVarChanged', pacientes_datos);

                      _this3.loading.classList.add("d-none");

                    case 8:
                    case "end":
                      return _context4.stop();
                  }
                }
              }, _callee4);
            }))();
          },
          handleAreaOption: function handleAreaOption(areaSiglas) {
            console.log(areaSiglas);
            this.FnAreaSiglas(areaSiglas);
            localStorage.setItem("area", areaSiglas);
            this.getAreaData();
            $("#messageModal").modal("hide");
          }
        },
        mounted: function mounted() {}
      };
    }
  }, {
    key: "AppBtnUbicacion",
    value: function AppBtnUbicacion() {
      /*
      <div :class="[{'border-right':item.area_lv_2 !==''},'d-flex px-1 justify-content-center flex-column']" style="line-height:0.5rem;height:100%">
                          <div class="h6 mb-0 text-nowrap mt-auto">
                              {{item.area_lv_1}}
                          </div>
                      </div>
                      <div :class="[{'border-right':item.area_lv_3 !==''},'d-flex px-1 justify-content-center flex-column']" style="line-height:0.5rem">
                          <div v-if="is_hospitalizacion(item.parent_cat_user_ubicacion_id)" style="font-size:0.7rem;">ALA</div>
                            <div class="h6 mb-0 text-nowrap">
                              {{item.area_lv_2}}
                          </div>
                      </div>
                      <div :class="[{'border-right':item.area_lv_4 !==''},'d-flex px-1 justify-content-center flex-column']"style="line-height:0.5rem">
                          <div v-if="is_hospitalizacion(item.parent_cat_user_ubicacion_id)" style="font-size:0.7rem;">HAB</div>
                              <div class="h6 mb-0 text-nowrap">
                                  {{item.area_lv_3}}
                              </div>
                          </div>
                          <div :class="[{'border-right':item.area_lv_4 !==''},'d-flex px-1 justify-content-center flex-column']"style="line-height:0.5rem">
                              <div class="h6 mb-0 text-nowrap">
                                  {{item.area_lv_4}}
                              </div>
                          </div>
                      </div>
        */
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        data: function data() {
          return {
            item: {}
          };
        },
        template:
        /*HTML */
        "\n                <button\n                    @click=\"handle_historial_ubicaciones()\"\n                    class=\"w-100 mr-1 mr-xl-0 order-1 order-1 btn btn-outline-gray p-1 d-flex flex-column justify-content-center align-items-center text-uppercase rounded\"\n                >\n                    <b class=\"small-title text-primary\">Ubicaci\xF3n</b>\n                    <div class=\"flex-fill d-flex justify-content-center  align-items-end\">\n                        {{patient_data.ubicacion}}\n                    </div>\n                </button>\n\n            ",
        methods: {
          handle_historial_ubicaciones: function handle_historial_ubicaciones() {
            _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_2__["default"].historialUbiEpisodio('.modal-body', this.patient_data.user_id);
          },
          is_hospitalizacion: function is_hospitalizacion(ubicacion_id) {
            var parent_ubicacion_id = [42, 71, 235, 236, 99, 127, 234, 155];

            if (parent_ubicacion_id.includes(ubicacion_id)) {
              return true;
            } else {
              return false;
            }
          }
        },
        mounted: function mounted() {
          var _this4 = this;

          this.item = cat_ubicaciones.find(function (x) {
            return x['id'] === _this4.patient_data.cat_user_ubicacion_id;
          });
        }
      };
    }
  }, {
    key: "AppBtnDias",
    value: function AppBtnDias() {
      return {
        data: function data() {
          return {
            h: null,
            m: null
          };
        },
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div :class=\"[([6,7,10,2,3,270,29,32].includes(this.patient_data.parent_cat_user_ubicacion_id) || [405,406].includes(this.patient_data.cat_user_ubicacion_id) ? 'col-xl-12'+' btn-'+patient_data.status_color_emergencia :'col-xl-6 btn-outline-gray' ) ,'mr-1 my-xl-1 mr-xl-0   justify-content-around order-2 btn p-1 d-flex justify-content-center align-items-center text-uppercase rounded']\">\n                    <div :class='[\"flex-column\",([6,7,10,2,3,270,29,32].includes(this.patient_data.parent_cat_user_ubicacion_id) || [405,406].includes(this.patient_data.cat_user_ubicacion_id) ? \"d-none\":\"d-flex\")]'>\n                        <b class=\"small-title text-primary\">D\xEDas</b>\n                        <div class=\"text-secondary mt-auto\">\n                        {{patient_data.dias}}\n                        </div>\n                    </div>\n                    <div :class=\"[([6,7,10,2,3,270,29,32].includes(this.patient_data.parent_cat_user_ubicacion_id) || [405,406].includes(this.patient_data.cat_user_ubicacion_id) ? 'd-flex':'d-none'),'flex-column']\">\n                        <b class=\"small-title\">Horas</b>\n                        <div :class=\"['mt-auto']\">\n                        <i :class=\"['fas fa-clock', {'d-none':!['warning','danger'].includes(patient_data.status_color_emergencia)},{'ping':patient_data.status_color_emergencia==='warning'},{'ping-2':patient_data.status_color_emergencia==='danger'}]\"></i> {{JSON.parse(patient_data.tiempo_transcurrido).h}} H, {{JSON.parse(patient_data.tiempo_transcurrido).m}} M\n                        </div>\n                    </div>\n                </div>\n            "
      };
    }
  }, {
    key: "AppPrealta",
    value: function AppPrealta() {
      return {
        template:
        /*html*/
        "\n                <button\n                    v-if=\"calendarOpen\"\n                    @click=\"createPrealta()\"\n\n                    :class=\"[getButtonVisible() ,{'heartbeat_infinity':['danger','warning'].includes(prealtaColor)}, 'col-xl d-flex flex-column flex-sm-row order-4 btn p-1 justify-content-center align-items-center text-uppercase rounded']\"\n                >\n                    <div class=\"px-1 border-sm-right pr-1\" style=\"font-size: 0.7rem;\">\n                        <i class=\"fas fa-stopwatch d-none d-sm-block\"></i> {{title}}\n                    </div>\n                    <div class=\"d-flex flex-column justify-content-center\">\n                        <div style=\"font-size: 2rem;font-weight:bold;line-height: 0.8;\">\n                            {{preAltaDay}}\n                        </div>\n                        <div style=\"font-size: 0.8rem;\">\n                            {{prealtaMonth}}\n                        </div>\n                    </div>\n                </button>\n                <button\n                    v-else\n                    @click=\"createPrealta()\"\n                    :class=\"[getButtonVisible() ,{'heartbeat_infinity':['danger','warning'].includes(prealtaColor)}, 'col-xl d-flex order-4 btn p-1 flex-column justify-content-center align-items-center text-uppercase rounded']\"\n                >\n                    <b class=\"small-title text-primary\">{{title}}</b>\n                    <i class=\"far fa-calendar text-gray  mt-auto\"></i>\n                </button>\n            ",
        data: function data() {
          return {
            title: "PREALTA",
            calendarOpen: false,
            preAltaDay: 0,
            prealtaMonth: "No indicado",
            prealtaColor: "default",
            item: []
          };
        },
        props: {
          patient_data: Object,
          index: Number
        },
        methods: {
          have_Prealta: function have_Prealta() {
            //console.log(this.patient_data.pre_alta);
            var fecha_prealta = this.patient_data.pre_alta; //console.log(fecha_prealta);

            if (fecha_prealta !== null) {
              this.calendarOpen = true;
              fecha_prealta = fecha_prealta.split(" ")[0];
              fecha_prealta = fecha_prealta.split("-"); //console.log(fecha_prealta);

              this.preAltaDay = fecha_prealta[2];
              this.prealtaMonth = this.meses(Number(fecha_prealta[1]) - 1).slice(0, 3);
              this.getColorPrealta();
            } else {
              this.calendarOpen = false;
            }
          },
          getButtonVisible: function getButtonVisible() {
            return this.calendarOpen ? 'btn-' + this.prealtaColor : "btn-outline-gray";
          },
          getColorPrealta: function getColorPrealta() {
            // Define una fecha dada
            var fecha_prealta = this.patient_data.pre_alta;
            var fechaDada = new Date(fecha_prealta); // Cambia la fecha según tus necesidades
            // Obtén la fecha actual

            var fechaActual = new Date(); // Calcula la diferencia en milisegundos

            var diferenciaMilisegundos = fechaDada - fechaActual; // Convierte la diferencia de milisegundos a días

            var diferenciaDias = diferenciaMilisegundos / (1000 * 60 * 60 * 24); // Redondea la diferencia a un número entero (puedes ajustar el redondeo según tus necesidades)

            var diasPorDelante = Math.ceil(diferenciaDias);

            if (diasPorDelante <= 0) {
              this.prealtaColor = "danger";
            }

            if (diasPorDelante === 1) {
              this.prealtaColor = "warning";
            }

            if (diasPorDelante >= 2) {
              this.prealtaColor = "success";
            } // console.log(`Días por delante: ${diasPorDelante}`);

          },
          meses: function meses(p) {
            var mes = ["Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio", "Julio", "Agosto", "Septiembre", "Octubre", "Noviembre", "Diciembre", "Enero"];
            return mes[p];
          },
          handleDaySelected: function handleDaySelected(e) {
            //Obtener dia actual
            var currentDay = new Date(); //obtener dia actual en formato milisegundos

            var currentDayMilisecons = new Date(currentDay.getFullYear(), currentDay.getMonth() + 1, currentDay.getDate() + 1); //Obtener dia seleccionado en el calendario

            var selectedDay = new Date(e.date); //Obtener el dia seleccionado en milisegundos

            var selectedDayMilisecons = new Date(selectedDay.getFullYear(), selectedDay.getMonth() + 1, selectedDay.getDate() + 1);

            if (selectedDayMilisecons.getTime() < currentDayMilisecons.getTime()) {
              alert("La fecha ingresada no puede ser anterior a hoy.");
              return false;
            } else {
              this.patient_data.pre_alta = selectedDay.getFullYear() + '-' + (selectedDay.getMonth() + 1).toString().padStart(2, '0') + '-' + selectedDay.getDate().toString().padStart(2, '0'); //console.log(this.patient.prealta);
            }
          },
          updatePrealta: function updatePrealta() {
            var _this5 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5() {
              var formdata;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
                while (1) {
                  switch (_context5.prev = _context5.next) {
                    case 0:
                      formdata = new FormData();
                      formdata.append("user_id", _this5.patient_data.user_id);
                      formdata.append("_token", $("#_token").val());
                      formdata.append("valor", _this5.patient_data.pre_alta);
                      _context5.next = 6;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_episodio/prealta", formdata);

                    case 6:
                      return _context5.abrupt("return", _context5.sent);

                    case 7:
                    case "end":
                      return _context5.stop();
                  }
                }
              }, _callee5);
            }))();
          },
          createPrealta: function createPrealta() {
            var _this6 = this;

            var that = this;
            var modal = document.querySelector("#messageModal div.modal-body");
            modal.innerHTML =
            /*html */
            "\n                            <div class=\"container-fluid\">\n                                <div class=\"row\">\n                                    <div class=\"col-12 h3 text-primary font-weight-bold text-center\">\n                                        Fecha Probable de Alta\n                                    </div>\n                                </div>\n                            </div>\n                            <div id=\"calendar\" data-user_id=\"".concat(this.patient_data.user_id, "\" class=\"daterange\" style=\"width: 100%\"></div>\n                            <div class=\"container-fluid\">\n                                <div class=\"row\">\n                                    <div class=\"col-12\">\n                                        <button id=\"storePrealta\" data-user_id=\"").concat(this.patient_data.user_id, "\" class=\"btn btn-success btn-block my-1\">Confirmar</button>\n                                    </div>\n                                    <div class=\"col-12\">\n                                        <button id=\"destroyPrealta\" class=\"btn btn-primary btn-block my-1\">Cancelar Prealta</button>\n                                    </div>\n                                </div>\n                            </div>\n                        ");
            document.getElementById('storePrealta').addEventListener("click", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6() {
              var result;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
                while (1) {
                  switch (_context6.prev = _context6.next) {
                    case 0:
                      _context6.next = 2;
                      return that.updatePrealta();

                    case 2:
                      result = _context6.sent;
                      pacientes_datos[_this6.index] = _this6.patient_data;
                      that.have_Prealta();
                      $("#messageModal").modal("hide");

                    case 6:
                    case "end":
                      return _context6.stop();
                  }
                }
              }, _callee6);
            })));
            document.getElementById('destroyPrealta').addEventListener("click", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee7() {
              var result;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee7$(_context7) {
                while (1) {
                  switch (_context7.prev = _context7.next) {
                    case 0:
                      _this6.patient_data.pre_alta = null;
                      _context7.next = 3;
                      return that.updatePrealta();

                    case 3:
                      result = _context7.sent;
                      pacientes_datos[_this6.index] = _this6.patient_data;
                      that.have_Prealta();
                      $("#messageModal").modal("hide");

                    case 7:
                    case "end":
                      return _context7.stop();
                  }
                }
              }, _callee7);
            })));
            $('#calendar').datepicker({
              language: "es",
              toggleActive: true,
              todayHighlight: true
            });
            $('#calendar').datepicker().on("changeDate", function (e) {
              that.handleDaySelected(e);
            });
            $("#messageModal").modal("show");
          }
        },
        mounted: function mounted() {
          var _this7 = this;

          return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee8() {
            return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee8$(_context8) {
              while (1) {
                switch (_context8.prev = _context8.next) {
                  case 0:
                    //this.patient_data
                    //this.index =
                    _this7.have_Prealta(); //console.log(this.item);


                  case 1:
                  case "end":
                    return _context8.stop();
                }
              }
            }, _callee8);
          }))();
        }
      };
    }
  }, {
    key: "AppBtnTrasladoAmbulancia",
    value: function AppBtnTrasladoAmbulancia() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        data: function data() {
          return {
            selector: null,
            propName: "btn_traslado_ambulancia"
          };
        },
        template:
        /*HTML */
        "\n                <button\n                    :id=\"propName+'_'+patient_data.episodio_id\"\n                    @click=\"handle_historial_ubicaciones()\"\n                    :class=\"[([6,7,10,2,3,270,29,32].includes(this.patient_data.parent_cat_user_ubicacion_id) || [405,406].includes(this.patient_data.cat_user_ubicacion_id) ? 'col-xl-12 mb-xl-1 mr-1' :'col-xl-6 my-xl-1' ) ,' order-3 btn btn-outline-gray p-1 d-flex flex-column justify-content-center align-items-center text-uppercase rounded']\"\n                >\n                    <b class=\"small-title text-primary\">Traslado</b>\n                    <div class=\"mt-auto\">\n                        <i :class=\"['fas fa-ambulance',(isActive ? 'text-danger' : 'text-gray')]\"></i>\n                    </div>\n                </button>\n            ",
        computed: {
          isActive: function isActive() {
            return this.patient_data.traslado_ambulancia !== null ? true : false;
          }
        },
        methods: {
          handle_historial_ubicaciones: function handle_historial_ubicaciones() {
            _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_2__["default"].trasladoAmbulanciaIndex(this.patient_data.episodio_id, this.patient_data.user_id);
          },
          destroyTooltip: function destroyTooltip() {
            var btn = this.selector;

            if (btn._tippy !== undefined) {
              var instance = btn._tippy;
              instance.destroy();
            }
          },
          activateTooltip: function activateTooltip() {
            this.destroyTooltip();

            if (this.patient_data.traslado_ambulancia !== null) {
              tippy(this.selector, {
                content: "Fecha del traslado: ".concat(this.patient_data.traslado_ambulancia.fecha_traslado),
                theme: 'tooltip-cmdlt-danger',
                zIndex: 200000,
                allowHTML: true
              });
            }
          }
        },
        mounted: function mounted() {
          this.selector = document.querySelector('#' + this.propName + '_' + this.patient_data.episodio_id); //console.log('#'+this.propName+'_'+this.patient_data.episodio_id);
          //console.log(this.selector);

          this.activateTooltip();
        }
      };
    }
  }, {
    key: "AppBtnRiesgoHerida",
    value: function AppBtnRiesgoHerida() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*html*/
        "\n                <div class=\"dropdown\">\n                    <button\n                        :id=\"propName+'_'+patient_data.user_id\"\n                        class=\"btn btn-default p-1 dropdown-toggle\"\n                        type=\"button\"\n                        data-toggle=\"dropdown\"\n                        aria-haspopup=\"true\"\n                        aria-expanded=\"false\"\n                    >\n                    <i :class=\"[icon,('text-'+getColor),{'ping-2':Number(patient_data[this.propName])==1},{'ping':Number(patient_data[this.propName])==2}]\"></i>\n                    </button>\n                    <div class=\"dropdown-menu\" aria-labelledby=\"dropdownMenuButton\">\n                        <a @click=\"create(3)\" href=\"#\" :class=\"[{'active':Number(riesgo_value)===3},'dropdown-item']\">\n                            <span :class=\"[icon,'text-success']\"></span>\n                            Estable\n                        </a>\n                        <a @click=\"create(2)\" href=\"#\" :class=\"[{'active':Number(riesgo_value)===2},'dropdown-item']\">\n                            <span :class=\"[icon,'text-warning']\"></span>\n                            Intermedio\n                        </a>\n                        <a @click=\"create(1)\" href=\"#\" :class=\"[{'active':Number(riesgo_value)===1},'dropdown-item']\">\n                            <span :class=\"[icon,'text-danger']\"></span>\n                            De cuidado\n                        </a>\n                    </div>\n                </div>\n            ",
        data: function data() {
          return {
            index: null,
            icon: "pc-cirugia",
            title: "Riesgo de Herida",
            propName: "cirugia",
            riesgo_coment: "",
            riesgo_value: 1,
            riesgo_color: "success",
            item: []
          };
        },
        computed: {
          getTooltipMessage: function getTooltipMessage() {
            var riesgo = "";

            if (this.patient_data[this.propName] == 1 || this.patient_data[this.propName] == null) {
              riesgo = 'Estable';
            }

            if (this.patient_data[this.propName] == 2) {
              riesgo = 'Intermedio';
            }

            if (this.patient_data[this.propName] == 3) {
              riesgo = 'De cuidado';
            }

            return (
              /*html*/
              "\n                        <b>\n                            <i class='".concat(this.icon, "'></i>\n                            ").concat(this.title, ":\n                        </b>\n                        ").concat(riesgo, "\n                        <small class=\"").concat([null, 'null', undefined, 'undefined', ''].includes(this.patient_data[this.propName]) || [null, 'null', undefined, 'undefined', ''].includes(this.patient_data[this.propName + '_description']) ? 'd-none' : '', "\">\n                            <br>\n                            ").concat(this.patient_data[this.propName + '_description'], "\n                        </small>\n                    ")
            );
          },
          getColor: function getColor() {
            if (Number(this.patient_data[this.propName]) == 1 || Number(this.patient_data[this.propName]) == null) {
              return 'danger';
            }

            if (Number(this.patient_data[this.propName]) == 2) {
              return 'warning';
            }

            if (Number(this.patient_data[this.propName]) == 3 || this.patient_data[this.propName] === null) {
              return 'success';
            }
          }
        },
        methods: {
          have_item: function have_item() {
            if (this.patient_data[this.propName] !== null) {
              this.riesgo_value = this.patient_data[this.propName];
              this.riesgo_coment = this.patient_data[this.propName + '_description'];
            } else {
              this.riesgo_value = 1;
              this.riesgo_coment = null;
            }

            this.activateTooltip();
          },
          view1: function view1() {
            return (
              /*html */
              "\n                        <div class=\"container-fluid py-3\">\n                            <div class=\"row\">\n                                <div class=\"col-md-12\">\n                                    <textarea\n                                        class=\"form-control\"\n                                        placeholder=\"Escribe un comentario sobre tu selecci\xF3n. (Opcional)\"\n                                        rows=\"3\"\n                                        id=\"coment\"></textarea>\n                                </div>\n                            </div>\n                            <div class=\"row\">\n                                <div class=\"col-md-12 pt-3\">\n                                    <a id=\"submit\" class=\"btn btn-block btn-primary\"  href=\"#\" role=\"button\">Guardar</a>\n                                </div>\n                            </div>\n                        </div>\n                    "
            );
          },
          destroyTooltip: function destroyTooltip() {
            var btn = document.querySelector('#' + this.propName + '_' + this.patient_data.user_id);

            if (btn._tippy !== undefined) {
              var instance = btn._tippy;
              instance.destroy();
            }
          },
          activateTooltip: function activateTooltip() {
            this.destroyTooltip();
            tippy('#' + this.propName + '_' + this.patient_data.user_id, {
              content: this.getTooltipMessage,
              theme: 'tooltip-cmdlt-' + this.getColor,
              zIndex: 200000,
              allowHTML: true
            });
          },
          store: function store(value) {
            var _this8 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee9() {
              var formData, model;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee9$(_context9) {
                while (1) {
                  switch (_context9.prev = _context9.next) {
                    case 0:
                      _this8.riesgo_value = Number(value);
                      _this8.riesgo_coment = document.getElementById('coment').value;
                      formData = new FormData();
                      formData.append("episodio_id", _this8.patient_data.episodio_id);
                      formData.append("value", _this8.riesgo_value);
                      formData.append("user_id_paciente", _this8.patient_data.user_id);
                      formData.append("description", !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(_this8.riesgo_coment) ? _this8.riesgo_coment : null);
                      formData.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                      formData.append("_token", $("#_token").val());
                      _context9.next = 11;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_riesgo_quirurgico/store", formData);

                    case 11:
                      model = _context9.sent;
                      _this8.patient_data[_this8.propName] = Number(_this8.riesgo_value);
                      _this8.patient_data[_this8.propName + '_description'] = _this8.riesgo_coment;
                      pacientes_datos[_this8.index] = _this8.patient_data[_this8.propName];

                      _this8.have_item();
                      /* Swal.fire(
                          `Riesgo actualizado!`,
                          `La registro se hizo correctamente.`,
                          'success'
                      ) */


                      $("#messageModal").modal("hide");

                    case 17:
                    case "end":
                      return _context9.stop();
                  }
                }
              }, _callee9);
            }))();
          },
          create: function create(value) {
            var that = this;
            $("#messageModal").modal("show");
            $("#messageModal .modal-body").html(this.view1());
            document.getElementById('submit').addEventListener("click", function (e) {
              that.store(value);
            });
          }
        },
        mounted: function mounted() {
          this.have_item();
        }
      };
    }
  }, {
    key: "AppBtnRiesgoCaida",
    value: function AppBtnRiesgoCaida() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*html*/
        "\n                <div class=\"dropdown\">\n                    <button\n                        :id=\"propName+'_'+patient_data.user_id\"\n                        class=\"btn btn-default p-1 dropdown-toggle\"\n                        type=\"button\"\n                        data-toggle=\"dropdown\"\n                        aria-haspopup=\"true\"\n                        aria-expanded=\"false\"\n                    >\n                        <i :class=\"[icon,('text-'+getColor),{'ping-2':riesgo_value==1},{'ping':riesgo_value==2}]\"></i>\n                    </button>\n                    <div class=\"dropdown-menu\" aria-labelledby=\"dropdownMenuButton\">\n                        <a @click=\"create(3)\" href=\"#\" :class=\"[{'active':riesgo_value==3},'dropdown-item']\">\n                            <span :class=\"[icon,'text-success']\"></span>\n                            Estable\n                        </a>\n                        <a @click=\"create(2)\" href=\"#\" :class=\"[{'active':riesgo_value==2},'dropdown-item']\">\n                            <span :class=\"[icon,'text-warning']\"></span>\n                            Intermedio\n                        </a>\n                        <a @click=\"create(1)\" href=\"#\" :class=\"[{'active':riesgo_value==1},'dropdown-item']\">\n                            <span :class=\"[icon,'text-danger']\"></span>\n                            De cuidado\n                        </a>\n                    </div>\n                </div>\n            ",
        data: function data() {
          return {
            index: null,
            icon: "pc-resbalar",
            title: "Riesgo de caida",
            propName: "resbalar",
            riesgo_coment: "",
            riesgo_value: 3,
            riesgo_color: "success",
            item: []
          };
        },
        computed: {
          getTooltipMessage: function getTooltipMessage() {
            var riesgo = "";

            if (this.patient_data[this.propName] == "3" || this.patient_data[this.propName] === null) {
              riesgo = 'Estable';
            }

            if (this.patient_data[this.propName] == "2") {
              riesgo = 'Intermedio';
            }

            if (this.patient_data[this.propName] == "1") {
              riesgo = 'De cuidado';
            }

            return (
              /*html*/
              "\n                        <b>\n                            <i class='".concat(this.icon, "'></i>\n                            ").concat(this.title, ":\n                        </b>\n                        ").concat(riesgo, "\n                        <small class=\"").concat([null, 'null', undefined, 'undefined', ''].includes(this.patient_data[this.propName]) || [null, 'null', undefined, 'undefined', ''].includes(this.patient_data[this.propName + '_description']) ? 'd-none' : '', "\">\n                            <br>\n                            ").concat(this.patient_data[this.propName + '_description'], "\n                        </small>\n                    ")
            );
          },
          getColor: function getColor() {
            if (this.patient_data[this.propName] == "3" || this.patient_data[this.propName] === null) {
              return 'success';
            }

            if (this.patient_data[this.propName] == "2") {
              return 'warning';
            }

            if (this.patient_data[this.propName] == "1") {
              return 'danger';
            }
          }
        },
        methods: {
          have_item: function have_item() {
            if (this.patient_data[this.propName] !== null) {
              this.riesgo_value = Number(this.patient_data[this.propName]);
              this.riesgo_coment = this.patient_data[this.propName + '_description'];
            } else {
              this.riesgo_value = "3";
              this.riesgo_coment = null;
            }

            this.activateTooltip();
          },
          view1: function view1() {
            return (
              /*html */
              "\n                        <div class=\"container-fluid py-3\">\n                            <div class=\"row\">\n                                <div class=\"col-md-12\">\n                                    <textarea\n                                        class=\"form-control\"\n                                        placeholder=\"Escribe un comentario sobre tu selecci\xF3n. (Opcional)\"\n                                        rows=\"3\"\n                                        id=\"coment\"></textarea>\n                                </div>\n                            </div>\n                            <div class=\"row\">\n                                <div class=\"col-md-12 pt-3\">\n                                    <a id=\"submit\" class=\"btn btn-block btn-primary\"  href=\"#\" role=\"button\">Guardar</a>\n                                </div>\n                            </div>\n                        </div>\n                    "
            );
          },
          destroyTooltip: function destroyTooltip() {
            var btn = document.querySelector('#' + this.propName + '_' + this.patient_data.user_id);

            if (btn._tippy !== undefined) {
              var instance = btn._tippy;
              instance.destroy();
            }
          },
          activateTooltip: function activateTooltip() {
            this.destroyTooltip();
            tippy('#' + this.propName + '_' + this.patient_data.user_id, {
              content: this.getTooltipMessage,
              theme: 'tooltip-cmdlt-' + this.getColor,
              zIndex: 200000,
              allowHTML: true
            });
          },
          store: function store(value) {
            var _this9 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee10() {
              var formData, model;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee10$(_context10) {
                while (1) {
                  switch (_context10.prev = _context10.next) {
                    case 0:
                      _this9.riesgo_value = Number(value);
                      _this9.riesgo_coment = document.getElementById('coment').value;
                      formData = new FormData();
                      formData.append("episodio_id", _this9.patient_data.episodio_id);
                      formData.append("value", _this9.riesgo_value);
                      formData.append("user_id_paciente", _this9.patient_data.user_id);
                      formData.append("description", !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(_this9.riesgo_coment) ? _this9.riesgo_coment : null);
                      formData.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                      formData.append("_token", $("#_token").val());
                      _context10.next = 11;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_resbalar/store", formData);

                    case 11:
                      model = _context10.sent;
                      _this9.patient_data[_this9.propName] = Number(_this9.riesgo_value);
                      _this9.patient_data[_this9.propName + '_description'] = _this9.riesgo_coment;
                      pacientes_datos[_this9.index] = _this9.patient_data[_this9.propName];

                      _this9.have_item();
                      /* Swal.fire(
                          `Riesgo actualizado!`,
                          `La registro se hizo correctamente.`,
                          'success'
                      ) */


                      $("#messageModal").modal("hide");

                    case 17:
                    case "end":
                      return _context10.stop();
                  }
                }
              }, _callee10);
            }))();
          },
          create: function create(value) {
            var that = this;
            $("#messageModal").modal("show");
            $("#messageModal .modal-body").html(this.view1());
            document.getElementById('submit').addEventListener("click", function (e) {
              that.store(value);
            });
          }
        },
        mounted: function mounted() {
          this.have_item();
        }
      };
    }
  }, {
    key: "AppBtnRiesgoAlerta",
    value: function AppBtnRiesgoAlerta() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*html*/
        "\n                <div class=\"dropdown\">\n                    <button\n                        :id=\"propName+'_'+patient_data.user_id\"\n                        class=\"btn btn-default p-1 dropdown-toggle\"\n                        type=\"button\"\n                        data-toggle=\"dropdown\"\n                        aria-haspopup=\"true\"\n                        aria-expanded=\"false\"\n                    >\n                        <i :class=\"[getIcon,('text-'+getColor),{'ping-2':Number(riesgo_value)===1},{'ping':Number(riesgo_value)===2}]\"></i>\n                    </button>\n                    <div class=\"dropdown-menu\" aria-labelledby=\"dropdownMenuButton\">\n                        <a @click=\"create(3)\" href=\"#\" :class=\"[{'active':riesgo_value===3},'dropdown-item']\">\n                            <span :class=\"['pc-alerta_estable','text-success']\"></span>\n                            Estable\n                        </a>\n                        <a @click=\"create(2)\" href=\"#\" :class=\"[{'active':riesgo_value===2},'dropdown-item']\">\n                            <span :class=\"['pc-alerta_intermedia','text-warning']\"></span>\n                            Intermedio\n                        </a>\n                        <a @click=\"create(1)\" href=\"#\" :class=\"[{'active':riesgo_value===1},'dropdown-item']\">\n                            <span :class=\"['pc-alerta_alta','text-danger']\"></span>\n                            De cuidado\n                        </a>\n                    </div>\n                </div>\n            ",
        data: function data() {
          return {
            index: null,
            icon: "",
            title: "Alerta de riesgo",
            propName: "alert",
            riesgo_coment: "",
            riesgo_value: 3,
            riesgo_color: "success",
            item: []
          };
        },
        computed: {
          getTooltipMessage: function getTooltipMessage() {
            var riesgo = "";

            if (Number(this.patient_data[this.propName]) === 3 || this.patient_data[this.propName] === null) {
              riesgo = 'Estable';
            }

            if (Number(this.patient_data[this.propName]) === 2) {
              riesgo = 'Intermedio';
            }

            if (Number(this.patient_data[this.propName]) === 1) {
              riesgo = 'De cuidado';
            }

            return (
              /*html*/
              "\n                        <b>\n                            <i class='".concat(this.getIcon, "'></i>\n                            ").concat(this.title, ":\n                        </b>\n                        ").concat(riesgo, "\n                        <small class=\"").concat([null, 'null', undefined, 'undefined', ''].includes(this.patient_data[this.propName]) || [null, 'null', undefined, 'undefined', ''].includes(this.patient_data[this.propName + '_description']) ? 'd-none' : '', "\">\n                            <br>\n                            ").concat(this.patient_data[this.propName + '_description'], "\n                        </small>\n                    ")
            );
          },
          getColor: function getColor() {
            if (Number(this.patient_data[this.propName]) === 3 || this.patient_data[this.propName] === null) {
              return 'success';
            }

            if (Number(this.patient_data[this.propName]) === 2) {
              return 'warning';
            }

            if (Number(this.patient_data[this.propName]) === 1) {
              return 'danger';
            }
          },
          getIcon: function getIcon() {
            if (Number(this.patient_data[this.propName]) === 3 || this.patient_data[this.propName] === null) {
              return 'pc-alerta_estable';
            }

            if (Number(this.patient_data[this.propName]) === 2) {
              return 'pc-alerta_intermedia';
            }

            if (Number(this.patient_data[this.propName]) === 1) {
              return 'pc-alerta_alta';
            }
          }
        },
        methods: {
          have_item: function have_item() {
            if (this.patient_data[this.propName] !== null) {
              this.riesgo_value = Number(this.patient_data[this.propName]);
              this.riesgo_coment = this.patient_data[this.propName + '_description'];
            } else {
              this.riesgo_value = 3;
              this.riesgo_coment = null;
            }

            this.activateTooltip();
          },
          view1: function view1() {
            return (
              /*html */
              "\n                        <div class=\"container-fluid py-3\">\n                            <div class=\"row\">\n                                <div class=\"col-md-12\">\n                                    <textarea\n                                        class=\"form-control\"\n                                        placeholder=\"Escribe un comentario sobre tu selecci\xF3n. (Opcional)\"\n                                        rows=\"3\"\n                                        id=\"coment\"></textarea>\n                                </div>\n                            </div>\n                            <div class=\"row\">\n                                <div class=\"col-md-12 pt-3\">\n                                    <a id=\"submit\" class=\"btn btn-block btn-primary\"  href=\"#\" role=\"button\">Guardar</a>\n                                </div>\n                            </div>\n                        </div>\n                    "
            );
          },
          destroyTooltip: function destroyTooltip() {
            var btn = document.querySelector('#' + this.propName + '_' + this.patient_data.user_id);

            if (btn._tippy !== undefined) {
              var instance = btn._tippy;
              instance.destroy();
            }
          },
          activateTooltip: function activateTooltip() {
            this.destroyTooltip();
            tippy('#' + this.propName + '_' + this.patient_data.user_id, {
              content: this.getTooltipMessage,
              theme: 'tooltip-cmdlt-' + this.getColor,
              zIndex: 200000,
              allowHTML: true
            });
          },
          store: function store(value) {
            var _this10 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee11() {
              var formData, model;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee11$(_context11) {
                while (1) {
                  switch (_context11.prev = _context11.next) {
                    case 0:
                      _this10.riesgo_value = Number(value);
                      _this10.riesgo_coment = document.getElementById('coment').value;
                      formData = new FormData();
                      formData.append("episodio_id", _this10.patient_data.episodio_id);
                      formData.append("alert", _this10.riesgo_value);
                      formData.append("user_id_paciente", _this10.patient_data.user_id);
                      formData.append("description", !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(_this10.riesgo_coment) ? _this10.riesgo_coment : null);
                      formData.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                      formData.append("_token", $("#_token").val());
                      _context11.next = 11;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_alert/store", formData);

                    case 11:
                      model = _context11.sent;
                      _this10.patient_data[_this10.propName] = Number(_this10.riesgo_value);
                      _this10.patient_data[_this10.propName + '_description'] = _this10.riesgo_coment;
                      pacientes_datos[_this10.index] = _this10.patient_data[_this10.propName];

                      _this10.have_item();
                      /* Swal.fire(
                          `Riesgo actualizado!`,
                          `La registro se hizo correctamente.`,
                          'success'
                      ) */


                      $("#messageModal").modal("hide");

                    case 17:
                    case "end":
                      return _context11.stop();
                  }
                }
              }, _callee11);
            }))();
          },
          create: function create(value) {
            var that = this;
            $("#messageModal").modal("show");
            $("#messageModal .modal-body").html(this.view1());
            document.getElementById('submit').addEventListener("click", function (e) {
              that.store(value);
            });
          }
        },
        mounted: function mounted() {
          this.have_item();
        }
      };
    }
  }, {
    key: "AppBtnPatientVip",
    value: function AppBtnPatientVip() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*html*/
        "\n                <div class=\"dropdown\">\n                    <button\n                        @click=\"create()\"\n                        :id=\"propName+'_'+patient_data.user_id\"\n                        class=\"btn btn-default p-1\"\n                        type=\"button\"\n                        aria-haspopup=\"true\"\n                        aria-expanded=\"false\"\n                    >\n                        <i :class=\"[icon,('text-'+getColor),{'text-info':Number(riesgo_value)===1},{'text-info-disabled':Number(riesgo_value)===0}]\"></i>\n                    </button>\n\n                </div>\n            ",
        data: function data() {
          return {
            index: null,
            icon: "pc-paciente_vip",
            title: "VIP",
            propName: "vip",
            riesgo_coment: "",
            riesgo_value: 0,
            riesgo_color: "",
            item: []
          };
        },
        computed: {
          getTooltipMessage: function getTooltipMessage() {
            return (
              /*html*/
              "\n                        <b>\n                            <i class='".concat(this.icon, "'></i>\n                            ").concat(this.title, ":\n                        </b>\n                        <small class=\"").concat([null, 'null', undefined, 'undefined', ''].includes(this.patient_data[this.propName]) || [null, 'null', undefined, 'undefined', ''].includes(this.patient_data[this.propName + '_description']) ? 'd-none' : '', "\">\n                            <br>\n                            ").concat(this.patient_data[this.propName + '_description'], "\n                        </small>\n                    ")
            );
          },
          getColor: function getColor() {
            return 'info';
            /* if (Number(this.patient_data[this.propName]) === 3 || this.patient_data[this.propName] === null) {
                return 'success'
            }
            if (Number(this.patient_data[this.propName]) === 2) {
                return 'warning'
            }
            if (Number(this.patient_data[this.propName]) === 1) {
                return 'danger'
            } */
          }
        },
        methods: {
          have_item: function have_item() {
            if (this.patient_data[this.propName] !== null) {
              this.riesgo_value = Number(this.patient_data[this.propName]);
              this.riesgo_coment = this.patient_data[this.propName + '_description'];
            } else {
              this.riesgo_value = 0;
              this.riesgo_coment = null;
            }

            this.activateTooltip();
          },
          view1: function view1() {
            return (
              /*html */
              "\n                        <div class=\"container-fluid py-3\">\n                            <div class=\"row\">\n                                <div class=\"col-md-12\">\n                                    <textarea\n                                        class=\"form-control\"\n                                        placeholder=\"Escribe un comentario sobre tu selecci\xF3n. (Opcional)\"\n                                        rows=\"3\"\n                                        id=\"coment\"></textarea>\n                                </div>\n                            </div>\n                            <div class=\"row\">\n                                <div class=\"col-md-12 pt-3\">\n                                    <a id=\"submit\" class=\"btn btn-block btn-primary\"  href=\"#\" role=\"button\">Guardar</a>\n                                </div>\n                            </div>\n                        </div>\n                    "
            );
          },
          destroyTooltip: function destroyTooltip() {
            var btn = document.querySelector('#' + this.propName + '_' + this.patient_data.user_id);

            if (btn._tippy !== undefined) {
              var instance = btn._tippy;
              instance.destroy();
            }
          },
          activateTooltip: function activateTooltip() {
            this.destroyTooltip();
            tippy('#' + this.propName + '_' + this.patient_data.user_id, {
              content: this.getTooltipMessage,
              theme: 'tooltip-cmdlt-' + this.getColor,
              zIndex: 200000,
              allowHTML: true
            });
          },
          store: function store() {
            var _this11 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee12() {
              var formData, model;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee12$(_context12) {
                while (1) {
                  switch (_context12.prev = _context12.next) {
                    case 0:
                      _this11.riesgo_coment = document.getElementById('coment').value;
                      formData = new FormData();
                      formData.append("episodio_id", _this11.patient_data.episodio_id);
                      formData.append("value", _this11.riesgo_value);
                      formData.append("user_id_paciente", _this11.patient_data.user_id);
                      formData.append("description", !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(_this11.riesgo_coment) ? _this11.riesgo_coment : null);
                      formData.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                      formData.append("_token", $("#_token").val());
                      _context12.next = 10;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_vip/store", formData);

                    case 10:
                      model = _context12.sent;
                      _this11.riesgo_value = Number(model.value);
                      console.log(model);
                      _this11.patient_data[_this11.propName] = Number(_this11.riesgo_value);
                      _this11.patient_data[_this11.propName + '_description'] = _this11.riesgo_coment;
                      pacientes_datos[_this11.index] = _this11.patient_data[_this11.propName];

                      _this11.have_item();
                      /* Swal.fire(
                          `Riesgo actualizado!`,
                          `La registro se hizo correctamente.`,
                          'success'
                      ) */


                      $("#messageModal").modal("hide");

                    case 18:
                    case "end":
                      return _context12.stop();
                  }
                }
              }, _callee12);
            }))();
          },
          create: function create() {
            var that = this;
            $("#messageModal").modal("show");
            $("#messageModal .modal-body").html(this.view1());
            document.getElementById('submit').addEventListener("click", function (e) {
              that.store();
            });
          }
        },
        mounted: function mounted() {
          this.have_item();
        }
      };
    }
  }, {
    key: "AppDataPaciente",
    value: function AppDataPaciente() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        components: {
          "app-paciente-vip": this.AppBtnPatientVip(),
          "app-riesgo-alerta": this.AppBtnRiesgoAlerta(),
          "app-riesgo-resbalar": this.AppBtnRiesgoCaida(),
          "app-riesgo-herida": this.AppBtnRiesgoHerida(),
          "app-tag-walkin": this.AppTagWalkin(),
          "app-tag-interconsulta": this.AppTagInterconsulta(),
          "app-tag-padel": this.AppTagPadel()
        },
        template:
        /*html */
        "\n            <div class=\"d-flex flex-column\">\n                <div v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\" class=\"order-1 d-flex justify-content-end\">\n                    <app-riesgo-herida :patient_data=\"patient_data\" :index=\"index\"></app-riesgo-herida>\n                    <app-riesgo-resbalar :patient_data=\"patient_data\" :index=\"index\"></app-riesgo-resbalar>\n                    <app-riesgo-alerta :patient_data=\"patient_data\" :index=\"index\"></app-riesgo-alerta>\n                    <app-paciente-vip :patient_data=\"patient_data\" :index=\"index\"></app-paciente-vip>\n                </div>\n                <div class=\"order-2 w-100  d-flex align-items-center\">\n                    <img\n                        :src=\"patient_data.imagen\"\n                        onerror=\"this.src='/image/system/icono-paciente-blanco.svg'\"\n                        loading=\"lazy\"\n                        class=\"align-self-start ml-1 mt-1 img-user-circle\"\n                        alt=\"Imagen No Encontrada\"\n                    >\n                    <div class=\"text-user-name text-primary text-uppercase pl-1\">\n                        {{patient_data.paciente}}\n                    </div>\n                </div>\n                <div class=\"order-3 d-flex flex-column\">\n\n                    <div class=\"tag-box px-1 d-flex  align-items-center flex-wrap pb-1 text-uppercase\">\n                        <div\n                            class=\"mt-1 badge d-flex align-items-center badge-primary mr-1\"\n                            style=\"font-size: 0.6rem; padding: 0.1rem;\"\n                        >\n                            <i class=\"mx-1 fas fa-id-card\"></i> <span style=\"font-size: 0.9rem; font-weight: 500;\">{{patient_data.cedula_formated}}</span>\n                        </div>\n                        <div\n                            class=\"mt-1 badge d-flex align-items-center badge-warning mr-1\"\n                            style=\"font-size: 0.6rem; padding: 0.1rem;\"\n                        >\n                            <i class=\"mx-1 fas fa-birthday-cake\"></i>\n                            <span style=\"font-size: 0.9rem; font-weight: 500;\">{{patient_data.edad}} A</span>\n                        </div>\n                        <div\n                            :class=\"['mt-1 badge d-flex align-items-center mr-1 text-uppercase',(patient_data.genero ==='f' ? 'badge-pink': 'badge-primary')]\"\n                            style=\"font-size: 0.6rem; padding: 0.1rem;\"\n                        >\n                            <i class=\"mx-1 fas fa-venus-mars\"></i>\n                            <span style=\"font-size: 0.9rem; font-weight: 500;\">{{patient_data.genero}}</span>\n                        </div>\n                        <app-tag-walkin v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\" :patient_data=\"patient_data\" :index=\"index\"></app-tag-walkin>\n                        <app-tag-padel v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\" :patient_data=\"patient_data\" :index=\"index\"></app-tag-padel>\n\n\n                    </div>\n                    <div>\n                        <app-tag-interconsulta :patient_data=\"patient_data\" :index=\"index\"></app-tag-interconsulta>\n                     \n                       \n                \n                    </div>\n                </div>\n\n            </div>\n        "
      };
    }
  }, {
    key: "AppBtnSignosVitales",
    value: function AppBtnSignosVitales() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <button\n                    data-toggle=\"collapse\"\n                    :href=\"'#collapse'+index\"\n                    role=\"button\"\n                    aria-expanded=\"false\"\n                    aria-controls=\"collapseExample\"\n                    class=\"d-xl-none btn btn-default btn-sm collapsed\"\n                >\n                    <i class=\"pc-signos_vitales1 h3 text-danger\"></i>\n                </button>\n            "
      };
    }
  }, {
    key: "AppBtnInfoPaciente",
    value: function AppBtnInfoPaciente() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div :class=\"['px-1',{'flex-fill':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))}]\">\n                    <a\n                        :id=\"'btn_infoPaciente_'+patient_data.user_id\"\n                        data-tippy-content=\"Datos del Paciente\"\n                        @click=\"handle_info_paciente()\"\n                        class=\"h4 cursor-pointer\"\n                    >\n                        <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"/image/system/info.svg\">\n                    </a>\n                </div>\n            ",
        methods: {
          handle_info_paciente: function handle_info_paciente() {
            userCuestPaciente.edit('.modal-body', this.patient_data.user_id);
          }
        }
      };
    }
  }, {
    key: "AppBtnEpisodiosAnteriores",
    value: function AppBtnEpisodiosAnteriores() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div :class=\"['px-1',{'flex-fill':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))}]\">\n                    <a\n                        :id=\"'btn_episodios_anteriores_'+patient_data.user_id\"\n                        @click=\"handle_episodios_anteriores()\"\n                        data-tippy-content=\"Historias anteriores\"\n                        class=\"h4 cursor-pointer\"\n                    >\n                        <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"/image/system/icono-new-historia.png\">\n                    </a>\n                </div>\n            ",
        methods: {
          handle_episodios_anteriores: function handle_episodios_anteriores() {
            _UserCuestEpisodio_js__WEBPACK_IMPORTED_MODULE_11__["default"].historialEpisodios(this.patient_data, this.index);
          }
        }
      };
    }
  }, {
    key: "AppBtnReingreso",
    value: function AppBtnReingreso() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div :class=\"['px-1',{'flex-fill':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))}]\">\n                    <a\n                        :id=\"'btn_reingresar_'+patient_data.user_id\"\n                        @click=\"handle_reingresar()\"\n                        data-tippy-content=\"Reingresar\"\n                        class=\"h4 cursor-pointer\"\n                    >\n                        <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"/image/system/icono-new-evolucion.svg\">\n                    </a>\n                </div>\n            ",
        methods: {
          handle_reingresar: function handle_reingresar() {
            var _this12 = this;

            Swal.fire({
              title: "Reingreso del paciente",
              text: "Esta acción creará un nuevo episodio.",
              //allowOutsideClick: false,
              icon: "info",
              showCancelButton: true,
              confirmButtonColor: "#3085d6",
              cancelButtonColor: "#d33",
              confirmButtonText: "De acuerdo!",
              cancelButtonText: "Cancelar"
            }).then(function (result) {
              if (result.isConfirmed) {
                _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_2__["default"].reingreso('.modal-body', _this12.patient_data.user_id);
              }
            });
          }
        }
      };
    }
  }, {
    key: "AppBtnRestablecerEpisodio",
    value: function AppBtnRestablecerEpisodio() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        data: function data() {
          return {
            items: pacientes_datos
          };
        },
        template:
        /*HTML */
        "\n                <div :class=\"['px-1',{'flex-fill':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))}]\">\n                    <a\n                        :id=\"'btn_restablecer_'+patient_data.user_id\"\n                        @click=\"handle_restablecer()\"\n                        title=\"Restablecer Episodio\"\n                        class=\"h4 mb-0 cursor-pointer text-primary\"\n                    >\n                        <i class=\"fas fa-undo\"></i>\n                    </a>\n                </div>\n            ",
        created: function created() {
          var that = this; // Escuchar cambios en el bus de eventos

          EventBus.$on('externalVarChanged', function (newValue) {
            that.items = newValue;
          });
        },
        methods: {
          handle_restablecer: function handle_restablecer() {
            var _this13 = this;

            var that = this;
            Swal.fire({
              title: "Restablecer el episodio",
              text: "Esta acción cerrará otros episodios activos.",
              //allowOutsideClick: false,
              icon: "warning",
              showCancelButton: true,
              confirmButtonColor: "#3085d6",
              cancelButtonColor: "#d33",
              confirmButtonText: "De acuerdo!",
              cancelButtonText: "Cancelar"
            }).then( /*#__PURE__*/function () {
              var _ref20 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee13(result) {
                var formdata, model;
                return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee13$(_context13) {
                  while (1) {
                    switch (_context13.prev = _context13.next) {
                      case 0:
                        if (!result.isConfirmed) {
                          _context13.next = 10;
                          break;
                        }

                        formdata = new FormData();
                        formdata.append("episodio_id", _this13.patient_data.episodio_id);
                        formdata.append("user_id", _this13.patient_data.user_id);
                        formdata.append("_token", d.querySelector("#_token").value);
                        _context13.next = 7;
                        return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_episodio/restablecer_episodio", formdata);

                      case 7:
                        model = _context13.sent;

                        if (model.success) {
                          pacientes_datos = that.items.filter(function (item) {
                            return item.episodio_id !== _this13.patient_data.episodio_id;
                          });
                          EventBus.$emit('externalVarChanged', pacientes_datos);
                        }

                        console.log(model);

                      case 10:
                      case "end":
                        return _context13.stop();
                    }
                  }
                }, _callee13);
              }));

              return function (_x2) {
                return _ref20.apply(this, arguments);
              };
            }());
          }
        }
      };
    }
  }, {
    key: "AppBtnInfoAdministrativa",
    value: function AppBtnInfoAdministrativa() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div class=\"flex-fill px-1\">\n                    <div class=\"btn-group\">\n                        <a\n                            @click=\"handle_info_administrativa()\"\n                            href=\"#\"\n                            class=\"heartbeat\"\n                            style=\"width: 30px;height: 30px;\"\n                            data-display=\"static\"\n                            aria-haspopup=\"true\"\n                            data-tippy-content=\"Datos Administrativos\"\n                            aria-expanded=\"false\"\n                        >\n                            <i class=\"pc-info_administracion_solid  text-purple h3 mb-0\"></i>\n                        </a>\n                    </div>\n                </div>\n            ",
        methods: {
          handle_info_administrativa: function handle_info_administrativa() {
            console.log(this.patient_data); //UserCuestAdministracion.index( this.patient_data.episodio_id )

            Object(_UserCuestAdministracion_js__WEBPACK_IMPORTED_MODULE_10__["default"])(this.patient_data, this.index);
          }
        }
      };
    }
  }, {
    key: "AppBtnEvolucion",
    value: function AppBtnEvolucion() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div :class=\"['px-1',{'flex-fill':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))}]\">\n                    <a\n                        :id=\"'btn_evolucion_'+patient_data.user_id\"\n                        @click=\"handle_evolucion()\"\n                        data-tippy-content=\"Evoluciones\"\n                        class=\"h4 cursor-pointer\"\n                    >\n                        <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"/image/system/icono-new-evolucion.svg\">\n                    </a>\n                </div>\n            ",
        methods: {
          handle_evolucion: function handle_evolucion() {
            userCuestHistoria.index(this.patient_data, this.index); //UserCuestHistoria.index('.modal-body',this.patient_data.user_id)
          }
        }
      };
    }
  }, {
    key: "AppBtnPendientes",
    value: function AppBtnPendientes() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div class=\"flex-fill px-1\">\n                    <div\n                        class=\"heartbeat d-flex\"\n                        style=\"position: relative;cursor:pointer;\"\n                        data-tippy-content=\"Pendientes\"\n                        @click=\"handle_pendientes\"\n                    >\n                        <img style=\"height: 35px;width: 33px;\" src=\"/image/system/todolist.svg\">\n                        <span v-if=\"containTasks\" data-tippy-content=\"Total Pendientes\" class=\"badge rounded-circle badge-danger align-self-start\" style=\"font-size: 0.6rem;\">\n                            {{patient_data.t_pendiente}}\n                        </span>\n\n\n\n                    </div>\n                </div>\n            ",
        computed: {
          containTasks: function containTasks() {
            return this.patient_data.t_pendiente > 0 ? true : false;
          }
        },
        methods: {
          handle_pendientes: function handle_pendientes() {
            userCuestPendiente.index('.modal-body', this.patient_data.user_id);
          }
        }
      };
    }
  }, {
    key: "AppBtnChatWhatsapp",
    value: function AppBtnChatWhatsapp() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div class=\"flex-fill px-1\">\n                    <div\n                        class=\"heartbeat\"\n                        style=\"position: relative;cursor:pointer;\"\n                        data-tippy-content=\"Whatsapp\"\n                        @click=\"handle_chat_whatsapp\"\n                    >\n                        <img style=\"height: 35px;width: 35px;\" src=\"/image/system/ws.png\">\n                    </div>\n                </div>\n            ",
        methods: {
          handle_chat_whatsapp: function handle_chat_whatsapp() {
            Object(_UserCuestChat_js__WEBPACK_IMPORTED_MODULE_13__["index"])(this.patient_data, this.index);
          }
        }
      };
    }
  }, {
    key: "AppBtnHistoria",
    value: function AppBtnHistoria() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div :class=\"['px-1',{'flex-fill':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))}]\">\n                    <div class=\"btn-group\">\n                        <a\n                            href=\"#\"\n                            data-toggle=\"dropdown\"\n                            data-display=\"static\"\n                            aria-haspopup=\"true\"\n                            data-tippy-content=\"Historia del Paciente\"\n                            aria-expanded=\"false\"\n                            class=\"d-flex \"\n                        >\n                            <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"/image/system/icono-new-historia.png\">\n                            <span v-if=\"containEpisodes\" title=\"Total Episodios\" class=\"badge rounded-circle badge-info align-self-start\" style=\"font-size: 0.6rem;\">\n                                {{patient_data.total_episodios}}\n                            </span>\n                        </a>\n                        <div class=\"dropdown-menu dropdown-menu-lg-right\">\n                            <a\n                                @click=\"handle_historia_inicial()\"\n                                data-tippy-content=\"Historia de Ingreso\"\n                                class=\"dropdown-item cursor-pointer\"\n                            >\n                                <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"/image/system/h_inicial.svg\"> Historia Inicial\n                            </a>\n                            <a\n                                @click=\"handle_episodios_anteriores()\"\n                                data-tippy-content=\"Historias anteriores\"\n                                class=\"dropdown-item cursor-pointer\"\n                            >\n                                <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"/image/system/h_episodios.svg\"> Episodios anteriores\n                                <span v-if=\"containEpisodes\" title=\"Total Episodios\" class=\"badge rounded-circle badge-info align-self-start\" style=\"font-size: 0.6rem;\">\n                                    {{patient_data.total_episodios}}\n                                </span>\n                            </a>\n                        </div>\n                    </div>\n                </div>\n            ",
        computed: {
          containEpisodes: function containEpisodes() {
            return this.patient_data.total_episodios > 0 ? true : false;
          }
        },
        methods: {
          handle_historia_inicial: function handle_historia_inicial() {
            Object(_UserCuestHistoria_js__WEBPACK_IMPORTED_MODULE_14__["create_historia_inicial"])(this.patient_data, this.index);
          },
          handle_episodios_anteriores: function handle_episodios_anteriores() {
            _UserCuestEpisodio_js__WEBPACK_IMPORTED_MODULE_11__["default"].historialEpisodios(this.patient_data, this.index);
          }
        }
      };
    }
  }, {
    key: "AppBtnGroup2",
    value: function AppBtnGroup2() {
      var that = this;
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        data: function data() {
          return {
            user_id_medico: null
          };
        },
        components: {
          'app-btn-signos-vitales': that.AppBtnSignosVitales(),
          'app-btn-info-paciente': that.AppBtnInfoPaciente(),
          'app-btn-episodios-anteriores': that.AppBtnEpisodiosAnteriores(),
          'app-btn-reingreso': that.AppBtnReingreso(),
          'app-btn-restablecer-episodio': that.AppBtnRestablecerEpisodio(),
          'app-btn-info-administrativa': that.AppBtnInfoAdministrativa(),
          'app-btn-historia': that.AppBtnHistoria(),
          'app-btn-evolucion': that.AppBtnEvolucion(),
          'app-btn-pendientes': that.AppBtnPendientes(),
          'app-btn-chat-whatsapp': that.AppBtnChatWhatsapp()
        },
        template:
        /*html*/
        "\n                <div :class=\"[\n                    'd-flex flex-nowrap align-items-center mb-2  text-center',\n                    {' justify-content-left':['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                    {' justify-content-around':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                    ]\">\n                    <app-btn-signos-vitales \n                        v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\" \n                    />\n                    <app-btn-info-paciente \n                        :patient_data=\"patient_data\" \n                        :index=\"index\" \n                    />\n                    <app-btn-episodios-anteriores \n                        v-if=\"['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\" \n                    />\n                    <app-btn-reingreso \n                        v-if=\"['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\" \n                    />\n                    <app-btn-info-administrativa \n                        v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\" \n                    />\n                    <app-btn-historia \n                        v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\" \n                    />\n                    <app-btn-evolucion \n                        v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\"\n                    />\n                    <app-btn-pendientes \n                        v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\"\n                    />\n                    <app-btn-chat-whatsapp \n                        v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\"\n                    /> \n                    <app-btn-restablecer-episodio\n                        v-if=\"['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area')) && \n                        [\n                            2523, //andrea\n                            2501, // teresa\n                            19, // parodi\n                            22, //pruebas\n                            19092 //gabriel\n                        ].includes(user_id_medico)\"\n                        :patient_data=\"patient_data\" \n                        :index=\"index\"\n                    />\n\n                </div>\n            ",
        mounted: function mounted() {
          this.user_id_medico = Number(userIdMedico);
        }
      };
    }
  }, {
    key: "AppTagWalkin",
    value: function AppTagWalkin() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div\n                    :id=\"'badge_walkin_'+patient_data['user_id']\"\n                    :class=\"[getVisibility(),'mt-1 mr-1 badge font-italic',getTextColor()]\"\n                    style=\" font-size: 0.8rem;\"\n                >\n                    <i :class=\"['pc-servicio_walkin ',getTextColor2()]\"></i> {{patient_data['servicio_emergencia']['description']}}\n                </div>\n            ",
        methods: {
          getVisibility: function getVisibility() {
            return [1, 2, 3, 4, 5, 6].includes(Number(this.patient_data['servicio_emergencia']['value'])) ? '' : 'd-none';
          },
          getTextColor2: function getTextColor2() {
            return [1, 2, 3, 4, 5].includes(Number(this.patient_data['servicio_emergencia']['value'])) ? 'text-white' : 'text-secondary';
          },
          getTextColor: function getTextColor() {
            return 'badge-' + this.patient_data['servicio_emergencia']['color'];
          }
        }
      };
    }
  }, {
    key: "AppTagInterconsulta",
    value: function AppTagInterconsulta() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        data: function data() {
          return {
            selector: null,
            tippyInstance: null,
            propName: "tag_interconsulta"
          };
        },
        template:
        /*HTML */
        "\n             \n                <div \n                    v-if=\"patient_data.interconsultation_request.filter(x=>!x.fecha_confirmacion).length > 0\"\n                    :id=\"'tag_interconsulta_'+patient_data['episodio_id']\"\n                    :class=\"['card-medic-team-active d-flex align-items-center']\"\n                    style=\"\n                        border: 1px solid #dfdcdc;\n                        border-radius: 5px;\n                        margin: 0 0 5px 5px;\n                        width: fit-content;\n                        padding: 0 5px;\n                        cursor: default;\n                    \"\n                >\n                    <!--<i class=\"far fa-bell text-info mx-1 ping\"></i>-->\n                    <div class=\"badge badge-info medico-badge-width scale-in-hor-left\">IN</div>\n                    <img loading=\"lazy\" style=\"width:20px;height:20px;border-radius:20px;\" class=\"img-doctor mx-1\" onerror=\"this.src='/image/system/patient.svg'\" src=\"/image/system/patient.svg\">\n                    <b class=\"text-nowrap text-dark\">\n                        {{getLastInterconsultation()}} \n                        <span class=\"badge mb-1 font-weight-bold text-info\">\n                            {{patient_data.interconsultation_request.filter(x=>!x.fecha_confirmacion).length > 1 ? '+' + (patient_data.interconsultation_request.filter(x=>!x.fecha_confirmacion).length-1) : ''}}\n                        </span>\n                    </b>\n                    \n                </div>\n            ",
        methods: {
          getLastInterconsultation: function getLastInterconsultation() {
            var lastMedic = this.patient_data.interconsultation_request.filter(function (x) {
              return !x.end_date;
            })[0]; // cero porque esta ordenado descendente el arreglo

            return lastMedic.medico;
          }
        },
        mounted: function mounted() {
          this.selector = document.querySelector('#' + this.propName + '_' + this.patient_data.episodio_id);
        }
      };
    }
  }, {
    key: "AppTagPadel",
    value: function AppTagPadel() {
      return {
        props: {
          patient_data: Object,
          index: Number
        },
        template:
        /*HTML */
        "\n                <div\n                    :id=\"'badge_padel_'+patient_data['user_id']\"\n                    :class=\"[getVisibility(),'mt-1 mr-1 badge font-italic badge-orange text-white']\"\n                    style=\" font-size: 0.8rem;\"\n                >\n                    <i class=\"pc-padel\"></i> Padel Fest 3\n                </div>\n            ",
        methods: {
          getVisibility: function getVisibility() {
            return [429].includes(Number(this.patient_data['cat_user_ubicacion_id'])) ? '' : 'd-none';
          }
        }
      };
    }
  }, {
    key: "AppFormTemperatura",
    value: function AppFormTemperatura() {
      return {
        props: {
          patient_data: Object,
          index: Number,
          fnOnEnter: Function,
          myInputValue: Number,
          myInputValue2: Number,
          myInputValue3: Number
        },
        data: function data() {
          return {
            unidadMedida: "°C",
            siglas: "temp"
          };
        },
        template:
        /*HTML */
        "\n                <div class=\"col-12 p-0 col-xl rounded d-flex  flex-xl-column border\">\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"w-custom-1 text-uppercase btn btn-outline-gray border-0\"\n                    >\n                        {{siglas}}\n                    </div>\n                    <input\n                        :id=\"siglas+'_'+patient_data['user_id']\"\n                        @keyup.enter=\"handleEnterKey('fr')\"\n                        v-model=\"inputValue\"\n                        type=\"text\"\n                        class=\"shadow-inset-center w-100 text-center input-full-height border-0\"\n                    >\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"p-0 btn btn-outline-gray border-0 font-weight-bold height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\"\n                    >\n                        <span\n                            v-if=\"!['nc','sc',null,'','NaN','NaN/NaN'].includes(patient_data[siglas])\"\n                            :class=\"[getColor(),'text-nowrap']\">{{patient_data[siglas]}} {{unidadMedida}}\n                        </span>\n                    </div>\n                </div>\n            ",
        computed: {
          // Crear un modelo computado para la props `value`
          inputValue: {
            get: function get() {
              return this.myInputValue;
            },
            set: function set(newValue) {
              // Emitir un evento llamado 'input' con el nuevo valor
              this.$emit('input', newValue);
            }
          }
        },
        methods: {
          handleEnterKey: function handleEnterKey(siglas) {
            this.fnOnEnter(siglas);
          },
          handle_item: function handle_item() {
            _UserCuestTemperatura_js__WEBPACK_IMPORTED_MODULE_3__["default"].index('.modal-body', this.patient_data.user_id, this.patient_data.episodio_id);
          },
          getColor: function getColor() {
            return _UserCuestTemperatura_js__WEBPACK_IMPORTED_MODULE_3__["default"].colorTemp(this.patient_data[this.siglas]);
          }
        }
      };
    }
  }, {
    key: "AppFormFrecuenciaRespiratoria",
    value: function AppFormFrecuenciaRespiratoria() {
      return {
        props: {
          patient_data: Object,
          index: Number,
          fnOnEnter: Function,
          myInputValue: Number
        },
        data: function data() {
          return {
            unidadMedida: "resp/min",
            siglas: "fr"
          };
        },
        template:
        /*HTML */
        "\n                <div class=\"col-12 p-0 col-xl rounded d-flex  flex-xl-column border\">\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"w-custom-1 text-uppercase btn btn-outline-gray border-0\"\n                    >\n                        {{siglas}}\n                    </div>\n                    <input\n                        :id=\"siglas+'_'+patient_data['user_id']\"\n                        @keyup.enter=\"handleEnterKey('fc')\"\n                        v-model=\"inputValue\"\n                        type=\"text\"\n                        class=\"shadow-inset-center w-100 text-center input-full-height border-0\"\n                    >\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"p-0 btn btn-outline-gray border-0 font-weight-bold height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\"\n                    >\n                        <span\n                            v-if=\"!['nc','sc',null,'','NaN','NaN/NaN'].includes(patient_data[siglas])\"\n                            :class=\"[getColor(),'text-nowrap']\">{{patient_data[siglas]}} {{unidadMedida}}\n                        </span>\n                    </div>\n                </div>\n            ",
        computed: {
          // Crear un modelo computado para la props `value`
          inputValue: {
            get: function get() {
              return this.myInputValue;
            },
            set: function set(newValue) {
              // Emitir un evento llamado 'input' con el nuevo valor
              this.$emit('input', newValue);
            }
          }
        },
        methods: {
          handleEnterKey: function handleEnterKey(siglas) {
            this.fnOnEnter(siglas);
          },
          handle_item: function handle_item() {
            _UserCuestFr_js__WEBPACK_IMPORTED_MODULE_4__["default"].index('.modal-body', this.patient_data.user_id, this.patient_data.episodio_id);
          },
          getColor: function getColor() {
            return _UserCuestFr_js__WEBPACK_IMPORTED_MODULE_4__["default"].colorFr(this.patient_data[this.siglas]);
          }
        }
      };
    }
  }, {
    key: "AppFormFrecuenciaCardiaca",
    value: function AppFormFrecuenciaCardiaca() {
      return {
        props: {
          patient_data: Object,
          fnOnEnter: Function,
          index: Number,
          myInputValue: Number
        },
        data: function data() {
          return {
            unidadMedida: "lat/min",
            siglas: "fc"
          };
        },
        template:
        /*HTML */
        "\n                <div class=\"col-12 p-0 col-xl rounded d-flex  flex-xl-column border\">\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"w-custom-1 text-uppercase btn btn-outline-gray border-0\"\n                    >\n                        {{siglas}}\n                    </div>\n                    <input\n                        :id=\"siglas+'_'+patient_data['user_id']\"\n                        @keyup.enter=\"handleEnterKey('tasis')\"\n                        v-model=\"inputValue\"\n                        type=\"text\"\n                        class=\"shadow-inset-center w-100 text-center input-full-height border-0\"\n                    >\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"p-0 btn btn-outline-gray border-0 font-weight-bold height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\"\n                    >\n                        <span\n                            v-if=\"!['nc','sc',null,'','NaN','NaN/NaN'].includes(patient_data[siglas])\"\n                            :class=\"[getColor(),'text-nowrap']\">{{patient_data[siglas]}} {{unidadMedida}}\n                        </span>\n                    </div>\n                </div>\n            ",
        computed: {
          // Crear un modelo computado para la props `value`
          inputValue: {
            get: function get() {
              return this.myInputValue;
            },
            set: function set(newValue) {
              // Emitir un evento llamado 'input' con el nuevo valor
              this.$emit('input', newValue);
            }
          }
        },
        methods: {
          handleEnterKey: function handleEnterKey(siglas) {
            this.fnOnEnter(siglas);
          },
          handle_item: function handle_item() {
            _UserCuestFc_js__WEBPACK_IMPORTED_MODULE_6__["default"].index('.modal-body', this.patient_data.user_id, this.patient_data.episodio_id);
          },
          getColor: function getColor() {
            return _UserCuestFc_js__WEBPACK_IMPORTED_MODULE_6__["default"].colorFc(this.patient_data[this.siglas]);
          }
        }
      };
    }
  }, {
    key: "AppFormTensionArterial",
    value: function AppFormTensionArterial() {
      return {
        data: function data() {
          return {
            unidadMedida: "mmHg",
            siglas: "ta"
          };
        },
        template:
        /*html */
        "\n                <div class=\"col-12 p-0 col-xl-2 rounded d-flex flex-xl-column border\">\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"w-custom-1 text-uppercase btn btn-outline-gray border-0\"\n                    >\n                        {{siglas}}\n                    </div>\n                    <div class=\"w-100 flex-lg-fill d-flex flex-column\">\n                        <input\n                            :id=\"'tasis_'+patient_data['user_id']\"\n                            @keyup.enter=\"handleEnterKey('tadia')\"\n                            v-model=\"input1\"\n                            @input=\"onInputChange('tasis', input1)\"\n                            type=\"text\"\n                            placeholder=\"Sis\"\n                            class=\"shadow-inset-center w-100 text-center input-full-height border-0\"\n                        >\n                        <hr class=\"border-top m-0\">\n                        <input\n                            :id=\"'tadia_'+patient_data['user_id']\"\n                            @keyup.enter=\"handleEnterKey('oxi')\"\n                            v-model=\"input2\"\n                            @input=\"onInputChange('tadia', input2)\"\n                            type=\"text\"\n                            placeholder=\"Dia\"\n                            class=\"shadow-inset-center w-100 text-center input-full-height border-0\"\n                        >\n                        <div class=\"flex-fill flex-column d-flex justify-content-center align-items-center text-secondary bg-light px-1\">\n                            <div>\n                                {{input3}}\n                            </div>\n                            <div class=\"mt-auto\" style=\"font-size:0.7rem;\">\n                                Media\n                            </div>\n\n                        </div>\n\n\n                    </div>\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"p-0 btn btn-outline-gray border-0 font-weight-bold height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\"\n                    >\n                        <span\n                            v-if=\"!['nc','sc',null,'','NaN','NaN/NaN'].includes(patient_data[siglas])\"\n                            :class=\"[getColor(),'text-nowrap']\">{{patient_data[siglas]}} {{unidadMedida}}\n                        </span>\n                    </div>\n\n                </div>\n            ",
        props: {
          patient_data: Object,
          fnOnEnter: Function,
          index: Number,
          input1: {
            type: Number,
            required: true
          },
          input2: {
            type: Number,
            required: true
          },
          input3: {
            type: Number,
            required: true
          }
        },
        computed: {
          getValues: function getValues() {
            var temCurrentValue = this.patient_data[this.siglas] === null ? [0, 0] : this.patient_data[this.siglas].split("/").map(function (x) {
              return Number(x);
            });
            return {
              dia: temCurrentValue[0],
              sys: temCurrentValue[1]
            };
          }
        },
        methods: {
          handleEnterKey: function handleEnterKey(siglas) {
            this.fnOnEnter(siglas);
          },
          onInputChange: function onInputChange(inputName, newValue) {
            // Emite un evento con el nombre del input y su nuevo valor
            this.$emit('input-change', {
              inputName: inputName,
              newValue: newValue
            });
          },
          handle_item: function handle_item() {
            _UserCuestTa_js__WEBPACK_IMPORTED_MODULE_5__["default"].index('.modal-body', this.patient_data.user_id, this.patient_data.episodio_id);
          },
          getColor: function getColor() {
            //UserCuestTa.colorTa(cadena[0],cadena[1])
            var temCurrentValue = this.patient_data[this.siglas] === null ? [0, 0] : this.patient_data[this.siglas].split("/").map(function (x) {
              return Number(x);
            });
            return _UserCuestTa_js__WEBPACK_IMPORTED_MODULE_5__["default"].colorTa(temCurrentValue[0], temCurrentValue[1]);
          }
        }
      };
    }
  }, {
    key: "AppFormOximetria",
    value: function AppFormOximetria() {
      return {
        props: {
          patient_data: Object,
          fnOnEnter: Function,
          index: Number,
          myInputValue: Number
        },
        data: function data() {
          return {
            unidadMedida: "%",
            siglas: "oxi"
          };
        },
        template:
        /*HTML */
        "\n                <div class=\"col-12 p-0 col-xl rounded d-flex  flex-xl-column border\">\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"w-custom-1 text-uppercase btn btn-outline-gray border-0\"\n                    >\n                        {{siglas}}\n                    </div>\n                    <input\n                        :id=\"siglas+'_'+patient_data['user_id']\"\n                        @keyup.enter=\"handleEnterKey('gl')\"\n                        v-model=\"inputValue\"\n                        type=\"text\"\n                        class=\"shadow-inset-center w-100 text-center input-full-height border-0\"\n                    >\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"p-0 btn btn-outline-gray border-0 font-weight-bold height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\"\n                    >\n                        <span\n                            v-if=\"!['nc',,null,'','NaN','NaN/NaN'].includes(patient_data[siglas])\"\n                            :class=\"[getColor(),'text-nowrap']\">{{patient_data[siglas]}} {{unidadMedida}}\n                        </span>\n                    </div>\n                </div>\n            ",
        computed: {
          // Crear un modelo computado para la props `value`
          inputValue: {
            get: function get() {
              return this.myInputValue;
            },
            set: function set(newValue) {
              // Emitir un evento llamado 'input' con el nuevo valor
              this.$emit('input', newValue);
            }
          }
        },
        methods: {
          handleEnterKey: function handleEnterKey(siglas) {
            this.fnOnEnter(siglas);
          },
          handle_item: function handle_item() {
            _UserCuestOximetria_js__WEBPACK_IMPORTED_MODULE_7__["default"].index('.modal-body', this.patient_data.user_id, this.patient_data.episodio_id);
          },
          getColor: function getColor() {
            return _UserCuestOximetria_js__WEBPACK_IMPORTED_MODULE_7__["default"].colorOxi(this.patient_data[this.siglas]);
          }
        }
      };
    }
  }, {
    key: "AppFormSintomas",
    value: function AppFormSintomas() {
      return {
        props: {
          patient_data: Object,

          /* fnOnEnter:Function, */
          index: Number
          /* myInputValue:Number, */

        },
        template:
        /*HTML */
        "\n                <div class=\"col-12 p-0 col-xl rounded d-flex  flex-column border\">\n                    <div class=\"d-flex justify-content-between\">\n                        <div\n                            @click=\"handle_item()\"\n                            class=\"w-custom-1 text-uppercase btn btn-outline-gray border-0\"\n                        >\n                            S\xEDntomas\n                        </div>\n                        <div class=\"dropdown\">\n                            <button class=\"btn btn-default dropdown-toggle\" type=\"button\" data-toggle=\"dropdown\" aria-expanded=\"false\">\n                                <i class=\"fas fa-bars\"></i>\n                            </button>\n                            <div class=\"dropdown-menu py-0\">\n                                <input placeholder=\"Escribe un sintoma\" :ref=\"'searchInput'+patient_data.user_id\" type=\"text\" @keypress=\"filterValues()\" class=\"form-control rounded-0 border-0 bg-light\">\n                                <div :ref=\"'myList'+patient_data.user_id\" class=\"overflow-auto\" style=\"max-height: 200px;\">\n                                    <a\n                                        v-for=\"(item,index) in getSintomas\"\n                                        :key=\"'sintoma_'+index\"\n                                        class=\"dropdown-item\"\n                                        href=\"#\"\n                                        @click=\"addItem(item.id)\"\n                                    >\n                                        {{item.description}}\n                                    </a>\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n\n\n                    <div class=\"d-flex flex-fill bg-white flex-wrap align-items-center p-1 rounded my-1\" style=\"overflow: auto;max-height: 108px;\">\n                        <span v-for=\"(item,index) in registred\" :key=\"index\" style=\"font-size: 1rem;\" class=\"badge badge-gray text-dark mb-1 mr-1 d-flex justify-content-between\">\n                            <span>{{item.description}}</span>\n                            <button\n                                class=\"border-0 bg-transparent text-white\"\n                                @click=\"deleteItem(item.id)\"\n                            >\n                                x\n                            </button>\n                        </span>\n                    </div>\n\n                </div>\n            ",
        data: function data() {
          return {
            registred: [],
            sintomas: [{
              'id': 12,
              'description': 'Adenopatias'
            }, {
              'id': 14,
              'description': 'Anosmia'
            }, {
              'id': 13,
              'description': 'Artralgias'
            }, {
              'id': 17,
              'description': 'Cefalea'
            }, {
              'id': 195,
              'description': 'Diaforesis'
            }, {
              'id': 18,
              'description': 'Diarrea'
            }, {
              'id': 19,
              'description': 'Disfagia'
            }, {
              'id': 4,
              'description': 'Disfonia'
            }, {
              'id': 5,
              'description': 'Dolor cervical'
            }, {
              'id': 194,
              'description': 'Dolor lumbar'
            }, {
              'id': 100,
              'description': 'Eritema'
            }, {
              'id': 16,
              'description': 'Epistaxis'
            }, {
              'id': 1,
              'description': 'Disgeusia'
            }, {
              'id': 112,
              'description': 'Palidez'
            }, {
              'id': 27,
              'description': 'Vómitos'
            }, {
              'id': 20,
              'description': 'Disnea'
            }, {
              'id': 21,
              'description': 'Dolor abdominal'
            }, {
              'id': 22,
              'description': 'Dolor torácico'
            }, {
              'id': 196,
              'description': 'Edema MI'
            }, {
              'id': 24,
              'description': 'Fatiga'
            }, {
              'id': 25,
              'description': 'Fiebre'
            }, {
              'id': 26,
              'description': 'Hemoptisis'
            }, {
              'id': 28,
              'description': 'Hipo'
            }, {
              'id': 29,
              'description': 'Mialgias'
            }, {
              'id': 30,
              'description': 'Náuseas'
            }, {
              'id': 31,
              'description': 'Odinofagia'
            }, {
              'id': 33,
              'description': 'Rinorrea'
            }, {
              'id': 34,
              'description': 'Tos Productiva'
            }, {
              'id': 35,
              'description': 'Tos seca'
            }, {
              'id': 36,
              'description': 'Otros'
            }],
            loading: document.getElementById('loadingScreen')
          };
        },
        computed: {
          getSintomas: function getSintomas() {
            var that = this;
            return this.sintomas.filter(function (item) {
              var is_registred = that.registred.find(function (x) {
                return x.id === item.id;
              });

              if (is_registred === undefined) {
                return item;
              }
            });
          }
        },
        methods: {
          filterValues: function filterValues() {
            var searchInput = this.$refs['searchInput' + this.patient_data.user_id];
            var searchText = searchInput.value.toLowerCase();
            var listItems = this.$refs["myList".concat(this.patient_data.user_id)].querySelectorAll("a");
            console.log(listItems);
            listItems.forEach(function (item) {
              var itemText = item.textContent.toLowerCase();
              item.style.display = itemText.includes(searchText) ? "block" : "none";
            });
          },
          addItem: function addItem(id) {
            var _this14 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee14() {
              var is_registred, item;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee14$(_context14) {
                while (1) {
                  switch (_context14.prev = _context14.next) {
                    case 0:
                      is_registred = _this14.registred.find(function (x) {
                        return x.id === id;
                      });

                      if (is_registred === undefined) {
                        item = _this14.sintomas.find(function (x) {
                          return x.id === id;
                        });

                        _this14.registred.push(item);
                      }

                      _context14.next = 4;
                      return _this14.store();

                    case 4:
                    case "end":
                      return _context14.stop();
                  }
                }
              }, _callee14);
            }))();
          },
          store: function store() {
            var _this15 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee15() {
              var model, value, formdata, created_at;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee15$(_context15) {
                while (1) {
                  switch (_context15.prev = _context15.next) {
                    case 0:
                      _this15.loading.classList.remove("d-none"); //[{"name":"model[]","value":"208"},{"name":"model[]","value":"223"},{"name":"model[]","value":"250"}]


                      model = _this15.registred.map(function (item) {
                        return {
                          "value": item.id
                        };
                      });
                      value = JSON.stringify(model);
                      formdata = new FormData();
                      created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])();
                      formdata.append("episodio_id", _this15.patient_data.episodio_id);
                      formdata.append("user_id", _this15.patient_data.user_id);
                      formdata.append("model", value);
                      formdata.append("since", created_at);
                      formdata.append("sintoma_nuevo", null);
                      formdata.append("created_at", created_at);
                      formdata.append("sintoma_observacion", "");
                      formdata.append("_token", $("#_token").val());
                      _context15.next = 15;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_sintoma/store", formdata);

                    case 15:
                      _this15.loading.classList.add("d-none");

                    case 16:
                    case "end":
                      return _context15.stop();
                  }
                }
              }, _callee15);
            }))();
          },
          handle_item: function handle_item() {
            UserCuestSintoma.index('.modal-body', this.patient_data.user_id, this.patient_data.episodio_id);
          },
          destroy: function destroy(id) {
            var _this16 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee16() {
              var formdata;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee16$(_context16) {
                while (1) {
                  switch (_context16.prev = _context16.next) {
                    case 0:
                      formdata = new FormData();
                      formdata.append("episodio_id", _this16.patient_data.episodio_id);
                      formdata.append("user_id", _this16.patient_data.user_id);
                      formdata.append("cat_user_cuestionario_id", id);
                      formdata.append("_token", $("#_token").val());
                      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                      _context16.next = 8;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_sintoma/destroy2", formdata);

                    case 8:
                      return _context16.abrupt("return", _context16.sent);

                    case 9:
                    case "end":
                      return _context16.stop();
                  }
                }
              }, _callee16);
            }))();
          },
          deleteItem: function deleteItem(id) {
            var _this17 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee17() {
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee17$(_context17) {
                while (1) {
                  switch (_context17.prev = _context17.next) {
                    case 0:
                      _this17.loading.classList.remove("d-none");

                      _this17.registred = _this17.registred.filter(function (x) {
                        return x.id !== id;
                      });
                      _context17.next = 4;
                      return _this17.destroy(id, _this17.patient_data.user_id);

                    case 4:
                      _this17.loading.classList.add("d-none");

                    case 5:
                    case "end":
                      return _context17.stop();
                  }
                }
              }, _callee17);
            }))();
          }
        },
        mounted: function mounted() {
          var _this18 = this;

          return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee18() {
            var sintomas;
            return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee18$(_context18) {
              while (1) {
                switch (_context18.prev = _context18.next) {
                  case 0:
                    //console.log(this.patient_data.sintoma);
                    if (_this18.patient_data.sintoma !== "") {
                      sintomas = _this18.patient_data.sintoma.split(",").map(function (x) {
                        return x.trim();
                      }); //console.log("sintomas registrados",sintomas);

                      _this18.registred = _this18.sintomas.filter(function (item) {
                        if (sintomas.includes(item.description)) {
                          return item;
                        }
                      }); //console.log(this.registred);
                    }

                  case 1:
                  case "end":
                    return _context18.stop();
                }
              }
            }, _callee18);
          }))();
        }
      };
    }
  }, {
    key: "AppFormGlicemia",
    value: function AppFormGlicemia() {
      return {
        data: function data() {
          return {
            unidadMedida: "mg/dL",
            siglas: "gl"
          };
        },
        template:
        /*html */
        "\n                <div class=\"col-12 p-0 col-xl-2 rounded d-flex flex-xl-column border\">\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"w-custom-1 text-uppercase btn btn-outline-gray border-0\"\n                    >\n                        {{siglas}}\n                    </div>\n                    <div class=\"w-100 flex-lg-fill d-flex flex-column\">\n                        <input\n                            :id=\"siglas+'_'+patient_data['user_id']\"\n                            @keyup.enter=\"handleEnterKey('insulina')\"\n                            v-model=\"input1\"\n                            @input=\"onInputChange('gl', input1)\"\n                            type=\"text\"\n                            placeholder=\"mg/dL\"\n                            class=\"shadow-inset-center w-100 text-center input-full-height border-0\"\n                        >\n                        <hr class=\"border-top m-0\">\n                        <input\n                            :id=\"'insulina_'+patient_data['user_id']\"\n                            @keyup.enter=\"handleEnterKey('created_at')\"\n                            v-model=\"input2\"\n                            @input=\"onInputChange('unidades_insulina', input2)\"\n                            type=\"text\"\n                            placeholder=\"Insulina\"\n                            class=\"shadow-inset-center w-100 text-center input-full-height border-0\"\n                        >\n                    </div>\n                    <div\n                        @click=\"handle_item()\"\n                        class=\"p-0 btn btn-outline-gray border-0 font-weight-bold height-100 last-result-box w-last-result-box d-flex align-items-center\"\n                    >\n                        <div\n                            v-if=\"!['nc','sc',null,'','NaN','NaN/NaN'].includes(patient_data[siglas])\"\n                            :class=\"[getColor(),'text-nowrap flex-fill text-center border-right']\"> {{ ![NaN,'NaN',null,'null',undefined].includes( patient_data[siglas] ) ? patient_data[siglas] +' '+unidadMedida : '' }}\n                        </div>\n                        <div\n                            v-if=\"![null,'','NaN','NaN/NaN'].includes(patient_data['unidades_insulina'])\"\n                            :class=\"['text-nowrap text-secondary flex-fill text-center']\">{{ ![NaN,'NaN',null,'null',undefined].includes(patient_data['unidades_insulina']) ? patient_data['unidades_insulina']+' UI' : '' }}\n                        </div>\n                    </div>\n\n                </div>\n            ",
        props: {
          patient_data: Object,
          fnOnEnter: Function,
          index: Number,
          input1: {
            type: Number,
            required: true
          },
          input2: {
            type: Number,
            required: true
          }
        },
        methods: {
          handleEnterKey: function handleEnterKey(siglas) {
            this.fnOnEnter(siglas);
          },
          onInputChange: function onInputChange(inputName, newValue) {
            // Emite un evento con el nombre del input y su nuevo valor
            this.$emit('input-change', {
              inputName: inputName,
              newValue: newValue
            });
          },
          handle_item: function handle_item() {
            _UserCuestGl_js__WEBPACK_IMPORTED_MODULE_8__["default"].index('.modal-body', this.patient_data.user_id, this.patient_data.episodio_id);
          },
          getColor: function getColor() {
            //UserCuestTa.colorTa(cadena[0],cadena[1])
            return _UserCuestGl_js__WEBPACK_IMPORTED_MODULE_8__["default"].colorGl(this.patient_data[this.siglas]);
          }
        }
      };
    }
  }, {
    key: "AppFormVitalSigns",
    value: function AppFormVitalSigns() {
      return {
        template:
        /*html */
        "\n                <div class=\"d-flex flex-wrap flex-xl-nowrap\" style=\"height: 100%;gap: 4px;\">\n                    <app-form-temperatura\n                        v-model=\"form.temp\"\n                        :fnOnEnter=\"changeNextInput\"\n                        :patient_data=\"patient_data\"\n                        :index=\"index\"\n                    ></app-form-temperatura>\n                    <app-form-frecuencia-respiratoria\n                        v-model=\"form.fr\"\n                        :fnOnEnter=\"changeNextInput\"\n                        :patient_data=\"patient_data\"\n                        :index=\"index\"\n                    ></app-form-frecuencia-respiratoria>\n                    <app-form-frecuencia-cardiaca\n                        v-model=\"form.fc\"\n                        :fnOnEnter=\"changeNextInput\"\n                        :patient_data=\"patient_data\"\n                        :index=\"index\"\n                    ></app-form-frecuencia-cardiaca>\n                    <app-form-tension-arterial\n                        :fnOnEnter=\"changeNextInput\"\n                        :input1=\"form.tasis\"\n                        :input2=\"form.tadia\"\n                        :input3=\"form.tamedia\"\n                        @input-change=\"handleInputChange\"\n                        :patient_data=\"patient_data\"\n                        :index=\"index\"\n                    ></app-form-tension-arterial>\n                    <app-form-oximetria\n                        :fnOnEnter=\"changeNextInput\"\n                        v-model=\"form.oxi\"\n                        :patient_data=\"patient_data\"\n                        :index=\"index\"\n                    ></app-form-oximetria>\n                    <app-form-glicemia\n                        :fnOnEnter=\"changeNextInput\"\n                        :input1=\"form.gl\"\n                        :input2=\"form.unidades_insulina\"\n                        :patient_data=\"patient_data\"\n                        :index=\"index\"\n                        @input-change=\"handleInputChange\"\n                    ></app-form-glicemia>\n                    <app-form-sintomas\n\n                        :patient_data=\"patient_data\"\n                        :index=\"index\"\n\n                    ></app-form-sintomas>\n                    <div class=\"col-12 col-sm col-xl p-0 rounded d-flex  justify-content-between flex-column\">\n\n                            <input\n                                :id=\"'created_at_'+patient_data['user_id']\"\n                                @keyup.enter=\"changeNextInput('time')\"\n                                type=\"date\"\n                                v-model=\"created_at\"\n                                class=\"form-control text-center input-full-height \"\n                            >\n                            <input\n                                :id=\"'time_'+patient_data['user_id']\"\n                                @keyup.enter=\"changeNextInput('submit')\"\n                                type=\"time\"\n                                v-model=\"time\"\n                                class=\"form-control mt-1 text-center input-full-height \"\n                            >\n\n                        <button\n                            :id=\"'submit_'+patient_data['user_id']\"\n                            @click=\"submitForm()\"\n                            class=\"btn btn-outline-success mt-1 w-100 btn-sm\"\n                        >\n                            Guardar todo\n                        </button>\n                        <a\n                            :href=\"'/user/informe/signos_vitales/'+patient_data.episodio_id\"\n                            target=\"_blank\"\n                            class=\"btn btn-outline-primary mt-1 w-100 btn-sm\"\n                        >\n                            Imprimir Signos Vitales\n                        </a>\n                    </div>\n                </div>\n            ",
        data: function data() {
          return {
            form: {
              temp: "",
              fr: "",
              fc: "",
              tasis: "",
              tadia: "",
              tamedia: "",
              oxi: "",
              gl: "",
              unidades_insulina: ""
            },
            time: "",
            realTime: "",
            created_at: this.fechaInputDate()
          };
        },
        components: {
          'app-form-sintomas': this.AppFormSintomas(),
          'app-form-temperatura': this.AppFormTemperatura(),
          'app-form-frecuencia-respiratoria': this.AppFormFrecuenciaRespiratoria(),
          'app-form-frecuencia-cardiaca': this.AppFormFrecuenciaCardiaca(),
          'app-form-tension-arterial': this.AppFormTensionArterial(),
          'app-form-oximetria': this.AppFormOximetria(),
          'app-form-glicemia': this.AppFormGlicemia()
        },
        props: {
          patient_data: Object,
          index: Number
        },
        computed: {
          calculateTamedia: function calculateTamedia() {
            var temp_tasis = this.form.tasis === "" ? 0 : Number(this.form.tasis);
            var temp_tadia = this.form.tadia === "" ? 0 : Number(this.form.tadia);
            console.log('temp_tasis:', temp_tasis, 'temp_tadia:', temp_tadia); //PAS + (2 × PAD)/3

            this.form.tamedia = (temp_tasis + 2 * temp_tadia) / 3;
            this.form.tamedia = this.form.tamedia.toFixed(2);
            return this.form.tamedia;
          }
        },
        methods: {
          changeNextInput: function changeNextInput(siglas) {
            document.querySelector('#' + siglas + '_' + this.patient_data.user_id).focus();
          },
          handleInputChange: function handleInputChange(_ref21) {
            var inputName = _ref21.inputName,
                newValue = _ref21.newValue;
            this.form[inputName] = Number(newValue);

            if (['tasis', 'tadia'].includes(inputName)) {
              this.calculateTamedia;
            }
          },
          storeSign: function storeSign(_ref22) {
            var _this19 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee19() {
              var inputName, newValue, _ref22$route, route, formdata, result;

              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee19$(_context19) {
                while (1) {
                  switch (_context19.prev = _context19.next) {
                    case 0:
                      inputName = _ref22.inputName, newValue = _ref22.newValue, _ref22$route = _ref22.route, route = _ref22$route === void 0 ? "" : _ref22$route;
                      formdata = new FormData();
                      formdata.append(inputName, newValue);
                      formdata.append("episodio_id", _this19.patient_data.episodio_id);
                      formdata.append("user_id", _this19.patient_data.user_id);
                      formdata.append("sintoma_observacion", "");
                      formdata.append("created_at", "".concat(_this19.created_at, " ").concat(_this19.realTime));
                      formdata.append("_token", $("#_token").val());
                      _context19.next = 10;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + route, formdata);

                    case 10:
                      result = _context19.sent;

                    case 11:
                    case "end":
                      return _context19.stop();
                  }
                }
              }, _callee19);
            }))();
          },
          storeSign2: function storeSign2(_ref23) {
            var _this20 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee20() {
              var _ref23$route, route, formdata, result;

              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee20$(_context20) {
                while (1) {
                  switch (_context20.prev = _context20.next) {
                    case 0:
                      _ref23$route = _ref23.route, route = _ref23$route === void 0 ? "" : _ref23$route;
                      formdata = new FormData();
                      formdata.append("value", _this20.form.tasis);
                      formdata.append("value2", _this20.form.tadia);
                      formdata.append("media", _this20.form.tamedia);
                      formdata.append("episodio_id", _this20.patient_data.episodio_id);
                      formdata.append("user_id", _this20.patient_data.user_id);
                      formdata.append("sintoma_observacion", "");
                      formdata.append("created_at", "".concat(_this20.created_at, " ").concat(_this20.realTime));
                      formdata.append("_token", $("#_token").val());
                      _context20.next = 12;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + route, formdata);

                    case 12:
                      result = _context20.sent;

                    case 13:
                    case "end":
                      return _context20.stop();
                  }
                }
              }, _callee20);
            }))();
          },
          storeSign3: function storeSign3(_ref24) {
            var _this21 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee21() {
              var _ref24$route, route, formdata, result;

              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee21$(_context21) {
                while (1) {
                  switch (_context21.prev = _context21.next) {
                    case 0:
                      _ref24$route = _ref24.route, route = _ref24$route === void 0 ? "" : _ref24$route;
                      formdata = new FormData();
                      formdata.append("value", _this21.form.gl);
                      formdata.append("unidades_insulina", _this21.form.unidades_insulina);
                      formdata.append("episodio_id", _this21.patient_data.episodio_id);
                      formdata.append("user_id", _this21.patient_data.user_id);
                      formdata.append("sintoma_observacion", "");
                      formdata.append("created_at", "".concat(_this21.created_at, " ").concat(_this21.realTime));
                      formdata.append("_token", $("#_token").val());
                      _context21.next = 11;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + route, formdata);

                    case 11:
                      result = _context21.sent;

                    case 12:
                    case "end":
                      return _context21.stop();
                  }
                }
              }, _callee21);
            }))();
          },
          submitForm: function submitForm() {
            var _this22 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee22() {
              var currentTime;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee22$(_context22) {
                while (1) {
                  switch (_context22.prev = _context22.next) {
                    case 0:
                      currentTime = new Date();
                      _this22.realTime = _this22.time === "" ? "".concat(currentTime.getHours().toString().padStart(2, '0'), ":").concat(currentTime.getMinutes().toString().padStart(2, '0'), ":").concat(currentTime.getSeconds().toString().padStart(2, '0')) : "".concat(_this22.time, ":00");
                      Swal.fire({
                        title: "Signos a guardar",
                        html: "\n                                Por favor, verifique sus datos antes de guardar:<br>\n                                Fecha registro: ".concat(_this22.created_at, " ").concat(_this22.realTime, "<br>\n                                Temp: ").concat(_this22.form.temp, "<br>\n                                FR: ").concat(_this22.form.fr, "<br>\n                                FC: ").concat(_this22.form.fc, "<br>\n                                TASIS: ").concat(_this22.form.tasis, "<br>\n                                TADIA: ").concat(_this22.form.tadia, "<br>\n                                TAMEDIA: ").concat(_this22.form.tamedia, "<br>\n                                OXI: ").concat(_this22.form.oxi, "<br>\n                                GLIC: ").concat(_this22.form.gl, "<br>\n                                INSULINA: ").concat(_this22.form.unidades_insulina, "<br>\n                            "),
                        //allowOutsideClick: false,
                        icon: "info",
                        showCancelButton: true,
                        confirmButtonColor: "#3085d6",
                        cancelButtonColor: "#d33",
                        confirmButtonText: "De acuerdo!",
                        cancelButtonText: "No guardar"
                      }).then(function (result) {
                        if (result.isConfirmed) {
                          var siglas = "temp";

                          if (_this22.form[siglas] !== "") {
                            _this22.storeSign({
                              inputName: "cat_user_cuestionario_84",
                              newValue: _this22.form[siglas],
                              route: "/user_cuest_temperatura/store"
                            });

                            document.querySelector('#' + siglas + '_' + _this22.patient_data.user_id).value = "";
                            _this22.patient_data[siglas] = _this22.form[siglas]; // pacientes_datos[this.index] =  this.patient_data[siglas]

                            _this22.form[siglas] = "";
                          }

                          siglas = "fr";

                          if (_this22.form[siglas] !== "") {
                            _this22.storeSign({
                              inputName: "value",
                              newValue: _this22.form[siglas],
                              route: "/user_cuest_fr/store"
                            });

                            document.querySelector('#' + siglas + '_' + _this22.patient_data.user_id).value = "";
                            _this22.patient_data[siglas] = _this22.form[siglas]; // pacientes_datos[this.index] =  this.patient_data[siglas]

                            _this22.form[siglas] = "";
                          }

                          siglas = "fc";

                          if (_this22.form[siglas] !== "") {
                            _this22.storeSign({
                              inputName: "cat_user_cuestionario_185",
                              newValue: _this22.form[siglas],
                              route: "/user_cuest_fc/store"
                            });

                            document.querySelector('#' + siglas + '_' + _this22.patient_data.user_id).value = "";
                            _this22.patient_data[siglas] = _this22.form[siglas]; // pacientes_datos[this.index] =  this.patient_data[siglas]

                            _this22.form[siglas] = "";
                          }

                          siglas = "tasis";
                          var siglas2 = "tadia";

                          if (_this22.form[siglas] !== "" && _this22.form[siglas2] !== "") {
                            _this22.storeSign2({
                              route: "/user_cuest_ta/store"
                            });

                            document.querySelector('#' + siglas + '_' + _this22.patient_data.user_id).value = "";
                            document.querySelector('#' + siglas2 + '_' + _this22.patient_data.user_id).value = "";
                            _this22.patient_data['ta'] = _this22.form[siglas] + "/" + _this22.form[siglas2];
                            _this22.patient_data['ta_media'] = _this22.form['tamedia']; // pacientes_datos[this.index] =  this.patient_data[siglas]

                            _this22.form[siglas] = "";
                            _this22.form[siglas2] = "";
                          }

                          siglas = "oxi";

                          if (_this22.form[siglas] !== "") {
                            _this22.storeSign({
                              inputName: "cat_user_cuestionario_73",
                              newValue: _this22.form[siglas],
                              route: "/user_cuest_oximetria/store"
                            });

                            document.querySelector('#' + siglas + '_' + _this22.patient_data.user_id).value = "";
                            _this22.patient_data[siglas] = _this22.form[siglas]; // pacientes_datos[this.index] =  this.patient_data[siglas]

                            _this22.form[siglas] = "";
                          }

                          siglas = "gl";

                          if (_this22.form[siglas] !== "") {
                            _this22.storeSign3({
                              inputName: "value",
                              newValue: _this22.form[siglas],
                              route: "/user_cuest_gl/store"
                            });

                            _this22.patient_data[siglas] = _this22.form[siglas];
                            _this22.patient_data['unidades_insulina'] = _this22.form['unidades_insulina']; // pacientes_datos[this.index] =  this.patient_data[siglas]

                            _this22.form[siglas] = "";
                            _this22.form['unidades_insulina'] = "";
                          }
                        }
                      });

                    case 3:
                    case "end":
                      return _context22.stop();
                  }
                }
              }, _callee22);
            }))();
          },
          fechaInputDate: function fechaInputDate() {
            var fecha = new Date();
            var response = "";
            var mes = fecha.getMonth() + 1; //obteniendo mes

            var dia = fecha.getDate(); //obteniendo dia

            var ano = fecha.getFullYear(); //obteniendo año

            if (dia < 10) {
              dia = '0' + dia;
            } //agrega cero si el menor de 10


            if (mes < 10) {
              mes = '0' + mes;
            } //agrega cero si el menor de 10


            return response = ano + "-" + mes + "-" + dia;
          }
        },
        mounted: function mounted() {
          return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee23() {
            return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee23$(_context23) {
              while (1) {
                switch (_context23.prev = _context23.next) {
                  case 0:
                  case "end":
                    return _context23.stop();
                }
              }
            }, _callee23);
          }))();
        }
      };
    }
  }, {
    key: "AppRowVitalSign",
    value: function () {
      var _AppRowVitalSign = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee24() {
        var that, d, App, app;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee24$(_context24) {
          while (1) {
            switch (_context24.prev = _context24.next) {
              case 0:
                that = this;
                d = document;
                App = d.querySelector("#AppRowVitalSign");
                App.innerHTML =
                /*html*/
                "\n                <div v-for=\"(item,index) in items\" :key=\"'row'+index\" :id=\"'row-paciente-'+item.user_id+'-'+item.cat_user_ubicacion_id\" \n                :class=\"[\n                    'row-paciente ',\n                    {'col-12 mt-3 col-sm-12 col-md-6 col-lg-6 col-xl-12':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                    {'col-12 mt-3 col-sm-12 col-md-6 col-lg-6 col-xl-4 col-xxl-3':['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                ]\"\n                \n                >\n\n                    <div :class=\"[\n                        'd-flex shadow-sm rounded bg-light ',\n                        {'flex-column flex-xl-row':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                        {'flex-column flex-xl-column':['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n\n                        ]\">\n                        <div \n                            v-if=\"['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\" \n                            :class=\"[\n                                'p-1',\n                                {'col col-md col-xl-1 d-flex flex-xl-wrap ':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                                {'order-2 col-12 col-sm-12':['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                            ]\"\n                            \n                        >\n                            <div class=\"w-100 d-flex flex-wrap\" :style=\"['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))?'gap: 0.5rem !important;':''\">\n                                <div class=\"col-12 col-sm-3 d-flex flex-column border border-gray mb-1 rounded\">\n                                    <b class=\"small-title text-primary text-uppercase text-center\">Ubicaci\xF3n</b>\n                                    <div class=\"flex-fill d-flex justify-content-center align-items-end align-items-xl-center text-gray\">\n                                        {{item.ubicacion_alta}}\n                                    </div>\n                                </div>\n                                <div class=\"col col-sm-3 d-flex flex-column border border-gray mb-1 rounded\">\n                                    <b class=\"small-title text-primary text-uppercase text-center\">Ingreso</b>\n                                    <div class=\"flex-fill d-flex justify-content-center align-items-end align-items-xl-center text-gray\">\n                                        {{item.ingreso_episodio}}\n                                    </div>\n                                    <div class=\"flex-fill d-flex justify-content-center  align-items-end text-gray\">\n                                        {{item.hora_ingreso}}\n                                    </div>\n                                </div>\n\n                                <div class=\"col d-flex flex-column border border-gray mb-1 rounded\">\n                                    <b class=\"small-title text-primary text-uppercase text-center\">Egreso</b>\n                                    <div class=\"flex-fill d-flex justify-content-center align-items-end align-items-xl-center text-gray\">\n                                        {{item.egreso_episodio}}\n                                    </div>\n                                    <div class=\"flex-fill d-flex justify-content-center  align-items-end text-gray\">\n                                        {{item.hora_egreso}}\n                                    </div>\n                                </div>\n                                <div class=\"col-2 d-flex flex-column border border-gray mb-1 rounded\">\n                                    <b class=\"small-title text-primary text-uppercase text-center\">D\xEDas</b>\n                                    <div class=\"flex-fill d-flex justify-content-center align-items-end align-items-xl-center text-gray\">\n                                        {{item.dias}}\n                                    </div>\n                                </div>\n                            </div>\n                   \n                        </div>\n                        <div v-else class=\"col col-md col-xl-1 d-flex flex-xl-wrap p-1\">\n                            <app-btn-ubicacion :patient_data=\"item\" :index=\"index\"></app-btn-ubicacion>\n                            <app-btn-dias :patient_data=\"item\" :index=\"index\"></app-btn-dias>\n                            <app-prealta :patient_data=\"item\" :index=\"index\"></app-prealta>\n                            <app-btn-traslado-ambulancia :patient_data=\"item\" :index=\"index\"></app-btn-traslado-ambulancia>\n                        </div>\n                        <div class=\"col px-1 d-flex flex-column \">\n                            <div class=\"rounded d-flex flex-column flex-xl-row my-1\" style=\"height: 100% !important;gap: 4px;\">\n                                <div :class=\"[\n                                        'rounded d-flex flex-column justify-content-between border',\n                                        {'width-custom':!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                                        {'w-100':['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))},\n                                    ]\" \n                                >\n                                    <app-data-paciente :patient_data=\"item\" :index=\"index\"></app-data-paciente>\n                                    <app-btn-group2 :patient_data=\"item\" :index=\"index\"></app-btn-group2>\n\n                                </div>\n                                <div\n                                    v-if=\"!['alta','traslado','contraopinion','fallecido','otro'].includes(localStorage.getItem('area'))\" \n                                    class=\"collapseVitalsign p-1 collapse col align-content-center\"\n                                    :id=\"'collapse'+index\"\n                                >\n                                     <app-form-vital-signs\n                                            :patient_data=\"item\"\n                                            :index=\"index\"\n                                        ></app-form-vital-signs>\n\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n                </div>\n            ";
                app = new Vue({
                  el: "#AppRowVitalSign",
                  components: {
                    "app-btn-ubicacion": that.AppBtnUbicacion(),
                    "app-btn-dias": that.AppBtnDias(),
                    "app-prealta": that.AppPrealta(),
                    "app-btn-traslado-ambulancia": that.AppBtnTrasladoAmbulancia(),
                    "app-data-paciente": that.AppDataPaciente(),
                    "app-btn-group2": that.AppBtnGroup2(),
                    "app-form-vital-signs": that.AppFormVitalSigns()
                  },
                  data: function data() {
                    return {
                      areaSiglas: "tp",
                      items: pacientes_datos
                    };
                  },
                  computed: {},
                  methods: {},
                  created: function created() {
                    var that = this; // Escuchar cambios en el bus de eventos

                    EventBus.$on('externalVarChanged', function (newValue) {
                      that.items = newValue;
                    });
                  },
                  mounted: function mounted() {
                    var that = this;
                    /*  setInterval(() => {
                         that.items = pacientes_datos
                     }, 100); */
                  }
                });

              case 5:
              case "end":
                return _context24.stop();
            }
          }
        }, _callee24, this);
      }));

      function AppRowVitalSign() {
        return _AppRowVitalSign.apply(this, arguments);
      }

      return AppRowVitalSign;
    }()
  }, {
    key: "AppNavMenuArea",
    value: function () {
      var _AppNavMenuArea = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee26() {
        var App, that, app;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee26$(_context26) {
          while (1) {
            switch (_context26.prev = _context26.next) {
              case 0:
                App = d.querySelector("#App_nav_menu_areas");
                App.innerHTML =
                /*html*/
                "\n                <li\n                    v-for=\"(item,index) in getItems.filter(x=>['tp','ea','ep',].includes(x.siglas))\"\n                    :key=\"'area_option_'+index\"\n                    class=\"nav-item\"\n                    :title=\"item.description\"\n                    role=\"presentation\"\n                >\n                    <a\n                        @click=\"handleAreaOption(item.siglas)\"\n                        :class=\"['font-weight-bold text-uppercase nav-link',{'active':item.siglas===areaSiglas}]\"\n\n                        :href=\"'#pills-'+item.siglas\"\n                        role=\"tab\"\n                        :aria-controls=\"'pills-'+item.siglas\"\n                        aria-selected=\"false\">\n                        {{item.siglas}}\n                    </a>\n                </li>\n                <li\n                    class=\"nav-item\"\n                    title=\"AQ\"\n                    role=\"presentation\"\n                >\n                    <a\n                        @click=\"handleMenuAQ\"\n                        :class=\"['font-weight-bold text-uppercase nav-link',{'active':['aq_torre_hosp','aq_mapm'].includes(areaSiglas)}]\"\n\n                        href=\"#pills-aq\"\n                        role=\"tab\"\n                        aria-controls=\"pills-aq\"\n                        aria-selected=\"false\"\n                    >\n                       AQ\n                    </a>\n                </li>\n                <li\n                    v-for=\"(item,index) in getItems.filter(x=>['hcep','hp2','hp3','hp4','hp5','hp6','utia','utip','utin',].includes(x.siglas))\"\n                    :key=\"'area_option_'+index\"\n                    class=\"nav-item\"\n                    :title=\"item.description\"\n                    role=\"presentation\"\n                >\n                    <a\n                        @click=\"handleAreaOption(item.siglas)\"\n                        :class=\"['font-weight-bold text-uppercase nav-link',{'active':item.siglas===areaSiglas}]\"\n\n                        :href=\"'#pills-'+item.siglas\"\n                        role=\"tab\"\n                        :aria-controls=\"'pills-'+item.siglas\"\n                        aria-selected=\"false\">\n                        {{item.siglas}}\n                    </a>\n                </li>\n                <li\n                    class=\"nav-item\"\n                    title=\"Pad\"\n                    role=\"presentation\"\n                >\n                    <a\n                        @click=\"handleMenuPad\"\n                        :class=\"['font-weight-bold text-uppercase nav-link',{'active':['pad','pad_emergencia_adulto','pad_emergencia_pediatrica','pad_postquirurgico_amb','pad_postquirurgico_hosp','pad_medico'].includes(areaSiglas)}]\"\n\n                        href=\"#pills-pad\"\n                        role=\"tab\"\n                        aria-controls=\"pills-pad\"\n                        aria-selected=\"false\"\n                    >\n                       PAD\n                    </a>\n                </li>\n                <li\n                    class=\"nav-item\"\n                    title=\"Altas\"\n                    role=\"presentation\"\n                >\n                    <a\n                        @click=\"handleMenuEgresos\"\n                        :class=\"['font-weight-bold text-uppercase nav-link',{'active':['alta','traslado','contraopinion','fallecido','otro'].includes(areaSiglas)}]\"\n\n                        href=\"#pills-TP\"\n                        role=\"tab\"\n                        aria-controls=\"pills-TP\"\n                        aria-selected=\"false\"\n                    >\n                       ALTA\n                    </a>\n                </li>\n                <li\n                    style=\"background-color: #f0efef;border-radius: 5px;\"\n                    class=\"d-none nav-item\"\n                    title=\"Padel\"\n                    role=\"presentation\"\n                >\n                    <a\n                        @click=\"handleAreaOption('pfest')\"\n                        :class=\"['font-weight-bold text-uppercase nav-link',{'active':['pfest'].includes(areaSiglas)}]\"\n                        :href=\"'#pills-pfest'\"\n                        role=\"tab\"\n                        :aria-controls=\"'pills-pfest'\"\n                        aria-selected=\"false\">\n                       PADEL FEST 3\n                    </a>\n                </li>\n            ";
                that = this;
                app = new Vue({
                  el: "#App_nav_menu_areas",
                  data: function data() {
                    return {
                      areaSiglas: "tp",
                      areaName: "Todos los Pacientes",
                      selectorAreaName: document.getElementById('areaName'),
                      loading: document.getElementById('loadingScreen'),
                      items: [{
                        name: "Todos los Pacientes",
                        siglas: "tp"
                      }, {
                        name: "Emergencia Adulto",
                        siglas: "ea"
                      }, {
                        name: "Emergencia Pediátrica",
                        siglas: "ep"
                      }, {
                        name: "Área Quirúrgica",
                        siglas: "aq"
                      }, {
                        name: "AQ Torre Hospitalización",
                        siglas: "aq_torre_hosp"
                      }, {
                        name: "AQ MAPM",
                        siglas: "aq_mapm"
                      }, {
                        name: "Hospitalización Corta Estancia Pediátrica",
                        siglas: "hcep"
                      }, {
                        name: "Hospitalización Piso 2",
                        siglas: "hp2"
                      }, {
                        name: "Hospitalización Piso 3",
                        siglas: "hp3"
                      }, {
                        name: "Hospitalización Piso 4",
                        siglas: "hp4"
                      }, {
                        name: "Hospitalización Piso 5",
                        siglas: "hp5"
                      }, {
                        name: "Hospitalización Piso 6",
                        siglas: "hp6"
                      }, {
                        name: "Unidad de Terapia Intensiva Adulto",
                        siglas: "utia"
                      }, {
                        name: "Unidad de Terapia Intensiva Pediátrica",
                        siglas: "utip"
                      }, {
                        name: "Unidad de Terapia Intensiva Neonatal",
                        siglas: "utin"
                      }, {
                        name: "Prorama de Atención Domiciliaria",
                        siglas: "pad"
                      }, {
                        name: "PAD Emergencia Adulto",
                        siglas: "pad_emergencia_adulto"
                      }, {
                        name: "PAD Emergencia Pediátrica",
                        siglas: "pad_emergencia_pediatrica"
                      }, {
                        name: "PAD Postquirúrgico Ambulatorio",
                        siglas: "pad_postquirurgico_amb"
                      }, {
                        name: "PAD Postquirúrgico Hospitalización",
                        siglas: "pad_postquirurgico_hosp"
                      }, {
                        name: "PAD Médico",
                        siglas: "pad_medico"
                      }, {
                        name: "Altas médicas",
                        siglas: "alta"
                      }, {
                        name: "Traslados a otra institución",
                        siglas: "traslado"
                      }, {
                        name: "Contraopinión médica",
                        siglas: "contraopinion"
                      }, {
                        name: "Contraopinión médica",
                        siglas: "fallecido"
                      }, {
                        name: "Otro concepto de egreso",
                        siglas: "otro"
                      }, {
                        name: "Padel Fest 3",
                        siglas: "pfest"
                      }]
                    };
                  },
                  components: {},
                  computed: {
                    getItems: function getItems() {
                      return this.items.filter(function (item) {
                        return !['alta', 'traslado', 'contraopinion', 'fallecido', 'otro', 'pad', 'pad_emergencia_adulto', 'pad_emergencia_pediatrica', 'pad_postquirurgico_amb', 'pad_postquirurgico_hosp', 'pad_medico'].includes(item.siglas);
                      });
                    }
                  },
                  methods: {
                    actualizarMensaje: function actualizarMensaje(nuevoMensaje) {
                      //alert(nuevoMensaje)
                      this.areaSiglas = nuevoMensaje;
                      this.selectorAreaName.textContent = this.items.find(function (x) {
                        return x.siglas === nuevoMensaje;
                      }).name;
                    },
                    getRouteParams: function getRouteParams() {
                      // Crear un objeto URLSearchParams a partir de la parte de consulta de la URL
                      var params = new URLSearchParams(window.location.search); // Crear un objeto vacío para almacenar los parámetros

                      var queryParams = {}; // Iterar sobre los parámetros y agregarlos al objeto

                      params.forEach(function (value, key) {
                        queryParams[key] = value;
                      }); // Imprimir los parámetros obtenidos

                      console.log('Parámetros de consulta:', queryParams); // Puedes acceder a un parámetro específico usando su clave
                      // Por ejemplo, si tienes un parámetro 'nombre' en la URL, puedes obtener su valor así:

                      this.areaSiglas = queryParams['area'];
                      console.log('Valor de "area":', this.areaSiglas);
                    },
                    getAreaData: function getAreaData() {
                      var _this23 = this;

                      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee25() {
                        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee25$(_context25) {
                          while (1) {
                            switch (_context25.prev = _context25.next) {
                              case 0:
                                _this23.areaSiglas = [null, undefined, 'undefined'].includes(localStorage.getItem('area')) ? 'tp' : localStorage.getItem('area');
                                _this23.selectorAreaName.textContent = _this23.items.find(function (x) {
                                  return x.siglas === _this23.areaSiglas;
                                }).name;

                                _this23.loading.classList.remove("d-none");

                                _context25.next = 5;
                                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/vistas/" + _this23.areaSiglas);

                              case 5:
                                pacientes_datos = _context25.sent;
                                EventBus.$emit('externalVarChanged', pacientes_datos);
                                that.AppBtnTotales(pacientes_datos);

                                _this23.loading.classList.add("d-none");

                              case 9:
                              case "end":
                                return _context25.stop();
                            }
                          }
                        }, _callee25);
                      }))();
                    },
                    handleMenuPad: function handleMenuPad() {
                      $("#messageModal").modal("show");
                      this.areaSiglas = [null, undefined, 'undefined'].includes(localStorage.getItem('area')) ? 'tp' : localStorage.getItem('area');
                      this.selectorAreaName.textContent = "PAD";
                      localStorage.setItem("area", this.areaSiglas);
                      var App = d.querySelector("#messageModal .modal-body");
                      App.innerHTML = null;
                      App.innerHTML = "\n                                <div id=\"AppMenuPad\">\n                                </div>\n                            ";
                      var app = new Vue(that.AppMenuPad());
                      app._props.areaSiglas = this.areaSiglas;
                      app._props.FnAreaSiglas = this.actualizarMensaje;
                      console.log(app);
                    },
                    handleMenuAQ: function handleMenuAQ() {
                      $("#messageModal").modal("show");
                      this.areaSiglas = [null, undefined, 'undefined'].includes(localStorage.getItem('area')) ? 'tp' : localStorage.getItem('area');
                      this.selectorAreaName.textContent = "AQ";
                      localStorage.setItem("area", this.areaSiglas);
                      var App = d.querySelector("#messageModal .modal-body");
                      App.innerHTML = null;
                      App.innerHTML = "\n                                <div id=\"AppMenuAQ\">\n                                </div>\n                            ";
                      var app = new Vue(that.AppMenuAQ());
                      app._props.areaSiglas = this.areaSiglas;
                      app._props.FnAreaSiglas = this.actualizarMensaje;
                      console.log(app);
                    },
                    handleMenuEgresos: function handleMenuEgresos() {
                      $("#messageModal").modal("show");
                      this.areaSiglas = [null, undefined, 'undefined'].includes(localStorage.getItem('area')) ? 'tp' : localStorage.getItem('area');
                      localStorage.setItem("area", this.areaSiglas);
                      var App = d.querySelector("#messageModal .modal-body");
                      App.innerHTML = null;
                      App.innerHTML = "\n                                <div id=\"AppMenuEgresos\">\n                                </div>\n                            ";
                      var app = new Vue(that.AppMenuEgresos());
                      app._props.areaSiglas = this.areaSiglas;
                      app._props.FnAreaSiglas = this.actualizarMensaje; //console.log(app);
                    },
                    handleAreaOption: function handleAreaOption(areaSiglas) {
                      //console.log(areaSiglas)
                      this.areaSiglas = areaSiglas;
                      localStorage.setItem("area", this.areaSiglas);
                      this.getAreaData();
                    }
                  },
                  mounted: function mounted() {
                    var _this24 = this;

                    this.areaSiglas = [null, undefined, 'undefined'].includes(localStorage.getItem('area')) ? 'tp' : localStorage.getItem('area'); //console.log(this.areaSiglas);

                    this.selectorAreaName.textContent = this.items.find(function (x) {
                      return x.siglas === _this24.areaSiglas;
                    }).name;
                    /* if(this.areaSiglas === "pad"){
                        this.handleMenuPad()
                        return false
                    } */

                    this.getAreaData();
                  }
                });
                return _context26.abrupt("return", app);

              case 5:
              case "end":
                return _context26.stop();
            }
          }
        }, _callee26, this);
      }));

      function AppNavMenuArea() {
        return _AppNavMenuArea.apply(this, arguments);
      }

      return AppNavMenuArea;
    }()
  }, {
    key: "AppBtnTotales",
    value: function AppBtnTotales(pacientes_datos) {
      //console.log("------------>",pacientes_datos)
      var App = d.querySelector("#btnNotificaciones");
      App.innerHTML =
      /*HTML */
      "\n\n                <button v-if=\"getArea() == 'tp'\" type=\"button\" class=\"text-uppercase d-flex flex-column flex-md-row flex-nowrap align-items-center text-primary font-weight-bold btn btn-gray mr-1\">\n                    <span class=\"mr-0 mr-md-1 mb-1 mb-md-0 d-flex align-items-center\">\n                        <i class=\"pc-paciente text-info mr-1\"></i> <span class=\"d-md-block text-nowrap\">TP</span>\n                    </span>\n                    <span class=\"w-100 badge badge-light text-primary\" style=\"font-size: 1rem;font-weight: 600;\">{{getTotalPacientes()}}</span>\n                </button>\n                <button\n                    v-if=\"!['tp'].includes( getArea() )\"\n                    type=\"button\"\n                    :class=\"[{'flex-column':['tp'].includes( getArea() )},'text-uppercase d-flex flex-md-row flex-nowrap align-items-center text-primary font-weight-bold btn btn-gray mr-1']\"\n                >\n                    <span class=\"mr-0 mr-md-1 mb-1 mb-md-0 d-flex align-items-center\">\n                        <i class=\"pc-paciente text-info mr-1\"></i> <span class=\"d-md-block text-nowrap\">Total Pacientes</span>\n                    </span>\n                    <span class=\"ml-1 w-100 badge badge-light text-primary\" style=\"font-size: 1rem;font-weight: 600;\">{{getTotalPacientes()}}{{getTotalPacienteArea()}}</span>\n                </button>\n                <button v-if=\"getArea() == 'tp'\" type=\"button\" class=\"text-uppercase d-flex flex-column flex-md-row flex-nowrap align-items-center text-primary font-weight-bold btn btn-gray mr-1\">\n                    <span class=\"mr-0 mr-md-1 mb-1 mb-md-0 d-flex align-items-center\">\n                        <i class=\"pc-emergencia text-info mr-1\"></i> <span class=\"d-md-block text-nowrap\">EMER</span>\n                    </span>\n                    <span class=\"w-100 badge badge-light text-primary\" style=\"font-size: 1rem;font-weight: 600;\">{{getTotalEmergencia()}}</span>\n                </button>\n                <!--<button type=\"button\" class=\"text-uppercase d-flex flex-nowrap align-items-center text-primary font-weight-bold btn btn-gray mr-1\">\n                    <span class=\"mr-1\"><i class=\"pc-cirugia-light text-info mr-1\"></i> <span class=\"d-none d-md-block text-nowrap\">QX</span></span>\n                    <span class=\"badge badge-light text-primary\" style=\"font-size: 1rem;font-weight: 600;\">0</span>\n                </button>-->\n                <button v-if=\"getArea() == 'tp'\" type=\"button\" class=\"text-uppercase d-flex flex-column flex-md-row flex-nowrap align-items-center text-primary font-weight-bold btn btn-gray mr-1\">\n                    <span class=\"mr-0 mr-md-1 mb-1 mb-md-0 d-flex align-items-center\">\n                        <i class=\"pc-cirugia-light  text-info mr-1\"></i> <span class=\"d-md-block text-nowrap\">AQ</span>\n                    </span>\n                    <span class=\"w-100 badge badge-light text-primary\" style=\"font-size: 1rem;font-weight: 600;\">{{getTotalAQ()}}</span>\n                </button>\n                <button v-if=\"getArea() == 'tp'\" type=\"button\" class=\"text-uppercase d-flex flex-column flex-md-row flex-nowrap align-items-center text-primary font-weight-bold btn btn-gray mr-1\">\n                    <span class=\"mr-0 mr-md-1 mb-1 mb-md-0 d-flex align-items-center\">\n                        <i class=\"pc-light-pisos  text-info mr-1\"></i> <span class=\"d-md-block text-nowrap\">HOSP</span>\n                    </span>\n                    <span class=\"w-100 badge badge-light text-primary\" style=\"font-size: 1rem;font-weight: 600;\">{{getTotalHospitalizacion()}}</span>\n                </button>\n                <button v-if=\"getArea() == 'tp'\" type=\"button\" class=\"text-uppercase d-flex flex-column flex-md-row flex-nowrap align-items-center text-primary font-weight-bold btn btn-gray mr-1\">\n                    <span class=\"mr-0 mr-md-1 mb-1 mb-md-0 d-flex align-items-center\">\n                        <i class=\"pc-uti-light text-info mr-1\"></i> <span class=\"d-md-block text-nowrap\">UTI</span>\n                    </span>\n                    <span class=\"w-100 badge badge-light text-primary\" style=\"font-size: 1rem;font-weight: 600;\">{{getTotalUti()}}</span>\n                </button>\n                <button v-if=\"getArea() == 'tp'\" type=\"button\" class=\"text-uppercase d-flex flex-column flex-md-row flex-nowrap align-items-center text-primary font-weight-bold btn btn-gray mr-1\">\n                    <span class=\"mr-0 mr-md-1 mb-1 mb-md-0 d-flex align-items-center\">\n                        <i class=\"pc-light-homecare text-info mr-1\"></i> <span class=\"d-md-block text-nowrap\">PAD</span>\n                    </span>\n                    <span class=\"w-100 badge badge-light text-primary\" style=\"font-size: 1rem;font-weight: 600;\">{{getTotalPad()}}</span>\n                </button>\n            ";
      var app = new Vue({
        el: "#btnNotificaciones",
        props: {
          patient_data: Object,

          /* fnOnEnter:Function, */
          index: Number
          /* myInputValue:Number, */

        },
        data: function data() {
          return {
            aq_parent_id: [418, 119, 420, 421, 422, 423, 424, 425],
            hospitalizacion_parent_id: [42, 235, 236, 99, 71, 234, 127, 155],
            emergencia_parent_id: [2, 3, 270, 28, 29, 32, 392],
            uti_parent_id: [182, 211],
            pad_parent_id: [229, 228, 373, 374, 382],
            loading: document.getElementById('loadingScreen')
          };
        },
        computed: {},
        methods: {
          getArea: function getArea() {
            return localStorage.getItem("area");
          },
          getTotalPacientes: function getTotalPacientes() {
            var area = this.getArea();

            if (['tp'].includes(area)) {
              return this.getTotalHospitalizacion() + this.getTotalAQ() + this.getTotalEmergencia() + this.getTotalUti() + this.getTotalPad();
            }

            if (['ea'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [4, 5, 405].includes(Number(x.cat_user_ubicacion_id)) || [270].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['ep'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [30, 31, 406].includes(Number(x.cat_user_ubicacion_id)) || [32].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['hcep'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [390].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['aq'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [418, 119, 420, 421, 422, 423, 424, 425].includes(Number(x.cat_user_ubicacion_id));
              }).length;
            }

            if (['aq_torre_hosp'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [418, 422].includes(Number(x.cat_user_ubicacion_id));
              }).length;
            }

            if (['aq_mapm'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [424, 425].includes(Number(x.cat_user_ubicacion_id));
              }).length;
            }

            if (['hp2'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [42].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['hp3'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [71, 235].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['hp4'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [236, 99].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['hp5'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [127, 234].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['hp6'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [155].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['utia'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [182].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['utip'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [211].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['utin'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [391].includes(Number(x.parent_cat_user_ubicacion_id));
              }).length;
            }

            if (['pad_emergencia_adulto'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [229].includes(Number(x.cat_user_ubicacion_id));
              }).length;
            }

            if (['pad_emergencia_pediatrica'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [228].includes(Number(x.cat_user_ubicacion_id));
              }).length;
            }

            if (['pad_postquirurgico_amb'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [373].includes(Number(x.cat_user_ubicacion_id));
              }).length;
            }

            if (['pad_postquirurgico_hosp'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [374].includes(Number(x.cat_user_ubicacion_id));
              }).length;
            }

            if (['pad_medico'].includes(area)) {
              return pacientes_datos.filter(function (x) {
                return [382].includes(Number(x.cat_user_ubicacion_id));
              }).length;
            }
          },
          getTotalPacienteArea: function getTotalPacienteArea() {
            var a = this.getArea();
            var area = {
              'ea': 17,
              'ep': 8,
              'hp2': 26,
              'hp3': 26,
              'hp4': 32,
              'hp5': 35,
              'hp6': 35,
              'utia': 10,
              'utip': 8
            };
            var exclude = [];
            var result = "";

            if (area[a] !== undefined) {
              result = '/' + area[a];
            }

            return !exclude.includes(a) ? result : '0';
          },
          getTotalAQ: function getTotalAQ() {
            var _this25 = this;

            return pacientes_datos.filter(function (x) {
              return _this25.aq_parent_id.includes(Number(x.cat_user_ubicacion_id));
            }).length;
          },
          getTotalHospitalizacion: function getTotalHospitalizacion() {
            var _this26 = this;

            return pacientes_datos.filter(function (x) {
              return _this26.hospitalizacion_parent_id.includes(Number(x.parent_cat_user_ubicacion_id));
            }).length;
          },
          getTotalEmergencia: function getTotalEmergencia() {
            var _this27 = this;

            return pacientes_datos.filter(function (x) {
              return _this27.emergencia_parent_id.includes(Number(x.parent_cat_user_ubicacion_id));
            }).length;
          },
          getTotalPad: function getTotalPad() {
            var _this28 = this;

            return pacientes_datos.filter(function (x) {
              return _this28.pad_parent_id.includes(Number(x.cat_user_ubicacion_id));
            }).length;
          },
          getTotalUti: function getTotalUti() {
            var _this29 = this;

            return pacientes_datos.filter(function (x) {
              return _this29.uti_parent_id.includes(Number(x.parent_cat_user_ubicacion_id)) || [391].includes(Number(x.cat_user_ubicacion_id));
            }).length;
          }
        },
        mounted: function mounted() {
          return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee27() {
            return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee27$(_context27) {
              while (1) {
                switch (_context27.prev = _context27.next) {
                  case 0:
                  case "end":
                    return _context27.stop();
                }
              }
            }, _callee27);
          }))();
        }
      });
      return app;
    }
  }, {
    key: "ViewEnfermeria",
    value: function () {
      var _ViewEnfermeria = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee28() {
        var app1;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee28$(_context28) {
          while (1) {
            switch (_context28.prev = _context28.next) {
              case 0:
                _context28.next = 2;
                return this.AppRowVitalSign();

              case 2:
                _context28.next = 4;
                return this.AppNavMenuArea();

              case 4:
                app1 = _context28.sent;

              case 5:
              case "end":
                return _context28.stop();
            }
          }
        }, _callee28, this);
      }));

      function ViewEnfermeria() {
        return _ViewEnfermeria.apply(this, arguments);
      }

      return ViewEnfermeria;
    }()
    /*************** */

  }, {
    key: "qrCode",
    value: function qrCode(_ref25) {
      _objectDestructuringEmpty(_ref25);

      var qrcode = new QRCode(document.getElementById("qrcode"), {
        width: 100,
        height: 100
      });
      qrcode.makeCode(444444);
    }
  }, {
    key: "titleArea",
    value: function titleArea(_ref26) {
      var title = _ref26.title,
          _ref26$area = _ref26.area,
          area = _ref26$area === void 0 ? 'tp' : _ref26$area,
          _ref26$searchBy = _ref26.searchBy,
          searchBy = _ref26$searchBy === void 0 ? 'medico' : _ref26$searchBy,
          _ref26$cat_especialid = _ref26.cat_especialidad_id,
          cat_especialidad_id = _ref26$cat_especialid === void 0 ? 'null' : _ref26$cat_especialid;

      //console.log(cat_especialidad_id)
      if (searchBy === "medico") {
        searchBy = "UserEquipoSalud.menuEquipoEnArea({area:'".concat(area, "'})");
      }

      if (searchBy === "especialidad") {
        searchBy = "UserEquipoSalud.menuEquipoEnEspecialidad({cat_especialidad_id:".concat(cat_especialidad_id, "})");
      }

      $("#titleArea").html("\n            <button onclick=\"".concat(searchBy, "\" data-tippy-content=\"Pulse para ver el Equipo M\xE9dico en el \xE1rea\" type=\"button\" class=\"btn btn-light btn-block\">\n                <span style=\"font-weight:bold;color:var(--primary)!important\">").concat(title, "</span>\n            </button>\n        "));
      /*  tippy('[data-tippy-content]', {
           theme: 'tooltip-cmdlt-primary',
       }) */
    }
  }, {
    key: "menuAreas",
    value: function menuAreas(selector) {
      CatUserUbicacion.showMenu().then(function (response) {
        var CatUserUbicacion = "\n                    <li class=\"menuArea nav-item\" data-titlearea=\"Total pacientes\" data-area=\"tp\" role=\"presentation\" onclick=\"\">\n                        <a class=\"nav-link active\" id=\"pills-tp-tab\" data-toggle=\"pill\" href=\"#pills-tp\" role=\"tab\" aria-controls=\"pills-tp\" aria-selected=\"false\">TP</a>\n                    </li>\n                ";
        Component.titleArea({
          title: "Total Pacientes",
          area: 'tp'
        });
        $.each(response, function (indexInArray, valueOfElement) {
          CatUserUbicacion += "\n                    <li class=\"menuArea nav-item\" data-titleArea=\"".concat(valueOfElement['name'], "\" data-area=\"").concat(valueOfElement['description'], "\" role=\"presentation\" onclick=\"\">\n                        <a class=\"nav-link\" id=\"pills-").concat(valueOfElement['description'], "-tab\" data-toggle=\"pill\" href=\"#pills-").concat(valueOfElement['description'], "\" role=\"tab\" aria-controls=\"pills-").concat(valueOfElement['description'], "\" aria-selected=\"false\">").concat(valueOfElement['description'], "</a>\n                    </li>\n                ");
        });
        $(selector).html("\n                    <ul class=\"nav nav-pills flex-nowrap\" role=\"tablist\">\n                        ".concat(CatUserUbicacion, "\n                    </ul>\n                "));
        $(".menuArea").on("click", function () {
          $('#btnNotificaciones').empty();
          Component.menu($(this).data("area"), $(this).data("titlearea"));
        });
        tippy('[data-tippy-content]', {
          theme: 'tooltip-cmdlt-primary'
        });
      });

      if (location.pathname === "/telesalud/empresarial/index") {
        document.getElementById("cat_user_ubicacion_menu").style.display = "none";
      } //console.log('%cComponent.js line:1740 location', 'color: #007acc;', location);

    }
  }, {
    key: "searchPaciente",
    value: function searchPaciente(data) {
      var filtro = "";

      if (data.search != undefined) {
        filtro = "{'ubicacion':'TP'}";
        Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])("medico/rowPacienteCovid/" + filtro);
      }
    }
  }, {
    key: "newsSignos",
    value: function newsSignos(selector) {
      //Indica aquí, en pixels, el ancho de tu marquesina
      var marqueewidth = 600; //Indica aquí, en pixels, el alto de tu marquesina

      var marqueeheight = 20; //Indica aquí la velocidad de recorrido del texto

      var speed = 5; //Escribe el mensaje que debe aparecer (con sus correspondientes enlaces, si los hay)

      function news() {
        Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/vistas/v_prioridad_spo2").then(function (response) {
          var news = "";
          var total_news = response.length;
          $.each(response, function (indexInArray, valueOfElement) {
            news += "\n                            <span class=\"text-".concat(valueOfElement['prioridad'], "\" onclick=\"filtroPriorizados('").concat(valueOfElement['paciente'], "')\">\n                                <span class=\"font-weight-bold\">").concat(valueOfElement['signo'], "</span> -\n                                <span class=\"font-weight-bold\">").concat(valueOfElement['value'], "</span> |\n                                <span class=\"font-weight-bold text-gray\">").concat(valueOfElement['paciente'], "</span> |\n                                <span class=\"font-weight-bold text-gray\">").concat(valueOfElement['ubicacion'], "</span>\n                            </span>\n                        ");

            if (indexInArray < total_news - 1) {
              news += "\n                            <span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;-&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</span>\n                        ";
            }
          });
          $(selector).html('<marquee class="cursor-pointer" onmouseout="this.start()" onmouseover="this.stop()" scrollAmount=' + speed + ' style="width:' + marqueewidth + '">' + news + '</marquee>');
        });
      }

      news();
    }
  }, {
    key: "viewByEspecialidad",
    value: function viewByEspecialidad(_ref27) {
      var especialidad = _ref27.especialidad,
          cat_especialidad_id = _ref27.cat_especialidad_id;
      $('#btnNotificaciones').empty();
      $("#messageModal").modal("hide");
      $('#pills-tp-tab').tab('show'); //Component.titleArea({title:data.especialidad,area:data.cat_user_especialidad_id})

      Component.titleArea({
        title: especialidad,
        area: cat_especialidad_id,
        cat_especialidad_id: cat_especialidad_id,
        searchBy: "especialidad"
      });
      $("#row_paciente").html("\n            <tr>\n                <td colspan=\"18\">\n                    <div class=\"d-flex m-4 justify-content-center text-primary\">\n                        Cargando...\n                        <div class=\"spinner-border\" role=\"status\">\n                            <span class=\"sr-only\"></span>\n                        </div>\n                    </div>\n                </td>\n            </tr>\n        ");
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/viewbyespecialidad/" + cat_especialidad_id).then(function (response) {
        $('#btnNotificaciones').empty();
        var contadores = {
          'TP': 0,
          'EA': 0,
          'Ecvd': 0,
          'EP': 0,
          'AQ': 0,
          'HP2': 0,
          'HP3': 0,
          'HP4': 0,
          'HP5': 0,
          'HP6': 0,
          'UTIA': 0,
          'UTICvd': 0,
          'UTIP': 0,
          'PAD': 0,
          'Domicilio': 0
        };
        $.each(response, function (indexInArray, valueOfElement) {
          //console.log(valueOfElement['area'])
          contadores[valueOfElement['area']] = contadores[valueOfElement['area']] + 1;
        });
        $.each(contadores, function (indexInArray, valueOfElement) {
          if (contadores[indexInArray] > 0) {
            Component.notification('#btnNotificaciones', {
              route: '#',
              name: indexInArray,
              cantidad: contadores[indexInArray],
              length: length
            });
          }
        }); //

        /*
        */

        Component.filasViewCovid(response);
      });
    }
  }, {
    key: "viewByMedico",
    value: function viewByMedico(_ref28) {
      var medico_id = _ref28.medico_id,
          medico = _ref28.medico;
      $('#btnNotificaciones').empty();
      $("#modelId").modal("hide");
      $('#pills-tp-tab').tab('show');
      Component.titleArea({
        title: medico,
        area: medico_id
      }); //Component.titleArea(data.especialidad)

      $("#row_paciente").html("\n            <tr>\n                <td colspan=\"18\">\n                    <div class=\"d-flex m-4 justify-content-center text-primary\">\n                        Cargando...\n                        <div class=\"spinner-border\" role=\"status\">\n                            <span class=\"sr-only\"></span>\n                        </div>\n                    </div>\n                </td>\n            </tr>\n        ");
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/viewbymedico/" + medico_id).then(function (response) {
        $('#btnNotificaciones').empty();
        var contadores = {
          'TP': 0,
          'EA': 0,
          'Ecvd': 0,
          'EP': 0,
          'AQ': 0,
          'HP2': 0,
          'HP3': 0,
          'HP4': 0,
          'HP5': 0,
          'HP6': 0,
          'UTIA': 0,
          'UTICvd': 0,
          'UTIP': 0,
          'PAD': 0,
          'Domicilio': 0
        };
        $.each(response, function (indexInArray, valueOfElement) {
          //console.log(valueOfElement['area'])
          contadores[valueOfElement['area']] = contadores[valueOfElement['area']] + 1;
        });
        $.each(contadores, function (indexInArray, valueOfElement) {
          if (contadores[indexInArray] > 0) {
            Component.notification('#btnNotificaciones', {
              route: '#',
              name: indexInArray,
              cantidad: contadores[indexInArray],
              length: length
            });
          }
        }); //

        /*
        */

        Component.filasViewCovid(response);
      });
    }
  }, {
    key: "viewMedico",
    value: function viewMedico(selector, data) {
      $(selector).html("\n            <div class=\"container-fluid sticky-top\">\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 \">\n                        <div class=\"table-responsive-md\">\n                            <table id=\"tablapacientes\" class=\"w-100\">\n                                <thead>\n                                    <tr>\n                                        <th class=\"text-uppercase sticky-top\" colspan=\"13\" style=\"top: -0.1em !important;\">\n                                            <div id=\"cat_user_ubicacion_menu\"></div>\n                                        </th>\n                                        <th align=\"middle\" class=\"bg-white sticky-top\" colspan=\"5\" style=\"top: -0.1em !important;\">\n                                            <button id=\"btn_paciente_create\" onclick=\"window.location='/medico/create_paciente'\"  class=\"btn btn-success font-weight-bold btn-block\" style=\"\" >Nuevo Paciente</button>\n                                        </th>\n                                    </tr>\n\n                            </table>\n                            <div id=\"cat_user_ubicacion_menu_sub_1_content\"></div>\n                        </div>\n                    </div>\n                </div>\n            </div>\n        "); //console.log(data.pacientes)

      /*Fila a crear*/

      var response = [{
        name: "FHOI",
        description: "Fundación Hospital Ortopédico Infantil",
        id: "1"
      }, {
        name: "UROLÓGICO",
        description: "Hospital Urológico San Román",
        id: "2"
      }, {
        name: "CMDLT",
        description: "CMDLT",
        id: "3"
      }];
      var response2 = [{
        name: "MAR",
        description: "Marformaciones Ano Rectales",
        id: "1"
      }, {
        name: "UROPED",
        description: "Urología Pediátrica",
        id: "2"
      }, {
        name: "CIRUPED",
        description: "Cirugia Pediátrica general",
        id: "3"
      }];
      var CatUserUbicacion = "";
      var CatUserUbicacionContent = "";
      /*menu 1*/

      $.each(response, function (indexInArray, valueOfElement) {
        CatUserUbicacion += "\n                    <li class=\"nav-item\" role=\"presentation\">\n                        <a class=\"nav-link ".concat(indexInArray == 0 ? 'active' : '', "\" id=\"hospital_").concat(valueOfElement['id'], "-tab\" data-toggle=\"pill\" href=\"#hospital_").concat(valueOfElement['id'], "\" role=\"tab\" aria-controls=\"hospital_").concat(valueOfElement['id'], "\" aria-selected=\"true\" data-tippy-content=\"").concat(valueOfElement['description'], "\">").concat(valueOfElement['name'], "</a>\n                    </li>\n                ");
        CatUserUbicacionContent += "\n                    <div class=\"tab-pane fade  ".concat(indexInArray == 0 ? 'show active' : '', "\" id=\"hospital_").concat(valueOfElement['id'], "\" role=\"tabpanel\" aria-labelledby=\"hospital_").concat(valueOfElement['id'], "-tab\">\n                        <div id=\"menu_hospital_").concat(valueOfElement['id'], "\">").concat(valueOfElement['id'], "</div>\n                        <div id=\"content_hospital_").concat(valueOfElement['id'], "\">").concat(valueOfElement['id'], "</div>\n                    </div>\n                ");
      });
      $("#cat_user_ubicacion_menu").html("\n                <ul class=\"nav nav-pills bg-light\" id=\"pills-tab1\" style=\"width: max-content !important;\" role=\"tablist\">\n                    ".concat(CatUserUbicacion, "\n                </ul>\n            "));
      $("#cat_user_ubicacion_menu_sub_1_content").html("\n            <div class=\"tab-content\" id=\"pills-tabContent1\">\n                ".concat(CatUserUbicacionContent, "\n            </div>\n        "));
      /** */

      /*menu 2*/

      $.each(response, function (indexInArray, valueOfElement) {
        var CatUserUbicacion2 = "";
        var CatUserUbicacion2Content = "";
        $.each(response2, function (indexInArray2, valueOfElement2) {
          CatUserUbicacion2 += "\n                    <li class=\"nav-item\" role=\"presentation\">\n                        <a class=\"nav-link ".concat(indexInArray2 == 0 ? 'active' : '', "\" id=\"pills-").concat(valueOfElement['id'], "-sub").concat(valueOfElement2['id'], "-tab\" data-toggle=\"pill\" href=\"#pills-").concat(valueOfElement['id'], "-sub").concat(valueOfElement2['id'], "\" role=\"tab\" aria-controls=\"pills-").concat(valueOfElement['id'], "-sub").concat(valueOfElement2['id'], "\" aria-selected=\"true\" data-tippy-content=\"").concat(valueOfElement2['description'], "\">").concat(valueOfElement2['name'], "</a>\n                    </li>\n                ");
          CatUserUbicacion2Content += "\n                    <div class=\"tab-pane fade  ".concat(indexInArray2 == 0 ? 'show active' : '', "\" id=\"pills-").concat(valueOfElement['id'], "-sub").concat(valueOfElement2['id'], "\" role=\"tabpanel\" aria-labelledby=\"pills-").concat(valueOfElement['id'], "-sub").concat(valueOfElement2['id'], "-tab\">\n                        <table id=\"tablapacientes\" class=\"w-100\">\n                            <thead>\n                                <tr>\n                                    <th class=\"bg-white text-uppercase sticky-top\" colspan=\"13\" style=\"top: 2.3em;\">\n                                        <div id=\"cat_user_ubicacion_menu_sub_1\"></div>\n                                    </th>\n                                    <th align=\"middle\" class=\"bg-white sticky-top\" colspan=\"5\" style=\"top: 2.3em;\">\n                                        <input type=\"text\" class=\"form-control\" id=\"busquedapaciente\" placeholder=\"Buscar paciente...\" aria-label=\"Buscar paciente...\" aria-describedby=\"button-addon2\">\n                                    </th>\n                                </tr>\n                                <tr>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Ubicaci\xF3n del paciente\">UBI</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"D\xEDas desde el ingreso\">D\xCDAS</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Informaci\xF3n del paciente\">PACIENTE</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"G\xE9nero\">SEXO</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Edad\">EDAD</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Temperaratura\">TEMP</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Frecuencia Card\xEDaca\">F.C</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Frecuencia Respiratoria\">F.R</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Tensi\xF3n Arterial\">T.A</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Glicemia\">GL</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Oximetr\xEDa\">SPO2</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Oxigenoterapia\">OXIGENOTERAPIA</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Sintomas\">S\xCDNTOMA</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"COVID PCR\">PCR</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"COVID PDR\">PDR</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"COVID ANTIG\xC9NICA\">ANTG</th>\n                                    <th class=\"text-center bg-gray p-2 sticky-top\" style=\"top: 5.3em;color: var(--secondary) !important\" data-tippy-content=\"Tomografpia axial computarizada\">TAC</th>\n\n                                </tr>\n                            </thead>\n                            <tbody id=\"row_paciente\">\n\n                            </tbody>\n                        </table>\n                    </div>\n                ");
        });
        $("#menu_hospital_".concat(valueOfElement['id'])).html("\n                <ul class=\"nav nav-pills bg-light\" id=\"pills-tab1\" style=\"width: max-content !important;\" role=\"tablist\">\n                    ".concat(CatUserUbicacion2, "\n                </ul>\n            "));
        $("#content_hospital_".concat(valueOfElement['id'])).html("\n                <div class=\"tab-content\" id=\"pills-tabContent2".concat(valueOfElement['id'], "\">\n                    ").concat(CatUserUbicacion2Content, "\n                </div>\n            "));
      });
      /** */

      Component.searchPaciente({}); //filtroTabla()

      if (data.pacientes != null) {
        if (data.pacientes.length > 0) {
          $.each(data.pacientes, function (indexInArray, valueOfElement) {
            var col_light = true;
            /*inicio fila*/

            var fila = "<tr class='row_".concat(valueOfElement['user_id'], "'>");

            for (var i = 1; i <= 18; i++) {
              var stilos = "";

              if (i == 5) {
                stilos = "border-right: 2px solid #bebcbc !important";
              }

              fila += "<td id=\"col_".concat(i, "_").concat(valueOfElement['user_id'], "\" style=\" ").concat(stilos, "\" class=\"border ").concat(col_light == true ? 'bg-gray-2' : '', "\">");
              fila += "";
              fila += "</td>";
              col_light == true ? col_light = false : col_light = true;
            }

            fila += "</tr>";
            $("#row_paciente").append(fila);
            /*fin fila*/
            //**icono calendario */

            var dias_cal = "<img loading='lazy' class='img-fluid heartbeat' style='width: 30px;height: 30px;' src='/image/system/icono_cal_7.png' alt='7'>";

            if (valueOfElement['cal'] == 7) {
              dias_cal = "<img loading='lazy' class='img-fluid heartbeat' style='width: 30px;height: 30px;' src='/image/system/icono_cal_7.png' alt='7'>";
            } else if (valueOfElement['cal'] == 14) {
              dias_cal = "<img loading='lazy' class='img-fluid heartbeat' style='width: 30px;height: 30px;' src='/image/system/icono_cal_14.png' alt='14'>";
            } else if (valueOfElement['cal'] == 21) {
              dias_cal = "<img loading='lazy' class='img-fluid heartbeat' style='width: 30px;height: 30px;' src='/image/system/icono_cal_21.png' alt='21'>";
            } //**icono alerta */


            var alerta = "<span id='default-alert_${valueOfElement['user_id']}' style='font-size: 25px !important;' class='heartbeat text-success alert-estable font-weight-bold'>!</span>";

            if (valueOfElement['alert'] == 1) {
              alerta = "<span id='default-alert_${valueOfElement['user_id']}' style='font-size: 25px !important;' class='heartbeat text-success alert-estable font-weight-bold'>!</span>";
            } else if (valueOfElement['alert'] == 2) {
              alerta = "<span id='default-alert_${valueOfElement['user_id']}' style='font-size: 25px !important;' class='heartbeat text-warning alert-estable font-weight-bold ping'>!!</span>";
            } else if (valueOfElement['alert'] == 3) {
              alerta = "<span id='default-alert_${valueOfElement['user_id']}' style='font-size: 25px !important;' class='heartbeat text-danger alert-estable font-weight-bold ping-2'>!!!</span>";
            }

            $("#col_1_".concat(valueOfElement['user_id'])).html("\n                        <div class=\"text-secondary text-center\">\n                            <button class=\"btn btn-light\">".concat(valueOfElement['ubicacion'], "</button>\n                        </div>\n                    "));
            $("#col_2_".concat(valueOfElement['user_id'])).html("\n                        <div class=\"text-center text-secondary\">\n                            ".concat(valueOfElement['dias'], "\n                        </div>\n                    "));
            $("#col_3_".concat(valueOfElement['user_id'])).html("\n                        <div class=\"text-secondary mb-2\">\n                            <table style=\"width: calc(13vh*2) !important;\">\n                                <tr>\n                                    <td class=\"h4 pb-4\" colspan=\"6\">\n                                        <span class=\"cursor-pointer\">\n                                            ".concat(valueOfElement['paciente'], "\n                                        </span>\n                                        <span>\n                                            <div class=\"btn-group\" style=\"margin-left: 20px\"\n                                                role=\"group\" aria-label=\"\">\n                                                <div class=\"btn-group\" role=\"group\">\n                                                    <a id=\"default-alert_").concat(valueOfElement['user_id'], "\" type=\"button\"\n                                                        data-toggle=\"dropdown\"\n                                                        aria-haspopup=\"true\"\n                                                        aria-expanded=\"false\">\n                                                        ").concat(alerta, "\n                                                    </a>\n                                                    <div class=\"dropdown-menu\"\n                                                        aria-labelledby=\"dropdownId\">\n                                                        <a onclick=\"addAlert(1,").concat(valueOfElement['user_id'], ")\"\n                                                            class=\"dropdown-item\"><span\n                                                                class=\"text-success\">!111111</span>\n                                                            Estable</a>\n                                                        <a onclick=\"addAlert(2,").concat(valueOfElement['user_id'], ")\"\n                                                            class=\"dropdown-item\"><span\n                                                                class=\"text-warning\">!!</span>\n                                                            Intermedio</a>\n                                                        <a onclick=\"addAlert(3,").concat(valueOfElement['user_id'], ")\"\n                                                            class=\"dropdown-item\"><span\n                                                                class=\"text-danger\">!!!</span>\n                                                            De cuidado</a>\n                                                    </div>\n                                                </div>\n                                            </div>\n                                        </span>\n                                    </td>\n                                </tr>\n                                <tr>\n                                    <td>\n                                        <div class=\"dropdown\">\n                                            <a id=\"triggerId_").concat(valueOfElement['user_id'], "\" class=\"h4 cursor-pointer\" data-toggle=\"dropdown\" aria-haspopup=\"true\" aria-expanded=\"false\">\n                                                ").concat(dias_cal, "\n                                            </a>\n                                            <div class=\"dropdown-menu\" style=\"width:4em;z-index: 10000;\" aria-labelledby=\"triggerId_").concat(valueOfElement['user_id'], "\">\n                                                <a onclick=\"addCal(7,").concat(valueOfElement['user_id'], ")\" style=\"width:4em\" class=\"dropdown-item cursor-pointer\"><img loading=\"lazy\" class=\"img-fluid\" style=\"width: 30px;height: 30px;\" src=\"").concat(location.origin, "/image/system/icono_cal_7.png\" alt=\"\"></a>\n                                                <a onclick=\"addCal(14,").concat(valueOfElement['user_id'], ")\" style=\"width:4em\" class=\"dropdown-item cursor-pointer\"><img loading=\"lazy\" class=\"img-fluid\" style=\"width: 30px;height: 30px;\" src=\"").concat(location.origin, "/image/system/icono_cal_14.png\" alt=\"\"></a>\n                                                <a onclick=\"addCal(21,").concat(valueOfElement['user_id'], ")\" style=\"width:4em\" class=\"dropdown-item cursor-pointer\"><img loading=\"lazy\" class=\"img-fluid\" style=\"width: 30px;height: 30px;\" src=\"").concat(location.origin, "/image/system/icono_cal_21.png\" alt=\"\"></a>\n                                            </div>\n                                        </div>\n                                    </td>\n                                    <td>\n                                        <a onclick=\"UserCuestHistoria.index('.modal-body',").concat(valueOfElement['user_id'], ")\" class=\"h4 cursor-pointer\">\n                                            <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"").concat(location.origin, "/image/system/icono-new-historia.png\">\n                                        </a>\n                                    </td>\n                                    <td>\n                                        <div id=\"col_pad_").concat(valueOfElement['user_id'], "\"></div>\n                                    </td>\n                                    <td>\n                                        <a id=\"btn_infoPaciente_").concat(valueOfElement['user_id'], "\" onclick=\"datosInfoPaciente(").concat(valueOfElement['user_id'], ")\"class=\"h4 cursor-pointer\">\n                                            <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"").concat(location.origin, "/image/system/info.svg\">\n                                        </a>\n                                    </td>\n                                    <td>\n                                        <a id=\"btn_evolucion_").concat(valueOfElement['user_id'], "\" onclick=\"datosPaciente(").concat(valueOfElement['user_id'], ")\"class=\"h4 cursor-pointer\">\n                                            <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"").concat(location.origin, "/image/system/icono-new-evolucion.png\">\n                                        </a>\n                                    </td>\n                                    <td>\n                                        <a onclick=\"UserCuestInforme.index('.modal-body',").concat(valueOfElement['user_id'], ")\" class=\"h4 cursor-pointer\">\n                                            <img loading=\"lazy\" class=\"heartbeat\" style=\"width: 30px;height: 30px;\" src=\"").concat(location.origin, "/image/system/icono_archivo2.svg\">\n                                        </a>\n                                    </td>\n                                </tr>\n                            </table>\n                        </div>\n                    "));
            $("#col_4_".concat(valueOfElement['user_id'])).html("\n                        <div class=\"text-center text-uppercase text-secondary\">\n                            ".concat(valueOfElement['sexo'], "\n                        </div>\n                    "));
            $("#col_5_".concat(valueOfElement['user_id'])).html("\n                        <div class=\"text-center text-secondary\">\n                            ".concat(valueOfElement['edad'], "\n                        </div>\n                    "));
            /*IMAGEN PAD */

            UserCuestPad.columnaValor("#col_pad_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': 1
            });
            /*TEMPERATURA*/

            _UserCuestTemperatura_js__WEBPACK_IMPORTED_MODULE_3__["default"].columnaValor("#col_6_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['temp']
            });
            /*FRECUENCIA CARDIACA*/

            _UserCuestFc_js__WEBPACK_IMPORTED_MODULE_6__["default"].columnaValor("#col_7_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['fc']
            });
            /*FRECUENCIA RESPIRATORIA*/

            _UserCuestFr_js__WEBPACK_IMPORTED_MODULE_4__["default"].columnaValor("#col_8_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['fr']
            });
            /*TENSION ARTERIAL*/

            _UserCuestTa_js__WEBPACK_IMPORTED_MODULE_5__["default"].columnaValor("#col_9_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['ta']['sys'],
              'valor2': valueOfElement['ta']['dia']
            });
            /*GLICEMIA*/

            _UserCuestGl_js__WEBPACK_IMPORTED_MODULE_8__["default"].columnaValor("#col_10_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['gl']
            });
            /*OXIMETRIA*/

            _UserCuestOximetria_js__WEBPACK_IMPORTED_MODULE_7__["default"].columnaValor("#col_11_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['oxi']
            });
            /*OXIGENOTERAPIA*/

            UserCuestOxigenoterapia.columnaValor("#col_12_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['oxit']
            });
            /*SINTOMA*/

            UserCuestSintoma.columnaValor("#col_13_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['sintoma']
            });
            /*COVID PCR*/

            UserCuestPcr.columnaValor("#col_14_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['pcr']
            });
            /*COVID PDR*/

            UserCuestPdr.columnaValor("#col_15_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['pdr']
            });
            /*COVID ANTG*/

            UserCuestAntg.columnaValor("#col_16_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['antg']
            });
            /*COVID TAC*/

            UserCuestTac.columnaValor("#col_17_".concat(valueOfElement['user_id']), {
              'user_id': valueOfElement['user_id'],
              'valor': valueOfElement['tac']
            });
          });
        }
      } else {
        $("#row_paciente").html("\n                <tr>\n                    <td colspan=\"18\" class=\"text-center p-3 text-primary\">\n                        No se encontraron pacientes\n                    </td>\n                </tr>\n            ");
      }

      tippy('[data-tippy-content]', {
        theme: 'tooltip-cmdlt-primary'
      });
      tippy('[data-tippy-content]', {
        theme: 'tooltip-cmdlt-primary'
      });
    }
  }, {
    key: "viewPendientes",
    value: function viewPendientes(selector) {
      $(selector).html("\n            <div class=\"container-fluid sticky-top\">\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 \">\n                        <div class=\"h1 text-primary\">\n                            Nuevos pacientes\n\n                            <button onclick=\"window.location=''\" type=\"button\" class=\"m-1 text-uppercase text-primary font-weight-bold btn btn-gray float-right mr-1\">\n                                Total P2 <span id=\"total_prioridad2\" class=\"badge badge-light\">0</span>\n                            </button>\n                            <button onclick=\"window.location=''\" type=\"button\" class=\"m-1 text-uppercase text-primary font-weight-bold btn btn-gray float-right mr-1\">\n                                Total P1 <span id=\"total_prioridad1\" class=\"badge badge-light\">0</span>\n                            </button>\n                        </div>\n                    </div>\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg6 \">\n                        <div class=\"h3 text-primary\">\n                            Prioridad 1\n                        </div>\n                        <div class=\"table-responsive-lg\">\n                            <table class=\"w-100 table-bordered\">\n                                <thead>\n                                    <tr>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"Nombre del paciente\">Paciente</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"Tel\xE9fono del contacto\">Tel\xE9fono</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"Temperatura al momento del registro\">Temp</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"Oximetr\xEDa al momento del registro\">Oxi</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"V\xEDa de cont\xE1cto con la instituci\xF3n\">Ubicaci\xF3n</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\">Acci\xF3n</th>\n                                    </tr>\n                                </thead>\n                                <tbody id=\"row_prioridad1\">\n\n                                </tbody>\n                            </table>\n                        </div>\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg6 \">\n                        <div class=\"h3 text-primary\">\n                            Prioridad 2\n                        </div>\n                        <div class=\"table-responsive-lg\">\n                            <table class=\"w-100 table-bordered\">\n                                <thead>\n                                    <tr>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"Nombre del paciente\">Paciente</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"Tel\xE9fono del contacto\">Tel\xE9fono</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"Temperatura al momento del registro\">Temp</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"Oximetr\xEDa al momento del registro\">Oxi</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\" data-tippy-content=\"V\xEDa de cont\xE1cto con la instituci\xF3n\">Ubicaci\xF3n</th>\n                                        <th class=\"text-center bg-gray p-2 sticky-top\" style=\"color: var(--primary) !important\">Acci\xF3n</th>\n                                    </tr>\n                                </thead>\n                                <tbody id=\"row_prioridad2\">\n\n                                </tbody>\n                            </table>\n                        </div>\n                    </div>\n                </div>\n            </div>\n        ");
      Component.viewListado("prioridad1", "/vistas/v_prioridad_1");
      Component.viewListado("prioridad2", "/vistas/v_prioridad_2"); // let prioridad1 = get( location.origin+"/vistas/v_prioridad_1");
      //let prioridad2 = get( location.origin+"/vistas/v_prioridad_2");

      tippy('[data-tippy-content]', {
        theme: 'tooltip-cmdlt-primary'
      });
    }
  }, {
    key: "notification",
    value: function notification(selector, data) {
      $(selector).append("\n            <button data-area=\"".concat(data.area, "\" data-titlearea=\"").concat(data.name, "\" type=\"button\" class=\"btnNotification text-uppercase text-primary font-weight-bold btn btn-gray float-right mr-1\">\n                ").concat(data.name, " <span style=\"font-size: 96%;\" class=\"badge badge-light text-secondary\">").concat(data.cantidad, "</span>\n            </button>\n        "));
    }
  }, {
    key: "viewListado",
    value: function viewListado(selector, vista) {
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(vista).then(function (response) {
        var row = "";
        $("#total_" + selector).html(response.length);
        $("#row_" + selector).empty();

        if (response.length > 0) {
          $.each(response, function (indexInArray, valueOfElement) {
            row += "\n                        <tr id=\"row_".concat(valueOfElement['user_id'], "\">\n                            <td class=\"text-center\">").concat(valueOfElement['paciente'], "</td>\n                            <td class=\"text-center\">").concat(valueOfElement['celular'], "</td>\n                            <td class=\"text-center\">").concat(valueOfElement['temperatura'] != "" ? valueOfElement['temperatura'] + '°C' : '', "</td>\n                            <td class=\"text-center\">").concat(valueOfElement['oximetria'] != "" ? valueOfElement['oximetria'] + '%' : '', "</td>\n                            <td class=\"text-center\">").concat(valueOfElement['ubicacion'], "</td>\n                            <td style=\"width:2em;\">\n                            <div class=\"d-flex\">\n                                <div>\n                                    <a data-id=\"").concat(valueOfElement['user_id'], "\" class=\"btn btn-block btn-default btn-read-").concat(valueOfElement['user_id'], "\"><i class=\"fas fa-search text-primary\" data-tippy-content=\"Ver cuestionario\"></i></a>\n\n                                </div>\n                                <div>\n                                    <a data-id=\"").concat(valueOfElement['user_id'], "\" class=\"btn btn-block btn-default btn-move-").concat(valueOfElement['user_id'], "\"><i class=\"fas fa-share-square text-primary\" data-tippy-content=\"Trasladar\"></i></a>\n\n                                </div>\n                            </div>\n                                <!--<a data-id=\"").concat(valueOfElement['user_id'], "\" class=\"btn btn-default btn-delete-").concat(valueOfElement['user_id'], "\"><i class=\"fas fa-trash text-primary\" data-tippy-content=\"Eliminar\"></i></a>-->\n                            </td>\n                        </tr>\n                        ");
          });
          $("#row_" + selector).html(row);
          $.each(response, function (indexInArray, valueOfElement) {
            $(".btn-read-".concat(valueOfElement['user_id'])).on("click", function () {
              _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_9__["default"].edit('.modal-body', $(this).data('id')); //UserCuestObservacion.create(".modal-body", $(this).data('id'))
            });
            $(".btn-delete-".concat(valueOfElement['user_id'])).on("click", function () {
              alert("No disponible aún.");
            });
            $(".btn-move-".concat(valueOfElement['user_id'])).on("click", function () {
              var ingreso = "Ingreso";
              _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_2__["default"].create3(".modal-body", $(this).data('id'));
            });
          });
        } else {
          $("#row_" + selector).html("\n                    <tr>\n                        <td colspan=\"7\" style=\"font-size: 1.6em;\" class=\"text-center text-primary\">\n                            No se encontraron pacientes\n                        </td>\n                    </tr>\n                ");
        }
      });
    }
  }], [{
    key: "alertMessage",
    value: function alertMessage(caso) {
      switch (caso) {
        case 'expire_sesion':
          return {
            'title': 'Atención',
            'message': 'Su sesión ha finalizado por inactividad.',
            'image': 'success'
          };
          break;

        case 'input_text_empty':
          return {
            'title': 'Atención',
            'message': 'El campo no puede estar vacio.',
            'image': 'warning'
          };
          break;

        case 'input_select_empty':
          return {
            'title': 'Atención',
            'message': 'Debe seleccionar una opción válida.',
            'image': 'warning'
          };
          break;

        case 'input_datetime_empty':
          return {
            'title': 'Atención',
            'message': 'Debe seleccionar una fecha y hora válida.',
            'image': 'warning'
          };
          break;

        case 'input_date_empty':
          return {
            'title': 'Atención',
            'message': 'Debe seleccionar una fecha válida.',
            'image': 'warning'
          };
          break;

        case 'input_checkbox_empty':
          return {
            'title': 'Atención',
            'message': 'Debe seleccionar al menos una opción.',
            'image': 'warning'
          };
          break;

        case 'destroy_row_cuestion':
          return {
            'title': 'Atención',
            'message': '¿Seguro que desea eliminar este registro?',
            'image': 'warning'
          };
          break;

        case 'update_row_cuestion':
          return {
            'title': 'Atención',
            'message': '¿Seguro que desea actualizar este registro?',
            'image': 'warning'
          };
          break;

        case 'user_no_valid':
          return {
            'title': 'Atención',
            'message': 'Ingrese un Documento de identidad ó un Correo Electrónico para continuar',
            'image': 'warning'
          };
          break;

        case 'close_cie11':
          return {
            'title': 'Atención',
            'message': '¿Desea cerrar el clasificador CIE11?',
            'image': 'warning'
          };
          break;

        case 'cedula_registrado':
          return {
            'title': 'Atención',
            'message': 'El Documento de identidad ya se encuentra asociado a un paciente. Ingrese uno distinto.',
            'image': 'warning'
          };
          break;

        case 'email_registrado':
          return {
            'title': 'Atención',
            'message': 'El Correo Electrónico ya se encuentra asociado a un paciente. Ingrese uno distinto.',
            'image': 'warning'
          };
          break;

        case 'send_success':
          return {
            'title': 'Completado',
            'message': 'Registro creado exitosamente',
            'image': 'success'
          };
          break;

        case 'error':
          return {
            'title': 'Error',
            'message': 'Ha ocurrido un error',
            'image': 'error'
          };
          break;

        case 'update_success':
          return {
            'title': 'Completado',
            'message': 'Registro actualizado exitosamente',
            'image': 'success'
          };
          break;

        case 'cedula_asignada':
          return {
            'title': 'Atención',
            'message': ' ya se encuentra asignado a un paciente, ¿desea reingresarlo?.',
            'image': 'warning'
          };
          break;

        case 'cedula_asignada2':
          return {
            'title': 'Atención',
            'message': ' ya se encuentra asignada a un paciente registrado.',
            'image': 'warning'
          };
          break;

        default:
          console.log("error_menssage");
          break;
      }
    }
  }]);

  return Component;
}();



/***/ }),

/***/ "./public/class_js/MedicosPaciente.js":
/*!********************************************!*\
  !*** ./public/class_js/MedicosPaciente.js ***!
  \********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers */ "./public/helpers/helpers.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); enumerableOnly && (symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; })), keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = null != arguments[i] ? arguments[i] : {}; i % 2 ? ownKeys(Object(source), !0).forEach(function (key) { _defineProperty(target, key, source[key]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }



var AppResumeHealthTeam = function AppResumeHealthTeam(episodio_id) {
  var app = new Vue({
    el: '#AppResumeHealthTeam',
    template: "\n      <div class=\"flex-fill p-1 card overflow-hidden\">\n        <ul class=\"nav nav-tabs p-0 m-0 flex-nowrap\" id=\"myTab\" role=\"tablist\">\n          <li class=\"nav-item p-0 m-0\" role=\"presentation\">\n            <a class=\"nav-link p-0 m-0 px-2 active\" id=\"equipoEspecialistas-tab\" data-toggle=\"tab\"\n              href=\"#equipoEspecialistas\" role=\"tab\" aria-controls=\"equipoEspecialistas\" aria-selected=\"true\">\n              <b class=\"text-center text-primary\" style=\"font-size:0.8em;\">M\xE9dicos</b>\n            </a>\n          </li>\n          <li class=\"nav-item p-0 m-0\" role=\"presentation\">\n            <a class=\"nav-link p-0 px-2 m-0\" id=\"equipoEnfermeria-tab\" data-toggle=\"tab\" href=\"#equipoEnfermeria\" role=\"tab\"\n              aria-controls=\"equipoEnfermeria\" aria-selected=\"false\">\n              <b class=\"text-center text-warning\" style=\"font-size:0.8em;\">Enfermer\xEDa</b>\n            </a>\n          </li>\n        </ul>\n        <div class=\"tab-content \" id=\"myTabContent\">\n          <div class=\"tab-pane fade show active\" id=\"equipoEspecialistas\" role=\"tabpanel\"\n            aria-labelledby=\"equipoEspecialistas-tab\">\n\n            <div class=\"p-0\"> \n              <ul class=\"list-group list-group-flush\">\n                <li v-for=\"(item,index) in resumeTeam.filter(x=>x.team_id!==6)\" :key=\"index\" data-toggle=\"tooltip\"\n                  data-placement=\"top\" :title=\"'Equipo '+item.name\" :data-custom-class=\"'tooltip-'+item.color \"\n                  class=\"list-group-item list-group-item-action p-0 cursor-pointer\" @click=\"getTeamList(item.team_id)\">\n                  <div class=\"d-flex flex-column\">\n                    <div class=\"d-flex align-items-center \">\n                      <div :class=\"['badge mx-2','badge-'+item.color]\" style=\"width:25px\">\n                        {{item.siglas}}\n                      </div>\n                      <img v-if=\"getLastMedic(item.team_id)\" loading=\"lazy\" style=\"width:20px;height:20px;border-radius:20px;\" class=\"img-doctor mx-1\"\n                        onerror=\"this.src='https://placehold.co/30x30'\" :src=\"getLastMedic(item.team_id).medico_img\">\n                   \n                      <div class=\"text-nowrap position-relative d-flex align-items-center\">\n                        <div >{{getLastMedic(item.team_id) ? getLastMedic(item.team_id).medico : item.name}}</div>\n                      \n                        <span \n                        v-if=\"item.team_id !== 2 && episode?.medico_paciente.filter(x=>x.team_id===item.team_id).length > 1\"\n                        class=\"bg-gray text-secondary ml-2 px-1 rounded \" style=\"top:-5px;font-size: 0.7rem;\">\n                        +{{episode?.medico_paciente.filter(x=>x.team_id===item.team_id).length}}\n                        </span>\n             \n                      </div>\n                    </div>\n\n                  </div>\n                </li>\n              </ul>\n            </div>\n\n          </div>\n          <div class=\"tab-pane fade\" id=\"equipoEnfermeria\" role=\"tabpanel\" aria-labelledby=\"equipoEnfermeria-tab\">\n            <div class=\"p-0\">\n              <ul class=\"list-group list-group-flush\">\n                <li v-for=\"(item,index) in resumeTeam.filter(x=>x.team_id===6)\" :key=\"index\" data-toggle=\"tooltip\"\n                  data-placement=\"top\" :title=\"'Equipo '+item.name\" :data-custom-class=\"'tooltip-'+item.color \"\n                  class=\"list-group-item list-group-item-action p-0 cursor-pointer\" @click=\"getTeamList(item.team_id)\">\n                  <div class=\"d-flex flex-column\">\n                    <div class=\"d-flex align-items-center \">\n                      <div :class=\"['badge mx-2','badge-'+item.color]\" style=\"width:25px\">\n                        {{item.siglas}}\n                      </div>\n                      <img v-if=\"getLastMedic(item.team_id)\" loading=\"lazy\" style=\"width:20px;height:20px;border-radius:20px;\" class=\"img-doctor mx-1\"\n                        onerror=\"this.src='https://placehold.co/30x30'\" :src=\"getLastMedic(item.team_id).medico_img\">\n                   \n                      <div class=\"text-nowrap position-relative d-flex align-items-center\">\n                        <div >{{getLastMedic(item.team_id) ? getLastMedic(item.team_id).medico : item.name}}</div>\n                      \n                        <span \n                        v-if=\"item.team_id !== 2 && episode?.medico_paciente.filter(x=>x.team_id===item.team_id).length > 1\"\n                        class=\"bg-gray text-secondary ml-2 px-1 rounded \" style=\"top:-5px;font-size: 0.7rem;\">\n                        +{{episode?.medico_paciente.filter(x=>x.team_id===item.team_id).length}}\n                        </span>\n             \n                      </div>\n                    </div>\n\n                  </div>\n                </li>\n              </ul>\n            </div>\n          </div>\n        </div>\n        <!-- Modal -->\n        <div class=\"modal fade\" id=\"healthTeamModal\" tabindex=\"-1\" aria-labelledby=\"healthTeamModalLabel\"\n          aria-hidden=\"true\">\n          <div class=\"modal-dialog modal-xl\" style=\"padding: 3rem;\">\n                <div class=\"modal-content\" \n                    style=\"\n                        height: 90vh;\n                        width: 95vw;\n                    \"\n                >\n    \n              <div\n                :class=\"['modal-header d-flex align-items-center justify-content-between text-white','bg-'+currentModalTeam?.color]\">\n                <h5 class=\"modal-title\">Equipo {{currentModalTeam?.name}}</h5>\n                <button type=\"button\" class=\"btn btn-default p-1\" @click=\"handleCloseModal\" aria-label=\"Close\">\n                  <span class=\"text-white\" aria-hidden=\"true\">\xD7</span>\n                </button>\n              </div>\n              <div class=\"modal-body d-flex p-0 overflow-auto\" style=\"height: 75vh;\">\n                <div class=\"col-12 col-md-6 p-0 d-flex flex-column\">\n                  <div class=\"p-1\">\n                    <div \n                      class=\"text-dark\" \n                      style=\"    \n                        font-size: 1.2rem;\n                        font-weight: 500;\n                      \"\n                    >\n                      A\xF1adir nuevo {{currentModalTeam?.name}}:\n                    </div>\n                    <input \n                      type=\"search\" \n                      v-model=\"searchQuery\"\n                      :placeholder=\"'Escribe '+currentModalTeam?.name+' a buscar...'\"\n                      class=\"form-control\"\n                    >\n                    <div class=\"alert alert-warning my-1\">\n                      <b>Pulsa</b> en un {{currentModalTeam?.team_id!==6?'m\xE9dico':'personal de enfermer\xEDa'}} del siguiente listado para asignarlo al paciente.\n                    </div>\n                  </div>\n                  <div class=\"flex-fill d-flex flex-column overflow-auto p-1\">\n                    <ul class=\"mt-1 list-group\">\n                      <template v-for=\"(especiality,index) in filteredHealthTeam\" >\n                        <li \n                          \n                          :key=\"index\" \n                          style=\"font-weight: 500 !important;\"\n                          :class=\"['list-group-item d-flex flex-column list-group-item-action p-0','text-'+currentModalTeam?.color]\"\n                        >\n                          <div class=\"pl-1 w-100 border-bottom\">{{especiality.name}}</div>\n                          <ul class=\"mt-1 list-group list-group-flush\">\n                            <template v-for=\"(team,index2) in especiality.healthTeam\" >\n                              <li \n                                :key=\"index2\" \n                                style=\"font-weight: 500 !important;\"\n                                :class=\"['list-group-item d-flex flex-column list-group-item-action p-1',{['active-'+currentModalTeam?.color]:is_assigned(team)}\n                                \n                                ]\"\n                                @click=\"addTeam(team)\"  \n                              >\n                                <div class=\"d-flex align-items-center\" style=\"gap:3px;\">\n                                    <img loading=\"lazy\" style=\"width:20px;height:20px;border-radius:20px;\" class=\"img-doctor mx-1\" onerror=\"this.src='https://placehold.co/30x30'\" :src=\"team.imagen\">\n                                    <div>{{team.medico}}</div>\n                                </div>\n                                \n                                <div class=\"d-flex\" style=\"gap:3px;\">\n                                  <button v-if=\"!['null',null,'undefined',undefined].includes(team.telefono_movil)\" class=\"btn btn-outline-success btn-sm px-1\" style=\"height:20px;font-size:0.7rem;line-height: 0.5;\"><i class=\"ml-1 fab fa-whatsapp text-success\"></i>{{team.telefono_movil}}\n                                  </button>\n                                  <button v-if=\"!['null',null,'undefined',undefined].includes(team.extension_telefonica)\" class=\"btn btn-outline-primary btn-sm px-1\" style=\"height:20px;font-size:0.7rem;line-height: 0.5;\"><i class=\"ml-1 fas fa-phone\"></i> {{team.extension_telefonica}}\n                                  </button>\n\n                                </div>\n                              </li>\n                            </template>\n                          </ul>\n                        </li>\n                        \n                      </template>\n                    </ul>\n                  </div>\n                  <div class=\"text-center \">Mostrando <b>{{showingTotal}}</b> resultados</div>\n                </div>\n                <div class=\"col-12 col-md-6 p-0 d-flex flex-column\">\n                  <div class=\"p-1\">\n                    <div \n                      class=\"text-dark\" \n                      style=\"    \n                        font-size: 1.2rem;\n                        font-weight: 500;\n                      \"\n                    >\n                      Equipo {{currentModalTeam?.name}}:\n                    </div>\n                    <ul v-if=\"currentModalTeam?.team_id==2\" class=\"nav nav-tabs justify-content-end\" id=\"myTab\" role=\"tablist\">\n                      <li class=\"nav-item\" role=\"presentation\">\n                        <button class=\"nav-link active\" id=\"assigns-tab\" data-toggle=\"tab\" data-target=\"#assigns\" type=\"button\" role=\"tab\" aria-controls=\"assigns\" aria-selected=\"true\">\n                          Solicitadas \n                          <span v-if=\"episode?.interconsultation_request.filter(x=>!x.fecha_confirmacion).length > 0\" :class=\"['badge badge-'+currentModalTeam?.color]\">\n                            {{episode?.interconsultation_request.filter(x=>!x.fecha_confirmacion).length}}\n                          </span>\n                        </button>\n                      </li>\n                    \n                      <li class=\"nav-item\" role=\"presentation\">\n                        <button class=\"nav-link\" id=\"answered-tab\" data-toggle=\"tab\" data-target=\"#answered\" type=\"button\" role=\"tab\" aria-controls=\"answered\" aria-selected=\"false\">\n                          Completadas \n                          <span v-if=\"episode?.interconsultation_request.filter(x=>x.fecha_confirmacion).length > 0\" :class=\"['badge badge-'+currentModalTeam?.color]\">{{episode?.interconsultation_request.filter(x=>x.fecha_confirmacion).length}}</span></button>\n                      </li>\n                    </ul>\n                  </div>\n                  <div class=\"tab-content flex-fill d-flex flex-column overflow-auto p-1\" id=\"myTabContent\">\n                    <div :class=\"['tab-pane fade show active',{'active':currentModalTeam?.team_id==2}]\" id=\"assigns\" role=\"tabpanel\" aria-labelledby=\"assigns-tab\">\n                      <div class=\"p-1\">\n                        <ul class=\"mt-1 list-group list-group-flush\">\n                          <template v-if=\"medicos_paciente?.length > 0\">\n                            <li \n                                 v-for=\"(team,index2) in medicos_paciente\"\n                              :key=\"index2\" \n                              style=\"font-weight: 500 !important;\"\n                              :class=\"['list-group-item d-flex align-items-center list-group-item-action p-1']\"\n                            >\n                              <div class=\"d-flex flex-column \">\n                                <div class=\"d-flex align-items-center\" style=\"gap:3px;\">\n                                    <img loading=\"lazy\" style=\"width:20px;height:20px;border-radius:20px;\" class=\"img-doctor mx-1\" onerror=\"this.src='https://placehold.co/30x30'\" :src=\"team.medico_img\">\n                                    <div>{{team.medico}}</div>\n                                </div>\n                                <div class=\"d-flex\" style=\"gap:3px;\">\n                                  <button v-if=\"team.telefono_movil\" class=\"btn btn-outline-success btn-sm px-1\" style=\"height:20px;font-size:0.7rem;line-height: 0.5;\">\n                                    <i class=\"ml-1 fab fa-whatsapp text-success\"></i>{{team.telefono_movil}}\n                                  </button>\n                                  <button v-if=\"team.extension_telefonica\" class=\"btn btn-outline-primary btn-sm px-1\" style=\"height:20px;font-size:0.7rem;line-height: 0.5;\"><i class=\"ml-1 fas fa-phone\"></i> {{team.extension_telefonica}}</button>\n\n                                </div>\n                              </div>\n                    \n                              <div class=\"ml-auto d-flex\" style=\"gap:3px;font-size:0.9rem;\">\n                                <div v-if=\"currentModalTeam?.team_id==2 && episode?.interconsultation_request.filter(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).length > 0\" class=\"flex-column text-right mr-1\" style=\"line-height: 1.2;\">\n                                    <div>Fecha y hora de solicitud</div>\n                                    <div>\n                                        <span :class=\"'text-secondary'\">\n                                            <i class=\"fas fa-calendar-alt\"></i>  {{episode?.interconsultation_request.find(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).fecha_solicitud}}\n                                        </span>\n                                    </div>\t\n                                </div>\n                                <div v-if=\"currentModalTeam?.team_id==2 && episode?.interconsultation_request.filter(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).length > 0\" class=\"flex-column text-right mr-1\" style=\"line-height: 1.2;\">\n                                    <div>Tiempo transcurrido</div>\n                                    <div>\n                                        <span :class=\"['text-'+episode?.interconsultation_request.find(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).color_tiempo_transcurrido]\">\n                                            <i class=\"fas fa-clock\"></i>  {{episode?.interconsultation_request.find(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).tiempo_transcurrido}}\n                                        </span>\n                                    </div>\t\n                                </div>\n                                \n                                <button \n                                  v-if=\"currentModalTeam?.team_id==2\"  \n                                  @click=\"\n                                  episode?.interconsultation_request.filter(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).length === 0\n                                  ? addInterconsultation(team)\n                                  : finishInterconsultation(team,  episode?.interconsultation_request.find(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).id)\n                                  \" \n                                  :class=\"[\n                                    'btn btn-sm',\n                                    episode?.interconsultation_request.filter(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).length > 0 ? 'btn-success': 'btn-info'\n                                  ]\"\n                                ><i :class=\"currentModalTeam?.team_id==2 && episode?.interconsultation_request.filter(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).length > 0 ? 'fas fa-hand-holding-medical': 'far fa-bell text-white'\"></i>\n                                </button>\n                                <button \n                                v-if=\"episode?.interconsultation_request.filter(x=>!x.fecha_confirmacion && x.doctor_id === team.user_id_medico).length === 0\"\n                                @click=\"deleteTeam(team)\" \n                                class=\"btn btn-danger btn-sm\"\n                                ><i class=\"fas fa-minus\"></i>\n                              </div>\n                              \n                            </li>\n                          </template>\n                          <template v-else>\n                            <li \n                              style=\"font-weight: 500 !important;\"\n                              :class=\"['list-group-item justify-content-center d-flex align-items-center list-group-item-action p-1']\"\n                            >\n                              No se encontr\xF3 {{currentModalTeam?.name}}.\n                            </li>\n                          </template>\n                        </ul>\n                    \n                      </div>\n                    </div>\n                    <div :class=\"['tab-pane fade',{'d-none':currentModalTeam?.team_id!==2}]\" id=\"answered\" role=\"tabpanel\" aria-labelledby=\"answered-tab\">\n                      <div class=\"p-1\">\n                        <ul class=\"mt-1 list-group list-group-flush\">\n                          <template v-if=\"episode?.interconsultation_request.filter(x=>x.fecha_confirmacion).length > 0\" >\n                            <li \n                              v-for=\"(team,index2) in episode?.interconsultation_request.filter(x=>x.fecha_confirmacion)\" \n                              :key=\"index2\" \n                              style=\"font-weight: 500 !important;\"\n                              :class=\"['list-group-item d-flex align-items-center list-group-item-action p-1']\"\n                            >\n                              <div class=\"d-flex flex-column \">\n                                <div class=\"d-flex align-items-center\" style=\"gap:3px;\">\n                                    <img loading=\"lazy\" style=\"width:20px;height:20px;border-radius:20px;\" class=\"img-doctor mx-1\" onerror=\"this.src='https://placehold.co/30x30'\" :src=\"team.imagen_medico\">\n                                    <div>{{team.medico}}</div>\n                                </div>\n                                <div class=\"d-flex\" style=\"gap:3px;\">\n                                  <button v-if=\"team.telefono_movil\" class=\"btn btn-outline-success btn-sm px-1\" style=\"height:20px;font-size:0.7rem;line-height: 0.5;\"><i class=\"ml-1 fab fa-whatsapp text-success\"></i>{{team.telefono_movil}}\n                                  </button>\n                                  <button v-if=\"team.extension_telefonica\" class=\"btn btn-outline-primary btn-sm px-1\" style=\"height:20px;font-size:0.7rem;line-height: 0.5;\"><i class=\"ml-1 fas fa-phone\"></i> {{team.extension_telefonica}}</button>\n\n                                </div>\n                              </div>\n                              <div class=\"ml-auto d-flex\" style=\"gap:3px;font-size:0.9rem;\">\n                                <div class=\"flex-column text-right pr-1 mr-1\" style=\"border-right: 1px solid #dee2e6 !important;line-height: 1.2;\">\n                                    <div>Fecha de solicitud</div>\n                                    <div>\n                                        <span :class=\"'text-secondary'\">\n                                            <i class=\"fas fa-calendar-alt\"></i>  {{team.fecha_solicitud}}\n                                        </span>\n                                    </div>\t\n                                </div>\n                                <div class=\"flex-column text-right pr-1 mr-1\" style=\"border-right: 1px solid #dee2e6 !important;line-height: 1.2;\">\n                                    <div>Fecha de respuesta</div>\n                                    <div>\n                                        <span :class=\"'text-secondary'\">\n                                            <i class=\"fas fa-calendar-alt\"></i>  {{team.fecha_confirmacion}}\n                                        </span>\n                                    </div>\t\n                                </div>\n                                <div class=\"  info-interconsulta-6915  flex-column text-right mr-1\" style=\"line-height: 1.2;\">\n                                    <div>Tiempo de respuesta</div>\t\n                                    <div>\n                                        <span :class=\"['text-'+team.color_tiempo_respuesta]\">\n                                            <i class=\"fas fa-clock\"></i>  {{team.tiempo_respuesta}}\n                                        </span>\n                                    </div>\t\n                                </div>\n                            \n                                <div v-if=\"!team.answered\" class=\"position-absolute bg-danger\" style=\"line-height: 1.2;line-height: 1.2;\n                                  right: 0;\n                                  top: 0px;\n                                  padding: 0px 5px;\n                                  color: white;\n                                  font-size: 0.7rem;\n                                  border-bottom-left-radius: 10px;\"\n                                >\n                                  <div>No acudi\xF3</div>\t\n                                </div>\n                              \n                              </div>\n                              \n                            </li>\n                          </template>\n                          <template v-else>\n                            <li \n                              style=\"font-weight: 500 !important;\"\n                              :class=\"['list-group-item justify-content-center d-flex align-items-center list-group-item-action p-1']\"\n                            >\n                              Sin interconsultas respondidas\n                            </li>\n                          </template>\n                        </ul>\n                      </div>\n\n                    </div> \n                  </div>\n                </div>\n              </div>\n              <div class=\"modal-footer\">\n                <button type=\"button\" :class=\"['btn btn-'+currentModalTeam?.color]\"  @click=\"handleCloseModal\" >Cerrar</button>\n              </div>\n            </div>\n          </div>\n        </div>\n      </div>\n      ",
    data: function data() {
      return {
        searchQuery: '',
        showingTotal: 0,
        episodios: pacientes_datos,
        episode_id: episodio_id,
        episode: null,
        assignedHealthTeam: [],
        currentModalTeam: null,
        healthTeam: [],
        resumeTeam: [{
          siglas: 'TR',
          name: 'Tratante',
          team_id: 1,
          color: 'success',
          system_group_id: [1, 2, 3, 4, 5, 6, 7, 8, 9]
        }, {
          siglas: 'IN',
          name: 'Interconsultante',
          team_id: 2,
          color: 'info',
          system_group_id: [1, 2, 3, 4, 5, 6, 7, 8, 9]
        }, {
          siglas: 'FE',
          name: 'Fellow',
          team_id: 3,
          color: 'orange',
          system_group_id: [3, 4]
        }, {
          siglas: 'RE',
          name: 'Residente',
          team_id: 4,
          color: 'secondary',
          system_group_id: [5, 6, 7, 8]
        }, {
          siglas: 'RA',
          name: 'RAMH',
          team_id: 5,
          color: 'purple',
          system_group_id: [9]
        }, {
          siglas: 'EN',
          name: 'Enfermería',
          team_id: 6,
          color: 'warning',
          system_group_id: [10]
        }]
      };
    },
    computed: {
      medicos_paciente: function medicos_paciente() {
        var _this$episode,
            _this = this;

        return (_this$episode = this.episode) === null || _this$episode === void 0 ? void 0 : _this$episode.medico_paciente.filter(function (x) {
          var _this$currentModalTea;

          return x.team_id === ((_this$currentModalTea = _this.currentModalTeam) === null || _this$currentModalTea === void 0 ? void 0 : _this$currentModalTea.team_id);
        });
      },
      filteredHealthTeam: function filteredHealthTeam() {
        var _this2 = this;

        var result = this.healthTeam.map(function (especiality) {
          var filteredHealthTeam = especiality.healthTeam.filter(function (team) {
            var _team$medico;

            return (_team$medico = team.medico) === null || _team$medico === void 0 ? void 0 : _team$medico.toLowerCase().includes(_this2.searchQuery.toLowerCase());
          });
          return _objectSpread(_objectSpread({}, especiality), {}, {
            healthTeam: filteredHealthTeam
          });
        }).filter(function (especiality) {
          return especiality.healthTeam.length > 0;
        });
        this.showingTotal = result.reduce(function (total, x) {
          return total + x.healthTeam.length;
        }, 0);
        return result;
      },
      getLastInterconsultation: function getLastInterconsultation() {
        var _this$episode2;

        if ((_this$episode2 = this.episode) !== null && _this$episode2 !== void 0 && _this$episode2.interconsultation_request) {
          var _this$episode$interco, _this$episode3, _this$episode3$interc;

          return (_this$episode$interco = ((_this$episode3 = this.episode) === null || _this$episode3 === void 0 ? void 0 : (_this$episode3$interc = _this$episode3.interconsultation_request) === null || _this$episode3$interc === void 0 ? void 0 : _this$episode3$interc.filter(function (x) {
            return !x.fecha_confirmacion;
          }))[0]) === null || _this$episode$interco === void 0 ? void 0 : _this$episode$interco.medico;
        }

        return '';
      }
    },
    methods: {
      handleCloseModal: function handleCloseModal() {
        $('#healthTeamModal').modal('hide');
      },
      have_active_interconsultation: function have_active_interconsultation(team) {
        var _this$episode4;

        return (_this$episode4 = this.episode) === null || _this$episode4 === void 0 ? void 0 : _this$episode4.interconsultation_request.map(function (x) {
          return x.doctor_id;
        }).includes(team.user_id);
      },
      is_assigned: function is_assigned(team) {
        var _this$episode5;

        return (_this$episode5 = this.episode) === null || _this$episode5 === void 0 ? void 0 : _this$episode5.medico_paciente.some(function (t) {
          return t.user_id_medico === team.user_id && t.team_id === team.team_id;
        });
      },
      finishInterconsultation: function finishInterconsultation(team, interconsulta_id) {
        var _this3 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2() {
          var that;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
            while (1) {
              switch (_context2.prev = _context2.next) {
                case 0:
                  that = _this3;
                  Swal.fire({
                    title: "¿Completar interconsulta?",
                    text: "\xBFEl ".concat(team.medico, " acudi\xF3 a la solicitud de interconsulta?"),
                    icon: "info",
                    showCancelButton: true,
                    confirmButtonColor: "#2fa600",
                    cancelButtonColor: "#d33",
                    confirmButtonText: "Si",
                    showDenyButton: true,
                    denyButtonText: "No",
                    denyButtonColor: "#3085d6",
                    cancelButtonText: "Cancelar"
                  }).then( /*#__PURE__*/function () {
                    var _ref = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(result) {
                      var formdata, _result, index, _formdata, _result2, _index;

                      return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
                        while (1) {
                          switch (_context.prev = _context.next) {
                            case 0:
                              if (!result.isConfirmed) {
                                _context.next = 12;
                                break;
                              }

                              formdata = new FormData();
                              formdata.append("user_id_medico", team.user_id_medico);
                              formdata.append("answered", 1);
                              formdata.append("episodio_id", _this3.episode_id);
                              formdata.append("_token", $("#_token").val());
                              _context.next = 8;
                              return Object(_helpers_helpers__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/solicitud_interconsulta/update/" + interconsulta_id, formdata);

                            case 8:
                              _result = _context.sent;
                              index = _this3.episodios.findIndex(function (x) {
                                return x['episodio'] === _this3.episode_id;
                              });
                              _this3.episodios[index].interconsultation_request = _result;
                              EventBus.$on('externalVarChanged', function (newValue) {
                                that.episodios = newValue;
                              });

                            case 12:
                              if (!result.isDenied) {
                                _context.next = 24;
                                break;
                              }

                              _formdata = new FormData();

                              _formdata.append("answered", 0);

                              _formdata.append("user_id_medico", team.user_id_medico);

                              _formdata.append("episodio_id", _this3.episode_id);

                              _formdata.append("_token", $("#_token").val());

                              _context.next = 20;
                              return Object(_helpers_helpers__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/solicitud_interconsulta/update/" + interconsulta_id, _formdata);

                            case 20:
                              _result2 = _context.sent;
                              _index = _this3.episodios.findIndex(function (x) {
                                return x['episodio'] === _this3.episode_id;
                              });
                              _this3.episodios[_index].interconsultation_request = _result2;
                              EventBus.$on('externalVarChanged', function (newValue) {
                                that.episodios = newValue;
                              });

                            case 24:
                            case "end":
                              return _context.stop();
                          }
                        }
                      }, _callee);
                    }));

                    return function (_x) {
                      return _ref.apply(this, arguments);
                    };
                  }());

                case 2:
                case "end":
                  return _context2.stop();
              }
            }
          }, _callee2);
        }))();
      },
      addInterconsultation: function addInterconsultation(team) {
        var _this4 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4() {
          var that;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
            while (1) {
              switch (_context4.prev = _context4.next) {
                case 0:
                  that = _this4;
                  Swal.fire({
                    title: "Solicitud de interconsulta",
                    text: "\xBFDesea solicitar una interconsulta con el ".concat(team.medico, "?"),
                    icon: "info",
                    showCancelButton: true,
                    confirmButtonColor: "#3085d6",
                    cancelButtonColor: "#d33",
                    confirmButtonText: "Aceptar",
                    cancelButtonText: "Cancelar"
                  }).then( /*#__PURE__*/function () {
                    var _ref2 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3(result) {
                      var interconsultation, formdata, _result3, index;

                      return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
                        while (1) {
                          switch (_context3.prev = _context3.next) {
                            case 0:
                              if (!result.isConfirmed) {
                                _context3.next = 18;
                                break;
                              }

                              interconsultation = that.episode.interconsultation_request.find(function (x) {
                                return x.doctor_id === team.user_id && !x.fecha_confirmacion;
                              });

                              if (!interconsultation) {
                                _context3.next = 5;
                                break;
                              }

                              alert("".concat(team.medico, " tiene una interconsulta por responder."));
                              return _context3.abrupt("return", false);

                            case 5:
                              document.querySelector('#loadingScreen').classList.remove('d-none');
                              formdata = new FormData();
                              formdata.append("user_id_medico", team.user_id_medico);
                              formdata.append("user_id", that.episode.user_id);
                              formdata.append("episodio_id", that.episode_id);
                              formdata.append("_token", $("#_token").val());
                              _context3.next = 13;
                              return Object(_helpers_helpers__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/solicitud_interconsulta/store", formdata);

                            case 13:
                              _result3 = _context3.sent;
                              index = that.episodios.findIndex(function (x) {
                                return x['episodio'] === that.episode_id;
                              });
                              that.episodios[index].interconsultation_request = _result3;
                              EventBus.$on('externalVarChanged', function (newValue) {
                                that.episodios = newValue;
                              });
                              document.querySelector('#loadingScreen').classList.add('d-none');

                            case 18:
                            case "end":
                              return _context3.stop();
                          }
                        }
                      }, _callee3);
                    }));

                    return function (_x2) {
                      return _ref2.apply(this, arguments);
                    };
                  }());

                case 2:
                case "end":
                  return _context4.stop();
              }
            }
          }, _callee4);
        }))();
      },
      addTeam: function addTeam(team) {
        var _this5 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5() {
          var formdata, result;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
            while (1) {
              switch (_context5.prev = _context5.next) {
                case 0:
                  if (!_this5.is_assigned(team)) {
                    _context5.next = 3;
                    break;
                  }

                  alert("".concat(team.medico, " ya est\xE1 asignado a al paciente."));
                  return _context5.abrupt("return", false);

                case 3:
                  document.querySelector('#loadingScreen').classList.remove('d-none');
                  formdata = new FormData();
                  formdata.append("user_id_medico", team.user_id);
                  formdata.append("episodio_id", _this5.episode_id);
                  formdata.append("user_id_paciente", _this5.episode.user_id);
                  formdata.append("position_id", team.team_id == 2 ? 2 : team.posicion_id);
                  formdata.append("_token", $("#_token").val());
                  formdata.append("created_at", Object(_helpers_helpers__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                  _context5.next = 13;
                  return Object(_helpers_helpers__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_medico_paciente/store", formdata);

                case 13:
                  result = _context5.sent;
                  result.map(function (x) {
                    if ([1].includes(x.cat_user_medico_posicion_id)) {
                      x.team_id = 1;
                    }

                    if ([2].includes(x.cat_user_medico_posicion_id)) {
                      x.team_id = 2;
                    }

                    if ([3, 4].includes(x.cat_user_medico_posicion_id)) {
                      x.team_id = 3;
                    }

                    if ([5, 6, 7, 8].includes(x.cat_user_medico_posicion_id)) {
                      x.team_id = 4;
                    }

                    if ([9].includes(x.cat_user_medico_posicion_id)) {
                      x.team_id = 5;
                    }

                    if ([10].includes(x.cat_user_medico_posicion_id)) {
                      x.team_id = 6;
                    }

                    return x;
                  });
                  _this5.episode.medico_paciente = result;
                  document.querySelector('#loadingScreen').classList.add('d-none'); ///$('#healthTeamModal').modal('hide')

                case 17:
                case "end":
                  return _context5.stop();
              }
            }
          }, _callee5);
        }))();
      },
      deleteTeam: function deleteTeam(team) {
        var _this6 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6() {
          var formdata;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
            while (1) {
              switch (_context6.prev = _context6.next) {
                case 0:
                  /* medicos_paciente() {
                       this.episode?.medico_paciente.filter(
                        (x) => x.team_id === this.currentModalTeam?.team_id
                      );
                    }, */
                  document.querySelector('#loadingScreen').classList.remove('d-none');
                  formdata = new FormData();
                  formdata.append('id', team.id);
                  formdata.append('_token', $('#_token').val());
                  formdata.append('created_at', Object(_helpers_helpers__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                  _context6.next = 7;
                  return Object(_helpers_helpers__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + '/user_cuest_medico_paciente/eliminarById', formdata);

                case 7:
                  _this6.episode.medico_paciente = _this6.episode.medico_paciente.filter(function (x) {
                    return x.id !== team.id;
                  });
                  document.querySelector('#loadingScreen').classList.add('d-none');

                case 9:
                case "end":
                  return _context6.stop();
              }
            }
          }, _callee6);
        }))();
      },
      assignTeamGroup: function assignTeamGroup(team_id) {
        var _this$episode6;

        (_this$episode6 = this.episode) === null || _this$episode6 === void 0 ? void 0 : _this$episode6.medico_paciente.map(function (x) {
          if ([1].includes(x.cat_user_medico_posicion_id)) {
            x.team_id = 1;
          }

          if ([2].includes(x.cat_user_medico_posicion_id)) {
            x.team_id = 2;
          }

          if ([3, 4].includes(x.cat_user_medico_posicion_id)) {
            x.team_id = 3;
          }

          if ([5, 6, 7, 8].includes(x.cat_user_medico_posicion_id)) {
            x.team_id = 4;
          }

          if ([9].includes(x.cat_user_medico_posicion_id)) {
            x.team_id = 5;
          }

          if ([10].includes(x.cat_user_medico_posicion_id)) {
            x.team_id = 6;
          }

          return x;
        });
        this.currentModalTeam = this.resumeTeam.find(function (x) {
          return x.team_id === team_id;
        });
        this.healthTeam = this.getHealthTeam(team_id);
        this.searchQuery = '';
      },
      getTeamList: function getTeamList(team_id) {
        //console.log('2', this.episode?.medico_paciente);
        this.assignTeamGroup(team_id);
        $('#healthTeamModal').modal('show');
      },
      getLastMedic: function getLastMedic(team_id) {
        var _this$episode7;

        if ((_this$episode7 = this.episode) !== null && _this$episode7 !== void 0 && _this$episode7.medico_paciente) {
          var _this$episode8, _this$episode8$medico, _this$episode9, _this$episode9$medico;

          return ((_this$episode8 = this.episode) === null || _this$episode8 === void 0 ? void 0 : (_this$episode8$medico = _this$episode8.medico_paciente) === null || _this$episode8$medico === void 0 ? void 0 : _this$episode8$medico.filter(function (x) {
            return x.team_id === team_id;
          }))[((_this$episode9 = this.episode) === null || _this$episode9 === void 0 ? void 0 : (_this$episode9$medico = _this$episode9.medico_paciente) === null || _this$episode9$medico === void 0 ? void 0 : _this$episode9$medico.filter(function (x) {
            return x.team_id === team_id;
          }).length) - 1];
        }

        return '';
      },
      getHealthTeam: function getHealthTeam(team_id) {
        if (team_id === 6) {
          return [{
            name: '',
            healthTeam: medicos_datos.filter(function (medic) {
              if (medic.posicion_id === 10) {
                return medic;
              }
            }).map(function (x) {
              x.team_id = team_id;
              return x;
            })
          }];
        }

        return Array.from(new Set(medicos_datos.map(function (x) {
          return x.especialidad;
        }))).map(function (especiality) {
          var team = [];

          if (team_id === 1) {
            team = [1, 2, 3, 4, 5, 6, 7, 8, 9];
          }

          if (team_id === 2) {
            team = [1, 2, 3, 4, 5, 6, 7, 8, 9];
          }

          if (team_id === 3) {
            team = [3, 4];
          }

          if (team_id === 4) {
            team = [5, 6, 7, 8];
          }

          if (team_id === 5) {
            team = [9];
          }

          return {
            name: especiality,
            healthTeam: medicos_datos.filter(function (medic) {
              return medic.especialidad === especiality && team.includes(medic.posicion_id);
            }).map(function (x) {
              x.team_id = team_id;
              return x;
            })
          };
        });
      }
    },
    mounted: function mounted() {
      var _this7 = this;

      this.episode = pacientes_datos.find(function (x) {
        return x.episodio_id === episodio_id;
      }); //console.log('1', this.episode);

      [1, 2, 3, 4, 5, 6].forEach(function (team_id) {
        _this7.assignTeamGroup(team_id);
      });
      $('[data-toggle="tooltip"]').tooltip();
    }
  });
};

/* harmony default export */ __webpack_exports__["default"] = (AppResumeHealthTeam);

/***/ }),

/***/ "./public/class_js/User.js":
/*!*********************************!*\
  !*** ./public/class_js/User.js ***!
  \*********************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return User; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserProfile_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./UserProfile.js */ "./public/class_js/UserProfile.js");
/* harmony import */ var _Component_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./Component.js */ "./public/class_js/Component.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }




var userProfile = new _UserProfile_js__WEBPACK_IMPORTED_MODULE_2__["default"]();
var message;

var User = /*#__PURE__*/function () {
  function User() {
    _classCallCheck(this, User);
  }

  _createClass(User, [{
    key: "create",
    value: function create(selector) {
      $(selector).html("\n            <div class=\"row\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg62 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"email\">Correo electr\xF3nico</label>\n                        <input type=\"email\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"email\" id=\"email\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg62 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"password\">Contrase\xF1a</label>\n                        <input type=\"password\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"password\" id=\"password\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId2\" class=\"form-text text-muted\"></small>\n                    </div>\n                </div>\n            </div>\n        ");
    }
  }, {
    key: "validate2",
    value: function validate2() {
      var nacionalidad = $("#nacionalidad");
      var cedula = $("#cedula");

      if (nacionalidad.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("input_select_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              return nacionalidad.trigger("focus");
            }, 110);
          }
        });
        return false;
      }

      if (cedula.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("input_text_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              return cedula.trigger("focus");
            }, 110);
          }
        });
        return false;
      }

      return true;
    }
  }, {
    key: "getByEmail",
    value: function getByEmail(email) {
      var formdata = new FormData();
      formdata.append("email", email);
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/getByEmail", formdata);
    }
  }, {
    key: "reingresoByCedula",
    value: function () {
      var _reingresoByCedula = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(cedula) {
        var model, tiempoTranscurrido, hoy, fn, paciente;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                if (!(location.pathname == "/medico/create_paciente")) {
                  _context.next = 13;
                  break;
                }

                console.log("cedula->", cedula);
                _context.next = 4;
                return _UserProfile_js__WEBPACK_IMPORTED_MODULE_2__["default"].getCedula(cedula);

              case 4:
                model = _context.sent;
                console.log("reingresoByCedula->", model);
                tiempoTranscurrido = Date.now();
                hoy = new Date(tiempoTranscurrido);
                model.fnacimiento = model.fnacimiento === null ? hoy.toISOString() : model.fnacimiento;
                fn = new Date(model.fnacimiento);
                paciente = model.nombres + " " + model.apellidos;
                pacientes_datos = [{
                  cedula: model.nacionalidad + model.cedula,
                  dias: 0,
                  edad: calcularEdad(fn.getFullYear() + '-' + fn.getMonth() + '-' + fn.getDate()),
                  imagen: model.genero !== null ? model.imagen : "",
                  imp_diagn: null,
                  ingreso: hoy.toISOString(),
                  medico_felow: null,
                  medico_interconsultante: null,
                  medico_ramh: null,
                  medico_residente: null,
                  medico_tratante: null,
                  paciente: paciente !== " " ? paciente : "No Indicado",
                  sexo: model.genero !== null ? model.genero.toUpperCase() : "M",
                  telefono_movil: model.telefono_movil !== null ? model.telefono_movil : "No Indicado",
                  ubicacion: "ALTA | Domicilio",
                  user_id: model.user_id
                }]; //UserCuestUbicacion.reingreso('.modal-body',model.user_id)

                UserCuestHistoria.index('.modal-body', model.user_id);

              case 13:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function reingresoByCedula(_x) {
        return _reingresoByCedula.apply(this, arguments);
      }

      return reingresoByCedula;
    }()
  }, {
    key: "reingresoByEmail",
    value: function () {
      var _reingresoByEmail = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2(cedula) {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
          while (1) {
            switch (_context2.prev = _context2.next) {
              case 0:
              case "end":
                return _context2.stop();
            }
          }
        }, _callee2);
      }));

      function reingresoByEmail(_x2) {
        return _reingresoByEmail.apply(this, arguments);
      }

      return reingresoByEmail;
    }()
  }], [{
    key: "create2",
    value: function create2(selector) {
      $(selector).html("\n            <div class=\"row\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"cedula\">Documento de identidad</label>\n                        <table class=\"w-100\">\n                            <tr>\n                                <td style=\"width: 30%\">\n                                    <select class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"nacionalidad\" id=\"nacionalidad\">\n                                        <option value=\"V\">V</option>\n                                        <option value=\"E\">E</option>\n                                        <option value=\"P\">P</option>\n                                        <option value=\"J\">J</option>\n                                    </select>\n                                </td>\n                                <td>\n                                    <input type=\"text\" max=\"9\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"cedula\" id=\"cedula\" aria-describedby=\"helpId\" placeholder=\"\">\n                                </td>\n                            </tr>\n                        </table>\n                        <small id=\"sms_cedula\" class=\"form-text text-danger notification\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div id=\"correoAdulto\" class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"email\">Correo electr\xF3nico</label>\n                        <input type=\"email\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"email\" id=\"email\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"sms_email\" class=\"form-text text-danger notification\"></small>\n                    </div>\n                    <div id=\"emailAlternativo\" class=\"form-group d-none\">\n                        <label class=\"label-text text-secondary\" for=\"email_alternativo\">Correo electr\xF3nico <span class=\"font-weight-bold\">(del representante)</span></label>\n                        <input type=\"email\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"email_alternativo\" id=\"email_alternativo\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"sms_email_alternativo\" class=\"form-text text-danger notification\"></small>\n                    </div>\n                </div>\n            </div>\n        ");
      var temp_cedula = "";
      var temp_email = "";

      function validarNumeroConDosDecimales(inputValue) {
        var regex = /^\d+\.\d{2}$/;
        return regex.test(inputValue);
      }

      $("#cedula").on("change", function () {
        var nacionalidad = $("#nacionalidad");

        if (nacionalidad.val() != "") {
          //console.log(response)
          var email = $("#email");

          var _nacionalidad = $("#nacionalidad");

          var cedula = $("#cedula");
          temp_cedula = cedula.val();
          userProfile.validateCedula().then(function (response) {
            if (response.exist) {
              if (response.episodesOpen.length === undefined) {
                document.querySelector("#content_1").style.visibility = 'hidden';
                Swal.fire({
                  title: "¡Episodio activo!",
                  html: "\n                                            La c\xE9dula <b>".concat(cedula.val(), "</b> est\xE1 asociada al paciente:\n                                            <table class=\"table table-bordered\">\n                                                <tr>\n                                                    <th>Nombres:</th>\n                                                    <th>Apellidos:</th>\n                                                    <th>Ubicaci\xF3n actual:</th>\n                                                </tr>\n                                                <tr>\n                                                    <td>").concat(response.user.profile.nombres, "</td>\n                                                    <td>").concat(response.user.profile.apellidos, "</td>\n                                                    <td>").concat(response.episodesOpen.ubicacion_description, "</td>\n                                                </tr>\n                                            </table>\n                                            Para reingresarlo, deber\xE1 crearle el alta a su episodio actual. Si desea reubicarlo de area, pulse en la opci\xF3n <b>traslado</b> desde su ubicaci\xF3n actual.\n                                        "),
                  allowOutsideClick: false,
                  icon: "warning",
                  showCancelButton: false,
                  confirmButtonColor: "#3085d6",
                  //cancelButtonColor: "#d33",
                  confirmButtonText: "De acuerdo!" //cancelButtonText: "No!"

                }).then(function (result) {
                  if (result.isConfirmed) {
                    document.querySelector("#content_1").style.visibility = 'visible';
                    cedula.val('');
                    cedula.trigger("focus");
                  }
                });
                return false;
              }

              $("#sms_cedula").html("<i class='fas fa-exclamation-triangle'></i> La cédula está registrada"); //console.log("2 ",location.pathname !== "/paciente/create")

              if (location.pathname !== "/medico/create") {
                if (location.pathname === "/paciente/create" || //location.pathname === "/medico/create" ||
                location.pathname === "/medico") {
                  message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("cedula_asignada2");
                  Swal.fire(message['title'], _nacionalidad.val() + cedula.val() + message['message'], message['image']).then(function () {
                    setTimeout(function () {
                      if (location.pathname !== "/medico") {
                        cedula.val('');
                      } else {
                        cedula.val(document.getElementById('cedula').dataset.cedula);
                      }

                      cedula.trigger("focus");
                    }, 110);
                  });
                } else {
                  Swal.fire({
                    title: "Cédula registrada!",
                    html: "\n                                                La c\xE9dula <b>".concat(cedula.val(), "</b> est\xE1 asociada al paciente:\n                                                <table class=\"table table-bordered\">\n                                                    <tr>\n                                                        <th>Nombres:</th>\n                                                        <th>Apellidos:</th>\n                                                        <th>Ubicaci\xF3n actual:</th>\n                                                    </tr>\n                                                    <tr>\n                                                        <td>").concat(response.user.profile.nombres, "</td>\n                                                        <td>").concat(response.user.profile.apellidos, "</td>\n                                                        <td>").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(response.episodesOpen.ubicacion_description) ? '' : response.episodesOpen.ubicacion_description, "</td>\n                                                    </tr>\n                                                </table>\n                                                <h4>\xBFQuieres reingresarlo? </h4>\n                                                Esta acci\xF3n crear\xE1 un nuevo episodio.\n                                            "),
                    allowOutsideClick: false,
                    icon: "warning",
                    showCancelButton: true,
                    confirmButtonColor: "#3085d6",
                    cancelButtonColor: "#d33",
                    confirmButtonText: "De acuerdo!",
                    cancelButtonText: "No!"
                  }).then(function (result) {
                    if (result.isConfirmed) {
                      User.reingresoByCedula(temp_cedula);
                    } else {
                      setTimeout(function () {
                        cedula.val('');
                        cedula.trigger("focus");
                      }, 110);
                    }
                  });
                }
              }
            } else {
              //$("#sms_cedula").empty();
              $("#sms_cedula").html("<i class='fas fa-check-circle text-success'></i><span class='text-success'> Cédula no registrada</span>");
            }
          });
          $("#sms_email").empty();
        } else {
          message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("input_select_empty");
          Swal.fire({
            icon: message['image'],
            title: message['title'],
            text: message['message'],
            didClose: function didClose() {
              setTimeout(function () {
                nacionalidad.trigger("focus");
              }, 110);
            }
          }); //alert(message['message'])
          //nacionalidad.trigger("focus")

          $(this).val('');
          return false;
        }
      });
      $("#cedula").on("keyup", function (e) {
        //console.log(e.currentTarget.value)
        var numeroValido = validarNumeroConDosDecimales(e.currentTarget.value);

        if (numeroValido) {
          document.querySelector('#email').value = "".concat(e.currentTarget.value, "@correotemporal.com");
          document.querySelector('#correoAdulto').classList.add("d-none");
          document.querySelector('#emailAlternativo').classList.remove("d-none");
        } else {
          document.querySelector('#email').value = "";
          document.querySelector('#correoAdulto').classList.remove("d-none");
          document.querySelector('#emailAlternativo').classList.add("d-none");
        }
      });
      var that = this;
      $("#email").on("change", function () {
        that.validateEmail().then(function (response) {
          //console.log(response)
          var email = $("#email");

          if (location.pathname !== "/medico/create") {
            if (response == "email_existe") {
              var _temp_email = email.val();

              $("#sms_email").html("<i class='fas fa-exclamation-triangle'></i> " + response);
              message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("email_registrado");
              Swal.fire({
                icon: message['image'],
                title: message['title'],
                text: email.val() + ". " + message['message'],
                showDenyButton: false,
                showCancelButton: false,
                confirmButtonText: "Entendido",
                didClose: function didClose() {
                  setTimeout(function () {
                    if (location.pathname !== "/medico") {
                      email.val('');
                    } else {
                      email.val(document.getElementById('email').dataset.email);
                    }

                    email.trigger("focus");
                  }, 110);
                }
              }).then(function (result) {
                if (result.isConfirmed) {
                  var cedula = $("#cedula");
                  setTimeout(function () {
                    email.val('');
                    email.trigger("focus");
                  }, 110);
                }
              });
            } else {
              //$("#sms_cedula").empty();
              $("#sms_email").html("<i class='fas fa-check-circle text-success'></i> ");
            }
          }
        });
      });
    }
  }, {
    key: "store",
    value: function () {
      var _store = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3() {
        var email, email_alternativo, cedula, nacionalidad, password, formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
          while (1) {
            switch (_context3.prev = _context3.next) {
              case 0:
                email = $("#email");
                email_alternativo = $("#email_alternativo");
                cedula = $("#cedula");
                nacionalidad = $("#nacionalidad");
                password = $("#password");
                formdata = new FormData();
                formdata.append("email", email.val());
                formdata.append("email_alternativo", email_alternativo.val());
                formdata.append("cedula", cedula.val());
                formdata.append("nacionalidad", nacionalidad.val());
                formdata.append("password", password.val() != undefined ? password.val() : 123456);
                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                formdata.append("_token", $("#_token").val());
                _context3.next = 15;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/store", formdata);

              case 15:
                return _context3.abrupt("return", _context3.sent);

              case 16:
              case "end":
                return _context3.stop();
            }
          }
        }, _callee3);
      }));

      function store() {
        return _store.apply(this, arguments);
      }

      return store;
    }()
  }, {
    key: "update",
    value: function () {
      var _update = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4() {
        var email, email_alternativo, cedula, formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
          while (1) {
            switch (_context4.prev = _context4.next) {
              case 0:
                email = $("#email");
                email_alternativo = $("#email_alternativo");
                cedula = $("#user_id");
                formdata = new FormData();
                formdata.append("email", email.val());
                formdata.append("email_alternativo", email_alternativo.val());
                formdata.append("user_id", document.getElementById('email').dataset.user_id);
                formdata.append("updated_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                formdata.append("_token", $("#_token").val());
                _context4.next = 11;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/update", formdata);

              case 11:
                return _context4.abrupt("return", _context4.sent);

              case 12:
              case "end":
                return _context4.stop();
            }
          }
        }, _callee4);
      }));

      function update() {
        return _update.apply(this, arguments);
      }

      return update;
    }()
  }, {
    key: "validate",
    value: function validate() {
      var nacionalidad = $("#nacionalidad");
      var cedula = $("#cedula");
      var email = $("#email");

      if (nacionalidad.val() == "" && cedula.val() == "" && email.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("user_no_valid"); //alert(message['message'])
        //nacionalidad.trigger("focus")

        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              return nacionalidad.trigger("focus");
            }, 110);
          }
        });
        return false;
      }

      if (nacionalidad.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("input_select_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              return nacionalidad.trigger("focus");
            }, 110);
          }
        });
        return false;
      }

      if (cedula.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("input_text_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              return cedula.trigger("focus");
            }, 110);
          }
        });
        return false;
      }

      if (email.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_3__["default"].alertMessage("input_text_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              return email.trigger("focus");
            }, 110);
          }
        });
        return false;
      }

      return true;
    }
  }, {
    key: "validateEmail",
    value: function () {
      var _validateEmail = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5() {
        var nacionalidad, cedula, email, formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
          while (1) {
            switch (_context5.prev = _context5.next) {
              case 0:
                nacionalidad = $("#nacionalidad");
                cedula = $("#cedula");
                email = $("#email");
                formdata = new FormData();
                formdata.append("nacionalidad", nacionalidad.val());
                formdata.append("cedula", cedula.val());
                formdata.append("email", email.val());
                formdata.append("_token", $("#_token").val());
                _context5.next = 10;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/emailExist", formdata);

              case 10:
                return _context5.abrupt("return", _context5.sent);

              case 11:
              case "end":
                return _context5.stop();
            }
          }
        }, _callee5);
      }));

      function validateEmail() {
        return _validateEmail.apply(this, arguments);
      }

      return validateEmail;
    }()
  }, {
    key: "show",
    value: function () {
      var _show = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6(user_id) {
        var formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
          while (1) {
            switch (_context6.prev = _context6.next) {
              case 0:
                formdata = new FormData();
                formdata.append("user_id", user_id);
                formdata.append("_token", $("#_token").val());
                _context6.next = 5;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/show", formdata);

              case 5:
                return _context6.abrupt("return", _context6.sent);

              case 6:
              case "end":
                return _context6.stop();
            }
          }
        }, _callee6);
      }));

      function show(_x3) {
        return _show.apply(this, arguments);
      }

      return show;
    }()
  }]);

  return User;
}();



/***/ }),

/***/ "./public/class_js/UserCuestAdministracion.js":
/*!****************************************************!*\
  !*** ./public/class_js/UserCuestAdministracion.js ***!
  \****************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return AppAdministrativeData; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"]();

var UserCuestAdministracion = /*#__PURE__*/function () {
  function UserCuestAdministracion() {
    _classCallCheck(this, UserCuestAdministracion);
  }

  _createClass(UserCuestAdministracion, null, [{
    key: "handleStoreTipoPaciente",
    value:
    /* static handleEmergenciaPaciente(tipo_emergencia,episodio_id){
          let datos_episodio = pacientes_datos.find(x=>x['episodio'] === episodio_id)
            console.log(tipo_emergencia,datos_episodio)
          let input_tipo_emergencia = document.querySelector('#input_tipo_emergencia')
            input_tipo_emergencia.value = tipo_emergencia
            let options_ambulatoria = document.querySelector("#options_ambulatoria")
            options_ambulatoria.classList.replace("d-flex","d-none")
              if (input_tipo_emergencia.value === "Ambulatoria") {
                options_ambulatoria.classList.replace("d-none","d-flex")
            }
            if (input_tipo_emergencia.value === "Convencional") {
                UserCuestAdministracion.handleServicioEmergencia(
                    {
                        'tipo_emergencia':input_tipo_emergencia.value,
                        'description':'Convencional',
                        'value':6,
                        'episodio_id':datos_episodio.episodio_id,
                        'color':'gray',
                        'user_id_paciente':datos_episodio.user_id
                    }
                )
            }
    } */

    /* static handleServicioEmergencia(datos){
          //console.log(datos)
        Swal.fire({
            icon: "warning",
            title: `Servicio Emergencia ${datos.description}?`,
            text:"Haz clic abajo para activar o desactivar este servicio.",
            showDenyButton: true,
            showCancelButton: true,
            confirmButtonText: `Activar`,
            cancelButtonText: `No hacer nada`,
            denyButtonText: `Desactivar`,
          }).then( async (result) => {
            if (result.isConfirmed) {
                  let formdata = new FormData();
                    formdata.append("tipo_emergencia", datos.tipo_emergencia)
                    formdata.append("user_id_paciente", datos.user_id_paciente)
                    formdata.append("episodio_id", datos.episodio_id)
                    formdata.append("value",datos.value)
                    formdata.append("description",datos.description)
                    formdata.append("_token", $("#_token").val())
                    formdata.append("created_at", timestamps())
                    let result = await post( location.origin+"/user/servicios/emergencia/store", formdata)
                  let index = pacientes_datos.findIndex(x=>x['episodio'] === datos.episodio_id)
                      pacientes_datos[index].servicio_emergencia = result
                    UserCuestAdministracion.index(datos.episodio_id)
                    let badge = document.querySelector("#badge_walkin_"+datos.user_id_paciente)
                        badge.classList.remove("d-none","badge-info","badge-success","badge-primary","badge-gray","badge-secondary","text-white","text-secondary")
                        badge.classList.add("badge-"+result.color)
                        if ( [1,2,3,4,5].includes(Number(result['value']) ) ) {
                            badge.classList.add("text-white")
                        }else{
                            badge.classList.add("text-secondary")
                        }
                          badge.innerHTML = `<i class="pc-servicio_walkin text-white "></i> ${result.description}`
                        Swal.fire({
                            icon: "success",
                            title: `Servicio Emergencia ${datos.description} activado`,
                            showDenyButton: false,
                            showCancelButton: false,
                            confirmButtonText: `Ok!`,
                        })
              }
            if (result.isDenied) {
                let formdata = new FormData();
                    formdata.append("episodio_id", datos.episodio_id)
                    formdata.append("_token", $("#_token").val())
                let result = await post( location.origin+"/user/servicios/emergencia/destroy", formdata)
                  //let user = pacientes_datos.find(x=>x['episodio'] === datos.episodio_id)
                let index = pacientes_datos.findIndex(x=>x['episodio'] === datos.episodio_id)
                    pacientes_datos[index].servicio_emergencia = result
                    UserCuestAdministracion.index(datos.episodio_id)
                let badge = document.querySelector("#badge_walkin_"+datos.user_id_paciente)
                    badge.classList.remove("d-none","badge-info","badge-success","badge-primary","badge-gray","badge-secondary","text-white","text-secondary")
                    badge.classList.add("d-none")
                    badge.innerHTML = ``
                    Swal.fire({
                        icon: "success",
                        title: `Servicio Emergencia ${datos.description} desactivado`,
                        showDenyButton: false,
                        showCancelButton: false,
                        confirmButtonText: `Ok!`,
                    })
              }
          })
    } */

    /* static handletipoPaciente(tipo_paciente){
            //SELECTOR INPUT OCULTO TIPO PACIENTE
        let input_tipo_paciente = document.querySelector('#tipo_paciente')
              //SELECTORES OPTIONS
        let options_asegurado = document.querySelector('#options_asegurado')
        let options_empresarial = document.querySelector('#options_empresarial')
        let options_cortesia = document.querySelector('#options_cortesia')
        let options_empleado = document.querySelector('#options_empleado')
        let options_otro = document.querySelector('#options_otro')
              //OCULTAR TODAS LAS CAJAS OPTIONS
            options_asegurado.classList.replace("d-block","d-none")
            options_empresarial.classList.replace("d-block","d-none")
            options_cortesia.classList.replace("d-block","d-none")
            options_empleado.classList.replace("d-block","d-none")
            options_otro.classList.replace("d-block","d-none")
            console.log(tipo_paciente);
            //MOSTRAMOS LAS OPCIONES PARA EL TIPO DE PACIENTE ACTIVO
            if (tipo_paciente === "Asegurado") {
                options_asegurado.classList.replace("d-none","d-block")
            }
            if (tipo_paciente === "Empresarial") {
                options_empresarial.classList.replace("d-none","d-block")
            }
            if (tipo_paciente === "Cortesía") {
                options_cortesia.classList.replace("d-none","d-block")
            }
            if (tipo_paciente === "Empleado") {
                options_empleado.classList.replace("d-none","d-block")
            }
            if (tipo_paciente === "Otro") {
                options_otro.classList.replace("d-none","d-block")
            }
            input_tipo_paciente.value = tipo_paciente
      } */
    function handleStoreTipoPaciente(episodio_id) {
      var episodio_paciente = pacientes_datos.find(function (x) {
        return x['episodio'] === episodio_id;
      });
      var tempFormData = {
        'tipo_paciente': 'Particular',
        'cat_aseguradora_id': null,
        'n_poliza': null,
        'cat_empresa_id': null,
        'autorizado_por_id': null,
        'directorio_organizacional_id': null,
        'observaciones': null
      }; //SELECTOR INPUT OCULTO TIPO PACIENTE

      var input_tipo_paciente = document.querySelector('#tipo_paciente'); //SELECTOR INPUT ASEGURADORA

      var cat_aseguradora_id = document.querySelector('#cat_aseguradora_id'); //SELECTOR INPUT NUMERO POLIZA

      var n_poliza = document.querySelector('#n_poliza'); //SELECTOR INPUT INSTITUCION / EMPRESA / FUNDACION

      var cat_empresa_id = document.querySelector('#cat_empresa_id'); //SELECTOR INPUT AUTORIZAZADO POR

      var autorizado_por_id = document.querySelector('#autorizado_por_id'); //SELECTOR INPUT PERTENECE A

      var directorio_organizacional_id = document.querySelector('#directorio_organizacional_id'); //SELECTOR INPUT OBSERVACIONES

      var observaciones = document.querySelector('#observaciones'); //SELECTOR INPUT PACIENTE CORTESIA

      var paciente_cortesia = document.querySelector('#paciente_cortesia'); //ASIGNAR VALOR TIPO PACIENTE AL OBJETO

      tempFormData.tipo_paciente = input_tipo_paciente.value; //console.log('tempFormData',tempFormData)
      //SI TIPO PACIENTE ES ASEGURADO VERIFICAR SI SE HA SELECCIONADO EL SEGURO Y LA POLIZA

      if (tempFormData.tipo_paciente === "Asegurado") {
        if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(cat_aseguradora_id.value)) {
          Swal.fire({
            icon: "warning",
            title: "Debe seleccionar una Aseguradora",
            showDenyButton: false,
            showCancelButton: false,
            confirmButtonText: "Ok!",
            didClose: function didClose() {
              setTimeout(function () {
                cat_aseguradora_id.focus();
              }, 110);
            }
          });
          return false;
        } else {
          tempFormData.cat_aseguradora_id = cat_aseguradora_id.value;
        }

        if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(n_poliza.value)) {
          /* Swal.fire({
              icon: "warning",
              title: `Debe escribir un número de poliza`,
              showDenyButton: false,
              showCancelButton: false,
              confirmButtonText: `Ok!`,
              didClose: () => {
                  setTimeout(() => { n_poliza.focus();}, 110);
              }
          }) */
          tempFormData.n_poliza = "";
          return false;
        } else {
          tempFormData.n_poliza = n_poliza.value;
        }
      } //SI TIPO PACIENTE ES EMPRESARIAL VERIFICAR SI SE HA SELECCIONADO LA INSTITUCION/EMPRESA/FUNDACION


      if (tempFormData.tipo_paciente === "Empresarial") {
        if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(cat_empresa_id.value)) {
          Swal.fire({
            icon: "warning",
            title: "Debe seleccionar una Instituci\xF3n / Empresa / Fundaci\xF3n",
            showDenyButton: false,
            showCancelButton: false,
            confirmButtonText: "Ok!",
            didClose: function didClose() {
              setTimeout(function () {
                cat_empresa_id.focus();
              }, 110);
            }
          });
          return false;
        } else {
          tempFormData.cat_empresa_id = cat_empresa_id.value;
        }
      } //SI TIPO PACIENTE ES CORTESIA VERIFICAR SI SE HA SELECCIONADO QUIEN LA AUTORIZA


      if (tempFormData.tipo_paciente === "Cortesía") {
        if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(autorizado_por_id.value)) {
          Swal.fire({
            icon: "warning",
            title: "Debe seleccionar quien autoriza la cortes\xEDa",
            showDenyButton: false,
            showCancelButton: false,
            confirmButtonText: "Ok!",
            didClose: function didClose() {
              setTimeout(function () {
                autorizado_por_id.focus();
              }, 110);
            }
          });
          return false;
        } else {
          tempFormData.autorizado_por_id = autorizado_por_id.value;
        }
      } //SI TIPO PACIENTE ES EMPLEADO VERIFICAR SI SE HA SELECCIONADO EL AREA A LA QUE PERTENECE


      if (tempFormData.tipo_paciente === "Empleado") {
        if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(directorio_organizacional_id.value)) {
          Swal.fire({
            icon: "warning",
            title: "Debe seleccionar a qu\xE9 area pertenece el empleado",
            showDenyButton: false,
            showCancelButton: false,
            confirmButtonText: "Ok!",
            didClose: function didClose() {
              setTimeout(function () {
                directorio_organizacional_id.focus();
              }, 110);
            }
          });
          return false;
        } else {
          tempFormData.directorio_organizacional_id = directorio_organizacional_id.value;
        }
      } //SI TIPO PACIENTE ES OTRO VERIFICAR SI SE HA DESCRITO QUE OTRO TIPO DE PACIENTE ES


      if (tempFormData.tipo_paciente === "Otro") {
        if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(observaciones.value)) {
          Swal.fire({
            icon: "warning",
            title: "Debe describir qu\xE9 otro tipo de paciente es.",
            showDenyButton: false,
            showCancelButton: false,
            confirmButtonText: "Ok!",
            didClose: function didClose() {
              setTimeout(function () {
                observaciones.focus();
              }, 110);
            }
          });
          return false;
        } else {
          tempFormData.observaciones = observaciones.value;
        }
      }

      Swal.fire({
        icon: "warning",
        title: "\xBFQuieres guardar los datos?",
        showDenyButton: false,
        showCancelButton: true,
        confirmButtonText: "Si",
        cancelButtonText: "No, a\xFAn no"
        /* denyButtonText: `Desactivar`, */

      }).then( /*#__PURE__*/function () {
        var _ref = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(result) {
          var formdata, key, _result, index;

          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
            while (1) {
              switch (_context.prev = _context.next) {
                case 0:
                  if (!result.isConfirmed) {
                    _context.next = 14;
                    break;
                  }

                  formdata = new FormData();

                  for (key in tempFormData) {
                    formdata.append(key, tempFormData[key]);
                  }

                  formdata.append("user_id_paciente", episodio_paciente.user_id);
                  formdata.append("episodio_id", episodio_paciente.episodio_id);
                  formdata.append("_token", $("#_token").val());
                  formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                  _context.next = 9;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/tipopaciente/store", formdata);

                case 9:
                  _result = _context.sent;
                  index = pacientes_datos.findIndex(function (x) {
                    return x['episodio'] === episodio_paciente.episodio_id;
                  });
                  pacientes_datos[index].user_tipo_paciente = _result;
                  UserCuestAdministracion.index(episodio_paciente.episodio_id);
                  Swal.fire({
                    icon: "success",
                    title: "Datos guardados correctamente",
                    showDenyButton: false,
                    showCancelButton: false,
                    confirmButtonText: "Ok!"
                  });

                case 14:
                case "end":
                  return _context.stop();
              }
            }
          }, _callee);
        }));

        return function (_x) {
          return _ref.apply(this, arguments);
        };
      }());
    }
  }, {
    key: "index",
    value: function index(episodio_id) {
      var episodio = pacientes_datos.find(function (x) {
        return x['episodio'] === episodio_id;
      });
      console.log(episodio);

      var formTipoPaciente = function formTipoPaciente() {
        return (
          /*html */
          "\n                    <h3 class=\"ml-2 text-center text-secondary\">Pulsa en el Tipo de Paciente</h3>\n                    <div id=\"menu_tipoPaciente\" class=\"d-flex flex-wrap bg-light m-1\">\n                        <button\n                            onClick=\"UserCuestAdministracion.handletipoPaciente('Particular')\"\n                            class=\"".concat(episodio.user_tipo_paciente.tipo_paciente === 'Particular' ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\">\n                            <i class=\"fas fa-user-injured h3\"></i>\n                            <b class=\"flex-fill mb-2 title font-italic\">\n                                Particular\n                            </b>\n                        </button>\n                        <button\n                            onClick=\"UserCuestAdministracion.handletipoPaciente('Asegurado')\"\n                            class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Asegurado' ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\">\n                            <i class=\"fas fa-user-injured h3\"></i>\n                            <b class=\"flex-fill mb-2 title font-italic\">\n                                Asegurado\n                            </b>\n                        </button>\n                        <button\n                            onClick=\"UserCuestAdministracion.handletipoPaciente('Empresarial')\"\n                            class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Empresarial' ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\">\n                            <i class=\"fas fa-user-injured h3\"></i>\n                            <b class=\"flex-fill mb-2 title font-italic\">\n                                Empresarial\n                            </b>\n                        </button>\n                        <button\n                            onClick=\"UserCuestAdministracion.handletipoPaciente('Cortes\xEDa')\"\n                            class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Cortesía' ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\">\n                            <i class=\"fas fa-user-injured h3\"></i>\n                            <b class=\"flex-fill mb-2 title font-italic\">\n                                Cortes\xEDa\n                            </b>\n                        </button>\n                        <button\n                            onClick=\"UserCuestAdministracion.handletipoPaciente('Empleado')\"\n                            class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Empleado' ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\">\n                            <i class=\"fas fa-user-injured h3\"></i>\n                            <b class=\"flex-fill mb-2 title font-italic\">\n                                Empleado\n                            </b>\n                        </button>\n                        <button\n                            onClick=\"UserCuestAdministracion.handletipoPaciente('Otro')\"\n                            class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Otro' ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\">\n                            <i class=\"fas fa-user-injured h3\"></i>\n                            <b class=\"flex-fill mb-2 title font-italic\">\n                                Otro\n                            </b>\n                        </button>\n                    </div>\n                    <div class=\"container-fluid\">\n                        <div class=\"row\">\n                            <div id=\"options_asegurado\" class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Asegurado' ? 'd-block' : 'd-none', " col-12\">\n                                <div class=\"row\">\n                                    <div class=\"col-12 col-sm-6\">\n                                        <label>Aseguradora</label>\n                                        <select class=\"form-control bg-gray-footer-parodi\" name=\"cat_aseguradora_id\" id=\"cat_aseguradora_id\">\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === null ? 'selected' : '', " value=\"\">Seleccione</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 1 ? 'Selected' : '', " value=\"1\">Vumi Group</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 2 ? 'Selected' : '', " value=\"2\">Best Doctors Insurance Ho</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 3 ? 'Selected' : '', " value=\"3\">Mercantil</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 4 ? 'Selected' : '', " value=\"4\">Humanitas</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 5 ? 'Selected' : '', " value=\"5\">Seguros Caracas De Libert</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 6 ? 'Selected' : '', " value=\"6\">Usa Medical Services Vene</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 7 ? 'Selected' : '', " value=\"7\">Banesco</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 8 ? 'Selected' : '', " value=\"8\">Beneficios Medicos Intern</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 9 ? 'Selected' : '', " value=\"9\">Makler Administradora C.A</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 10 ? 'Selected' : '', " value=\"10\">Administradora De Riesgos Parsalud</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 11 ? 'Selected' : '', " value=\"11\">Euro-Center Ltda</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 12 ? 'Selected' : '', " value=\"12\">Ever Insurance</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 13 ? 'Selected' : '', " value=\"13\">Euro-Center Ltda</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 14 ? 'Selected' : '', " value=\"14\">La Internacional De Seguros,S.A</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 15 ? 'Selected' : '', " value=\"15\">Loyal</option>\n                                        <option ").concat(episodio.user_tipo_paciente.cat_aseguradora_id === 16 ? 'Selected' : '', " value=\"16\">Mafre la seguridad, ca (solo POLAR)</option>\n\n\n                                            \n                                        </select>\n                                    </div>\n                                    <div class=\"col-12 col-sm-6\">\n                                        <label>N\xB0 de poliza</label>\n                                        <input type=\"text\" value=\"").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(episodio.user_tipo_paciente.value2) ? '' : episodio.user_tipo_paciente.value2, "\" class=\"form-control bg-gray-footer-parodi\" id=\"n_poliza\">\n                                    </div>\n                                </div>\n                            </div>\n                            <div id=\"options_empresarial\" class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Empresarial' ? 'd-block' : 'd-none', " col-12 col-sm-4\">\n                                <label>Instituci\xF3n / Empresa / Fundaci\xF3n</label>\n                                <select class=\"form-control bg-gray-footer-parodi\" name=\"cat_empresa_id\" id=\"cat_empresa_id\">\n                                    <option ").concat(episodio.user_tipo_paciente.cat_empresa_id === null ? 'selected' : '', "  value=\"\">Seleccione</option>\n                                    <option ").concat(episodio.user_tipo_paciente.cat_empresa_id === "1" ? 'selected' : '', "  value=\"1\">Empresas Polar</option>\n                                    <option ").concat(episodio.user_tipo_paciente.cat_empresa_id === "2" ? 'selected' : '', "  value=\"2\">Farmatodo</option>\n                                    <option ").concat(episodio.user_tipo_paciente.cat_empresa_id === "3" ? 'selected' : '', "  value=\"3\">Excelsior\xA0Gama</option>\n                                </select>\n                            </div>\n\n                            <div id=\"options_cortesia\" class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Cortesía' ? 'd-block' : 'd-none', " col-12 col-sm-4\">\n                                <label>Autorizado por:</label>\n                                <input value=\"").concat([null, 'null'].includes(episodio.user_tipo_paciente.autorizado_por_id) ? '' : episodio.user_tipo_paciente.autorizado_por_id, "\" class=\"form-control bg-gray-footer-parodi\" name=\"autorizado_por_id\" id=\"autorizado_por_id\">\n\n                            </div>\n                            <div id=\"options_empleado\" class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Empleado' ? 'd-block' : 'd-none', " col-12 col-sm-4\">\n                                <label>Pertenece a:</label>\n                                <select class=\"form-control bg-gray-footer-parodi\" name=\"directorio_organizacional_id\" id=\"directorio_organizacional_id\">\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === null ? 'selected' : '', " value=\"\">Seleccione</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 1 ? 'selected' : '', " value=\"1\">Presidencia</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 2 ? 'selected' : '', " value=\"2\">Vicepresidencia</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 3 ? 'selected' : '', " value=\"3\">Secretar\xEDa</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 4 ? 'selected' : '', " value=\"4\">Direcci\xF3n M\xE9dica</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 5 ? 'selected' : '', " value=\"5\">Direcci\xF3n de Educaci\xF3n e Investigaci\xF3n</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 6 ? 'selected' : '', " value=\"6\">Direcci\xF3n de Operaciones y Log\xEDstica</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 7 ? 'selected' : '', " value=\"7\">Direcci\xF3n de Comercializaci\xF3n</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 8 ? 'selected' : '', " value=\"8\">Direcci\xF3n de Cumplimiento y Apoyo Corporativo</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 9 ? 'selected' : '', " value=\"9\">Direcci\xF3n de Administraci\xF3n y Finanzas</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 10 ? 'selected' : '', " value=\"10\">Direcci\xF3n de Ingenier\xEDa y Proyectos</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 11 ? 'selected' : '', " value=\"11\">Direcci\xF3n de Medicina Comunitaria y Programas Sociales</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 12 ? 'selected' : '', " value=\"12\">Consejo Consultivo</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 13 ? 'selected' : '', " value=\"13\">Departamento de Cirug\xEDa</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 14 ? 'selected' : '', " value=\"14\">Departamento de Odontolog\xEDa</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 15 ? 'selected' : '', " value=\"15\">Departamento de Medicina</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 16 ? 'selected' : '', " value=\"16\">Departamento de Pediatr\xEDa</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 17 ? 'selected' : '', " value=\"17\">Departamento de Ginecolog\xEDa y Obstetricia</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 18 ? 'selected' : '', " value=\"18\">Departamento de Medicina Cr\xEDtica y Emergencia</option>\n                                    <option ").concat(episodio.user_tipo_paciente.directorio_organizacional_id === 19 ? 'selected' : '', " value=\"19\">Departamento de Medicina Cr\xEDtica y Emergencia</option>\n                                </select>\n                            </div>\n                            <div id=\"options_otro\" class=\"").concat(episodio.user_tipo_paciente.tipo_paciente === 'Otro' ? 'd-block' : 'd-none', " col-12\">\n                                <label>Descripci\xF3n sobre el otro tipo de paciente.</label>\n                                <textarea id=\"observaciones\" row=\"2\" class=\"form-control bg-gray-footer-parodi\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(episodio.user_tipo_paciente.value1) ? '' : episodio.user_tipo_paciente.value1, "</textarea>\n                            </div>\n                            <div class=\"col-12 text-center\">\n                                <button onclick=\"UserCuestAdministracion.handleStoreTipoPaciente(").concat(episodio_id, ")\" class=\"btn btn-success w-25 my-1\">\n                                    Guardar\n                                </button>\n                            </div>\n                        </div>\n                    </div>\n                ")
        );
      };

      var formServicioEmergencia = function formServicioEmergencia() {
        return (
          /*html*/
          "\n                    <h3 class=\"ml-2 text-center text-primary\">Selecciona tipo de Emergencia</h3>\n                    <div id=\"menu_emergencia\" class=\"d-flex flex-wrap bg-light m-1\">\n                        <button\n                            onClick=\"UserCuestAdministracion.handleEmergenciaPaciente('Ambulatoria',".concat(episodio_id, ")\"\n                            class=\"").concat(episodio.servicio_emergencia.tipo_emergencia === 'Ambulatoria' ? 'active' : '', "  col d-flex flex-column align-items-center card-user-admin-serv primary\">\n                            <i class=\"pc-emergencia h3\"></i>\n                            <b class=\"flex-fill mb-2 title font-italic\">\n                                Emergencia Ambulatoria<br>(Walk in)\n                            </b>\n                        </button>\n                        <button\n                            onClick=\"UserCuestAdministracion.handleEmergenciaPaciente('Convencional',").concat(episodio_id, ")\"\n\n                            class=\"").concat(episodio.servicio_emergencia.tipo_emergencia === 'Convencional' ? 'active' : '', "  col d-flex flex-column align-items-center card-user-admin-serv primary\">\n                            <i class=\"pc-emergencia h3\"></i>\n                            <b class=\"flex-fill mb-2 title font-italic\">\n                                Emergencia Convencional\n                            </b>\n                        </button>\n                    </div>\n                    <div id=\"options_ambulatoria\" class=\"").concat(episodio.servicio_emergencia.tipo_emergencia === 'Ambulatoria' ? 'd-flex' : 'd-none', "   flex-column flex-wrap bg-light\">\n                        <h3 class=\"mr-2 text-primary text-center\">\n                            Walk in Emergency\n                        </h3>\n                        <div class=\"d-flex flex-wrap bg-light\">\n                            <button\n                                onclick=\"UserCuestAdministracion.handleServicioEmergencia({'tipo_emergencia':'Ambulatoria','description':'Ambulatoria Walk in A','value':1,'episodio_id':").concat(episodio_id, ",'color':'info','user_id_paciente':").concat(episodio.user_id, "})\"\n                                class=\"").concat(episodio.servicio_emergencia.value === "1" ? 'active' : '', "  col d-flex flex-column align-items-center card-user-admin-serv primary\"\n                            >\n                                <i class=\"icon-no-check pc-serv_a mb-2\"></i>\n                                <i class=\"icon-check pc-check mb-2\"></i>\n                                <b class=\"mb-2 title font-italic\">\n                                    Walk in Tipo A\n                                </b>\n                                <div class=\"flex-fill d-flex flex-column justify-content-center mb-2 card-description\">\n                                    Evaluaci\xF3n por M\xE9dico Especialista<br>(Coordinador de Emergencia)\n                                </div>\n                                <div class=\"status p-2 rounded bg-white font-weight-bold\"></div>\n                            </button>\n                            <button\n                                onclick=\"UserCuestAdministracion.handleServicioEmergencia({'tipo_emergencia':'Ambulatoria','description':'Ambulatoria Walk in A Plus','value':2,'episodio_id':").concat(episodio_id, ",'color':'info','user_id_paciente':").concat(episodio.user_id, "})\"\n                                class=\"").concat(episodio.servicio_emergencia.value === "2" ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\"\n                            >\n                                <i class=\"icon-no-check pc-serv_a_plus mb-2\"></i>\n                                <i class=\"icon-check pc-check mb-2\"></i>\n                                <b class=\"mb-2 title font-italic\">\n                                    Walk in Tipo A Plus\n                                </b>\n                                <div class=\"flex-fill d-flex flex-column justify-content-center mb-2 card-description\">\n                                    Evaluaci\xF3n por M\xE9dico Especialista<br>(Coordinador de Emergencia)<br>+ Evaluaci\xF3n de Especialista Adicional\n                                </div>\n                                <div class=\"status p-2 rounded bg-white font-weight-bold\"></div>\n                            </button>\n                            <button\n                                onclick=\"UserCuestAdministracion.handleServicioEmergencia({'tipo_emergencia':'Ambulatoria','description':'Ambulatoria Walk in B','value':3,'episodio_id':").concat(episodio_id, ",'color':'success','user_id_paciente':").concat(episodio.user_id, "})\"\n                                class=\"").concat(episodio.servicio_emergencia.value === "3" ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\"\n                            >\n                                <i class=\"icon-no-check pc-serv_b mb-2\"></i>\n                                <i class=\"icon-check pc-check mb-2\"></i>\n                                <b class=\"mb-2 title font-italic\">\n                                    Walk in Tipo B\n                                </b>\n                                <div class=\"flex-fill d-flex flex-column justify-content-center mb-2 card-description\">\n                                    Evaluaci\xF3n por M\xE9dico Especialista<br>(Coordinador de Emergencia)\n                                </div>\n                                <div class=\"status p-2 rounded bg-white font-weight-bold\"></div>\n                            </button>\n                            <button\n                                onclick=\"UserCuestAdministracion.handleServicioEmergencia({'tipo_emergencia':'Ambulatoria','description':'Ambulatoria Walk in B Plus','value':4,'episodio_id':").concat(episodio_id, ",'color':'success','user_id_paciente':").concat(episodio.user_id, "})\"\n                                class=\"").concat(episodio.servicio_emergencia.value === "4" ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\"\n                            >\n                                <i class=\"icon-no-check pc-serv_b_plus mb-2\"></i>\n                                <i class=\"icon-check pc-check mb-2\"></i>\n                                <b class=\"mb-2 title font-italic\">\n                                    Walk in Tipo B Plus\n                                </b>\n                                <div class=\"flex-fill d-flex flex-column justify-content-center mb-2 card-description\">\n                                    Evaluaci\xF3n por M\xE9dico Especialista<br>(Coordinador de Emergencia)<br>+ Evaluaci\xF3n de Especialista Adicional\n                                </div>\n                                <div class=\"status p-2 rounded bg-white font-weight-bold\"></div>\n                            </button>\n                            <button\n                                onclick=\"UserCuestAdministracion.handleServicioEmergencia({'tipo_emergencia':'Ambulatoria','description':'Ambulatoria No Walk in','value':5,'episodio_id':").concat(episodio_id, ",'color':'secondary','user_id_paciente':").concat(episodio.user_id, "})\"\n                                class=\"").concat(episodio.servicio_emergencia.value === "5" ? 'active' : '', " col d-flex flex-column align-items-center card-user-admin-serv primary\"\n                            >\n                                <i class=\"icon-no-check pc-serv_nw mb-2\"></i>\n                                <i class=\"icon-check pc-check mb-2\"></i>\n                                <b class=\"mb-2 title font-italic\">\n                                    No Walk in\n                                </b>\n                                <div class=\"flex-fill d-flex flex-column justify-content-center mb-2 card-description\">\n                                    Evaluaci\xF3n por M\xE9dico Especialista<br>(Coordinador de Emergencia)<br>+ Evaluaci\xF3n de Especialistas Adicionales\n                                </div>\n                                <div class=\"status p-2 rounded bg-white font-weight-bold\"></div>\n                            </button>\n                        </div>\n                    </div>\n                ")
        );
      };

      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n                <div id=\"infoPaciente\"></div>\n                <div class=\"container-fluid\">\n                    <div class=\"myContent flex-fill overflow-auto p-1\">\n                    <div class=\"col-12  overflow-auto\">\n                        <div class=\"text-primary h2\">\n                            Datos Administrativos\n                        </div>\n                        <div class=\"accordion\" id=\"accordionExample\">\n\n                            <div class=\"card\">\n                                <div class=\"card-header d-flex align-items-center h1 text-primary mb-1\" id=\"heading2\">\n                                    <i class=\"pc-seguro_medico mr-1\"></i>\n                                    <button class=\"btn btn-block mb-0 text-secondary text-left\" style=\"font-size:1.5rem\" type=\"button\" data-toggle=\"collapse\" data-target=\"#collapse2\" aria-expanded=\"true\" aria-controls=\"collapse2\">\n                                        Tipo de paciente\n                                    </button>\n                                </div>\n                                <div id=\"collapse2\" class=\"collapse show\" aria-labelledby=\"heading2\" data-parent=\"#accordionExample\">\n                                    <input type=\"hidden\" id=\"tipo_paciente\" value=\"".concat(episodio.user_tipo_paciente.tipo_paciente !== 'Particular' ? episodio.user_tipo_paciente.tipo_paciente : 'Particular', "\">\n                                    ").concat(formTipoPaciente(), "\n                            </div>\n                            <div class=\"card\">\n                                <div class=\"card-header d-flex align-items-center h1 text-primary mb-1\" id=\"heading1\">\n                                    <i class=\"pc-servicio_walkin mr-1\"></i>\n                                    <button class=\"btn btn-block mb-0 text-secondary text-left\" style=\"font-size:1.5rem\" type=\"button\" data-toggle=\"collapse\" data-target=\"#collapse1\" aria-expanded=\"true\" aria-controls=\"collapse1\">\n                                        Servicios\n                                    </button>\n                                </div>\n                                <div id=\"collapse1\" class=\"collapse ").concat(episodio.servicio_emergencia.tipo_emergencia !== null ? 'show' : '', "\" aria-labelledby=\"heading1\" data-parent=\"#accordionExample\">\n                                    <input type=\"hidden\" id=\"input_tipo_emergencia\" value=\"").concat(episodio.servicio_emergencia.tipo_emergencia !== null ? episodio.servicio_emergencia.tipo_emergencia : '', "\">\n                                    ").concat(formServicioEmergencia(), "\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n                </div>\n            "));
      _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"].infoPaciente("#infoPaciente", episodio.user_id);
    }
  }]);

  return UserCuestAdministracion;
}();

function AppTipoPaciente() {
  return {
    name: "AppTipoPaciente",
    template:
    /*html */
    "\n            <div class=\"mb-1\">\n                <h3 class=\"ml-2 text-center text-secondary\">\n                    Pulsa en el Tipo de Paciente\n                </h3>\n\n                <div id=\"menu_tipoPaciente\" class=\"d-flex flex-wrap bg-light m-1\">\n                    <button\n                        v-for=\"(option,index) in options\"\n                        :key=\"'tipo_paciente_'+index\"\n                        @click=\"handletipoPaciente(option.description)\"\n                        :class=\"['col d-flex flex-column align-items-center card-user-admin-serv primary ',(tipo_paciente===option.description ? 'active': '')]\">\n                        <i :class=\"['h3 '+option.icon]\"></i>\n                        <b class=\"flex-fill mb-2 title font-italic\">\n                            {{option.description}}\n                        </b>\n                    </button>\n\n                </div>\n                <div class=\"container-fluid\">\n\n\n                    <div class=\"row\">\n                        <div\n                            id=\"options_asegurado\"\n                            :class=\"[(tipo_paciente==='Asegurado' ? 'd-block': 'd-none'), 'col-12']\"\n                        >\n                            <div class=\"row\">\n                                <div class=\"col-12 col-sm-6\">\n                                    <label>Aseguradora</label>\n                                    <select\n                                        v-model=\"cat_aseguradora_id\"\n                                        class=\"form-control bg-gray-footer-parodi\"\n                                        name=\"cat_aseguradora_id\"\n                                        id=\"cat_aseguradora_id\"\n                                    >\n                                        <option value=\"null\">Seleccione</option>\n                                        <option value=\"\">Seleccione</option>\n                                        <option value=\"1\">Vumi Group</option>\n                                        <option value=\"2\">Best Doctors Insurance Ho</option>\n                                        <option value=\"3\">Mercantil</option>\n                                        <option value=\"4\">Humanitas</option>\n                                        <option value=\"5\">Seguros Caracas De Libert</option>\n                                        <option value=\"6\">Usa Medical Services Vene</option>\n                                        <option value=\"7\">Banesco</option>\n                                        <option value=\"8\">Beneficios Medicos Intern</option>\n                                        <option value=\"9\">Makler Administradora C.A</option>\n                                        <option value=\"10\">Administradora De Riesgos Parsalud</option>\n                                        <option value=\"11\">Euro-Center Ltda</option>\n                                        <option value=\"12\">Ever Insurance</option>\n                                        <option value=\"13\">Euro-Center Ltda</option>\n                                        <option value=\"14\">La Internacional De Seguros,S.A</option>\n                                        <option value=\"15\">Loyal</option>\n                                        <option value=\"16\">Mafre la seguridad, ca (solo POLAR)</option>\n                                    </select>\n                                </div>\n                                <div class=\"col-12 col-sm-6\">\n                                    <label>N\xB0 de poliza</label>\n                                    <input\n                                        v-model=\"n_poliza\"\n                                        type=\"text\"\n                                        class=\"form-control bg-gray-footer-parodi\"\n                                        id=\"n_poliza\"\n                                    >\n                                </div>\n\n                            </div>\n                        </div>\n                        <div\n                            id=\"options_empresarial\"\n                            :class=\"[(tipo_paciente==='Empresarial' ? 'd-block': 'd-none'), 'col-12 col-sm-4']\"\n                        >\n                            <label>Instituci\xF3n / Empresa / Fundaci\xF3n</label>\n                            {{cat_empresa_id}}\n                            <select\n                                v-model=\"cat_empresa_id\"\n                                class=\"form-control bg-gray-footer-parodi\"\n                                name=\"cat_empresa_id\"\n                                id=\"cat_empresa_id\"\n                            >\n                                <option value=\"null\">Seleccione</option>\n                                <option value=\"1\">Empresas Polar</option>\n                                <option value=\"2\">Farmatodo</option>\n                                <option value=\"3\">Excelsior\xA0Gama</option>\n                            </select>\n                        </div>\n                        <div\n                            id=\"options_cortesia\"\n                            :class=\"[(tipo_paciente==='Cortes\xEDa' ? 'd-block': 'd-none'), 'col-12 col-sm-4']\"\n                        >\n                            <label>Autorizado por:</label>\n                            <input\n                                v-model=\"autorizado_por_id\"\n                                class=\"form-control bg-gray-footer-parodi\"\n                                name=\"autorizado_por_id\"\n                                id=\"autorizado_por_id\"\n                            >\n                        </div>\n                        <div\n                            id=\"options_empleado\"\n                            :class=\"[(tipo_paciente==='Empleado' ? 'd-block': 'd-none'), 'col-12 col-sm-4']\"\n                        >\n                            <label>Pertenece a:</label>\n                            <select\n                                v-model=\"directorio_organizacional_id\"\n                                class=\"form-control bg-gray-footer-parodi\"\n                                name=\"directorio_organizacional_id\"\n                                id=\"directorio_organizacional_id\"\n                            >\n                                <option value=\"null\">Seleccione</option>\n                                <option value=\"1\">Presidencia</option>\n                                <option value=\"2\">Vicepresidencia</option>\n                                <option value=\"3\">Secretar\xEDa</option>\n                                <option value=\"4\">Direcci\xF3n M\xE9dica</option>\n                                <option value=\"5\">Direcci\xF3n de Educaci\xF3n e Investigaci\xF3n</option>\n                                <option value=\"6\">Direcci\xF3n de Operaciones y Log\xEDstica</option>\n                                <option value=\"7\">Direcci\xF3n de Comercializaci\xF3n</option>\n                                <option value=\"8\">Direcci\xF3n de Cumplimiento y Apoyo Corporativo</option>\n                                <option value=\"9\">Direcci\xF3n de Administraci\xF3n y Finanzas</option>\n                                <option value=\"10\">Direcci\xF3n de Ingenier\xEDa y Proyectos</option>\n                                <option value=\"11\">Direcci\xF3n de Medicina Comunitaria y Programas Sociales</option>\n                                <option value=\"12\">Consejo Consultivo</option>\n                                <option value=\"13\">Departamento de Cirug\xEDa</option>\n                                <option value=\"14\">Departamento de Odontolog\xEDa</option>\n                                <option value=\"15\">Departamento de Medicina</option>\n                                <option value=\"16\">Departamento de Pediatr\xEDa</option>\n                                <option value=\"17\">Departamento de Ginecolog\xEDa y Obstetricia</option>\n                                <option value=\"18\">Departamento de Medicina Cr\xEDtica y Emergencia</option>\n                                <option value=\"19\">Departamento de Medicina Cr\xEDtica y Emergencia</option>\n                            </select>\n                        </div>\n                        <div\n                            id=\"options_otro\"\n                            :class=\"[(tipo_paciente==='Otro' ? 'd-block': 'd-none'), 'col-12']\"\n                        >\n                            <label>Descripci\xF3n sobre el otro tipo de paciente.</label>\n                            <textarea\n                                id=\"observaciones\"\n                                v-model=\"observaciones\"\n                                row=\"2\"\n                                class=\"form-control bg-gray-footer-parodi\"\n                                >{{observaciones}}</textarea>\n                        </div>\n                        <div class=\"col-12 text-center\">\n                            <button @click=\"handleStoreTipoPaciente()\" class=\"btn btn-success w-25 my-1\">\n                                Guardar\n                            </button>\n                        </div>\n                    </div>\n                </div>\n\n            </div>\n        ",
    props: {
      index: Number,
      patient_data: Object
    },
    components: {},
    data: function data() {
      return {
        //optionSelected:"Particular",
        options: [{
          icon: "fas fa-user-injured",
          description: "Particular"
        }, {
          icon: "fas fa-user-injured",
          description: "Asegurado"
        }, {
          icon: "fas fa-user-injured",
          description: "Empresarial"
        }, {
          icon: "fas fa-user-injured",
          description: "Cortesía"
        }, {
          icon: "fas fa-user-injured",
          description: "Empleado"
        }, {
          icon: "fas fa-user-injured",
          description: "Otro"
        }],
        episodios: pacientes_datos,
        tipo_paciente: 'Particular',
        cat_aseguradora_id: null,
        n_poliza: null,
        cat_empresa_id: null,
        autorizado_por_id: "",
        directorio_organizacional_id: null,
        observaciones: null
      };
    },
    methods: {
      handletipoPaciente: function handletipoPaciente(newOption) {
        this.tipo_paciente = newOption;
      },
      handleStoreTipoPaciente: function handleStoreTipoPaciente() {
        var _this = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3() {
          var cat_aseguradora_id, n_poliza, cat_empresa_id, autorizado_por_id, directorio_organizacional_id, observaciones;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
            while (1) {
              switch (_context3.prev = _context3.next) {
                case 0:
                  //SELECTOR INPUT ASEGURADORA
                  cat_aseguradora_id = document.querySelector('#cat_aseguradora_id'); //SELECTOR INPUT NUMERO POLIZA

                  n_poliza = document.querySelector('#n_poliza'); //SELECTOR INPUT INSTITUCION / EMPRESA / FUNDACION

                  cat_empresa_id = document.querySelector('#cat_empresa_id'); //SELECTOR INPUT AUTORIZAZADO POR

                  autorizado_por_id = document.querySelector('#autorizado_por_id'); //SELECTOR INPUT PERTENECE A

                  directorio_organizacional_id = document.querySelector('#directorio_organizacional_id'); //SELECTOR INPUT OBSERVACIONES

                  observaciones = document.querySelector('#observaciones'); //SI TIPO PACIENTE ES ASEGURADO VERIFICAR SI SE HA SELECCIONADO EL SEGURO Y LA POLIZA

                  if (!(_this.tipo_paciente === "Asegurado")) {
                    _context3.next = 10;
                    break;
                  }

                  if (!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(_this.cat_aseguradora_id)) {
                    _context3.next = 10;
                    break;
                  }

                  Swal.fire({
                    icon: "warning",
                    title: "Debe seleccionar una Aseguradora",
                    showDenyButton: false,
                    showCancelButton: false,
                    confirmButtonText: "Ok!",
                    didClose: function didClose() {
                      setTimeout(function () {
                        cat_aseguradora_id.focus();
                      }, 110);
                    }
                  });
                  return _context3.abrupt("return", false);

                case 10:
                  if (!(_this.tipo_paciente === "Empresarial")) {
                    _context3.next = 14;
                    break;
                  }

                  if (!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(_this.cat_empresa_id)) {
                    _context3.next = 14;
                    break;
                  }

                  Swal.fire({
                    icon: "warning",
                    title: "Debe seleccionar una Instituci\xF3n / Empresa / Fundaci\xF3n",
                    showDenyButton: false,
                    showCancelButton: false,
                    confirmButtonText: "Ok!",
                    didClose: function didClose() {
                      setTimeout(function () {
                        cat_empresa_id.focus();
                      }, 110);
                    }
                  });
                  return _context3.abrupt("return", false);

                case 14:
                  if (!(_this.tipo_paciente === "Cortesía")) {
                    _context3.next = 18;
                    break;
                  }

                  if (!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(_this.autorizado_por_id)) {
                    _context3.next = 18;
                    break;
                  }

                  Swal.fire({
                    icon: "warning",
                    title: "Debe escribir qui\xE9n autoriza la cortes\xEDa",
                    showDenyButton: false,
                    showCancelButton: false,
                    confirmButtonText: "Ok!",
                    didClose: function didClose() {
                      setTimeout(function () {
                        autorizado_por_id.focus();
                      }, 110);
                    }
                  });
                  return _context3.abrupt("return", false);

                case 18:
                  if (!(_this.tipo_paciente === "Empleado")) {
                    _context3.next = 22;
                    break;
                  }

                  if (!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(_this.directorio_organizacional_id)) {
                    _context3.next = 22;
                    break;
                  }

                  Swal.fire({
                    icon: "warning",
                    title: "Debe seleccionar a qu\xE9 area pertenece el empleado",
                    showDenyButton: false,
                    showCancelButton: false,
                    confirmButtonText: "Ok!",
                    didClose: function didClose() {
                      setTimeout(function () {
                        directorio_organizacional_id.focus();
                      }, 110);
                    }
                  });
                  return _context3.abrupt("return", false);

                case 22:
                  if (!(_this.tipo_paciente === "Otro")) {
                    _context3.next = 26;
                    break;
                  }

                  if (!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(_this.observaciones)) {
                    _context3.next = 26;
                    break;
                  }

                  Swal.fire({
                    icon: "warning",
                    title: "Debe describir qu\xE9 otro tipo de paciente es.",
                    showDenyButton: false,
                    showCancelButton: false,
                    confirmButtonText: "Ok!",
                    didClose: function didClose() {
                      setTimeout(function () {
                        observaciones.focus();
                      }, 110);
                    }
                  });
                  return _context3.abrupt("return", false);

                case 26:
                  Swal.fire({
                    icon: "warning",
                    title: "\xBFQuieres guardar los datos?",
                    showDenyButton: false,
                    showCancelButton: true,
                    confirmButtonText: "Si",
                    cancelButtonText: "No, a\xFAn no"
                    /* denyButtonText: `Desactivar`, */

                  }).then( /*#__PURE__*/function () {
                    var _ref2 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2(result) {
                      var that, formdata, _result2, index;

                      return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
                        while (1) {
                          switch (_context2.prev = _context2.next) {
                            case 0:
                              that = _this;

                              if (that.tipo_paciente === "Particular") {
                                that.cat_aseguradora_id = null;
                                that.n_poliza = null;
                                that.cat_empresa_id = null;
                                that.autorizado_por_id = "";
                                that.directorio_organizacional_id = null;
                                that.observaciones = null;
                              }

                              if (that.tipo_paciente === "Asegurado") {
                                that.cat_empresa_id = null;
                                that.autorizado_por_id = "";
                                that.directorio_organizacional_id = null;
                                that.observaciones = null;
                              }

                              if (that.tipo_paciente === "Empresarial") {
                                that.cat_aseguradora_id = null;
                                that.n_poliza = null;
                                that.autorizado_por_id = "";
                                that.directorio_organizacional_id = null;
                                that.observaciones = null;
                              }

                              if (that.tipo_paciente === "Empleado") {
                                that.cat_aseguradora_id = null;
                                that.n_poliza = null;
                                that.cat_empresa_id = null;
                                that.autorizado_por_id = "";
                                that.observaciones = null;
                              }

                              if (that.tipo_paciente === "Otro") {
                                that.cat_aseguradora_id = null;
                                that.n_poliza = null;
                                that.cat_empresa_id = null;
                                that.autorizado_por_id = "";
                                that.directorio_organizacional_id = null;
                              }

                              if (!result.isConfirmed) {
                                _context2.next = 20;
                                break;
                              }

                              formdata = new FormData();
                              ['tipo_paciente', 'cat_aseguradora_id', 'n_poliza', 'cat_empresa_id', 'autorizado_por_id', 'directorio_organizacional_id', 'observaciones'].forEach(function (key) {
                                formdata.append(key, that[key]);
                              });
                              formdata.append("user_id_paciente", that.patient_data.user_id);
                              formdata.append("episodio_id", that.patient_data.episodio_id);
                              formdata.append("_token", $("#_token").val());
                              formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                              _context2.next = 15;
                              return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/tipopaciente/store", formdata);

                            case 15:
                              _result2 = _context2.sent;
                              index = pacientes_datos.findIndex(function (x) {
                                return x['episodio'] === that.patient_data.episodio_id;
                              });
                              _this.episodios[index].user_tipo_paciente = _result2;
                              EventBus.$on('externalVarChanged', function (newValue) {
                                that.episodios = newValue;
                              });
                              Swal.fire({
                                icon: "success",
                                title: "Datos guardados correctamente",
                                showDenyButton: false,
                                showCancelButton: false,
                                confirmButtonText: "Ok!"
                              });

                            case 20:
                            case "end":
                              return _context2.stop();
                          }
                        }
                      }, _callee2);
                    }));

                    return function (_x2) {
                      return _ref2.apply(this, arguments);
                    };
                  }());

                case 27:
                case "end":
                  return _context3.stop();
              }
            }
          }, _callee3);
        }))();
      }
    },
    mounted: function mounted() {
      console.log(this.patient_data);
      var _this$patient_data$us = this.patient_data.user_tipo_paciente,
          tipo_paciente = _this$patient_data$us.tipo_paciente,
          cat_aseguradora_id = _this$patient_data$us.cat_aseguradora_id,
          cat_empresa_id = _this$patient_data$us.cat_empresa_id,
          value1 = _this$patient_data$us.value1,
          value2 = _this$patient_data$us.value2,
          autorizado_por_id = _this$patient_data$us.autorizado_por_id,
          directorio_organizacional_id = _this$patient_data$us.directorio_organizacional_id;
      this.tipo_paciente = tipo_paciente;
      this.cat_aseguradora_id = cat_aseguradora_id;
      this.n_poliza = value2;
      this.cat_empresa_id = cat_empresa_id;
      this.autorizado_por_id = autorizado_por_id;
      this.directorio_organizacional_id = directorio_organizacional_id;
      this.observaciones = value1;
    }
  };
}

function AppServiciosPaciente() {
  return {
    name: "AppServiciosPaciente",
    template:
    /*html*/
    "\n            <div>\n                <h3 class=\"ml-2 text-center text-primary\">Selecciona tipo de Emergencia</h3>\n                <div id=\"menu_emergencia\" class=\"d-flex flex-wrap bg-light m-1\">\n                    <button\n                        @click=\"handleEmergenciaPaciente('Ambulatoria')\"\n                        :class=\"[{'active': tipo_emergencia==='Ambulatoria' }, 'col d-flex flex-column align-items-center card-user-admin-serv primary']\"\n                    >\n                        <i class=\"pc-emergencia h3\"></i>\n                        <b class=\"flex-fill mb-2 title font-italic\">\n                            Emergencia Ambulatoria<br>(Walk in)\n                        </b>\n                    </button>\n                    <button\n                        @click=\"handleEmergenciaPaciente('Convencional')\"\n                        :class=\"[{'active': tipo_emergencia==='Convencional' },  'col d-flex flex-column align-items-center card-user-admin-serv primary']\"\n                    >\n                        <i class=\"pc-emergencia h3\"></i>\n                        <b class=\"flex-fill mb-2 title font-italic\">\n                            Emergencia Convencional\n                        </b>\n                    </button>\n                </div>\n                <div\n                    id=\"options_ambulatoria\"\n                    :class=\"[(tipo_emergencia==='Ambulatoria' ? 'd-flex': 'd-none' ),  'flex-column flex-wrap bg-light']\"\n                >\n                    <h3 class=\"mr-2 text-primary text-center\">\n                        Walk in Emergency\n                    </h3>\n                    <div class=\"d-flex flex-wrap bg-light\">\n                        <button\n                        v-for=\"(item,index) in options\"\n                            :key=\"'option_'+index\"\n                            @click=\"handleEmergenciaAmbulatoriaPaciente(index)\"\n                            :class=\"[{'active':optionSelected==item.value }, 'col d-flex flex-column align-items-center card-user-admin-serv primary']\"\n                        >\n                            <i v-if=\"optionSelected==item.value\" :class=\"item.icon\"></i>\n                            <i v-else class=\"icon-check pc-check mb-2\"></i>\n                            <b class=\"mb-2 title font-italic\">\n                                {{item.title}}\n                            </b>\n                            <div class=\"flex-fill d-flex flex-column justify-content-center mb-2 card-description\">\n                                {{item.description}}\n                            </div>\n                            <div class=\"status p-2 rounded bg-white font-weight-bold\"></div>\n                        </button>\n                    </div>\n                </div>\n            </div>\n        ",
    props: {
      patient_data: Object
    },
    components: {},
    data: function data() {
      return {
        episodios: pacientes_datos,
        tipo_emergencia: null,
        optionSelected: null,
        options: [{
          icon: "icon-no-check pc-serv_a mb-2",
          title: "Walk in Tipo A",
          description: "Evaluación por Médico Especialista (Coordinador de Emergencia)",
          value: 1
        }, {
          icon: "icon-no-check pc-serv_a mb-2",
          title: "Walk in Tipo A Plus",
          description: " Evaluación por Médico Especialista (Coordinador de Emergencia) + Evaluación de Especialista Adicional",
          value: 2
        }, {
          icon: "icon-no-check pc-serv_a mb-2",
          title: "Walk in Tipo B",
          description: "Evaluación por Médico Especialista<br>(Coordinador de Emergencia)",
          value: 3
        }, {
          icon: "icon-no-check pc-serv_a mb-2",
          title: "Walk in Tipo B Plus",
          description: "Evaluación por Médico Especialista (Coordinador de Emergencia) + Evaluación de Especialista Adicional",
          value: 4
        }, {
          icon: "icon-no-check pc-serv_a mb-2",
          title: "No Walk in",
          description: "Evaluación por Médico Especialista (Coordinador de Emergencia) + Evaluación de Especialistas Adicionales",
          value: 5
        }]
      };
    },
    methods: {
      handleServicioEmergencia: function handleServicioEmergencia(datos) {
        var _this2 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5() {
          var that;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
            while (1) {
              switch (_context5.prev = _context5.next) {
                case 0:
                  that = _this2; //console.log(datos)

                  Swal.fire({
                    icon: "warning",
                    title: "Servicio Emergencia ".concat(_this2.tipo_emergencia, "?"),
                    text: "Haz clic abajo para activar o desactivar este servicio.",
                    showDenyButton: true,
                    showCancelButton: true,
                    confirmButtonText: "Activar",
                    cancelButtonText: "No hacer nada",
                    denyButtonText: "Desactivar"
                  }).then( /*#__PURE__*/function () {
                    var _ref3 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4(result) {
                      var formdata, _result3, index, _formdata, myResult, _index;

                      return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
                        while (1) {
                          switch (_context4.prev = _context4.next) {
                            case 0:
                              if (!result.isConfirmed) {
                                _context4.next = 17;
                                break;
                              }

                              formdata = new FormData();
                              formdata.append("tipo_emergencia", _this2.tipo_emergencia);
                              formdata.append("user_id_paciente", _this2.patient_data.user_id);
                              formdata.append("episodio_id", _this2.patient_data.episodio_id);
                              formdata.append("value", datos.value);
                              formdata.append("description", _this2.tipo_emergencia);
                              formdata.append("_token", $("#_token").val());
                              formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                              _context4.next = 11;
                              return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/servicios/emergencia/store", formdata);

                            case 11:
                              _result3 = _context4.sent;
                              index = pacientes_datos.findIndex(function (x) {
                                return x['episodio'] === that.patient_data.episodio_id;
                              });
                              that.episodios[index].servicio_emergencia = _result3;
                              EventBus.$on('externalVarChanged', function (newValue) {
                                that.episodios = newValue;
                              }); //console.log(pacientes_datos[index]);

                              $("#modal-patient-item").modal("hide");
                              Swal.fire({
                                icon: "success",
                                title: "Servicio Emergencia ".concat(datos.description, " activado"),
                                showDenyButton: false,
                                showCancelButton: false,
                                confirmButtonText: "Ok!"
                              });

                            case 17:
                              if (!result.isDenied) {
                                _context4.next = 29;
                                break;
                              }

                              _formdata = new FormData();

                              _formdata.append("episodio_id", _this2.patient_data.episodio_id);

                              _formdata.append("_token", $("#_token").val());

                              _context4.next = 23;
                              return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/servicios/emergencia/destroy", _formdata);

                            case 23:
                              myResult = _context4.sent;
                              _index = pacientes_datos.findIndex(function (x) {
                                return x['episodio'] === _this2.patient_data.episodio_id;
                              });
                              that.episodios[_index].servicio_emergencia = myResult;
                              EventBus.$on('externalVarChanged', function (newValue) {
                                that.episodios = newValue;
                              });
                              $("#modal-patient-item").modal("hide");
                              Swal.fire({
                                icon: "success",
                                title: "Servicio Emergencia ".concat(datos.description, " desactivado"),
                                showDenyButton: false,
                                showCancelButton: false,
                                confirmButtonText: "Ok!"
                              });

                            case 29:
                            case "end":
                              return _context4.stop();
                          }
                        }
                      }, _callee4);
                    }));

                    return function (_x3) {
                      return _ref3.apply(this, arguments);
                    };
                  }());

                case 2:
                case "end":
                  return _context5.stop();
              }
            }
          }, _callee5);
        }))();
      },
      handleEmergenciaAmbulatoriaPaciente: function handleEmergenciaAmbulatoriaPaciente(index) {
        this.tipo_emergencia = "Ambulatoria";
        this.optionSelected = this.options[index].value;
        this.handleServicioEmergencia({
          'tipo_emergencia': this.tipo_emergencia,
          'description': this.options[index].title,
          'value': this.optionSelected,
          'color': 'gray'
        });
      },
      handleEmergenciaPaciente: function handleEmergenciaPaciente(tipo_emergencia) {
        this.tipo_emergencia = tipo_emergencia;

        if (tipo_emergencia === "Ambulatoria") {
          return false;
        }

        this.handleServicioEmergencia({
          'tipo_emergencia': this.tipo_emergencia,
          'description': 'Convencional',
          'value': 6,
          'color': 'gray'
        });
      }
    },
    mounted: function mounted() {
      this.tipo_emergencia = this.patient_data.servicio_emergencia.tipo_emergencia;
      this.optionSelected = this.patient_data.servicio_emergencia.value;
    }
  };
}

function AppAdministrativeData(patient_data, index_episodio) {
  $("#modal-patient-item").modal("show");
  document.querySelector("#modal-patient-item .modal-body-2").innerHTML = "\n        <div>\n            <div id=\"infoPaciente\"></div>\n            <div id='AppAdministrativeData' class=\"fade-in flex-fill d-flex flex-column  overflow-auto\">\n                <div class=\"container-fluid\">\n                    <h3 class=\"text-primary\">\n                        Datos Administrativos\n                    </h3>\n                </div>\n                <div class=\"container-fluid\">\n                    <div class=\"accordion  flex-fill overflow-auto\" id=\"accordionExample\">\n                        <div class=\"card\">\n                            <div\n                                class=\"card-header d-flex align-items-center h1 text-primary mb-1\"\n                                id=\"heading1\"\n                            >\n                                <i class=\"pc-seguro_medico mr-1\"></i>\n                                <button\n\n                                    :class=\"['btn btn-block mb-0 text-secondary text-left collapsed']\"\n                                    style=\"font-size:1.5rem\"\n                                    type=\"button\"\n                                    data-toggle=\"collapse\"\n                                    data-target=\"#collapse1\"\n                                    :aria-expanded=\"true\"\n                                    aria-controls=\"collapse1\"\n                                >\n                                    Tipo de paciente\n                                </button>\n                            </div>\n                            <div\n                                id=\"collapse1\"\n                                :class=\"['collapse show']\"\n                                aria-labelledby=\"heading1\"\n\n                            >\n                                <app-tipo-paciente :index=\"index\" :patient_data=\"patient_data\" />\n                            </div>\n                        </div>\n                        <div class=\"card\">\n                            <div class=\"card-header d-flex align-items-center h1 text-primary mb-1\" id=\"heading2\">\n                                <i class=\"pc-servicio_walkin mr-1\"></i>\n                                <button\n\n                                    :class=\"['btn btn-block mb-0 text-secondary text-left collapsed']\"\n                                    style=\"font-size:1.5rem\"\n                                    type=\"button\"\n                                    data-toggle=\"collapse\"\n                                    data-target=\"#collapse2\"\n                                    :aria-expanded=\"true\"\n                                    aria-controls=\"collapse2\"\n                                >\n                                    Servicios\n                                </button>\n                            </div>\n                            <div\n                                id=\"collapse2\"\n                                :class=\"['collapse show']\"\n                                aria-labelledby=\"heading2\"\n\n                            >\n                                <app-servicios-paciente :index=\"index\" :patient_data=\"patient_data\" />\n                            </div>\n                        </div>\n                    </div>\n                </div>\n            </div>\n        </div>\n\n\n    "; //

  var app = new Vue({
    name: "AppAdministrativeData",
    el: "#AppAdministrativeData",
    components: {
      "app-tipo-paciente": AppTipoPaciente(),
      "app-servicios-paciente": AppServiciosPaciente()
    },
    data: function data() {
      return {
        index: index_episodio,
        patient_data: patient_data,
        isOpenTipoPaciente: true,
        isOpenServiciosPaciente: true
      };
    },
    mounted: function mounted() {
      userCuestPaciente.infoPaciente("#infoPaciente", this.patient_data.user_id);
    }
  });
}

/***/ }),

/***/ "./public/class_js/UserCuestChat.js":
/*!******************************************!*\
  !*** ./public/class_js/UserCuestChat.js ***!
  \******************************************/
/*! exports provided: index */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "index", function() { return index; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
var _this = undefined;

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function _iterableToArrayLimit(arr, i) { var _i = arr == null ? null : typeof Symbol !== "undefined" && arr[Symbol.iterator] || arr["@@iterator"]; if (_i == null) return; var _arr = []; var _n = true; var _d = false; var _s, _e; try { for (_i = _i.call(arr); !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }



function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }



var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"]();
/*



*/

/*


*/

var AppCreateChat = function AppCreateChat() {
  return {
    name: "AppListChats",
    template:
    /*html */
    "\n            <div class=\"d-flex flex-column overflow-auto\">\n                <div class=\"form-group\">\n                    <label class=\"text-secondary\">Chat con:</label>\n                    <select\n                        v-model=\"form.chat_con\"\n                        class=\"form-control-lg border border-gray w-100 bg-light\"\n                        name=\"chat_con\"\n                        id=\"chat_con\"\n                    >\n                        <option value=\"paciente\">Paciente</option>\n                        <option value=\"medico\">M\xE9dico</option>\n                    </select>\n                </div>\n                <div class=\"flex-fill mb-1\">\n                    <textarea\n                        v-model=\"form.chat_mensaje\"\n                        class=\"form-control bg-gray-footer-parodi \"\n                        name=\"chat_mensaje\"\n                        id=\"chat_mensaje\"\n                        rows=\"2\"\n                        style=\" width: 100%;height: 100%;resize: none;\"\n                        placeholder=\"Ingrese chat\"\n                    ></textarea>\n                </div>\n                <div class=\"mb-1\">\n                    <textarea\n                        v-model=\"form.chat_observacion\"\n                        class=\"form-control bg-gray-footer-parodi \"\n                        name=\"chat_observacion\"\n                        id=\"chat_observacion\"\n                        rows=\"2\"\n                        style=\" width: 100%;height: 100%;resize: none;\"\n                        placeholder=\"Observaci\xF3n\"\n                    ></textarea>\n                </div>\n                <div class=\"mb-1\">\n                    <input\n                        type=\"file\"\n                        style=\"height: calc(2.25rem + 8px);\"\n                        id=\"user_file\"\n                        multiple\n                        class=\"form-control input-lg\"\n                        >\n                </div>\n\n            </div>\n        ",
    props: {
      patient_data: Object,
      index: Number,
      form: Object,
      store: Function
    }
  };
};

var AppChats = function AppChats() {
  return {
    name: "AppListChats",
    template:
    /*html */
    "\n            <div>\n\n                <div v-if=\"fechas.length > 0\" class=\"timeline timeline-inverse\">\n                    <div v-for=\"(item,index) in fechas\" :key=\"index\">\n\n                        <div class=\"time-label\">\n                            <span :class=\"'bg-'+color\">\n                                {{item.fecha}}\n                            </span>\n                        </div>\n\n                        <div v-for=\"(item2,index2) in item.items.filter(x=>x.chat_con ===chat_con)\" :key=\"index2\">\n                            <i :class=\"'fas fa-comments bg-'+color\" style=\"font-size: 1.2rem;color: white;\"></i>\n                            <div class=\"timeline-item\">\n                                <span class=\"time\">\n                                    <i class=\"far fa-clock\"></i>\n                                    {{( item2.created_at.split(\" \") )[1]}}\n                                </span>\n                                <h3\n                                    data-toggle=\"collapse\"\n                                    :href=\"'#collapseExample'+index2\"\n                                    role=\"button\"\n                                    aria-expanded=\"false\"\n                                    aria-controls=\"collapseExample\"\n                                    class=\"timeline-header\"\n                                >\n                                    <b class=\"text-primary\">{{item2.medico}}</b> envi\xF3 nuevo chat.\n                                </h3>\n                                <div :id=\"'collapseExample'+index2\" style=\"white-space: break-spaces;\" class=\"collapse show timeline-body\">\n                                    <div  v-html=\"item2.message\">\n\n                                    </div>\n                                    <div v-if=\"item2.coments != null && item2.coments != ''\">\n                                        <b>Observaciones:</b><br>{{item2.coments}}\n                                    </div>\n                                    <div v-if=\"!['undefined',undefined,null,'null'].includes(item2.image) && validateFile(item2.image)==='image'\"  >\n                                        <span v-for=\"(item3,index3 ) in Array.from(JSON.parse(item2.image))\" :key=\"index3\" >\n                                            <img\n                                                title=\"Formato de archivo no reconocido\"\n                                                @click=\"window.open('image/user/chatPaciente/'+item3, '_blank')\"\n                                                style=\"cursor:pointer;width: 100px; height:100px;\"\n                                                class=\"img-thumbnail ampliar zoom img-fluid\"\n                                                src='/image/system/nofound.png'\n                                            >\n\n                                        </span>\n                                    </div>\n                                    <div v-if=\"!['undefined',undefined,null,'null'].includes(item2.image) && validateFile(item2.image)==='audio'\"  >\n                                        <span v-for=\"(item3,index3 ) in Array.from(JSON.parse(item2.image))\" :key=\"index3\" >\n                                            <audio\n                                                controls\n                                                :src=\"'image/user/chatPaciente/'+item3\"\n                                            >\n                                                {{item3}}\n                                            </audio>\n                                        </span>\n                                    </div>\n                                    <div v-if=\"!['undefined',undefined,null,'null'].includes(item2.image) && !['audio','image'].includes(validateFile(item2.image))\"  >\n                                        <span v-for=\"(item3,index3 ) in Array.from(JSON.parse(item2.image))\" :key=\"index3\" >\n                                            <img\n                                                onerror=\"this.src='/image/system/nofound.png';\"\n                                                @click=\"window.open('image/user/chatPaciente/'+item3, '_blank')\"\n                                                style=\"cursor:pointer;width: 100px; height:100px;\"\n                                                class=\"img-thumbnail ampliar zoom img-fluid\"\n                                                :src=\"'image/user/chatPaciente/'+item3\"\n                                            >\n                                        </span>\n                                    </div>\n\n\n                                </div>\n\n                            </div>\n                        </div>\n                    </div>\n\n\n\n                    <div>\n                        <i\n                            style=\"font-size: 1.2rem;\"\n                            class=\"far fa-clock border border-circle bg-gray\"\n                        ></i>\n                    </div>\n                </div>\n                <div v-else class=\"timeline timeline-inverse\">\n                    <div>\n                        <div>\n                            <i aria-hidden=\"true\" class=\"timelime-medic-icon fas fa-comments bg-success text-white\"></i>\n                            <div class=\"timeline-item\">\n                                <div\n                                    :class=\"['text-secondary timeline-body text-center font-weight-bold']\"\n                                    style=\"font-size: 1.2rem;\"\n                                >\n                                    No existe historial\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n                    <div>\n                        <i\n                            style=\"font-size: 1.2rem;\"\n                            class=\"far fa-clock border border-circle bg-gray\"\n                        ></i>\n                    </div>\n                </div>\n            </div>\n\n        ",
    props: {
      patient_data: Object,
      index: Number,
      getChats: Function,
      chat_row: Function,
      chat_con: String,
      fechas: {
        type: Array,
        "default": []
      },
      chats: {
        type: Array,
        "default": []
      },
      color: {
        type: String,
        "default": "primary"
      }
    },
    methods: {
      validateFile: function validateFile(file) {
        Array.from(JSON.parse(file)).forEach(function (valueOfElement) {
          if (["jpg", "jpeg", "pbg", "bmp"].includes(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement))) {
            return 'image';
          }

          if (["mp3", "ogg", "wav", "opus", "flac", "aac"].includes(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement))) {
            return 'audio';
          }
        });
        return null;
      }
    },
    mounted: function mounted() {
      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }))();
    }
  };
};

var index = function index(patient_data, index_episodio) {
  var that = _this;
  $("#modal-patient-item").modal("show");
  $("#modal-patient-item .modal-body-2").html(
  /*html */
  "\n        <div id=\"AppChatWhatsapp\" class=\"flex-fill d-flex flex-column overflow-auto\">Cargando...</div>\n    ");
  var app = new Vue({
    el: "#AppChatWhatsapp",
    name: "AppChatWhatsapp",
    template:
    /*html */
    "\n            <div class=\"flex-fill d-flex flex-column overflow-auto\">\n                <div id=\"infoPaciente\"></div>\n                <div class=\"d-flex justify-content-between\">\n                    <h1 class=\"h1 text-primary\">\n                        Chat WhatsApp\n                    </h1>\n                </div>\n                <ul class=\"nav nav-pills mb-0\" id=\"pills-tab\" role=\"tablist\">\n                    <li @click=\"create_active=false\" class=\"nav-item\" role=\"presentation\">\n                        <a class=\"nav-link h4 text-success active\" id=\"pills-paciente-tab\" data-toggle=\"pill\" href=\"#pills-paciente\" role=\"tab\" aria-controls=\"pills-paciente\" aria-selected=\"true\">Paciente</a>\n                    </li>\n                    <li @click=\"create_active=false\" class=\"nav-item\" role=\"presentation\">\n                        <a class=\"nav-link h4 text-primary\" id=\"pills-medico-tab\" data-toggle=\"pill\" href=\"#pills-medico\" role=\"tab\" aria-controls=\"pills-medico\" aria-selected=\"false\">M\xE9dico</a>\n                    </li>\n                    <li class=\"ml-auto nav-item\" role=\"presentation\">\n                        <a @click=\"create_active=true\" class=\"nav-link h4 text-primary\" id=\"pills-createchat-tab\" data-toggle=\"pill\" href=\"#pills-createchat\" role=\"tab\" aria-controls=\"pills-createchat\" aria-selected=\"false\">\n                            Nuevo Chat\n                            <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                        </a>\n                    </li>\n                </ul>\n                <div class=\"flex-fill overflow-auto d-flex flex-column tab-content mb-1\" id=\"nav-tabContent\">\n\n                    <div\n                        class=\"flex-fill tab-pane fade show active\"\n                        id=\"pills-paciente\"\n                        role=\"tabpanel\"\n                        aria-labelledby=\"pills-paciente-tab\"\n                    >\n                        <app-list-chats\n                            :chats=\"chats\"\n                            :patient_data=\"patient_data\"\n                            :fechas=\"fechas\"\n                            index=\"index\"\n                            chat_con=\"paciente\"\n                            color=\"success\"\n                        />\n                    </div>\n                    <div\n                        class=\"flex-fill tab-pane fade\"\n                        id=\"pills-medico\"\n                        role=\"tabpanel\"\n                        aria-labelledby=\"pills-medico-tab\"\n                    >\n                        <app-list-chats\n                            :chats=\"chats\"\n                            :patient_data=\"patient_data\"\n                            :fechas=\"fechas\"\n                            index=\"index\"\n                            chat_con=\"medico\"\n                            color=\"primary\"\n                        />\n                    </div>\n                    <div\n                        class=\"flex-fill tab-pane fade\"\n                        id=\"pills-createchat\"\n                        role=\"tabpanel\"\n                        aria-labelledby=\"pills-createchat-tab\"\n                    >\n                        <app-create-chat\n                            :form=\"form\"\n                            :patient_data=\"patient_data\"\n                            index=\"index\"\n\n                        />\n                    </div>\n                </div>\n\n                <div class=\"d-flex\">\n                    <div :class=\"[{'d-none':!create_active},{'col-12 col-md-6 px-0 pl-md-0 pr-md-1':create_active}]\">\n                        <button @click=\"handle_store()\"  class=\"btn btn-success w-100\">Agregar</button>\n                    </div>\n                    <div :class=\"[{'col-12':!create_active},{'col-12 col-md-6 px-0 pl-md-1 pr-md-0':create_active}]\">\n                        <button data-dismiss=\"modal\" aria-label=\"Close\"  class=\"btn btn-primary w-100\">Regresar</button>\n                    </div>\n                </div>\n            </div>\n        ",
    components: {
      'app-list-chats': AppChats(),
      'app-create-chat': AppCreateChat()
    },
    data: function data() {
      return {
        create_active: false,
        patient_data: patient_data,
        index: index_episodio,
        chats: [],
        fechas: [],
        form: {
          files: null,
          chat_observacion: "",
          chat_mensaje: "",
          chat_con: "paciente"
        }
      };
    },
    computed: {},
    methods: {
      chat_row: function chat_row() {
        var _this2 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2() {
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
            while (1) {
              switch (_context2.prev = _context2.next) {
                case 0:
                  _this2.fechas = Array.from(new Set(_this2.chats.map(function (x) {
                    return x.created_at.split(" ")[0];
                  })));
                  _this2.fechas = _this2.fechas.map(function (x) {
                    //console.log("----->",x)
                    var _x$split = x.split("-"),
                        _x$split2 = _slicedToArray(_x$split, 3),
                        anio = _x$split2[0],
                        mes = _x$split2[1],
                        dia = _x$split2[2]; //console.log(anio,mes,dia);


                    var fecha = dia + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(Number(mes) - 1).slice(0, 3) + ", " + anio;

                    var items = _this2.chats.filter(function (y) {
                      var fecha = y.created_at.split(" ")[0];

                      if (fecha === x) {
                        return y;
                      }
                    });

                    return {
                      fecha: fecha,
                      items: items
                    };
                  });

                case 2:
                case "end":
                  return _context2.stop();
              }
            }
          }, _callee2);
        }))();
      },
      handle_store: function handle_store() {
        var _this3 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3() {
          var formData, cant_files, i;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
            while (1) {
              switch (_context3.prev = _context3.next) {
                case 0:
                  if (!_this3.validate()) {
                    _context3.next = 19;
                    break;
                  }

                  formData = new FormData();
                  cant_files = document.getElementById("user_file").files.length; //console.log(cant_files);

                  if (cant_files > 0) {
                    for (i = 0; i < cant_files; i++) {
                      formData.append("files[]", document.getElementById("user_file").files[i]);
                    }
                  } else {
                    formData.append("files[]", null);
                  }

                  formData.append('cant_files', cant_files);
                  formData.append('chat_mensaje', _this3.form.chat_mensaje.trim());
                  formData.append('chat_con', _this3.form.chat_con);
                  formData.append('chat_observacion', _this3.form.chat_observacion.trim());
                  formData.append('user_id_paciente', _this3.patient_data.user_id);
                  formData.append('created_at', Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                  formData.append("_token", $("#_token").val());
                  _context3.next = 13;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_chat/store", formData);

                case 13:
                  _context3.next = 15;
                  return _this3.getChats();

                case 15:
                  _context3.next = 17;
                  return _this3.chat_row();

                case 17:
                  /* let miDivciente = document.getElementById('pills-paciente'); // Reemplaza 'miDiv' con el ID de tu div
                  let miDivmedico = document.getElementById('pills-medico'); // Reemplaza 'miDiv' con el ID de tu div
                      miDivciente.scrollIntoView({ behavior: 'smooth' });
                      miDivmedico.scrollIntoView({ behavior: 'smooth' }); */
                  $('a[href="#pills-' + _this3.form.chat_con + '"]').tab('show');
                  _this3.form = {
                    files: null,
                    chat_observacion: "",
                    chat_mensaje: "",
                    chat_con: "paciente"
                  };

                case 19:
                case "end":
                  return _context3.stop();
              }
            }
          }, _callee3);
        }))();
      },
      validate: function validate() {
        if (this.form.chat_mensaje == "") {
          alert("El campo Chat no puede estar vacio.");
          return false;
        }

        return true;
      },
      getChats: function getChats() {
        var _this4 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4() {
          var formdata;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
            while (1) {
              switch (_context4.prev = _context4.next) {
                case 0:
                  formdata = new FormData();
                  formdata.append("user_id_paciente", _this4.patient_data.user_id);
                  formdata.append("_token", $("#_token").val());
                  _context4.next = 5;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_chat/index", formdata);

                case 5:
                  _this4.chats = _context4.sent;

                case 6:
                case "end":
                  return _context4.stop();
              }
            }
          }, _callee4);
        }))();
      }
    },
    mounted: function mounted() {
      var _this5 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
          while (1) {
            switch (_context5.prev = _context5.next) {
              case 0:
                _context5.next = 2;
                return _this5.getChats();

              case 2:
                _context5.next = 4;
                return _this5.chat_row();

              case 4:
                userCuestPaciente.infoPaciente("#infoPaciente", _this5.patient_data.user_id);

              case 5:
              case "end":
                return _context5.stop();
            }
          }
        }, _callee5);
      }))();
    }
  });
};

var UserCuestChat = /*#__PURE__*/function () {
  function UserCuestChat() {
    _classCallCheck(this, UserCuestChat);
  }

  _createClass(UserCuestChat, [{
    key: "index",
    value: function () {
      var _index = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6(selector, user_id) {
        var formdata, response, chat_paciente, chat_medico, chat_row, paciente, medico, that;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
          while (1) {
            switch (_context6.prev = _context6.next) {
              case 0:
                formdata = new FormData();
                formdata.append("user_id_paciente", user_id);
                formdata.append("_token", $("#_token").val());
                _context6.next = 5;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_chat/index", formdata);

              case 5:
                response = _context6.sent;

                chat_row = function chat_row(response) {
                  var color = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "primary";
                  var fecha;
                  var tempFecha;
                  var f;
                  var chat = "";

                  if (response.length > 0) {
                    $.each(response, function (indexInArray, value) {
                      //console.log(response)
                      var image = "";
                      f = new Date(value.created_at);
                      fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();

                      if (tempFecha != fecha) {
                        chat += "\n                                <div class=\"time-label\">\n                                    <span class=\"bg-".concat(color, "\">\n                                        ").concat(fecha, "\n                                    </span>\n                                </div>\n                            ");
                        tempFecha = fecha;
                      }

                      var comentario = "";

                      if (value.coments != null && value.coments != '') {
                        comentario = "Observaciones: \n" + value.coments;
                      }

                      if (tempFecha == fecha) {
                        if (value.image != "undefined" && value.image != "" && value.image != "null" && value.image != null) {
                          $.each(JSON.parse(value.image), function (indexInArray, valueOfElement) {
                            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "jpg" || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "jpeg" || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "png" || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "bmp") {
                              image += "\n                                            <img onerror=\"this.src='/image/system/nofound.png';\" onclick=\"window.open('image/user/chatPaciente/".concat(valueOfElement, "', '_blank')\" style=\"cursor:pointer;width: 100px; height:100px;\" class=\"img-thumbnail ampliar zoom img-fluid\" src='image/user/chatPaciente/").concat(valueOfElement, "'>\n                                        ");
                            }

                            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "mp3" || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "ogg" || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "wav" || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "opus" || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "flac" || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) == "aac") {
                              image += "\n                                            <audio controls src='image/user/chatPaciente/".concat(valueOfElement, "'>").concat(valueOfElement, "</audio>\n                                        ");
                            }

                            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "jpg" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "jpeg" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "png" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "bmp" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "mp3" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "ogg" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "wav" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "opus" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "flac" && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension2"])(valueOfElement) != "aac") {
                              image += "\n                                            <img title=\"Formato de archivo no reconocido\" onclick=\"window.open('image/user/chatPaciente/".concat(valueOfElement, "', '_blank')\" style=\"cursor:pointer;width: 100px; height:100px;\" class=\"img-thumbnail ampliar zoom img-fluid\"  src='/image/system/nofound.png'>\n                                        ");
                            } //console.log(getFileExtension2(valueOfElement));

                          });
                        }

                        chat +=
                        /*html */
                        "\n                                <div>\n                                    <i\n                                        class=\"fas fa-comments bg-".concat(color, "\"\n                                        style=\"font-size: 1.2rem;color: white;\"\n                                    ></i>\n                                    <div class=\"timeline-item\">\n                                        <span class=\"time\">\n                                            <i class=\"far fa-clock\"></i>\n                                            ").concat(f.getHours() + ':' + f.getMinutes() + ':' + f.getSeconds(), "\n                                        </span>\n                                        <h3\n                                            data-toggle=\"collapse\"\n                                            href=\"#collapseExample0\"\n                                            role=\"button\"\n                                            aria-expanded=\"false\"\n                                            aria-controls=\"collapseExample\"\n                                            class=\"timeline-header\"\n                                        >\n                                            <b class=\"text-primary\">Dr ").concat(value.medico, "</b> envi\xF3 nuevo chat.\n                                        </h3>\n                                        <div id=\"collapseExample0\" class=\"collapse show timeline-body\">\n                                            ").concat(value.message != null ? value.message.replace(/\n/g, "<br />") : '', "\n\n").concat(comentario.replace(/\n/g, "<br />"), "\n                                            <br>\n                                            ").concat(image, "\n                                        </div>\n                                    </div>\n                                </div>\n                            ");
                      }
                    });
                  } else {
                    chat +=
                    /*html */
                    "\n                        <div>\n                            <i aria-hidden=\"true\" class=\"timelime-medic-icon fas fa-comments bg-success text-white\"></i>\n                            <div class=\"timeline-item\">\n                                <div class=\"timeline-body text-center text-".concat(color, " font-weight-bold\" style=\"font-size: 1.2rem;\">\n                                    No existe historial\n                                </div>\n                            </div>\n                        </div>\n                    ");
                  }

                  return chat;
                };

                paciente = response.filter(function (x) {
                  return x.chat_con === "paciente";
                });
                medico = response.filter(function (x) {
                  return x.chat_con === "medico";
                });
                chat_paciente = chat_row(paciente, "success");
                chat_medico = chat_row(medico, "primary");
                that = this;
                $("#modal-patient-item").modal("show");
                $("#modal-patient-item .modal-body-2").html(
                /*html */
                "\n                <div id=\"infoPaciente\"></div>\n                <div class=\"d-flex justify-content-between\">\n                    <h1 class=\"h1 text-primary\">\n                        Chat WhatsApp\n                    </h1>\n                    <span id=\"user_cuest_model_create\" onclick=\"that.create('.modal-body',".concat(user_id, ")\" class=\"float-right text-shadow-drop-tl text-primary text-right\" style=\"font-size: 2rem !important; cursor:pointer;\">\n                        Nuevo chat\n                        <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                    </span>\n                </div>\n                <ul class=\"nav nav-pills mb-3\" id=\"pills-tab\" role=\"tablist\">\n                    <li class=\"nav-item\" role=\"presentation\">\n                        <a class=\"nav-link h4 text-success active\" id=\"pills-paciente-tab\" data-toggle=\"pill\" href=\"#pills-paciente\" role=\"tab\" aria-controls=\"pills-paciente\" aria-selected=\"true\">Paciente</a>\n                    </li>\n                    <li class=\"nav-item\" role=\"presentation\">\n                        <a class=\"nav-link h4 text-primary\" id=\"pills-medico-tab\" data-toggle=\"pill\" href=\"#pills-medico\" role=\"tab\" aria-controls=\"pills-medico\" aria-selected=\"false\">M\xE9dico</a>\n                    </li>\n                </ul>\n\n                <div class=\"tab-content flex-fill overflow-auto mb-1\" id=\"nav-tabContent\">\n                    <div class=\"tab-pane fade show active\" id=\"pills-paciente\" role=\"tabpanel\" aria-labelledby=\"pills-paciente-tab\">\n                        <div style=\"/* height: 38vh;overflow: auto; */margin-bottom: 1em;\">\n                            <div class=\"container-fluid\">\n                                <div class=\"row\">\n                                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 text-primary\">\n                                        <div class=\"timeline timeline-inverse\">\n                                            <div>\n                                                ").concat(chat_paciente, "\n                                                <div>\n                                                    <i style=\"font-size: 1.2rem;\" class=\"far fa-clock bg-gray\"></i>\n                                                </div>\n                                            </div>\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"tab-pane fade\" id=\"pills-medico\" role=\"tabpanel\" aria-labelledby=\"pills-medico-tab\">\n                        <div style=\"/* height: 38vh;overflow: auto; */margin-bottom: 1em;\">\n                            <div class=\"container-fluid\">\n                                <div class=\"row\">\n                                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 text-primary\">\n                                        <div class=\"timeline timeline-inverse\">\n                                            <div>\n                                                ").concat(chat_medico, "\n                                                <div>\n                                                    <i style=\"font-size: 1.2rem;\" class=\"far fa-clock bg-gray\"></i>\n                                                </div>\n                                            </div>\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n                </div>\n                <div class=\"d-flex\">\n                    <button data-dismiss=\"modal\" aria-label=\"Close\"  class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n                </div>\n\n            "));
                userCuestPaciente.infoPaciente("#infoPaciente", user_id);

              case 15:
              case "end":
                return _context6.stop();
            }
          }
        }, _callee6, this);
      }));

      function index(_x, _x2) {
        return _index.apply(this, arguments);
      }

      return index;
    }()
  }, {
    key: "create",
    value: function create(selector, user_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                    Chat WhatsApp\n                </h1>\n                <span id=\"user_cuest_model_create\" onclick=\"this.create('.modal-body',".concat(user_id, ")\" class=\"float-right text-shadow-drop-tl text-primary text-right\" style=\"font-size: 2rem !important; cursor:pointer;\">\n                    Nuevo chat\n                    <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                </span>\n            </div>\n\n            <div class=\"form-group\">\n                <label class=\"text-secondary\">Chat con:</label>\n                <select class=\"form-control-lg border border-gray w-100 bg-light\" name=\"chat_con\" id=\"chat_con\">\n                    <option value=\"paciente\">Paciente</option>\n                    <option value=\"medico\">M\xE9dico</option>\n                </select>\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"chat_mensaje\" id=\"chat_mensaje\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Ingrese chat\"></textarea>\n            </div>\n            <div class=\"mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"chat_observacion\" id=\"chat_observacion\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n            <div class=\"mb-1\">\n                <input type=\"file\" style=\"height: calc(2.25rem + 8px);\" id=\"user_file").concat(user_id, "\" multiple class=\"form-control input-lg\">\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"font-weight-bold btn btn-success w-100\">Agregar</button>\n                <button id=\"regresar\" onclick=\"this.index('.modal-body',").concat(user_id, ")\" class=\"font-weight-bold btn btn-primary mb-1 w-100\">Regresar</button>\n            </div>\n\n        "));
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#user_cuest_model_store").on("click", function () {
        var _this6 = this;

        if (this.validate()) {
          this.store(user_id).then(function (response) {
            console.log("this:", response);

            _this6.index('.modal-body', user_id);
          });
        }
      });
    }
  }, {
    key: "store",
    value: function store(user_id) {
      var formData = new FormData();
      var cant_files = document.getElementById("user_file".concat(user_id)).files.length;

      for (var i = 0; i < cant_files; i++) {
        formData.append("files[]", document.getElementById("user_file".concat(user_id)).files[i]);
      }

      formData.append('cant_files', cant_files);
      formData.append('chat_mensaje', $("#chat_mensaje").val().trim());
      formData.append('chat_con', $("#chat_con").val());
      formData.append('chat_observacion', $("#chat_observacion").val().trim());
      formData.append('user_id_paciente', user_id);
      formData.append('created_at', Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formData.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_chat/store", formData);
    }
  }, {
    key: "validate",
    value: function validate() {
      var chat_mensaje = $("#chat_mensaje");

      if (chat_mensaje.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        chat_mensaje.trigger("focus");
        return false;
      }

      return true;
    }
  }]);

  return UserCuestChat;
}();

/***/ }),

/***/ "./public/class_js/UserCuestDireccion.js":
/*!***********************************************!*\
  !*** ./public/class_js/UserCuestDireccion.js ***!
  \***********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestDireccion; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _CatUserEstado_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./CatUserEstado.js */ "./public/class_js/CatUserEstado.js");
/* harmony import */ var _CatUserMunicipio_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./CatUserMunicipio.js */ "./public/class_js/CatUserMunicipio.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }




var catUserEstado = new _CatUserEstado_js__WEBPACK_IMPORTED_MODULE_2__["default"]();
var catUserMunicipio = new _CatUserMunicipio_js__WEBPACK_IMPORTED_MODULE_3__["default"]();

var UserCuestDireccion = /*#__PURE__*/function () {
  function UserCuestDireccion() {
    _classCallCheck(this, UserCuestDireccion);
  }

  _createClass(UserCuestDireccion, [{
    key: "index",
    value: function index(selector, user_id) {
      $("#modelId").modal("show");
      $(selector).html("\n            <div id=\"infoPaciente\"><div>\n            <div class=\"row mt-5\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <div>\n                        <h1 class=\"text-primary\">\n                            Temperatura <span id=\"info\" title=\"Ayuda\" class=\"c-pointer text-success heartbeat float-right h5\"><i class=\"fa fa-question-circle\" aria-hidden=\"true\"></i></span>\n                        </h1>\n                    </div>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <div id=\"user_cuest_temperatura_create\" class=\"text-shadow-drop-tl text-primary text-right\" style=\"font-size: 2rem !important; cursor:pointer;\">\n                        Nuevo valor\n                        <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                    </div>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <canvas id=\"chart_0\" style=\"height:40vh; width:100vw\"></canvas>\n                </div>h\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <div class=\"table-responsive\">\n                        <table class=\"table table-bordered mb-3\">\n                            <tr>\n                                <th class=\"text-primary text-center\">Fecha</th>\n                                <th class=\"text-primary text-center\">Valor</th>\n                                <th class=\"text-primary text-center\">Observaci\xF3n</th>\n                                <th class=\"text-primary text-center\">Episodio</th>\n                                <th class=\"text-primary text-center\">Acci\xF3n</th>\n                            </tr>\n                            <tbody id=\"filas\">\n                                <tr>\n                                    <td colspan=\"5\" class=\"text-center\">\n                                        <div class=\"spinner-border text-primary\" role=\"status\">\n                                            <span class=\"sr-only\">Loading...</span>\n                                        </div>\n                                    </td>\n                                </tr>\n                            </tbody>\n                        </table>\n                    </div>\n                </div>\n            </div>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <a class='btn bg-primary text-primary btn-block btn-lg' data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</a>\n                </div>\n            </div>\n        ");
      Paciente.InfoPaciente("#infoPaciente", user_id);
      var colorTemp = 1;
      var bordeGrafico = "#2FA600";
      var fondoGrafico = "#2fa60078";

      if (colorTemp == 1) {
        //verde
        bordeGrafico = "green";
        fondoGrafico = "#2fa60078";
      }

      if (colorTemp == 2) {
        //amarillo
        bordeGrafico = "green";
        fondoGrafico = "#2fa60078";
      }

      if (colorTemp == 3) {
        //rojo
        fondoGrafico = "#2FA600";
      }
      /*** */


      var data = {
        labels: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul"],
        datasets: [{
          label: "Dataset #1",
          backgroundColor: fondoGrafico,
          borderColor: bordeGrafico,
          borderWidth: 2,
          hoverBackgroundColor: "rgba(255,99,132,0.4)",
          hoverBorderColor: "rgba(255,99,132,1)",
          data: [65, 59, 20, 81, 56, 55, 40]
        }]
      };
      var option = {
        responsive: true,
        scales: {
          yAxes: [{
            stacked: true,
            gridLines: {
              display: true,
              color: "rgba(255,99,132,0.2)"
            }
          }],
          xAxes: [{
            gridLines: {
              display: false
            }
          }, {
            type: 'time',
            time: {
              displayFormats: {
                quarter: 'DD MMM YYYY'
              },
              tooltipFormat: 'DD [de] MMM [de] YYYY'
            },
            stacked: true,
            ticks: {
              fontSize: 10 //maxRotation: 50, // angle in degrees

            }
          }]
        },
        tooltips: {
          callbacks: {
            labelColor: function labelColor(tooltipItem, chart) {
              return {
                backgroundColor: 'rgba(60, 141, 188, 0.2)',
                borderColor: 'rgba(60, 141, 188,1)'
              };
            },
            label: function label(tooltipItem, data) {
              return '#: ' + tooltipItem.yLabel;
            }
          }
        },
        elements: {
          line: {
            tension: 0
          }
        },
        animation: {
          duration: 500 // general animation time

        },
        hover: {
          animationDuration: 500 // duration of animations when hovering an item

        },
        responsiveAnimationDuration: 500 // animation duration after a resize

      };
      Chart.Line('chart_0', {
        options: option,
        data: data
      });
      var that = this;
      /** */

      var formdata = new FormData();
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_temperatura/index", formdata).then(function (response) {
        var filas = "";
        var tempRow = "";
        var dia = "";
        var count = 0;
        var m = "";
        var d = "";

        if (response.length > 0) {
          $(response).each(function (index, element) {
            d = new Date(element.created_at);

            if (dia != d.getDate()) {
              dia = d.getDate();
              count++;
              m = count;
            } else {
              m = "";
            }

            tempRow = "\n                            <tr id=\"temp_".concat(element.id, "\">\n                                <td class=\"text-center\">").concat(jsUcfirst(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(d.getMonth())) + "/" + d.getDate() + " a las " + formatAMPM(d), "</td>\n                                <td class=\"text-center font-weight-bold ").concat(that.colorTemp(element.value), "\" id=\"modal-temp-").concat(element.id, "\">").concat(element.value, "\xB0C</td>\n                                <td class=\"text-center\">\n                                    ").concat(element.coments != null ? element.coments : "", "\n                                </td>\n                                <td class=\"text-center\"></td>\n                                <td align=\"center\">\n                                    <button title=\"Eliminar valor\" onclick=\"that.eliminar(").concat(element.id, ",").concat(user_id, ")\" class=\"delete-row btn btn-danger\" data-option=\"").concat(element.id, "\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button>\n                                </td>\n                            </tr>\n                        ");
            filas = filas + tempRow;
          });
        } else {
          filas = "\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center text-primary\">Sin registros</td>\n                        </tr>\n                    ";
        }

        $("#filas").html(filas);
      });
      $("#user_cuest_temperatura_create").on('click', function () {
        that.create(".modal-body", user_id);
      });
      $("#info").on("click", function () {
        that.help(selector, user_id);
      });
    }
  }, {
    key: "create",
    value: function () {
      var _create = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(selector) {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                $(selector).html("\n\n            <div class=\"row\">\n                <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"cat_estado_id\">Estado</label>\n                        <select class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"cat_estado_id\"\n                            id=\"cat_estado_id\" aria-describedby=\"helpId\" placeholder=\"\"></select>\n                        <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\"\n                            for=\"cat_municipio_id\">Municipio</label>\n                        <select class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"cat_municipio_id\"\n                            id=\"cat_municipio_id\" aria-describedby=\"helpId\" placeholder=\"\"></select>\n                        <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"description\">Ciudad</label>\n                        <input type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\"\n                            name=\"description\" id=\"description\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                    </div>\n                </div>\n            </div>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"domicilio\">Domicilio</label>\n                        <textarea  class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"domicilio\" id=\"domicilio\" rows=\"2\"></textarea>\n                        <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                    </div>\n                </div>\n            </div>\n\n        ");
                catUserEstado.getAll().then(function (response) {
                  $("#cat_estado_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(response));
                  return response[0].id;
                }).then(function (response) {
                  catUserMunicipio.show(response).then(function (response) {
                    $("#cat_municipio_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(response));
                  });
                });
                $("#cat_estado_id").on("change", function () {
                  //loadingModal("show")
                  catUserMunicipio.show($(this).val()).then(function (response) {
                    $("#cat_municipio_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(response));
                    return response;
                  }).then(function (response) {// $("#messageModal").modal('hide')
                    //loadingModal("hide")
                  });
                });

              case 3:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function create(_x) {
        return _create.apply(this, arguments);
      }

      return create;
    }()
  }, {
    key: "store",
    value: function () {
      var _store = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2(user_id) {
        var message, formdata, cat_estado_id, cat_municipio_id, description, domicilio;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
          while (1) {
            switch (_context2.prev = _context2.next) {
              case 0:
                formdata = new FormData();
                cat_estado_id = $("#cat_estado_id");
                cat_municipio_id = $("#cat_municipio_id");
                description = $("#description");
                domicilio = $("#domicilio");
                formdata.append("cat_estado_id", cat_estado_id.val());
                formdata.append("cat_municipio_id", cat_municipio_id.val());
                formdata.append("description", description.val());
                formdata.append("domicilio", domicilio.val());
                formdata.append("user_id", user_id);
                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                formdata.append("_token", $("#_token").val());
                _context2.next = 14;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_direccion/store", formdata);

              case 14:
                return _context2.abrupt("return", _context2.sent);

              case 15:
              case "end":
                return _context2.stop();
            }
          }
        }, _callee2);
      }));

      function store(_x2) {
        return _store.apply(this, arguments);
      }

      return store;
    }()
  }, {
    key: "update",
    value: function update(user_id) {
      var message;
      var formdata = new FormData();
      var cat_estado_id = $("#cat_estado_id");
      var cat_municipio_id = $("#cat_municipio_id");
      var description = $("#description");
      var domicilio = $("#domicilio");
      formdata.append("cat_estado_id", cat_estado_id.val());
      formdata.append("cat_municipio_id", cat_municipio_id.val());
      formdata.append("description", description.val());
      formdata.append("domicilio", domicilio.val());
      formdata.append("user_id", user_id);
      formdata.append("updated_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_direccion/update", formdata);
    }
  }, {
    key: "destroy",
    value: function destroy(id) {
      var formdata = new FormData();
      formdata.append("id", id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_temperatura/destroy", formdata);
    }
  }, {
    key: "show",
    value: function show(user_id) {
      var formdata = new FormData();
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/userDirection/show", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var that = this;
      var message = component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        that.destroy(row_id).then(function (response) {
          var temp = $("#value").val();

          if (response.length > 0) {
            that.columnaValor("#col_6_".concat(user_id), {
              'user_id': user_id,
              'valor': response[0].value
            });
          } else {
            $("#modelId").modal("hide");
            that.columnaValor("#col_6_".concat(user_id), {
              'user_id': user_id,
              'valor': null
            });
          }

          that.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "colorTemp",
    value: function colorTemp(temp) {
      //$(selector).removeClass(["text-secondary", "text-cyan", "text-success", "text-danger"])
      if (temp <= 30.4) {
        return "text-cyan";
      } else if (temp >= 30.5 && temp <= 37.5) {
        return "text-success";
      } else if (temp >= 37.6 && temp <= 37.9) {
        return "text-warning";
      } else if (temp >= 38) {
        return "text-danger";
      } else {}
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      $(selector).html("\n            <div onclick=\"this.index('.modal-body',".concat(data.user_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    TEMP\n                </div>\n                ").concat(data.valor == "" || data.valor == null ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + this.colorTemp(data.valor) + "'>" + data.valor + "°C</span>", "\n            </div>\n        "));
    }
  }, {
    key: "help",
    value: function help(selector, user_id) {
      var that = this;
      $(selector).html("\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <h1 class=\"text-primary\">\n                        Manual operativo de usuario\n                    </h1>\n                </div>\n            </div>\n            <br>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <object id=\"pdfHolder\" data=\"/help/temperatura.pdf\" type=\"application/pdf\" style=\"width:100%;height:500px;\">\n                        <a title=\"Pulse para descargar\" href=\"/help/temperatura.pdf\" download ><img src=\"/image/system/icono_archivo2.svg\" ></a>\n                    </object>\n                </div>\n            </div>\n            <br>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <button id=\"regresar\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                </div>\n            </div>\n\n        ");
      $("#regresar").on("click", function () {
        that.index(selector, user_id);
      });
    }
  }, {
    key: "validate",
    value: function validate() {
      var cat_estado_id = $("#cat_estado_id");
      var cat_municipio_id = $("#cat_municipio_id");
      var description = $("#description");
      var domicilio = $("#domicilio");

      if (location.pathname !== "/medico") {
        if (cat_estado_id.val() == "") {
          message = Component.alertMessage("input_select_empty");
          Toast.fire({
            icon: message['image'],
            title: message['title'],
            text: message['message'],
            didClose: function didClose() {
              setTimeout(function () {
                cat_estado_id.trigger("focus");
              }, 110);
            }
          }); //alert(message['message'])
          //cat_estado_id.trigger("focus")

          return false;
        }

        if (cat_municipio_id.val() == "") {
          message = Component.alertMessage("input_select_empty");
          Toast.fire({
            icon: message['image'],
            title: message['title'],
            text: message['message'],
            didClose: function didClose() {
              setTimeout(function () {
                cat_municipio_id.trigger("focus");
              }, 110);
            }
          }); //alert(message['message'])
          //cat_municipio_id.trigger("focus")

          return false;
        }

        if (description.val() == "") {
          message = Component.alertMessage("input_text_empty");
          Toast.fire({
            icon: message['image'],
            title: message['title'],
            text: message['message'],
            didClose: function didClose() {
              setTimeout(function () {
                description.trigger("focus");
              }, 110);
            }
          }); //alert(message['message'])
          //description.trigger("focus")

          return false;
        }

        if (domicilio.val() == "") {
          message = Component.alertMessage("input_text_empty");
          Toast.fire({
            icon: message['image'],
            title: message['title'],
            text: message['message'],
            didClose: function didClose() {
              setTimeout(function () {
                domicilio.trigger("focus");
              }, 110);
            }
          }); //alert(message['message'])
          //domicilio.trigger("focus")

          return false;
        }
      }

      return true;
    }
  }, {
    key: "infoPaciente",
    value: function infoPaciente(selector, user_id) {
      $(selector).html("\n            <div class=\"container\">\n                <nav class=\"navbar navbar-expand-lg m-0 navbar-light w-100 bg-light position-absolute\" style=\"left:0;top:0\">\n                <div class=\"table-responsive\">\n                    <table class=\" slide-in-right\" style=\"width: 1100px !important;\">\n                        <tr>\n                            <td class=\"text-gray pl-2 pr-2\">\n                                <b>Paciente:</b> Ranses Jimenez Dominguez <br>\n                                <b>C\xE9dula:</b> 22.014.778\n                            </td>\n                            <td class=\"text-gray pl-2 pr-2\">\n                                <b>Edad:</b> 36 a\xF1os <br>\n                                <b>Sexo:</b> M\n                            </td>\n                            <td class=\"text-gray pl-2 pr-2\">\n                                <b>Ingreso:</b> 22/03/2021 <br>\n                                <b>Episodio:</b> No indicado\n                            </td>\n                            <td class=\"text-gray pl-2 pr-2\">\n                                <b>M\xE9dico Tratante:</b> Dr. Luis Eduardo Parodi Arrieta <br>\n                                <b>Especialidad:</b> Alergia, Asma e Inmunolog\xEDa\n                            </td>\n                        </tr>\n                    </table>\n                </div>\n                </nav>\n            </div>\n        ");
    }
  }]);

  return UserCuestDireccion;
}();



/***/ }),

/***/ "./public/class_js/UserCuestEpisodio.js":
/*!**********************************************!*\
  !*** ./public/class_js/UserCuestEpisodio.js ***!
  \**********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestEpisodio; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPendiente_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./UserCuestPendiente.js */ "./public/class_js/UserCuestPendiente.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }



var userCuestPendiente = new _UserCuestPendiente_js__WEBPACK_IMPORTED_MODULE_2__["default"]();

var onOffBlueCode = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(user_id) {
    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
      while (1) {
        switch (_context.prev = _context.next) {
          case 0:
            if ($("#blue_code_".concat(user_id)).val() == 1) {
              Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/user_cuest_episodio/blue_code/" + user_id + "/0").then(function (response) {
                $("#blue_code_".concat(user_id)).val(0);
                $("#ico_blue_code_".concat(user_id)).css("color", "var(--danger)");
                $("#ico_blue_code_".concat(user_id)).removeClass("pc-fa-blue-code-on");
                $("#ico_blue_code_".concat(user_id)).addClass("pc-fa-blue-code-off");
                $("#ico_blue_code_".concat(user_id)).attr('title', "Código azul DESACTIVADO");
              });
            } else {
              Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/user_cuest_episodio/blue_code/" + user_id + "/1").then(function (response) {
                $("#blue_code_".concat(user_id)).val(1);
                $("#ico_blue_code_".concat(user_id)).css("color", "var(--primary)");
                $("#ico_blue_code_".concat(user_id)).removeClass("pc-fa-blue-code-off");
                $("#ico_blue_code_".concat(user_id)).addClass("pc-fa-blue-code-on");
                $("#ico_blue_code_".concat(user_id)).attr('title', "Código azul ACTIVADO");
              });
            }

          case 1:
          case "end":
            return _context.stop();
        }
      }
    }, _callee);
  }));

  return function onOffBlueCode(_x) {
    return _ref.apply(this, arguments);
  };
}();

var listGoup = function listGoup(_ref2) {
  var _ref2$eventEliminar = _ref2.eventEliminar,
      eventEliminar = _ref2$eventEliminar === void 0 ? "" : _ref2$eventEliminar,
      _ref2$eventUpdateFiel = _ref2.eventUpdateField,
      eventUpdateField = _ref2$eventUpdateFiel === void 0 ? "" : _ref2$eventUpdateFiel,
      _ref2$eventCompletar = _ref2.eventCompletar,
      eventCompletar = _ref2$eventCompletar === void 0 ? "" : _ref2$eventCompletar,
      _ref2$created_at = _ref2.created_at,
      created_at = _ref2$created_at === void 0 ? "00/00/000" : _ref2$created_at,
      _ref2$description = _ref2.description,
      description = _ref2$description === void 0 ? "Description" : _ref2$description,
      _ref2$tagDescription = _ref2.tagDescription1,
      tagDescription1 = _ref2$tagDescription === void 0 ? true : _ref2$tagDescription,
      _ref2$tagDescription2 = _ref2.tagDescription2,
      tagDescription2 = _ref2$tagDescription2 === void 0 ? false : _ref2$tagDescription2,
      _ref2$description2 = _ref2.description2,
      description2 = _ref2$description2 === void 0 ? "" : _ref2$description2,
      _ref2$titleImg = _ref2.titleImg,
      titleImg = _ref2$titleImg === void 0 ? "https://placeimg.com/30/30/any" : _ref2$titleImg,
      _ref2$destroy = _ref2.destroy,
      destroy = _ref2$destroy === void 0 ? true : _ref2$destroy,
      _ref2$completar = _ref2.completar,
      completar = _ref2$completar === void 0 ? false : _ref2$completar,
      _ref2$edit = _ref2.edit,
      edit = _ref2$edit === void 0 ? false : _ref2$edit,
      _ref2$especialidad = _ref2.especialidad,
      especialidad = _ref2$especialidad === void 0 ? "Especialidad no indicada" : _ref2$especialidad,
      _ref2$hora = _ref2.hora,
      hora = _ref2$hora === void 0 ? "00:00 PM" : _ref2$hora,
      _ref2$id = _ref2.id,
      id = _ref2$id === void 0 ? 0 : _ref2$id,
      _ref2$posicion = _ref2.posicion,
      posicion = _ref2$posicion === void 0 ? "Posición no indicada" : _ref2$posicion,
      _ref2$posicionColor = _ref2.posicionColor,
      posicionColor = _ref2$posicionColor === void 0 ? "No indicado" : _ref2$posicionColor,
      _ref2$tagArchivo = _ref2.tagArchivo,
      tagArchivo = _ref2$tagArchivo === void 0 ? false : _ref2$tagArchivo,
      _ref2$tagColor = _ref2.tagColor,
      tagColor = _ref2$tagColor === void 0 ? "primary" : _ref2$tagColor,
      _ref2$tagTitleImg = _ref2.tagTitleImg,
      tagTitleImg = _ref2$tagTitleImg === void 0 ? false : _ref2$tagTitleImg,
      _ref2$tagDescription3 = _ref2.tagDescription,
      tagDescription = _ref2$tagDescription3 === void 0 ? false : _ref2$tagDescription3,
      _ref2$tagEspecialidad = _ref2.tagEspecialidad,
      tagEspecialidad = _ref2$tagEspecialidad === void 0 ? false : _ref2$tagEspecialidad,
      _ref2$tagPosicion = _ref2.tagPosicion,
      tagPosicion = _ref2$tagPosicion === void 0 ? false : _ref2$tagPosicion,
      _ref2$tagTelefono = _ref2.tagTelefono,
      tagTelefono = _ref2$tagTelefono === void 0 ? false : _ref2$tagTelefono,
      _ref2$tagTime = _ref2.tagTime,
      tagTime = _ref2$tagTime === void 0 ? false : _ref2$tagTime,
      _ref2$tagTitle = _ref2.tagTitle,
      tagTitle = _ref2$tagTitle === void 0 ? false : _ref2$tagTitle,
      _ref2$telefono = _ref2.telefono,
      telefono = _ref2$telefono === void 0 ? "Teléfono no indicado" : _ref2$telefono,
      _ref2$tipoArchivo = _ref2.tipoArchivo,
      tipoArchivo = _ref2$tipoArchivo === void 0 ? null : _ref2$tipoArchivo,
      _ref2$registromedico = _ref2.registromedico,
      registromedico = _ref2$registromedico === void 0 ? "" : _ref2$registromedico,
      _ref2$title = _ref2.title,
      title = _ref2$title === void 0 ? "Sample Sample Sample" : _ref2$title,
      _ref2$model = _ref2.model,
      model = _ref2$model === void 0 ? 'sample' : _ref2$model;
  return "\n\n            <li id=\"row_".concat(id, "_").concat(model, "\" class=\"list-group-item p-0\">\n                <div class=\"d-flex flex-row  justify-content-between\">\n                    <div class=\"p-2 bd-highlight\" style=\"width: 90%;\">\n                        <!--Titulo tarjeta-->\n                        <div class=\"d-inline-flex align-middle\">\n                            <div class=\"").concat(tagTitleImg ? '' : 'd-none', " pr-2\">\n                                <img src=\"").concat(location.origin).concat(titleImg, "\" style=\"width: 2em;height: 2em;\" alt=\"foto medico\" onerror=\"this.src = '/image/system/medic.svg'\" class=\"rounded-circle p-0 img-thumbnail\">\n                            </div>\n                            <div class=\"").concat(tagTitle ? '' : 'd-none', " text-").concat(tagColor, " font-weight-bold\">\n                                ").concat(title, "\n                            </div>\n                        </div>\n\n                        <!--campo-valor tarjeta-->\n                        <div id=\"update_").concat(id, "_").concat(model, "\" style=\"display:none\">\n                            <textarea id=\"input_").concat(id, "_").concat(model, "\" rows=\"10\" class=\"form-control\" style=\"color:var(--primary) !important\">").concat(description, "</textarea>\n                            <button onclick=\"").concat(eventUpdateField, "\" class=\"btn btn-primary mt-1 btn-block btn-sm\">Actualizar</button>\n                        </div>\n\n                        <!--descripcion tarjeta-->\n                        <div id=\"description_").concat(id, "_").concat(model, "\" class=\"").concat(tagDescription ? '' : 'd-none', "\" style=\"font-size:1em\" >\n                            <div class=\"").concat(tagDescription1 ? '' : 'd-none', "\">").concat(description, "</div>\n                            <div class=\"").concat(tagDescription2 ? '' : 'd-none', "\">").concat(description2, "</div>\n                        </div>\n\n                        <!--archivos-->\n                        <div class=\"").concat(tagArchivo ? '' : 'd-none', "\">\n                            ").concat(formatImage(description, tipoArchivo), "\n                        </div>\n\n                        <!--dia y fecha-->\n                        <div class=\"").concat(tagTime ? '' : 'd-none', " float-right text-primary\" style=\"font-size:0.9em\">\n                            <span data-tippy-content=\"").concat(registromedico, "\" class=\"badge badge-gray tooltip-primary text-primary\">\n                                <i class=\"far fa-clock\"></i> ").concat(created_at, " <span class=\"text-primary\">|</span> ").concat(hora, "\n                            </span>\n                        </div>\n\n                        <!--etiquetas-->\n                        <div>\n                            <span class=\"").concat(tagPosicion ? '' : 'd-none', " badge-custom tooltip-").concat(posicionColor, " badge-pill badge-").concat(posicionColor, "\"  data-tippy-content=\"Posici\xF3n o cargo: ").concat(posicion, "\">").concat(posicion, "</span>\n\n                            <span class=\"").concat(tagEspecialidad ? '' : 'd-none', " badge-custom tooltip-primary badge-pill badge-primary\"  data-tippy-content=\"Especialidad: ").concat(especialidad, "\">").concat(especialidad, "</span>\n\n                            <button class=\"").concat(tagTelefono ? '' : 'd-none', " btn btn-outline-primary btn-small-custom tooltip-primary\"  data-tippy-content=\"Tel\xE9fono contacto del m\xE9dico: ").concat(telefono, "\" onclick=\"window.open('https://api.whatsapp.com/send?phone=").concat(telefono, "')\"><i class=\"fab fa-whatsapp text-success\"></i> ").concat(telefono, "</button>\n                            </span>\n                        </div>\n\n                    </div>\n\n                    <div class=\"p-2 bd-highlight\">\n                        <!--<i class=\"").concat(edit ? '' : 'd-none', " tooltip-primary fas fa-edit text-primary heartbeat cursor-pointer\"  onclick=\"showHide('#update_").concat(id, "_").concat(model, "');showHide('#description_").concat(id, "_").concat(model, "')\"  data-tippy-content=\"Editar\"></i>\n                        <i onclick=\"").concat(eventEliminar, "\" class=\"").concat(destroy ? '' : 'd-none', " tooltip-danger fas fa-minus-square text-danger heartbeat cursor-pointer\" data-tippy-content=\"Eliminar\"></i>-->\n                        <i onclick=\"").concat(eventCompletar, "\" class=\"").concat(completar ? '' : 'd-none', " tooltip-success fas fa-clipboard-check text-success heartbeat cursor-pointer\" data-tippy-content=\"Completar\"></i>\n                    </div>\n                </div>\n            </li>\n\n    ");
};

var formatImage = function formatImage(url, tipoArchivo) {
  switch (tipoArchivo) {
    case 'jpeg':
      return "<a href=\"/".concat(url, "\" target=\"_black\"><img src=\"").concat(location.origin, "/").concat(url, "\" style=\"width:4em;height:4em\" class=\"img-fluid heartbeat cursor-pointer\" onerror=\"this.src='https://via.placeholder.com/150'\"></a>");
      break;

    case 'jpg':
      return "<a href=\"/".concat(url, "\" target=\"_black\"><img src=\"").concat(location.origin, "/").concat(url, "\" style=\"width:4em;height:4em\" class=\"img-fluid heartbeat cursor-pointer\" onerror=\"this.src='https://via.placeholder.com/150'\"></a>");
      break;

    case 'png':
      return "<a href=\"/".concat(url, "\" target=\"_black\"><img src=\"").concat(location.origin, "/").concat(url, "\" style=\"width:4em;height:4em\" class=\"img-fluid heartbeat cursor-pointer\" onerror=\"this.src='https://via.placeholder.com/150'\"></a>");
      break;

    case 'pdf':
      return "<a href=\"/".concat(url, "\" target=\"_black\"><img src=\"").concat(location.origin, "/image/system/icono_archivo.svg\" style=\"width:4em;height:4em\" class=\"img-fluid heartbeat cursor-pointer\" onerror=\"this.src='https://via.placeholder.com/150'\"></a>");
      break;

    case null:
      return "";
      break;

    default:
      return "<a href=\"/".concat(url, "\" target=\"_black\"><img src=\"").concat(location.origin, "/image/system/icono_archivo.svg\" style=\"width:4em;height:4em\" class=\"img-fluid heartbeat cursor-pointer\" onerror=\"this.src='https://via.placeholder.com/150'\"></a>");
      break;
  }
};

var UserCuestEpisodio = /*#__PURE__*/function () {
  function UserCuestEpisodio() {
    _classCallCheck(this, UserCuestEpisodio);
  }

  _createClass(UserCuestEpisodio, null, [{
    key: "modelTitle",
    value: //static modelTitle ="Episodio";
    //static classModel ="UserCuestEpisodio";
    //static prefixRoute = "user_cuest_episodio";
    function modelTitle() {
      return "Episodio";
    }
  }, {
    key: "classModel",
    value: function classModel() {
      return "UserCuestEpisodio";
    }
  }, {
    key: "prefixRoute",
    value: function prefixRoute() {
      return "user_cuest_episodio";
    }
  }, {
    key: "index",
    value: function index(_ref3) {
      var _ref3$selector = _ref3.selector,
          selector = _ref3$selector === void 0 ? ".modal-body" : _ref3$selector,
          user_id = _ref3.user_id;
      //incio component
      var model = UserCuestEpisodio.classModel();
      Component.modalTemplate_1({
        //title: Nombre del modelo que se esta mostrando
        title: UserCuestEpisodio.modelTitle(),
        //selector: identificador html en donde se va a pintar el componente
        selector: selector,
        //user_id: id del usuario que se quiere visualizar
        user_id: user_id,
        //eventModelCreate: evento a ejecutar al pulsar en el botón Nuevo valor
        //OJO: se le pasa un string, porque la funcion eval, es la que determinará a que modelo nos estamos refiriendo.
        eventModelCreate: model + ".create({user_id:" + user_id + "})",
        //eventModelContent: contenido del modelo que se esta mostrando
        eventModelContent: "Component.modelIndex_1({user_id:" + user_id + ",columName:['" + UserCuestEpisodio.modelTitle() + "'],classModel:'" + model + "'})",
        //eventModelFooter: opciones del modelo que se esta mostrando
        eventModelFooter: "UserCuestHistoria.regresar({user_id:" + user_id + "})" //si se está en la otra vista que no sea index,
        //se le pasa el parametro btn_create:false
        //para que no muestre el botón de crear

      }, function () {}); //fin component
    }
  }, {
    key: "indexEpisodioCIE11",
    value: function indexEpisodioCIE11(_ref4) {
      var _ref4$selector = _ref4.selector,
          selector = _ref4$selector === void 0 ? ".modal-body" : _ref4$selector,
          user_id = _ref4.user_id;
      //incio component
      var model = UserCuestEpisodio.classModel();
      Component.modalTemplate_1({
        //title: Nombre del modelo que se esta mostrando
        title: "Clasificador CIE-11 <br><span class='h4'>Egresos sin clasificar</span>",
        //selector: identificador html en donde se va a pintar el componente
        selector: selector,
        //user_id: id del usuario que se quiere visualizar
        user_id: user_id,
        //eventModelCreate: evento a ejecutar al pulsar en el botón Nuevo valor
        //OJO: se le pasa un string, porque la funcion eval, es la que determinará a que modelo nos estamos refiriendo.
        eventModelCreate: model + ".create({user_id:" + user_id + "})",
        //eventModelContent: contenido del modelo que se esta mostrando
        //eventModelContent: "Component.modelIndex_2({tableId:'"+model+"table',user_id:"+user_id+",columName:['Paciente','Ingreso', 'Egreso'],classModel:'"+model+"'})",
        //eventModelFooter: opciones del modelo que se esta mostrando
        eventModelFooter: "Component.btn_closeModal({})",
        //btn_create:false  oculta el boton
        btn_create: false,
        datatable: true,
        headerPaciente: false //si se está en la otra vista que no sea index,
        //se le pasa el parametro btn_create:false
        //para que no muestre el botón de crear

      }, function () {
        var columName = ['Paciente', 'Cédula', 'Ingreso', 'Egreso'];
        Component.modelIndex_2({
          tableId: model + 'table',
          user_id: user_id,
          columName: columName,
          datatablereportes: true
        }, function () {
          eval(model + ".episodioPendiente()").then(function (response) {
            cl(response);
            var filas = "";

            if (Object.keys(response).length > 0) {
              var tempRow = "";
              $(response).each(function (index, element) {
                tempRow = "\n                                    <tr id=\"temp_".concat(element.id, "\">\n\n                                        <td class=\"text-secondary\" >\n                                            ").concat(element.paciente, "\n                                        </td>\n                                        <td class=\"text-secondary\" >\n                                            ").concat(element.cedula, "\n                                            <div class=\"d-none\">\n                                                ").concat(element.cedula_original, "\n                                            </div>\n                                        </td>\n                                        <td class=\"text-secondary text-center\" >\n                                            ").concat(element.ingreso, "\n                                        </td>\n                                        <td class=\"text-secondary text-center\" >\n                                            ").concat(element.egreso, "\n                                        </td>\n\n                                        <td style=\"width: 60px;\" align=\"center\">\n                                            <a data-tippy-content=\"Clasificaci\xF3n CIE-11\" target=\"_blank\" class=\"btn tooltip-primary btn-light\" onclick=\"window.open('/clasificador_cie11/").concat(element.user_id, "/").concat(element.id, "')\" ><i class=\"fas fa-book-medical text-primary\"></i></a>\n                                        </td>\n                                    </tr>\n                                ");
                filas = filas + tempRow;
              });
            } else {
              filas = "\n                                <tr>\n                                    <td colspan=\"".concat(columName.length + 1, "\" class=\"text-center text-primary\">Sin registros</td>\n                                </tr>\n                            ");
            }

            $("#filas").html(filas);
            $(".delete-row").on("click", function () {
              var c = confirm("¿Seguro que desea eliminar este registro?");

              if (c) {
                var id = $(this).data('id');
                eval(classModel() + ".eliminar({id:" + id + ",user_id:" + user_id + "})");
              }
            });
            Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["activarTooltip"])();
            $("#".concat(model + 'table')).DataTable({
              dom: 'Bfrtip',
              buttons: ['excel', 'pdf']
              /*'copy','csv','print'*/

            });
          });
        });
      }); //fin component
    }
  }, {
    key: "indexEntregaGuardia",
    value: function indexEntregaGuardia(_ref5) {
      var _ref5$selector = _ref5.selector,
          selector = _ref5$selector === void 0 ? ".modal-body" : _ref5$selector,
          user_id = _ref5.user_id;
      //incio component
      var model = UserCuestEpisodio.classModel();
      Component.modalTemplate_1({
        //title: Nombre del modelo que se esta mostrando
        title: "Entrega de guardia <br><span class='h4'></span>",
        //selector: identificador html en donde se va a pintar el componente
        selector: selector,
        //user_id: id del usuario que se quiere visualizar
        user_id: user_id,
        //eventModelCreate: evento a ejecutar al pulsar en el botón Nuevo valor
        //OJO: se le pasa un string, porque la funcion eval, es la que determinará a que modelo nos estamos refiriendo.
        //eventModelCreate: model+".menuEntregaGuardia({user_id:"+user_id+"})",
        //eventModelContent: contenido del modelo que se esta mostrando
        //eventModelContent: "Component.modelIndex_2({tableId:'"+model+"table',user_id:"+user_id+",columName:['Paciente','Ingreso', 'Egreso'],classModel:'"+model+"'})",
        //eventModelFooter: opciones del modelo que se esta mostrando
        eventModelFooter: "Component.btn_closeModal({})",
        //btn_create:false  oculta el boton
        btn_create: false,
        datatable: true,
        headerPaciente: false //si se está en la otra vista que no sea index,
        //se le pasa el parametro btn_create:false
        //para que no muestre el botón de crear

      }, function () {
        UserCuestEpisodio.menuEntregaGuardia({});
      }); //fin component
    }
  }, {
    key: "menuEntregaGuardia",
    value: function menuEntregaGuardia(_ref6) {
      var _ref6$selector = _ref6.selector,
          selector = _ref6$selector === void 0 ? '.modal-body' : _ref6$selector;
      var area = [{
        title: "TP",
        evento: "redireccionar('tp')"
      }, {
        title: "EA",
        evento: "redireccionar('ea')"
      }, {
        title: "ECVD",
        evento: "redireccionar('ecvd')"
      }, {
        title: "EP",
        evento: "redireccionar('ep')"
      }, //{title:"AQ",evento:`redireccionar('aq')`},
      //{title:"HP2", evento:`redireccionar('hp2')`},
      {
        title: "HP3",
        evento: "redireccionar('hp3')"
      }, {
        title: "HP4",
        evento: "redireccionar('hp4')"
      }, {
        title: "HP5",
        evento: "redireccionar('hp5')"
      }, {
        title: "HP6",
        evento: "redireccionar('hp6')"
      }, {
        title: "UTIA",
        evento: "redireccionar('utia')"
      }, {
        title: "UTICVD",
        evento: "redireccionar('uticvd')"
      }, {
        title: "UTIP",
        evento: "redireccionar('utip')}"
      }, {
        title: "PAD",
        evento: "redireccionar('pad')"
      }];
      var grupoCard = "";
      area.forEach(function (x) {
        grupoCard += "\n                <div class=\"col-xs-4 col-md-4 col-lg-4 mb-4\">\n                    ".concat(customCard({
          title: x.title,
          evento: x.evento
        }), "\n                </div>\n            ");
      });
      console.log(area);
      $(selector).html("\n            <div>\n                <h1 class=\"text-primary text-center\">\n                    Seleccione un \xE1rea\n                </h1>\n            </div>\n            <div class=\"row row-cols-sm-3 row-cols-md-3 row-cols-lg-3 row-cols-xl-3 justify-content-center\" style=\"max-height: 60vh;overflow: auto;\" >\n                ".concat(grupoCard, "\n            </div>\n\n            <div class=\"botones-bottom-modal\">\n                <div>\n                    <a class='btn bg-primary text-primary btn-block btn-lg' data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</a>\n                </div>\n            </div>\n        "));
    }
  }, {
    key: "listadoEntrega",
    value: function listadoEntrega(_ref7) {
      var _ref7$selector = _ref7.selector,
          selector = _ref7$selector === void 0 ? '#filasPacientes' : _ref7$selector,
          _ref7$area_id = _ref7.area_id,
          area_id = _ref7$area_id === void 0 ? [] : _ref7$area_id,
          pacientes_datos = _ref7.pacientes_datos;
      // console.clear()
      $("#cargando").css("display", "flex");
      $("#example,#sinresultados").css("display", "none");
      $("#total_paciente").html(0);

      var configDatatable = function configDatatable() {
        table = $('#example').DataTable({
          dom: '<"dom_wrapper fh-fixedHeader" Bf>tip',
          //responsive: true,
          //scrollY:        '73vh',
          //searching: false,
          //ordering: false,
          order: [0, 'asc'],
          buttons: [{
            extend: "excel",
            className: "d-none"
          }, {
            extend: "pdf",
            className: "d-none"
          }, {
            extend: "print",
            className: "d-none"
          }],
          bInfo: false,
          scrollY: "71.2vh",
          scrollX: true,
          //fixedColumns:   {
          //    left: 1
          //},
          //scrollCollapse: true,
          paging: false
        });
        $('a.toggle-vis').on('click', function (e) {//e.preventDefault();
          //showHide(".DTFC_Cloned")
          // Get the column API object
          //var column = table.column( $(this).attr('data-column') );
          // Toggle the visibility
          //column.visible( ! column.visible() );
        });
        $('#search').on('change', function (param) {
          table.search($(this).val()).draw();
        });
      };

      var filas = " ";
      $(selector).html(filas);
      setTimeout(function () {
        var model = [];
        console.log(area_id);

        if (area_id.length > 0) {
          area_id.forEach(function (y, index) {
            pacientes_datos.forEach(function (x) {
              if (x.parent_cat_user_ubicacion_id === y) {
                model.push(x);
              }
            });
          });
        } else {
          model = pacientes_datos;
        }

        if (model.length > 0) {
          filas = "";
          model.forEach(function (element) {
            var listadoMed = "<ul class=\"list-group list-group-flush\" style=\"width:20em;\">";
            element.medico_paciente.forEach(function (x) {
              listadoMed += listGoup({
                /*
                    Tarjeta medico:
                    Nombre, especialidad, telefono, posicion
                */
                eventEliminar: "UserCuestMedicoPaciente.eliminar(".concat(x.id, ",").concat(element.user_id, ")"),
                tagTitle: true,
                tagTitleImg: true,
                titleImg: x.medico_img,
                title: x.medico,
                tagPosicion: true,
                posicion: x.posicion,
                tagEspecialidad: true,
                especialidad: x.especialidad,
                tagTelefono: true,
                telefono: x.telefono_movil,
                id: x.id,
                posicionColor: x.tagColor,
                model: 'medico'
              });
            });
            listadoMed += "</ul>";
            var listadoImpDiagn = "<ul class=\"list-group list-group-flush\" style=\"width:20em;\">";
            element.resultados.forEach(function (x) {
              var f = new Date(x.created_at);
              var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();

              if (x.description === "Probabilidad diagnóstica") {
                listadoImpDiagn += listGoup({
                  edit: true,
                  tagTitle: true,
                  title: "Probabilidad Diagnóstica",
                  tagDescription: true,
                  description: x.value,
                  tagTime: true,
                  created_at: fecha,
                  hora: x.hora,
                  registromedico: "".concat(x.medico, " | ").concat(fecha, " | ").concat(x.hora),
                  id: x.id,
                  model: "impDiagn",
                  eventUpdateField: "UserCuestImpresionDiagnostica.update2(".concat(element.user_id, ",").concat(x.id, ",'impDiagn')"),
                  eventEliminar: "UserCuestImpresionDiagnostica.destroy2(".concat(element.user_id, ",").concat(x.id, ")")
                });
              }
            });
            listadoImpDiagn += "</ul>";
            var listadoEvolucion = "<ul class=\"list-group list-group-flush\" style=\"width:20em;\">";
            element.resultados.forEach(function (x) {
              var f = new Date(x.created_at);
              var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();

              if (x.description === "Evolución") {
                listadoEvolucion += listGoup({
                  //edit      : true,
                  destroy: false,
                  tagTitle: true,
                  title: "Evolución",
                  tagDescription: true,
                  description: x.value,
                  tagTime: true,
                  created_at: fecha,
                  hora: x.hora,
                  registromedico: "".concat(x.medico, " | ").concat(fecha, " | ").concat(x.hora),
                  id: x.id,
                  model: "evolucion",
                  eventUpdateField: "UserCuestEvolucion.update2(".concat(element.user_id, ",").concat(x.id, ",'evolucion')"),
                  eventEliminar: "UserCuestEvolucion.destroy2(".concat(element.user_id, ",").concat(x.id, ")")
                });
              }
            });
            listadoEvolucion += "</ul>";
            var listadoPlan = "<ul class=\"list-group list-group-flush\" style=\"width:20em;\">";
            element.resultados.forEach(function (x) {
              var f = new Date(x.created_at);
              var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();

              if (x.description === "Plan") {
                listadoPlan += listGoup({
                  edit: true,
                  tagTitle: true,
                  title: x.description,
                  tagDescription: true,
                  description: x.value,
                  tagTime: true,
                  created_at: fecha,
                  hora: x.hora,
                  registromedico: "".concat(x.medico, " | ").concat(fecha, " | ").concat(x.hora),
                  id: x.id,
                  model: "plan",
                  eventUpdateField: "UserCuestPlan.update2(".concat(element.user_id, ",").concat(x.id, ",'plan')"),
                  eventEliminar: "UserCuestPlan.destroy2(".concat(element.user_id, ",").concat(x.id, ")")
                });
              }
            });
            listadoPlan += "</ul>";
            var listadoResultado = "<ul class=\"list-group flex-column-reverse list-group-flush\" style=\"width:20em;\">";
            element.resultados.forEach(function (x) {
              var f = new Date(x.created_at);
              var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();

              if (x.description !== "Probabilidad diagnóstica" && x.description !== "Observación" && x.description !== "Evolución" && x.description !== "Pendiente" && x.description !== "Plan") {
                var tipo = x.description.substring(0, 3);
                listadoResultado += listGoup({
                  created_at: fecha,
                  description: x.value,
                  description2: x.coments,
                  destroy: false,
                  eventEliminar: "UserCuestPlan.destroy2(".concat(element.user_id, ",").concat(x.id, ")"),
                  hora: x.hora,
                  id: x.id,
                  model: "result" + tipo,
                  registromedico: "".concat(x.medico, " | ").concat(fecha, " | ").concat(x.hora),
                  tagArchivo: true,
                  tagDescription: true,
                  tagDescription1: false,
                  tagDescription2: true,
                  tagTime: true,
                  tagTitle: true,
                  tipoArchivo: Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension1"])(x.value),
                  title: x.description
                });
              }
            });
            listadoResultado += "</ul>";
            var listadoPendiente = "<ul id=\"listado_pendiente_".concat(element.user_id, "\" class=\"list-group list-group-flush\" style=\"width:20em;\">");
            element.resultados.forEach(function (x) {
              var f = new Date(x.created_at);
              var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();

              if (x.description === "Pendiente") {
                var tipo = x.description.substring(0, 3);
                listadoPendiente += listGoup({
                  eventCompletar: "UserCuestPendiente.update2(".concat(element.user_id, ",").concat(x.id, ",'pendiente')"),
                  completar: true,
                  tagTitle: true,
                  title: x.value,
                  tagDescription: true,
                  description: x.coments,
                  destroy: false,
                  tagTime: true,
                  created_at: fecha,
                  hora: x.hora,
                  registromedico: "".concat(x.medico, " | ").concat(fecha, " | ").concat(x.hora),
                  id: x.id,
                  model: "pendiente",
                  eventEliminar: "UserCuestPlan.destroy2(".concat(element.user_id, ",").concat(x.id, ")")
                });
              }
            });
            listadoPendiente += "</ul>";
            var f = new Date(element.ingreso_episodio);
            var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();
            var fechaIngreso = f.getFullYear() + "-" + ("0" + (f.getMonth() + 1)).slice(-2) + "-" + ("0" + f.getDate()).slice(-2);
            var blue_code = element.codigo_azul; //console.log(element.episodio)

            filas += "\n                                <tr class=\"row-".concat(element.parent_cat_user_ubicacion_id, "\" style=\"border-bottom: 3px solid #dee2e6 !important;\">\n                                    <td class=\"d-none\">\n                                        ").concat(element.ubicacion, "\n                                    </td>\n                                    <td class=\"text-center\">\n                                        <div class='container-fluid border rounded'>\n                                            <div class='d-flex flex-row  p-1' style='height:100%'>\n                                            <div class='p-1 flex-fill align-self-start flex-grow-1 border-right'>\n                                                <div class='d-flex flex-row justify-content-center p-1 border-bottom'>\n                                                    <div class=\"d-flex\" style=\"align-items: center;\">\n                                                        <div class=\"mx-1 mb-1\">\n                                                            <img onerror=\"this.src='/image/system/icono-paciente-blanco.svg';\" src=\"").concat(element.imagen, "\"\n                                                                style=\"width:1.5cm;height:1.5cm\" data-tippy-content=\"Imagen del paciente\"\n                                                                class=\"tooltip-primary border border-primary rounded-circle mx-auto d-block\" alt=\"...\">\n\n                                                        </div>\n                                                        <div>\n                                                            <div>\n                                                                <h4 data-tippy-content=\"Nombre del paciente\" class=\"tooltip-primary text-primary\"\n                                                                    style=\"margin-bottom: 0;\">\n                                                                    ").concat(element.paciente, "\n                                                                </h4>\n                                                            </div>\n                                                        </div>\n                                                    </div>\n                                                </div>\n                                                <div class='d-flex justify-content-center p-1'>\n                                                    <div class='d-flex p-1 flex-fill  align-self-start'>\n                                                        <div class='d-flex  flex-column flex-fill align-items-center p-1'>\n                                                            <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                                <b class=\"text-primary\" style=\"font-size:0.8em;\">C\xE9dula</b>\n                                                            </div>\n                                                            <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                                <div class=\"text-gray text-nowrap overflow-hidden\">\n                                                                    ").concat(element.cedula.replace('.', '').replace('.', ''), "\n                                                                </div>\n                                                            </div>\n                                                        </div>\n                                                        <div class='d-flex flex-column  flex-fill align-items-center p-1 border-left border-right'>\n                                                            <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                                <b class=\"text-primary\" style=\"font-size:0.8em;\">Edad</b>\n                                                            </div>\n                                                            <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                                <div class=\"text-gray\">\n                                                                    ").concat(element.edad, "\n                                                                </div>\n                                                            </div>\n                                                        </div>\n                                                        <div class='d-flex flex-column flex-fill align-items-center p-1'>\n                                                            <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                                <b class=\"text-primary\" style=\"font-size:0.8em;\">Sexo</b>\n                                                            </div>\n                                                            <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                                <div class=\"text-gray\">\n                                                                    ").concat(element.sexo.toUpperCase(), "\n                                                                </div>\n                                                            </div>\n                                                        </div>\n                                                    </div>\n                                                </div>\n                                            </div>\n                                            <div class='p-1 flex-fill align-self-start'>\n                                                <div class='d-flex flex-column align-items-center p-1'>\n                                                    <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                        <b class=\"text-primary\" style=\"font-size:0.9em;\">Fecha ingreso</b>\n                                                    </div>\n                                                    <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                        <div class=\"text-gray text-nowrap overflow-hidden\">\n                                                            <input\n                                                                class=\"tooltip-primary cursor-pointer\"\n                                                                data-tippy-content=\"Fecha de ingreso: ").concat(fecha, "\"\n                                                                onchange=\"UserCuestEpisodio.update3(this.id,").concat(element.episodio, ",").concat(element.user_id, ")\"\n                                                                id=\"ingreso").concat(element.user_id, "\"\n                                                                style=\"\n                                                                    border: 1px solid var(--gray);\n                                                                    background-color: transparent;\n\n                                                                    color: var( --secondary);\n                                                                    text-align: center;\n                                                                    border-radius: 50px;\" type=\"date\"\n                                                                value=\"").concat(fechaIngreso, "\">\n\n                                                            <small id=\"help_ingreso_").concat(element.user_id, "\"></small>\n\n                                                        </div>\n                                                    </div>\n                                                </div>\n                                                <div class='d-flex flex-column align-items-center p-1'>\n                                                    <div class='d-flex p-1 flex-fill text-nowrap justify-content-center'>\n                                                        <b class=\"text-primary\" style=\"font-size:0.9em;\">\n                                                            Ubicaci\xF3n Actual\n                                                            <span class=\"badge-gray tooltip-primary text-primary p-1 rounded\" style=\"font-size: 0.9em;\" data-tippy-content=\"D\xEDas en el \xE1rea\">\n                                                                ").concat(element.dias, "\n                                                            </span>\n                                                        </b>\n                                                    </div>\n                                                    <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                        <div class=\"text-gray text-nowrap overflow-hidden\">\n                                                            ").concat(element.ubicacion, "\n                                                        </div>\n                                                    </div>\n                                                </div>\n                                                <div class='d-flex flex-column align-items-center p-1'>\n                                                    <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                        <b class=\"text-primary\" style=\"font-size:0.9em;\">Tel\xE9fono Contacto</b>\n                                                    </div>\n                                                    <div class='d-flex p-1 flex-fill justify-content-center'>\n                                                        <button\n                                                            class=\"btn btn-outline-primary text-nowrap btn-sm tooltip-primary\"\n                                                            data-tippy-content=\"Tel\xE9fono contacto del paciente: ").concat(element.telefono_movil, "\"\n                                                            onclick=\"window.open('https://api.whatsapp.com/send?phone=").concat(element.telefono_movil, "')\">\n                                                            <i class=\"fab fa-whatsapp text-success\"></i> ").concat(element.telefono_movil, "\n                                                        </button>\n\n                                                    </div>\n                                                </div>\n                                            </div>\n                                            </div>\n                                        </div>\n\n\n\n\n\n                                        <!--\n                                            <div>\n                                                ").concat(element.paciente, "\n                                            </div>\n                                            <div>\n                                                <b class=\"tooltip-primary text-secondary\" data-tippy-content=\"Edad\">\n                                                ").concat(element.edad, " A\xF1os\n                                                </b>\n                                            </div>\n\n                                            <div class=\"mt-1\">\n                                                <span class=\"tooltip-primary ml-2 badge badge-gray text-primary\"  data-tippy-content=\"C\xE9dula de identidad\" style=\"line-height: inherit;font-size: 1em;\">\n                                                    <b>").concat(element.cedula.replace('.', '').replace('.', ''), "</b>\n                                                </span>\n                                            </div>\n                                            <div class=\"mt-1\">\n\n                                                <button class=\"btn btn-outline-primary tooltip-success\" data-tippy-content=\"Tel\xE9fono contacto del paciente: ").concat(element.telefono_movil, "\" onclick=\"window.open('https://api.whatsapp.com/send?phone=").concat(element.telefono_movil, "')\"><i class=\"fab fa-whatsapp text-success\"></i> ").concat(element.telefono_movil, "</button>\n\n                                            </div>-->\n\n                                    </td>\n                                    <!--\n\n\n                                    <td nowrap class=\"text-center\">\n                                        <div>\n                                            ").concat(fecha, "\n                                        </div>\n                                        <div>\n                                            <span class=\"tooltip-primary ml-2 badge badge-gray text-secondary\"  data-tippy-content=\"D\xEDas desde el ingreso\" style=\"line-height: inherit;\">\n                                                ").concat(element.dias, " d\xEDas\n                                            </span>\n                                        </div>\n\n                                    </td>-->\n                                    <td class=\"p-1\">\n                                        <div style=\"max-height: 20em;overflow: auto;\">\n                                            ").concat(listadoMed, "\n                                        </div>\n\n                                    </td>\n\n\n                                    <td class=\"p-1\">\n\n                                        <ul class=\"list-group list-group-flush\">\n                                            <li class=\"list-group-item p-0\">\n                                                <div class=\"d-flex p-1 form-impresion_diagnostica-user_").concat(element.user_id, "\">\n\n                                                    <div class=\"flex-grow-1\">\n                                                        <input type=\"hidden\" name=\"model\" data-name=\"Probabilidad diagn\xF3stica\" value=\"user_cuest_impresion_diagnostica\" >\n                                                        <input type=\"text\" name=\"comentario\" class=\"form-control\" placeholder=\"Escribe una Prob. Diagn\xF3stica    .\" >\n                                                    </div>\n                                                    <div >\n                                                        <button type=\"button\" data-type=\"impresion_diagnostica\"  data-episodio_id=\"").concat(element.episodio, "\" data-user_id_paciente=\"").concat(element.user_id, "\" data-tippy-content=\"Guardar\" class=\"btn-impresion_diagnostica-store btn btn-outline-success tooltip-primary ml-1\">\n                                                            <i  data-type=\"impresion_diagnostica\" data-episodio_id=\"").concat(element.episodio, "\" data-user_id_paciente=\"").concat(element.user_id, "\" class=\"far fa-save btn-impresion_diagnostica-store\"></i>\n                                                        </button>\n                                                    </div>\n                                                </div>\n                                            </li>\n                                        </ul>\n                                        <div class=\"impresion_diagnostica-listado-").concat(element.user_id, "\" style=\"max-height: 20em;overflow: auto;\">\n                                            ").concat(listadoImpDiagn, "\n                                        </div>\n                                    </td>\n                                    <td class=\"p-1\">\n\n                                        <ul class=\"list-group list-group-flush\">\n                                            <li class=\"list-group-item p-0\">\n                                                <div class=\"d-flex p-1 form-evolucion-user_").concat(element.user_id, "\">\n\n                                                    <div class=\"flex-grow-1\">\n                                                        <input type=\"hidden\" name=\"model\" data-name=\"Evoluci\xF3n\" value=\"user_cuest_evolucion\" >\n                                                        <input type=\"text\" name=\"comentario\" class=\"form-control\" placeholder=\"Escribe una evoluci\xF3n.\" >\n                                                    </div>\n                                                    <div >\n                                                        <button type=\"button\" data-type=\"evolucion\"  data-episodio_id=\"").concat(element.episodio, "\" data-user_id_paciente=\"").concat(element.user_id, "\" data-tippy-content=\"Guardar\" class=\"btn-evolucion-store btn btn-outline-success tooltip-primary  ml-1\">\n                                                            <i  data-type=\"evolucion\" data-episodio_id=\"").concat(element.episodio, "\" data-user_id_paciente=\"").concat(element.user_id, "\" class=\"far fa-save btn-evolucion-store\"></i>\n                                                        </button>\n                                                    </div>\n                                                </div>\n                                            </li>\n                                        </ul>\n                                        <div class=\"evolucion-listado-").concat(element.user_id, "\" style=\"max-height: 20em;overflow: auto;\">\n                                            ").concat(listadoEvolucion, "\n                                        </div>\n                                    </td>\n                                    <td class=\"p-1\">\n\n                                        <ul class=\"list-group list-group-flush\">\n                                            <li class=\"list-group-item p-0\">\n                                                <div class=\"d-flex p-1 form-plan-user_").concat(element.user_id, "\">\n\n                                                    <div class=\"flex-grow-1\">\n                                                        <input type=\"hidden\" name=\"model\" data-name=\"Plan\" value=\"user_cuest_plan\" >\n                                                        <input type=\"text\" name=\"comentario\" class=\"form-control\" placeholder=\"Escribe un plan\" >\n                                                    </div>\n                                                    <div >\n                                                        <button type=\"button\" data-type=\"plan\" data-episodio_id=\"").concat(element.episodio, "\" data-user_id_paciente=\"").concat(element.user_id, "\" data-tippy-content=\"Guardar\" class=\"btn-plan-store btn btn-outline-success tooltip-primary  ml-1\">\n                                                            <i data-type=\"plan\" data-episodio_id=\"").concat(element.episodio, "\" data-user_id_paciente=\"").concat(element.user_id, "\" class=\"far fa-save btn-plan-store\"></i>\n                                                        </button>\n                                                    </div>\n                                                </div>\n                                            </li>\n                                        </ul>\n                                        <div class=\"plan-listado-").concat(element.user_id, "\" style=\"max-height: 20em;overflow: auto;\">\n                                            ").concat(listadoPlan, "\n                                        </div>\n                                    </td>\n                                    <td class=\"p-1\">\n\n                                        <ul class=\"list-group list-group-flush\">\n                                            <li class=\"list-group-item p-0\">\n                                                <div class=\"d-flex flex-column p-1 form-resultados-user_").concat(element.user_id, "\">\n                                                    <div>\n                                                        <div class=\"d-flex\">\n                                                            <div class=\"flex-fill\">\n\n                                                                <select name=\"tipo_resultado\" class=\"form-control rounded-0\" style=\"border-radius: 3px 0px 0px 0px !important;\">\n                                                                    <option value=\"\">Resultado</option>\n                                                                    <option value=\"Laboratorio\">Laboratorio</option>\n                                                                    <option value=\"Imagen\">Imagen</option>\n                                                                    <option value=\"Fotograf\xEDa\">Fotograf\xEDa</option>\n                                                                    <option value=\"Otro archivo\">Otro archivo</option>\n                                                                </select>\n                                                            </div>\n                                                            <div class=\"flex-fill\">\n                                                                <input type=\"text\" name=\"comentario\" class=\"form-control rounded-0\" placeholder=\"Escribe un comentario\" style=\"border-left: 0;border-right: 0;\">\n                                                            </div>\n                                                            <div class=\"flex-grow-1\">\n                                                                <button type=\"button\" data-type=\"plan\" data-episodio_id=\"").concat(element.episodio, "\" data-user_id_paciente=\"").concat(element.user_id, "\" style=\"border-radius: 0px 3px 0px 0px;\" data-tippy-content=\"Guardar Resultado\" class=\"btn-resultado-store btn btn-outline-success tooltip-primary btn-flat\">\n                                                                    <i data-episodio_id=\"").concat(element.episodio, "\" data-user_id_paciente=\"").concat(element.user_id, "\" class=\"far fa-save btn-resultado-store\"></i>\n                                                                </button>\n                                                            </div>\n                                                        </div>\n                                                    </div>\n                                                    <div>\n                                                        <input type=\"file\" name=\"file\" class=\"form-control border-top-0 rounded-bottom\" style=\"height: 43px;border-radius: 0px 0px 3px 3px !important;\">\n                                                    </div>\n                                                </div>\n\n\n\n                                            </li>\n                                        </ul>\n                                        <div class=\"resultados-listado-").concat(element.user_id, "\" style=\"max-height: 20em;overflow: auto;\">\n                                            ").concat(listadoResultado, "\n                                        </div>\n                                    </td>\n                                    <td class=\"p-1\">\n                                        <ul class=\"list-group list-group-flush\">\n                                            <li class=\"list-group-item p-0\">\n                                                <div class=\"d-flex p-1\">\n\n                                                    <div class=\"flex-grow-1\">\n                                                        <input id=\"privado_").concat(element.user_id, "\" class=\"d-none\" value=\"0\">\n                                                        <input id=\"coments_").concat(element.user_id, "\" placeholder=\"Escribe un nuevo pendiente\" class=\"form-control\">\n                                                    </div>\n                                                    <div >\n                                                        <button id=\"btn_pendiente_create_").concat(element.user_id, "\" onclick=\"UserCuestPendiente.store2(").concat(element.user_id, ")\" type=\"button\" data-tippy-content=\"Guardar\" class=\"btn btn-outline-success tooltip-primary  ml-1\">\n                                                            <i class=\"far fa-save\"></i>\n                                                        </button>\n                                                    </div>\n                                                </div>\n\n                                            </li>\n                                        </ul>\n                                        <div style=\"max-height: 20em;overflow: auto;\">\n                                           ").concat(listadoPendiente, "\n                                        </div>\n\n                                    </td>\n                                    <td class=\"p-1 text-center\">\n                                        <input id=\"blue_code_").concat(element.user_id, "\" class=\"d-none\" value=\"").concat(blue_code, "\">\n                                        <span id=\"ico_blue_code_").concat(element.user_id, "\" onclick=\"onOffBlueCode(").concat(element.user_id, ")\" title=\"").concat(blue_code ? 'Código azul ACTIVADO' : 'Código azul DESACTIVADO', "\" style=\"font-size: 4em; color:").concat(blue_code ? 'var(--primary)' : 'var(--danger)', "\" class=\"").concat(blue_code ? 'pc-fa-blue-code-on' : 'pc-fa-blue-code-off', " heartbeat cursor-pointer\"></span>\n                                    </td>\n                                    <td>\n                                        <buttom onclick=\"UserCuestUbicacion.egreso('.modal-body',").concat(element.user_id, ")\" class=\"btn btn-primary btn-block\">Alta</buttom>\n                                    </td>\n                                </tr>");
          });
          $(selector).html(filas);
          $("#total_paciente").html(model.length); //configDatatable()

          $("#cargando").css("display", "none");
          $("#example").css("display", "block");
          Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["activarTooltip"])();
        } else {
          $("#sinresultados").css("display", "flex");
          $("#cargando").css("display", "none");
        }
      }, 500);
      document.addEventListener("click", /*#__PURE__*/function () {
        var _ref8 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2(e) {
          var _e$target$dataset, user_id_paciente, episodio_id, type, $form, file, comentario, tipo_resultado, formdata, result, index, _$form, _comentario, model, _formdata, _result, _index;

          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
            while (1) {
              switch (_context2.prev = _context2.next) {
                case 0:
                  _e$target$dataset = e.target.dataset, user_id_paciente = _e$target$dataset.user_id_paciente, episodio_id = _e$target$dataset.episodio_id, type = _e$target$dataset.type;

                  if (!e.target.classList.contains("btn-resultado-store")) {
                    _context2.next = 44;
                    break;
                  }

                  $form = document.querySelector(".form-resultados-user_".concat(user_id_paciente));
                  file = $form.querySelector("input[name=file]").files[0]; //console.log("type_file",type_file)

                  comentario = $form.querySelector("input[name=comentario]"); //console.log("comentario",comentario)

                  tipo_resultado = $form.querySelector("select[name=tipo_resultado]"); //console.log("tipo_resultado",tipo_resultado)

                  if (!(tipo_resultado.value === "")) {
                    _context2.next = 10;
                    break;
                  }

                  alert("No has seleccionado un tipo de resultado.");
                  tipo_resultado.focus();
                  return _context2.abrupt("return", false);

                case 10:
                  if (!(comentario.value === "")) {
                    _context2.next = 14;
                    break;
                  }

                  alert("No has escrito un comentario.");
                  comentario.focus();
                  return _context2.abrupt("return", false);

                case 14:
                  formdata = new FormData();
                  formdata.append("coments", comentario.value);
                  formdata.append("file", file);
                  formdata.append("episodio_id", episodio_id);
                  formdata.append("user_id", user_id_paciente);
                  formdata.append("_token", $("#_token").val());
                  formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());

                  if (!(tipo_resultado.value == "Laboratorio")) {
                    _context2.next = 25;
                    break;
                  }

                  _context2.next = 24;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_laboratorio/store", formdata);

                case 24:
                  result = _context2.sent;

                case 25:
                  if (!(tipo_resultado.value == "Imagen")) {
                    _context2.next = 29;
                    break;
                  }

                  _context2.next = 28;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_imagen/store", formdata);

                case 28:
                  result = _context2.sent;

                case 29:
                  if (!(tipo_resultado.value == "Fotografía")) {
                    _context2.next = 33;
                    break;
                  }

                  _context2.next = 32;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_fotografia/store", formdata);

                case 32:
                  result = _context2.sent;

                case 33:
                  if (!(tipo_resultado.value == "Otro archivo")) {
                    _context2.next = 37;
                    break;
                  }

                  _context2.next = 36;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_otro_archivo/store", formdata);

                case 36:
                  result = _context2.sent;

                case 37:
                  Object.assign(result, {
                    "description": tipo_resultado.value
                  }); //console.log(result)

                  index = pacientes_datos.findIndex(function (paciente) {
                    return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["equalsInt"])(paciente['user_id'], user_id_paciente);
                  });
                  pacientes_datos[index]['resultados'].push(result);
                  document.querySelector(".resultados-listado-".concat(user_id_paciente)).innerHTML = null;
                  pacientes_datos[index]['resultados'].forEach(function (resultado) {
                    var listadoResultado = "<ul class=\"list-group flex-column-reverse list-group-flush\" style=\"width:20em;\">";
                    pacientes_datos[index]['resultados'].forEach(function (x) {
                      var f = new Date(x.created_at);
                      var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();

                      if (x.description !== "Probabilidad diagnóstica" && x.description !== "Observación" && x.description !== "Evolución" && x.description !== "Pendiente" && x.description !== "Plan") {
                        var tipo = x.description.substring(0, 3);
                        listadoResultado += listGoup({
                          tagArchivo: true,
                          tipoArchivo: Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["getFileExtension1"])(x.value),
                          tagTitle: true,
                          title: x.description,
                          tagDescription: true,
                          description: x.value,
                          tagDescription1: false,
                          tagDescription2: true,
                          description2: x.coments,
                          destroy: false,
                          tagTime: true,
                          created_at: fecha,
                          hora: x.hora,
                          registromedico: "".concat(x.medico, " | ").concat(fecha, " | ").concat(x.hora),
                          id: x.id,
                          model: "result" + tipo,
                          eventEliminar: "UserCuestPlan.destroy2(".concat(user_id_paciente, ",").concat(x.id, ")")
                        });
                      }
                    });
                    listadoResultado += "</ul>";
                    document.querySelector(".resultados-listado-".concat(user_id_paciente)).innerHTML = listadoResultado;
                  });
                  comentario.value = "";
                  tipo_resultado.value = "";

                case 44:
                  if (!(e.target.classList.contains("btn-plan-store") || e.target.classList.contains("btn-evolucion-store") || e.target.classList.contains("btn-impresion_diagnostica-store"))) {
                    _context2.next = 77;
                    break;
                  }

                  _$form = document.querySelector(".form-".concat(e.target.dataset['type'], "-user_").concat(user_id_paciente));
                  _comentario = _$form.querySelector("input[name=comentario]");
                  model = _$form.querySelector("input[name=model]");

                  if (!(_comentario.value === "")) {
                    _context2.next = 52;
                    break;
                  }

                  alert("No has escrito un comentario.");

                  _comentario.focus();

                  return _context2.abrupt("return", false);

                case 52:
                  _formdata = new FormData();

                  _formdata.append("value", _comentario.value);

                  _formdata.append("episodio_id", episodio_id);

                  _formdata.append("user_id", user_id_paciente);

                  _formdata.append("_token", $("#_token").val());

                  _formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());

                  if (!(model.dataset['name'] == "Plan")) {
                    _context2.next = 62;
                    break;
                  }

                  _context2.next = 61;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])("/".concat(model.value, "/store"), _formdata);

                case 61:
                  _result = _context2.sent;

                case 62:
                  if (!(model.dataset['name'] == "Evolución")) {
                    _context2.next = 66;
                    break;
                  }

                  _context2.next = 65;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_evolucion/store", _formdata);

                case 65:
                  _result = _context2.sent;

                case 66:
                  if (!(model.dataset['name'] == "Probabilidad diagnóstica")) {
                    _context2.next = 70;
                    break;
                  }

                  _context2.next = 69;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_impresion_diagnostica/store", _formdata);

                case 69:
                  _result = _context2.sent;

                case 70:
                  Object.assign(_result, {
                    "description": model.dataset['name']
                  });
                  console.log(_result);
                  _index = pacientes_datos.findIndex(function (paciente) {
                    return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["equalsInt"])(paciente['user_id'], user_id_paciente);
                  });

                  pacientes_datos[_index]['resultados'].push(_result);

                  document.querySelector(".".concat(type, "-listado-").concat(user_id_paciente)).innerHTML = null;

                  pacientes_datos[_index]['resultados'].forEach(function (resultado) {
                    var listado = "<ul class=\"list-group flex-column-reverse list-group-flush\" style=\"width:20em;\">";

                    pacientes_datos[_index]['resultados'].forEach(function (x) {
                      var f = new Date(x.created_at);
                      var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();

                      if (model.dataset['name'] == x.description) {
                        listado += listGoup({
                          edit: true,
                          destroy: false,
                          tagTitle: true,
                          title: x.description,
                          tagDescription: true,
                          description: x.value,
                          tagTime: true,
                          created_at: fecha,
                          hora: x.hora,
                          registromedico: "".concat(x.medico, " | ").concat(fecha, " | ").concat(x.hora),
                          id: x.id,
                          model: type
                        });
                      } //}

                    });

                    listado += "</ul>";
                    document.querySelector(".".concat(type, "-listado-").concat(user_id_paciente)).innerHTML = listado;
                  });

                  _comentario.value = "";

                case 77:
                case "end":
                  return _context2.stop();
              }
            }
          }, _callee2);
        }));

        return function (_x2) {
          return _ref8.apply(this, arguments);
        };
      }());
    }
  }, {
    key: "episodioPendiente",
    value: function episodioPendiente() {
      var formdata = new FormData();
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/episodioPendiente", formdata);
    }
  }, {
    key: "create",
    value: function create(_ref9) {
      var _ref9$selector = _ref9.selector,
          selector = _ref9$selector === void 0 ? ".modal-body" : _ref9$selector,
          user_id = _ref9.user_id;
      //incio component
      var model = UserCuestEpisodio.classModel();
      Component.modalTemplate_1({
        //title: Nombre del modelo que se esta mostrando
        title: UserCuestEpisodio.modelTitle(),
        //selector: identificador html en donde se va a pintar el componente
        selector: selector,
        //user_id: id del usuario que se quiere visualizar
        user_id: user_id,
        //eventModelCreate: evento a ejecutar al pulsar en el botón Nuevo valor
        //OJO: se le pasa un string, porque la funcion eval, es la que determinará a que modelo nos estamos refiriendo.
        eventModelCreate: model + ".create({user_id:" + user_id + "})",
        //eventModelContent: contenido del modelo que se esta mostrando
        eventModelContent: "Component.formTemplate_2({user_id:" + user_id + "})",
        //eventModelCreate: evento a ejecutar al pulsar en los botones del footer
        eventModelFooter: model + ".regresar({user_id:" + user_id + "})",
        //btn_create:false  oculta el boton
        btn_create: false //si se está en la otra vista que no sea index,
        //se le pasa el parametro btn_create:false
        //para que no muestre el botón de crear

      }, function () {
        $("#btn_modalTemplate_store").on("click", function () {
          eval(model + ".store({user_id:" + user_id + "})").then(function (response) {
            eval(model + ".index({user_id:" + user_id + "})");
          });
        });
      }); //fin component
    }
  }, {
    key: "show",
    value: function show(_ref10) {
      var user_id = _ref10.user_id;
      var formdata = new FormData();
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/show/" + user_id, formdata);
    }
  }, {
    key: "store",
    value: function store(_ref11) {
      var user_id = _ref11.user_id;
      var formdata = new FormData();
      formdata.append('cat_user_ubicacion_id', $("#cat_user_ubicacion_id_actual").val());
      formdata.append('cat_user_informe_id', 2);
      formdata.append('cat_user_ubicacion_id_destino', $("#cat_user_ubicacion_id").val()); //formdata.append('diag_egreso',$("#coments").val());

      formdata.append('coment', $("#coment").val());
      formdata.append('value', ingreso);
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/store", formdata);
    }
  }, {
    key: "get_historiaIngreso",
    value: function () {
      var _get_historiaIngreso = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3(user_id) {
        var formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
          while (1) {
            switch (_context3.prev = _context3.next) {
              case 0:
                //para actualizar cualquier campo de un registro especifico
                formdata = new FormData();
                formdata.append("user_id", user_id);
                formdata.append("_token", $("#_token").val());
                _context3.next = 5;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/get_historiaIngreso", formdata);

              case 5:
                return _context3.abrupt("return", _context3.sent);

              case 6:
              case "end":
                return _context3.stop();
            }
          }
        }, _callee3);
      }));

      function get_historiaIngreso(_x3) {
        return _get_historiaIngreso.apply(this, arguments);
      }

      return get_historiaIngreso;
    }()
  }, {
    key: "store3",
    value: function () {
      var _store = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4(user_id, data) {
        var formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
          while (1) {
            switch (_context4.prev = _context4.next) {
              case 0:
                //para actualizar cualquier campo de un registro especifico
                formdata = new FormData();
                formdata.append('campo', data.name);
                formdata.append('valor', data.value);
                formdata.append('user_cuest_episodio_id', data.episodio_id);
                formdata.append("user_id", user_id);
                formdata.append("_token", $("#_token").val());
                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                _context4.next = 9;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/update2", formdata);

              case 9:
                return _context4.abrupt("return", _context4.sent);

              case 10:
              case "end":
                return _context4.stop();
            }
          }
        }, _callee4);
      }));

      function store3(_x4, _x5) {
        return _store.apply(this, arguments);
      }

      return store3;
    }()
  }, {
    key: "update",
    value: function update(_ref12) {
      var user_id = _ref12.user_id;
      var formdata = new FormData();
      formdata.append('user_cuest_episodio_id', user_cuest_episodio_id);
      formdata.append('uti', $("#uti").val());
      formdata.append('uti_causa', $("#uti_causa").val()); //formdata.append('diag_egreso_cod_cie',$("#input-code-2").val());
      //formdata.append('diag_egreso_cod_cie_description',$("#input-title-2").val());

      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/update", formdata);
    }
  }, {
    key: "update2",
    value: function update2(selector) {
      //alert(selector)
      //para actualizar cualquier campo de un registro especifico
      var formdata = new FormData();
      formdata.append('campo', selector);
      formdata.append('valor', $("#" + selector).val());
      formdata.append('user_cuest_episodio_id', user_cuest_episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/update2", formdata).then(function (response) {
        message = Component.alertMessage("update_success");
        $(".help_" + selector).html("<div class='scale-up-ver-top text-center text-success'>".concat(message['message'], " <i class='fas fa-check-circle'></i></span>"));
      });
    }
  }, {
    key: "update3",
    value: function update3(selector, user_cuest_episodio_id, user_id) {
      //alert(selector)
      //para actualizar cualquier campo de un registro especifico
      var formdata = new FormData();
      formdata.append('campo', "ingreso");
      formdata.append('valor', $("#ingreso" + user_id).val());
      formdata.append('user_cuest_episodio_id', user_cuest_episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/update2", formdata).then(function (response) {
        pacientes_datos.forEach(function (x) {
          if (x.user_id === response.user_id) {
            x.ingreso_episodio = response.ingreso;
          }
        });
        message = Component.alertMessage("update_success");
        $("#help_ingreso_" + user_id).html("<div class='scale-up-ver-top text-center text-success'>".concat(message['message'], " <i class='fas fa-check-circle'></i></span>"));
      });
    } //METODO historialEpisodios MOVIDO EL 12-07-2024 HACIA component.js con el nombre AppHistorialEpisodios

  }, {
    key: "historialEpisodios",
    value: function () {
      var _historialEpisodios = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5(patient_data, index_episodio) {
        var user_id, $cabecera, $cargando, modalBody, response, imagen, docu, adjuntos, item, cardhistorias;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
          while (1) {
            switch (_context5.prev = _context5.next) {
              case 0:
                user_id = patient_data.user_id;
                $("#modal-patient-item").modal("show");
                $cabecera = document.getElementById("historialEpisodios").content.cloneNode(true);
                $cargando = document.getElementById("cargando").content.cloneNode(true);
                modalBody = document.querySelector("#modal-patient-item .modal-body-2");
                modalBody.innerHTML = null;
                modalBody.appendChild($cargando);
                _context5.next = 9;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])("/episodio/historial/".concat(user_id));

              case 9:
                response = _context5.sent;

                if (response.length === 0) {
                  modalBody.innerHTML = "\n                    <div class=\"container-fluid h-100\">\n                        <div class=\"row justify-content-center align-items-center\">\n                            <div class=\"col-md-8 text-center\">\n                                <h3 class=\"text-primary\">No se encontraron episodios anteriores</h3>\n                            </div>\n                        </div>\n                    </div>\n                ";
                } else {
                  imagen = function imagen(x) {
                    if (["jpeg", "jpg", "png", "gif", "bmp"].includes(x.file_type)) {
                      return "\n                            <li>\n                                <span class=\"mailbox-attachment-icon has-img\">\n                                <img src=\"/".concat(x.value, "\" alt=\"Attachment\" /></span>\n                                <div class=\"mailbox-attachment-info\">\n                                    <a href=\"/").concat(x.value, "\" target=\"_blank\" class=\"btn btn-default btn-block mt-2\"><i class=\"far fa-eye\"></i> Ver</a>\n                                </div>\n                            </li>\n                        ");
                    }
                  };

                  docu = function docu(x, optionName) {
                    switch (optionName) {
                      case 'Laboratorio':
                        optionName = "icono_laboratorio.svg";
                        break;

                      case 'Imagenes':
                        optionName = "icono_laboratorio.svg";
                        break;

                      case 'Estudio':
                        optionName = "icono_estudio.svg";
                        break;

                      case 'Fotografías':
                        optionName = "icono_fotografia.svg";
                        break;

                      case 'Otros Archivos':
                        optionName = "icono_archivo.svg";
                        break;

                      default:
                        optionName = "icono_archivo.svg";
                        break;
                    }

                    if (["pdf", "doc", "txt", "docx", "bmp", "ods", "xls", "xlsx", "ppt", "pptx"].includes(x.file_type)) {
                      return "\n                            <li>\n                                <span class=\"mailbox-attachment-icon has-img\">\n                                <img src=\"/image/system/".concat(optionName, "\" alt=\"Attachment\" style=\"width: 50%;height: 50%;\" /></span>\n                                <div class=\"mailbox-attachment-info\">\n                                    <a href=\"/").concat(x.value, "\" target=\"_blank\" class=\"btn btn-default btn-block\"><i class=\"far fa-eye\"></i> Ver</a>\n                                </div>\n                                <div class=\"mailbox-attachment-size text-center clearfix\">\n                                    <span>").concat(x.file_type.toUpperCase(), "</span>\n                                </div>\n                            </li>\n                        ");
                    }
                  };

                  adjuntos = function adjuntos(imagenes, documentos) {
                    return "\n                        <div>\n                            ".concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(imagenes) || !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(documentos) ? '<i class="fas fa-paperclip"></i> Archivos Adjuntos' : '', "\n                            <div class=\"card-footer bg-white ").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(imagenes) && Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(documentos) ? 'd-none' : '', "\">\n                                <ul class=\"mailbox-attachments d-flex align-items-stretch clearfix\">\n                                    ").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(imagenes) || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(imagenes) ? '' : imagenes, "\n                                    ").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(documentos) || Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(documentos) ? '' : documentos, "\n                                </ul>\n                            </div>\n                        </div>\n                    ");
                  };

                  item = function item(x, itemName) {
                    var imagenes = "";
                    var documentos = "";
                    var adjuntos_temp = "";
                    var mostra;

                    if (["Imagenes", "Otros archivos"].includes(itemName)) {
                      if (x !== null && x !== void 0 && x.file_type) {
                        imagenes = "";
                        imagenes += imagen(x);
                        documentos = "";
                        documentos += docu(x, itemName);
                      }

                      adjuntos_temp = adjuntos(imagenes, documentos);
                    }

                    var medico = medicos_datos.find(function (medico) {
                      return parseInt(medico.user_id) === parseInt(x.user_id_medico);
                    });
                    var mostrarMedico = true;

                    if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(medico)) {
                      mostrarMedico = false;
                    } //console.log("medico -->",medico)


                    return "\n                        <div data-episodio_id=\"".concat(x.episodio_id, "\" class=\"post\">\n                            <div class=\"user-block ").concat(!mostrarMedico ? 'd-none' : '', "\">\n                                <img onerror=\"this.src='/image/system/icono-paciente-blanco.svg'\" class=\"img-circle img-bordered-sm\" src=\"").concat(!mostrarMedico ? '#' : medico.imagen, "\" alt=\"user-img 128x128\">\n                                <div class=\"d-flex flex-column\">\n                                    <span class=\"username\">\n                                        <a href=\"#\">").concat(!mostrarMedico ? '#' : medico.medico, "</a>\n                                    </span>\n                                    <span class=\"description\">\n                                        <i class=\"far fa-clock\"></i> ").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaCortaCustom2"])(x.created_at), "\n                                    </span>\n                                </div>\n                            </div>\n                            <p>\n                                    ").concat(!["Imagenes", "Laboratorio", "Fotografías", "Otros archivos"].includes(itemName) && !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(x.description) && !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(x.description) ? x.description + '<br>' : '', "\n\n                                    ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(x.coment) && !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(x.coment) ? x.coment + '<br>' : '', "\n                                    ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(x.coments) && !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(x.coments) ? x.coments + '<br>' : '', "\n                                    ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(x.cod_cie) && !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(x.cod_cie) ? x.cod_cie + '<br>' : '', "\n                                    ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(x.cod_cie_description) && !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(x.cod_cie_description) ? x.cod_cie_description + '<br>' : '', "\n                            </p>\n\n                            ").concat(adjuntos_temp, "\n\n                        </div>\n                    ");
                  };

                  cardhistorias = "";
                  response.forEach(function (historia, index) {
                    var items = "";
                    var opcionesHistoria = "";
                    var index1 = 0;

                    var _loop = function _loop(key) {
                      if (historia.episodio[key].items.length > 0 || key === 'historia_inicial') {
                        var existHistoriaInicial = 0;

                        if (key === 'historia_inicial') {
                          var _historia$episodio$ke = historia.episodio[key].items.SIGNOS,
                              FR = _historia$episodio$ke.FR,
                              GLIC = _historia$episodio$ke.GLIC,
                              OXI = _historia$episodio$ke.OXI,
                              TA = _historia$episodio$ke.TA,
                              TEMP = _historia$episodio$ke.TEMP,
                              FC = _historia$episodio$ke.FC,
                              PESO = _historia$episodio$ke.PESO,
                              TALLA = _historia$episodio$ke.TALLA;
                          var _historia$episodio$ke2 = historia.episodio[key].items,
                              MOTIVO_CONSULTA = _historia$episodio$ke2.MOTIVO_CONSULTA,
                              ANTECEDENTES = _historia$episodio$ke2.ANTECEDENTES,
                              ENFERMEDAD_ACTUAL = _historia$episodio$ke2.ENFERMEDAD_ACTUAL,
                              EXAMEN_FISICO = _historia$episodio$ke2.EXAMEN_FISICO,
                              IMP_DIAG = _historia$episodio$ke2.IMP_DIAG,
                              HOSPITALIZACION = _historia$episodio$ke2.HOSPITALIZACION,
                              TERAPIA_INTENSIVA = _historia$episodio$ke2.TERAPIA_INTENSIVA,
                              CIRUGIA = _historia$episodio$ke2.CIRUGIA,
                              NIVEL_TRIAJE = _historia$episodio$ke2.NIVEL_TRIAJE,
                              REQUIERE_INTERCONSULTA = _historia$episodio$ke2.REQUIERE_INTERCONSULTA,
                              REALIZAR_PROCEDIMIENTO = _historia$episodio$ke2.REALIZAR_PROCEDIMIENTO,
                              CANTIDAD_ESPECIALISTAS = _historia$episodio$ke2.CANTIDAD_ESPECIALISTAS,
                              PROCEDIMIENTO_COMPLEJIDAD = _historia$episodio$ke2.PROCEDIMIENTO_COMPLEJIDAD;

                          if (FR || GLIC || OXI || TA || TEMP || FC || PESO || TALLA || MOTIVO_CONSULTA || ANTECEDENTES || ENFERMEDAD_ACTUAL || EXAMEN_FISICO || IMP_DIAG || HOSPITALIZACION || TERAPIA_INTENSIVA || CIRUGIA) {
                            existHistoriaInicial = 1;
                          }
                        }

                        opcionesHistoria += "\n                                        <a\n                                            class=\"nav-link flex-nowrap d-flex justify-content-between align-items-center text-secondary\"\n                                            id=\"nav-".concat(historia.episodio_id, "-tabs-").concat(index1, "-tab\"\n                                            data-toggle=\"pill\"\n                                            href=\"#nav-").concat(historia.episodio_id, "-tabs-").concat(index1, "\"\n                                            role=\"tab\" aria-controls=\"nav-").concat(historia.episodio_id, "-tabs-").concat(index1, "\"\n                                            aria-selected=\"false\"\n                                        >\n                                            <span>").concat(historia.episodio[key].title, " </span>\n                                            <span class=\"").concat(['historia_inicial'].includes(key) && !existHistoriaInicial && 'd-none', " badge badge-primary\">\n                                                ").concat(['historia_inicial'].includes(key) && existHistoriaInicial ? existHistoriaInicial : historia.episodio[key].items.length, "\n                                            </span>\n                                        </a>\n                                    ");

                        if (['historia_inicial'].includes(key)) {
                          var _historia$episodio$ke3 = historia.episodio[key].items.SIGNOS,
                              _FR = _historia$episodio$ke3.FR,
                              _GLIC = _historia$episodio$ke3.GLIC,
                              _OXI = _historia$episodio$ke3.OXI,
                              _TA = _historia$episodio$ke3.TA,
                              _TEMP = _historia$episodio$ke3.TEMP,
                              _FC = _historia$episodio$ke3.FC,
                              _PESO = _historia$episodio$ke3.PESO,
                              _TALLA = _historia$episodio$ke3.TALLA;
                          var _historia$episodio$ke4 = historia.episodio[key].items,
                              _MOTIVO_CONSULTA = _historia$episodio$ke4.MOTIVO_CONSULTA,
                              _ANTECEDENTES = _historia$episodio$ke4.ANTECEDENTES,
                              _ENFERMEDAD_ACTUAL = _historia$episodio$ke4.ENFERMEDAD_ACTUAL,
                              _EXAMEN_FISICO = _historia$episodio$ke4.EXAMEN_FISICO,
                              _IMP_DIAG = _historia$episodio$ke4.IMP_DIAG,
                              _HOSPITALIZACION = _historia$episodio$ke4.HOSPITALIZACION,
                              _TERAPIA_INTENSIVA = _historia$episodio$ke4.TERAPIA_INTENSIVA,
                              _CIRUGIA = _historia$episodio$ke4.CIRUGIA,
                              _NIVEL_TRIAJE = _historia$episodio$ke4.NIVEL_TRIAJE,
                              _REQUIERE_INTERCONSULTA = _historia$episodio$ke4.REQUIERE_INTERCONSULTA,
                              _REALIZAR_PROCEDIMIENTO = _historia$episodio$ke4.REALIZAR_PROCEDIMIENTO,
                              _CANTIDAD_ESPECIALISTAS = _historia$episodio$ke4.CANTIDAD_ESPECIALISTAS,
                              _PROCEDIMIENTO_COMPLEJIDAD = _historia$episodio$ke4.PROCEDIMIENTO_COMPLEJIDAD;
                          var mostrarMedico = true;
                          var medico = null; //console.log(IMP_DIAG);

                          if (_IMP_DIAG) {
                            medico = medicos_datos.find(function (medico) {
                              return parseInt(medico.user_id) === parseInt(_IMP_DIAG.user_id_medico);
                            });
                          }

                          if (!medico) {
                            mostrarMedico = false;
                          }

                          items += "\n                                                <div class=\"tab-pane text-left fade\" id=\"nav-".concat(historia.episodio_id, "-tabs-").concat(index1, "\" role=\"tabpanel\" aria-labelledby=\"nav-").concat(historia.episodio_id, "-tabs-").concat(index1, "\">\n                                                    \n                                                    <div class=\"d-flex flex-column\">\n                                                        <div class=\"user-block ").concat(!mostrarMedico ? 'd-none' : '', "\">\n                                                            <img \n                                                                onerror=\"this.src='/image/system/icono-paciente-blanco.svg'\" \n                                                                class=\"img-circle img-bordered-sm\" \n                                                                src=\"").concat(!mostrarMedico ? '#' : medico.imagen, "\" \n                                                                alt=\"user-img 128x128\"\n                                                            >\n                                                            <div class=\"d-flex flex-column\">\n                                                                <span class=\"username\">\n                                                                    <a href=\"#\">").concat(!mostrarMedico ? '#' : medico.medico, "</a>\n                                                                </span>\n                                                                <span class=\"description\">\n                                                                    <i class=\"far fa-clock\"></i> ").concat(mostrarMedico ? Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaCortaCustom2"])(_IMP_DIAG.created_at) : '', "\n                                                                </span>\n                                                            </div>\n                                                        </div>\n                                                        <div class=\"container-fluid py-1\">\n                                                            <div class=\"row\">\n                                                                <div class=\"col-12 col-sm-4 col-md-3 col-lg-3 col-xl-3\">\n                                                                    <div class=\"card p-1 mb-1\">\n                                                                        <h5 class=\"text-primary\">Signos Vitales</h5>\n                                                                        <div class=\"d-flex flex-column\">\n                                                                            <div class=\"d-flex mb-1\">\n                                                                                <div class=\"pl-2 text-secondary font-weight-bold rounded\" style=\"width:60px\">\n                                                                                Peso\n                                                                                </div>\n                                                                                <div class=\"pl-2\">\n                                                                                ").concat(_PESO ? _PESO + ' Kg.' : '', "\n                                                                                </div>\n                                                                            </div>\n                                                                            <div class=\"d-flex mb-1\">\n                                                                                <div class=\"pl-2 text-secondary font-weight-bold\" style=\"width:60px\">\n                                                                                Talla\n                                                                                </div>\n                                                                                <div class=\"pl-2\">\n                                                                                ").concat(_TALLA ? _TALLA + ' cm.' : '', "\n                                                                                </div>\n                                                                            </div>\n                                                                            <div class=\"d-flex mb-1\">\n                                                                                <div class=\"pl-2 text-secondary font-weight-bold\" style=\"width:60px\">\n                                                                                Temp\n                                                                                </div>\n                                                                                <div class=\"pl-2\">\n                                                                                ").concat(_TEMP ? _TEMP + ' ºC' : '', "\n                                                                                </div>\n                                                                            </div>\n                                                                            <div class=\"d-flex mb-1\">\n                                                                                <div class=\"pl-2 text-secondary font-weight-bold\" style=\"width:60px\">\n                                                                                Oxi\n                                                                                </div>\n                                                                                <div class=\"pl-2\">\n                                                                                ").concat(_OXI ? _OXI + ' %' : '', "\n                                                                                </div>\n                                                                            </div>\n                                                                            <div class=\"d-flex mb-1\">\n                                                                                <div class=\"pl-2 text-secondary font-weight-bold\" style=\"width:60px\">\n                                                                                Fc\n                                                                                </div>\n                                                                                <div class=\"pl-2\">\n                                                                                ").concat(_FC ? _FC + ' resp/min' : '', "\n                                                                                </div>\n                                                                            </div>\n                                                                            <div class=\"d-flex mb-1\">\n                                                                                <div class=\"pl-2 text-secondary font-weight-bold\" style=\"width:60px\">\n                                                                                Gl\n                                                                                </div>\n                                                                                <div class=\"pl-2\">\n                                                                                ").concat(_GLIC ? _GLIC + ' mg/dL' : '', "\n                                                                                </div>\n                                                                            </div>\n                                                                            <div class=\"d-flex mb-1\">\n                                                                                <div class=\"pl-2 text-secondary font-weight-bold\" style=\"width:60px\">\n                                                                                Ta\n                                                                                </div>\n                                                                                <div class=\"pl-2\">\n                                                                                ").concat(_TA ? _TA + ' mmHg' : '', "\n                                                                                </div>\n                                                                            </div>\n                                                                        </div>\n                                                                    </div>\n                                                                    <div class=\"card p-1 mb-1\">\n                                                                        <h5 class=\"text-primary\">Sospecho que el paciente requerir\xE1:</h5>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-secondary font-weight-bold rounded\" >\n                                                                            Interconsulta con m\xE9dico especialista:\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                            ").concat([null, 'null', undefined, 'undefined', 0].includes(_REQUIERE_INTERCONSULTA) ? 'No' : 'Si', "\n                                                                            ").concat(_CANTIDAD_ESPECIALISTAS > 0 ? ', ' + _CANTIDAD_ESPECIALISTAS + ' especialista' + (_CANTIDAD_ESPECIALISTAS > 1 ? 's' : '') : '', "\n                                                                            \n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-secondary font-weight-bold rounded\" >\n                                                                            \xBFRealizar procedimiento?\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                            ").concat([null, 'null', undefined, 'undefined', 0].includes(_REALIZAR_PROCEDIMIENTO) ? 'No' : 'Si', "\n                                                                            ").concat([null, 'null', undefined, 'undefined', 0].includes(_PROCEDIMIENTO_COMPLEJIDAD) ? '' : ', ' + _PROCEDIMIENTO_COMPLEJIDAD, "\n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-secondary font-weight-bold rounded\" >\n                                                                            \xBFHospitalizaci\xF3n?\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                                ").concat([null, 'null', undefined, 'undefined', 0].includes(_HOSPITALIZACION) ? 'No' : 'Si', "\n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-secondary font-weight-bold rounded\" >\n                                                                            \xBFTerapia intensiva?\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                                ").concat([null, 'null', undefined, 'undefined', 0].includes(_TERAPIA_INTENSIVA) ? 'No' : 'Si', "\n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-secondary font-weight-bold rounded\" >\n                                                                            \xBFCirug\xEDa?\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                                ").concat([null, 'null', undefined, 'undefined', 0].includes(_CIRUGIA) ? 'No' : 'Si', "\n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-secondary font-weight-bold rounded\" >\n                                                                            Clasificaci\xF3n del triaje\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                                ").concat([null, 'null', undefined, 'undefined'].includes(_NIVEL_TRIAJE) ? 'No indicado' : _NIVEL_TRIAJE, "\n                                                                            </div>\n                                                                        </div>\n                                                                    </div>\n                                                                </div>\n                                                                <div class=\"col-12 col-sm-8 col-md-9 col-lg-9 col-xl-9\">\n                                                                    <div class=\"card p-1 mb-1\">\n                                                                        \n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-primary font-weight-bold rounded\" >\n                                                                            Motivo de consulta\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                                ").concat([null, 'null', undefined, 'undefined'].includes(_MOTIVO_CONSULTA) ? 'No indicado' : _MOTIVO_CONSULTA, "\n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-primary font-weight-bold rounded\" >\n                                                                            Antecedentes\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                                ").concat([null, 'null', undefined, 'undefined'].includes(_ANTECEDENTES) ? 'No indicado' : _ANTECEDENTES, "\n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-primary font-weight-bold rounded\" >\n                                                                            Enfermedad Actual\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                                ").concat([null, 'null', undefined, 'undefined'].includes(_ENFERMEDAD_ACTUAL) ? 'No indicado' : _ENFERMEDAD_ACTUAL, "\n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-primary font-weight-bold rounded\" >\n                                                                            Examen F\xEDsico\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                                ").concat([null, 'null', undefined, 'undefined'].includes(_EXAMEN_FISICO) ? 'No indicado' : _EXAMEN_FISICO, "\n                                                                            </div>\n                                                                        </div>\n                                                                        <div class=\"d-flex flex-column mb-1\">\n                                                                            <div class=\"pl-2 text-primary font-weight-bold rounded\" >\n                                                                            Problema de ingreso\n                                                                            </div>\n                                                                            <div class=\"pl-2\">\n                                                                            ").concat(_IMP_DIAG ? [null, 'null', undefined, 'undefined'].includes(_IMP_DIAG.value) ? 'No indicado' : _IMP_DIAG.value : '', "\n                                                                            </div>\n                                                                        </div>\n                                                                    </div>\n                                                                </div>\n                                                            </div>\n                                                        </div>\n                                                    </div>\n                                                </div>\n                                            ");
                        } else {
                          var itemsOpcionesHistoria = "";
                          historia.episodio[key].items.forEach(function (x, index2) {
                            if (!["Traslados", "Equipo Médico"].includes(historia.episodio[key].title)) {
                              itemsOpcionesHistoria += item(x, historia.episodio[key].title);
                            }
                          });
                          items += "\n                                                <div class=\"tab-pane text-left fade\" id=\"nav-".concat(historia.episodio_id, "-tabs-").concat(index1, "\" role=\"tabpanel\" aria-labelledby=\"nav-").concat(historia.episodio_id, "-tabs-").concat(index1, "\">\n                                                    ").concat(itemsOpcionesHistoria, "\n                                                </div>\n                                            ");
                        }
                      }

                      index1++;
                    };

                    for (var key in historia.episodio) {
                      _loop(key);
                    }

                    cardhistorias += "\n                                <div class=\"card\">\n                                    <div class=\"card-header p-0\" id=\"heading".concat(index, "\">\n                                        <h2 class=\"mb-0\">\n                                            <button class=\"btn btn-link btn-block text-left\" type=\"button\" data-toggle=\"collapse\" data-target=\"#collapse").concat(index, "\" aria-expanded=\"true\" aria-controls=\"collapse").concat(index, "\">\n                                                <div class=\"d-flex align-content-md-end align-content-sm-start\">\n                                                    <div class=\"px-1 border-right\">\n                                                        <div class=\"m-0 description text-center\">\n                                                            <b class=\"text-primary\">Episodio:</b>\n                                                            <div class=\"font-weight-bold\">").concat(index + 1, "</div>\n                                                        </div>\n                                                    </div>\n                                                    <div class=\"px-1 border-right\">\n                                                        <div class=\"m-0 description text-center\">\n                                                            <b class=\"text-primary\">Ingreso:</b>\n                                                            <div class=\"font-weight-bold\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(historia.fecha_ingreso) ? '' : Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaCortaCustom1"])(historia.fecha_ingreso), "</div>\n                                                        </div>\n                                                    </div>\n                                                    <div class=\"px-1 border-right\">\n                                                        <div class=\"m-0 description text-center\">\n                                                            <b class=\"text-primary\">Egreso:</b>\n                                                            <div class=\"font-weight-bold\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(historia.fecha_egreso) ? '' : Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaCortaCustom1"])(historia.fecha_egreso), "</div>\n                                                        </div>\n                                                    </div>\n                                                    <div class=\"px-1 border-right\">\n                                                        <div class=\"m-0 description text-center\">\n                                                            <b class=\"text-primary\">D\xEDas de Hospitalizaci\xF3n:</b>\n                                                            <div class=\"font-weight-bold\">").concat(historia.dia_hos, "</div>\n                                                        </div>\n                                                    </div>\n                                                    <div class=\"px-1\">\n                                                        <div class=\"m-0 description text-center\">\n                                                            <b class=\"text-primary\">\xC1rea de Egreso:</b>\n                                                            <div class=\"font-weight-bold\">").concat(historia.area_de_egreso, "</div>\n                                                        </div>\n                                                    </div>\n                                                </div>\n                                            </button>\n                                        </h2>\n                                    </div>\n                                    <div id=\"collapse").concat(index, "\" class=\"collapse\" aria-labelledby=\"heading").concat(index, "\" data-parent=\"#accordionExample\">\n                                        <div class=\"card-body\">\n                                            <div class=\"row\">\n                                                <div class=\"col-5 col-sm-3 col-lg-2 col-xl-2\">\n                                                    <div class=\"nav flex-column nav-tabs\" id=\"vert-tabs-tab\" role=\"tablist\" aria-orientation=\"vertical\">\n                                                    ").concat(opcionesHistoria, "\n                                                    </div>\n                                                </div>\n                                                <div class=\"col-7 col-sm-9\">\n                                                    <div class=\"tab-content\" id=\"vert-tabs-tabContent\">\n                                                    ").concat(items, "\n                                                    </div>\n                                                </div>\n                                            </div>\n                                        </div>\n                                    </div>\n                                </div>\n                            ");
                  });
                  modalBody.innerHTML = "\n                        <div id=\"infoPaciente\" style=\"position: absolute;width: 100%;top:0;left:0;z-index: 200000;\"></div>\n\n                        <h1 class=\"text-center text-primary\" style=\"margin-top: 50px\">Historial de Episodios</h1>\n                        <div class=\"container-fluid\">\n                            <div class=\"row\">\n                                <div class=\"col-md-12\">\n\n                                    <div class=\"accordion\" id=\"accordionExample\">\n                                        ".concat(cardhistorias, "\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n                    ");
                }

              case 11:
              case "end":
                return _context5.stop();
            }
          }
        }, _callee5);
      }));

      function historialEpisodios(_x6, _x7) {
        return _historialEpisodios.apply(this, arguments);
      }

      return historialEpisodios;
    }()
  }, {
    key: "eliminar",
    value: function eliminar(_ref13) {
      var id = _ref13.id,
          user_id = _ref13.user_id;
      UserCuestEpisodio.destroy({
        id: id
      }).then(function (response) {
        UserCuestEpisodio.index({
          user_id: user_id
        });
      });
    }
  }, {
    key: "destroy",
    value: function destroy(_ref14) {
      var id = _ref14.id;
      var formdata = new FormData();
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/destroy/" + id, formdata);
    }
  }, {
    key: "regresar",
    value: function regresar(_ref15) {
      var user_id = _ref15.user_id,
          _ref15$selector = _ref15.selector,
          selector = _ref15$selector === void 0 ? '#modalTemplate_2_footer' : _ref15$selector;
      Component.btn_regresar({
        selector: selector,
        eventModel: "UserCuestEpisodio.index({user_id:" + user_id + "})"
      });
    }
  }, {
    key: "fixed_episodio",
    value: function () {
      var _fixed_episodio = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6(row) {
        var formdata, episodio;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
          while (1) {
            switch (_context6.prev = _context6.next) {
              case 0:
                //CREA EL EPISODIO SI EL PACIENTEW NO LO TIENE
                formdata = new FormData();
                formdata.append('value', "episodio_null");
                formdata.append("user_id", row.user_id);
                formdata.append("_token", $("#_token").val());
                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                _context6.next = 7;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/store", formdata);

              case 7:
                episodio = _context6.sent;
                return _context6.abrupt("return", episodio.id);

              case 9:
              case "end":
                return _context6.stop();
            }
          }
        }, _callee6);
      }));

      function fixed_episodio(_x8) {
        return _fixed_episodio.apply(this, arguments);
      }

      return fixed_episodio;
    }()
  }, {
    key: "updatePrealta",
    value: function () {
      var _updatePrealta = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee7(user_id, valor) {
        var formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee7$(_context7) {
          while (1) {
            switch (_context7.prev = _context7.next) {
              case 0:
                formdata = new FormData();
                formdata.append("user_id", user_id);
                formdata.append("_token", $("#_token").val());
                formdata.append("valor", valor);
                _context7.next = 6;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/" + UserCuestEpisodio.prefixRoute() + "/prealta", formdata);

              case 6:
                return _context7.abrupt("return", _context7.sent);

              case 7:
              case "end":
                return _context7.stop();
            }
          }
        }, _callee7);
      }));

      function updatePrealta(_x9, _x10) {
        return _updatePrealta.apply(this, arguments);
      }

      return updatePrealta;
    }()
  }, {
    key: "prealtaColor",
    value: function prealtaColor(fecha) {
      var date_1 = new Date(fecha);
      var date_1N = new Date(date_1.getFullYear(), date_1.getMonth() + 1, date_1.getDate() + 1);
      var date_2 = new Date(Date.now());
      var date_2N = new Date(date_2.getFullYear(), date_2.getMonth() + 1, date_2.getDate()); //console.log(date_1N.getTime(),date_2N.getTime())

      var day_as_milliseconds = 86400000;
      var diff_in_millisenconds = date_1N.getTime() - date_2N.getTime();
      var diff_in_days = diff_in_millisenconds / day_as_milliseconds;

      if (parseInt(diff_in_days) < 0) {
        diff_in_days = 0;
      } //console.log(diff_in_days)


      if (parseInt(diff_in_days) >= 2) {
        return "success";
      }

      if (parseInt(diff_in_days) === 1) {
        return "warning";
      }

      if (parseInt(diff_in_days) === 0) {
        return "danger";
      }

      return false;
    }
  }, {
    key: "addPrealta",
    value: function () {
      var _addPrealta = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee11(user_id) {
        var selector, model;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee11$(_context11) {
          while (1) {
            switch (_context11.prev = _context11.next) {
              case 0:
                selector = ".modal-body"; //incio component

                model = UserCuestEpisodio.classModel();
                Component.modalTemplate_3({
                  title: UserCuestEpisodio.modelTitle()
                }, /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee10() {
                  var calendar;
                  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee10$(_context10) {
                    while (1) {
                      switch (_context10.prev = _context10.next) {
                        case 0:
                          calendar = document.querySelector("#messageModal div.modal-body");
                          $(calendar).html("\n\n                    <div class=\"container-fluid\">\n                        <div class=\"row\">\n                            <div class=\"col-12 h3 text-primary font-weight-bold text-center\">\n                                Fecha Probable de Alta\n                            </div>\n\n                        </div>\n                    </div>\n                    <div id=\"calendar\" data-user_id=\"".concat(user_id, "\" class=\"daterange\" style=\"width: 100%\"></div>\n                    <div class=\"container-fluid\">\n                        <div class=\"row\">\n                            <div class=\"col-12\">\n                                <button id=\"store_prealta\" data-user_id=\"").concat(user_id, "\" class=\"btn btn-success btn-block my-1\">Confirmar</button>\n                            </div>\n                            <div class=\"col-12\">\n                                <button id=\"destroy_prealta\" class=\"btn btn-primary btn-block my-1\">Cancelar</button>\n                            </div>\n                        </div>\n                    </div>\n\n                "));
                          document.querySelector("#store_prealta").addEventListener("click", /*#__PURE__*/function () {
                            var _ref17 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee8(e) {
                              var d, fecha, date_1, date_1N, date_2, date_2N;
                              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee8$(_context8) {
                                while (1) {
                                  switch (_context8.prev = _context8.next) {
                                    case 0:
                                      //let user_id =document.querySelector("#store_prealta").dataset.user_id
                                      d = document;
                                      fecha = document.getElementById("fecha_prealta_".concat(user_id)).value;

                                      if (fecha !== "") {
                                        date_1 = new Date(fecha);
                                        date_1N = new Date(date_1.getFullYear(), date_1.getMonth() + 1, date_1.getDate() + 1);
                                        date_2 = new Date(Date.now());
                                        date_2N = new Date(date_2.getFullYear(), date_2.getMonth() + 1, date_2.getDate());

                                        if (date_1N.getTime() < date_2N.getTime()) {
                                          alert("La fecha ingresada no puede ser anterior a hoy.");
                                        } else {
                                          UserCuestEpisodio.updatePrealta(user_id, fecha).then(function (response) {
                                            var btn = d.querySelector("#col_1_".concat(user_id, " .calendar-prealta"));
                                            btn.classList.replace("fa-calendar", "fa-calendar-check");
                                            ["info", "danger", "warning", "success", "gray"].forEach(function (x) {
                                              return btn.classList.remove("text-" + x);
                                            });
                                            btn.classList.add("text-" + UserCuestEpisodio.prealtaColor(fecha));
                                            var card = d.querySelector("#col_1_".concat(user_id, " #card_prealta_").concat(user_id));
                                            card.classList.remove("d-none");

                                            if (UserCuestEpisodio.prealtaColor(fecha) !== "danger" && UserCuestEpisodio.prealtaColor(fecha) !== "warning") {
                                              card.classList.remove('heartbeat_infinity');
                                            } else {
                                              card.classList.add('heartbeat_infinity');
                                            }

                                            if (UserCuestEpisodio.prealtaColor(fecha) === "danger") document.getElementById('title_prealta_' + user_id).innerHTML = "ALTA";
                                            if (UserCuestEpisodio.prealtaColor(fecha) === "warning") document.getElementById('title_prealta_' + user_id).innerHTML = "PRE-ALTA";
                                            if (UserCuestEpisodio.prealtaColor(fecha) === "success") document.getElementById('title_prealta_' + user_id).innerHTML = "PRE-ALTA";

                                            if (UserCuestEpisodio.prealtaColor(fecha) !== "success") {
                                              d.querySelectorAll(".row_".concat(user_id, " > td")).forEach(function (x) {
                                                x.classList.replace("bg-gray-2", "prealta");
                                              });
                                            } else {
                                              d.querySelectorAll(".row_".concat(user_id, " > td")).forEach(function (x) {
                                                x.classList.replace("prealta", "bg-gray-2");
                                              });
                                            }

                                            pacientes_datos.map(function (x) {
                                              if (x.user_id == user_id) {
                                                x.prealta = fecha;
                                              }
                                            });
                                            Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["activarTooltip"])();
                                            $("#messageModal").modal("hide");
                                          });
                                        }
                                      } else {
                                        alert("No has seleccionado un día");
                                      }

                                    case 3:
                                    case "end":
                                      return _context8.stop();
                                  }
                                }
                              }, _callee8);
                            }));

                            return function (_x12) {
                              return _ref17.apply(this, arguments);
                            };
                          }());
                          document.querySelector("#destroy_prealta").addEventListener("click", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee9() {
                            return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee9$(_context9) {
                              while (1) {
                                switch (_context9.prev = _context9.next) {
                                  case 0:
                                    UserCuestEpisodio.updatePrealta(user_id, null).then(function (response) {
                                      var d = document;
                                      document.getElementById("fecha_prealta_".concat(user_id)).value = "";
                                      var btn = d.querySelector("#col_1_".concat(user_id, " .calendar-prealta"));
                                      btn.classList.replace("fa-calendar-check", "fa-calendar");
                                      ["info", "danger", "warning", "success"].forEach(function (x) {
                                        return btn.classList.remove("text-" + x);
                                      });
                                      btn.classList.add("text-gray");
                                      var card = d.querySelector("#col_1_".concat(user_id, " #card_prealta_").concat(user_id));
                                      card.classList.add("d-none");
                                      ["info", "danger", "warning", "success"].forEach(function (x) {
                                        card.classList.remove("tooltip-" + x);
                                        card.classList.remove("bg-" + x);
                                      });
                                      card.classList.add("tooltip-info");
                                      card.classList.add("bg-info");
                                      pacientes_datos.map(function (x) {
                                        if (x.user_id == user_id) {
                                          x.prealta = null;
                                        }
                                      });
                                      d.querySelectorAll(".row_".concat(user_id, " > td")).forEach(function (x) {
                                        x.classList.replace("prealta", "bg-gray-2");
                                      });
                                      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["activarTooltip"])();
                                      $("#messageModal").modal("hide");
                                    });

                                  case 1:
                                  case "end":
                                    return _context9.stop();
                                }
                              }
                            }, _callee9);
                          })));
                          $('#calendar').datepicker({
                            language: "es",
                            toggleActive: true,
                            todayHighlight: true
                          });
                          $('#calendar').datepicker().on("changeDate", function (e) {
                            var d = document;
                            var f = new Date(e.date);
                            var fechaIngreso = f.getFullYear() + "-" + (f.getMonth() + 1) + "-" + f.getDate();
                            document.getElementById("fecha_prealta_".concat(user_id)).value = fechaIngreso;
                            var btn = d.querySelector("#col_1_".concat(user_id, " .calendar-prealta"));
                            ["info", "danger", "warning", "success"].forEach(function (x) {
                              return btn.classList.remove("text-" + x);
                            });
                            btn.classList.add("text-" + UserCuestEpisodio.prealtaColor(fechaIngreso));
                            var card = d.querySelector("#col_1_".concat(user_id, " #card_prealta_").concat(user_id));
                            ["info", "danger", "warning", "success"].forEach(function (x) {
                              card.classList.remove("tooltip-" + x);
                              card.classList.remove("bg-" + x);
                            });
                            if (UserCuestEpisodio.prealtaColor(fechaIngreso) === "danger") document.getElementById('title_prealta_' + user_id).innerHTML = "ALTA";
                            if (UserCuestEpisodio.prealtaColor(fechaIngreso) === "warning") document.getElementById('title_prealta_' + user_id).innerHTML = "PRE-ALTA";
                            if (UserCuestEpisodio.prealtaColor(fechaIngreso) === "success") document.getElementById('title_prealta_' + user_id).innerHTML = "PRE-ALTA";
                            card.classList.add("tooltip-" + UserCuestEpisodio.prealtaColor(fechaIngreso));
                            card.classList.add("bg-" + UserCuestEpisodio.prealtaColor(fechaIngreso));
                            fechaIngreso = f.getDate() + "-" + capitalize(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).slice(0, 3)) + "-" + f.getFullYear();
                            card.querySelectorAll("div")[1].textContent = fechaIngreso;
                          });

                        case 6:
                        case "end":
                          return _context10.stop();
                      }
                    }
                  }, _callee10);
                }))); //fin component

              case 3:
              case "end":
                return _context11.stop();
            }
          }
        }, _callee11);
      }));

      function addPrealta(_x11) {
        return _addPrealta.apply(this, arguments);
      }

      return addPrealta;
    }()
  }]);

  return UserCuestEpisodio;
}();



/***/ }),

/***/ "./public/class_js/UserCuestFc.js":
/*!****************************************!*\
  !*** ./public/class_js/UserCuestFc.js ***!
  \****************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestFc; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__["default"]();

var UserCuestFc = /*#__PURE__*/function () {
  function UserCuestFc() {
    _classCallCheck(this, UserCuestFc);
  }

  _createClass(UserCuestFc, null, [{
    key: "index",
    value: function index(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                    Frecuencia Card\xEDaca\n                </h1>\n                <span id=\"user_cuest_model_create\" class=\"text-shadow-drop-tl text-primary mr-2\" style=\"float:right;font-size: 2rem !important; cursor:pointer;\">\n                    Nuevo valor\n                    <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                </span>\n            </div>\n            <div class=\"flex-fill overflow-auto mb-1\">\n                <table class=\"table table-bordered mb-3\">\n                    <tr>\n                        <th class=\"text-primary text-center\">Fecha</th>\n                        <th class=\"text-primary text-center\">Valor</th>\n                        <th class=\"text-primary text-center\">Observaci\xF3n</th>\n                        <th class=\"text-primary text-center\">Acci\xF3n</th>\n                    </tr>\n                    <tbody id=\"filas\">\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center\">\n                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                    <span class=\"sr-only\">Loading...</span>\n                                </div>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n            </div>\n\n            <div class=\"d-flex\">\n                <a class='btn btn-primary btn-block' data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</a>\n\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      var formdata = new FormData();
      formdata.append("user_id", user_id);
      formdata.append("episodio_id", episodio_id);
      formdata.append("_token", $("#_token").val());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_fc/show", formdata).then(function (response) {
        var filas = "";
        var tempRow = "";
        var dia = "";
        var count = 0;
        var m = "";
        var d = "";

        if (response.length > 0) {
          $(response).each(function (index, element) {
            d = new Date(element.created_at);

            if (dia != d.getDate()) {
              dia = d.getDate();
              count++;
              m = count;
            } else {
              m = "";
            }

            tempRow = "\n                            <tr id=\"temp_".concat(element.id, "\">\n                                <td class=\"text-center\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["jsUcfirst"])(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["meses"])(d.getMonth())) + "/" + d.getDate() + " a las " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["formatAMPM"])(d), "</td>\n                                <td class=\"text-center font-weight-bold ").concat(UserCuestFc.colorFc(element.value, 'historial'), "\" id=\"modal-temp-").concat(element.id, "\">").concat(element.value, " <small>lat/min</small></td>\n                                <td class=\"text-center\">\n                                    ").concat(element.coments != null ? element.coments : "", "\n                                </td>\n                                <td class=\"text-center\">\n\n                                </td>\n                                <td align=\"center\">\n                                <!-- <button title=\"Eliminar valor\" onclick=\"UserCuestFc.eliminar(").concat(element.id, ",").concat(user_id, ")\" class=\"delete-row btn btn-danger\" data-option=\"").concat(element.id, "\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button> -->\n                                </td>\n                            </tr>\n                        ");
            filas = filas + tempRow;
          });
        } else {
          filas = "\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center text-primary\">Sin registros</td>\n                        </tr>\n                    ";
        }

        $("#filas").html(filas);
      });
      $("#user_cuest_model_create").on('click', function () {
        UserCuestFc.create(".modal-body", user_id, episodio_id);
      });
    }
  }, {
    key: "create",
    value: function create(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex \">\n                <h1 class=\"h1 text-primary\">\n                Frecuencia Card\xEDaca\n                </h1>\n\n            </div>\n            <div class=\"d-flex mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0 mr-1\">\n                    <input type=\"number\" class=\"form-control bg-gray-footer-parodi\" name=\"cat_user_cuestionario_185\" id=\"cat_user_cuestionario_185\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0\">\n                    <input type=\"datetime-local\" class=\"form-control bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"sintoma_observacion\" id=\"sintoma_observacion\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"mr-1 font-weight-bold btn btn-success w-100\">Agregar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        UserCuestFc.index(selector, user_id, episodio_id);
      });
      $("#user_cuest_model_store").on("click", function () {
        UserCuestFc.store(user_id, episodio_id).then(function (response) {
          var valor = $("#cat_user_cuestionario_185").val();
          UserCuestFc.columnaValor("#col_7_".concat(user_id), {
            'user_id': user_id,
            'valor': valor,
            'episodio_id': episodio_id
          });
          UserCuestFc.index('.modal-body', user_id, episodio_id);
        });
      });
    }
  }, {
    key: "store",
    value: function store(user_id, episodio_id) {
      var message;
      var formdata = new FormData();
      var cat_user_cuestionario_185 = $("#cat_user_cuestionario_185");
      var created_at = $("#created_at");
      var sintoma_observacion = $("#sintoma_observacion");

      if (cat_user_cuestionario_185.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        cat_user_cuestionario_185.on('focus');
        return false;
      }

      if (created_at.val() == "") {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])();
      } else {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps2"])(created_at.val());
      }

      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("cat_user_cuestionario_185", cat_user_cuestionario_185.val());
      formdata.append("created_at", created_at);
      formdata.append("sintoma_observacion", sintoma_observacion.val());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_fc/store", formdata);
    }
  }, {
    key: "store3",
    value: function store3(episodio_id, user_id, value) {
      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("cat_user_cuestionario_185", value);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_fc/store", formdata);
    }
  }, {
    key: "destroy",
    value: function destroy(value) {
      var formdata = new FormData();
      formdata.append("id", value);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_fc/destroy", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var message = Component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        UserCuestFc.destroy(row_id).then(function (response) {
          if (response.length > 0) {
            UserCuestFc.columnaValor("#col_7_".concat(user_id), {
              'user_id': user_id,
              'valor': response[0].value
            });
          } else {
            $("#modelId").modal("hide");
            UserCuestFc.columnaValor("#col_7_".concat(user_id), {
              'user_id': user_id,
              'valor': null
            });
          }

          UserCuestFc.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "colorFc",
    value: function colorFc(value) {
      if (value < 55) {
        return "text-danger";
      }

      if (value >= 55 && value <= 59.9) {
        return "text-warning";
      }

      if (value >= 60 && value <= 100) {
        return "text-success";
      }

      if (value > 100) {
        return "text-danger";
      }
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      $(selector).html("\n            <div onclick=\"UserCuestFc.index('.modal-body',".concat(data.user_id, ",").concat(data.episodio_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    F.C.\n                </div>\n                ").concat(data.valor == "" || data.valor == null ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + UserCuestFc.colorFc(data.valor) + "'>" + data.valor + " lat/min</span>", "\n            </div>\n        "));
    }
  }]);

  return UserCuestFc;
}();



/***/ }),

/***/ "./public/class_js/UserCuestFr.js":
/*!****************************************!*\
  !*** ./public/class_js/UserCuestFr.js ***!
  \****************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestFr; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__["default"]();

var UserCuestFr = /*#__PURE__*/function () {
  function UserCuestFr() {
    _classCallCheck(this, UserCuestFr);
  }

  _createClass(UserCuestFr, null, [{
    key: "index",
    value: function index(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                    Frecuencia respiratoria\n                </h1>\n                <span id=\"user_cuest_model_create\" class=\"text-shadow-drop-tl text-primary mr-2\" style=\"float:right;font-size: 2rem !important; cursor:pointer;\">\n                    Nuevo valor\n                    <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                </span>\n            </div>\n            <div class=\"flex-fill overflow-auto mb-1\">\n                <table class=\"table table-bordered mb-3\">\n                    <tr>\n                        <th class=\"text-primary text-center\">Fecha</th>\n                        <th class=\"text-primary text-center\">Valor</th>\n                        <th class=\"text-primary text-center\">Observaci\xF3n</th>\n                        <th class=\"text-primary text-center\">Acci\xF3n</th>\n                    </tr>\n                    <tbody id=\"filas\">\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center\">\n                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                    <span class=\"sr-only\">Loading...</span>\n                                </div>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n            </div>\n\n            <div class=\"d-flex\">\n                <a class='btn btn-primary btn-block' data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</a>\n\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      var formdata = new FormData();
      formdata.append("user_id", user_id);
      formdata.append("episodio_id", episodio_id);
      formdata.append("_token", $("#_token").val());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_fr/show", formdata).then(function (response) {
        var filas = "";
        var tempRow = "";
        var dia = "";
        var count = 0;
        var m = "";
        var d = "";

        if (response.length > 0) {
          $(response).each(function (index, element) {
            d = new Date(element.created_at);

            if (dia != d.getDate()) {
              dia = d.getDate();
              count++;
              m = count;
            } else {
              m = "";
            }

            tempRow = "\n                            <tr id=\"temp_".concat(element.id, "\">\n                                <td class=\"text-center\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["jsUcfirst"])(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["meses"])(d.getMonth())) + "/" + d.getDate() + " a las " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["formatAMPM"])(d), "</td>\n                                <td class=\"text-center font-weight-bold ").concat(UserCuestFr.colorFr(element.value, 'historial'), "\" id=\"modal-temp-").concat(element.id, "\">").concat(element.value, " resp/min</td>\n                                <td class=\"text-center\">\n                                    ").concat(element.coments != null ? element.coments : "", "\n                                </td>\n                                <td align=\"center\">\n                                <!-- <button title=\"Eliminar valor\" onclick=\"UserCuestFr.eliminar(").concat(element.id, ",").concat(user_id, ")\" class=\"delete-row btn btn-danger\" data-option=\"").concat(element.id, "\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button>-->\n                                </td>\n                            </tr>\n                        ");
            filas = filas + tempRow;
          });
        } else {
          filas = "\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center text-primary\">Sin registros</td>\n                        </tr>\n                    ";
        }

        $("#filas").html(filas);
      });
      $("#user_cuest_model_create").on('click', function () {
        UserCuestFr.create(".modal-body", user_id, episodio_id);
      });
      /*  $("#info").on("click", function() {
           UserCuestFr.help(selector, user_id);
       }); */
    }
  }, {
    key: "create",
    value: function create(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex \">\n                <h1 class=\"h1 text-primary\">\n                Frecuencia respiratoria\n                </h1>\n\n            </div>\n            <div class=\"d-flex mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0 mr-1\">\n                    <input type=\"number\" class=\"form-control bg-gray-footer-parodi\" name=\"value\" id=\"value\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0\">\n                    <input type=\"datetime-local\" class=\"form-control bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"sintoma_observacion\" id=\"sintoma_observacion\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"mr-1 font-weight-bold btn btn-success w-100\">Agregar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        UserCuestFr.index(selector, user_id, episodio_id);
      });
      $("#user_cuest_model_store").on("click", function () {
        UserCuestFr.store(user_id, episodio_id).then(function (response) {
          var valor = $("#value").val();
          UserCuestFr.columnaValor("#col_8_".concat(user_id), {
            'user_id': user_id,
            'valor': valor,
            'episodio_id': episodio_id
          });
          UserCuestFr.index('.modal-body', user_id, episodio_id);
        });
      });
      $("#info").on("click", function () {
        UserCuestFr.help(selector, user_id);
      });
    }
  }, {
    key: "store",
    value: function store(user_id, episodio_id) {
      var message;
      var formdata = new FormData();
      var value = $("#value");
      var created_at = $("#created_at");
      var sintoma_observacion = $("#sintoma_observacion");

      if (value.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        value.focus();
        return false;
      }

      if (created_at.val() == "") {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])();
      } else {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps2"])(created_at.val());
      }

      formdata.append("user_id", user_id);
      formdata.append("episodio_id", episodio_id);
      formdata.append("value", value.val());
      formdata.append("created_at", created_at);
      formdata.append("sintoma_observacion", sintoma_observacion.val());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_fr/store", formdata);
    }
  }, {
    key: "store3",
    value: function store3(episodio_id, user_id, value) {
      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("value", value);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_fr/store", formdata);
    }
  }, {
    key: "destroy",
    value: function destroy(id) {
      var formdata = new FormData();
      formdata.append("id", id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_fr/destroy", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var message = Component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        UserCuestFr.destroy(row_id).then(function (response) {
          if (response.length > 0) {
            UserCuestFr.columnaValor("#col_8_".concat(user_id), {
              'user_id': user_id,
              'valor': response[0].value
            });
          } else {
            $("#modelId").modal("hide");
            UserCuestFr.columnaValor("#col_8_".concat(user_id), {
              'user_id': user_id,
              'valor': null
            });
          }

          UserCuestFr.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "colorFr",
    value: function colorFr(value) {
      if (value < 10) {
        return "text-danger";
      }

      if (value >= 10 && value <= 11.9) {
        return "text-warning";
      }

      if (value >= 12 && value <= 20) {
        return "text-success";
      }

      if (value >= 21 && value <= 22.9) {
        return "text-warning";
      }

      if (value > 23) {
        return "text-danger";
      }
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      $(selector).html("\n            <div onclick=\"UserCuestFr.index('.modal-body',".concat(data.user_id, ",").concat(data.episodio_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    F.R\n                </div>\n                ").concat(data.valor == "" || data.valor == null ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + UserCuestFr.colorFr(data.valor) + "'>" + data.valor + " resp/min</span>", "\n            </div>\n        "));
    }
  }]);

  return UserCuestFr;
}();



/***/ }),

/***/ "./public/class_js/UserCuestGl.js":
/*!****************************************!*\
  !*** ./public/class_js/UserCuestGl.js ***!
  \****************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestGl; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__["default"]();

var UserCuestGl = /*#__PURE__*/function () {
  function UserCuestGl() {
    _classCallCheck(this, UserCuestGl);
  }

  _createClass(UserCuestGl, null, [{
    key: "index",
    value: function index(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                Glicemia\n                </h1>\n                <span id=\"user_cuest_model_create\" class=\"text-shadow-drop-tl text-primary mr-2\" style=\"float:right;font-size: 2rem !important; cursor:pointer;\">\n                    Nuevo valor\n                    <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                </span>\n            </div>\n            <div class=\"flex-fill overflow-auto mb-1\">\n                <table class=\"table table-bordered mb-3\">\n                    <tr>\n                        <th class=\"text-primary text-center\">Fecha</th>\n                        <th class=\"text-primary text-center\">Valor</th>\n                        <th class=\"text-primary text-center\">Observaci\xF3n</th>\n                        <th class=\"text-primary text-center\">Acci\xF3n</th>\n                    </tr>\n                    <tbody id=\"filas\">\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center\">\n                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                    <span class=\"sr-only\">Loading...</span>\n                                </div>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n            </div>\n\n            <div class=\"d-flex\">\n                <a class='btn btn-primary btn-block' data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</a>\n\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_gl/show", formdata).then(function (response) {
        var filas = "";
        var tempRow = "";
        var dia = "";
        var count = 0;
        var m = "";
        var d = "";

        if (response.length > 0) {
          $(response).each(function (index, element) {
            d = new Date(element.created_at);

            if (dia != d.getDate()) {
              dia = d.getDate();
              count++;
              m = count;
            } else {
              m = "";
            }

            tempRow = "\n                            <tr id=\"model_".concat(element.id, "\">\n                                <td class=\"text-center\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["jsUcfirst"])(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["meses"])(d.getMonth())) + "/" + d.getDate() + " a las " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["formatAMPM"])(d), "</td>\n                                <td class=\"text-center font-weight-bold ").concat(UserCuestGl.colorGl(element.value, 'historial'), "\" id=\"modal-model-").concat(element.id, "\">").concat(element.value, " mg/dL</td>\n                                <td class=\"text-center\">\n                                    ").concat(element.coments != null ? element.coments : "", "\n                                </td>\n                                <td class=\"text-center\">\n                                </td>\n                                <td align=\"center\">\n                                <!-- <button title=\"Eliminar valor\" onclick=\"UserCuestGl.eliminar(").concat(element.id, ",").concat(user_id, ")\" class=\"delete-row btn btn-danger\" data-option=\"").concat(element.id, "\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button> -->\n                                </td>\n                            </tr>\n                        ");
            filas = filas + tempRow;
          });
        } else {
          filas = "\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center text-primary\">Sin registros</td>\n                        </tr>\n                    ";
        }

        $("#filas").html(filas);
      });
      $("#user_cuest_model_create").on('click', function () {
        UserCuestGl.create(".modal-body", user_id, episodio_id);
      });
      /*  $("#info").on("click", function() {
           UserCuestGl.help(selector, user_id);
       }); */
    }
  }, {
    key: "create",
    value: function create(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex \">\n                <h1 class=\"h1 text-primary\">\n                    Glicemia\n                </h1>\n            </div>\n            <div class=\"d-flex mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0 mr-1\">\n                    <input type=\"number\" class=\"form-control bg-gray-footer-parodi\" name=\"value\" id=\"value\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0\">\n                    <input type=\"datetime-local\" class=\"form-control bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"sintoma_observacion\" id=\"sintoma_observacion\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"mr-1 font-weight-bold btn btn-success w-100\">Agregar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        UserCuestGl.index(selector, user_id, episodio_id);
      });
      $("#user_cuest_model_store").on("click", function () {
        UserCuestGl.store(user_id, episodio_id).then(function (response) {
          var valor = $("#value").val();
          UserCuestGl.columnaValor("#col_10_".concat(user_id), {
            'user_id': user_id,
            'valor': valor,
            'episodio_id': episodio_id
          });
          UserCuestGl.index('.modal-body', user_id, episodio_id);
        });
      });
      /*  $("#info").on("click", function() {
           UserCuestGl.help(selector, user_id);
       }); */
    }
  }, {
    key: "store",
    value: function store(user_id, episodio_id) {
      var message;
      var formdata = new FormData();
      var value = $("#value");
      var created_at = $("#created_at");
      var sintoma_observacion = $("#sintoma_observacion");

      if (value.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        value.focus();
        return false;
      }

      if (created_at.val() == "") {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])();
      } else {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps2"])(created_at.val());
      }

      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("value", value.val());
      formdata.append("created_at", created_at);
      formdata.append("sintoma_observacion", sintoma_observacion.val());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_gl/store", formdata);
    }
  }, {
    key: "store3",
    value: function store3(episodio_id, user_id, value) {
      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("value", value);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_gl/store", formdata);
    }
  }, {
    key: "destroy",
    value: function destroy(id) {
      var formdata = new FormData();
      formdata.append("id", id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_gl/destroy", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var message = Component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        UserCuestGl.destroy(row_id).then(function (response) {
          if (response.length > 0) {
            UserCuestGl.columnaValor("#col_10_".concat(user_id), {
              'user_id': user_id,
              'valor': response[0].value
            });
          } else {
            $("#modelId").modal("hide");
            UserCuestGl.columnaValor("#col_10_".concat(user_id), {
              'user_id': user_id,
              'valor': null
            });
          }

          UserCuestGl.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "colorGl",
    value: function colorGl(value) {
      if (value < 60) {
        return "text-danger";
      }

      if (value >= 60 && value <= 69.9) {
        return "text-warning";
      }

      if (value >= 70 && value <= 100) {
        return "text-success";
      }

      if (value >= 100.1 && value <= 125) {
        return "text-warning";
      }

      if (value > 125) {
        return "text-danger";
      }
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      $(selector).html("\n            <div onclick=\"UserCuestGl.index('.modal-body',".concat(data.user_id, ",").concat(data.episodio_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    GL\n                </div>\n                ").concat(data.valor == "" || data.valor == null ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + UserCuestGl.colorGl(data.valor) + "'>" + data.valor + " mg/dL</span>", "\n            </div>\n        "));
    }
  }]);

  return UserCuestGl;
}();



/***/ }),

/***/ "./public/class_js/UserCuestHistoria.js":
/*!**********************************************!*\
  !*** ./public/class_js/UserCuestHistoria.js ***!
  \**********************************************/
/*! exports provided: create_historia_inicial, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "create_historia_inicial", function() { return create_historia_inicial; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestHistoria; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var vue__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! vue */ "./node_modules/vue/dist/vue.common.js");
/* harmony import */ var vue__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(vue__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
/* harmony import */ var _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./UserCuestUbicacion.js */ "./public/class_js/UserCuestUbicacion.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }

function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }



function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }





var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_3__["default"]();
var listTemplate1 =
/*html */
"\n    <div class=\"flex-fill d-flex flex-column overflow-auto\">\n        <ul class=\"nav bg-light justify-content-between nav-pills\" id=\"pills-tab\" role=\"tablist\">\n\n            <app-filter\n                :tipo_filtro=\"tipo_filtro\"\n                :filtro_valor=\"filtro_valor\"\n                @update-filtro=\"updateFiltro\"\n                :currentItem=\"currentItem\"\n                :dataEpisodio=\"dataEpisodio\"\n            />\n            <li\n                v-if=\"index !== 0\"\n                class=\"nav-item\" role=\"presentation\"\n                @click=\"sendDataToParent\"\n            >\n                <a\n\n                    class=\"nav-link currentItem\"\n                    id=\"pills-createItem-tab\"\n                    data-toggle=\"pill\"\n                    href=\"#pills-createItem\"\n                    role=\"tab\"\n                    aria-controls=\"pills-createItem\"\n                    aria-selected=\"false\"\n                >\n                    <h3 class=\"mb-0\">Agregar {{currentItem}}</h3>\n                </a>\n            </li>\n        </ul>\n        <div class=\"flex-fill tab-content d-flex flex-column overflow-auto\" id=\"pills-tabContent\">\n            <div\n                class=\"tab-pane overflow-auto fade show active\"\n                id=\"pills-listItems\"\n                role=\"tabpanel\"\n                aria-labelledby=\"pills-listItems-tab\"\n            >\n                <app-list-items-historia\n                    :patient_data=\"patient_data\"\n                    :dataEpisodio=\"dataEpisodio\"\n                    :index=\"index\"\n                />\n            </div>\n            <div class=\"tab-pane fade flex-fill flex-column\" id=\"pills-createItem\" role=\"tabpanel\" aria-labelledby=\"pills-profile-tab\">\n                <component\n                    :is=\"currentComponent\"\n                    :currentItem=\"currentItem\"\n                ></component>\n\n            </div>\n        </div>\n        <div class=\"d-none bg-danger p-4\"></div>\n    </div>\n";
var listTemplate2 =
/*html */
"\n    <div class=\"flex-fill d-flex flex-column overflow-auto\">\n        <ul class=\"nav bg-light justify-content-between nav-pills\" id=\"pills-tab\" role=\"tablist\">\n\n            <app-filter\n                :tipo_filtro=\"tipo_filtro\"\n                :filtro_valor=\"filtro_valor\"\n                @update-filtro=\"updateFiltro\"\n                :currentItem=\"currentItem\"\n                :dataEpisodio=\"dataEpisodio\"\n            />\n            <li\n                v-if=\"index !== 0\"\n                class=\"nav-item\" role=\"presentation\"\n                @click=\"sendDataToParent\"\n            >\n                <a\n\n                    class=\"nav-link currentItem\"\n                    id=\"pills-createItem-tab\"\n                    data-toggle=\"pill\"\n                    href=\"#pills-createItem\"\n                    role=\"tab\"\n                    aria-controls=\"pills-createItem\"\n                    aria-selected=\"false\"\n                >\n                    <h3 class=\"mb-0\">Agregar {{currentItem}}</h3>\n                </a>\n            </li>\n        </ul>\n        <div class=\"flex-fill tab-content d-flex flex-column overflow-auto\" id=\"pills-tabContent\">\n            <div\n                class=\"tab-pane overflow-auto fade show active\"\n                id=\"pills-listItems\"\n                role=\"tabpanel\"\n                aria-labelledby=\"pills-listItems-tab\"\n            >\n            <div class=\"table-responsive\">\n            <table class=\"table table-bordered mb-3\">\n                <thead>\n                    <tr>\n                        <th class=\"text-primary text-center\">\n                            #\n                        </th>\n                        <th class=\"text-primary text-center\">Antecedentes/Comorbilidad de Egreso</th>\n                        <th class=\"d-none text-primary text-center\">Acci\xF3n</th>\n\n                    </tr>\n                </thead>\n                <!--<tbody v-if=\"items.length > 0\">\n                    <tr v-for=\"(item,index) in items\" :key=\"index\">\n                        <td style=\"width: 20px;\" class=\"text-right text-secondary\" id=\"modal-793\">\n                            {{ index+1 }}\n                        </td>\n                        <td class=\"text-secondary\" id=\"modal-793\">\n                            {{ item.description }}\n                        </td>\n                        <td class=\"d-none\" style=\"width: 60px;\" align=\"center\">\n                            <button  data-tippy-content=\"Eliminar valor\" class=\"delete-row btn btn-danger\" data-id=\"793\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button>\n                        </td>\n                    </tr>\n                </tbody>-->\n                <tbody v-else>\n                    <tr>\n                        <td colspan=\"3\" class=\"text-center text-primary\">\n                            No se encontraron resultados\n                        </td>\n                    </tr>\n                </tbody>\n            </table>\n        </div>\n            </div>\n            <div class=\"tab-pane fade flex-fill flex-column\" id=\"pills-createItem\" role=\"tabpanel\" aria-labelledby=\"pills-profile-tab\">\n                <component\n                    :is=\"currentComponent\"\n                    :currentItem=\"currentItem\"\n                ></component>\n\n            </div>\n        </div>\n        <div class=\"d-none bg-danger p-4\"></div>\n    </div>\n";

var ApphistoriaInicialSignosVitales = function ApphistoriaInicialSignosVitales() {
  return {
    name: "ApphistoriaInicialSignosVitales",
    props: {
      parentForm: Object,
      patient_data: Object
    },
    template:
    /*html */
    "\n            <div class=\"card card-outline collapsed-card card-primary\" style=\"background-color: rgba(0, 0, 0, 0.03);\">\n                <div class=\"card-header cursor-pointer\" data-card-widget=\"collapse\">\n                    <h3 class=\"card-title text-primary font-weight-bold\">Signos Vitales</h3>\n                </div>\n                <div id=\"signosVitales\" class=\"card-body p-0\" style=\"background-color: rgba(0, 0, 0, 0.03); display: block;\">\n                    <ul class=\"list-group list-group-flush\">\n                        <li class=\"list-group-item p-0\">\n\n                            <div class=\"input-group m-0 rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 60px;\">PESO</span>\n                                </div>\n                                <input v-model=\"form.peso\" @keyup=\"updateParentForm()\"  name=\"user_peso\" type=\"text\" aria-label=\"First name\" class=\"form-control rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 75px;\">Kg.</span>\n                                </div>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-0\">\n\n                            <div class=\"input-group m-0 rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 60px;\">TALLA</span>\n                                </div>\n                                <input v-model=\"form.talla\" @keyup=\"updateParentForm()\" name=\"user_talla\" type=\"text\" aria-label=\"First name\" class=\"form-control rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 75px;\">Cm.</span>\n                                </div>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-0\">\n\n                            <div class=\"input-group m-0 rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 60px;\">TEMP.</span>\n                                </div>\n                                <input v-model=\"form.temp\" @keyup=\"updateParentForm()\" name=\"user_temperatura\" type=\"text\" aria-label=\"First name\" class=\"form-control rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 75px;\">\xB0C</span>\n                                </div>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-0\">\n\n                            <div class=\"input-group m-0 rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 60px;\">OXI</span>\n                                </div>\n                                <input v-model=\"form.oxi\" @keyup=\"updateParentForm()\" name=\"user_oximetria\" type=\"text\" aria-label=\"First name\" class=\"form-control rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 75px;\">%</span>\n                                </div>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-0\">\n\n                            <div class=\"input-group m-0 rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 60px;\">F.C.</span>\n                                </div>\n                                <input v-model=\"form.fc\" @keyup=\"updateParentForm()\" name=\"user_fc\" type=\"text\" aria-label=\"First name\" class=\"form-control rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 75px;\">lat/min</span>\n                                </div>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-0\">\n\n                            <div class=\"input-group m-0 rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 60px;\">F.R.</span>\n                                </div>\n                                <input v-model=\"form.fr\" @keyup=\"updateParentForm()\" name=\"user_fr\" type=\"text\" aria-label=\"First name\" class=\"form-control rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 75px;\">resp/min</span>\n                                </div>\n                            </div>\n                        </li>\n\n                        <li class=\"list-group-item p-0\">\n\n                            <div class=\"input-group m-0 rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 60px;\">GL.</span>\n                                </div>\n                                <input v-model=\"form.gl\" @keyup=\"updateParentForm()\" name=\"user_glic\" type=\"text\" aria-label=\"First name\" class=\"form-control rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 75px;\">mg/dL</span>\n                                </div>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-0\">\n                            <div class=\"input-group m-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                <span class=\"input-group-text rounded-0 border-0\" style=\"width: 60px;\">T.A.</span>\n                                </div>\n                                <input v-model=\"form.ta_sis\" @keyup=\"updateParentForm\" name=\"user_ta_sis\" placeholder=\"Sist\xF3lica\" type=\"text\" aria-label=\"First name\" class=\"form-control rounded-0 border-0\">\n                                <input v-model=\"form.ta_dia\" @keyup=\"updateParentForm\" name=\"user_ta_dia\" placeholder=\"Diast\xF3lica\" type=\"text\" aria-label=\"Last name\" class=\"form-control rounded-0 border-0\">\n                                <div class=\"input-group-prepend rounded-0 border-0\">\n                                    <span class=\"input-group-text rounded-0 border-0\" style=\"width: 75px;\">mmHg</span>\n                                </div>\n                            </div>\n\n                        </li>\n\n\n                    </ul>\n                </div>\n            </div>\n        ",
    components: {},
    data: function data() {
      return {
        form: {
          peso: "",
          talla: "",
          temp: "",
          oxi: "",
          fc: "",
          fr: "",
          gl: "",
          ta_sis: "",
          ta_dia: ""
        }
      };
    },
    methods: {
      updateParentForm: function updateParentForm() {
        this.$emit('formupdate', this.form);
      }
    },
    mounted: function mounted() {
      var _this = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                _this.form = _this.parentForm;

              case 1:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }))();
    }
  };
};

var ApphistoriaInicialSospechas = function ApphistoriaInicialSospechas() {
  return {
    name: "ApphistoriaInicialSospechas",
    props: {
      parentForm: Object,
      patient_data: Object
    },
    template:
    /*html */
    "\n            <div class=\"card card-outline collapsed-card card-primary\" style=\"background-color: rgba(0, 0, 0, 0.03);\">\n                <div class=\"card-header cursor-pointer\" data-card-widget=\"collapse\">\n                    <h3 class=\"card-title text-primary font-weight-bold\">Sospecho que el paciente requerir\xE1:</h3>\n                    <div class=\"card-tools\">\n\n                    </div>\n                </div>\n                <div id=\"evaluacionIngreso\" class=\"card-body p-0\" style=\"background-color: rgba(0, 0, 0, 0.03); display: block;\">\n                    <ul class=\"list-group list-group-flush\">\n             \n                        <li class=\"list-group-item p-1\">\n                            <div class=\"form-group mb-0\">\n                                <label class=\"text-gray mb-0\" for=\"\">Interconsulta con m\xE9dico especialista:</label>\n                                <select v-model=\"form.requiere_intersonsulta\" @change=\"req_interconsulta()\" class=\"form-control border-0\" name=\"requiere_intersonsulta\" id=\"requiere_intersonsulta\">\n                                    <option value=\"0\" selected>No</option>\n                                    <option value=\"1\">Si</option>\n                                </select>\n                            \n                            </div>\n                        </li>\n                        <li  v-if=\"Number(form.requiere_intersonsulta) === 1\" class=\"list-group-item p-1\">\n                            <div class=\"form-group mb-0\">\n               \n                                <label>Seleccione una opci\xF3n:</label>\n                                <select v-model=\"form.cantidad_especialistas\" class=\"form-control border-0\" style=\"background-color: #f9f9f9;\" @change=\"updateParentForm()\" name=\"cantidad_especialistas\" id=\"cantidad_especialistas\">\n                                    <option value=\"1\">1 especialistas</option>\n                                    <option value=\"2\">2 especialistas</option>\n                                    <option value=\"3\">+2 especialistas</option>\n                                </select>\n                                \n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-1\">\n                            <div class=\"form-group mb-0\">\n                                <label class=\"text-gray mb-0\" for=\"\">\xBFRealizar procedimiento?</label>\n                                <select v-model=\"form.realizar_procedimiento\" @change=\"rea_procedimiento()\" class=\"form-control border-0\" name=\"realizar_procedimiento\" id=\"realizar_procedimiento\">\n                                    <option value=\"0\" selected>No</option>\n                                    <option value=\"1\">Si</option>\n                                </select>\n                            \n                            </div>\n                        </li>\n                        <li v-if=\"Number(form.realizar_procedimiento) === 1\" class=\"list-group-item p-1\">\n                            <div class=\"form-group mb-0\">\n                                <label class=\"text-gray mb-0\" for=\"\">Complejidad</label>\n                                <select v-model=\"form.procedimiento_complejidad\" @change=\"updateParentForm()\" class=\"form-control border-0\" name=\"procedimiento_complejidad\" id=\"procedimiento_complejidad\">\n                                    <option value=\"\">Seleccione</option>\n                                    <option value=\"Procedimiento de baja complejidad\" selected>Procedimiento de baja complejidad</option>\n                                    <option value=\"Procedimiento de alta complejidad\">Procedimiento de alta complejidad</option>\n                                </select>\n                            \n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-1\">\n                            <div class=\"form-group mb-0\">\n                                <label class=\"text-gray mb-0\" for=\"\">Hospitalizaci\xF3n:</label>\n                                <select v-model=\"form.requiere_hospitalizacion\" @change=\"updateParentForm()\" class=\"form-control border-0\" name=\"hospitalizacion\" id=\"hospitalizacion\">\n                                    <option value=\"0\">No</option>\n                                    <option value=\"1\">Si</option>\n                                </select>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-1\">\n                            <div class=\"form-group mb-0\">\n                                <label class=\"text-gray mb-0\" for=\"\">Terapia Intensiva:</label>\n                                <select v-model=\"form.requiere_uti\" @change=\"updateParentForm()\" class=\"form-control border-0\" name=\"terapia_intensiva\" id=\"terapia_intensiva\">\n                                    <option value=\"0\">No</option>\n                                    <option value=\"1\">Si</option>\n\n                                </select>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-1\">\n                            <div class=\"form-group mb-0\">\n                                <label class=\"text-gray mb-0\" for=\"\">Cirug\xEDa:</label>\n                                <select v-model=\"form.requiere_cirugia\" class=\"form-control border-0\" @change=\"updateParentForm()\" name=\"caso_tipo_medico\" id=\"caso_tipo_medico\">\n                                    <option value=\"0\">No</option>\n                                    <option value=\"1\">Si</option>\n                                </select>\n                            </div>\n                        </li>\n                        <li class=\"list-group-item p-1\">\n                            <div class=\"form-group mb-0\">\n                                <label class=\"text-gray mb-0\" for=\"\">Clasificaci\xF3n del Triaje:</label>\n                                <select v-model=\"form.nivel_triaje\" class=\"form-control border-0\" @change=\"updateParentForm()\" name=\"nivel_triaje\" id=\"nivel_triaje\">\n                                    <option value=\"null\">Seleccione</option>\n                                    <option value=\"1\">Nivel 1</option>\n                                    <option value=\"2\">Nivel 2</option>\n                                    <option value=\"3\">Nivel 3</option>\n                                    <option value=\"4\">Nivel 4</option>\n                                    <option value=\"5\">Nivel 5</option>\n                                </select>\n                            </div>\n                        </li>\n\n\n                    </ul>\n                </div>\n                <!-- /.card-body -->\n            </div>\n        ",
    components: {},
    data: function data() {
      return {
        filtro: '',
        form: {
          requiere_intersonsulta: "0",
          realizar_procedimiento: "0",
          cantidad_especialistas: "0",
          procedimiento_complejidad: "",
          requiere_hospitalizacion: "0",
          requiere_uti: "0",
          requiere_cirugia: "0",
          nivel_triaje: "null"
        }
      };
    },
    methods: {
      req_interconsulta: function req_interconsulta() {
        if (this.form.requiere_intersonsulta === "0") {
          this.form.doctor_id = null;
        }
      },
      rea_procedimiento: function rea_procedimiento() {
        if (this.form.requiere_hospitalizacion === "0") {
          this.form.procedimiento_complejidad = "";
        }
      },
      updateParentForm: function updateParentForm() {
        this.$emit('formupdate', this.form);
      }
    },
    mounted: function mounted() {
      var _this2 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
          while (1) {
            switch (_context2.prev = _context2.next) {
              case 0:
                _this2.form = _this2.parentForm;

              case 1:
              case "end":
                return _context2.stop();
            }
          }
        }, _callee2);
      }))();
    }
  };
};

var create_historia_inicial = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee18(patient_data, episodio_index) {
    var app1;
    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee18$(_context18) {
      while (1) {
        switch (_context18.prev = _context18.next) {
          case 0:
            $("#modal-patient-item").modal("show");
            document.querySelector("#modal-patient-item .modal-body-2").innerHTML =
            /*html */
            "\n        <div id=\"AppHistoriaInicial\"></div>\n    ";
            app1 = new Vue({
              name: "AppHistoriaInicial",
              el: '#AppHistoriaInicial',
              template:
              /*html */
              "\n            <div class=\"d-flex flex-column flex-fill overflow-auto\">\n                <div id=\"infoPaciente\"></div>\n                <div class=\"flex-fill d-flex flex-md-row flex-column overflow-auto\">\n                    <div  class=\"container\">\n                        <div class=\"row justify-content-center\">\n                            <div class=\"col-md-12\">\n                                <h3 class=\"text-primary\">Historia Preliminar de Emergencia</h3>\n                            </div>\n                            <div class=\"col-md-4\">\n                                <app-historia-inicial-signos-vitales\n                                    :parentForm=\"form\"\n                                    @formupdate=\"formUpdate\"\n                                />\n                                <br>\n                                <app-sospechas-triaje\n                                    :parentForm=\"form\"\n                                    @formupdate=\"formUpdate\"\n                                >\n                            </div>\n                            <div id=\"datos_ingreso\" class=\"col-md-8\">\n\n\n                                <div class=\"card card-outline collapsed-card card-primary\"\n                                    style=\"background-color: rgba(0, 0, 0, 0.03);\">\n                                    <div class=\"card-header cursor-pointer\" data-card-widget=\"collapse\">\n                                        <h3 class=\"card-title text-primary font-weight-bold\">Motivo de consulta</h3>\n                                        <div class=\"card-tools\">\n\n                                        </div>\n                                    </div>\n                                    <div class=\"card-body p-0\" style=\"background-color: rgba(0, 0, 0, 0.03); display: block;\">\n                                        <textarea v-model=\"form.motivo_consulta\" class=\"form-control textarea\" placeholder=\"Escriba aqu\xED por qu\xE9 el paciente acudi\xF3 a la emergencia\" name=\"user_motivo_consulta\" id=\"user_problema_salud\"\n                                            rows=\"3\"></textarea>\n\n                                    </div>\n                                </div>\n                                <br>\n                                <div class=\"card card-outline collapsed-card card-primary\"\n                                    style=\"background-color: rgba(0, 0, 0, 0.03);\">\n                                    <div class=\"card-header cursor-pointer\" data-card-widget=\"collapse\">\n                                        <h3 class=\"card-title text-primary font-weight-bold\">Antecedentes\n                                        </h3>\n                                        <div class=\"card-tools\">\n\n                                        </div>\n                                    </div>\n                                    <div class=\"card-body p-0\" style=\"background-color: rgba(0, 0, 0, 0.03); display: block;\">\n                                        <textarea v-model=\"form.antecedentes\" class=\"form-control textarea\" placeholder=\"Escriba aqu\xED los antecedentes del paciente\" name=\"user_antecedentes\" id=\"user_antecedentes\"\n                                            rows=\"3\"></textarea>\n                                    </div>\n                                </div>\n                                <br>\n                                <div class=\"card card-outline collapsed-card card-primary\"\n                                    style=\"background-color: rgba(0, 0, 0, 0.03);\">\n                                    <div class=\"card-header cursor-pointer\" data-card-widget=\"collapse\">\n                                        <h3 class=\"card-title text-primary font-weight-bold\">Enfermedad Actual</h3>\n                                        <div class=\"card-tools\">\n\n                                        </div>\n                                    </div>\n                                    <div class=\"card-body p-0\" style=\"background-color: rgba(0, 0, 0, 0.03); display: block;\">\n                                        <textarea v-model=\"form.enfermedad_actual\" class=\"form-control textarea\" placeholder=\"Describa aqu\xED el problema de salud actual\" name=\"user_enfermedad_actual\" id=\"user_problema_salud\"\n                                            rows=\"3\"></textarea>\n                                    </div>\n                                </div>\n                                <br>\n                                <div class=\"card card-outline collapsed-card card-primary\"\n                                    style=\"background-color: rgba(0, 0, 0, 0.03);\">\n                                    <div class=\"card-header cursor-pointer\" data-card-widget=\"collapse\">\n                                        <h3 class=\"card-title text-primary font-weight-bold\">Examen F\xEDsico</h3>\n                                        <div class=\"card-tools\">\n\n                                        </div>\n                                    </div>\n                                    <div class=\"card-body p-0\" style=\"background-color: rgba(0, 0, 0, 0.03); display: block;\">\n                                        <textarea v-model=\"form.examen_fisico\" class=\"form-control textarea\" placeholder=\"Describa brevemente los hallazgos positivos del examen f\xEDsico realizado.\" name=\"user_examen_fisico\" id=\"user_examen_fisico\"\n                                            rows=\"3\"></textarea>\n                                    </div>\n                                </div>\n                                <br>\n                                <div class=\"card card-outline collapsed-card card-primary\"\n                                    style=\"background-color: rgba(0, 0, 0, 0.03);\">\n                                    <div class=\"card-header cursor-pointer\" data-card-widget=\"collapse\">\n                                        <h3 class=\"card-title text-primary font-weight-bold\">Problema de ingreso\n                                        </h3>\n                                        <div class=\"card-tools\">\n\n                                        </div>\n                                    </div>\n                                    <div class=\"card-body p-0\" style=\"background-color: rgba(0, 0, 0, 0.03); display: block;\">\n                                        <textarea v-model=\"form.problema_de_ingreso\" class=\"form-control textarea\" placeholder=\"Escriba aqu\xED el diagn\xF3stico presuntivo del paciente\" name=\"user_diagnostico_presuntivo\" id=\"user_diagnostico_presuntivo\"\n                                            rows=\"3\"></textarea>\n                                    </div>\n                                </div>\n\n\n                            </div>\n\n                        </div>\n                    </div>\n                </div>\n                <div  class=\"container-fluid mt-1\">\n                    <div :class=\"['row',{'justify-content-center':!informeGenerado}]\">\n                        <div v-if=\"informeGenerado\" class=\"col-12 col-md-4 mb-1 order-3 order-md-1\">\n                            <button  @click=\"openInformeEmergencia()\" class=\"w-100 btn btn-info\">Informe Preliminar de Emergencia</button>\n                        </div>\n                        <div class=\"col-6 col-md-4 mb-1 order-1 order-md-2\">\n                            <button @click=\"handle_store()\" class=\"w-100 btn btn-success\">Guardar</button>\n                        </div>\n                        <div class=\"col-6 mb-1 col-md-4 order-2 \">\n                            <button data-dismiss=\"modal\" class=\"w-100 btn btn-primary\">Regresar</button>\n                        </div>\n\n\n                    </div>\n                </div>\n            </div>\n        ",
              components: {
                'app-historia-inicial-signos-vitales': ApphistoriaInicialSignosVitales(),
                'app-sospechas-triaje': ApphistoriaInicialSospechas()
              },
              computed: {},
              data: function data() {
                return {
                  patient_data: patient_data,
                  index: episodio_index,
                  loading: document.getElementById('loadingScreen'),
                  informeGenerado: false,
                  form: {
                    peso: "",
                    talla: "",
                    temp: "",
                    oxi: "",
                    fc: "",
                    fr: "",
                    gl: "",
                    ta_sis: "",
                    ta_dia: "",
                    //
                    requiere_intersonsulta: "0",
                    realizar_procedimiento: "0",
                    cantidad_especialistas: "0",
                    procedimiento_complejidad: "",
                    requiere_hospitalizacion: "0",
                    requiere_uti: "0",
                    requiere_cirugia: "0",
                    nivel_triaje: "null",
                    //
                    motivo_consulta: "",
                    antecedentes: "",
                    enfermedad_actual: "",
                    examen_fisico: "",
                    problema_de_ingreso: ""
                  }
                };
              },
              methods: {
                formUpdate: function formUpdate(data) {
                  for (var key in this.form) {
                    if (Object.hasOwnProperty.call(data, key)) {
                      this.form[key] = data[key];
                    }
                  }
                },
                get_historiaIngreso: function get_historiaIngreso() {
                  var _this3 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3() {
                    var formdata, result, _result$episodio$SIGN, TEMP, OXI, FC, FR, GLIC, TA, TALLA, PESO, _result$episodio, HOSPITALIZACION, TERAPIA_INTENSIVA, NIVEL_TRIAJE, IMP_DIAG, CIRUGIA, MOTIVO_CONSULTA, ENFERMEDAD_ACTUAL, ANTECEDENTES, EXAMEN_FISICO, REQUIERE_INTERCONSULTA, REALIZAR_PROCEDIMIENTO, CANTIDAD_ESPECIALISTAS, PROCEDIMIENTO_COMPLEJIDAD, existeInforme;

                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
                      while (1) {
                        switch (_context3.prev = _context3.next) {
                          case 0:
                            _this3.loading.classList.remove("d-none");

                            formdata = new FormData();
                            formdata.append("user_id", _this3.patient_data.user_id);
                            formdata.append("_token", $("#_token").val());
                            _context3.next = 6;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_episodio/get_historiaIngreso", formdata);

                          case 6:
                            result = _context3.sent;
                            _result$episodio$SIGN = result.episodio.SIGNOS, TEMP = _result$episodio$SIGN.TEMP, OXI = _result$episodio$SIGN.OXI, FC = _result$episodio$SIGN.FC, FR = _result$episodio$SIGN.FR, GLIC = _result$episodio$SIGN.GLIC, TA = _result$episodio$SIGN.TA, TALLA = _result$episodio$SIGN.TALLA, PESO = _result$episodio$SIGN.PESO;
                            _result$episodio = result.episodio, HOSPITALIZACION = _result$episodio.HOSPITALIZACION, TERAPIA_INTENSIVA = _result$episodio.TERAPIA_INTENSIVA, NIVEL_TRIAJE = _result$episodio.NIVEL_TRIAJE, IMP_DIAG = _result$episodio.IMP_DIAG, CIRUGIA = _result$episodio.CIRUGIA, MOTIVO_CONSULTA = _result$episodio.MOTIVO_CONSULTA, ENFERMEDAD_ACTUAL = _result$episodio.ENFERMEDAD_ACTUAL, ANTECEDENTES = _result$episodio.ANTECEDENTES, EXAMEN_FISICO = _result$episodio.EXAMEN_FISICO, REQUIERE_INTERCONSULTA = _result$episodio.REQUIERE_INTERCONSULTA, REALIZAR_PROCEDIMIENTO = _result$episodio.REALIZAR_PROCEDIMIENTO, CANTIDAD_ESPECIALISTAS = _result$episodio.CANTIDAD_ESPECIALISTAS, PROCEDIMIENTO_COMPLEJIDAD = _result$episodio.PROCEDIMIENTO_COMPLEJIDAD;
                            _this3.form.temp = TEMP !== null ? TEMP.value : "";
                            _this3.form.oxi = OXI !== null ? OXI.value : "";
                            _this3.form.fc = FC !== null ? FC.value : "";
                            _this3.form.fr = FR !== null ? FR.value : "";
                            _this3.form.gl = GLIC !== null ? GLIC.value : "";
                            _this3.form.ta_sis = TA !== null ? TA.value : "";
                            _this3.form.ta_dia = TA !== null ? TA.value2 : "";
                            _this3.form.peso = PESO !== null ? PESO.value : "";
                            _this3.form.talla = TALLA !== null ? TALLA.value : "";
                            _this3.form.requiere_intersonsulta = [null, 0].includes(REQUIERE_INTERCONSULTA) ? 0 : 1;
                            _this3.form.realizar_procedimiento = [null, 0].includes(REALIZAR_PROCEDIMIENTO) ? 0 : 1;
                            _this3.form.cantidad_especialistas = CANTIDAD_ESPECIALISTAS === null ? null : CANTIDAD_ESPECIALISTAS;
                            _this3.form.procedimiento_complejidad = PROCEDIMIENTO_COMPLEJIDAD === null ? "" : PROCEDIMIENTO_COMPLEJIDAD;
                            _this3.form.requiere_hospitalizacion = [null, 0].includes(HOSPITALIZACION) ? 0 : 1;
                            _this3.form.requiere_uti = [null, 0].includes(TERAPIA_INTENSIVA) ? 0 : 1;
                            _this3.form.requiere_cirugia = [null, 0].includes(CIRUGIA) ? 0 : 1;
                            _this3.form.nivel_triaje = [null, 'null'].includes(NIVEL_TRIAJE) ? 0 : NIVEL_TRIAJE;
                            _this3.form.motivo_consulta = MOTIVO_CONSULTA === null ? "" : MOTIVO_CONSULTA;
                            _this3.form.antecedentes = ANTECEDENTES === null ? "" : ANTECEDENTES;
                            _this3.form.enfermedad_actual = ENFERMEDAD_ACTUAL === null ? "" : ENFERMEDAD_ACTUAL;
                            _this3.form.examen_fisico = EXAMEN_FISICO === null ? "" : EXAMEN_FISICO;
                            _this3.form.problema_de_ingreso = IMP_DIAG === null ? "" : IMP_DIAG.value;
                            _context3.next = 33;
                            return _this3.existInformeEmergencia();

                          case 33:
                            existeInforme = _context3.sent;

                            if (existeInforme !== undefined) {
                              _this3.informeGenerado = true;
                            } //console.log(existeInforme)


                            _this3.loading.classList.add("d-none");

                          case 36:
                          case "end":
                            return _context3.stop();
                        }
                      }
                    }, _callee3);
                  }))();
                },
                storeMotivoConsulta: function storeMotivoConsulta() {
                  var _this4 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4() {
                    var formdata;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
                      while (1) {
                        switch (_context4.prev = _context4.next) {
                          case 0:
                            formdata = new FormData();
                            formdata.append("value", _this4.form.motivo_consulta);
                            formdata.append("episodio_id", _this4.patient_data.episodio_id);
                            formdata.append("user_id", _this4.patient_data.user_id);
                            formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                            formdata.append("_token", $("#_token").val());
                            _context4.next = 8;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_motivo_consulta/store2", formdata);

                          case 8:
                            return _context4.abrupt("return", _context4.sent);

                          case 9:
                          case "end":
                            return _context4.stop();
                        }
                      }
                    }, _callee4);
                  }))();
                },
                storeAntecedentes: function storeAntecedentes() {
                  var _this5 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5() {
                    var formdata;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
                      while (1) {
                        switch (_context5.prev = _context5.next) {
                          case 0:
                            formdata = new FormData();
                            formdata.append("value", _this5.form.antecedentes);
                            formdata.append("episodio_id", _this5.patient_data.episodio_id);
                            formdata.append("user_id", _this5.patient_data.user_id);
                            formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                            formdata.append("_token", $("#_token").val());
                            _context5.next = 8;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_antecedente/store", formdata);

                          case 8:
                            return _context5.abrupt("return", _context5.sent);

                          case 9:
                          case "end":
                            return _context5.stop();
                        }
                      }
                    }, _callee5);
                  }))();
                },
                storeEnfermedadActual: function storeEnfermedadActual() {
                  var _this6 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6() {
                    var formdata;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
                      while (1) {
                        switch (_context6.prev = _context6.next) {
                          case 0:
                            formdata = new FormData();
                            formdata.append("value", _this6.form.enfermedad_actual);
                            formdata.append("episodio_id", _this6.patient_data.episodio_id);
                            formdata.append("user_id", _this6.patient_data.user_id);
                            formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                            formdata.append("_token", $("#_token").val());
                            _context6.next = 8;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_enfermedad_actual/store2", formdata);

                          case 8:
                            return _context6.abrupt("return", _context6.sent);

                          case 9:
                          case "end":
                            return _context6.stop();
                        }
                      }
                    }, _callee6);
                  }))();
                },
                storeExamenFisico: function storeExamenFisico() {
                  var _this7 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee7() {
                    var formdata;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee7$(_context7) {
                      while (1) {
                        switch (_context7.prev = _context7.next) {
                          case 0:
                            formdata = new FormData();
                            formdata.append("value", _this7.form.examen_fisico);
                            formdata.append("episodio_id", _this7.patient_data.episodio_id);
                            formdata.append("user_id", _this7.patient_data.user_id);
                            formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                            formdata.append("_token", $("#_token").val());
                            _context7.next = 8;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_examen_fisico/store2", formdata);

                          case 8:
                            return _context7.abrupt("return", _context7.sent);

                          case 9:
                          case "end":
                            return _context7.stop();
                        }
                      }
                    }, _callee7);
                  }))();
                },
                storeProblemaDeIngreso: function storeProblemaDeIngreso() {
                  var _this8 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee8() {
                    var formdata;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee8$(_context8) {
                      while (1) {
                        switch (_context8.prev = _context8.next) {
                          case 0:
                            formdata = new FormData();
                            formdata.append("value", _this8.form.problema_de_ingreso);
                            formdata.append("episodio_id", _this8.patient_data.episodio_id);
                            formdata.append("user_id", _this8.patient_data.user_id);
                            formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                            formdata.append("_token", $("#_token").val());
                            _context8.next = 8;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_impresion_diagnostica/store", formdata);

                          case 8:
                            return _context8.abrupt("return", _context8.sent);

                          case 9:
                          case "end":
                            return _context8.stop();
                        }
                      }
                    }, _callee8);
                  }))();
                },
                existInformeEmergencia: function existInformeEmergencia() {
                  var _this9 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee9() {
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee9$(_context9) {
                      while (1) {
                        switch (_context9.prev = _context9.next) {
                          case 0:
                            _context9.next = 2;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["get"])("/user/tipo_documento/show/".concat(_this9.patient_data.episodio_id));

                          case 2:
                            return _context9.abrupt("return", _context9.sent);

                          case 3:
                          case "end":
                            return _context9.stop();
                        }
                      }
                    }, _callee9);
                  }))();
                },
                openInformeEmergencia: function openInformeEmergencia() {
                  window.open("/pdf/informeSeguro/".concat(this.patient_data.user_id));
                },
                informePreliminarDeEmergencia: function informePreliminarDeEmergencia() {
                  var _this10 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee10() {
                    var formdata, firma_sello, existeInforme;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee10$(_context10) {
                      while (1) {
                        switch (_context10.prev = _context10.next) {
                          case 0:
                            formdata = new FormData();
                            formdata.append("_token", $("#_token").val());
                            formdata.append("episodio_id", _this10.patient_data.episodio_id);
                            formdata.append("user_id_paciente", _this10.patient_data.user_id);
                            _context10.next = 6;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])("/user/tipo_documento/store", formdata);

                          case 6:
                            firma_sello = _context10.sent;
                            _context10.next = 9;
                            return _this10.existInformeEmergencia();

                          case 9:
                            existeInforme = _context10.sent;

                            if (existeInforme !== undefined) {
                              _this10.informeGenerado = true;
                            }

                          case 11:
                          case "end":
                            return _context10.stop();
                        }
                      }
                    }, _callee10);
                  }))();
                },
                storeSospechasEpisodio: function storeSospechasEpisodio(data) {
                  var _this11 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee11() {
                    var formdata;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee11$(_context11) {
                      while (1) {
                        switch (_context11.prev = _context11.next) {
                          case 0:
                            formdata = new FormData();
                            formdata.append('campo', data.name);
                            formdata.append('valor', data.value);
                            formdata.append('user_cuest_episodio_id', _this11.patient_data.episodio_id);
                            formdata.append("user_id", _this11.patient_data.user_id);
                            formdata.append("_token", $("#_token").val());
                            formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                            _context11.next = 9;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_episodio/update2", formdata);

                          case 9:
                            return _context11.abrupt("return", _context11.sent);

                          case 10:
                          case "end":
                            return _context11.stop();
                        }
                      }
                    }, _callee11);
                  }))();
                },
                storeSignos: function storeSignos(_ref2) {
                  var _this12 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee12() {
                    var route, value, _ref2$value, value2, formdata;

                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee12$(_context12) {
                      while (1) {
                        switch (_context12.prev = _context12.next) {
                          case 0:
                            route = _ref2.route, value = _ref2.value, _ref2$value = _ref2.value2, value2 = _ref2$value === void 0 ? null : _ref2$value;
                            formdata = new FormData();
                            formdata.append("user_id", _this12.patient_data.user_id);
                            formdata.append("user_id_paciente", patient_data.user_id);
                            formdata.append("episodio_id", patient_data.episodio_id);
                            formdata.append("cat_user_cuestionario_84", value);
                            formdata.append("cat_user_cuestionario_73", value);
                            formdata.append("cat_user_cuestionario_185", value);
                            formdata.append("value", value);
                            formdata.append("value2", value2);
                            formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                            formdata.append("sintoma_observacion", "");
                            formdata.append("_token", $("#_token").val());
                            _context12.next = 15;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + route, formdata);

                          case 15:
                            return _context12.abrupt("return", _context12.sent);

                          case 16:
                          case "end":
                            return _context12.stop();
                        }
                      }
                    }, _callee12);
                  }))();
                },
                store: function store() {
                  var _this13 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee13() {
                    var that;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee13$(_context13) {
                      while (1) {
                        switch (_context13.prev = _context13.next) {
                          case 0:
                            that = _this13;
                            that.loading.classList.remove("d-none");

                            if (!(that.form.motivo_consulta !== "")) {
                              _context13.next = 5;
                              break;
                            }

                            _context13.next = 5;
                            return that.storeMotivoConsulta();

                          case 5:
                            if (!(that.form.antecedentes !== "")) {
                              _context13.next = 8;
                              break;
                            }

                            _context13.next = 8;
                            return that.storeAntecedentes();

                          case 8:
                            if (!(that.form.enfermedad_actual !== "")) {
                              _context13.next = 11;
                              break;
                            }

                            _context13.next = 11;
                            return that.storeEnfermedadActual();

                          case 11:
                            if (!(that.form.examen_fisico !== "")) {
                              _context13.next = 14;
                              break;
                            }

                            _context13.next = 14;
                            return that.storeExamenFisico();

                          case 14:
                            if (!(that.form.problema_de_ingreso !== "")) {
                              _context13.next = 17;
                              break;
                            }

                            _context13.next = 17;
                            return that.storeProblemaDeIngreso();

                          case 17:
                            if (!(that.form.peso !== "")) {
                              _context13.next = 20;
                              break;
                            }

                            _context13.next = 20;
                            return _this13.storeSignos({
                              route: "/user_peso/store2",
                              value: _this13.form.peso
                            });

                          case 20:
                            if (!(that.form.talla !== "")) {
                              _context13.next = 23;
                              break;
                            }

                            _context13.next = 23;
                            return _this13.storeSignos({
                              route: "/user_talla/store",
                              value: _this13.form.talla
                            });

                          case 23:
                            if (!(that.form.temp !== "")) {
                              _context13.next = 26;
                              break;
                            }

                            _context13.next = 26;
                            return _this13.storeSignos({
                              route: "/user_cuest_temperatura/store",
                              value: _this13.form.temp
                            });

                          case 26:
                            if (!(that.form.oxi !== "")) {
                              _context13.next = 29;
                              break;
                            }

                            _context13.next = 29;
                            return _this13.storeSignos({
                              route: "/user_cuest_oximetria/store",
                              value: _this13.form.oxi
                            });

                          case 29:
                            if (!(that.form.fc !== "")) {
                              _context13.next = 32;
                              break;
                            }

                            _context13.next = 32;
                            return _this13.storeSignos({
                              route: "/user_cuest_fc/store",
                              value: _this13.form.fc
                            });

                          case 32:
                            if (!(that.form.fr !== "")) {
                              _context13.next = 35;
                              break;
                            }

                            _context13.next = 35;
                            return _this13.storeSignos({
                              route: "/user_cuest_fr/store",
                              value: _this13.form.fr
                            });

                          case 35:
                            if (!(that.form.gl !== "")) {
                              _context13.next = 38;
                              break;
                            }

                            _context13.next = 38;
                            return _this13.storeSignos({
                              route: "/user_cuest_gl/store",
                              value: _this13.form.gl
                            });

                          case 38:
                            if (!(that.form.ta_sis !== "" && that.form.ta_dia !== "")) {
                              _context13.next = 41;
                              break;
                            }

                            _context13.next = 41;
                            return _this13.storeSignos({
                              route: "/user_cuest_ta/store",
                              value: _this13.form.ta_sis,
                              value2: _this13.form.ta_dia
                            });

                          case 41:
                            _this13.storeSospechasEpisodio({
                              "name": "requiere_intersonsulta",
                              "value": _this13.form.requiere_intersonsulta
                            });

                            _this13.storeSospechasEpisodio({
                              "name": "realizar_procedimiento",
                              "value": _this13.form.realizar_procedimiento
                            });

                            _this13.storeSospechasEpisodio({
                              "name": "cantidad_especialistas",
                              "value": _this13.form.cantidad_especialistas
                            });

                            _this13.storeSospechasEpisodio({
                              "name": "procedimiento_complejidad",
                              "value": _this13.form.procedimiento_complejidad
                            });

                            _this13.storeSospechasEpisodio({
                              "name": "hospitalizacion",
                              "value": _this13.form.requiere_hospitalizacion
                            });

                            _this13.storeSospechasEpisodio({
                              "name": "terapia_intensiva",
                              "value": _this13.form.requiere_uti
                            });

                            _this13.storeSospechasEpisodio({
                              "name": "cirugia",
                              "value": _this13.form.requiere_cirugia
                            });

                            if (that.form.nivel_triaje !== "null") {
                              _this13.storeSospechasEpisodio({
                                "name": "nivel_triaje",
                                "value": _this13.form.nivel_triaje
                              });
                            }

                            that.informePreliminarDeEmergencia();
                            that.loading.classList.add("d-none");
                            return _context13.abrupt("return", true);

                          case 52:
                          case "end":
                            return _context13.stop();
                        }
                      }
                    }, _callee13);
                  }))();
                },
                handle_store: function handle_store() {
                  var _this14 = this;

                  return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee16() {
                    var that;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee16$(_context16) {
                      while (1) {
                        switch (_context16.prev = _context16.next) {
                          case 0:
                            //that.informeGenerado = true
                            that = _this14;
                            Swal.fire({
                              icon: "info",
                              title: "¿Quieres guardar la Historia Inicial?",
                              text: "Esta acción habilitará el informe preliminar de emergencia.",
                              showCancelButton: true,
                              confirmButtonText: "Si, guardar",
                              cancelButtonText: "No, a\xFAn no."
                            }).then( /*#__PURE__*/function () {
                              var _ref3 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee15(result) {
                                return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee15$(_context15) {
                                  while (1) {
                                    switch (_context15.prev = _context15.next) {
                                      case 0:
                                        if (!result.isConfirmed) {
                                          _context15.next = 4;
                                          break;
                                        }

                                        _context15.next = 3;
                                        return that.store();

                                      case 3:
                                        Swal.fire({
                                          icon: "success",
                                          title: "Historia Inicial Guardada",
                                          text: "Pulse en continuar para cerrar esta ventana, o pulse en Informe Preliminar de Emergencia para abrirlo.",
                                          showDenyButton: true,
                                          //showCancelButton: true,
                                          confirmButtonText: "Continuar",
                                          denyButtonText: "Informe Preliminar de Emergencia" //cancelButtonText: `Abrir Informe Preliminar de Emergencia`,

                                        }).then( /*#__PURE__*/function () {
                                          var _ref4 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee14(result) {
                                            return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee14$(_context14) {
                                              while (1) {
                                                switch (_context14.prev = _context14.next) {
                                                  case 0:
                                                    if (result.isConfirmed) {
                                                      $("#modal-patient-item").modal("hide");
                                                    }

                                                    if (result.isDenied) {
                                                      that.openInformeEmergencia();
                                                    }

                                                  case 2:
                                                  case "end":
                                                    return _context14.stop();
                                                }
                                              }
                                            }, _callee14);
                                          }));

                                          return function (_x4) {
                                            return _ref4.apply(this, arguments);
                                          };
                                        }());

                                      case 4:
                                      case "end":
                                        return _context15.stop();
                                    }
                                  }
                                }, _callee15);
                              }));

                              return function (_x3) {
                                return _ref3.apply(this, arguments);
                              };
                            }());
                            /*
                            $("#modal-patient-item").modal("hide"); */

                          case 2:
                          case "end":
                            return _context16.stop();
                        }
                      }
                    }, _callee16);
                  }))();
                }
              },
              mounted: function mounted() {
                var _this15 = this;

                return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee17() {
                  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee17$(_context17) {
                    while (1) {
                      switch (_context17.prev = _context17.next) {
                        case 0:
                          _context17.next = 2;
                          return _this15.get_historiaIngreso();

                        case 2:
                          userCuestPaciente.infoPaciente("#infoPaciente", _this15.patient_data.user_id);

                        case 3:
                        case "end":
                          return _context17.stop();
                      }
                    }
                  }, _callee17);
                }))();
              }
            });

          case 3:
          case "end":
            return _context18.stop();
        }
      }
    }, _callee18);
  }));

  return function create_historia_inicial(_x, _x2) {
    return _ref.apply(this, arguments);
  };
}();

var AppListItemsHistoria = function AppListItemsHistoria() {
  return {
    props: {
      currentItem: String,
      patient_data: Object,
      dataEpisodio: Array,
      index: Number
    },
    template:
    /*html */
    "\n            <div class=\"container-fluid\">\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 p-1\">\n                        <div\n                            v-if=\"dataEpisodio.length > 0\"\n                            class=\"timeline timeline-inverse\"\n                        >\n                            <!-- FECHA DEL DIA -->\n                            <div v-for=\"(item,index) in dataEpisodio\" :key=\"index\">\n\n                                <div v-if=\"item.items.length !== item.items.filter(x=>!x.active).length\" class=\"time-label\">\n                                    <span class=\"bg-primary\">\n                                        {{ item.fechaFormated }}\n                                    </span>\n                                </div>\n\n                                <div\n                                    v-for=\"(item2,index2) in item.items\"\n                                    :key=\"index2\"\n                                    v-show=\"item2.active\"\n                                >\n\n                                    <i\n                                        :content=\"'Equipo M\xE9dico'\"\n\n                                        data-tippy-content=\"Equipo M\xE9dico\"\n                                        class=\"timelime-medic-icon fas fa-user-md\"\n                                        aria-hidden=\"true\">\n                                    </i>\n                                    <div class=\"timeline-item\">\n                                        <span class=\"time\">\n                                            <i class=\"far fa-clock\"></i>\n                                            {{ item2.fechaItem }}\n                                            <span class=\"text-primary\">|</span>\n                                            {{ item2.hora }}\n                                            <a\n                                                @click=\"imprimir(item2)\"\n                                                class=\"m-0 p-0 imprimir\"\n                                                type=\"button\"\n\n                                                :content=\"'Imprimir'\"\n\n                                            >\n                                                <i\n\n                                                    class=\"imprimir m-0 p-0 fas fa-print text-info heartbeat\"\n                                                    style=\"font-size: 1em;cursor:pointer; margin-left:1em;\"\n                                                    aria-hidden=\"true\">\n                                                </i>\n                                            </a>\n                                        </span>\n                                        <h3\n                                            data-toggle=\"collapse\"\n                                            :href=\"'#collapseExample'+index2\"\n                                            role=\"button\"\n                                            aria-expanded=\"false\"\n                                            aria-controls=\"collapseExample\"\n                                            class=\"timeline-header\"\n                                        >\n                                            <b>{{ item2.description }}</b> |\n                                            <b class=\"text-primary\">{{ item2.dataMedico.prefijo }} {{ item2.dataMedico.medico }}</b>\n                                            <span\n                                                :content=\"'Equipo '+item2.dataMedico_posicion.nombre\"\n\n                                                :class=\"['badge  scale-in-hor-left',('badge-'+item2.dataMedico_posicion.color)]\"\n                                                style=\"width:2em;\">\n                                                {{ item2.dataMedico_posicion.siglas }}\n                                            </span>\n\n\n                                        </h3>\n                                        <div\n                                            :id=\"'collapseExample'+index2\"\n                                            class=\"collapse show timeline-body\"\n                                            v-html=\"item2.dato\"\n                                        >\n                                        </div>\n\n                                    </div>\n                                </div>\n                            </div>\n                            <div>\n                                <i class=\"far fa-clock bg-secondary rounded-circle text-white\"></i>\n                            </div>\n                        </div>\n                        <div v-else class=\"timeline timeline-inverse\">\n                            <div>\n                                <div>\n                                    <div class=\"timeline-item\">\n                                        <div class=\"timeline-body text-secondary text-center\">\n                                            No posee historia este d\xEDa\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                            <div>\n                                <i class=\"far fa-clock bg-gray\"></i>\n                            </div>\n                        </div>\n                    </div>\n                </div>\n            </div>\n\n        ",
    data: function data() {
      return {};
    },
    computed: {},
    methods: {
      imprimir: function imprimir(item) {
        //imprimir(name,id){
        var name = item.description,
            id = item.id;
        var description = "";
        var type = "one";

        switch (name) {
          case "Orden Médica":
            description = "orden_medica";
            break;

          case "Evolución":
            description = "evolucion";
            break;

          case "Pendiente":
            description = "pendiente";
            break;

          case "Probabilidad diagnóstica":
            description = "probabilidad_diagnostica";
            break;

          case "Plan":
            description = "plan";
            break;

          case "Observación":
            description = "observacion";
            break;

          case "Récipe":
            description = "recipe";
            break;

          case "Estudio Diagnóstico":
            description = "estudio_diagnostico";
            break;

          case "Laboratorio":
            description = "laboratorio";
            break;

          case "Imagenes":
            description = "imagenes";
            break;

          case "Otro":
            description = "otro";
            break;

          default:
            alert("Error. Reporte no encotrado");
            return false;
            break;
        }

        if (['Orden Médica'].includes(name)) {
          window.open(location.origin + "/user/informe/omed/orden_medica/".concat(this.patient_data.episodio_id, "/").concat(id));
          return false;
        }

        if (['Evolución'].includes(name)) {
          window.open(location.origin + "/user/informe/nde/evolucion_medica/".concat(this.patient_data.episodio_id, "/").concat(id));
          return false;
        }

        if (['Récipe'].includes(name)) {
          if (this.esArrayDeObjetos(item.dato)) {
            this.printPdf(item);
          }
          /* else{
             window.open(location.origin+`/user/informe/recmed/recipe_medico/${this.patient_data.episodio_id}/${id}`)
          } */


          return false;
        }

        if (type === "one") {
          window.open("/user/reporte/".concat(description, "/").concat(this.patient_data.episodio_id, "/").concat(id, "/").concat($(this).data('impresion')));
        } else {
          window.open("/user/reporte/".concat(description, "/").concat($(this).data('episodio_id'), "/all/").concat($(this).data('impresion')));
        }
      }
    },
    mounted: function mounted() {
      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee19() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee19$(_context19) {
          while (1) {
            switch (_context19.prev = _context19.next) {
              case 0:
              case "end":
                return _context19.stop();
            }
          }
        }, _callee19);
      }))();
    }
  };
};

var AppCreateItemFile = function AppCreateItemFile() {
  return {
    name: "AppCreateLaboratorio",
    props: {
      updateComponentToReturn: Function,
      componentToReturn: String,
      returnTo: String,
      currentItem: String,
      patient_data: Object,
      dataEpisodio: Array,
      index: Number
    },
    template:
    /*html */
    "\n            <div class=\"flex-fill p-1 d-flex flex-column overflow-auto\" >\n\n                <div class=\"flex-fill overflow-auto mb-1\">\n                    <textarea\n                        class=\"form-control bg-gray-footer-parodi \"\n                        :placeholder=\"'Escriba un comentario'\"\n                        name=\"value\"\n                        ref=\"value\"\n                        style=\" width: 100%;height: 100%;resize: none;\"\n                        v-model=\"form.value\"\n                    ></textarea>\n                </div>\n                <div v-if=\"fileActive()\" class=\"input-group my-1\">\n                    <div class=\"input-group-prepend\">\n                        <span class=\"input-group-text\" id=\"inputGroupFileAddon01\">\n                            <img style=\"width: 2em;height: 2em;\" :src=\"[imagenes[ currentItem ]]\">\n                        </span>\n                    </div>\n                    <div class=\"custom-file\">\n                        <input\n                            type=\"file\"\n                            class=\"custom-file-input\"\n                            id=\"file\"\n                            aria-describedby=\"file\"\n                            @change=\"handle_upload_files()\"\n\n                        >\n                        <label\n\n                            class=\"custom-file-label\"\n                            style=\"height: calc(2.7rem + 3px);\"\n                            for=\"inputGroupFile01\"\n                        >\n                            {{this.form.files !== null ? this.form.files.name : 'Pulsa aqu\xED para subir archivos'}}\n                        </label>\n                    </div>\n                </div>\n                <div v-if=\"this.form.files !== null\" class=\"w-100\" id=\"file_temp\">\n                    <div class=\"col-xs-2 col-sm-2 col-md-2 col-lg-2 col-xl-2 text-center\">\n                        <img\n                            class=\"img-fluid\"\n                            style=\"height: 4em;width: 4em;margin: 0.5em;\"\n                            src=\"/image/system/icono_fotografia.svg\"\n                        >\n                    </div>\n                </div>\n                <div class=\"rounded d-flex mt-1\">\n                    <button @click=\"submit()\" class=\"flex-fill btn btn-success mr-1\">\n                        Agregar\n                    </button>\n                    <button @click=\"regresar()\" class=\"flex-fill btn btn-primary\"   >\n                        Regresar\n                    </button>\n\n                </div>\n\n            </div>\n        ",
    data: function data() {
      return {
        loading: document.getElementById('loadingScreen'),
        prefixRoute: {
          "Laboratorio": "user_cuest_laboratorio",
          "Imagenes": "user_cuest_imagen",
          "Fotografías": "user_cuest_fotografia",
          "Otros Archivos": "user_cuest_otro_archivo",
          "Probabilidad diagnóstica": "user_cuest_impresion_diagnostica",
          "Evolución": "user_cuest_evolucion",
          "Orden Médica": "user_cuest_orden_medica",
          "Plan": "user_cuest_plan",
          "Récipe": "user_cuest_recipe",
          "Observación": "user_cuest_observacion",
          "Estudio Diagnóstico": "user_cuest_estudio",
          "Antecedentes/Comorbilidad de Egreso": "user_cuest_comorbilidad"
        },
        imagenes: {
          "Laboratorio": "/image/system/carga1-success.svg",
          "Imagenes": "/image/system/carga2-success.svg",
          "Fotografías": "/image/system/carga3-success.svg",
          "Otros Archivos": "/image/system/carga5-success.svg"
        },
        form: {
          value: "",
          files: null
        }
      };
    },
    methods: {
      fileActive: function fileActive() {
        var componentWithFiles = ["Laboratorio", "Imagenes", "Fotografías", "Otros Archivos"];
        return componentWithFiles.includes(this.currentItem);
      },
      handle_upload_files: function handle_upload_files() {
        this.form.files = document.getElementById("file").files[0];
      },
      regresar: function regresar() {
        $("#modal-patient-item").modal("hide");
      },
      submit: function submit() {
        var _this16 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee20() {
          var that, formdata, result, temp;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee20$(_context20) {
            while (1) {
              switch (_context20.prev = _context20.next) {
                case 0:
                  that = _this16;

                  if (!(_this16.form.value === "")) {
                    _context20.next = 4;
                    break;
                  }

                  Swal.fire({
                    icon: "success",
                    title: "Campo obligatorio",
                    text: "Un comentario es requerido.",
                    didClose: function didClose() {
                      setTimeout(function () {
                        return _this16.$refs.value.focus();
                      }, 110);
                    }
                  });
                  return _context20.abrupt("return", false);

                case 4:
                  _this16.loading.classList.remove("d-none");

                  formdata = new FormData();
                  formdata.append("description", _this16.form.value);
                  formdata.append("value", _this16.form.value);
                  formdata.append("coments", _this16.form.value);
                  formdata.append("user_id", _this16.patient_data.user_id);
                  formdata.append("episodio_id", _this16.patient_data.episodio_id);
                  formdata.append("file", _this16.form.files);
                  formdata.append("_token", $("#_token").val());
                  formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                  _context20.next = 16;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/" + _this16.prefixRoute[_this16.currentItem] + "/store", formdata);

                case 16:
                  result = _context20.sent;
                  temp = _this16.dataEpisodio;
                  that.currentComponent = that.updateComponentToReturn;
                  that.updateComponentToReturn();

                  _this16.loading.classList.add("d-none");

                  userCuestPaciente.infoPaciente("#infoPaciente", _this16.patient_data.user_id);
                  /* Swal.fire({
                      icon: "warning",
                      title: "Datos guardados",
                      text: "El registro se ha guardado exitosamente.",
                      didClose: () => {
                          that.currentComponent = that.updateComponentToReturn
                          that.updateComponentToReturn()
                      }
                  }) */

                case 22:
                case "end":
                  return _context20.stop();
              }
            }
          }, _callee20);
        }))();
      }
    }
  };
};

var AppCreateRecipe = function AppCreateRecipe() {
  return {
    name: "AppCreateRecipe",
    props: {
      updateComponentToReturn: Function,
      componentToReturn: String,
      returnTo: String,
      currentItem: String,
      patient_data: Object,
      dataEpisodio: Array,
      index: Number
    },
    template:
    /*html */
    "\n            <div class=\"flex-fill p-1 d-flex flex-column overflow-auto\" >\n            <div class=\"flex-fill d-flex flex-column overflow-auto\">\n     \n                <div v-for=\"(item,index) in medications\" :key=\"index\" class=\"d-flex mb-3\">\n                    <div v-if=\"medications.length > 1\">{{index+1}}</div>\n                    <div class=\"d-flex flex-wrap flex-fill\">\n                        <div class=\"col-6 p-1\">\n                        <input\n                            :ref=\"'name'+index\"\n                            v-model=\"item.name\"\n                            type=\"text\"\n                            class=\"form-control bg-gray-footer-parodi\"\n                            placeholder=\"Medicamento\"\n                        >\n                        </div>\n                        <div class=\"col-3 p-1\">\n                        <input\n                            :ref=\"'presentation'+index\"\n                            v-model=\"item.presentation\"\n                            type=\"text\"\n                            class=\"form-control bg-gray-footer-parodi\"\n                            placeholder=\"Presentaci\xF3n\"\n                        >\n                        </div>\n                        <div class=\"col-3 p-1\">\n\n\n                        <input v-model=\"item.units\" type=\"text\" list=\"Nums_controls\" placeholder=\"Unidades\" class=\"form-control bg-gray-footer-parodi\" >\n                        <datalist id=\"Nums_controls\">\n                            <option value=\"cc\">\n                            <option value=\"mg\">\n                            <option value=\"gr\">\n                            <option value=\"UI\">\n                            <option value=\"gotas\">\n                        </datalist>\n\n\n                        </div>\n                        <div class=\"col-12 p-1\">\n                        <textarea :ref=\"'indications'+index\" v-model=\"item.indications\" placeholder=\"Indicaciones\" style=\"resize: none; overflow: hidden;\" class=\"form-control bg-gray-footer-parodi auto-resize-textarea\" rows=\"1\"></textarea>\n                        </div>\n                    </div>\n                    <div v-if=\"medications.length > 1\">\n                        <button @click=\"destroyItem(index)\" class=\"btn btn-default\">\n                            <i class=\"fa fa-times text-primary\"></i>\n                        </button>\n                    </div>\n\n                    </div>\n                    <button\n                    @click=\"addItem\"\n\n                    class=\"btn btn-default border w-100 mb-0\"\n                    >\n                    + Agregar Medicamento\n                    </button>\n                </div>\n               \n                \n                \n                <div class=\"rounded d-flex mt-1\">\n                    <button @click=\"submit()\" class=\"flex-fill btn btn-success mr-1\">\n                        Agregar\n                    </button>\n                    <button @click=\"regresar()\" class=\"flex-fill btn btn-primary\"   >\n                        Regresar\n                    </button>\n\n                </div>\n\n            </div>\n        ",
    data: function data() {
      return {
        loading: document.getElementById('loadingScreen'),
        medication: {
          //formulario de medicacion
          name: "",
          presentation: "",
          units: "",
          indications: ""
        },
        medications: [],
        //medicaciones
        episode_id: null,
        //id del episodio
        created_at: null,
        //fecha de creacion
        creator_id: null //id del creador

      };
    },
    methods: {
      addItem: function addItem(p_index) {
        this.medications.push(JSON.parse(JSON.stringify(this.medication)));
      },
      destroyItem: function destroyItem(p_index) {
        this.medications = this.medications.filter(function (item, index) {
          return index !== p_index;
        });
      },
      regresar: function regresar() {
        $("#modal-patient-item").modal("hide");
      },
      validations: function validations() {
        var _this17 = this;

        var result = true;
        var input = null;
        this.medications.forEach(function (x, index) {
          if (x.name === "" && result) {
            Swal.fire({
              icon: "warning",
              title: "Atención",
              text: "El campo medicamento no puede estar vac\xEDo",
              didClose: function didClose() {
                _this17.$refs['name' + index][0].focus();
              }
            });
            result = false;
          }

          if (x.presentation === "" && result) {
            Swal.fire({
              icon: "warning",
              title: "Atención",
              text: "El campo presentaci\xF3n no puede estar vac\xEDo",
              didClose: function didClose() {
                _this17.$refs['presentation' + index][0].focus();
              }
            });
            result = false;
          }

          if (x.indications === "" && result) {
            Swal.fire({
              icon: "warning",
              title: "Atención",
              text: "El campo indicaciones no puede estar vac\xEDo",
              didClose: function didClose() {
                _this17.$refs['indications' + index][0].focus();
              }
            });
            result = false;
          }
        });
        return result;
      },
      submit: function submit() {
        var _this18 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee21() {
          var that, formdata, result, temp;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee21$(_context21) {
            while (1) {
              switch (_context21.prev = _context21.next) {
                case 0:
                  if (!_this18.validations()) {
                    _context21.next = 20;
                    break;
                  }

                  that = _this18;

                  _this18.loading.classList.remove("d-none");

                  formdata = new FormData();
                  formdata.append("description", null);
                  formdata.append("value", JSON.stringify(_this18.medications));
                  formdata.append("coments", null);
                  formdata.append("user_id", _this18.patient_data.user_id);
                  formdata.append("episodio_id", _this18.patient_data.episodio_id);
                  formdata.append("file", null);
                  formdata.append("_token", $("#_token").val());
                  formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                  _context21.next = 14;
                  return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_recipe/store", formdata);

                case 14:
                  result = _context21.sent;
                  temp = _this18.dataEpisodio;
                  that.currentComponent = that.updateComponentToReturn;
                  that.updateComponentToReturn();

                  _this18.loading.classList.add("d-none");

                  userCuestPaciente.infoPaciente("#infoPaciente", _this18.patient_data.user_id);

                case 20:
                case "end":
                  return _context21.stop();
              }
            }
          }, _callee21);
        }))();
      }
    },
    created: function created() {
      this.medications.push(JSON.parse(JSON.stringify(this.medication)));
    }
  };
};

var AppRowTimeline = function AppRowTimeline() {
  return {
    name: "AppRowTimeline",
    props: {
      currentItem: String,
      patient_data: Object,
      item: Object,
      index: Number
    },
    template:
    /*HTML */
    "\n            <div class=\"fade-in\">\n                <i data-tippy-content=\"Equipo M\xE9dico\" class=\"tooltip-primary fas fa-user-md bg-primary text-white\" aria-hidden=\"true\"></i>\n                <div class=\"timeline-item\">\n                    <span class=\"time\">\n\n                        <i class=\"far fa-clock\"></i>\n                        18 Junio, 2024\n                        <span class=\"text-primary\">|</span>\n                        8:03:53 PM\n                        <a class=\"m-0 p-0 imprimir\" type=\"button\" id=\"triggerIdObservaci\xF3n5737\" data-type=\"one\" data-name=\"Observaci\xF3n\" data-id=\"5737\" data-episodio_id=\"31172\" data-impresion=\"color\">\n                            <i data-tippy-content=\"Imprimir\" class=\"tooltip-info imprimir m-0 p-0 fas fa-print text-info heartbeat\" style=\"font-size: 1em;cursor:pointer; margin-left:1em;\" data-type=\"one\" data-name=\"Observaci\xF3n\" data-id=\"5737\" data-episodio_id=\"31172\" data-impresion=\"color\" aria-hidden=\"true\"></i>\n                        </a>\n                    </span>\n\n\n                    <h3 class=\"timeline-header\">\n                        <b>{{item.description}}</b> | <a href=\"#\">{{item.medico}}</a> <span data-tippy-content=\"Equipo Tratante\" class=\"badge tooltip-success bg-success scale-in-hor-left\" style=\"width:2em;\">TR</span></h3>\n\n                    <div class=\"timeline-body\" style=\"font-size: 0.5em;    white-space: pre-line;\">\n\n                        {{item}}\n\n                    </div>\n                    <div class=\"timeline-footer\">\n\n                    </div>\n                </div>\n            </div>\n        "
  };
};

var AppFilter = function AppFilter() {
  return {
    name: "AppFilter",
    props: {
      currentItem: String,
      patient_data: Object,
      dataEpisodio: Array,
      index: Number
    },
    data: function data() {
      return {
        filterActive: false,
        tipo_filtro: "",
        filtro_valor: "",
        medicosOptions: []
      };
    },
    template:
    /*html */
    "\n            <li class=\"nav-item dropdown\" role=\"presentation\">\n                <a\n                    :class=\"[{'nav-link':!filterActive},{'btn btn-info':filterActive}]\"\n                    data-toggle=\"dropdown\"\n                    href=\"#\"\n                    role=\"button\"\n                    aria-haspopup=\"true\"\n                    aria-expanded=\"false\"\n                >\n                    <h3 class=\"mb-0\">\n                    {{currentItem ===\"Ver Todo\" ? \"\":currentItem}}\n                        <i style=\"font-size:1.2rem\" class=\"fas fa-filter\"></i>\n                    </h3>\n                </a>\n                <div  class=\"dropdown-menu p-1\">\n\n                    <h6 class=\"dropdown-header text-center text-primary\">Ver por equipo de salud</h6>\n\n                    <div class=\"container-fluid overflow-auto\" >\n                        <ul style=\"max-height: 10rem;\" class=\"list-group list-group-flush overflow-auto\">\n                            <li\n                                @click=\"handleFilter({tipo_filtro:'medico',filtro_valor:item.medico})\"\n                                class=\"cursor-pointer list-group-item text-nowrap py-1 d-flex align-items-center justify-content-between list-group-item-action\"\n                                v-for=\"(item,index) in getMedicosOptions.orderByTeem['Tratante']\"\n                                :key=\"'filter_' + item.posicion.siglas + index\"\n                                data-value=\"item\"\n                            >\n                                <div class=\"d-flex align-items-center\">\n                                    <span :class=\"['mr-1 badge','badge-'+item.posicion.color]\">{{item.posicion.siglas}}</span>\n                                    <div class=\"ml-1\">\n                                        {{item.medico}}\n                                    </div>\n                                </div>\n                                <div>\n                                    <span class=\"mr-1 badge badge-counter\">{{counterRow(item.medico)}}</span>\n                                </div>\n                            </li>\n                            <li\n                                @click=\"handleFilter({tipo_filtro:'medico',filtro_valor:item.medico})\"\n                                class=\"cursor-pointer list-group-item text-nowrap py-1 d-flex align-items-center justify-content-between list-group-item-action\"\n                                v-for=\"(item,index) in getMedicosOptions.orderByTeem['Interconsultante']\"\n                                :key=\"'filter_' + item.posicion.siglas + index\"\n                                data-value=\"item\"\n                            >\n                                <div class=\"d-flex align-items-center\">\n                                    <span :class=\"['mr-1 badge','badge-'+item.posicion.color]\">{{item.posicion.siglas}}</span>\n                                    <div class=\"ml-1\">\n                                        {{item.medico}}\n                                    </div>\n                                </div>\n\n                                <div>\n                                    <span class=\"ml-1 badge badge-counter\">{{counterRow(item.medico)}}</span>\n                                </div>\n                            </li>\n                            <li\n                                @click=\"handleFilter({tipo_filtro:'medico',filtro_valor:item.medico})\"\n                                class=\"cursor-pointer list-group-item text-nowrap py-1 d-flex align-items-center justify-content-between list-group-item-action\"\n                                v-for=\"(item,index) in getMedicosOptions.orderByTeem['Fellows']\"\n                                :key=\"'filter_' + item.posicion.siglas + index\"\n                                data-value=\"item\"\n                            >\n                                <div class=\"d-flex align-items-center\">\n                                    <span :class=\"['mr-1 badge','badge-'+item.posicion.color]\">{{item.posicion.siglas}}</span>\n                                    <div class=\"ml-1\">\n                                        {{item.medico}}\n                                    </div>\n                                </div>\n\n                                <div>\n                                    <span class=\"ml-1 badge badge-counter\">{{counterRow(item.medico)}}</span>\n                                </div>\n                            </li>\n                            <li\n                                @click=\"handleFilter({tipo_filtro:'medico',filtro_valor:item.medico})\"\n                                class=\"cursor-pointer list-group-item text-nowrap py-1 d-flex align-items-center justify-content-between list-group-item-action\"\n                                v-for=\"(item,index) in getMedicosOptions.orderByTeem['Residentes']\"\n                                :key=\"'filter_' + item.posicion.siglas + index\"\n                                data-value=\"item\"\n                            >\n                                <div class=\"d-flex align-items-center\">\n                                    <span :class=\"['mr-1 badge','badge-'+item.posicion.color]\">{{item.posicion.siglas}}</span>\n                                    <div class=\"ml-1\">\n                                        {{item.medico}}\n                                    </div>\n                                </div>\n\n                                <div>\n                                    <span class=\"ml-1 badge badge-counter\">{{counterRow(item.medico)}}</span>\n                                </div>\n                            </li>\n                            <li\n                                @click=\"handleFilter({tipo_filtro:'medico',filtro_valor:item.medico})\"\n                                class=\"cursor-pointer list-group-item text-nowrap py-1 d-flex align-items-center justify-content-between list-group-item-action\"\n                                v-for=\"(item,index) in getMedicosOptions.orderByTeem['RAMH']\"\n                                :key=\"'filter_' + item.posicion.siglas + index\"\n                                data-value=\"item\"\n                            >\n                                <div class=\"d-flex align-items-center\">\n                                    <span :class=\"['mr-1 badge','badge-'+item.posicion.color]\">{{item.posicion.siglas}}</span>\n                                    <div class=\"ml-1\">\n                                        {{item.medico}}\n                                    </div>\n                                </div>\n\n                                <div>\n                                    <span class=\"ml-1 badge badge-counter\">{{counterRow(item.medico)}}</span>\n                                </div>\n                            </li>\n                            <li\n                                @click=\"handleFilter({tipo_filtro:'medico',filtro_valor:item.medico})\"\n                                class=\"cursor-pointer list-group-item text-nowrap py-1 d-flex align-items-center justify-content-between list-group-item-action\"\n                                v-for=\"(item,index) in getMedicosOptions.orderByTeem['Enfermer\xEDa']\"\n                                :key=\"'filter_' + item.posicion.siglas + index\"\n                                data-value=\"item\"\n                            >\n                                <div class=\"d-flex align-items-center\">\n                                    <span :class=\"['mr-1 badge','badge-'+item.posicion.color]\">{{item.posicion.siglas}}</span>\n                                    <div class=\"ml-1\">\n                                        {{item.medico}}\n                                    </div>\n                                </div>\n                                <div>\n                                    <span class=\"ml-1 badge badge-counter\">{{counterRow(item.medico)}}</span>\n                                </div>\n                            </li>\n\n\n                        </ul>\n                    </div>\n\n                    <h6 class=\"dropdown-header text-center text-primary\">Ver por D\xEDa</h6>\n                    <div class=\"container-fluid overflow-auto\" >\n                        <ul style=\"max-height: 10rem;\" class=\"list-group list-group-flush\">\n                            <li\n                                @click=\"handleFilter({tipo_filtro:'fecha',filtro_valor:item})\"\n                                class=\"cursor-pointer list-group-item py-1 d-flex align-items-center justify-content-between list-group-item-action\"\n                                v-for=\"(item,index) in getFechasOptions\"\n                                :key=\"'filter_fec_'+index\"\n                                data-value=\"item\"\n                            >\n\n                                <div>\n                                    {{item}}\n                                </div>\n\n                                <div>\n                                    <span class=\"ml-1 badge badge-counter\">{{counterRow2(item)}}</span>\n                                </div>\n                            </li>\n\n                        </ul>\n                    </div>\n\n                </div>\n            </li>\n        ",

    /** */
    computed: {
      getFechasOptions: function getFechasOptions() {
        //console.log(this.dataEpisodio);
        return new Set(this.dataEpisodio.map(function (x) {
          /*  let fecha = (x.created_at.split(" "))[0]
               fecha = (fecha.split("-"))[2]+"-"+(fecha.split("-"))[1]+"-"+(fecha.split("-"))[0] */
          return x.fechaFormated;
        }));
      },
      getMedicosOptions: function getMedicosOptions() {
        //console.log("1---->",this.dataEpisodio[0].items);
        var that = this;
        var equipoMedico = new Set(this.dataEpisodio.map(function (x) {
          return x.items;
        }).flat().map(function (x) {
          return x.medico;
        }));
        var tempEquipoMedico = {
          orderByTeem: {
            'Tratante': [],
            'Interconsultante': [],
            'Fellows': [],
            'Residentes': [],
            'RAMH': [],
            'Enfermería': []
          },
          orderByName: []
        };
        equipoMedico.forEach(function (x) {
          var temp = that.dataEpisodio.map(function (z) {
            return z.items;
          }).flat().find(function (z) {
            return z.medico === x;
          });

          if (['Residentes'].includes(temp.dataMedico_posicion.nombre)) {
            tempEquipoMedico.orderByTeem['Residentes'].push({
              'medico': temp.medico,
              'posicion': temp.dataMedico_posicion
            });
          }

          if (['Fellow'].includes(temp.dataMedico_posicion.nombre)) {
            tempEquipoMedico.orderByTeem['Fellows'].push({
              'medico': temp.medico,
              'posicion': temp.dataMedico_posicion
            });
          }

          if (['RAMH', "Tratante", "Enfermería", "Interconsultante"].includes(temp.dataMedico_posicion.nombre)) {
            tempEquipoMedico.orderByTeem[temp.dataMedico_posicion.nombre].push({
              'medico': temp.medico,
              'posicion': temp.dataMedico_posicion
            });
          }

          tempEquipoMedico.orderByName.push({
            'medico': temp.medico,
            'posicion': temp.dataMedico_posicion
          });
        });
        tempEquipoMedico.orderByTeem['Tratante'] = tempEquipoMedico.orderByTeem['Tratante'].sort(function (a, b) {
          return a.medico.length - b.medico.length;
        });
        tempEquipoMedico.orderByTeem['Interconsultante'] = tempEquipoMedico.orderByTeem['Interconsultante'].sort(function (a, b) {
          return a.medico.length - b.medico.length;
        });
        tempEquipoMedico.orderByTeem['Residentes'] = tempEquipoMedico.orderByTeem['Residentes'].sort(function (a, b) {
          return a.medico.length - b.medico.length;
        });
        tempEquipoMedico.orderByTeem['Fellows'] = tempEquipoMedico.orderByTeem['Fellows'].sort(function (a, b) {
          return a.medico.length - b.medico.length;
        });
        tempEquipoMedico.orderByTeem['Residentes'] = tempEquipoMedico.orderByTeem['Residentes'].sort(function (a, b) {
          return a.medico.length - b.medico.length;
        });
        tempEquipoMedico.orderByTeem['RAMH'] = tempEquipoMedico.orderByTeem['RAMH'].sort(function (a, b) {
          return a.medico.length - b.medico.length;
        });
        tempEquipoMedico.orderByTeem['Enfermería'] = tempEquipoMedico.orderByTeem['Enfermería'].sort(function (a, b) {
          return a.medico.length - b.medico.length;
        });
        tempEquipoMedico.orderByName = tempEquipoMedico.orderByName.sort(function (a, b) {
          return a.medico.length - b.medico.length;
        });
        return tempEquipoMedico;
      }
    },
    methods: {
      counterRow: function counterRow(medico) {
        return this.dataEpisodio.map(function (x) {
          return x.items;
        }).flat().filter(function (x) {
          return x.medico === medico;
        }).length;
      },
      counterRow2: function counterRow2(fechaFormated) {
        return this.dataEpisodio.filter(function (x) {
          return x.fechaFormated === fechaFormated;
        }).map(function (x) {
          return x.items;
        }).flat().length;
      },
      handleFilter: function handleFilter(data) {
        this.filterActive = true;
        var tipo_filtro = data.tipo_filtro,
            filtro_valor = data.filtro_valor;
        this.tipo_filtro = tipo_filtro;
        this.filtro_valor = filtro_valor;
        this.$emit('update-filtro', {
          tipo_filtro: this.tipo_filtro,
          itemName: this.currentItem,
          filtro_valor: this.filtro_valor
        });
      }
      /* dropdownStop(e){
           e.stopPropagation();
      }, */

    }
  };
};

var AppLaboratorio = function AppLaboratorio() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppLaboratorio";
  return newAppTemplate;
};

var AppImagenes = function AppImagenes() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppImagenes";
  return newAppTemplate;
};

var AppFotografias = function AppFotografias() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppFotografias";
  return newAppTemplate;
};

var AppOtrosArchivos = function AppOtrosArchivos() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppOtrosArchivos";
  return newAppTemplate;
};

var AppProbabilidadDiagnostica = function AppProbabilidadDiagnostica() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppProbabilidadDiagnostica";
  return newAppTemplate;
};

var AppEvolucion = function AppEvolucion() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppEvolucion";
  return newAppTemplate;
};

var AppOrdenMedica = function AppOrdenMedica() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppOrdenMedica";
  return newAppTemplate;
};

var AppPlan = function AppPlan() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppPlan";
  return newAppTemplate;
};

var AppRecipe = function AppRecipe() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppRecipe";
  return newAppTemplate;
};

var AppObservacion = function AppObservacion() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppObservacion";
  return newAppTemplate;
};

var AppEstudioDiagnostico = function AppEstudioDiagnostico() {
  var newAppTemplate = AppTemplate1ForFiles;
  newAppTemplate.name = "AppEstudioDiagnostico";
  return newAppTemplate;
};

var AppAntecedenteComorbilidad = function AppAntecedenteComorbilidad() {
  var newAppTemplate = AppTemplate2ForFiles;
  newAppTemplate.name = "AppAntecedenteComorbilidad";
  return newAppTemplate;
};

var AppListAll = function AppListAll() {
  return {
    name: "AppListAll",
    props: {
      currentItem: String,
      patient_data: Object,
      dataEpisodio: Array,
      index: Number
    },
    template:
    /*html */
    "\n            <div class=\"flex-fill d-flex flex-column overflow-auto\">\n                <ul class=\"nav bg-light justify-content-between nav-pills\" id=\"pills-tab\" role=\"tablist\">\n\n                    <app-filter\n                        :tipo_filtro=\"tipo_filtro\"\n                        :filtro_valor=\"filtro_valor\"\n                        @update-filtro=\"updateFiltro\"\n                        :currentItem=\"currentItem\"\n                        :dataEpisodio=\"dataEpisodio\"\n                    />\n                    <li\n                        v-if=\"index !== 0\"\n                        class=\"d-none nav-item\" role=\"presentation\"\n\n                    >\n                        <a\n                            class=\"nav-link currentItem\"\n                            id=\"pills-createItem-tab\"\n                            data-toggle=\"pill\"\n                            href=\"#pills-createItem\"\n                            role=\"tab\"\n                            aria-controls=\"pills-createItem\"\n                            aria-selected=\"false\"\n\n                        >\n                            <h3 class=\"mb-0\">Agregar {{currentItem}}</h3>\n                        </a>\n                    </li>\n                </ul>\n                <div class=\"flex-fill tab-content d-flex flex-column overflow-auto\" id=\"pills-tabContent\">\n                    <div\n                        class=\"tab-pane overflow-auto fade show active\"\n                        id=\"pills-listItems\"\n                        role=\"tabpanel\"\n                        aria-labelledby=\"pills-listItems-tab\"\n                    >\n                        <div class=\"container-fluid\">\n                            <div class=\"row\">\n                                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 p-1\">\n                                    <div\n                                        v-if=\"dataEpisodio.length > 0\"\n                                        class=\"timeline timeline-inverse\"\n                                    >\n                                        <!-- FECHA DEL DIA -->\n                                        <div v-for=\"(item,index) in dataEpisodio\" :key=\"index\">\n\n                                            <div\n                                                v-if=\"item.items.length !== item.items.filter(x=>!x.active).length\"\n                                                class=\"time-label\">\n                                                <span class=\"bg-primary\"\n                                            >\n                                                    {{ item.fechaFormated }}\n                                                </span>\n                                            </div>\n\n                                            <div\n                                                v-for=\"(item2,index2) in item.items\"\n                                                :key=\"index2\"\n                                                v-show=\"item2.active\"\n                                            >\n\n                                                <i\n                                                    :class=\"['timelime-medic-icon fas fa-user-md',{'bg-primary text-white':medicOrEnfermery(item2)==='medic'},{'bg-warning text-dark':medicOrEnfermery(item2)==='enfermery'}]\"\n                                                    aria-hidden=\"true\">\n                                                </i>\n                                                <div class=\"timeline-item\">\n                                                    <span class=\"time\">\n                                                        <i class=\"far fa-clock\"></i>\n                                                        {{ item2.fechaItem }}\n                                                        <span class=\"text-primary\">|</span>\n                                                        {{ item2.hora }}\n                                                        <a\n                                                            :class=\"['m-0 p-0',{'d-none':noImprimir(item2.description)}]\"\n                                                            type=\"button\"\n                                                            @click=\"imprimir(item2)\"\n                                                            :content=\"'Imprimir'\"\n\n                                                        >\n                                                            <i\n\n                                                                class=\"imprimir m-0 p-0 fas fa-print text-info heartbeat\"\n                                                                style=\"font-size: 1em;cursor:pointer; margin-left:1em;\"\n                                                                aria-hidden=\"true\">\n                                                            </i>\n                                                        </a>\n                                                    </span>\n                                                    <h3\n                                                        data-toggle=\"collapse\"\n                                                        :href=\"'#collapseExample'+index2\"\n                                                        role=\"button\"\n                                                        aria-expanded=\"false\"\n                                                        aria-controls=\"collapseExample\"\n                                                        class=\"timeline-header\"\n                                                    >\n                                                        <b>{{ item2.description }}</b> |\n                                                        <b class=\"text-primary\">{{ item2.dataMedico  !== undefined ? item2.dataMedico.prefijo : '' }} {{ item2.dataMedico !== undefined  ? item2.dataMedico.medico : '' }}</b>\n                                                        <span\n                                                            :content=\"'Equipo '+item2.dataMedico_posicion.nombre\"\n\n                                                            :class=\"['badge  scale-in-hor-left',('badge-'+item2.dataMedico_posicion.color)]\"\n                                                            style=\"width:2em;\">\n                                                            {{ item2.dataMedico_posicion.siglas }}\n                                                        </span>\n\n\n                                                    </h3>\n                                                    <div\n                                                        v-if=\"!['R\xE9cipe'].includes(item2.description)\"\n                                                        :id=\"'collapseExample'+index2\"\n                                                        class=\"collapse show timeline-body\"\n                                                        v-html=\"item2.dato\"\n                                                    >\n                                                    </div>\n                                                    <div \n                                                        v-else\n                                                        :id=\"'collapseExample'+index2\"\n                                                        class=\"collapse show timeline-body\">\n                                                        {{validateRecipeData(item2.dato)}}\n                                                    </div>\n\n                                                </div>\n                                            </div>\n                                        </div>\n                                        <div>\n                                            <i class=\"far fa-clock bg-secondary rounded-circle text-white\"></i>\n                                        </div>\n                                    </div>\n                                    <div v-else class=\"timeline timeline-inverse\">\n                                        <div>\n                                            <div>\n                                                <div class=\"timeline-item\">\n                                                    <div class=\"timeline-body text-secondary text-center\">\n                                                        No posee historia este d\xEDa\n                                                    </div>\n                                                </div>\n                                            </div>\n                                        </div>\n                                        <div>\n                                            <i class=\"far fa-clock bg-gray\"></i>\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n\n\n\n                    </div>\n                    <div class=\"tab-pane fade flex-fill flex-column\" id=\"pills-createItem\" role=\"tabpanel\" aria-labelledby=\"pills-profile-tab\">                        Formulario\n                    </div>\n                </div>\n                <div class=\"d-none bg-danger p-4\"></div>\n            </div>\n\n\n\n        ",
    components: {
      "app-filter": AppFilter() //"app-row-timeline":AppRowTimeline()

    },
    data: function data() {
      return {
        logoURL: '../image/system/logo-cmdlt-color.png',
        //logo CMDLT
        pdfData: {
          //datos del pdf
          pdfName: "recipe",
          created_at: null,
          //fecha de creacion
          medicExt: null,
          // extencion telefonica del medico
          medicName: null,
          // nombre del medico
          medicCI: null,
          // cedula del medico
          medicMPPS: null,
          // MPPS del medico
          medicSchoolNumber: null,
          // numero de colegio del medico
          medicSignatureURL: null,
          //firma
          medicStampURL: null,
          //sello
          medicEspecialities: null,
          //especialidades
          patientAge: null,
          //edad del paciente
          patientCI: null,
          //CI del paciente
          patientName: null,
          //nombre del paciente
          medications: [] //medicaciones

        },
        tipo_filtro: "",
        filtro_valor: "",
        registros: []
      };
    },
    methods: {
      //init pdf
      getFooterDirection: function getFooterDirection(pageSize) {
        return [{
          canvas: [{
            type: 'rect',
            x: 0,
            y: 60,
            w: pageSize.width,
            h: 40,
            color: '#1c71ce'
          }]
        }, {
          alignment: 'justify',
          fontSize: 5,
          absolutePosition: {
            x: 0,
            y: 53
          },
          padding: [0, 0, 0, 30],
          columns: ['leftSide', 'rightSide'].map(function (h) {
            return {
              lineHeight: 0.5,
              color: '#fff',
              margin: [10, 10, 0, 20],
              columns: [{
                text: ["\n                                    Avenida intercomunal La Trinidad El Hatillo,\n \n                                    Apartado postal 80474 Caracas 1080-A\n \n                                    Tel\xE9fonos: (+58) 0212 -949.6411 (central)\n \n                                    Fax: (+58) 0212 -945.6346\n \n                                    Rif: J-00058551-2\n                                "],
                alignment: 'left'
              }, {
                text: ["www.cmdlt.edu.ve"],
                alignment: 'right',
                marginRight: 10
              }]
            };
          })
        }];
      },
      getImageBase64: function getImageBase64(imgURL) {
        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee22() {
          var response, blob, reader, logoDataURL;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee22$(_context22) {
            while (1) {
              switch (_context22.prev = _context22.next) {
                case 0:
                  _context22.next = 2;
                  return fetch(imgURL);

                case 2:
                  response = _context22.sent;

                  if (!(response.status !== 200)) {
                    _context22.next = 5;
                    break;
                  }

                  return _context22.abrupt("return", undefined);

                case 5:
                  _context22.next = 7;
                  return response.blob();

                case 7:
                  blob = _context22.sent;

                  if (!(blob.type !== 'image/png' && blob.type !== 'image/jpeg')) {
                    _context22.next = 11;
                    break;
                  }

                  console.error('El archivo no es un PNG o JPG');
                  return _context22.abrupt("return", undefined);

                case 11:
                  reader = new FileReader();
                  _context22.next = 14;
                  return new Promise(function (resolve, reject) {
                    reader.onload = function () {
                      return resolve(reader.result);
                    };

                    reader.onerror = function () {
                      return reject(reader.error);
                    };

                    reader.readAsDataURL(blob);
                  });

                case 14:
                  logoDataURL = _context22.sent;
                  return _context22.abrupt("return", logoDataURL);

                case 16:
                case "end":
                  return _context22.stop();
              }
            }
          }, _callee22);
        }))();
      },
      getSignatureAndStamp: function getSignatureAndStamp(xPosition) {
        var _this19 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee23() {
          var signatureBase64, stampBase64, signatureAndStamp;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee23$(_context23) {
            while (1) {
              switch (_context23.prev = _context23.next) {
                case 0:
                  _this19.signature = '../' + _this19.pdfData.medicSignatureURL;
                  _this19.stamp = '../' + _this19.pdfData.medicStampURL;

                  if (!_this19.signature) {
                    _context23.next = 8;
                    break;
                  }

                  _context23.next = 5;
                  return _this19.getImageBase64(_this19.signature);

                case 5:
                  _context23.t0 = _context23.sent;
                  _context23.next = 9;
                  break;

                case 8:
                  _context23.t0 = undefined;

                case 9:
                  signatureBase64 = _context23.t0;

                  if (!_this19.stamp) {
                    _context23.next = 18;
                    break;
                  }

                  _context23.next = 13;
                  return _this19.getImageBase64(_this19.stamp);

                case 13:
                  _context23.next = 15;
                  return _context23.sent;

                case 15:
                  _context23.t1 = _context23.sent;
                  _context23.next = 19;
                  break;

                case 18:
                  _context23.t1 = undefined;

                case 19:
                  stampBase64 = _context23.t1;
                  signatureAndStamp = [];

                  if (signatureBase64) {
                    signatureAndStamp.push({
                      image: signatureBase64,
                      fit: [100, 80],
                      absolutePosition: {
                        x: xPosition,
                        y: -25
                      }
                    });
                  }

                  if (stampBase64) {
                    signatureAndStamp.push({
                      image: stampBase64,
                      fit: [100, 80],
                      absolutePosition: {
                        x: xPosition,
                        y: 0
                      }
                    });
                  }

                  return _context23.abrupt("return", signatureAndStamp);

                case 24:
                case "end":
                  return _context23.stop();
              }
            }
          }, _callee23);
        }))();
      },
      getRecipeIndications: function getRecipeIndications(counter, indications) {
        return {
          color: '#000',
          width: '48%',
          text: (counter && counter + '. ') + indications,
          fontSize: 10,
          margin: [20, 15, 30, 0]
        };
      },
      getMedicData: function getMedicData(recipe) {
        var _this20 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee24() {
          var getSignatureAndStampL, getSignatureAndStampR, createdUserL, createdUserR;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee24$(_context24) {
            while (1) {
              switch (_context24.prev = _context24.next) {
                case 0:
                  _context24.next = 2;
                  return _this20.getSignatureAndStamp(280);

                case 2:
                  getSignatureAndStampL = _context24.sent;
                  _context24.next = 5;
                  return _this20.getSignatureAndStamp(700);

                case 5:
                  getSignatureAndStampR = _context24.sent;
                  createdUserL = _this20.getCreatedUser();
                  createdUserR = _this20.getCreatedUser();
                  return _context24.abrupt("return", [{
                    alignment: 'justify',
                    fontSize: 8,
                    absolutePosition: {
                      x: 0,
                      y: -7
                    },
                    padding: [0, 0, 0, 30],
                    columns: [{
                      lineHeight: 0.9,
                      color: '#000',
                      margin: [10, 10, 0, 20],
                      columns: [createdUserL, {
                        text: 'Firma y sello',
                        columns: getSignatureAndStampL
                      }]
                    }, {
                      lineHeight: 0.9,
                      color: '#000',
                      margin: [10, 10, 0, 20],
                      columns: [createdUserR, {
                        text: 'Firma y sello',
                        columns: getSignatureAndStampR
                      }]
                    }]
                  }]);

                case 9:
                case "end":
                  return _context24.stop();
              }
            }
          }, _callee24);
        }))();
      },
      getRecipeMedications: function getRecipeMedications(number, recipe) {
        return {
          margin: [0, 15, 0, 0],
          color: '#000',
          width: '48%',
          stack: [{
            text: number + '. ' + recipe.name
          }, {
            text: [{
              text: " "
            }, {
              text: "        ".concat(recipe.presentation ? recipe.presentation : '')
            }, {
              text: " "
            }, {
              text: "".concat(recipe.units ? recipe.units : '')
            }]
          }]
        };
      },
      getCreatedUser: function getCreatedUser() {
        var speciality = this.pdfData.medicEspecialities ? '/ ' + this.pdfData.medicEspecialities.map(function (us) {
          return us;
        }).join(', ') : '';
        return {
          text: 'Datos del médico',
          columns: [{
            stack: [{
              text: 'Datos del médico',
              bold: true
            }, {
              text: "".concat(this.pdfData.medicExt ? '(' + this.pdfData.medicExt + ') ' : '').concat(this.pdfData.medicName, " ").concat(speciality)
            }, {
              text: [{
                text: 'C.I.: '
              }, {
                text: this.pdfData.medicCI || ''
              }]
            }, {
              text: [{
                text: 'Colegio de Médicos: '
              }, {
                text: this.pdfData.medicSchoolNumber || ''
              }]
            }, {
              text: [{
                text: 'MPPS: '
              }, {
                text: this.pdfData.medicMPPS || ''
              }]
            }],
            unbreakable: true
          }]
        };
      },
      dateFormat: function dateFormat(dateStr) {
        var date = new Date(dateStr);
        var year = date.getFullYear();
        var month = String(date.getMonth() + 1).padStart(2, '0');
        var day = String(date.getDate()).padStart(2, '0');
        return "".concat(day, "/").concat(month, "/").concat(year);
      },
      printPdf: function printPdf(item) {
        var _this21 = this;

        return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee25() {
          var logoBase64, footerDirection, medicData, docDefinition;
          return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee25$(_context25) {
            while (1) {
              switch (_context25.prev = _context25.next) {
                case 0:
                  _this21.pdfData.created_at = _this21.dateFormat(item.created_at);
                  _this21.pdfData.patientAge = _this21.patient_data.edad;
                  _this21.pdfData.patientCI = _this21.patient_data.cedula_formated;
                  _this21.pdfData.patientName = _this21.patient_data.paciente;

                  if (_this21.esArrayDeObjetos(item.dato)) {
                    _this21.pdfData.medications = JSON.parse(item.value);
                  } else {
                    _this21.pdfData.medications = [{
                      presentation: '',
                      name: item.value,
                      indications: '',
                      unit: ''
                    }];
                  }

                  _this21.pdfData.medicExt = item.extencion_telefonica;
                  _this21.pdfData.medicName = item.medico;
                  _this21.pdfData.medicCI = item.medico_cedula;
                  _this21.pdfData.medicMPPS = item.mpps;
                  _this21.pdfData.medicSchoolNumber = item.colegio_medico;
                  _this21.pdfData.medicSignatureURL = item.firma;
                  _this21.pdfData.medicStampURL = item.sello;
                  _this21.pdfData.medicEspecialities = item.medico_especialidades ? item.medico_especialidades.split(',') : []; //Logo cmdlt

                  _context25.next = 15;
                  return _this21.getImageBase64(_this21.logoURL);

                case 15:
                  logoBase64 = _context25.sent;
                  //cintillo inferior del recipe
                  footerDirection = _this21.getFooterDirection; //datos del medico / sello / firma

                  _context25.next = 19;
                  return _this21.getMedicData();

                case 19:
                  medicData = _context25.sent;
                  docDefinition = {
                    pageSize: 'A4',
                    pageOrientation: 'landscape',
                    pageMargins: [20, 120, 20, 100],
                    header: {
                      stack: [{
                        margin: [0, 10, 0, 0],
                        alignment: 'justify',
                        columns: [{
                          marginRight: 10,
                          image: logoBase64,
                          fit: [120, 80],
                          alignment: 'right'
                        }, {
                          marginRight: 10,
                          image: logoBase64,
                          fit: [120, 80],
                          alignment: 'right'
                        }]
                      }, {
                        alignment: 'justify',
                        columns: ['leftSide', 'rightSide'].map(function (h, index) {
                          return {
                            margin: [20, 10, 20, 10],
                            columns: [{
                              fontSize: 12,
                              text: h === 'leftSide' ? 'Récipe' : 'Indicaciones',
                              bold: true
                            }, {
                              margin: [0, 0, 3, 0],
                              alignment: 'right',
                              text: [{
                                text: 'Fecha: ',
                                bold: true
                              }, {
                                color: '#000',
                                text: _this21.pdfData.created_at ? _this21.pdfData.created_at : 'No disponible'
                              }]
                            }]
                          };
                        })
                      }, {
                        alignment: 'justify',
                        columns: ['leftSide', 'rightSide'].filter(Boolean).map(function (h) {
                          return {
                            margin: [20, 0, 10, 5],
                            text: [{
                              text: 'Nombre y apellido: ',
                              bold: true
                            }, {
                              color: '#000',
                              text: _this21.pdfData.patientName ? _this21.pdfData.patientName : 'No disponible'
                            }]
                          };
                        })
                      }, {
                        alignment: 'justify',
                        columns: ['leftSide', 'rightSide'].filter(Boolean).map(function (h) {
                          return {
                            margin: [20, 0, 20, 0],
                            text: [{
                              text: 'C.I: ',
                              bold: true
                            }, {
                              color: '#000',
                              text: _this21.pdfData.patientCI ? _this21.pdfData.patientCI : 'No disponible'
                            }, {
                              text: '    '
                            }, {
                              text: 'Edad: ',
                              bold: true
                            }, {
                              color: '#000',
                              text: _this21.pdfData.patientAge + ' año' + (_this21.pdfData.patientAge !== 1 ? 's' : '')
                            }]
                          };
                        })
                      }]
                    },
                    content: _this21.pdfData.medications.map(function (item, index) {
                      return {
                        margin: [0, 10, 0, 0],
                        alignment: 'justify',
                        unbreakable: true,
                        columns: [_this21.getRecipeMedications(index + 1, item), {
                          width: '4%',
                          text: ' '
                        }, _this21.getRecipeIndications(index + 1, item.indications)]
                      };
                    }),
                    footer: function footer(currentPage, pageCount, pageSize) {
                      return [].concat(_toConsumableArray(medicData), _toConsumableArray(footerDirection(pageSize)));
                    },
                    styles: {
                      header: {
                        fontSize: 12,
                        bold: true
                      }
                    },
                    defaultStyle: {
                      fontSize: 10,
                      color: '#1c71ce'
                    }
                  }; //datos del paciente

                  pdfMake.createPdf(docDefinition).download(_this21.pdfData.pdfName + '-' + (_this21.pdfData.patientName ? _this21.pdfData.patientName.replace(/ /g, "") : 'patientName') + '-' + (_this21.pdfData.created_at ? _this21.pdfData.created_at.split(' ')[0] : 'created_at') + '.pdf');

                case 22:
                case "end":
                  return _context25.stop();
              }
            }
          }, _callee25);
        }))();
      },
      esArrayDeObjetos: function esArrayDeObjetos(str) {
        try {
          // Intentamos convertir el string en JSON
          var json = JSON.parse(str); // Verificamos que sea un array

          if (Array.isArray(json)) {
            // Verificamos que todos los elementos del array sean objetos
            return json.every(function (item) {
              return _typeof(item) === 'object' && item !== null;
            });
          }

          return false;
        } catch (e) {
          // Si ocurre un error al parsear, no es un JSON válido
          return false;
        }
      },
      //end pdf
      validateRecipeData: function validateRecipeData(item) {
        if (this.esArrayDeObjetos(item)) {
          return JSON.parse(item).map(function (x) {
            return x.name;
          }).join(", ");
        } else {
          return item;
        }
      },
      medicOrEnfermery: function medicOrEnfermery(item) {
        try {
          if ([1, 2, 3, 4, 5, 6, 7, 8, 9].includes(Number(item.dataMedico.posicion_id))) {
            return "medic";
          }

          if ([10].includes(Number(item.dataMedico.posicion_id))) {
            return "enfermery";
          }
        } catch (error) {
          console.log("data medico: ", item);
        }

        return "not-found";
      },
      noImprimir: function noImprimir(description) {
        if (["Fotografías", "Pendiente", "Probabilidad diagnóstica", "Plan", "Observación", "Laboratorio", "Imagenes", "Otro"].includes(description)) {
          return true;
        } else {
          return false;
        }
      },
      imprimir: function imprimir(item) {
        //imprimir(name,id){
        var name = item.description,
            id = item.id;
        var description = "";
        var type = "one";

        switch (name) {
          case "Orden Médica":
            description = "orden_medica";
            break;

          case "Evolución":
            description = "evolucion";
            break;

          case "Pendiente":
            description = "pendiente";
            break;

          case "Probabilidad diagnóstica":
            description = "probabilidad_diagnostica";
            break;

          case "Plan":
            description = "plan";
            break;

          case "Observación":
            description = "observacion";
            break;

          case "Récipe":
            description = "recipe";
            break;

          case "Estudio Diagnóstico":
            description = "estudio_diagnostico";
            break;

          case "Laboratorio":
            description = "laboratorio";
            break;

          case "Imagenes":
            description = "imagenes";
            break;

          case "Otro":
            description = "otro";
            break;

          default:
            alert("Error. Reporte no encotrado");
            return false;
            break;
        } //Los siguientes informes no se imprimen


        if (['Orden Médica'].includes(name)) {
          window.open(location.origin + "/user/informe/omed/orden_medica/".concat(this.patient_data.episodio_id, "/").concat(id));
          return false;
        }

        if (['Evolución'].includes(name)) {
          window.open(location.origin + "/user/informe/nde/evolucion_medica/".concat(this.patient_data.episodio_id, "/").concat(id));
          return false;
        }

        if (['Récipe'].includes(name)) {
          this.printPdf(item);
          return false;
        }

        if (type === "one") {
          window.open("/user/reporte/".concat(description, "/").concat(this.patient_data.episodio_id, "/").concat(id, "/mono"));
        } else {
          window.open("/user/reporte/".concat(description, "/").concat(this.patient_data.episodio_id, "/all/mono"));
        }
      },
      updateFiltro: function updateFiltro(data) {
        this.tipo_filtro = data.tipo_filtro;
        this.filtro_valor = data.filtro_valor;
        this.showRow();
      },
      showRow: function showRow() {
        var _this22 = this;

        if (this.filtro_valor === null) {
          this.registros.map(function (x) {
            x.active = true;
          });
          return false;
        }

        if (this.tipo_filtro === "medico") {
          this.registros.map(function (x) {
            if (x.medico === _this22.filtro_valor) {
              x.active = true;
            } else {
              x.active = false;
            }

            return x;
          });
        }

        if (this.tipo_filtro === "fecha") {
          this.registros.map(function (x) {
            if (x.fechaItem === _this22.filtro_valor) {
              x.active = true;
            } else {
              x.active = false;
            }

            return x;
          });
        }
      },
      getAllByDay: function getAllByDay(fecha) {
        return this.registros.filter(function (x) {
          return x.fechaFormated === fecha;
        }).map(function (x) {
          return x.items;
        }).flat();
      }
    },
    mounted: function mounted() {
      var _this23 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee26() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee26$(_context26) {
          while (1) {
            switch (_context26.prev = _context26.next) {
              case 0:
                _this23.registros = _this23.dataEpisodio.map(function (x) {
                  return x.items;
                }).flat();

              case 1:
              case "end":
                return _context26.stop();
            }
          }
        }, _callee26);
      }))();
    }
  };
};

var AppTemplate1ForFiles = {
  name: "App",
  props: {
    updateParentData: Function,
    buttonsState: Boolean,
    updateComponentToReturn: Function,
    currentItem: String,
    patient_data: Object,
    dataEpisodio: Array,
    index: Number,
    currentComponent: Function
  },
  template:
  /*html */
  "\n            <div class=\"flex-fill d-flex flex-column overflow-auto\">\n                <ul class=\"nav bg-light justify-content-between nav-pills\" id=\"pills-tab\" role=\"tablist\">\n\n                    <app-filter\n                        :tipo_filtro=\"tipo_filtro\"\n                        :filtro_valor=\"filtro_valor\"\n                        @update-filtro=\"updateFiltro\"\n                        :currentItem=\"currentItem\"\n                        :dataEpisodio=\"dataEpisodio\"\n                    />\n                    <li\n\n                        class=\"nav-item\" role=\"presentation\"\n                        @click=\"sendDataToParent\"\n                    >\n                        <a\n\n                            class=\"nav-link currentItem\"\n                            id=\"pills-createItem-tab\"\n                            data-toggle=\"pill\"\n                            href=\"#pills-createItem\"\n                            role=\"tab\"\n                            aria-controls=\"pills-createItem\"\n                            aria-selected=\"false\"\n                        >\n                            <h3 class=\"mb-0\">Agregar {{currentItem}}</h3>\n                        </a>\n                    </li>\n                </ul>\n                <div class=\"flex-fill tab-content d-flex flex-column overflow-auto\" id=\"pills-tabContent\">\n                    <div\n                        class=\"tab-pane overflow-auto fade show active\"\n                        id=\"pills-listItems\"\n                        role=\"tabpanel\"\n                        aria-labelledby=\"pills-listItems-tab\"\n                    >\n                        <div class=\"container-fluid\">\n                            <div class=\"row\">\n                                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 p-1\">\n                                    <div\n                                        v-if=\"dataEpisodio.length > 0\"\n                                        class=\"timeline timeline-inverse\"\n                                    >\n                                        <!-- FECHA DEL DIA -->\n                                        <div v-for=\"(item,index) in dataEpisodio\" :key=\"index\">\n\n                                            <div\n                                                v-if=\"item.items.length !== item.items.filter(x=>!x.active).length\"\n                                                class=\"time-label\">\n                                                <span class=\"bg-primary\"\n                                            >\n                                                    {{ item.fechaFormated }}\n                                                </span>\n                                            </div>\n\n                                            <div\n                                                v-for=\"(item2,index2) in item.items\"\n                                                :key=\"index2\"\n                                                v-show=\"item2.active\"\n                                            >\n\n                                                <i\n                                                    :class=\"['timelime-medic-icon fas fa-user-md',{'bg-primary text-white':medicOrEnfermery(item2)==='medic'},{'bg-warning text-dark':medicOrEnfermery(item2)==='enfermery'}]\"\n                                                    aria-hidden=\"true\">\n                                                </i>\n                                                <div class=\"timeline-item\">\n                                                    <span class=\"time\">\n                                                        <i class=\"far fa-clock\"></i>\n                                                        {{ item2.fechaItem }}\n                                                        <span class=\"text-primary\">|</span>\n                                                        {{ item2.hora }}\n                                                        <a\n                                                            :class=\"['m-0 p-0',{'d-none':noImprimir(item2.description)}]\"\n                                                            type=\"button\"\n                                                            @click=\"imprimir(item2)\"\n                                                            :content=\"'Imprimir'\"\n\n                                                        >\n                                                            <i\n\n                                                                class=\"imprimir m-0 p-0 fas fa-print text-info heartbeat\"\n                                                                style=\"font-size: 1em;cursor:pointer; margin-left:1em;\"\n                                                                aria-hidden=\"true\">\n                                                            </i>\n                                                        </a>\n                                                    </span>\n                                                    <h3\n                                                        data-toggle=\"collapse\"\n                                                        :href=\"'#collapseExample'+index2\"\n                                                        role=\"button\"\n                                                        aria-expanded=\"false\"\n                                                        aria-controls=\"collapseExample\"\n                                                        class=\"timeline-header\"\n                                                    >\n                                                        <b>{{ item2.description }}</b> |\n                                                        <b class=\"text-primary\">{{ item2.dataMedico !== undefined ? item2.dataMedico.prefijo : '' }} {{ item2.dataMedico !== undefined ? item2.dataMedico.medico : '' }}</b>\n                                                        <span\n                                                            :content=\"'Equipo '+item2.dataMedico_posicion.nombre\"\n\n                                                            :class=\"['badge  scale-in-hor-left',('badge-'+item2.dataMedico_posicion.color)]\"\n                                                            style=\"width:2em;\">\n                                                            {{ item2.dataMedico_posicion.siglas }}\n                                                        </span>\n\n\n                                                    </h3>\n                                                    <div\n                                                        v-if=\"!['R\xE9cipe'].includes(item2.description)\"\n                                                        :id=\"'collapseExample'+index2\"\n                                                        class=\"collapse show timeline-body\"\n                                                        v-html=\"item2.dato\"\n                                                    >\n                                                    </div>\n                                                    <div \n                                                        v-else\n                                                        :id=\"'collapseExample'+index2\"\n                                                        class=\"collapse show timeline-body\">\n                                                        {{validateRecipeData(item2.dato)}}\n                                                    </div>\n\n                                                </div>\n                                            </div>\n                                        </div>\n                                        <div>\n                                            <i class=\"far fa-clock bg-secondary rounded-circle text-white\"></i>\n                                        </div>\n                                    </div>\n                                    <div v-else class=\"timeline timeline-inverse\">\n                                        <div>\n                                            <div>\n                                                <div class=\"timeline-item\">\n                                                    <div class=\"timeline-body text-secondary text-center\">\n                                                        No posee historia este d\xEDa\n                                                    </div>\n                                                </div>\n                                            </div>\n                                        </div>\n                                        <div>\n                                            <i class=\"far fa-clock bg-gray\"></i>\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n\n\n\n                    </div>\n                    <div class=\"tab-pane fade flex-fill flex-column\" id=\"pills-createItem\" role=\"tabpanel\" aria-labelledby=\"pills-profile-tab\">\n\n                        <component\n                            :is=\"currentComponent\"\n                            :currentItem=\"currentItem\"\n                        ></component>\n                    </div>\n                </div>\n                <div class=\"d-none bg-danger p-4\"></div>\n            </div>\n\n\n\n        ",
  components: {
    "app-list-items-historia": AppListItemsHistoria(),
    "app-create-item-file": AppCreateItemFile(),
    "app-create-recipe": AppCreateRecipe(),
    "app-filter": AppFilter(),
    "app-row-timeline": AppRowTimeline()
  },
  data: function data() {
    return {
      logoURL: '../image/system/logo-cmdlt-color.png',
      //logo CMDLT
      pdfData: {
        //datos del pdf
        pdfName: "recipe",
        created_at: null,
        //fecha de creacion
        medicExt: null,
        // extencion telefonica del medico
        medicName: null,
        // nombre del medico
        medicCI: null,
        // cedula del medico
        medicMPPS: null,
        // MPPS del medico
        medicSchoolNumber: null,
        // numero de colegio del medico
        medicSignatureURL: null,
        //firma
        medicStampURL: null,
        //sello
        medicEspecialities: null,
        //especialidades
        patientAge: null,
        //edad del paciente
        patientCI: null,
        //CI del paciente
        patientName: null,
        //nombre del paciente
        medications: [] //medicaciones

      },
      currentComponent: null,
      tipo_filtro: "",
      filtro_valor: "",
      registros: []
    };
  },
  methods: {
    //init pdf
    getFooterDirection: function getFooterDirection(pageSize) {
      return [{
        canvas: [{
          type: 'rect',
          x: 0,
          y: 60,
          w: pageSize.width,
          h: 40,
          color: '#1c71ce'
        }]
      }, {
        alignment: 'justify',
        fontSize: 5,
        absolutePosition: {
          x: 0,
          y: 53
        },
        padding: [0, 0, 0, 30],
        columns: ['leftSide', 'rightSide'].map(function (h) {
          return {
            lineHeight: 0.5,
            color: '#fff',
            margin: [10, 10, 0, 20],
            columns: [{
              text: ["\n                                Avenida intercomunal La Trinidad El Hatillo,\n \n                                Apartado postal 80474 Caracas 1080-A\n \n                                Tel\xE9fonos: (+58) 0212 -949.6411 (central)\n \n                                Fax: (+58) 0212 -945.6346\n \n                                Rif: J-00058551-2\n                            "],
              alignment: 'left'
            }, {
              text: ["www.cmdlt.edu.ve"],
              alignment: 'right',
              marginRight: 10
            }]
          };
        })
      }];
    },
    getImageBase64: function getImageBase64(imgURL) {
      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee27() {
        var response, blob, reader, logoDataURL;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee27$(_context27) {
          while (1) {
            switch (_context27.prev = _context27.next) {
              case 0:
                _context27.next = 2;
                return fetch(imgURL);

              case 2:
                response = _context27.sent;

                if (!(response.status !== 200)) {
                  _context27.next = 5;
                  break;
                }

                return _context27.abrupt("return", undefined);

              case 5:
                _context27.next = 7;
                return response.blob();

              case 7:
                blob = _context27.sent;

                if (!(blob.type !== 'image/png' && blob.type !== 'image/jpeg')) {
                  _context27.next = 11;
                  break;
                }

                console.error('El archivo no es un PNG o JPG');
                return _context27.abrupt("return", undefined);

              case 11:
                reader = new FileReader();
                _context27.next = 14;
                return new Promise(function (resolve, reject) {
                  console.log(reader);

                  reader.onload = function () {
                    return resolve(reader.result);
                  };

                  reader.onerror = function () {
                    return reject(reader.error);
                  };

                  reader.readAsDataURL(blob);
                });

              case 14:
                logoDataURL = _context27.sent;
                return _context27.abrupt("return", logoDataURL);

              case 16:
              case "end":
                return _context27.stop();
            }
          }
        }, _callee27);
      }))();
    },
    getSignatureAndStamp: function getSignatureAndStamp(xPosition) {
      var _this24 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee28() {
        var signatureBase64, stampBase64, signatureAndStamp;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee28$(_context28) {
          while (1) {
            switch (_context28.prev = _context28.next) {
              case 0:
                _this24.signature = '../' + _this24.pdfData.medicSignatureURL;
                _this24.stamp = '../' + _this24.pdfData.medicStampURL;

                if (!_this24.signature) {
                  _context28.next = 8;
                  break;
                }

                _context28.next = 5;
                return _this24.getImageBase64(_this24.signature);

              case 5:
                _context28.t0 = _context28.sent;
                _context28.next = 9;
                break;

              case 8:
                _context28.t0 = undefined;

              case 9:
                signatureBase64 = _context28.t0;

                if (!_this24.stamp) {
                  _context28.next = 18;
                  break;
                }

                _context28.next = 13;
                return _this24.getImageBase64(_this24.stamp);

              case 13:
                _context28.next = 15;
                return _context28.sent;

              case 15:
                _context28.t1 = _context28.sent;
                _context28.next = 19;
                break;

              case 18:
                _context28.t1 = undefined;

              case 19:
                stampBase64 = _context28.t1;
                signatureAndStamp = [];

                if (signatureBase64) {
                  signatureAndStamp.push({
                    image: signatureBase64,
                    fit: [100, 80],
                    absolutePosition: {
                      x: xPosition,
                      y: -25
                    }
                  });
                }

                if (stampBase64) {
                  signatureAndStamp.push({
                    image: stampBase64,
                    fit: [100, 80],
                    absolutePosition: {
                      x: xPosition,
                      y: 0
                    }
                  });
                }

                return _context28.abrupt("return", signatureAndStamp);

              case 24:
              case "end":
                return _context28.stop();
            }
          }
        }, _callee28);
      }))();
    },
    getRecipeIndications: function getRecipeIndications(counter, indications) {
      return {
        color: '#000',
        width: '48%',
        text: (counter && counter + '. ') + indications,
        fontSize: 10,
        margin: [20, 15, 30, 0]
      };
    },
    getMedicData: function getMedicData(recipe) {
      var _this25 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee29() {
        var getSignatureAndStampL, getSignatureAndStampR, createdUserL, createdUserR;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee29$(_context29) {
          while (1) {
            switch (_context29.prev = _context29.next) {
              case 0:
                _context29.next = 2;
                return _this25.getSignatureAndStamp(280);

              case 2:
                getSignatureAndStampL = _context29.sent;
                _context29.next = 5;
                return _this25.getSignatureAndStamp(700);

              case 5:
                getSignatureAndStampR = _context29.sent;
                createdUserL = _this25.getCreatedUser();
                createdUserR = _this25.getCreatedUser();
                return _context29.abrupt("return", [{
                  alignment: 'justify',
                  fontSize: 8,
                  absolutePosition: {
                    x: 0,
                    y: -7
                  },
                  padding: [0, 0, 0, 30],
                  columns: [{
                    lineHeight: 0.9,
                    color: '#000',
                    margin: [10, 10, 0, 20],
                    columns: [createdUserL, {
                      text: 'Firma y sello',
                      columns: getSignatureAndStampL
                    }]
                  }, {
                    lineHeight: 0.9,
                    color: '#000',
                    margin: [10, 10, 0, 20],
                    columns: [createdUserR, {
                      text: 'Firma y sello',
                      columns: getSignatureAndStampR
                    }]
                  }]
                }]);

              case 9:
              case "end":
                return _context29.stop();
            }
          }
        }, _callee29);
      }))();
    },
    getRecipeMedications: function getRecipeMedications(number, recipe) {
      return {
        margin: [0, 15, 0, 0],
        color: '#000',
        width: '48%',
        stack: [{
          text: number + '. ' + recipe.name
        }, {
          text: [{
            text: " "
          }, {
            text: "        ".concat(recipe.presentation ? recipe.presentation : '')
          }, {
            text: " "
          }, {
            text: "".concat(recipe.units ? recipe.units : '')
          }]
        }]
      };
    },
    getCreatedUser: function getCreatedUser() {
      var speciality = this.pdfData.medicEspecialities ? '/ ' + this.pdfData.medicEspecialities.map(function (us) {
        return us;
      }).join(', ') : '';
      return {
        text: 'Datos del médico',
        columns: [{
          stack: [{
            text: 'Datos del médico',
            bold: true
          }, {
            text: "".concat(this.pdfData.medicExt ? '(' + this.pdfData.medicExt + ') ' : '').concat(this.pdfData.medicName, " ").concat(speciality)
          }, {
            text: [{
              text: 'C.I.: '
            }, {
              text: this.pdfData.medicCI || ''
            }]
          }, {
            text: [{
              text: 'Colegio de Médicos: '
            }, {
              text: this.pdfData.medicSchoolNumber || ''
            }]
          }, {
            text: [{
              text: 'MPPS: '
            }, {
              text: this.pdfData.medicMPPS || ''
            }]
          }],
          unbreakable: true
        }]
      };
    },
    dateFormat: function dateFormat(dateStr) {
      var date = new Date(dateStr);
      var year = date.getFullYear();
      var month = String(date.getMonth() + 1).padStart(2, '0');
      var day = String(date.getDate()).padStart(2, '0');
      return "".concat(day, "/").concat(month, "/").concat(year);
    },
    printPdf: function printPdf(item) {
      var _this26 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee30() {
        var logoBase64, footerDirection, medicData, docDefinition;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee30$(_context30) {
          while (1) {
            switch (_context30.prev = _context30.next) {
              case 0:
                _this26.pdfData.created_at = _this26.dateFormat(item.created_at);
                _this26.pdfData.patientAge = _this26.patient_data.edad;
                _this26.pdfData.patientCI = _this26.patient_data.cedula_formated;
                _this26.pdfData.patientName = _this26.patient_data.paciente;

                if (_this26.esArrayDeObjetos(item.dato)) {
                  _this26.pdfData.medications = JSON.parse(item.value);
                } else {
                  _this26.pdfData.medications = [{
                    presentation: '',
                    name: item.value,
                    indications: '',
                    unit: ''
                  }];
                }

                _this26.pdfData.medicExt = item.extencion_telefonica;
                _this26.pdfData.medicName = item.medico;
                _this26.pdfData.medicCI = item.medico_cedula;
                _this26.pdfData.medicMPPS = item.mpps;
                _this26.pdfData.medicSchoolNumber = item.colegio_medico;
                _this26.pdfData.medicSignatureURL = item.firma;
                _this26.pdfData.medicStampURL = item.sello;
                _this26.pdfData.medicEspecialities = item.medico_especialidades ? item.medico_especialidades.split(',') : [];
                console.log("2", _this26.pdfData); //Logo cmdlt

                _context30.next = 16;
                return _this26.getImageBase64(_this26.logoURL);

              case 16:
                logoBase64 = _context30.sent;
                //cintillo inferior del recipe
                footerDirection = _this26.getFooterDirection; //datos del medico / sello / firma

                _context30.next = 20;
                return _this26.getMedicData();

              case 20:
                medicData = _context30.sent;
                docDefinition = {
                  pageSize: 'A4',
                  pageOrientation: 'landscape',
                  pageMargins: [20, 120, 20, 100],
                  header: {
                    stack: [{
                      margin: [0, 10, 0, 0],
                      alignment: 'justify',
                      columns: [{
                        marginRight: 10,
                        image: logoBase64,
                        fit: [120, 80],
                        alignment: 'right'
                      }, {
                        marginRight: 10,
                        image: logoBase64,
                        fit: [120, 80],
                        alignment: 'right'
                      }]
                    }, {
                      alignment: 'justify',
                      columns: ['leftSide', 'rightSide'].map(function (h, index) {
                        return {
                          margin: [20, 10, 20, 10],
                          columns: [{
                            fontSize: 12,
                            text: h === 'leftSide' ? 'Récipe' : 'Indicaciones',
                            bold: true
                          }, {
                            margin: [0, 0, 3, 0],
                            alignment: 'right',
                            text: [{
                              text: 'Fecha: ',
                              bold: true
                            }, {
                              color: '#000',
                              text: _this26.pdfData.created_at ? _this26.pdfData.created_at : 'No disponible'
                            }]
                          }]
                        };
                      })
                    }, {
                      alignment: 'justify',
                      columns: ['leftSide', 'rightSide'].filter(Boolean).map(function (h) {
                        return {
                          margin: [20, 0, 10, 5],
                          text: [{
                            text: 'Nombre y apellido: ',
                            bold: true
                          }, {
                            color: '#000',
                            text: _this26.pdfData.patientName ? _this26.pdfData.patientName : 'No disponible'
                          }]
                        };
                      })
                    }, {
                      alignment: 'justify',
                      columns: ['leftSide', 'rightSide'].filter(Boolean).map(function (h) {
                        return {
                          margin: [20, 0, 20, 0],
                          text: [{
                            text: 'C.I: ',
                            bold: true
                          }, {
                            color: '#000',
                            text: _this26.pdfData.patientCI ? _this26.pdfData.patientCI : 'No disponible'
                          }, {
                            text: '    '
                          }, {
                            text: 'Edad: ',
                            bold: true
                          }, {
                            color: '#000',
                            text: _this26.pdfData.patientAge + ' año' + (_this26.pdfData.patientAge !== 1 ? 's' : '')
                          }]
                        };
                      })
                    }]
                  },
                  content: _this26.pdfData.medications.map(function (item, index) {
                    return {
                      margin: [0, 10, 0, 0],
                      alignment: 'justify',
                      unbreakable: true,
                      columns: [_this26.getRecipeMedications(index + 1, item), {
                        width: '4%',
                        text: ' '
                      }, _this26.getRecipeIndications(index + 1, item.indications)]
                    };
                  }),
                  footer: function footer(currentPage, pageCount, pageSize) {
                    return [].concat(_toConsumableArray(medicData), _toConsumableArray(footerDirection(pageSize)));
                  },
                  styles: {
                    header: {
                      fontSize: 12,
                      bold: true
                    }
                  },
                  defaultStyle: {
                    fontSize: 10,
                    color: '#1c71ce'
                  }
                }; //datos del paciente

                pdfMake.createPdf(docDefinition).download(_this26.pdfData.pdfName + '-' + (_this26.pdfData.patientName ? _this26.pdfData.patientName.replace(/ /g, "") : 'patientName') + '-' + (_this26.pdfData.created_at ? _this26.pdfData.created_at.split(' ')[0] : 'created_at') + '.pdf');

              case 23:
              case "end":
                return _context30.stop();
            }
          }
        }, _callee30);
      }))();
    },
    esArrayDeObjetos: function esArrayDeObjetos(str) {
      try {
        // Intentamos convertir el string en JSON
        var json = JSON.parse(str); // Verificamos que sea un array

        if (Array.isArray(json)) {
          // Verificamos que todos los elementos del array sean objetos
          return json.every(function (item) {
            return _typeof(item) === 'object' && item !== null;
          });
        }

        return false;
      } catch (e) {
        // Si ocurre un error al parsear, no es un JSON válido
        return false;
      }
    },
    //end pdf
    validateRecipeData: function validateRecipeData(item) {
      if (this.esArrayDeObjetos(item)) {
        return JSON.parse(item).map(function (x) {
          return x.name;
        }).join(", ");
      } else {
        return item;
      }
    },
    medicOrEnfermery: function medicOrEnfermery(item) {
      try {
        if ([1, 2, 3, 4, 5, 6, 7, 8, 9].includes(Number(item.dataMedico.posicion_id))) {
          return "medic";
        }

        if ([10].includes(Number(item.dataMedico.posicion_id))) {
          return "enfermery";
        }
      } catch (error) {
        console.log("data medico: ", item);
      }

      return "not-found";
    },
    imprimir: function imprimir(item) {
      var name = item.description,
          id = item.id;
      var description = "";
      var type = "one";

      switch (name) {
        case "Orden Médica":
          description = "orden_medica";
          break;

        case "Evolución":
          description = "evolucion";
          break;

        case "Pendiente":
          description = "pendiente";
          break;

        case "Probabilidad diagnóstica":
          description = "probabilidad_diagnostica";
          break;

        case "Plan":
          description = "plan";
          break;

        case "Observación":
          description = "observacion";
          break;

        case "Récipe":
          description = "recipe";
          break;

        case "Estudio Diagnóstico":
          description = "estudio_diagnostico";
          break;

        case "Laboratorio":
          description = "laboratorio";
          break;

        case "Imagenes":
          description = "imagenes";
          break;

        case "Otro":
          description = "otro";
          break;

        default:
          alert("Error. Reporte no encotrado");
          return false;
          break;
      } //Los siguientes informes no se imprimen


      if (['Orden Médica'].includes(name)) {
        window.open(location.origin + "/user/informe/omed/orden_medica/".concat(this.patient_data.episodio_id, "/").concat(id));
        return false;
      }

      if (['Evolución'].includes(name)) {
        window.open(location.origin + "/user/informe/nde/evolucion_medica/".concat(this.patient_data.episodio_id, "/").concat(id));
        return false;
      }

      if (['Récipe'].includes(name)) {
        this.printPdf(item);
        return false;
      }

      if (type === "one") {
        window.open("/user/reporte/".concat(description, "/").concat(this.patient_data.episodio_id, "/").concat(id, "/mono"));
      } else {
        window.open("/user/reporte/".concat(description, "/").concat(this.patient_data.episodio_id, "/all/mono"));
      }
    },
    noImprimir: function noImprimir(description) {
      if (["Fotografías", "Pendiente", "Probabilidad diagnóstica", "Plan", "Observación", "Laboratorio", "Imagenes", "Otro"].includes(description)) {
        return true;
      } else {
        return false;
      }
    },
    sendDataToParent: function sendDataToParent() {
      var that = this;
      this.buttonsState = false;
      this.updateParentData(this.buttonsState);
      var component = "app-create-item-file"; //aqui capturo si es un récipe y lo cambio a app-create-recipe

      if (that.currentItem === "Récipe") {
        component = "app-create-recipe";
      }

      this.currentComponent = {
        "extends": this.$options.components[component],
        props: {
          currentComponent: {
            type: Function,
            "default": that.currentComponent
          },
          updateComponentToReturn: {
            type: Function,
            "default": that.updateComponentToReturn
          },
          currentItem: {
            type: String,
            "default": that.currentItem
          },
          dataEpisodio: {
            type: Array,
            "default": function _default() {
              return that.dataEpisodio;
            }
          },
          patient_data: {
            type: Object,
            "default": that.patient_data
          }
        }
      };
    },
    updateFiltro: function updateFiltro(data) {
      //alert(data)
      this.tipo_filtro = data.tipo_filtro;
      this.filtro_valor = data.filtro_valor;
      this.showRow();
    },
    showRow: function showRow() {
      var _this27 = this;

      if (this.filtro_valor === null) {
        this.registros.map(function (x) {
          x.active = true;
        });
        return false;
      }

      if (this.tipo_filtro === "medico") {
        this.registros.map(function (x) {
          if (x.medico === _this27.filtro_valor) {
            x.active = true;
          } else {
            x.active = false;
          }

          return x;
        });
      }

      if (this.tipo_filtro === "fecha") {
        this.registros.map(function (x) {
          if (x.fechaItem === _this27.filtro_valor) {
            x.active = true;
          } else {
            x.active = false;
          }

          return x;
        });
      }
    },
    getAllByDay: function getAllByDay(fecha) {
      return this.registros.filter(function (x) {
        return x.fechaFormated === fecha;
      }).map(function (x) {
        return x.items;
      }).flat();
    }
  },
  mounted: function mounted() {
    var _this28 = this;

    return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee31() {
      return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee31$(_context31) {
        while (1) {
          switch (_context31.prev = _context31.next) {
            case 0:
              _this28.registros = _this28.dataEpisodio.map(function (x) {
                return x.items;
              }).flat();

            case 1:
            case "end":
              return _context31.stop();
          }
        }
      }, _callee31);
    }))();
  }
};
var AppTemplate2ForFiles = {
  name: "App",
  props: {
    updateParentData: Function,
    buttonsState: Boolean,
    updateComponentToReturn: Function,
    currentItem: String,
    patient_data: Object,
    dataEpisodio: Array,
    index: Number,
    currentComponent: Function
  },
  template:
  /*html */
  "\n        <div class=\"flex-fill d-flex flex-column overflow-auto\">\n            <ul class=\"nav bg-light justify-content-between nav-pills\" id=\"pills-tab\" role=\"tablist\">\n\n                <app-filter\n                    :tipo_filtro=\"tipo_filtro\"\n                    :filtro_valor=\"filtro_valor\"\n                    @update-filtro=\"updateFiltro\"\n                    :currentItem=\"currentItem\"\n                    :dataEpisodio=\"dataEpisodio\"\n                />\n                <li\n\n                    class=\"nav-item\" role=\"presentation\"\n                    @click=\"sendDataToParent\"\n                >\n                    <a\n\n                        class=\"nav-link currentItem\"\n                        id=\"pills-createItem-tab\"\n                        data-toggle=\"pill\"\n                        href=\"#pills-createItem\"\n                        role=\"tab\"\n                        aria-controls=\"pills-createItem\"\n                        aria-selected=\"false\"\n                    >\n                        <h3 class=\"mb-0\">Agregar {{currentItem}}</h3>\n                    </a>\n                </li>\n            </ul>\n            <div class=\"flex-fill tab-content d-flex flex-column overflow-auto\" id=\"pills-tabContent\">\n                <div\n                    class=\"tab-pane overflow-auto fade show active\"\n                    id=\"pills-listItems\"\n                    role=\"tabpanel\"\n                    aria-labelledby=\"pills-listItems-tab\"\n                >\n\n                    <div class=\"container-fluid\">\n                        <div class=\"row\">\n                            <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 p-1\">\n\n                                <div\n                                    v-if=\"dataEpisodio.length > 0\"\n                                    class=\"timeline timeline-inverse\"\n                                >\n                                    <!-- FECHA DEL DIA -->\n                                    <div v-for=\"(item,index) in dataEpisodio\" :key=\"index\">\n\n                                        <div\n                                            v-if=\"item.items.length !== item.items.filter(x=>!x.active).length\"\n                                            class=\"time-label\"\n\n                                        >\n                                            <span class=\"bg-primary\">\n                                                {{ item.fechaFormated }}\n                                            </span>\n                                        </div>\n\n                                        <div\n                                            v-for=\"(item2,index2) in item.items\"\n                                            :key=\"index2\"\n                                            v-show=\"item2.active\"\n                                        >\n\n                                            <i\n                                                :class=\"['timelime-medic-icon fas fa-user-md',{'bg-primary text-white':medicOrEnfermery(item2)==='medic'},{'bg-warning text-dark':medicOrEnfermery(item2)==='enfermery'}]\"\n                                                aria-hidden=\"true\">\n                                            </i>\n                                            <div class=\"timeline-item\">\n                                                <span class=\"time\">\n                                                    <i class=\"far fa-clock\"></i>\n                                                    {{ item2.fechaItem }}\n                                                    <span class=\"text-primary\">|</span>\n                                                    {{ item2.hora }}\n                                                    <a\n                                                        :class=\"['m-0 p-0',{'d-none':noImprimir(item2.description)}]\"\n                                                        type=\"button\"\n                                                        @click=\"imprimir(item2)\"\n                                                        :content=\"'Imprimir'\"\n\n                                                    >\n                                                        <i\n\n                                                            class=\"imprimir m-0 p-0 fas fa-print text-info heartbeat\"\n                                                            style=\"font-size: 1em;cursor:pointer; margin-left:1em;\"\n                                                            aria-hidden=\"true\">\n                                                        </i>\n                                                    </a>\n                                                </span>\n                                                <h3\n                                                    data-toggle=\"collapse\"\n                                                    :href=\"'#collapseExample'+index2\"\n                                                    role=\"button\"\n                                                    aria-expanded=\"false\"\n                                                    aria-controls=\"collapseExample\"\n                                                    class=\"timeline-header\"\n                                                >\n                                                    <b>{{ item2.description }}</b> |\n                                                    <b class=\"text-primary\">{{ item2.dataMedico !== undefined ? item2.dataMedico.prefijo : '' }} {{ item2.dataMedico !== undefined ? item2.dataMedico.medico : '' }}</b>\n                                                    <span\n                                                        :content=\"'Equipo '+item2.dataMedico_posicion.nombre\"\n\n                                                        :class=\"['badge  scale-in-hor-left',('badge-'+item2.dataMedico_posicion.color)]\"\n                                                        style=\"width:2em;\">\n                                                        {{ item2.dataMedico_posicion.siglas }}\n                                                    </span>\n\n\n                                                </h3>\n                                                <div\n                                                    :id=\"'collapseExample'+index2\"\n                                                    class=\"collapse show timeline-body\"\n                                                    v-html=\"item2.dato\"\n                                                >\n                                                </div>\n\n                                            </div>\n                                        </div>\n                                    </div>\n                                    <div>\n                                        <i class=\"far fa-clock bg-secondary rounded-circle text-white\"></i>\n                                    </div>\n                                </div>\n                                <div v-else class=\"timeline timeline-inverse\">\n                                    <div>\n                                        <div>\n                                            <div class=\"timeline-item\">\n                                                <div class=\"timeline-body text-secondary text-center\">\n                                                    No posee historia este d\xEDa\n                                                </div>\n                                            </div>\n                                        </div>\n                                    </div>\n                                    <div>\n                                        <i class=\"far fa-clock bg-gray\"></i>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n\n\n\n                </div>\n                <div class=\"tab-pane fade d-flex flex-fill flex-column\" id=\"pills-createItem\" role=\"tabpanel\" aria-labelledby=\"pills-profile-tab\">\n                    <component\n                        :is=\"currentComponent\"\n                        :currentItem=\"currentItem\"\n                    ></component>\n                </div>\n            </div>\n            <div class=\"d-none bg-danger p-4\"></div>\n        </div>\n\n\n\n    ",
  components: {
    /* "app-list-items-historia":AppListItemsHistoria(), */
    "app-create-item-file": AppCreateItemFile()
    /*  "app-filter":AppFilter(), */

    /* "app-row-timeline":AppRowTimeline() */

  },
  data: function data() {
    return {
      logoURL: '../image/system/logo-cmdlt-color.png',
      //logo CMDLT
      pdfData: {
        //datos del pdf
        pdfName: "recipe",
        created_at: null,
        //fecha de creacion
        medicExt: null,
        // extencion telefonica del medico
        medicName: null,
        // nombre del medico
        medicCI: null,
        // cedula del medico
        medicMPPS: null,
        // MPPS del medico
        medicSchoolNumber: null,
        // numero de colegio del medico
        medicSignatureURL: null,
        //firma
        medicStampURL: null,
        //sello
        medicEspecialities: null,
        //especialidades
        patientAge: null,
        //edad del paciente
        patientCI: null,
        //CI del paciente
        patientName: null,
        //nombre del paciente
        medications: [] //medicaciones

      },
      currentComponent: null,
      tipo_filtro: "",
      filtro_valor: "",
      registros: []
    };
  },
  methods: {
    //init pdf
    getFooterDirection: function getFooterDirection(pageSize) {
      return [{
        canvas: [{
          type: 'rect',
          x: 0,
          y: 60,
          w: pageSize.width,
          h: 40,
          color: '#1c71ce'
        }]
      }, {
        alignment: 'justify',
        fontSize: 5,
        absolutePosition: {
          x: 0,
          y: 53
        },
        padding: [0, 0, 0, 30],
        columns: ['leftSide', 'rightSide'].map(function (h) {
          return {
            lineHeight: 0.5,
            color: '#fff',
            margin: [10, 10, 0, 20],
            columns: [{
              text: ["\n                                Avenida intercomunal La Trinidad El Hatillo,\n \n                                Apartado postal 80474 Caracas 1080-A\n \n                                Tel\xE9fonos: (+58) 0212 -949.6411 (central)\n \n                                Fax: (+58) 0212 -945.6346\n \n                                Rif: J-00058551-2\n                            "],
              alignment: 'left'
            }, {
              text: ["www.cmdlt.edu.ve"],
              alignment: 'right',
              marginRight: 10
            }]
          };
        })
      }];
    },
    getImageBase64: function getImageBase64(imgURL) {
      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee32() {
        var response, blob, reader, logoDataURL;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee32$(_context32) {
          while (1) {
            switch (_context32.prev = _context32.next) {
              case 0:
                _context32.next = 2;
                return fetch(imgURL);

              case 2:
                response = _context32.sent;

                if (!(response.status !== 200)) {
                  _context32.next = 5;
                  break;
                }

                return _context32.abrupt("return", undefined);

              case 5:
                _context32.next = 7;
                return response.blob();

              case 7:
                blob = _context32.sent;

                if (!(blob.type !== 'image/png' && blob.type !== 'image/jpeg')) {
                  _context32.next = 11;
                  break;
                }

                console.error('El archivo no es un PNG o JPG');
                return _context32.abrupt("return", undefined);

              case 11:
                reader = new FileReader();
                _context32.next = 14;
                return new Promise(function (resolve, reject) {
                  reader.onload = function () {
                    return resolve(reader.result);
                  };

                  reader.onerror = function () {
                    return reject(reader.error);
                  };

                  reader.readAsDataURL(blob);
                });

              case 14:
                logoDataURL = _context32.sent;
                return _context32.abrupt("return", logoDataURL);

              case 16:
              case "end":
                return _context32.stop();
            }
          }
        }, _callee32);
      }))();
    },
    getSignatureAndStamp: function getSignatureAndStamp(xPosition) {
      var _this29 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee33() {
        var signatureBase64, stampBase64, signatureAndStamp;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee33$(_context33) {
          while (1) {
            switch (_context33.prev = _context33.next) {
              case 0:
                _this29.signature = '../' + _this29.pdfData.medicSignatureURL;
                _this29.stamp = '../' + _this29.pdfData.medicStampURL;

                if (!_this29.signature) {
                  _context33.next = 8;
                  break;
                }

                _context33.next = 5;
                return _this29.getImageBase64(_this29.signature);

              case 5:
                _context33.t0 = _context33.sent;
                _context33.next = 9;
                break;

              case 8:
                _context33.t0 = undefined;

              case 9:
                signatureBase64 = _context33.t0;

                if (!_this29.stamp) {
                  _context33.next = 18;
                  break;
                }

                _context33.next = 13;
                return _this29.getImageBase64(_this29.stamp);

              case 13:
                _context33.next = 15;
                return _context33.sent;

              case 15:
                _context33.t1 = _context33.sent;
                _context33.next = 19;
                break;

              case 18:
                _context33.t1 = undefined;

              case 19:
                stampBase64 = _context33.t1;
                signatureAndStamp = [];

                if (signatureBase64) {
                  signatureAndStamp.push({
                    image: signatureBase64,
                    fit: [100, 80],
                    absolutePosition: {
                      x: xPosition,
                      y: -25
                    }
                  });
                }

                if (stampBase64) {
                  signatureAndStamp.push({
                    image: stampBase64,
                    fit: [100, 80],
                    absolutePosition: {
                      x: xPosition,
                      y: 0
                    }
                  });
                }

                return _context33.abrupt("return", signatureAndStamp);

              case 24:
              case "end":
                return _context33.stop();
            }
          }
        }, _callee33);
      }))();
    },
    getRecipeIndications: function getRecipeIndications(counter, indications) {
      return {
        color: '#000',
        width: '48%',
        text: (counter && counter + '. ') + indications,
        fontSize: 10,
        margin: [20, 15, 30, 0]
      };
    },
    getMedicData: function getMedicData(recipe) {
      var _this30 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee34() {
        var getSignatureAndStampL, getSignatureAndStampR, createdUserL, createdUserR;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee34$(_context34) {
          while (1) {
            switch (_context34.prev = _context34.next) {
              case 0:
                _context34.next = 2;
                return _this30.getSignatureAndStamp(280);

              case 2:
                getSignatureAndStampL = _context34.sent;
                _context34.next = 5;
                return _this30.getSignatureAndStamp(700);

              case 5:
                getSignatureAndStampR = _context34.sent;
                createdUserL = _this30.getCreatedUser();
                createdUserR = _this30.getCreatedUser();
                return _context34.abrupt("return", [{
                  alignment: 'justify',
                  fontSize: 8,
                  absolutePosition: {
                    x: 0,
                    y: -7
                  },
                  padding: [0, 0, 0, 30],
                  columns: [{
                    lineHeight: 0.9,
                    color: '#000',
                    margin: [10, 10, 0, 20],
                    columns: [createdUserL, {
                      text: 'Firma y sello',
                      columns: getSignatureAndStampL
                    }]
                  }, {
                    lineHeight: 0.9,
                    color: '#000',
                    margin: [10, 10, 0, 20],
                    columns: [createdUserR, {
                      text: 'Firma y sello',
                      columns: getSignatureAndStampR
                    }]
                  }]
                }]);

              case 9:
              case "end":
                return _context34.stop();
            }
          }
        }, _callee34);
      }))();
    },
    getRecipeMedications: function getRecipeMedications(number, recipe) {
      return {
        margin: [0, 15, 0, 0],
        color: '#000',
        width: '48%',
        stack: [{
          text: number + '. ' + recipe.name
        }, {
          text: [{
            text: " "
          }, {
            text: "        ".concat(recipe.presentation ? recipe.presentation : '')
          }, {
            text: " "
          }, {
            text: "".concat(recipe.units ? recipe.units : '')
          }]
        }]
      };
    },
    getCreatedUser: function getCreatedUser() {
      var speciality = this.pdfData.medicEspecialities ? '/ ' + this.pdfData.medicEspecialities.map(function (us) {
        return us;
      }).join(', ') : '';
      return {
        text: 'Datos del médico',
        columns: [{
          stack: [{
            text: 'Datos del médico',
            bold: true
          }, {
            text: "".concat(this.pdfData.medicExt ? '(' + this.pdfData.medicExt + ') ' : '').concat(this.pdfData.medicName, " ").concat(speciality)
          }, {
            text: [{
              text: 'C.I.: '
            }, {
              text: this.pdfData.medicCI || ''
            }]
          }, {
            text: [{
              text: 'Colegio de Médicos: '
            }, {
              text: this.pdfData.medicSchoolNumber || ''
            }]
          }, {
            text: [{
              text: 'MPPS: '
            }, {
              text: this.pdfData.medicMPPS || ''
            }]
          }],
          unbreakable: true
        }]
      };
    },
    dateFormat: function dateFormat(dateStr) {
      var date = new Date(dateStr);
      var year = date.getFullYear();
      var month = String(date.getMonth() + 1).padStart(2, '0');
      var day = String(date.getDate()).padStart(2, '0');
      return "".concat(day, "/").concat(month, "/").concat(year);
    },
    printPdf: function printPdf(item) {
      var _this31 = this;

      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee35() {
        var logoBase64, footerDirection, medicData, docDefinition;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee35$(_context35) {
          while (1) {
            switch (_context35.prev = _context35.next) {
              case 0:
                _this31.pdfData.created_at = _this31.dateFormat(item.created_at);
                _this31.pdfData.patientAge = _this31.patient_data.edad;
                _this31.pdfData.patientCI = _this31.patient_data.cedula_formated;
                _this31.pdfData.patientName = _this31.patient_data.paciente;

                if (_this31.esArrayDeObjetos(item.dato)) {
                  _this31.pdfData.medications = JSON.parse(item.value);
                } else {
                  _this31.pdfData.medications = [{
                    presentation: '',
                    name: item.value,
                    indications: '',
                    unit: ''
                  }];
                }

                _this31.pdfData.medicExt = item.extencion_telefonica;
                _this31.pdfData.medicName = item.medico;
                _this31.pdfData.medicCI = item.medico_cedula;
                _this31.pdfData.medicMPPS = item.mpps;
                _this31.pdfData.medicSchoolNumber = item.colegio_medico;
                _this31.pdfData.medicSignatureURL = item.firma;
                _this31.pdfData.medicStampURL = item.sello;
                _this31.pdfData.medicEspecialities = item.medico_especialidades ? item.medico_especialidades.split(',') : [];
                console.log("3", _this31.pdfData); //Logo cmdlt

                _context35.next = 16;
                return _this31.getImageBase64(_this31.logoURL);

              case 16:
                logoBase64 = _context35.sent;
                //cintillo inferior del recipe
                footerDirection = _this31.getFooterDirection; //datos del medico / sello / firma

                _context35.next = 20;
                return _this31.getMedicData();

              case 20:
                medicData = _context35.sent;
                docDefinition = {
                  pageSize: 'A4',
                  pageOrientation: 'landscape',
                  pageMargins: [20, 120, 20, 100],
                  header: {
                    stack: [{
                      margin: [0, 10, 0, 0],
                      alignment: 'justify',
                      columns: [{
                        marginRight: 10,
                        image: logoBase64,
                        fit: [120, 80],
                        alignment: 'right'
                      }, {
                        marginRight: 10,
                        image: logoBase64,
                        fit: [120, 80],
                        alignment: 'right'
                      }]
                    }, {
                      alignment: 'justify',
                      columns: ['leftSide', 'rightSide'].map(function (h, index) {
                        return {
                          margin: [20, 10, 20, 10],
                          columns: [{
                            fontSize: 12,
                            text: h === 'leftSide' ? 'Récipe' : 'Indicaciones',
                            bold: true
                          }, {
                            margin: [0, 0, 3, 0],
                            alignment: 'right',
                            text: [{
                              text: 'Fecha: ',
                              bold: true
                            }, {
                              color: '#000',
                              text: _this31.pdfData.created_at ? _this31.pdfData.created_at : 'No disponible'
                            }]
                          }]
                        };
                      })
                    }, {
                      alignment: 'justify',
                      columns: ['leftSide', 'rightSide'].filter(Boolean).map(function (h) {
                        return {
                          margin: [20, 0, 10, 5],
                          text: [{
                            text: 'Nombre y apellido: ',
                            bold: true
                          }, {
                            color: '#000',
                            text: _this31.pdfData.patientName ? _this31.pdfData.patientName : 'No disponible'
                          }]
                        };
                      })
                    }, {
                      alignment: 'justify',
                      columns: ['leftSide', 'rightSide'].filter(Boolean).map(function (h) {
                        return {
                          margin: [20, 0, 20, 0],
                          text: [{
                            text: 'C.I: ',
                            bold: true
                          }, {
                            color: '#000',
                            text: _this31.pdfData.patientCI ? _this31.pdfData.patientCI : 'No disponible'
                          }, {
                            text: '    '
                          }, {
                            text: 'Edad: ',
                            bold: true
                          }, {
                            color: '#000',
                            text: _this31.pdfData.patientAge + ' año' + (_this31.pdfData.patientAge !== 1 ? 's' : '')
                          }]
                        };
                      })
                    }]
                  },
                  content: _this31.pdfData.medications.map(function (item, index) {
                    return {
                      margin: [0, 10, 0, 0],
                      alignment: 'justify',
                      unbreakable: true,
                      columns: [_this31.getRecipeMedications(index + 1, item), {
                        width: '4%',
                        text: ' '
                      }, _this31.getRecipeIndications(index + 1, item.indications)]
                    };
                  }),
                  footer: function footer(currentPage, pageCount, pageSize) {
                    return [].concat(_toConsumableArray(medicData), _toConsumableArray(footerDirection(pageSize)));
                  },
                  styles: {
                    header: {
                      fontSize: 12,
                      bold: true
                    }
                  },
                  defaultStyle: {
                    fontSize: 10,
                    color: '#1c71ce'
                  }
                }; //datos del paciente

                pdfMake.createPdf(docDefinition).download(_this31.pdfData.pdfName + '-' + (_this31.pdfData.patientName ? _this31.pdfData.patientName.replace(/ /g, "") : 'patientName') + '-' + (_this31.pdfData.created_at ? _this31.pdfData.created_at.split(' ')[0] : 'created_at') + '.pdf');

              case 23:
              case "end":
                return _context35.stop();
            }
          }
        }, _callee35);
      }))();
    },
    esArrayDeObjetos: function esArrayDeObjetos(str) {
      try {
        // Intentamos convertir el string en JSON
        var json = JSON.parse(str); // Verificamos que sea un array

        if (Array.isArray(json)) {
          // Verificamos que todos los elementos del array sean objetos
          return json.every(function (item) {
            return _typeof(item) === 'object' && item !== null;
          });
        }

        return false;
      } catch (e) {
        // Si ocurre un error al parsear, no es un JSON válido
        return false;
      }
    },
    //end pdf
    medicOrEnfermery: function medicOrEnfermery(item) {
      try {
        if ([1, 2, 3, 4, 5, 6, 7, 8, 9].includes(Number(item.dataMedico.posicion_id))) {
          return "medic";
        }

        if ([10].includes(Number(item.dataMedico.posicion_id))) {
          return "enfermery";
        }
      } catch (error) {
        console.log("data medico: ", item);
      }

      return "not-found";
    },
    noImprimir: function noImprimir(description) {
      if (["Fotografías", "Pendiente", "Probabilidad diagnóstica", "Plan", "Observación", "Laboratorio", "Imagenes", "Otro"].includes(description)) {
        return true;
      } else {
        return false;
      }
    },
    sendDataToParent: function sendDataToParent() {
      var that = this;
      this.buttonsState = false;
      this.updateParentData(this.buttonsState);
      this.currentComponent = {
        "extends": this.$options.components["app-create-item-file"],
        props: {
          currentComponent: {
            type: Function,
            "default": that.currentComponent
          },
          updateComponentToReturn: {
            type: Function,
            "default": that.updateComponentToReturn
          },
          currentItem: {
            type: String,
            "default": that.currentItem
          },
          dataEpisodio: {
            type: Array,
            "default": function _default() {
              return that.dataEpisodio;
            }
          },
          patient_data: {
            type: Object,
            "default": that.patient_data
          }
        }
      };
    },
    updateFiltro: function updateFiltro(data) {
      this.tipo_filtro = data.tipo_filtro;
      this.filtro_valor = data.filtro_valor;
      this.showRow();
    },
    showRow: function showRow() {
      var _this32 = this;

      if (this.filtro_valor === null) {
        this.registros.map(function (x) {
          x.active = true;
        });
        return false;
      }

      if (this.tipo_filtro === "medico") {
        this.registros.map(function (x) {
          if (x.medico === _this32.filtro_valor) {
            x.active = true;
          } else {
            x.active = false;
          }

          return x;
        });
      }

      if (this.tipo_filtro === "fecha") {
        this.registros.map(function (x) {
          if (x.fechaItem === _this32.filtro_valor) {
            x.active = true;
          } else {
            x.active = false;
          }

          return x;
        });
      }
    },
    getAllByDay: function getAllByDay(fecha) {
      return this.registros.filter(function (x) {
        return x.fechaFormated === fecha;
      }).map(function (x) {
        return x.items;
      }).flat();
    }
  },
  mounted: function mounted() {
    var _this33 = this;

    return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee36() {
      return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee36$(_context36) {
        while (1) {
          switch (_context36.prev = _context36.next) {
            case 0:
              _this33.registros = _this33.dataEpisodio.map(function (x) {
                return x.items;
              }).flat();

            case 1:
            case "end":
              return _context36.stop();
          }
        }
      }, _callee36);
    }))();
  }
};

var UserCuestHistoria = /*#__PURE__*/function () {
  function UserCuestHistoria() {
    _classCallCheck(this, UserCuestHistoria);
  }

  _createClass(UserCuestHistoria, [{
    key: "index",
    value: function index(patient_data, index_episodio) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"AppEvolucion\">Cargando...</div>\n        ");
      var app1 = new Vue({
        name: "AppHistoriaPaciente",
        el: '#AppEvolucion',
        data: function data() {
          return {
            currentIndex: localStorage.getItem("currentIndex") !== null ? localStorage.getItem("currentIndex") : 0,
            patient_data: patient_data,
            index: index_episodio,
            btnReingreso: false,
            counterDataEpisodio: [],
            buttonsState: true,
            currentComponent: null,
            dataEpisodio: [],
            currentItem: "Ver Todo",
            items: [{
              name: "Ver Todo",
              icon: null,
              cat_user_cuestionario_id: null,
              active: true,
              component: "app-list-all",
              totalItems: 0
            }, {
              name: "Laboratorio",
              icon: "pc-laboratorio1",
              cat_user_cuestionario_id: 90,
              active: false,
              component: "app-laboratorio",
              totalItems: 0
            }, {
              name: "Imagenes",
              icon: "pc-imagenes",
              cat_user_cuestionario_id: 100,
              active: false,
              component: "app-imagenes",
              totalItems: 0
            }, {
              name: "Fotografías",
              icon: "pc-fotografia",
              cat_user_cuestionario_id: 191,
              active: false,
              component: "app-fotografias",
              totalItems: 0
            }, {
              name: "Otros Archivos",
              icon: "pc-otros-archivos",
              cat_user_cuestionario_id: 122,
              active: false,
              component: "app-otros-archivos",
              totalItems: 0
            }, {
              name: "Probabilidad diagnóstica",
              icon: "pc-observacion",
              cat_user_cuestionario_id: 101,
              active: false,
              component: "app-probabilidad-diagnostica",
              totalItems: 0
            }, {
              name: "Evolución",
              icon: "pc-evolucion",
              cat_user_cuestionario_id: 175,
              active: false,
              component: "app-evolucion",
              totalItems: 0
            }, {
              name: "Orden Médica",
              icon: "pc-orden_medica",
              cat_user_cuestionario_id: 175,
              active: false,
              component: "app-orden-medica",
              totalItems: 0
            }, {
              name: "Plan",
              icon: "pc-plan",
              cat_user_cuestionario_id: 102,
              active: false,
              component: "app-plan",
              totalItems: 0
            }, {
              name: "Récipe",
              icon: "pc-recipe1",
              cat_user_cuestionario_id: 177,
              active: false,
              component: "app-recipe",
              totalItems: 0
            }, {
              name: "Observación",
              icon: "pc-buscar",
              cat_user_cuestionario_id: 176,
              active: false,
              component: "app-observacion",
              totalItems: 0
            }, {
              name: "Estudio Diagnóstico",
              icon: "pc-estudio-diagnostico",
              cat_user_cuestionario_id: 178,
              active: false,
              component: "app-estudio-diagnostico",
              totalItems: 0
            }
            /* {
                name:"Antecedentes/Comorbilidad de Egreso",
                icon:"pc-antecedentes ",
                cat_user_cuestionario_id:178,
                active:false,
                component:"app-antecedente-cormorbilidad",
                totalItems:0,
            }, */

            /* {
                name:"Antecedentes",
                icon:"pc-antecedentes",
                cat_user_cuestionario_id:189,
            }, */
            ]
          };
        },
        template:
        /*html */
        "\n                <div class=\"d-flex flex-column flex-fill overflow-auto\">\n                    <div id=\"infoPaciente\"></div>\n                    <div class=\"flex-fill d-flex flex-md-row flex-column overflow-auto\">\n                        <div class=\"flex-md-fill d-flex flex-column col-md-4 col-lg-3 col-xl-2 p-1 p-md-0  overflow-md-auto\">\n\n                            <ul id=\"menuEvolucion\" class=\"mt-1 pr-1 nav nav-pills flex-md-column flex-nowrap overflow-auto\" role=\"tablist\">\n\n                                <li\n                                    v-for=\"(item,index) in getItems\"\n                                    :key=\"index\"\n                                    class=\"nav-item\"\n                                    role=\"presentation\"\n                                >\n                                    <a\n                                        @click=\"handle_option(item,index)\"\n                                        :class=\"['nav-link d-flex align-items-center',{'active':Number(currentIndex)===Number(index)}]\"\n                                        :id=\"'pills-'+(index+1)+'-tab'\"\n                                        :href=\"'#pills-'+(index+1)\"\n                                        :aria-controls=\"'pills-'+(index+1)\"\n                                        role=\"tab\"\n                                        data-toggle=\"pill\"\n                                        aria-selected=\"true\"\n                                    >\n                                        <div class=\"d-flex flex-sm-row align-items-center mr-1\">\n                                            <i :class=\"['icon-option h3 mb-0 mr-1 fas fa-list',item.icon]\"></i>\n                                            <span class=\"text-option text-left text-custom-wrap\">{{ item.name }}</span>\n                                        </div>\n                                        <div :class=\"['ml-auto badge badge-counter',{'d-none':item.totalItems == 0 }]\">{{ (index===0) ? getItems.length : item.totalItems }}</div>\n                                    </a>\n                                </li>\n\n\n                            </ul>\n                        </div>\n                        <div class=\"bg-light rounded flex-fill d-flex flex-column overflow-auto rounded col-md-10 col-lg-10 col-xl-10 tab-content\" id=\"pills-tabContent\">\n\n                            <component\n                                :is=\"currentComponent\"\n                                :currentItem=\"currentItem\"\n                                :patient_data=\"patient_data\"\n                                :index=\"index\"\n                            ></component>\n\n                        </div>\n                    </div>\n                    <div v-if=\"buttonsState\" class=\"container-fluid mt-1\">\n                        <div class=\"row\">\n                            <div v-if=\"btnReingreso\" @click=\"handleBtnReingreso()\" class=\"btn btn-success btn-block mt-1\">Reingresar</div>\n                            <div v-if=\"!btnReingreso\" @click=\"handle_alta()\" class=\"button btn rounded-0 btn-success col\">Alta</div>\n                            <div v-if=\"!btnReingreso\" @click=\"handle_traslado()\" class=\"button btn rounded-0 btn-secondary col\">Traslado</div>\n                        </div>\n                    </div>\n                </div>\n            ",
        components: {
          'app-list-all': AppListAll(),
          'app-laboratorio': AppLaboratorio(),
          'app-imagenes': AppImagenes(),
          'app-fotografias': AppFotografias(),
          'app-otros-archivos': AppOtrosArchivos(),
          'app-probabilidad-diagnostica': AppProbabilidadDiagnostica(),
          'app-evolucion': AppEvolucion(),
          'app-orden-medica': AppOrdenMedica(),
          'app-plan': AppPlan(),
          'app-recipe': AppRecipe(),
          'app-observacion': AppObservacion(),
          'app-estudio-diagnostico': AppEstudioDiagnostico() //'app-antecedente-cormorbilidad':AppAntecedenteComorbilidad(),

        },
        computed: {
          getItems: function getItems() {
            var that = this;

            if (!['Antecedentes/Comorbilidad de Egreso'].includes(this.currentItem)) {
              return that.items.map(function (x) {
                x.totalItems = that.counterDataEpisodio.filter(function (z) {
                  if (z.hasOwnProperty('description')) {
                    var title = z.description;

                    if (title === "Otro") {
                      title = "Otros Archivos";
                    }

                    if (title === x.name) {
                      return z;
                    }
                  }
                }).length;
                return x;
              });
            } else {
              return [];
            }
          }
        },
        watch: {
          currentIndex: function currentIndex(newValue, oldValue) {
            this.currentIndex = newValue;
          }
        },
        methods: {
          showBtnReingreso: function showBtnReingreso() {
            if (location.pathname === "/medico/create_paciente" || localStorage.getItem('area') === "alta") {
              this.btnReingreso = true;
            }
          },
          handleBtnReingreso: function handleBtnReingreso() {
            _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_4__["default"].reingreso('.modal-body', this.patient_data.user_id);
          },
          handle_alta: function handle_alta() {
            _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_4__["default"].egreso('.modal-body', this.patient_data.user_id);
          },
          handle_traslado: function handle_traslado() {
            _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_4__["default"].create('.modal-body', this.patient_data.user_id);
          },
          meses: function meses(p) {
            var mes = ["Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio", "Julio", "Agosto", "Septiembre", "Octubre", "Noviembre", "Diciembre", "Enero"];
            return mes[p];
          },
          fechaCortaAPPLE: function fechaCortaAPPLE(param) {
            var hoy = param.split(" ");
            var fecha = hoy[0].split("-");
            var fullHora = hoy[1].split(":"); //2022-03-24 00:12:26

            var anio = fecha[0];
            var mes = parseInt(fecha[1]);
            var dia = fecha[2];
            var hora = fullHora[0];
            var minutos = fullHora[1];
            var segundos = fullHora[2];
            var horario = "AM";

            if (parseInt(hora) >= 12 && parseInt(hora) <= 23) {
              horario = "PM";
            }

            switch (hora) {
              case "13":
                hora = 1;
                break;

              case "14":
                hora = 2;
                break;

              case "15":
                hora = 3;
                break;

              case "16":
                hora = 4;
                break;

              case "17":
                hora = 5;
                break;

              case "18":
                hora = 6;
                break;

              case "19":
                hora = 7;
                break;

              case "20":
                hora = 8;
                break;

              case "21":
                hora = 9;
                break;

              case "22":
                hora = 10;
                break;

              case "23":
                hora = 11;
                break;

              case "00":
                hora = 12;
                break;
            }

            return dia + " de " + this.meses(mes - 1) + ", " + anio;
          },
          configData: function configData() {
            var _this34 = this;

            var tempFecha = [];
            this.dataEpisodio.forEach(function (element) {
              var f1 = element.created_at.split(" ")[0];
              tempFecha.push(f1);
            });
            var myData = [];
            Array.from(new Set(tempFecha)).forEach(function (item, index) {
              myData.push({
                fecha: item,
                fechaFormated: _this34.fechaCortaAPPLE(item + " 00:00:00"),

                /* items:[] */
                items: _this34.dataEpisodio.filter(function (element) {
                  var medico = medicos_datos.find(function (medico) {
                    return Number(medico.user_id) === element.user_id_medico;
                  }); //this.$store.state.medicos_datos.find( medico => medico.user_id === element.user_id_medico )

                  var dato = "";
                  var coments = "";
                  var valor = "";
                  var medico_posicion = {
                    "nombre": "Tratante",
                    "siglas": "TR",
                    "color": "success"
                  };

                  if (element.description == "Orden Médica" || element.description == "Evolución" || element.description == "Pendiente" || element.description == "Probabilidad diagnóstica" || element.description == "Plan" || element.description == "Observación" || element.description == "Récipe" || element.description == "Estudio Diagnóstico") {
                    if (element.value != null) {
                      valor = element.value.replace(/\n/g, "<br />"); //valor = valor.replace(/<[^>]*>/g, '');
                      // Remover estilos de Word

                      valor = valor.replace(/\s*style=("|\')[^"\']*("|\')/gi, '');
                    }

                    dato = "\n                                                        ".concat(valor, "\n                                                    ");
                  }

                  if (element.description == "Laboratorio" || element.description == "Imagenes" || element.description == "Fotografías" || element.description == "Otro") {
                    if (element.coments != null) {
                      coments = element.coments.replace(/\n/g, "<br />"); //coments = coments.replace(/<[^>]*>/g, '');
                      // Remover estilos de Word

                      coments = coments.replace(/\s*style=("|\')[^"\']*("|\')/gi, '');
                    }

                    var imageIcon = "";

                    if (element.description == "Laboratorio") {
                      imageIcon = "/image/system/icono_laboratorio.svg";
                    }

                    if (element.description == "Imagenes") {
                      imageIcon = "/".concat(element.value);
                    }

                    if (element.description == "Fotografías") {
                      imageIcon = "/".concat(element.value);
                    }

                    if (element.description == "Otro") {
                      imageIcon = "/image/system/icono_archivo.svg";
                    }

                    if (element.value != null) {
                      valor = "\n                                                            <img onclick=\"window.open('/".concat(element.value, "', '_blank')\" onerror=\"this.src='https://placehold.co/600x400'\" style=\"cursor:pointer;width: 50px;\" class=\"ampliar zoom img-fluid\" src='").concat(imageIcon, "'>\n                                                            <br>");
                    }

                    dato = "\n                                                        ".concat(valor, "\n                                                        ").concat(coments, "\n                                                    ");
                  }

                  if (!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_undefined"])(medico)) {
                    if ([2].includes(medico.posicion_id)) {
                      medico_posicion = {
                        "nombre": "Interconsultante",
                        "siglas": "IN",
                        "color": "info"
                      };
                    }

                    if ([3, 4].includes(medico.posicion_id)) {
                      medico_posicion = {
                        "nombre": "Fellow",
                        "siglas": "FE",
                        "color": "orange"
                      };
                    }

                    if ([5, 6, 7, 8].includes(medico.posicion_id)) {
                      medico_posicion = {
                        "nombre": "Residentes",
                        "siglas": "RE",
                        "color": "secondary"
                      };
                    }

                    if ([9].includes(medico.posicion_id)) {
                      medico_posicion = {
                        "nombre": "RAMH",
                        "siglas": "RA",
                        "color": "purple"
                      };
                    }

                    if ([10].includes(medico.posicion_id)) {
                      medico_posicion = {
                        "nombre": "Enfermería",
                        "siglas": "EN",
                        "color": "warning"
                      };
                    }
                    /* */

                  }

                  element.fechaItem = _this34.fechaCortaAPPLE(element.created_at);
                  element.dataMedico_posicion = medico_posicion;
                  element.dataMedico = medico;
                  element.dato = dato;
                  var f1 = element.created_at.split(" ")[0];

                  if (f1 === item) {
                    return element;
                  }
                })
              });
            });
            this.dataEpisodio = myData;
          },
          getHistoriaPaciente: function getHistoriaPaciente() {
            var _this35 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee37() {
              var formdata;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee37$(_context37) {
                while (1) {
                  switch (_context37.prev = _context37.next) {
                    case 0:
                      formdata = new FormData();
                      formdata.append("user_id", _this35.patient_data.user_id);
                      formdata.append("_token", $("#_token").val());
                      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])()); //formdata.append("created_at", "2021-04-1")

                      _context37.next = 6;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_historia_medica/getHistorial", formdata);

                    case 6:
                      _this35.dataEpisodio = _context37.sent;
                      _this35.dataEpisodio = _this35.dataEpisodio.map(function (x) {
                        x.active = true;
                        return x;
                      });
                      _this35.counterDataEpisodio = _this35.dataEpisodio;

                      _this35.configData();

                    case 10:
                    case "end":
                      return _context37.stop();
                  }
                }
              }, _callee37);
            }))();
          },
          getAntecedenteComorbilidad: function getAntecedenteComorbilidad() {
            var _this36 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee38() {
              var formdata;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee38$(_context38) {
                while (1) {
                  switch (_context38.prev = _context38.next) {
                    case 0:
                      formdata = new FormData();
                      formdata.append("user_id", _this36.patient_data.user_id);
                      formdata.append("_token", $("#_token").val());
                      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])()); //formdata.append("created_at", "2021-04-1")
                      //this.dataEpisodio = await post( location.origin+"/user_cuest_historia_medica/getHistorial", formdata)

                      _context38.next = 6;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_comorbilidad/show/" + user_id, formdata);

                    case 6:
                      _this36.dataEpisodio = _context38.sent;
                      _this36.dataEpisodio = _this36.dataEpisodio.map(function (x) {
                        x.active = true;
                        return x;
                      });

                      _this36.configData();

                    case 9:
                    case "end":
                      return _context38.stop();
                  }
                }
              }, _callee38);
            }))();
          },
          defaultComponent: function defaultComponent() {
            var _this37 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee39() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee39$(_context39) {
                while (1) {
                  switch (_context39.prev = _context39.next) {
                    case 0:
                      that = _this37;
                      _this37.buttonsState = true;
                      _context39.next = 4;
                      return _this37.getHistoriaPaciente();

                    case 4:
                      _this37.currentComponent = {
                        "extends": _this37.$options.components['app-list-all'],
                        props: {
                          currentItem: {
                            type: String,
                            "default": _this37.currentItem
                          },
                          patient_data: {
                            type: Object,
                            "default": pacientes_datos.find(function (x) {
                              return x.user_id === _this37.patient_data.user_id;
                            })
                          },
                          dataEpisodio: {
                            type: Array,
                            "default": function _default() {
                              return that.dataEpisodio;
                            }
                          },
                          buttonsState: {
                            type: Boolean,
                            "default": function _default() {
                              return that.buttonsState;
                            }
                          },
                          updateParentData: {
                            type: Function,
                            "default": that.updateParentData
                          }
                        }
                      };

                    case 5:
                    case "end":
                      return _context39.stop();
                  }
                }
              }, _callee39);
            }))();
          },
          updateParentData: function updateParentData(newData) {
            this.buttonsState = newData;
          },
          openDinamicComponent: function openDinamicComponent(item, index) {
            var _this38 = this;

            var that = this; //filtro todos los registros por el nombre a mostrar actual

            that.dataEpisodio = that.dataEpisodio.map(function (x) {
              var title = that.currentItem;

              if (title === "Otros Archivos") {
                title = "Otro";
              }

              x.items = x.items.filter(function (x) {
                return x.description === title;
              });
              return x;
            }); //cargo el componente actual

            this.currentComponent = {
              "extends": this.$options.components[this.items[index].component],
              props: {
                //guarda el componente a cargarse dinamicamente
                currentComponent: {
                  type: Function,
                  "default": this.currentComponent
                },
                //guarda el nombre actual del componente
                currentItem: {
                  type: String,
                  "default": this.currentItem
                },
                //contiene toda la data del episodio del paciente
                patient_data: {
                  type: Object,
                  "default": pacientes_datos.find(function (x) {
                    return x.user_id === _this38.patient_data.user_id;
                  })
                },
                dataEpisodio: {
                  type: Array,
                  "default": function _default() {
                    return that.dataEpisodio;
                  }
                },
                //boton para actualizar la visibilidad de los botones inferiores de la modal
                buttonsState: {
                  type: Boolean,
                  "default": function _default() {
                    return that.buttonsState;
                  }
                },
                updateComponentToReturn: {
                  type: Function,
                  "default": function () {
                    var _default2 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee40() {
                      return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee40$(_context40) {
                        while (1) {
                          switch (_context40.prev = _context40.next) {
                            case 0:
                              that.buttonsState = true;
                              _context40.next = 3;
                              return that.getHistoriaPaciente();

                            case 3:
                              that.dataEpisodio = that.dataEpisodio.map(function (x) {
                                var title = that.currentItem;

                                if (title === "Otros Archivos") {
                                  title = "Otro";
                                }

                                x.items = x.items.filter(function (x) {
                                  return x.description === title;
                                });
                                return x;
                              });
                              that.currentComponent = {
                                "extends": that.$options.components['app-list-all'],
                                props: {
                                  currentItem: {
                                    type: String,
                                    "default": that.currentItem
                                  },
                                  patient_data: {
                                    type: Object,
                                    "default": pacientes_datos.find(function (x) {
                                      return x.user_id === _this38.patient_data.user_id;
                                    })
                                  },
                                  dataEpisodio: {
                                    type: Array,
                                    "default": function _default() {
                                      return that.dataEpisodio;
                                    }
                                  },
                                  buttonsState: {
                                    type: Boolean,
                                    "default": function _default() {
                                      return that.buttonsState;
                                    }
                                  },
                                  updateParentData: {
                                    type: Function,
                                    "default": that.updateParentData
                                  }
                                }
                              };

                            case 5:
                            case "end":
                              return _context40.stop();
                          }
                        }
                      }, _callee40);
                    }));

                    function _default() {
                      return _default2.apply(this, arguments);
                    }

                    return _default;
                  }()
                },
                updateParentData: {
                  type: Function,
                  "default": that.updateParentData
                }
              }
            };
          },
          handle_option: function handle_option(item, index) {
            var _this39 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee41() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee41$(_context41) {
                while (1) {
                  switch (_context41.prev = _context41.next) {
                    case 0:
                      localStorage.setItem("currentIndex", index);
                      that = _this39; //Habilito los botones inferiores de la modal

                      _this39.buttonsState = true; //consulto la historia del paciente

                      _context41.next = 5;
                      return _this39.getHistoriaPaciente();

                    case 5:
                      //await this.getAntecedenteComorbilidad()
                      //asigno el nombre del item actual a mostrar
                      that.currentItem = item.name; //actualizo la visibilidad de todos los registros de la historia

                      _this39.dataEpisodio = _this39.dataEpisodio.map(function (x) {
                        x.items.map(function (x) {
                          x.active = true;
                          return x;
                        });
                        return x;
                      }); //si el item pulsado es Ver todo

                      if (!(Number(index) === 0)) {
                        _context41.next = 10;
                        break;
                      }

                      _this39.defaultComponent();

                      return _context41.abrupt("return", false);

                    case 10:
                      //alert(this.items[index].component)
                      that.openDinamicComponent(item, index);

                    case 11:
                    case "end":
                      return _context41.stop();
                  }
                }
              }, _callee41);
            }))();
          }
        },
        mounted: function mounted() {
          var _this40 = this;

          return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee42() {
            var item;
            return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee42$(_context42) {
              while (1) {
                switch (_context42.prev = _context42.next) {
                  case 0:
                    _this40.showBtnReingreso();

                    userCuestPaciente.infoPaciente("#infoPaciente", _this40.patient_data.user_id);
                    item = _this40.getItems[_this40.currentIndex]; //this.defaultComponent()

                    _this40.handle_option(item, _this40.currentIndex);

                  case 4:
                  case "end":
                    return _context42.stop();
                }
              }
            }, _callee42);
          }))();
        }
      });
    }
  }, {
    key: "indexHistoria_datos_pacientes",
    value: function indexHistoria_datos_pacientes($node, user_id) {
      var model = pacientes_datos.filter(function (x) {
        return x.user_id == user_id;
      })[0];
      var f = null;
      var ingreso;

      if (model.hasOwnProperty('ingreso_episodio')) {
        f = new Date(model.ingreso_episodio);
        ingreso = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["meses"])(f.getMonth()) + ", " + f.getFullYear();
      }

      var $nodo = $node.querySelectorAll('.col-md-4 .card')[0]; //imagen

      $nodo.querySelector('.widget-user-image img').src = model.imagen; //nombre

      $nodo.querySelector('.widget-user-username').textContent = model.paciente; //cedula

      $nodo.querySelector('.widget-user-desc').textContent = model.cedula;
      var $nodo_item = $nodo.querySelectorAll('span'); //edad

      $nodo_item[0].textContent = model.edad; //SEXO

      $nodo_item[1].textContent = model.sexo.toUpperCase(); //telefono

      $nodo_item[2].innerHTML = "<i class=\"fab fa-whatsapp text-success\"></i> ".concat(model.telefono_movil); //numero de historia

      $nodo_item[3].textContent = "# ".concat(model.total_episodios + 1); //fecha de historia

      $nodo_item[4].textContent = ingreso;
    }
  }, {
    key: "historiaIngreso",
    value: function historiaIngreso($node, user_id) {
      var model = pacientes_datos.filter(function (x) {
        return x.user_id == user_id;
      })[0];
      var f = null;
      var ingreso;

      if (model.hasOwnProperty('ingreso_episodio')) {
        f = new Date(model.ingreso_episodio);
        ingreso = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["meses"])(f.getMonth()) + ", " + f.getFullYear();
      }

      var $nodo = $node.querySelectorAll('.col-md-4 .card')[0]; //imagen

      $nodo.querySelector('.widget-user-image img').src = model.imagen; //nombre

      $nodo.querySelector('.widget-user-username').textContent = model.paciente; //cedula

      $nodo.querySelector('.widget-user-desc').textContent = model.cedula;
      var $nodo_item = $nodo.querySelectorAll('span'); //edad

      $nodo_item[0].textContent = model.edad; //SEXO

      $nodo_item[1].textContent = model.sexo.toUpperCase(); //telefono

      $nodo_item[2].innerHTML = "<i class=\"fab fa-whatsapp text-success\"></i> ".concat(model.telefono_movil); //numero de historia

      $nodo_item[3].textContent = "# ".concat(model.total_episodios + 1); //fecha de historia

      $nodo_item[4].textContent = ingreso;
    }
  }, {
    key: "indexHistoria_exp_fisica",
    value: function indexHistoria_exp_fisica($node, user_id) {
      var $nodo = $node.querySelectorAll('.col-md-4 .card')[1];
      console.log($nodo);
    }
  }, {
    key: "indexHistoria_comorbilidad",
    value: function () {
      var _indexHistoria_comorbilidad = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee44($node, user_id, data) {
        var $nodo, $cardBody, dp, $form;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee44$(_context44) {
          while (1) {
            switch (_context44.prev = _context44.next) {
              case 0:
                $nodo = $node.querySelectorAll('.col-md-4 .card')[2];
                $cardBody = $nodo.querySelector('.card-body');
                dp = [{
                  text: "aaa"
                }, {
                  text: "bbb"
                }];
                $cardBody.innerHTML = "";
                $cardBody.appendChild(newTable({
                  tbody: dp
                }));
                $form = $cardBody.querySelector("form");
                $form.querySelector("button").addEventListener("click", /*#__PURE__*/function () {
                  var _ref5 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee43(e) {
                    var input, tbody, $table;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee43$(_context43) {
                      while (1) {
                        switch (_context43.prev = _context43.next) {
                          case 0:
                            e.preventDefault();
                            input = $form.querySelector("input");

                            if (input.value === "") {
                              alert("El el campo está vacio");
                              input.focus();
                            } else {
                              dp.push({
                                "text": input.value
                              });
                              input.value = "";
                              tbody = dp;
                              $table = $cardBody.querySelector("tbody");
                              cl($table); //$table.querySelector("tbody").innerHTML = ""

                              $table.append(newTableRow(tbody));
                            }

                          case 3:
                          case "end":
                            return _context43.stop();
                        }
                      }
                    }, _callee43);
                  }));

                  return function (_x8) {
                    return _ref5.apply(this, arguments);
                  };
                }()); // console.log($botones)

              case 7:
              case "end":
                return _context44.stop();
            }
          }
        }, _callee44);
      }));

      function indexHistoria_comorbilidad(_x5, _x6, _x7) {
        return _indexHistoria_comorbilidad.apply(this, arguments);
      }

      return indexHistoria_comorbilidad;
    }()
  }, {
    key: "indexHistoria",
    value: function () {
      var _indexHistoria = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee47(data) {
        var _data, user_id, episodio, episodio_id, d, $modal, ingreso, _ingreso$episodio$SIG, FC, FR, GLIC, OXI, TA, TEMP, PESO, TALLA, useState, $cabecera, $signos, $btn_guardar, $evaluacion, $datosingreso, modalBody;

        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee47$(_context47) {
          while (1) {
            switch (_context47.prev = _context47.next) {
              case 0:
                $("#modal-patient-item").modal("show"); //cl(data)

                data = pacientes_datos.find(function (x) {
                  return equalsInt(x['user_id'], data.user_id);
                });
                _data = data, user_id = _data.user_id, episodio = _data.episodio;
                episodio_id = episodio;
                d = document;
                $modal = byId('modal-patient-item');
                _context47.next = 8;
                return UserCuestEpisodio.get_historiaIngreso(user_id);

              case 8:
                ingreso = _context47.sent;
                cl(ingreso);
                _ingreso$episodio$SIG = ingreso['episodio']['SIGNOS'], FC = _ingreso$episodio$SIG.FC, FR = _ingreso$episodio$SIG.FR, GLIC = _ingreso$episodio$SIG.GLIC, OXI = _ingreso$episodio$SIG.OXI, TA = _ingreso$episodio$SIG.TA, TEMP = _ingreso$episodio$SIG.TEMP, PESO = _ingreso$episodio$SIG.PESO, TALLA = _ingreso$episodio$SIG.TALLA;
                useState = {};
                $cabecera = document.getElementById("historia_inicial").content;
                $cabecera = document.importNode($cabecera, true);
                $signos = $cabecera.querySelectorAll("#signosVitales input");
                $btn_guardar = $cabecera.querySelector("button#guardarHistoriaInicial");
                $btn_guardar.addEventListener("click", function (e) {
                  Toast.fire({
                    icon: "info",
                    title: "¿Desea guardar la historia preliminar de emergencia?",
                    text: "Esta acción quedará firmada y sellada por el médico.",
                    showDenyButton: true,
                    showCancelButton: false,
                    confirmButtonText: "Si"
                    /* didClose: () => {
                        setTimeout(() => { email.val('');email.trigger("focus");}, 110);
                    } */

                  }).then( /*#__PURE__*/function () {
                    var _ref6 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee45(result) {
                      var formdata, firma_sello;
                      return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee45$(_context45) {
                        while (1) {
                          switch (_context45.prev = _context45.next) {
                            case 0:
                              console.log(result);
                              formdata = new FormData();
                              formdata.append("_token", $("#_token").val());
                              formdata.append("episodio_id", episodio_id);
                              formdata.append("user_id_paciente", user_id);
                              _context45.next = 7;
                              return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])("/user/tipo_documento/store", formdata);

                            case 7:
                              firma_sello = _context45.sent;
                              console.log(firma_sello);
                              Swal.fire('Envío exitoso.', "\n                                Se ha enviado este informe a las direcciones:\n                                <ul class='text-left'>\n                                    <li class='text-left'>luisa.lira@cmdlt.edu.ve</li>\n                                    <li class='text-left'>cmdlt.pe@gmail.com</li>\n                                    <li class='text-left'>admision.seguros@cmdlt.edu.ve</li>\n                                    <li class='text-left'>emergencia.seguros@cmdlt.edu.ve</li>\n                                </ul>\n                            ", 'success').then(function (result) {
                                $("#modelId").modal("hide");
                              });
                              return _context45.abrupt("return", false);

                            case 11:
                            case "end":
                              return _context45.stop();
                          }
                        }
                      }, _callee45);
                    }));

                    return function (_x10) {
                      return _ref6.apply(this, arguments);
                    };
                  }());
                });
                $signos[0].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(PESO) ? PESO.value : '';
                $signos[1].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(TALLA) ? TALLA.value : '';
                $signos[2].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(TEMP) ? TEMP.value : '';
                $signos[3].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(OXI) ? OXI.value : '';
                $signos[4].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(FC) ? FC.value : '';
                $signos[5].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(FR) ? FR.value : '';
                $signos[6].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(GLIC) ? GLIC.value : '';
                $signos[7].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(TA) ? TA.value : '';
                $signos[8].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(TA) ? TA.value2 : ''; //console.log($signos)

                $evaluacion = $cabecera.querySelectorAll("#evaluacionIngreso select"); //e.target.options[ e.target.selectedIndex ].value

                $evaluacion[0].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['ATENCION_EMERGENCIA']) ? ingreso['episodio']['ATENCION_EMERGENCIA'] : "";
                $evaluacion[0].dataset.episodio_id = episodio_id;
                $evaluacion[1].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['HOSPITALIZACION']) ? ingreso['episodio']['HOSPITALIZACION'] : "";
                $evaluacion[1].dataset.episodio_id = episodio_id;
                $evaluacion[2].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['TERAPIA_INTENSIVA']) ? ingreso['episodio']['TERAPIA_INTENSIVA'] : "";
                $evaluacion[2].dataset.episodio_id = episodio_id;
                $evaluacion[3].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['CASO_TIPO_MEDICO']) ? ingreso['episodio']['CASO_TIPO_MEDICO'] : "";
                $evaluacion[3].dataset.episodio_id = episodio_id;
                $evaluacion[4].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['NIVEL_TRIAJE']) ? ingreso['episodio']['NIVEL_TRIAJE'] : "";
                $evaluacion[4].dataset.episodio_id = episodio_id; //console.log($signos)

                $datosingreso = $cabecera.querySelectorAll("#datos_ingreso textarea");
                $datosingreso[0].dataset.episodio_id = episodio_id;
                $datosingreso[0].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['MOTIVO_CONSULTA']) ? ingreso['episodio']['MOTIVO_CONSULTA'] : "";
                $datosingreso[1].dataset.episodio_id = episodio_id;
                $datosingreso[1].dataset.user_id_paciente = user_id;
                $datosingreso[1].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['ANTECEDENTES']) ? ingreso['episodio']['ANTECEDENTES'] : "";
                $datosingreso[2].dataset.episodio_id = episodio_id;
                $datosingreso[2].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['ENFERMEDAD_ACTUAL']) ? ingreso['episodio']['ENFERMEDAD_ACTUAL'] : "";
                $datosingreso[3].dataset.episodio_id = episodio_id;
                $datosingreso[3].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['EXAMEN_FISICO']) ? ingreso['episodio']['EXAMEN_FISICO'] : "";
                $datosingreso[4].dataset.episodio_id = episodio_id;
                $datosingreso[4].dataset.user_id_paciente = user_id;
                $datosingreso[4].value = !Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(ingreso['episodio']['IMP_DIAG']) ? ingreso['episodio']['IMP_DIAG'].value : "";
                modalBody = document.querySelector("#modal-patient-item .modal-body-2");
                /*  let modalFooter = document.querySelector("#modelId div.modal-footer")
                     modalFooter.classList.add("d-none") */

                modalBody.innerHTML = "";
                modalBody.appendChild($cabecera);
                _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_3__["default"].infoPaciente("#infoPaciente", user_id);
                /*
                    modalFooter.innerHTML=`
                        <div class="container-fluid">
                            <div class="row justify-content-center">
                                <div class="col-md-8">
                                    <button type="button" class="btn btn-success btn-block mb-1" data-dismiss="modal">Guardar</button>
                                </div>
                                <div class="col-md-8">
                                    <button type="button" class="btn btn-primary btn-block" data-dismiss="modal">Cerrar</button>
                                </div>
                            </div>
                        </div>
                    `; */

                /*       $('.textarea').summernote({
                    placeholder: 'Escribe el texto aquí.',
                    toolbar: [
                      // [groupName, [list of button]]
                      ['style', ['bold', 'italic', 'underline']],
                      ['fontsize', ['fontsize']],
                      ['para', ['ul', 'ol', 'paragraph']],
                      ['table', ['table']],
                      ['view', ['fullscreen']],
                    ]
                  })   */
                //UserCuestHistoria.munecoAcciones()

                document.querySelector('#datos_ingreso').addEventListener("change", function (e) {
                  if (e.target.matches("textarea[name='user_motivo_consulta']")) {
                    UserProblemaSalud.store(user_id, {
                      "value": e.target.value,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }

                  if (e.target.matches("textarea[name='user_enfermedad_actual']")) {
                    UserEnfermedadActual.store(user_id, {
                      "value": e.target.value,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }

                  if (e.target.matches("textarea[name='user_examen_fisico']")) {
                    UserExamenFisico.store(user_id, {
                      "value": e.target.value,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }

                  if (e.target.matches("textarea[name='user_diagnostico_presuntivo']")) {
                    UserCuestImpresionDiagnostica.store3(user_id, {
                      "value": e.target.value,
                      "user_id_paciente": e.target.dataset.user_id_paciente,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }

                  if (e.target.matches("textarea[name='user_antecedentes']")) {
                    console.log(e.target.value);
                    UserCuestAntecedente.store(user_id, {
                      "value": e.target.value,
                      "user_id_paciente": e.target.dataset.user_id_paciente,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }
                });
                document.querySelector('#signosVitales').addEventListener("change", /*#__PURE__*/function () {
                  var _ref7 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee46(e) {
                    var value1, formdata, result, _value, _formdata, _result;

                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee46$(_context46) {
                      while (1) {
                        switch (_context46.prev = _context46.next) {
                          case 0:
                            if (!e.target.matches("input[name='user_peso']")) {
                              _context46.next = 13;
                              break;
                            }

                            value1 = e.target;

                            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_empty"])(value1.value)) {
                              _context46.next = 13;
                              break;
                            }

                            formdata = new FormData();
                            formdata.append("episodio_id", episodio_id);
                            formdata.append("value", value1.value);
                            formdata.append("user_id_paciente", user_id);
                            formdata.append("sintoma_observacion", "");
                            formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                            formdata.append("_token", $("#_token").val());
                            _context46.next = 12;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_peso/store2", formdata);

                          case 12:
                            result = _context46.sent;

                          case 13:
                            if (!e.target.matches("input[name='user_talla']")) {
                              _context46.next = 26;
                              break;
                            }

                            _value = e.target;

                            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_empty"])(_value.value)) {
                              _context46.next = 26;
                              break;
                            }

                            _formdata = new FormData();

                            _formdata.append("episodio_id", episodio_id);

                            _formdata.append("value", _value.value);

                            _formdata.append("user_id_paciente", user_id);

                            _formdata.append("sintoma_observacion", "");

                            _formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());

                            _formdata.append("_token", $("#_token").val());

                            _context46.next = 25;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_talla/store", _formdata);

                          case 25:
                            _result = _context46.sent;

                          case 26:
                            if (e.target.matches("input[name='user_temperatura']")) {
                              //alert(e.target.value)
                              UserCuestTemperatura.store3(user_id, e.target.value);
                            }

                            if (e.target.matches("input[name='user_oximetria']")) {
                              //alert(e.target.value)
                              UserCuestOximetria.store3(user_id, e.target.value);
                            }

                            if (e.target.matches("input[name='user_fc']")) {
                              //alert(e.target.value)
                              UserCuestFc.store3(user_id, e.target.value);
                            }

                            if (e.target.matches("input[name='user_fr']")) {
                              //alert(e.target.value)
                              UserCuestFr.store3(user_id, e.target.value);
                            }

                            if (e.target.matches("input[name='user_ta_sis']")) {
                              UserCuestTa.store3(user_id, e.target.value, d.querySelector("input[name='user_ta_dia']").value);
                            }

                            if (e.target.matches("input[name='user_ta_dia']")) {
                              UserCuestTa.store3(user_id, d.querySelector("input[name='user_ta_sis']").value, e.target.value);
                            }

                            if (e.target.matches("input[name='user_glic']")) {
                              //alert(e.target.value)
                              UserCuestGl.store3(user_id, e.target.value);
                            }

                          case 33:
                          case "end":
                            return _context46.stop();
                        }
                      }
                    }, _callee46);
                  }));

                  return function (_x11) {
                    return _ref7.apply(this, arguments);
                  };
                }());
                document.querySelector('#evaluacionIngreso').addEventListener("change", function (e) {
                  //console.log(e.target.name)
                  //console.log(e.target.options[ e.target.selectedIndex ].value)
                  if (e.target.matches("select[name='atencion_emergencia']")) {
                    UserCuestEpisodio.store3(user_id, {
                      "name": e.target.name,
                      "value": e.target.options[e.target.selectedIndex].value,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }

                  if (e.target.matches("select[name='hospitalizacion']")) {
                    UserCuestEpisodio.store3(user_id, {
                      "name": e.target.name,
                      "value": e.target.options[e.target.selectedIndex].value,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }

                  if (e.target.matches("select[name='terapia_intensiva']")) {
                    UserCuestEpisodio.store3(user_id, {
                      "name": e.target.name,
                      "value": e.target.options[e.target.selectedIndex].value,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }

                  if (e.target.matches("select[name='caso_tipo_medico']")) {
                    UserCuestEpisodio.store3(user_id, {
                      "name": e.target.name,
                      "value": e.target.options[e.target.selectedIndex].value,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }

                  if (e.target.matches("select[name='nivel_triaje']")) {
                    UserCuestEpisodio.store3(user_id, {
                      "name": e.target.name,
                      "value": e.target.options[e.target.selectedIndex].value,
                      "episodio_id": e.target.dataset.episodio_id
                    });
                  }
                });
                /* document.getElementById('model_historia_store').addEventListener("click", function(e){
                    alert("aaaa")
                    //UserCuestHistoria.create({user_id})
                }) */

              case 57:
              case "end":
                return _context47.stop();
            }
          }
        }, _callee47);
      }));

      function indexHistoria(_x9) {
        return _indexHistoria.apply(this, arguments);
      }

      return indexHistoria;
    }()
  }, {
    key: "indexHistoriaTriaje",
    value: function () {
      var _indexHistoriaTriaje = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee50(episodio_id) {
        var d, selector, App, colorValidationsTemperatura, app;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee50$(_context50) {
          while (1) {
            switch (_context50.prev = _context50.next) {
              case 0:
                $("#modal-patient-item").modal("show");
                d = document;
                selector = d.querySelector("#modal-patient-item .modal-body-2");
                selector.innerHTML = null;
                App = d.createElement('div');
                App.setAttribute("id", "App");
                App.classList.add("d-flex", "flex-column", "h-100");
                selector.insertAdjacentElement("afterBegin", App);

                colorValidationsTemperatura = function colorValidationsTemperatura(value) {
                  if (value <= 30.4) {
                    return "text-danger";
                  } else if (value >= 30.5 && value <= 37.5) {
                    return "text-success";
                  } else if (value >= 37.6 && value <= 37.9) {
                    return "text-warning";
                  } else if (value >= 38) {
                    return "text-danger";
                  } else {}
                };

                App.innerHTML =
                /*html */
                "\n                <div class=\"container flex-fill d-flex\">\n                    <div class=\"col-12 col-xl-4\">\n                       <!--  <div class=\"d-flex flex-wrap\">\n                            <div class=\"col-12 p-0 rounded d-flex border\">\n                            <div class=\"w-custom-1 btn btn-outline-gray border-0\">\n                                PESO\n                            </div>\n                            <input id=\"peso_19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" class=\"sv-peso w-100 text-center input-full-height border-0\">\n                            <div class=\"actual_peso_value_19404 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center text-secondary\">\n                                <span class=\"text-secondary\">11 Kg</span>\n                            </div>\n                        </div>\n                        <div class=\"col-12 p-0 rounded d-flex border\">\n                            <div class=\"w-custom-1 btn btn-outline-gray border-0\">\n                                TALLA\n                            </div>\n                            <input id=\"talla_19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" class=\"sv-talla w-100 text-center input-full-height border-0\">\n                            <div class=\"actual_talla_value_19404 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center text-secondary\">\n                                <span class=\"text-secondary\">10 cm</span>\n                            </div>\n                        </div>\n\n\n                        <div class=\"col-12 p-0 rounded d-flex border\">\n                            <div onclick=\"UserCuestTemperatura.index('.modal-body',19404,27625)\" class=\"w-custom-1 btn btn-outline-gray border-0\">\n                                TEMP\n                            </div>\n                            <input id=\"dropdown_pac_temp_19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" class=\"sv-temp w-100 text-center input-full-height border-0\">\n                            <div onclick=\"UserCuestTemperatura.index('.modal-body',19404,27625)\" class=\"actual_temp_value_19404 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\">\n                                <span class=\"text-cyan\">9\xB0C</span>\n                            </div>\n                        </div>\n\n\n                        <div class=\"col-12 p-0 rounded d-flex border\">\n                            <div onclick=\"UserCuestOximetria.index('.modal-body',19404,27625)\" class=\"w-custom-1 btn btn-outline-gray border-0\">\n                                OXI\n                            </div>\n                            <input id=\"dropdown_pac_oxi_19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" class=\"sv-oxi w-100 text-center input-full-height border-0\">\n                            <div onclick=\"UserCuestOximetria.index('.modal-body',19404,27625)\" class=\"actual_oxi_value_19404 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\">\n                                <span class=\"text-danger\">8%</span>\n                            </div>\n                        </div>\n\n\n                        <div class=\"col-12 p-0 rounded d-flex border\">\n                            <div onclick=\"UserCuestFc.index('.modal-body',19404,27625)\" class=\"w-custom-1 btn btn-outline-gray border-0\">FC</div>\n                            <input id=\"dropdown_pac_fc_19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" class=\"sv-fc w-100 text-center input-full-height border-0\">\n                            <div onclick=\"UserCuestFc.index('.modal-body',19404,27625)\" class=\"actual_fc_value_19404 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\">\n                                <span class=\"text-danger\">7 lat/min</span>\n                            </div>\n                        </div>\n\n\n                        <div class=\"col-12 p-0 rounded d-flex border\">\n                            <button onclick=\"UserCuestFr.index('.modal-body',19404,27625)\" class=\"w-custom-1 btn btn-outline-gray border-0\">FR</button>\n                            <input id=\"dropdown_pac_fr_19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" class=\"sv-fr w-100 text-center input-full-height border-0\">\n                            <div onclick=\"UserCuestFr.index('.modal-body',19404,27625)\" class=\"actual_fr_value_19404 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\">\n                                <span class=\"text-danger text-nowrap\">6 resp/min</span>\n                            </div>\n                        </div>\n\n\n                        <div class=\"col-12 p-0 rounded d-flex border\">\n                            <div onclick=\"UserCuestGl.index('.modal-body',19404,27625)\" class=\"w-custom-1 btn btn-outline-gray border-0\">\n                                GL\n                            </div>\n                            <div class=\"w-100 flex-lg-fill d-flex flex-column flex-lg-row bg-warning\">\n\n                                <input id=\"dropdown_pac_gl_19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" placeholder=\"mg/dl\" class=\"sv-gl w-100 text-center input-full-height border-0\">\n                                <input id=\"dropdown_pac_gl_ui_19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" placeholder=\"Insulina\" class=\"sv-gl w-100 text-center input-full-height border-0\">\n\n                            </div>\n                            <div onclick=\"UserCuestGl.index('.modal-body',19404,27625)\" class=\"w-100 d-flex\">\n                                <div class=\"actual_gl_value_19404 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\">\n                                    <span class=\"text-danger text-nowrap\">5 mg/dl</span>\n                                </div>\n                                <div class=\"actual_gl_value_ui_19404 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center\">\n                                    <span class=\"text-secondary text-nowrap\">5 UL</span>\n                                </div>\n                            </div>\n\n                        </div>\n\n\n                        <div class=\"col-12 p-0 rounded d-flex border\">\n                            <div onclick=\"UserCuestTa.index('.modal-body',19404,27625)\" class=\"w-custom-1 btn btn-outline-gray border-0\">\n                                TA\n                            </div>\n                            <div class=\"w-100 flex-lg-fill d-flex flex-column flex-lg-row bg-warning\">\n                                <input id=\"ta_sis19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" placeholder=\"Sis\" class=\"sv-ta w-100 text-center input-full-height border-0\">\n                                <input id=\"ta_dia19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" placeholder=\"Dia\" class=\"sv-ta w-100 text-center input-full-height border-0\">\n                                <input id=\"ta_media19404\" data-user_id_paciente=\"19404\" data-episodio_id=\"27625\" data-arrayindex=\"undefined\" type=\"text\" placeholder=\"Media\" class=\"sv-ta w-100 text-center input-full-height border-0\">\n\n\n                            </div>\n                            <div onclick=\"UserCuestTa.index('.modal-body',19404,27625)\" class=\"height-100 last-result-box w-last-result-box d-flex  flex-column flex-lg-row align-items-center justify-content-center\">\n                                <div class=\"actual_ta_value_19404 w-100 text-center\">\n                                    <span class=\"text-success\">3/2 mmHg</span>\n                                </div>\n                                <div class=\"actual_ta_media_value_19404 w-100 text-center\">\n                                    <span class=\"text-secondary\">1</span>\n                                </div>\n                            </div>\n                        </div> -->\n                            <vital-sign\n                                title=\"Temp\"\n                                attribute=\"temp\"\n                                form_data_key=\"cat_user_cuestionario_84\"\n                                :episodio_id=\"".concat(episodio_id, "\"\n                                siglas=\"\xB0C\"\n                                miruta=\"/user_cuest_temperatura/store\"\n                            ></vital-sign>\n                            <vital-sign\n                                title=\"Oxi\"\n                                attribute=\"oxi\"\n                                form_data_key=\"cat_user_cuestionario_73\"\n                                :episodio_id=\"").concat(episodio_id, "\"\n                                siglas=\"%\"\n                                miruta=\"/user_cuest_oximetria/store\"\n                            ></vital-sign>\n                            <vital-sign\n                                title=\"Fc\"\n                                attribute=\"fc\"\n                                form_data_key=\"cat_user_cuestionario_185\"\n                                :episodio_id=\"").concat(episodio_id, "\"\n                                siglas=\"lat/min\"\n                                miruta=\"/user_cuest_fc/store\"\n                            ></vital-sign>\n                            <vital-sign\n                                title=\"Fr\"\n                                attribute=\"fr\"\n                                form_data_key=\"value\"\n                                :episodio_id=\"").concat(episodio_id, "\"\n                                siglas=\"resp/min\"\n                                miruta=\"/user_cuest_fr/store\"\n                            ></vital-sign>\n\n\n                    </div>\n                    <div class=\"col-xl-8 p-4\">\n\ncljclkjlcxkjaslkdjklas\n                    </div>\n                </div>\n            ");
                Vue.component('vital-sign', {
                  props: {
                    'title': String,
                    'form_data_key': String,
                    'miruta': String,
                    'episodio_id': Number,
                    'siglas': String,
                    'attribute': String //'colorvalidation':Function,

                  },
                  data: function data() {
                    return {
                      loading: false,
                      inputValue: "",
                      observaciones: "",
                      paciente_index: 0,
                      paciente: {} //episodio_id: null,

                    };
                  },
                  template:
                  /*html*/
                  "\n                    <div class=\"col-12 p-0 rounded d-flex border mt-1\">\n                        <div class=\"w-custom-1 btn btn-outline-gray border-0\">\n                            {{title.toUpperCase()}}\n                        </div>\n                        <input\n                            v-model=\"inputValue\"\n                            @keyup.enter=\"handleEnter\"\n                            ref=\"this.attribute\"\n                            type=\"text\"\n                            class=\"w-100 text-center input-full-height border-0\"\n                        >\n                        <div class=\"px-1 height-100 last-result-box w-last-result-box d-flex align-items-center justify-content-center text-secondary\">\n                            <span v-if=\"loading\" class=\"spinner-border text-primary\" role=\"status\">\n                                <span class=\"sr-only\">Loading...</span>\n                            </span>\n                            <span v-else\n                                :class=\"['text-nowrap',this.getColor]\"\n                            >\n                                {{this.paciente[this.attribute]}} {{this.siglas}}\n\n                            </span>\n                        </div>\n                    </div>\n                ",
                  computed: {
                    getColor: function getColor() {
                      if (this.attribute === "temp") {
                        return colorValidationsTemperatura(this.paciente[this.attribute]);
                      }
                    }
                  },
                  methods: {
                    store: function store() {
                      var _this41 = this;

                      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee48() {
                        var formdata, result;
                        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee48$(_context48) {
                          while (1) {
                            switch (_context48.prev = _context48.next) {
                              case 0:
                                console.log(_this41.form_data_key);
                                _this41.loading = true;
                                formdata = new FormData();
                                formdata.append("episodio_id", _this41.episodio_id);
                                formdata.append("user_id", _this41.paciente.user_id);
                                formdata.append(_this41.form_data_key, _this41.inputValue);
                                formdata.append("sintoma_observacion", _this41.observaciones);
                                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
                                formdata.append("_token", $("#_token").val());
                                _context48.next = 11;
                                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + _this41.miruta, formdata);

                              case 11:
                                result = _context48.sent;
                                _this41.loading = false;
                                return _context48.abrupt("return", true);

                              case 14:
                              case "end":
                                return _context48.stop();
                            }
                          }
                        }, _callee48);
                      }))();
                    },
                    handleEnter: function handleEnter() {
                      var _this42 = this;

                      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee49() {
                        var result;
                        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee49$(_context49) {
                          while (1) {
                            switch (_context49.prev = _context49.next) {
                              case 0:
                                if (!(_this42.inputValue !== "")) {
                                  _context49.next = 7;
                                  break;
                                }

                                _context49.next = 3;
                                return _this42.store();

                              case 3:
                                result = _context49.sent;

                                if (result) {
                                  _this42.paciente[_this42.attribute] = _this42.inputValue;
                                  pacientes_datos[_this42.paciente_index] = _this42.paciente;
                                  _this42.inputValue = "";
                                }

                                _context49.next = 8;
                                break;

                              case 7:
                                alert("Escribe un valor"); //this.$refs[this.attribute].focus()

                              case 8:
                              case "end":
                                return _context49.stop();
                            }
                          }
                        }, _callee49);
                      }))();
                    }
                  },
                  mounted: function mounted() {
                    var _this43 = this;

                    //this.episodio_id = episodio_id
                    this.paciente_index = pacientes_datos.findIndex(function (item) {
                      return Number(item.episodio_id) === Number(_this43.episodio_id);
                    });
                    this.paciente = pacientes_datos[this.paciente_index];
                  }
                });
                app = new Vue({
                  el: '#App'
                }); //console.log(app);

              case 12:
              case "end":
                return _context50.stop();
            }
          }
        }, _callee50);
      }));

      function indexHistoriaTriaje(_x12) {
        return _indexHistoriaTriaje.apply(this, arguments);
      }

      return indexHistoriaTriaje;
    }()
  }, {
    key: "munecoAcciones",
    value: function munecoAcciones() {
      var sound = new Audio("click_muneco.mp3");
      var bg_success = '#2FA600';
      var bg_primary = '#185BA9';
      var contenedor = document.getElementById('figurahumana');
      contenedor.innerHTML = null; //Medidas contenedor 300:ancho 400:alto

      var rsr = Raphael(contenedor, 300, 400); //inicio muñeco frontal

      var cabezaCuelloAnterior = rsr.set();
      var cabezaCuelloAnterior_1 = rsr.path("M64,19c0,0,8,4,15,3s8-1,13-4c0,0,4,7,3,13c0,0,2,1,2,3c0,0,5-1,3,6s-3,6-3,6s1,2-2,3c0,0-10,10-14,10 s-8,1-16-10c0,0-3-1-3-3s-3-3-3-5c0,0-3-4,1-7c0,0,0.3-0.5,0.7-1.4C62.1,29.4,65.6,21.4,64,19z").attr({
        "class": 'cursor-pointer',
        parent: 'cabezaCuelloAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'cabezaCuelloAnterior_1');
      var cabezaCuelloAnterior_2 = rsr.path("M81,38l17-4V21c0,0-1-14.5-19-14C62.5,7.5,60,19,60,19v16L81,38z").attr({
        "class": 'st1',
        parent: 'cabezaCuelloAnterior',
        'fill': 'none',
        'stroke': bg_primary,
        'stroke-miterlimit': '10'
      }).data('id', 'cabezaCuelloAnterior_2');
      var cabezaCuelloAnterior_3 = rsr.path("M63.5,70.3c0,0,3.5-6.3,2.5-17.3c0,0,5,7,13,8s14-10,14-10s-1.8,11.6,2.1,18.8L63.5,70.3z");
      cabezaCuelloAnterior_3.attr({
        "class": 'st1',
        parent: 'cabezaCuelloAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'cabezaCuelloAnterior_3');
      cabezaCuelloAnterior.attr({
        'name': 'cabezaCuelloAnterior'
      });
      var mienbroInferiorDerechoAnterior = rsr.set();
      var mienbroInferiorDerechoAnterior_1 = rsr.path("M27,395c0-0.7,0-1.3,0-2c5.6-12.8,6.8-26.3,6-40.1c-0.8-14.8-3.3-29.4-1.4-44.4c1.5-11.2,7.1-21.2,8.5-32.6 c1.1-8.8,1.7-17.4,1.1-26.2c-1.2-17.3-2.8-34.6,0.7-51.8c0.7,0,1.3,0,2,0c11.4,5.4,21.1,12.9,29.2,22.6c1.3,1.5,2.9,2.8,4.8,3.4 c0,0.7,0,1.3,0,2c-5.8,17.8-10.8,33.8-13.7,48.4c-1.2,6.2-3,12.2-6.6,17.6c-1.9,2.8-2.5,6.1-2.4,9.5c0.2,7.8-1.5,15.2-3.7,22.5 c-1.8,6.1-4.1,12.1-4.5,18.4c-0.6,9.7-2.5,19.1-0.2,29.1c1.7,7.3,0.6,15.5-0.6,23.2c-0.3,2-1.3,4.4-3.6,4C37.4,398,31.5,399,27,395 z").attr({
        "class": 'cursor-pointer',
        parent: 'mienbroInferiorDerechoAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'mienbroInferiorDerechoAnterior_1');
      mienbroInferiorDerechoAnterior.attr({
        'name': 'mienbroInferiorDerechoAnterior'
      });
      var toraxAnterior = rsr.set();
      var toraxAnterior_1 = rsr.path("M117,74c0,12.8,0,25.7,0,38.5c0,1.3,0.5,2.6-2,2.6c-24.3-0.1-48.7-0.1-73-0.1c-0.9,0-1.2-0.2-1-1 c0.2-2,0.7-3.9,0.7-5.9c0.1-9.8,0.2-19.7,0.1-29.5c-0.1-3.7,1.1-5.4,4.8-6.1c4.9-0.8,10-1.8,14.1-4.7c4-2.7,8.1-3.4,12.7-3.2 c5.5,0.2,11,0,16.5,0.1c1.5,0,3.4-0.3,4.4,0.5C101.1,70.3,109.2,71.6,117,74z").attr({
        "class": 'cursor-pointer',
        parent: 'toraxAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'toraxAnterior_1');
      toraxAnterior.attr({
        'name': 'toraxAnterior'
      });
      var mienbroInferiorIzquierdoAnterior = rsr.set();
      var mienbroInferiorIzquierdoAnterior_1 = rsr.path("M116,198c2.2,21,1.3,41.9-0.8,62.8c-0.4,4,0.7,7.8,1.4,11.8c1.9,11.1,6.1,21.6,8.4,32.6 c2.9,14,1.2,28.2-0.2,42.3c-1.4,13.9-0.9,27.5,3.8,40.8c2.7,7.4,1.3,9.2-6.4,9.8c-0.3,0-0.7,0-1,0c-9.9,0.7-10.9-0.2-10.5-10.2 c0.1-3.7,0.2-7.3-0.6-10.9c-0.3-1.6-0.4-3.4,0-5c3.7-13.8,1.4-27.4-2.2-40.7c-3-10.8-5.4-21.6-5.8-32.8c-0.1-2-0.5-4-1.7-5.7 c-5.3-7.8-6.9-16.9-8.8-25.8c-3.1-14.3-7.9-28.2-12.5-42c0-0.3,0-0.7,0-1c2.1-0.4,3.8-1.4,5.1-3c7.7-10.2,17.9-17.1,28.9-23 C114,198,115,198,116,198z").attr({
        "class": 'cursor-pointer',
        parent: 'mienbroInferiorIzquierdoAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'mienbroInferiorIzquierdoAnterior_1');
      mienbroInferiorIzquierdoAnterior.attr({
        'name': 'mienbroInferiorIzquierdoAnterior'
      });
      var abdomenAnterior = rsr.set();
      var abdomenAnterior_1 = rsr.path("M41,121c0-1.7,0-3.3,0-5c25.3-0.5,50.7-0.5,76,0c0,1.7,0,3.3,0,5c-5.1,15.2-8.3,30.5-4,46.5 c1.2,4.2-0.5,5.7-4.7,5.6c-19.5-0.1-39-0.1-58.5,0c-4.2,0-5.9-1.4-4.7-5.6C49.3,151.5,46.1,136.2,41,121z").attr({
        "class": 'cursor-pointer',
        parent: 'abdomenAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'abdomenAnterior_1');
      abdomenAnterior.attr({
        'name': 'abdomenAnterior'
      });
      var mienbroSuperiorIzquierdoAnterior = rsr.set();
      var mienbroSuperiorIzquierdoAnterior_1 = rsr.path("M118,116c0-12.7,0-25.3,0-38c0-1.3-1.1-3.6,2-3c11.8,5.4,16.1,15.2,17.4,27.5c1.6,16-0.7,32.1,3.7,48.1 c3.6,13.4,2.5,27.8,3.1,41.7c0.3,6.7,1,13.3,2.8,19.7c0,4.7,1.8,11.2-0.5,13.5c-2.3,2.3-8.8,0.5-13.5,0.5c-0.3,0-0.7,0-1,0 c-1.3-2.1-3.9-4.3-3-6.6c2.1-4.9,0.3-10.2,2.1-15.1c2.8-7.2,1.8-14.2-0.9-21.4c-6.1-15.9-10.5-32.3-10.6-49.6 C119.7,127.6,118.6,121.8,118,116z").attr({
        "class": 'cursor-pointer',
        parent: 'mienbroSuperiorIzquierdoAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'mienbroSuperiorIzquierdoAnterior_1');
      mienbroSuperiorIzquierdoAnterior.attr({
        'name': 'mienbroSuperiorIzquierdoAnterior'
      });
      var genitalesAnterior = rsr.set();
      var genitalesAnterior_1 = rsr.path("M80,222c-1,0-2,0-3,0c-2.4-1.2-4.6-2.6-6.3-4.8c-7.4-9-16.2-16.2-26.9-21c-2.1-0.9-3.2-2.3-2.8-4.6 c1-5.9,1-11.9,3-17.6c23.3,0,46.7,0,70,0c0.7,5.3,1.3,10.5,2,15.8c0.4,3.1-0.1,5.4-3.4,6.8c-9.6,4.2-17.3,10.9-24.2,18.6 C86,217.9,83.3,220.3,80,222z").attr({
        "class": 'cursor-pointer',
        parent: 'genitalesAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'genitalesAnterior_1');
      genitalesAnterior.attr({
        'name': 'genitalesAnterior'
      });
      var mienbroSuperiorDerechoAnterior = rsr.set();
      var mienbroSuperiorDerechoAnterior_1 = rsr.path("M26,226c-4.7,0-11.2,1.8-13.5-0.5c-2.3-2.3-0.5-8.8-0.5-13.5c0-1,0-2,0-3c1.3-3.8,1.6-7.8,1.9-11.8 c1.1-15.4,0.4-31.1,3.2-46.3c2.2-11.5,4.3-22.8,3.6-34.6c-0.3-4.1,0-8.3,0.2-12.5C21.7,91.4,25.7,80.7,38,75c1.7,0,3.3-0.6,3.1,2.5 C40.7,85,41,92.5,41,100c-0.6,9.3-1.5,18.6-1.8,27.9c-0.5,17.6-2.3,35.4-9.9,51.2c-6.1,12.6-1.9,24.2-0.8,36 C29,219.7,28.8,222.8,26,226z").attr({
        "class": 'cursor-pointer',
        parent: 'mienbroSuperiorDerechoAnterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'mienbroSuperiorDerechoAnterior_1');
      mienbroSuperiorDerechoAnterior.attr({
        'name': 'mienbroSuperiorDerechoAnterior'
      }); //fin muñeco frontal
      //inicio muñeco posterior

      var regionDorsalPosterior = rsr.set();
      var regionDorsalPosterior_1 = rsr.path("M257.9,122c-26.1,0-52.1,0-78.2,0c0.2-2,0.7-4,0.7-6c0.1-10.3,0.2-20.6,0.1-31c-0.1-3.7,1.1-5.6,4.9-6.1 c4.2-0.6,8.7-1.2,12.1-3.7c6.4-4.7,13.6-4.5,20.8-4.3c3.9,0.1,7.8,0,11.7,0.1c1.5,0,3.3-0.1,4.4,0.7c6.1,4.3,13,6.3,20.2,7.8 c1.8,0.4,3.2,1.3,3,3.5C256.3,96,258.3,109,257.9,122z").attr({
        "class": 'cursor-pointer',
        parent: 'regionDorsalPosterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'regionDorsalPosterior_1');
      regionDorsalPosterior.attr({
        'name': 'regionDorsalPosterior'
      });
      var regionLumbarPosterior = rsr.set();
      var regionLumbarPosterior_1 = rsr.path("M257.9,123c0,1.7,0,3.4,0,5.1c-5.2,15.4-8.5,31-4.1,47.2c1.2,4.3-0.4,5.8-4.8,5.8c-19.6-0.1-39.2,0-58.9-0.1 c-6,0-6.3-0.5-5.2-6.6c2.3-11.9,3-23.8-1.3-35.5c-1.3-3.6-1.7-7.6-2.6-11.4c-0.8-3.2,0.3-4.8,3.7-4.8 C209.2,122.8,233.6,122.9,257.9,123z").attr({
        "class": 'cursor-pointer',
        parent: 'regionLumbarPosterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'regionLumbarPosterior_1');
      regionLumbarPosterior.attr({
        'name': 'regionLumbarPosterior'
      });
      var cabezaCuelloPosterior = rsr.set();
      var cabezaCuelloPosterior_1 = rsr.path("M205.1,70.2c0.3-3.2,0.6-6.4,0.8-9.6c0.3-5.3-0.6-10.1-3.3-15c-3.6-6.6-4.7-14.2-3.5-21.7 c1.7-10.5,8.6-16.5,22.4-15.4c15,1.2,20.7,10.5,18,27.2c-0.8,4.6-2.3,9-4.7,13.2c-3.9,6.8-1.9,14.1-0.3,21.2 C224.8,70.2,214.9,70.2,205.1,70.2z M201,28.1c0.1,12.2,1.7,15.1,9.9,21.6c2.4,1.9,5.4,0.8,7.6,2.2c4.9,3.1,8.7,0.3,11.9-2.4 c7.5-6,8-14.7,7.5-23.5c-0.2-4.3-1.1-8.6-4.7-11.3c-7.5-5.5-15.8-5.5-24-2C202.2,15.6,200.8,21.9,201,28.1z").attr({
        "class": 'cursor-pointer',
        parent: 'cabezaCuelloPosterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'cabezaCuelloPosterior_1');
      cabezaCuelloPosterior.attr({
        'name': 'cabezaCuelloPosterior'
      });
      var miembroInferiorDerechoPosterior = rsr.set();
      var miembroinferiorderechoposterior_1 = rsr.path("M259,399.2c-3-1.3-4.3-3.7-4.1-6.9c0.2-4.2,0.5-8.4,0.9-12.6c1.1-13.4-1.8-26.3-5.3-39 c-2.2-8-3.3-16.2-3.6-24.5c-0.1-2.2,0-4.5-1.2-6.4c-6.9-10.8-8.3-23.2-10.6-35.2c-2-10.5-6.5-20.3-8.6-30.8 c-3-14.9-4.2-13.4,10.4-14.4c10.2-0.6,17.4-5.8,21.1-15.6c0.5-1.3,1-2.5,1.6-3.7c0.1-0.1,0.3-0.2,1.2-0.6c0.5,4.5,1.3,8.7,1.5,12.9 c0.6,15.2,0.1,30.5-1.8,45.6c-1.3,10.2,1.9,19.7,3.2,29.5c0.6,4.6,1.9,8.8,3.7,13c5.3,13.1,4.7,26.9,3.8,40.6 c-0.6,8.8-1.4,17.5-1.8,26.3c-0.4,7.3,1.6,12.5,8.2,15.8c1.1,5.2-0.8,6.9-6.1,6.2C267.1,398.8,263,399.2,259,399.2z").attr({
        "class": 'cursor-pointer',
        parent: 'miembroInferiorDerechoPosterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'miembroinferiorderechoposterior_1');
      miembroInferiorDerechoPosterior.attr({
        'name': 'miembroInferiorDerechoPosterior'
      });
      var miembroInferiorIzquierdoPosterior = rsr.set();
      var miembroInferiorIzquierdoPosterior_1 = rsr.path("M214.3,230.6c0,2,0,4.1,0,6.1c-4.2,17.6-9.8,34.8-13.6,52.5c-3.2,15-8.4,29.5-11.1,44.8 c-3.3,18.8-8.5,37.2-5.7,56.6c0.5,3.3-0.2,6.4-3.2,8.6c-1,0-2,0-3,0c-3.8-0.6-7.7-1.2-11.5-1.8c-1.8-0.3-4-0.4-4.4-2.6 c-0.4-2,1.3-3,2.8-3.8c3.8-2.2,5.1-5.6,5.1-9.8c-0.1-6.9-0.2-13.8-0.8-20.7c-1.4-14.3-3.2-28.7,0.7-42.8c2.6-9.3,6.1-18.3,7.4-28.1 c0.7-5.3,2-10.5,1.7-16c-0.7-16.4-2.9-32.7-2-49.1c0.3-4.8,0.3-9.8,2.4-15.5c0.8,1.9,1.2,2.6,1.5,3.4c3.8,11.3,11.9,16.7,23.7,17.2 C207.6,229.7,210.9,230.3,214.3,230.6z").attr({
        "class": 'cursor-pointer',
        parent: 'miembroInferiorIzquierdoPosterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'miembroInferiorIzquierdoPosterior_1');
      miembroInferiorIzquierdoPosterior.attr({
        'name': 'miembroInferiorIzquierdoPosterior'
      });
      var regionLumboSacraPosterior = rsr.set();
      var regionLumboSacraPosterior_1 = rsr.path("M259,199.2c0,3.4,0,6.8,0,10.2c-4,9.8-10.2,17.3-21.3,19.3c-10.5,0-21,0-31.5,0c-3-0.7-6-1.1-8.9-2 c-10-3.1-17.4-12.9-16.7-22.8c0.4-5.9,1.7-11.7,2.5-17.5c0.5-3.1,2-4.7,5.4-4.7c20.8,0.1,41.5,0.1,62.3,0c3.4,0,4.8,1.7,5.3,4.7 C257,190.6,258,194.9,259,199.2z").attr({
        "class": 'cursor-pointer',
        parent: 'regionLumboSacraPosterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'regionLumboSacraPosterior_1');
      regionLumboSacraPosterior.attr({
        'name': 'regionLumboSacraPosterior'
      });
      var miembroSuperiorDerechoPosterior = rsr.set();
      var miembroSuperiorDerechoPosterior_1 = rsr.path("M258,121c0-12.7,0-25.3,0-38c0-1.3-1.1-3.6,2-3c11.8,5.4,16.1,15.2,17.4,27.5c1.6,16-0.7,32.1,3.7,48.1 c3.6,13.4,2.5,27.8,3.1,41.7c0.3,6.7,1,13.3,2.8,19.7c0,4.7,1.8,11.2-0.5,13.5c-2.3,2.3-8.8,0.5-13.5,0.5c-0.3,0-0.7,0-1,0 c-1.3-2.1-3.9-4.3-3-6.6c2.1-4.9,0.3-10.2,2.1-15.1c2.8-7.2,1.8-14.2-0.9-21.4c-6.1-15.9-10.5-32.3-10.6-49.6 C259.7,132.6,258.6,126.8,258,121z").attr({
        "class": 'cursor-pointer',
        parent: 'miembroSuperiorDerechoPosterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'miembroSuperiorDerechoPosterior_1');
      miembroSuperiorDerechoPosterior.attr({
        'name': 'miembroSuperiorDerechoPosterior'
      });
      var miembroSuperiorIzquierdoPosterior = rsr.set();
      var miembroSuperiorIzquierdoPosterior_1 = rsr.path("M165,231c-4.7,0-11.2,1.8-13.5-0.5c-2.3-2.3-0.5-8.8-0.5-13.5c0-1,0-2,0-3c1.3-3.8,1.6-7.8,1.9-11.8 c1.1-15.4,0.4-31.1,3.2-46.3c2.2-11.5,4.3-22.8,3.6-34.6c-0.3-4.1,0-8.3,0.2-12.5c0.8-12.5,4.7-23.2,17.1-28.9 c1.7,0,3.3-0.6,3.1,2.5c-0.4,7.5-0.1,15-0.1,22.5c-0.6,9.3-1.5,18.6-1.8,27.9c-0.5,17.6-2.3,35.4-9.9,51.2 c-6.1,12.6-1.9,24.2-0.8,36C168,224.7,167.8,227.8,165,231z").attr({
        "class": 'cursor-pointer',
        parent: 'miembroSuperiorIzquierdoPosterior',
        'stroke-width': '0',
        'stroke-opacity': '1',
        'fill': bg_primary
      }).data('id', 'miembroSuperiorIzquierdoPosterior_1');
      miembroSuperiorIzquierdoPosterior_1.attr({
        'name': 'miembroSuperiorIzquierdoPosterior'
      }); //fin muñeco posterior

      var rsrGroups = [toraxAnterior, mienbroInferiorDerechoAnterior, mienbroInferiorIzquierdoAnterior, abdomenAnterior, mienbroSuperiorIzquierdoAnterior, genitalesAnterior, mienbroSuperiorDerechoAnterior, cabezaCuelloAnterior, regionDorsalPosterior, regionLumbarPosterior, cabezaCuelloPosterior, miembroInferiorDerechoPosterior, miembroInferiorIzquierdoPosterior, regionLumboSacraPosterior, miembroSuperiorDerechoPosterior, miembroSuperiorIzquierdoPosterior];
      toraxAnterior.push(toraxAnterior_1);
      mienbroInferiorDerechoAnterior.push(mienbroInferiorDerechoAnterior_1);
      mienbroInferiorIzquierdoAnterior.push(mienbroInferiorIzquierdoAnterior_1);
      abdomenAnterior.push(abdomenAnterior_1);
      mienbroSuperiorIzquierdoAnterior.push(mienbroSuperiorIzquierdoAnterior_1);
      genitalesAnterior.push(genitalesAnterior_1);
      mienbroSuperiorDerechoAnterior.push(mienbroSuperiorDerechoAnterior_1);
      cabezaCuelloAnterior.push(cabezaCuelloAnterior_1, cabezaCuelloAnterior_2, cabezaCuelloAnterior_3);
      regionDorsalPosterior.push(regionDorsalPosterior_1);
      regionLumbarPosterior.push(regionLumbarPosterior_1);
      cabezaCuelloPosterior.push(cabezaCuelloPosterior_1);
      miembroInferiorDerechoPosterior.push(miembroinferiorderechoposterior_1);
      miembroInferiorIzquierdoPosterior.push(miembroInferiorIzquierdoPosterior_1);
      regionLumboSacraPosterior.push(regionLumboSacraPosterior_1);
      miembroSuperiorDerechoPosterior.push(miembroSuperiorDerechoPosterior_1);
      miembroSuperiorIzquierdoPosterior.push(miembroSuperiorIzquierdoPosterior_1); //eventos inicio muñeco frontal

      cabezaCuelloAnterior.click(function () {
        //alert("Cabeza y cuello anterior")
        cabezaCuelloAnterior_1.attr({
          'fill': bg_success
        });
        cabezaCuelloAnterior_2.attr({
          'stroke': bg_success,
          'stroke-miterlimit': '10'
        });
        cabezaCuelloAnterior_3.attr({
          'fill': bg_success
        });
      });
      mienbroInferiorDerechoAnterior.click(function () {
        //alert("Cabeza y cuello anterior")
        mienbroInferiorDerechoAnterior_1.attr({
          'fill': bg_success
        });
      });
      toraxAnterior.click(function () {
        //alert("Cabeza y cuello anterior")
        toraxAnterior_1.attr({
          'fill': bg_success
        });
      });
      mienbroInferiorIzquierdoAnterior.click(function () {
        //alert("Cabeza y cuello anterior")
        mienbroInferiorIzquierdoAnterior_1.attr({
          'fill': bg_success
        });
      });
      abdomenAnterior.click(function () {
        //alert("Cabeza y cuello anterior")
        abdomenAnterior_1.attr({
          'fill': bg_success
        });
      });
      mienbroSuperiorIzquierdoAnterior.click(function () {
        //alert("Cabeza y cuello anterior")
        mienbroSuperiorIzquierdoAnterior_1.attr({
          'fill': bg_success
        });
      });
      genitalesAnterior.click(function () {
        //alert("Cabeza y cuello anterior")
        genitalesAnterior_1.attr({
          'fill': bg_success
        });
      });
      mienbroSuperiorDerechoAnterior.click(function () {
        //alert("Cabeza y cuello anterior")
        mienbroSuperiorDerechoAnterior_1.attr({
          'fill': bg_success
        });
      }); //eventos fin muñeco frontal
      //eventos inicio muñeco posterior

      regionDorsalPosterior.click(function () {
        sound.play(); //alert("Cabeza y cuello anterior")

        regionDorsalPosterior_1.attr({
          'fill': bg_success
        });
      });
      regionLumbarPosterior.click(function () {
        sound.play(); //alert("Cabeza y cuello anterior")

        regionLumbarPosterior_1.attr({
          'fill': bg_success
        });
      });
      cabezaCuelloPosterior.click(function () {
        //alert("Cabeza y cuello anterior")
        sound.play();
        cabezaCuelloPosterior_1.attr({
          'fill': bg_success
        });
      });
      miembroInferiorDerechoPosterior.click(function () {
        //alert("Cabeza y cuello anterior")
        sound.play();
        miembroinferiorderechoposterior_1.attr({
          'fill': bg_success
        });
      });
      miembroInferiorIzquierdoPosterior.click(function () {
        //alert("Cabeza y cuello anterior")
        sound.play();
        miembroInferiorIzquierdoPosterior_1.attr({
          'fill': bg_success
        });
      });
      regionLumboSacraPosterior.click(function () {
        //alert("Cabeza y cuello anterior")
        sound.play();
        regionLumboSacraPosterior_1.attr({
          'fill': bg_success
        });
      });
      miembroSuperiorDerechoPosterior.click(function () {
        //alert("Cabeza y cuello anterior")
        sound.play();
        miembroSuperiorDerechoPosterior_1.attr({
          'fill': bg_success
        });
      });
      miembroSuperiorIzquierdoPosterior.click(function () {
        //alert("Cabeza y cuello anterior")
        sound.play();
        miembroSuperiorIzquierdoPosterior_1.attr({
          'fill': bg_success
        });
      }); //eventos fin muñeco posterior
    }
  }, {
    key: "getAll",
    value: function getAll(selector, data) {
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["loading"])("#user_cuest_model_all");
      var formdata = new FormData();
      formdata.append("user_id", data.user_id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", data.fecha); //formdata.append("created_at", "2021-04-1")

      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_historia_medica/getHistorial", formdata).then(function (response) {
        var chat = "";
        var fecha;
        var tempFecha;
        var f;
        $(selector).html("\n                    <div class=\"container-fluid p-0\">\n                        <div class=\"row\">\n                            <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 p-1\">\n                                <div class=\"timeline timeline-inverse\">\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n                ").slideDown("slow");

        if (response.length > 0) {
          $.each(response, function (indexInArray, value) {
            var medico = first(medicos_datos.filter(function (medico) {
              return equalsInt(medico.user_id, value.user_id_medico);
            }));

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["is_null"])(medico)) {
              medico = {
                "user_id": value.user_id_medico,
                "prefijo": "",
                "medico": "No Disponible",
                "posicion_id": 1,
                "posicion": "No disponible",
                "telefono_movil": "+584125963376",
                "cat_user_especialidad_id": 67,
                "especialidad": "Sin Especialidad",
                "genero": "F"
              };
            }

            var episodio_id = pacientes_datos.find(function (x) {
              return equalsInt(x['user_id'], data.user_id);
            })['episodio'];

            if (Object.keys(response).length > 0) {
              pacientes_datos.map(function (x) {
                if (x.user_id == first(response).user_id) {
                  x.resultados = response;
                }
              });
            } else {
              pacientes_datos.map(function (x) {
                if (x.user_id == data.user_id) {
                  x.resultados = [];
                }
              });
            }

            var imprimir = false;

            if (["Orden Médica", "Evolución", "Probabilidad diagnóstica", "Plan", "Observación", "Récipe", "Evolución", "Estudio Diagnóstico"].includes(value.description)) {
              imprimir = true;
            }

            fecha = fechaAPPLE(value.created_at);
            var fecha_grupo = fechaCortaAPPLE(value.created_at);
            var f1 = value.created_at.split(" ");

            if (tempFecha != f1[0]) {
              $(".timeline").append("\n                                    <div class=\"time-label\">\n                                        <span class=\"bg-primary\">\n                                            ".concat(fecha_grupo, "\n                                        </span>\n                                    </div>\n                                "));
              tempFecha = f1[0];
            }

            if (tempFecha == f1[0]) {
              var dato = "";
              var coments = "";
              var valor = "";

              if (value.description == "Orden Médica" || value.description == "Evolución" || value.description == "Pendiente" || value.description == "Probabilidad diagnóstica" || value.description == "Plan" || value.description == "Observación" || value.description == "Récipe" || value.description == "Estudio Diagnóstico") {
                if (value.value != null) {
                  valor = value.value.replace(/\n/g, "<br />");
                }

                dato = "\n                                        ".concat(valor, "\n                                    ");
              }

              if (value.description == "Laboratorio" || value.description == "Imagenes" || value.description == "Fotografías" || value.description == "Otro") {
                if (value.coments != null) {
                  coments = value.coments.replace(/\n/g, "<br />");
                }

                var imageIcon = "";

                if (value.description == "Laboratorio") {
                  imageIcon = "image/system/icono_laboratorio.svg";
                }

                if (value.description == "Imagenes") {
                  imageIcon = "".concat(value.value);
                }

                if (value.description == "Fotografías") {
                  imageIcon = "".concat(value.value);
                }

                if (value.description == "Otro") {
                  imageIcon = "image/system/icono_archivo.svg";
                }

                if (value.value != null) {
                  valor = "\n                                            <img onclick=\"window.open('".concat(value.value, "', '_blank')\" style=\"cursor:pointer;width: 50px;\" class=\"ampliar zoom img-fluid\" src='").concat(imageIcon, "'>\n                                            <br>");
                }

                dato = "\n                                        ".concat(valor, "\n                                        ").concat(coments, "\n                                    ");
              }

              var medico_posicion = {
                "nombre": "Tratante",
                "siglas": "TR",
                "color": "success"
              };

              try {
                if ([2].includes(medico.posicion_id)) {
                  medico_posicion = {
                    "nombre": "Interconsultante",
                    "siglas": "IN",
                    "color": "info"
                  };
                }

                if ([3, 4].includes(medico.posicion_id)) {
                  medico_posicion = {
                    "nombre": "Fellow",
                    "siglas": "FE",
                    "color": "orange"
                  };
                }

                if ([5, 6, 7, 8].includes(medico.posicion_id)) {
                  medico_posicion = {
                    "nombre": "Residentes",
                    "siglas": "RE",
                    "color": "secondary"
                  };
                }

                if ([9].includes(medico.posicion_id)) {
                  medico_posicion = {
                    "nombre": "RAMH",
                    "siglas": "RA",
                    "color": "purple"
                  };
                }

                if ([10].includes(medico.posicion_id)) {
                  medico_posicion = {
                    "nombre": "Enfermería",
                    "siglas": "EN",
                    "color": "warning"
                  };
                }
              } catch (error) {
                console.log("medico:", medico);
              }

              var icono_medico = {
                "icono": "md",
                "name": "Equipo Médico",
                "color": "primary"
              };

              if (equalsInt(medico.equipo_salud_id, 2)) {
                //console.log("-->", first(medicos_datos.filter(medico => equalsInt(medico.user_id, value.user_id_medico) ) ))
                icono_medico = {
                  "icono": "nurse",
                  "name": "Equipo Enfermeria",
                  "color": "warning"
                };
              }

              $(".timeline").append(
              /*html */
              "\n                                    <div>\n                                        <i data-tippy-content=\"".concat(icono_medico.name, "\" class=\"tooltip-").concat(icono_medico.color, " fas fa-user-").concat(icono_medico.icono, " bg-").concat(icono_medico.color, " text-white\" aria-hidden=\"true\"></i>\n                                        <div class=\"timeline-item\">\n                                            <span class=\"time\">\n\n                                            <i class=\"far fa-clock\"></i>\n                                            ").concat(fecha, "\n                                            <a class=\"m-0 p-0 imprimir\"  type=\"button\" id=\"triggerId").concat(value.description + value.id, "\" data-type=\"one\" data-name=\"").concat(value.description, "\" data-id=\"").concat(value.id, "\" data-episodio_id=\"").concat(episodio_id, "\" data-impresion=\"color\" >\n                                                <i data-tippy-content=\"Imprimir\" class=\"tooltip-info imprimir m-0 p-0 fas fa-print text-info heartbeat\"\n                                                style=\"font-size: 1em;cursor:pointer; margin-left:1em;\" data-type=\"one\" data-name=\"").concat(value.description, "\" data-id=\"").concat(value.id, "\" data-episodio_id=\"").concat(episodio_id, "\" data-impresion=\"color\" aria-hidden=\"true\"></i>\n                                            </a>\n                                            <!-- <div class=\"btn-group ").concat(imprimir ? '' : 'd-none', "\" style=\"margin-left:0.5em;\">\n\n                                                <div class=\"dropdown-menu dropdown-menu-right\" aria-labelledby=\"triggerId").concat(value.description + value.id, "\">\n                                                    <a class=\"dropdown-item imprimir\" data-type=\"one\" data-name=\"").concat(value.description, "\" data-id=\"").concat(value.id, "\" data-episodio_id=\"").concat(episodio_id, "\" data-impresion=\"color\" href=\"#\" style=\"display:none\">Color</a>\n                                                    <a class=\"dropdown-item imprimir\" data-type=\"one\" data-name=\"").concat(value.description, "\" data-id=\"").concat(value.id, "\" data-episodio_id=\"").concat(episodio_id, "\" data-impresion=\"bw\" href=\"#\">Blanco y negro</a>\n                                                    <a class=\"dropdown-item imprimir\" data-type=\"all\" data-name=\"").concat(value.description, "\" data-id=\"").concat(value.id, "\" data-episodio_id=\"").concat(episodio_id, "\" data-impresion=\"color\" href=\"#\">Todos los ").concat(value.description, "</a>\n                                                </div>\n                                            </div> -->\n\n                                            <!--\n                                            <i class=\"fas fa-times text-primary heartbeat delete\" data-id=\"").concat(value.id, "\" data-delete=\"").concat(value.description, "\" style=\"font-size: 1em;cursor:pointer; margin-left:0.5em;\" aria-hidden=\"true\"></i> -->\n                                            </span>\n\n\n                                            <h3 class=\"timeline-header\">\n                                            <b>").concat(value.description, "</b> | <a href=\"#\">").concat(value.medico, "</a> <span data-tippy-content=\"Equipo ").concat(medico_posicion.nombre, "\" class=\"badge tooltip-").concat(medico_posicion.color, " bg-").concat(medico_posicion.color, " scale-in-hor-left\" style=\"width:2em;\">").concat(medico_posicion.siglas, "</span></h3>\n\n                                            <div class=\"timeline-body\" style=\"font-size: 0.5em;    white-space: pre-line;\">\n                                                    ").concat(dato, "\n                                            </div>\n                                            <div class=\"timeline-footer\">\n\n                                            </div>\n                                        </div>\n                                    </div>\n                                "));
            }
          });
          $(".imprimir").on("click", function () {
            var description = "";

            switch (name) {
              case "Orden Médica":
                description = "orden_medica";
                break;

              case "Evolución":
                description = "evolucion";
                break;

              case "Pendiente":
                description = "pendiente";
                break;

              case "Probabilidad diagnóstica":
                description = "probabilidad_diagnostica";
                break;

              case "Plan":
                description = "plan";
                break;

              case "Observación":
                description = "observacion";
                break;

              case "Récipe":
                description = "recipe";
                break;

              case "Estudio Diagnóstico":
                description = "estudio_diagnostico";
                break;

              case "Laboratorio":
                description = "laboratorio";
                break;

              case "Imagenes":
                description = "imagenes";
                break;

              case "Otro":
                description = "otro";
                break;

              default:
                alert("Error. Reporte no encotrado");
                return false;
                break;
            }

            if (['Orden Médica'].includes($(this).data('name'))) {
              window.open(location.origin + "/user/informe/omed/orden_medica/".concat($(this).data('episodio_id'), "/").concat($(this).data('id')));
              return false;
            }

            if (['Evolución'].includes($(this).data('name'))) {
              window.open(location.origin + "/user/informe/nde/evolucion_medica/".concat($(this).data('episodio_id'), "/").concat($(this).data('id')));
              return false;
            }

            if (['Récipe'].includes($(this).data('name'))) {
              window.open(location.origin + "/user/informe/recmed/recipe_medico/".concat($(this).data('episodio_id'), "/").concat($(this).data('id')));
              return false;
            }

            if ($(this).data('type') === "one") {
              window.open("/user/reporte/".concat(description, "/").concat($(this).data('episodio_id'), "/").concat($(this).data('id'), "/").concat($(this).data('impresion')));
            } else {
              window.open("/user/reporte/".concat(description, "/").concat($(this).data('episodio_id'), "/all/").concat($(this).data('impresion')));
            }
          });
          $(".delete").on("click", function () {
            if ($(this).data('delete') == "Laboratorio") {
              UserCuestLaboratorio.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Evolución") {
              UserCuestEvolucion.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Orden Médica") {
              UserCuestOrdenMedica.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Probabilidad diagnóstica") {
              UserCuestImpresionDiagnostica.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Plan") {
              UserCuestPlan.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Observación") {
              UserCuestObservacion.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Imagenes") {
              UserCuestImagen.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Fotografías") {
              UserCuestFotografia.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Otro") {
              UserCuestOtroArchivo.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Récipe") {
              UserCuestRecipe.destroy($(this).data('id'), data.user_id);
            }

            if ($(this).data('delete') == "Estudio Diagnóstico") {
              UserCuestEstudioDiagnostico.destroy($(this).data('id'), data.user_id);
            }
          });
          Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["activarTooltip"])();
        } else {
          $(".timeline").append("\n                        <div>\n                            <div class=\"timeline-item\">\n                                <div class=\"timeline-body text-center text-primary font-weight-bold\" style=\"font-size: 0.5em;    white-space: pre-line;\">\n                                    No posee historia este d\xEDa\n                                </div>\n                            </div>\n                        </div>\n                    ");
        }

        $(".timeline").append("\n                        <div>\n                        <i class=\"far fa-clock bg-gray\"></i>\n                    </div>\n                ");
      });
    }
  }, {
    key: "create2",
    value: function () {
      var _create = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee51(selector, user_id, user_cuest_historia_medica_id) {
        var episode, formdata, option;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee51$(_context51) {
          while (1) {
            switch (_context51.prev = _context51.next) {
              case 0:
                $("#modelId").modal("show");
                $(selector).html("\n            <div>\n                <h1 class=\"text-primary\">\n                    Historia M\xE9dica | Egreso\n                </h1>\n            </div>\n            <div id=\"model\" style=\"display:none\" class=\"container-fluid\">\n                <div class=\"alert bg-gray\" role=\"alert\">\n                    <div class=\"row mt-2 mb-2\">\n                        <div class=\"col-xs-2 col-sm-2 col-md-2 col-lg-2 col-xl-2 text-center\">\n                            <b class=\"text-light h4\">N\xB0 Historia</b>:<br>\n                            <span class=\"text-primary font-weight-bold\" id=\"hm_historia_id\"></span>\n                        </div>\n                        <div class=\"col-xs-2 col-sm-2 col-md-2 col-lg-2 col-xl-2 text-center\">\n                            <b class=\"text-light h4\">N\xB0 Episodio</b>:<br>\n                            <span class=\"text-primary font-weight-bold\" id=\"hm_episodio_id\"></span>\n                        </div>\n                        <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4 text-center\">\n                            <b class=\"text-light h4\">Paciente</b>:<br>\n                            <span class=\"text-primary font-weight-bold\" id=\"hm_paciente\"></span>\n                        </div>\n                        <div class=\"col-xs-2 col-sm-1 col-md-1 col-lg-1 col-xl-1 text-center\">\n                            <b class=\"text-light h4\">Edad</b>:<br>\n                            <span class=\"text-primary font-weight-bold\" id=\"hm_paciente_edad\"></span>\n                        </div>\n                        <div class=\"col-xs-3 col-sm-3 col-md-3 col-lg-3 col-xl-3 text-center\">\n                            <b class=\"text-light h4\">Registrado desde </b>:<br>\n                            <span class=\"text-primary font-weight-bold\" id=\"hm_fecha_registro\"></span>\n                        </div>\n                    </div>\n                </div>\n\n\n\n\n\n                <div class=\"row mt-2\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <button id=\"btn_submit_historia\" class=\"btn btn-primary btn-block\">Finalizar episodio</button>\n                    </div>\n                </div>\n                <div class=\"row mt-2\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <button id=\"regresar\" class=\"btn btn-light text-primary btn-block\">Regresar</button>\n                    </div>\n                </div>\n            </div>\n\n        ");
                $("#model").slideDown("slow");
                formdata = new FormData();
                formdata.append("_token", $("#_token").val());
                formdata.append("user_id", user_id);
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_historia_medica/create", formdata).then(function (response) {
                  $.each(response, function (indexInArray, valueOfElement) {
                    $("#hm_" + indexInArray).html(valueOfElement);

                    if (indexInArray == "episodio_id") {
                      episode = valueOfElement;
                    }
                  });
                });
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])("catEspecialidad/index", formdata).then(function (response) {
                  option = "<option value=''>Seleccione Especialidad</option>";
                  $.each(response, function (indexInArray, valueOfElement) {
                    option += "<option value='".concat(valueOfElement.id, "'>").concat(valueOfElement.description, "</option>");
                  });
                  $("#cat_user_especialidad_id").html(option);
                });
                $("#cat_user_especialidad_id").on("change", function () {
                  formdata.append("cat_user_especialidad_id", $(this).val());
                  Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_especialidad/index", formdata).then(function (response) {
                    option = "<option value=''>Seleccione Médico Tratante</option>";
                    $.each(response, function (indexInArray, valueOfElement) {
                      option += "<option value='".concat(valueOfElement.id, "'>").concat(valueOfElement.nombres, " ").concat(valueOfElement.apellidos, "</option>");
                    });
                    $("#user_id_medico_tratante").html(option);
                  });
                });
                $("#btn_submit_historia").on("click", function () {
                  UserCuestHistoria.destroy(user_cuest_historia_medica_id).then(function (response) {
                    console.log();

                    if (response != undefined) {
                      UserCuestHistoria.index('.modal-body', user_id);
                    }
                  });
                });
                $("#regresar").on("click", function () {
                  UserCuestHistoria.index('.modal-body', user_id);
                });
                UserCuestAntecedente.create("#antecedente_personal", user_id, "personal");
                UserCuestAntecedente.create("#antecedente_familiar", user_id, "familiar");
                UserCuestAntecedente.create("#antecedente_quirurgico", user_id, "quirurgico");
                UserCuestAntecedente.create("#antecedente_hospitalizacion", user_id, "hospitalizacion");
                UserCuestAntecedente.index("#antecedente_quirurgico_show", user_id, "quirurgico");
                UserCuestAntecedente.index("#antecedente_hospitalizacion_show", user_id, "hospitalizacion");
                return _context51.abrupt("return", true);

              case 18:
              case "end":
                return _context51.stop();
            }
          }
        }, _callee51);
      }));

      function create2(_x13, _x14, _x15) {
        return _create.apply(this, arguments);
      }

      return create2;
    }()
  }, {
    key: "create",
    value: function create(data) {
      var user_id = data.user_id;
      $("#modelId").modal("show");
      $(".modal-body").html("\n            <div id=\"infoPaciente\" style=\"position: absolute;width: 100%;top:0;left:0;z-index: 200000;\"></div>\n            <div  class=\"row pt-6\" style=\" margin-top: 7.5rem !important;\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 text-primary\">\n                    Temperatura  <span id=\"info\" title=\"Ayuda\" class=\"c-pointer text-success heartbeat float-right h5\"><i class=\"fa fa-question-circle\" aria-hidden=\"true\"></i></span>\n                </div>\n            </div>\n            <div id=\"form\" class=\"row\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <input type=\"number\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"value\" id=\"value\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <input type=\"datetime-local\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <textarea class=\"form-control form-control-lg bg-gray-footer-parodi mb-3\" name=\"sintoma_observacion\" id=\"sintoma_observacion\" rows=\"2\" placeholder=\"Observaci\xF3n\"></textarea>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                </div>\n\n\n            </div>\n            </div>\n\n            <div class=\"botones-bottom-modal\">\n                <button id=\"user_cuest_temperatura_store\" class=\"font-weight-bold btn btn-success mb-1 btn-block btn-lg\">Agregar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n\n            </div>\n        ");
      _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_3__["default"].infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        UserCuestHistoria.indexHistoria({
          user_id: user_id
        });
      });
      $("#user_cuest_temperatura_store").on("click", function () {
        alert("aaaa");
      });
    }
  }, {
    key: "edit",
    value: function () {
      var _edit = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee52(selector, user_id, episode) {
        var formdata, option_user_id_medico_tratante;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee52$(_context52) {
          while (1) {
            switch (_context52.prev = _context52.next) {
              case 0:
                formdata = new FormData();
                formdata.append("user_id", user_id);
                formdata.append("episode", episode);
                formdata.append("_token", $("#_token").val());
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_ficha_medica/getLastFichaMedica", formdata).then(function (response) {
                  if (response.length > 0) {
                    $("#grupo_sanguineo").val(response[0]['grupo_sanguineo']);
                    $("#peso").val(response[0]['peso']);
                    $("#talla").val(response[0]['talla']);
                    $("#estatura").val(response[0]['estatura']);
                  }
                });
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_historia_medica/show", formdata).then(function (user_cuest_historia_medica) {
                  $.each(user_cuest_historia_medica, function (indexInArray, valueOfElement) {
                    $("#".concat(indexInArray)).val(valueOfElement);

                    if (indexInArray == "cat_user_especialidad_id") {
                      formdata.append("cat_user_especialidad_id", valueOfElement);
                      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_especialidad/index", formdata).then(function (user_especialidad) {
                        var option = "<option value=''>Seleccione Médico Tratante</option>";
                        $.each(user_especialidad, function (indexInArray, medico) {
                          option += "<option  ".concat(medico.id == user_cuest_historia_medica['user_id_medico_tratante'] ? 'selected' : '', " value='").concat(medico.id, "'>").concat(medico.nombres, " ").concat(medico.apellidos, "</option>");
                        });
                        $("#user_id_medico_tratante").html(option);
                      });
                    }
                  }); //return response;
                });

              case 6:
              case "end":
                return _context52.stop();
            }
          }
        }, _callee52);
      }));

      function edit(_x16, _x17, _x18) {
        return _edit.apply(this, arguments);
      }

      return edit;
    }()
  }, {
    key: "show",
    value: function show(selector, user_id, episode) {
      UserCuestHistoria.create(selector, user_id).then(function (response) {
        UserCuestHistoria.edit(selector, user_id);
      });
    }
  }, {
    key: "store",
    value: function store(user_id, episode) {
      if (UserCuestHistoria.store_validate()) {
        var formdata = new FormData();
        /*
        formdata.append("episode", episode)
        formdata.append("inicio_episodio", timestamps())
            //ingreso
        formdata.append("i_motivo_consulta", $("#i_motivo_consulta").val())
        formdata.append("i_enfermedad_actual", $("#i_enfermedad_actual").val())
        formdata.append("i_examen_fisico", $("#i_examen_fisico").val())
        formdata.append("i_diagnostico", $("#i_diagnostico").val())
        formdata.append("i_plan_trabajo", $("#i_plan_trabajo").val())
          formdata.append("cat_antecedente_personal_id", JSON.stringify($('[name="cat_ant_personal_id[]"]').serializeArray()))
        formdata.append("cat_antecedente_familiar_id", JSON.stringify($('[name="cat_ant_familiar_id[]"]').serializeArray()))
            formdata.append("user_id_medico_tratante", $("#user_id_medico_tratante").val())
        formdata.append("cat_user_especialidad_id", $("#cat_user_especialidad_id").val())*/

        formdata.append("user_id", user_id);
        formdata.append("_token", $("#_token").val());
        formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["timestamps"])());
        return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])("user_cuest_historia_medica/store", formdata);
      } else {
        return false;
      }
    }
  }, {
    key: "destroy",
    value: function destroy(user_cuest_historia_medica_id) {
      var formdata = new FormData();
      formdata.append("_token", $("#_token").val());
      formdata.append("user_cuest_historia_medica_id", user_cuest_historia_medica_id);
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])("/user_cuest_historia_medica/destroy", formdata);
    }
    /*
    getAll(user_id, selector) {
        let formdata = new FormData();
        formdata.append("user_id", user_id);
        formdata.append("_token", $("#_token").val());
        let result = post("user_cuest_historia_medica/index", formdata)
        result.then(response => {
            let row;
            if (response.length == 0) {
                row += `
                    <tr>
                        <td colspan="5" class="text-center">
                            No existen Episodios
                        </td>
                    </tr>
                `;
            } else {
                $.each(response, function(indexInArray, valueOfElement) {
                    if (valueOfElement['fin_episodio'] == "Activo") {
                        $("#user_cuest_historia_medica_create").slideUp('slow')
                    }
                    row += UserCuestHistoria.rowTableIndex(valueOfElement);
                });
            }
              $(selector).html(row);
          })
        return result;
    }*/

  }, {
    key: "rowTableIndex",
    value: function rowTableIndex(valueOfElement) {
      return "\n            <tr id=\"row_index".concat(valueOfElement.id, "\">\n                <td class=\"text-center\">\n                    ").concat(valueOfElement.episode, "\n                </td>\n                <td class=\"text-center\">\n                    ").concat(valueOfElement.inicio_episodio, "\n                </td>\n                <td class=\"text-center\">\n                    ").concat(valueOfElement.fin_episodio, "\n                </td>\n                <td class=\"text-center\">\n                    ").concat(valueOfElement.medico_tratante, "\n                </td>\n                <td >\n                    <button data-option=\"").concat(valueOfElement.episode, "\" title=\"Editar ingreso\" class=\"btn btn-primary user_cuest_historia_medica_edit ").concat(valueOfElement.fin_episodio != "Activo" ? 'd-none' : '', "\"><i class=\"fa fa-edit\" aria-hidden=\"true\"></i></button>\n                    <!--<button data-option=\"").concat(valueOfElement.id, "\" title=\"Finalizar Episodio\" class=\"btn btn-success user_cuest_historia_medica_destroy ").concat(valueOfElement.fin_episodio != "Activo" ? 'd-none' : '', "\"><i class=\"fa fa-plus\" aria-hidden=\"true\"></i></button>-->\n                    <button data-option=\"").concat(valueOfElement.id, "\" title=\"Finalizar Episodio\" class=\"btn btn-success user_cuest_historia_medica_egreso ").concat(valueOfElement.fin_episodio != "Activo" ? 'd-none' : '', "\"><i class=\"fas fa-sign-out-alt\"></i></button>\n                    <button data-option=\"").concat(valueOfElement.id, "\" title=\"Informe Egreso\" class=\"btn btn-outline-danger ").concat(valueOfElement.fin_episodio == "Activo" ? 'd-none' : '', "\"><i class=\"far fa-file-pdf\"></i></button>\n                </td>\n            </tr>\n        ");
    }
  }, {
    key: "store_validate",
    value: function store_validate() {
      var message;
      var model1 = $("#cat_user_especialidad_id");
      var model2 = $("#user_id_medico_tratante");
      var model3 = $("#i_motivo_consulta");
      var model4 = $("#i_enfermedad_actual");
      var model5 = $("#i_examen_fisico");
      var model6 = $("#i_diagnostico");
      var model7 = $("#i_plan_trabajo");

      if (model1.val() == "") {
        message = Component.alertMessage('input_select_empty');
        alert(message['message']);
        model1.focus();
        return false;
      }

      if (model2.val() == "") {
        message = Component.alertMessage('input_select_empty');
        alert(message['message']);
        model2.focus();
        return false;
      }

      if (model3.val() == "") {
        message = Component.alertMessage('input_text_empty');
        alert(message['message']);
        model3.focus();
        return false;
      }

      if (model4.val() == "") {
        message = Component.alertMessage('input_text_empty');
        alert(message['message']);
        model4.focus();
        return false;
      }

      if (model5.val() == "") {
        message = Component.alertMessage('input_text_empty');
        alert(message['message']);
        model5.focus();
        return false;
      }

      if (model6.val() == "") {
        message = Component.alertMessage('input_text_empty');
        alert(message['message']);
        model6.focus();
        return false;
      }

      if (model7.val() == "") {
        message = Component.alertMessage('input_text_empty');
        alert(message['message']);
        model7.focus();
        return false;
      }

      return true;
    }
  }, {
    key: "getHistorial",
    value: function () {
      var _getHistorial = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee53(user_id) {
        var formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee53$(_context53) {
          while (1) {
            switch (_context53.prev = _context53.next) {
              case 0:
                formdata = new FormData();
                formdata.append("user_id", user_id);
                formdata.append("_token", $("#_token").val());
                _context53.next = 5;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_2__["post"])(location.origin + "/user_cuest_historia_medica/getHistorial", formdata);

              case 5:
                return _context53.abrupt("return", _context53.sent);

              case 6:
              case "end":
                return _context53.stop();
            }
          }
        }, _callee53);
      }));

      function getHistorial(_x19) {
        return _getHistorial.apply(this, arguments);
      }

      return getHistorial;
    }()
  }, {
    key: "regresar",
    value: function regresar(_ref8) {
      var user_id = _ref8.user_id,
          _ref8$selector = _ref8.selector,
          selector = _ref8$selector === void 0 ? '#modalTemplate_2_footer' : _ref8$selector;
      Component.btn_regresar({
        selector: selector,
        eventModel: "UserCuestHistoria.index('.modal-body'," + user_id + ")"
      });
    }
  }]);

  return UserCuestHistoria;
}();



/***/ }),

/***/ "./public/class_js/UserCuestImpresionDiagnostica.js":
/*!**********************************************************!*\
  !*** ./public/class_js/UserCuestImpresionDiagnostica.js ***!
  \**********************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestImpresionDiagnostica; });
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }


var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_0__["default"]();

var UserCuestImpresionDiagnostica = /*#__PURE__*/function () {
  function UserCuestImpresionDiagnostica() {
    _classCallCheck(this, UserCuestImpresionDiagnostica);
  }

  _createClass(UserCuestImpresionDiagnostica, null, [{
    key: "create",
    value: function create(selector, user_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"h1 text-primary\">\n                Probabilidad diagn\xF3stica\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"value\" id=\"value\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n            <div class=\"input-group my-1 d-none\">\n                <div class=\"input-group-prepend\">\n                    <span class=\"input-group-text\" id=\"inputGroupFileAddon01\">\n                        <img style=\"width: 2em;height: 2em;\" src=\"/image/system/carga1-success.svg\">\n                    </span>\n                </div>\n                <div class=\"custom-file\">\n                    <input type=\"file\" class=\"custom-file-input\" id=\"file\" aria-describedby=\"file\">\n                    <label class=\"custom-file-label\" style=\"height: calc(2.7rem + 3px);\" for=\"inputGroupFile01\">Pulsa aqu\xED para subir archivos</label>\n                </div>\n            </div>\n            <div class=\"row d-none\" id=\"file_temp\">\n\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"font-weight-bold btn btn-success w-100 mr-1\">Agregar</button>\n                <button onclick=\"UserCuestHistoria.index('".concat(selector, "', ").concat(user_id, ")\" id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n            </div>\n\n        "));
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#user_cuest_model_store").on("click", function () {
        if (UserCuestImpresionDiagnostica.validate()) {
          UserCuestImpresionDiagnostica.store(user_id).then(function (response) {
            UserCuestHistoria.getHistorial(user_id).then(function (response) {
              if (Object.keys(response).length > 0) {
                pacientes_datos.map(function (x) {
                  if (x.user_id == user_id) {
                    x.resultados = response;
                  }
                });
              } else {
                pacientes_datos.map(function (x) {
                  if (x.user_id == user_id) {
                    x.resultados = [];
                  }
                });
              }

              UserCuestHistoria.index(".modal-body", user_id);
            });
          });
        }
      });
    }
  }, {
    key: "store",
    value: function store(user_id) {
      var formdata = new FormData();
      var value = $("#value");
      formdata.append("value", value.val());
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", timestamps());
      return post(location.origin + "/user_cuest_impresion_diagnostica/store", formdata);
    }
  }, {
    key: "store3",
    value: function store3(user_id, data) {
      var formdata = new FormData();
      formdata.append("value", data.value);
      formdata.append("user_id_paciente", data.user_id_paciente);
      formdata.append("episodio_id", data.episodio_id);
      formdata.append("created_at", timestamps());
      formdata.append("_token", $("#_token").val());
      return post(location.origin + "/user_impresion_diagnostica/store3", formdata);
    }
  }, {
    key: "store2",
    value: function store2(_ref) {
      var user_id = _ref.user_id,
          identificador = _ref.identificador;

      /*
      let diagn_ingre= []
      let diagn_ingre_c = $('input[name="diagn_ingre_c"]');
      let diagn_ingre_d = $('input[name="diagn_ingre_d"]');
      let diagn_ingre_v = $('input[name="diagn_ingre_v"]');
        for (let index = 0; index < diagn_ingre_c.length; index++) {
            if (
              diagn_ingre_v[index]['value'] != "" &&
              diagn_ingre_d[index]['value'] != "" &&
              diagn_ingre_c[index]['value'] != ""
          ) {
                diagn_ingre.push({
                  user_cuest_episodio_id,
                  id                 : $("#input-title-"+index).data('imp_diagn_id'),
                  value              : diagn_ingre_v[index]['value'],
                  cod_cie            : diagn_ingre_c[index]['value'],
                  cod_cie_description: diagn_ingre_d[index]['value']
              })
          }
        }*/
      var formdata = new FormData();
      formdata.append("model", JSON.stringify(diagn_ingre));
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", timestamps());
      return post(location.origin + "/user_cuest_impresion_diagnostica/store2", formdata);
    }
  }, {
    key: "update",
    value: function update(_ref2) {
      var user_id = _ref2.user_id,
          user_cuest_imp_diagn_id = _ref2.user_cuest_imp_diagn_id,
          cod_cie = _ref2.cod_cie,
          cod_cie_description = _ref2.cod_cie_description,
          value = _ref2.value,
          user_cuest_episodio_id = _ref2.user_cuest_episodio_id;

      if (user_cuest_imp_diagn_id !== undefined) {
        var formdata = new FormData();
        formdata.append("user_cuest_imp_diagn_id", user_cuest_imp_diagn_id);
        formdata.append("value", value);
        formdata.append("cod_cie", cod_cie);
        formdata.append("cod_cie_description", cod_cie_description);
        formdata.append("user_cuest_episodio_id", user_cuest_episodio_id);
        formdata.append("user_id", user_id);
        formdata.append("_token", $("#_token").val());
        formdata.append("created_at", timestamps());
        return post(location.origin + "/user_cuest_impresion_diagnostica/update", formdata);
      }
    }
  }, {
    key: "update2",
    value: function update2(user_id_paciente, user_cuest_imp_diagn_id, model) {
      //entrega de guardias
      var message = Component.alertMessage('update_row_cuestion');
      Swal.fire({
        icon: message['image'],
        title: message['message'],
        showDenyButton: true,
        showCancelButton: false,
        confirmButtonText: "Si"
      }).then(function (result) {
        if (result.isConfirmed) {
          var formdata = new FormData();
          formdata.append("id", user_cuest_imp_diagn_id);
          formdata.append("value", $("#input_".concat(user_cuest_imp_diagn_id, "_").concat(model)).val());
          formdata.append("_token", $("#_token").val());
          formdata.append("created_at", timestamps());
          post(location.origin + "/user_cuest_impresion_diagnostica/update2", formdata).then(function (response) {
            pacientes_datos.map(function (x) {
              if (x.user_id == user_id_paciente) {
                x.resultados.map(function (y) {
                  //console.log(y.id, user_cuest_imp_diagn_id)
                  if (y.id == user_cuest_imp_diagn_id && y.description == "Probabilidad diagnóstica") {
                    //console.log("antes ",y)
                    y.value = $("#input_".concat(user_cuest_imp_diagn_id, "_").concat(model)).val(); //console.log("despues ",y)
                  }
                });
              }
            });
            showHide("#update_".concat(user_cuest_imp_diagn_id, "_").concat(model));
            showHide("#description_".concat(user_cuest_imp_diagn_id, "_").concat(model));
            $("#description_".concat(user_cuest_imp_diagn_id, "_").concat(model)).html($("#input_".concat(user_cuest_imp_diagn_id, "_").concat(model)).val());
          });
        }
      });
    }
  }, {
    key: "show",
    value: function show(user_id) {
      var formdata = new FormData();
      formdata.append("_token", $("#_token").val());
      return post(location.origin + "/user_cuest_impresion_diagnostica/show/" + user_id, formdata);
    }
  }, {
    key: "destroy2",
    value: function destroy2(user_id_paciente, user_cuest_imp_diagn_id) {
      //entrega de guardias
      var message = Component.alertMessage('destroy_row_cuestion');
      Swal.fire({
        icon: message['image'],
        title: message['message'],
        showDenyButton: true,
        showCancelButton: false,
        confirmButtonText: "Si"
      }).then(function (result) {
        if (result.isConfirmed) {
          get(location.origin + "/user_cuest_impresion_diagnostica/destroy/" + user_cuest_imp_diagn_id).then(function (response) {
            UserCuestHistoria.getHistorial(user_id_paciente).then(function (response) {
              if (Object.keys(response).length > 0) {
                pacientes_datos.map(function (x) {
                  if (x.user_id == user_id_paciente) {
                    x.resultados = response;
                  }
                });
              } else {
                pacientes_datos.map(function (x) {
                  if (x.user_id == user_id_paciente) {
                    x.resultados = [];
                  }
                });
              }

              $("#row_".concat(user_cuest_imp_diagn_id, "_impDiagn")).slideUp("slow");
            });
          });
        }
      });
    }
  }, {
    key: "destroy",
    value: function destroy(id, user_id) {
      var message = Component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        get(location.origin + "/user_cuest_impresion_diagnostica/destroy/" + id).then(function (response) {
          UserCuestHistoria.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "destroycie11",
    value: function destroycie11(id, n) {
      var message = Component.alertMessage('destroy_row_cuestion');
      Swal.fire({
        icon: message['image'],
        title: message['message'],
        showDenyButton: true,
        showCancelButton: false,
        confirmButtonText: "Si"
      }).then(function (result) {
        if (result.isConfirmed) {
          get(location.origin + "/user_cuest_impresion_diagnostica/destroy/" + id).then(function (response) {
            $(".row-impDiagn-".concat(id)).slideUp("slow");
            !unde(n) ? $(".row-impDiagn-".concat(n)).slideUp("slow") : cl(n);
          });
        } else if (result.isDenied) {//Swal.fire('Changes are not saved', '', 'info')
        }
      });
    }
  }, {
    key: "validate",
    value: function validate() {
      var value = $("#value");

      if (value.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        value.trigger("focus");
        return false;
      }

      return true;
    }
  }]);

  return UserCuestImpresionDiagnostica;
}();



/***/ }),

/***/ "./public/class_js/UserCuestOximetria.js":
/*!***********************************************!*\
  !*** ./public/class_js/UserCuestOximetria.js ***!
  \***********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestOximetria; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__["default"]();

var UserCuestOximetria = /*#__PURE__*/function () {
  function UserCuestOximetria() {
    _classCallCheck(this, UserCuestOximetria);
  }

  _createClass(UserCuestOximetria, null, [{
    key: "index",
    value: function index(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                Oximetr\xEDa\n                </h1>\n                <span id=\"user_cuest_model_create\" class=\"text-shadow-drop-tl text-primary mr-2\" style=\"float:right;font-size: 2rem !important; cursor:pointer;\">\n                    Nuevo valor\n                    <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                </span>\n            </div>\n            <div class=\"flex-fill overflow-auto mb-1\">\n                <table class=\"table table-bordered mb-3\">\n                    <tr>\n                        <th class=\"text-primary text-center\">Fecha</th>\n                        <th class=\"text-primary text-center\">Valor</th>\n                        <th class=\"text-primary text-center\">Observaci\xF3n</th>\n                        <th class=\"text-primary text-center\">Acci\xF3n</th>\n                    </tr>\n                    <tbody id=\"filas\">\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center\">\n                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                    <span class=\"sr-only\">Loading...</span>\n                                </div>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n            </div>\n\n            <div class=\"d-flex\">\n                <a class='btn btn-primary btn-block' data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</a>\n\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      var formdata = new FormData();
      formdata.append("user_id", user_id);
      formdata.append("episodio_id", episodio_id);
      formdata.append("_token", $("#_token").val());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_oximetria/index", formdata).then(function (response) {
        var filas = "";
        var tempRow = "";
        var dia = "";
        var count = 0;
        var m = "";
        var d = "";

        if (response.length > 0) {
          $(response).each(function (index, element) {
            d = new Date(element.created_at);

            if (dia != d.getDate()) {
              dia = d.getDate();
              count++;
              m = count;
            } else {
              m = "";
            }

            tempRow = "\n                            <tr id=\"temp_".concat(element.id, "\">\n                                <td class=\"text-center\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["jsUcfirst"])(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["meses"])(d.getMonth())) + "/" + d.getDate() + " a las " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["formatAMPM"])(d), "</td>\n                                <td class=\"text-center font-weight-bold ").concat(UserCuestOximetria.colorOxi(element.value, 1), "\" id=\"modal-temp-").concat(element.id, "\">").concat(element.value, "%</td>\n                                <td class=\"text-center\">\n                                    ").concat(element.coments != null ? element.coments : "", "\n                                </td>\n                                <td class=\"text-center\">\n\n                                </td>\n                                <td align=\"center\">\n                                <!-- <button title=\"Eliminar valor\" onclick=\"UserCuestOximetria.eliminar(").concat(element.id, ",").concat(user_id, ")\" class=\"delete-row btn btn-danger\" data-option=\"").concat(element.id, "\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button>-->\n                                </td>\n                            </tr>\n                        ");
            filas = filas + tempRow;
          });
        } else {
          filas = "\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center text-primary\">Sin registros</td>\n                        </tr>\n                    ";
        }

        $("#filas").html(filas);
      });
      $("#user_cuest_model_create").on('click', function () {
        UserCuestOximetria.create(".modal-body", user_id, episodio_id);
      });
    }
  }, {
    key: "create",
    value: function create(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex \">\n                <h1 class=\"h1 text-primary\">\n                    Oximetr\xEDa\n                </h1>\n            </div>\n            <div class=\"d-flex mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0 mr-1\">\n                    <input type=\"number\" class=\"form-control bg-gray-footer-parodi\" name=\"cat_user_cuestionario_73\" id=\"cat_user_cuestionario_73\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0\">\n                    <input type=\"datetime-local\" class=\"form-control bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"sintoma_observacion\" id=\"sintoma_observacion\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"mr-1 font-weight-bold btn btn-success w-100\">Agregar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        UserCuestOximetria.index(selector, user_id, episodio_id);
      });
      $("#user_cuest_model_store").on("click", function () {
        UserCuestOximetria.store(user_id, episodio_id).then(function (response) {
          var valor = $("#cat_user_cuestionario_73").val();
          UserCuestOximetria.columnaValor("#col_11_".concat(user_id), {
            'user_id': user_id,
            'valor': valor,
            'episodio_id': episodio_id
          });
          /* if (valor < 94) {
              Component.newsSignos(".marquee-with-options");
          } */

          UserCuestOximetria.index('.modal-body', user_id, episodio_id);
        });
      });
    }
  }, {
    key: "store",
    value: function store(user_id, episodio_id) {
      var message;
      var formdata = new FormData();
      var cat_user_cuestionario_73 = $("#cat_user_cuestionario_73");
      var created_at = $("#created_at");
      var sintoma_observacion = $("#sintoma_observacion");

      if (cat_user_cuestionario_73.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        cat_user_cuestionario_73.on('focus');
        return false;
      }

      if (created_at.val() == "") {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])();
      } else {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps2"])(created_at.val());
      }

      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("cat_user_cuestionario_73", cat_user_cuestionario_73.val());
      formdata.append("created_at", created_at);
      formdata.append("sintoma_observacion", sintoma_observacion.val());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_oximetria/store", formdata);
    }
  }, {
    key: "store2",
    value: function store2(user_id) {
      var formdata = new FormData();
      var value = $("#model_oxi");
      formdata.append("user_id", user_id);
      formdata.append("cat_user_cuestionario_73", value.val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_oximetria/store", formdata);
    }
  }, {
    key: "store3",
    value: function store3(episodio_id, user_id, value) {
      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("cat_user_cuestionario_73", value);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_oximetria/store", formdata);
    }
  }, {
    key: "destroy",
    value: function destroy(id) {
      var formdata = new FormData();
      formdata.append("id", id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_oximetria/destroy", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var message = Component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        UserCuestOximetria.destroy(row_id).then(function (response) {
          if (response.length > 0) {
            UserCuestOximetria.columnaValor("#col_11_".concat(user_id), {
              'user_id': user_id,
              'valor': response[0].value
            });
          } else {
            $("#modelId").modal("hide");
            UserCuestOximetria.columnaValor("#col_11_".concat(user_id), {
              'user_id': user_id,
              'valor': null
            });
          }

          UserCuestOximetria.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "colorOxi",
    value: function colorOxi(oxi) {
      if (oxi <= 90) {
        return "text-danger";
      } else if (oxi >= 91 && oxi <= 93.9) {
        return "text-warning";
      } else if (oxi >= 94) {
        return "text-success";
      }
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      $(selector).html("\n            <div onclick=\"UserCuestOximetria.index('.modal-body',".concat(data.user_id, ",").concat(data.episodio_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    OXIMETR\xCDA\n                </div>\n                ").concat(data.valor == "" || data.valor == null ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + UserCuestOximetria.colorOxi(data.valor) + "'>" + data.valor + " %</span>", "\n            </div>\n        "));
    }
  }]);

  return UserCuestOximetria;
}();



/***/ }),

/***/ "./public/class_js/UserCuestPaciente.js":
/*!**********************************************!*\
  !*** ./public/class_js/UserCuestPaciente.js ***!
  \**********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestPaciente; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _MedicosPaciente_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./MedicosPaciente.js */ "./public/class_js/MedicosPaciente.js");
/* harmony import */ var _UserProfile_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./UserProfile.js */ "./public/class_js/UserProfile.js");
/* harmony import */ var _User_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./User.js */ "./public/class_js/User.js");
/* harmony import */ var _UserCuestDireccion_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./UserCuestDireccion.js */ "./public/class_js/UserCuestDireccion.js");
/* harmony import */ var _CatUserMunicipio_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./CatUserMunicipio.js */ "./public/class_js/CatUserMunicipio.js");
/* harmony import */ var _CatUserEstado_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./CatUserEstado.js */ "./public/class_js/CatUserEstado.js");
/* harmony import */ var _UserCuestUbicacion_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./UserCuestUbicacion.js */ "./public/class_js/UserCuestUbicacion.js");
/* harmony import */ var _Component_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./Component.js */ "./public/class_js/Component.js");
/* harmony import */ var _UserType_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./UserType.js */ "./public/class_js/UserType.js");
/* harmony import */ var _UserFamily_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./UserFamily.js */ "./public/class_js/UserFamily.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }












var userProfile = new _UserProfile_js__WEBPACK_IMPORTED_MODULE_3__["default"]();
var user = new _User_js__WEBPACK_IMPORTED_MODULE_4__["default"]();
var userCuestDireccion = new _UserCuestDireccion_js__WEBPACK_IMPORTED_MODULE_5__["default"](); //let userCuestUbicacion = new UserCuestUbicacion();

var userType = new _UserType_js__WEBPACK_IMPORTED_MODULE_10__["default"]();
var userFamily = new _UserFamily_js__WEBPACK_IMPORTED_MODULE_11__["default"]();
var catUserEstado = new _CatUserEstado_js__WEBPACK_IMPORTED_MODULE_7__["default"]();
var catUserMunicipio = new _CatUserMunicipio_js__WEBPACK_IMPORTED_MODULE_6__["default"]();

var UserCuestPaciente = /*#__PURE__*/function () {
  function UserCuestPaciente() {
    _classCallCheck(this, UserCuestPaciente);
  }

  _createClass(UserCuestPaciente, [{
    key: "createPaciente",
    value: function createPaciente() {
      var clase = this; //let index_episodio = pacientes_datos.findIndex(paciente=>paciente.user_id === user_id)
      //let episodio = pacientes_datos[index_episodio]

      $("#modal-patient-item").modal("show");
      document.querySelector("#modal-patient-item .modal-body-2").innerHTML =
      /*html */
      "\n\n            <div id='AppCreatetPatient' class=\"fade-in flex-fill d-flex flex-column  overflow-auto\">\n                <div class=\"container p-0\">\n                    <h3 class=\"text-primary\">\n                        Datos del Paciente\n                    </h3>\n                </div>\n                <div class=\"container\">\n                    <div class=\"row\">\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"cedula\">Documento de identidad</label>\n\n\n                                <div class=\"d-flex align-items-center\">\n                                    <select ref=\"nacionalidad\"\n                                        class=\"flex-shrink-1 form-control bg-gray-footer-parodi\"\n                                        style=\"width: fit-content;\" v-model=\"form.nacionalidad\">\n                                        <option value=\"\">Seleccione</option>\n                                        <option value=\"V\">V</option>\n                                        <option value=\"E\">E</option>\n                                        <option value=\"P\">P</option>\n                                    </select>\n\n                                    <div class=\"input-group\">\n\n                                        <input\n                                            v-model=\"form.cedula\"\n                                            ref=\"cedula\"\n                                            @change=\"validateCedula()\"\n                                            type=\"number\"\n                                            class=\"ml-1 flex-grow-1 form-control bg-gray-footer-parodi\"\n                                        >\n                                        <div class=\"input-group-prepend\">\n                                            <div\n                                                :class=\"{ 'input-group-text p-0 ml-2 border-0 bg-transparent': true, 'd-none': !loadingSearchCedula }\">\n                                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                                    <span class=\"sr-only\">Loading...</span>\n                                                </div>\n                                            </div>\n                                        </div>\n                                    </div>\n                                </div>\n\n\n\n\n                                <small v-if=\"isValidateCedula\" :class=\"['form-text notification',( cedulaExiste ? 'text-danger': 'text-success')]\">\n                                    <i class=\"fas fa-check-circle\"></i>\n                                    <span>C\xE9dula {{cedulaExiste ? ' no disponible para registrar': 'no registrada'}}</span>\n                                </small>\n                            </div>\n\n                        </div>\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n\n                            <div id=\"emailAlternativo\" class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"email_alternativo\">Correo electr\xF3nico\n                                <span v-if=\"is_child\" class=\"font-weight-bold\">(del representante)</span></label>\n                                <input\n                                    @change=\"updateField('email_alternativo')\"\n                                    v-model=\"form.email_alternativo\"\n                                    type=\"email\"\n                                    :class=\"['form-control bg-gray-footer-parodi',{'d-none':!is_child}]\"\n                                    name=\"email_alternativo\"\n                                    id=\"email_alternativo\"\n                                    ref=\"email_alternativo\"\n                                    required\n                                >\n                                <input\n                                    @change=\"updateField('email')\"\n                                    v-model=\"form.email\"\n                                    type=\"email\"\n                                    :class=\"['form-control bg-gray-footer-parodi',{'d-none':is_child}]\"\n                                    name=\"email\"\n                                    id=\"email\"\n                                    ref=\"email\"\n                                    required\n                                >\n\n                                <small id=\"sms_email_alternativo\" class=\"form-text text-danger notification\"></small>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"nombre\">Nombres</label>\n                                <input v-model=\"form.nombres\" type=\"text\" class=\"form-control bg-gray-footer-parodi\"\n                                    name=\"nombres\" id=\"nombres\" aria-describedby=\"helpId\" ref=\"nombres\" required>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"apellido\">Apellidos</label>\n                                <input v-model=\"form.apellidos\" type=\"text\" class=\"form-control bg-gray-footer-parodi\"\n                                    name=\"apellidos\" id=\"apellidos\" aria-describedby=\"helpId\" ref=\"apellidos\" required>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"genero\">G\xE9nero</label>\n                                <select v-model=\"form.genero\" class=\"form-control bg-gray-footer-parodi\" name=\"genero\"\n                                    id=\"genero\" aria-describedby=\"helpId\" ref=\"genero\" required>\n                                    <option value=\"\">Seleccione</option>\n                                    <option value=\"m\">Masculino</option>\n                                    <option value=\"f\">Femenino</option>\n                                </select>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n\n\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"fnacimiento\">Fecha de nacimiento</label>\n                                <div class=\"input-group mb-3\">\n                                    <input\n                                        ref=\"fnacimiento\"\n                                        v-model=\"form.fnacimiento\"\n                                        type=\"date\"\n                                        class=\"form-control bg-gray-footer-parodi\"\n                                    >\n                                    <div class=\"input-group-append\">\n                                        <span class=\"input-group-text\" id=\"basic-addon2\">\n                                        <i  class=\"fas fa-birthday-cake mr-2\"></i> {{ calcularEdad() }} a\xF1os</span>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"telefono_movil\">Tel\xE9fono\n                                    Contacto</label>\n                                <input id=\"telefono_movil\"\n                                    v-model=\"form.telefono_movil\"\n                                    ref=\"telefono_movil\"\n                                    required type=\"text\"\n                                    class=\"form-control bg-gray-footer-parodi\"\n                                    @keyup=\"vPrimerDigito('telefono_movil')\"\n                                >\n                                <small id=\"help_telefono_movil\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n\n                                <label class=\"label-text text-secondary\" for=\"telefono_hogar\">Tel\xE9fono\n                                    Secundario</label>\n                                <input\n                                    v-model=\"form.telefono_hogar\"\n                                    id=\"telefono_hogar\"\n                                    ref=\"telefono_hogar\"\n                                    type=\"text\"\n                                    class=\"form-control bg-gray-footer-parodi\"\n                                    @keyup=\"vPrimerDigito('telefono_hogar')\"\n                                >\n                                <small id=\"help_telefono_hogar\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                            <div class=\"h3\">\n                                Direcci\xF3n\n                            </div>\n                        </div>\n                    </div>\n                    <div id=\"user_cuest_direccion_create\">\n\n                        <div class=\"row\">\n                            <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"cat_estado_id\">Estado</label>\n                                    <select v-model=\"form.cat_estado_id\" class=\"form-control bg-gray-footer-parodi\"\n                                        name=\"cat_estado_id\" id=\"cat_estado_id\" aria-describedby=\"helpId\"\n                                        ref=\"cat_estado_id\" required>\n                                        <option value=\"null\">Seleccione</option>\n                                        <option v-for=\"(item, index) in cat_estados\" :key=\"index\" :value=\"item.id\">\n                                            {{ item.description }}</option>\n                                    </select>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                            <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"cat_municipio_id\">Municipio</label>\n                                    <select v-model=\"form.cat_municipio_id\" class=\"form-control bg-gray-footer-parodi\"\n                                        name=\"cat_municipio_id\" id=\"cat_municipio_id\" aria-describedby=\"helpId\"\n                                        ref=\"cat_municipio_id\" required>\n                                        <option value=\"null\">Seleccione</option>\n                                        <option v-for=\"(item, index) in getCatMunicipio\" :key=\"index\" :value=\"item.id\">\n                                            {{ item.description }}</option>\n                                    </select>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                            <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"description\">Ciudad</label>\n                                    <input v-model=\"form.description\" type=\"text\"\n                                        class=\"form-control bg-gray-footer-parodi\" name=\"description\" id=\"description\"\n                                        aria-describedby=\"helpId\" ref=\"description\" required>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                        </div>\n                        <div class=\"row\">\n                            <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"domicilio\">Domicilio</label>\n                                    <textarea v-model=\"form.domicilio\" class=\"form-control bg-gray-footer-parodi\"\n                                        name=\"domicilio\" id=\"domicilio\" rows=\"2\" ref=\"domicilio\" required></textarea>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                        </div>\n\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-12 col-sm-6\">\n\n                            <div class=\"d-flex align-items-center\">\n                                <div class=\"text-center\">\n\n                                    <img\n                                        onerror=\"this.src='/image/system/icono-paciente-blanco.svg'\"\n                                        id=\"imagen_perfil\"\n                                        style=\"width: 80px; height:80px\"\n                                        :src=\"form.imagen\"\n                                        class=\"imagen-perfil\"\n                                        alt=\"Medic default\"\n                                    >\n\n                                    <br>\n                                   <!--<i id=\"ampliar\"\n                                        class=\"heartbeat cursor-pointer mt-1 fas fa-search-plus text-primary mr-1\"\n                                        style=\"font-size:1.3em;\"></i>\n                                    <i id=\"reducir\" class=\"heartbeat cursor-pointer mt-1 fas fa-search-minus text-primary\"\n                                        style=\"font-size:1.3em;\"></i>-->\n                                </div>\n                                <div class=\"ml-1 flex-fill form-group text-center\">\n                                    <label class=\"label-text text-secondary\" for=\"imagen\">\n                                        Foto\n\n                                    </label>\n                                    <input\n                                        @change=\"validarArchivoImagen('imagen')\"\n                                        type=\"file\"\n                                        class=\"form-control bg-gray-footer-parodi\"\n                                        name=\"imagen\"\n                                        id=\"imagen\"\n                                        aria-describedby=\"helpId\"\n                                        accept=\"image/png, image/gif, image/jpeg\"\n                                    >\n                                    <h6>Solo archivos (.jpg o .png)</h6>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                        </div>\n\n\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"\">Fecha de ingreso:</label>\n                                <input\n                                    ref=\"ingreso\"\n                                    type=\"date\"\n                                    class=\"form-control bg-gray-footer-parodi\"\n                                    v-model=\"form.ingreso\">\n                            </div>\n                        </div>\n                    </div>\n                </div>\n                <div class=\"container\">\n                    <div class=\"row\">\n                        <div class=\"col-12 text-primary\">\n                            <div class=\"h3\">\n                                Traslado\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n\n                        <div class=\"col-12\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"cat_user_ubicacion_id\">\n                                    Ubicaci\xF3n Actual:\n                                </label>\n                                <select @change=\"updateCatUbicacion1()\"\n                                    ref=\"cat_user_ubicacion_id\"\n                                    name=\"cat_user_ubicacion_id\"\n                                    class=\"form-control bg-gray-footer-parodi\"\n                                >\n                                    <option value=\"245\">Sala de Espera (SE)</option>\n                                    <option v-for=\"(item, index) in getCatUbicacionAreas\" :key=\"index\"\n                                        :value=\"item.id\">{{ item.description }}</option>\n                                </select>\n                            </div>\n                        </div>\n                        <div :class=\"['col-12', { 'd-none': catUbicacionTemp2.length === 0 }]\">\n                            <div class=\"form-group\">\n                                <select @change=\"updateCatUbicacion2()\"\n                                    ref=\"cat_user_ubicacion_id_2\" class=\"form-control bg-gray-footer-parodi\">\n                                     <option value=\"\">Seleccione</option>\n                                    <option v-for=\"(item, index) in getCatUbicacionAreas2\" :key=\"index\"\n                                        :value=\"item.id\">{{ item.coments }}</option>\n                                </select>\n                            </div>\n                        </div>\n                        <div :class=\"['col-12', { 'd-none': catUbicacionTemp3.length === 0 }]\">\n                            <div class=\"form-group\">\n                                <select @change=\"updateCatUbicacion3()\"\n                                    ref=\"cat_user_ubicacion_id_3\" class=\"form-control bg-gray-footer-parodi\">\n                                    <option value=\"\">Seleccione</option>\n                                    <option v-for=\"(item, index) in getCatUbicacionAreas3\" :key=\"index\"\n                                        :value=\"item.id\">{{ item.coments }}</option>\n                                </select>\n                            </div>\n                        </div>\n                        <div :class=\"['col-12', { 'd-none': catUbicacionTemp4.length === 0 }]\">\n                            <div class=\"form-group\">\n                                <select @change=\"updateCatUbicacion4()\"\n                                    ref=\"cat_user_ubicacion_id_4\" class=\"form-control bg-gray-footer-parodi\">\n                                    <option value=\"\">Seleccione</option>\n                                    <option v-for=\"(item, index) in getCatUbicacionAreas4\" :key=\"index\"\n                                        :value=\"item.id\">{{ item.coments }}</option>\n                                </select>\n                            </div>\n                        </div>\n                    </div>\n                </div>\n\n                <div class=\"container\">\n                    <div\n                        class=\"row cursor-pointer\"\n                        data-toggle=\"collapse\"\n                        href=\"#collapseFamiliar\"\n                        role=\"button\"\n                        aria-expanded=\"false\"\n                        aria-controls=\"collapseFamiliar\"\n                    >\n                        <div class=\"col-12 text-primary\">\n                            <div class=\"h3\">\n                                Familiar\n                            </div>\n                        </div>\n                    </div>\n                    <div id=\"collapseFamiliar\" class=\"collapse row\">\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"cedula_familiar\">Documento de\n                                    identidad</label>\n\n                                <input v-model=\"form.cedula_familiar\" ref=\"cedula_familiar\" type=\"number\"\n                                    class=\"ml-1 flex-grow-1 form-control bg-gray-footer-parodi\">\n                                <small ref=\"sms_cedula_familiar\" id=\"sms_cedula_familiar\"\n                                    class=\"form-text text-danger notification\">\n                                </small>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"email_familiar\">Correo\n                                    electr\xF3nico</label>\n                                <input ref=\"email_familiar\" v-model=\"form.email_familiar\" type=\"email\"\n                                    class=\"form-control bg-gray-footer-parodi\">\n                                <small id=\"sms_email_familiar\"\n                                    class=\"form-text text-danger notification\"></small>\n                            </div>\n\n                        </div>\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"nombre_familiar\">Nombres</label>\n                                <input ref=\"nombres_familiar\" v-model=\"form.nombres_familiar\" type=\"text\"\n                                    class=\"form-control bg-gray-footer-parodi\">\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\"\n                                    for=\"apellido_familiar\">Apellidos</label>\n                                <input ref=\"apellidos_familiar\" v-model=\"form.apellidos_familiar\" type=\"text\"\n                                    class=\"form-control bg-gray-footer-parodi\">\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"genero_familiar\">G\xE9nero</label>\n                                <select ref=\"genero_familiar\" v-model=\"form.genero_familiar\" type=\"text\"\n                                    class=\"form-control bg-gray-footer-parodi\">\n                                    <option value=\"\">Seleccione</option>\n                                    <option value=\"m\">Masculino</option>\n                                    <option value=\"f\">Femenino</option>\n                                </select>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\"\n                                    for=\"cat_user_family_id\"\n                                >\n                                    V\xEDnculo\n                                </label>\n                                <select ref=\"cat_user_family_id\" v-model=\"form.cat_user_family_id\" type=\"text\"\n                                    class=\"form-control bg-gray-footer-parodi\">\n                                    <option value=\"\">Seleccione</option>\n                                    <option value=\"1\">Abuelo(a)</option>\n                                    <option value=\"2\">Amigo(a)</option>\n                                    <option value=\"3\">Esposo(a)</option>\n                                    <option value=\"4\">Hermano(a)</option>\n                                    <option value=\"5\">Hijo(a)</option>\n                                    <option value=\"6\">Madre</option>\n                                    <option value=\"7\">Padre</option>\n                                    <option value=\"8\">Pareja</option>\n                                    <option value=\"9\">Sobrino(a)</option>\n                                </select>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label\n                                    class=\"label-text text-secondary\"\n                                    for=\"telefono_movil_familiar\"\n                                >\n                                    Tel\xE9fono\n                                    Contacto\n                                </label><br>\n                                <input\n                                    v-model=\"form.telefono_movil_familiar\"\n                                    id=\"telefono_movil_familiar\"\n                                    ref=\"telefono_movil_familiar\"\n                                    type=\"number\"\n                                    class=\"form-control bg-gray-footer-parodi\"\n                                >\n                                <small ref=\"help_telefono_movil_familiar\"\n                                    class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n\n\n                    </div>\n                </div>\n                <div class=\"container\">\n                    <div class=\"row\">\n                        <div class=\"col-6\">\n                            <button @click=\"handle_store()\" id=\"user_cuest_model_update\"\n                                class=\"font-weight-bold btn btn-success w-100\">Registrar Paciente</button>\n                        </div>\n                        <div class=\"col-6\">\n                            <button id=\"regresar\" @click=\"closeModal()\" aria-label=\"Close\"\n                                class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n                        </div>\n                    </div>\n                </div>\n            </div>\n        ";
      var app = new Vue({
        el: "#AppCreatetPatient",
        data: function data() {
          return {
            loadingSearchCedula: false,
            catUbicacionTemp2: [],
            catUbicacionTemp3: [],
            catUbicacionTemp4: [],
            items: pacientes_datos,
            is_child: false,
            loading: document.getElementById("loadingScreen"),
            cat_estados: Array.from(catUserEstado.getAll()),
            telefonoConfig1: null,
            telefonoConfig2: null,
            telefonoConfig3: null,
            dataPaciente: null,
            isValidateCedula: false,
            cedulaExiste: false,
            formPRUEBA: {
              email: null,
              email_alternativo: null,
              nacionalidad: "V",
              cedula: null,
              telefono_movil: "+582126720356",
              imagen: "/image/system/icono-paciente-blanco.svg",
              nombres: "ranses",
              apellidos: "Jimenez",
              genero: "m",
              ingreso: Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaAMD"])(),
              fnacimiento: Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaAMD"])(),
              telefono_hogar: "+582126720356",
              cat_user_type_id: 1,
              cat_estado_id: 1,
              cat_municipio_id: 1,
              description: "Caracas",
              domicilio: "Chacaito",
              cat_user_ubicacion_id: 245,
              nombres_familiar: "Ramon",
              apellidos_familiar: "Perez",
              email_familiar: "a@a.com",
              cedula_familiar: "123456",
              cat_user_family_id: 1,
              genero_familiar: "f",
              telefono_movil_familiar: "+582126720356"
            },
            form: {
              email: null,
              email_alternativo: null,
              nacionalidad: "",
              cedula: null,
              telefono_movil: null,
              imagen: "/image/system/icono-paciente-blanco.svg",
              nombres: null,
              apellidos: null,
              genero: "",
              ingreso: Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaAMD"])(),
              fnacimiento: "",
              telefono_hogar: null,
              cat_user_type_id: null,
              cat_estado_id: null,
              cat_municipio_id: null,
              description: null,
              domicilio: null,
              cat_user_ubicacion_id: 245,
              nombre_familiar: "",
              apellido_familiar: "",
              email_familiar: "",
              cedula_familiar: "",
              cat_user_family_id: "",
              genero_familiar: "m",
              telefono_movil_familiar: ""
            }
          };
        },
        props: {//patient_data: Object,
          //index: Number,
        },
        computed: {
          getCatMunicipio: function getCatMunicipio() {
            var _this = this;

            this.form.cat_municipio_id = 1;
            return Array.from(catUserMunicipio.getAll()).filter(function (item) {
              return item["cat_estado_id"] === _this.form.cat_estado_id;
            });
          },
          getCatUbicacionAreas: function getCatUbicacionAreas() {
            return cat_ubicaciones.filter(function (item) {
              return [2, 28, 41, 42, 70, 98, 126, 154, 182, 211, 223, 390, 391, 237, 230].includes(item["id"]);
            }).sort(function (a, b) {
              return a.orden - b.orden;
            });
          },
          getCatUbicacionAreas2: function getCatUbicacionAreas2() {
            return this.catUbicacionTemp2.sort(function (a, b) {
              return a.orden - b.orden;
            });
          },
          getCatUbicacionAreas3: function getCatUbicacionAreas3() {
            return this.catUbicacionTemp3.sort(function (a, b) {
              return a.orden - b.orden;
            });
          },
          getCatUbicacionAreas4: function getCatUbicacionAreas4() {
            return this.catUbicacionTemp4.sort(function (a, b) {
              return a.orden - b.orden;
            });
          }
        },
        methods: {
          imagePreview: function imagePreview(archivo, selector) {
            var showImageName = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
            // We create the object of the FileReader class
            var reader = new FileReader(); // We read the uploaded file and pass it to our fileReader.

            reader.readAsDataURL(archivo);
            var file = archivo; //We look for the following element where we will show the name of the uploaded file.

            var name = file.name,
                size = file.size,
                type = file.type;

            if (showImageName) {//e.target.nextElementSibling.textContent= name
            } // We tell it that when it is ready, run the internal code.


            reader.onload = function (load) {
              document.getElementById(selector).setAttribute("src", reader.result);
            };
          },
          validarArchivoImagen: function validarArchivoImagen(id) {
            var archivoInput = document.getElementById(id);
            var archivo = archivoInput.files[0];
            console.log(archivo);

            if (archivo) {
              var extension = archivo.name.split(".").pop().toLowerCase();

              if (!(extension === "jpg" || extension === "jpeg" || extension === "png")) {
                Swal.fire({
                  icon: "error",
                  title: "Hubo un error...",
                  text: "Solo se permiten archivos .jpg o .png."
                });
                archivoInput.value = ""; // Limpiar el campo de entrada
              }

              this.imagePreview(archivo, "imagen_perfil");
            }
          },
          updateCatUbicacion1: function updateCatUbicacion1() {
            var _this2 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
                while (1) {
                  switch (_context.prev = _context.next) {
                    case 0:
                      that = _this2;
                      that.form.cat_user_ubicacion_id = 245;
                      that["catUbicacionTemp2"] = [];
                      that["catUbicacionTemp3"] = [];
                      that["catUbicacionTemp4"] = [];

                      if ([null, ""].includes(that.$refs["cat_user_ubicacion_id"].value)) {
                        _context.next = 10;
                        break;
                      }

                      that.form.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id"].value);
                      _context.next = 9;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_ubicacion/show/" + that.form.cat_user_ubicacion_id);

                    case 9:
                      that["catUbicacionTemp2"] = _context.sent.sort(function (a, b) {
                        return a.orden - b.orden;
                      });

                    case 10:
                      console.log({
                        "catUbicacionTemp2": that["catUbicacionTemp2"],
                        "catUbicacionTemp3": that["catUbicacionTemp3"],
                        "catUbicacionTemp4": that["catUbicacionTemp4"],
                        "cat_user_ubicacion_id": that.form.cat_user_ubicacion_id
                      });

                    case 11:
                    case "end":
                      return _context.stop();
                  }
                }
              }, _callee);
            }))();
          },
          updateCatUbicacion2: function updateCatUbicacion2() {
            var _this3 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
                while (1) {
                  switch (_context2.prev = _context2.next) {
                    case 0:
                      that = _this3;
                      that.form.cat_user_ubicacion_id = 245;
                      that["catUbicacionTemp3"] = [];
                      that["catUbicacionTemp4"] = [];

                      if ([null, ""].includes(that.$refs["cat_user_ubicacion_id_2"].value)) {
                        _context2.next = 9;
                        break;
                      }

                      that.form.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_2"].value);
                      _context2.next = 8;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_ubicacion/show/" + that.form.cat_user_ubicacion_id);

                    case 8:
                      that["catUbicacionTemp3"] = _context2.sent.sort(function (a, b) {
                        return a.orden - b.orden;
                      });

                    case 9:
                      console.log({
                        "catUbicacionTemp2": that["catUbicacionTemp2"],
                        "catUbicacionTemp3": that["catUbicacionTemp3"],
                        "catUbicacionTemp4": that["catUbicacionTemp4"],
                        "cat_user_ubicacion_id": that.form.cat_user_ubicacion_id
                      });

                    case 10:
                    case "end":
                      return _context2.stop();
                  }
                }
              }, _callee2);
            }))();
          },
          updateCatUbicacion3: function updateCatUbicacion3() {
            var _this4 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
                while (1) {
                  switch (_context3.prev = _context3.next) {
                    case 0:
                      that = _this4;
                      that.form.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_2"].value);
                      that["catUbicacionTemp4"] = [];

                      if ([null, ""].includes(that.$refs["cat_user_ubicacion_id_3"].value)) {
                        _context3.next = 8;
                        break;
                      }

                      that.form.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_3"].value);
                      _context3.next = 7;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_ubicacion/show/" + that.form.cat_user_ubicacion_id);

                    case 7:
                      that["catUbicacionTemp4"] = _context3.sent.sort(function (a, b) {
                        return a.orden - b.orden;
                      });

                    case 8:
                      console.log({
                        "catUbicacionTemp2": that["catUbicacionTemp2"],
                        "catUbicacionTemp3": that["catUbicacionTemp3"],
                        "catUbicacionTemp4": that["catUbicacionTemp4"],
                        "cat_user_ubicacion_id": that.form.cat_user_ubicacion_id
                      });

                    case 9:
                    case "end":
                      return _context3.stop();
                  }
                }
              }, _callee3);
            }))();
          },
          updateCatUbicacion4: function updateCatUbicacion4() {
            var _this5 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
                while (1) {
                  switch (_context4.prev = _context4.next) {
                    case 0:
                      that = _this5;
                      that.form.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_3"].value);

                      if (![null, ""].includes(that.$refs["cat_user_ubicacion_id_4"].value)) {
                        that.form.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_4"].value);
                      }

                      console.log({
                        "catUbicacionTemp2": that["catUbicacionTemp2"],
                        "catUbicacionTemp3": that["catUbicacionTemp3"],
                        "catUbicacionTemp4": that["catUbicacionTemp4"],
                        "cat_user_ubicacion_id": that.form.cat_user_ubicacion_id
                      });

                    case 4:
                    case "end":
                      return _context4.stop();
                  }
                }
              }, _callee4);
            }))();
          },
          calcularEdad: function calcularEdad() {
            var fechaNacimiento = new Date(this.form.fnacimiento);
            var fechaActual = new Date();
            var diferencia = fechaActual - fechaNacimiento;
            var edadEnMilisegundos = new Date(diferencia); // Extraer el año de la fecha de nacimiento

            var edad = Math.abs(edadEnMilisegundos.getUTCFullYear() - 1970); //console.log(`Edad --> ${edad} años`);
            // alert(edad)

            return String(edad) === "NaN" ? 0 : edad;
          },
          validateForm: function validateForm() {
            var formNotValidate = true;
            var inputName = "nacionalidad";
            var input = this.$refs[inputName];

            if ([null, ""].includes(this.form[inputName]) && formNotValidate) {
              alert("Una Nacionalidad debe estar seleccionada.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "cedula";
            input = this.$refs[inputName];

            if ([null, ""].includes(this.form[inputName]) && formNotValidate) {
              alert("El campo cédula no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "email";
            input = this.$refs[inputName];

            if ([null, ""].includes(this.form[inputName]) && formNotValidate) {
              alert("El campo Correo electrónico no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "nombres";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Nombres no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "apellidos";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Apellidos no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "genero";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Género no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "fnacimiento";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Fecha de Nacimiento no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "telefono_movil";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Teléfono Contacto no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "cat_estado_id";
            input = this.$refs[inputName];
            console.log(this.form[inputName]);

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Estado no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "cat_municipio_id";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Municipio no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "description";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Ciudad no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "domicilio";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Domicilio no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            if (formNotValidate) {
              return true;
            } else {
              return false;
            }
          },
          store: function store() {
            var _this6 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5() {
              var formData, key, element, resultset;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
                while (1) {
                  switch (_context5.prev = _context5.next) {
                    case 0:
                      formData = new FormData();

                      for (key in _this6.form) {
                        if (Object.hasOwnProperty.call(_this6.form, key)) {
                          element = _this6.form[key];
                          console.log(key, _this6.form[key]);

                          if (key === "email") {
                            if (["", null].includes(_this6.form[key])) {
                              formData.append("email", _this6.form["cedula"] + "@correotemporal.com");
                            } else {
                              //if (["", null].includes(this.form[key])) {
                              formData.append("email", _this6.form["email"]); //}
                            }
                          } else {
                            formData.append(key, element);
                          }
                        }
                      }

                      formData.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                      formData.append("_token", $("#_token").val());
                      _context5.next = 6;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/paciente/storePaciente", formData);

                    case 6:
                      resultset = _context5.sent;

                      if (resultset.success) {
                        Swal.fire({
                          icon: "success",
                          title: "Paciente Registrado",

                          /* text: "El paciente ya puede visualizarse desde el area indicada.", */
                          didClose: function didClose() {
                            setTimeout(function () {
                              _this6.$refs.cedula.focus();
                            }, 110);
                            $("#modal-patient-item").modal("hide");
                          }
                        });
                      } else {
                        console.log(resultset);
                      }

                    case 8:
                    case "end":
                      return _context5.stop();
                  }
                }
              }, _callee5);
            }))();
          },
          updateField: function updateField(fieldName) {
            var _this7 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6() {
              var result, _result, inputName, input;

              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
                while (1) {
                  switch (_context6.prev = _context6.next) {
                    case 0:
                      if (!(fieldName === "cedula")) {
                        _context6.next = 5;
                        break;
                      }

                      _context6.next = 3;
                      return _this7.validateCedula();

                    case 3:
                      result = _context6.sent;

                      if (!result.exist) {
                        _this7.is_a_child();
                      }

                    case 5:
                      if (!(fieldName === "email")) {
                        _context6.next = 11;
                        break;
                      }

                      _context6.next = 8;
                      return _this7.validateEmail();

                    case 8:
                      _result = _context6.sent;

                      if (_result) {
                        inputName = "email";
                        input = _this7.$refs[inputName];
                        alert(_this7.form.email + " ya se encuentra asociado a otro paciente.");
                        _this7.form.email = _this7.originalEmail;
                        input.focus();
                      }

                      return _context6.abrupt("return", false);

                    case 11:
                      if (fieldName === "telefono_movil") {
                        /*  this.form.telefono_movil =
                             this.telefonoConfig1.getNumber();
                         return false; */
                      }

                      if (fieldName === "telefono_hogar") {
                        /*   this.form.telefono_hogar =
                              this.telefonoConfig2.getNumber();
                          return false; */
                      }

                    case 13:
                    case "end":
                      return _context6.stop();
                  }
                }
              }, _callee6);
            }))();
          },
          is_a_child: function is_a_child() {
            if (this.form.cedula !== null) {
              var numeroValido = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["validarNumeroConDosDecimales"])(this.form.cedula);

              if (numeroValido) {
                this.is_child = true;
              } else {
                this.is_child = false;
              }
            }
          },
          validateEmail: function validateEmail() {
            var _this8 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee7() {
              var formdata, result;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee7$(_context7) {
                while (1) {
                  switch (_context7.prev = _context7.next) {
                    case 0:
                      if (!(_this8.form.cedula !== "")) {
                        _context7.next = 12;
                        break;
                      }

                      formdata = new FormData();
                      formdata.append("nacionalidad", _this8.form.nacionalidad);
                      formdata.append("cedula", _this8.form.cedula);
                      formdata.append("email", _this8.form.email);
                      formdata.append("_token", $("#_token").val());
                      _context7.next = 8;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/emailExist", formdata);

                    case 8:
                      result = _context7.sent;
                      return _context7.abrupt("return", result);

                    case 12:
                      return _context7.abrupt("return", {
                        exist: false
                      });

                    case 13:
                    case "end":
                      return _context7.stop();
                  }
                }
              }, _callee7);
            }))();
          },
          getDataPaciente: function getDataPaciente() {
            var _this9 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee8() {
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee8$(_context8) {
                while (1) {
                  switch (_context8.prev = _context8.next) {
                    case 0:
                      _context8.next = 2;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])("user_paciente/getdatapaciente/".concat(user_id));

                    case 2:
                      _this9.dataPaciente = _context8.sent;

                    case 3:
                    case "end":
                      return _context8.stop();
                  }
                }
              }, _callee8);
            }))();
          },
          validarNumeroConDosDecimales: function validarNumeroConDosDecimales(inputValue) {
            var regex = /^\d+\.\d{2}$/;
            return regex.test(inputValue);
          },
          validateCedula: function validateCedula() {
            var _this10 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee9() {
              var numeroValido, response;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee9$(_context9) {
                while (1) {
                  switch (_context9.prev = _context9.next) {
                    case 0:
                      numeroValido = _this10.validarNumeroConDosDecimales(_this10.form.cedula);

                      if (numeroValido) {
                        _this10.form.email = "".concat(_this10.form.cedula, "@correotemporal.com");
                        _this10.is_child = true;
                      } else {
                        _this10.is_child = false;
                        _this10.form.email = "";
                      }

                      _context9.prev = 2;
                      _this10.loadingSearchCedula = true;
                      _context9.next = 6;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/getPatient/" + _this10.form.cedula);

                    case 6:
                      response = _context9.sent;
                      console.log(response);
                      _this10.loadingSearchCedula = false;

                      if (response.exist) {
                        //this.$refs.sms_cedula.innerHTML = "<i class='fas fa-exclamation-triangle'></i> La cédula está registrada"
                        if (response.episodesOpen.length === 0) {
                          Swal.fire({
                            title: "Cédula registrada!",
                            html: "\n                                                La c\xE9dula <b>".concat(_this10.form.cedula, "</b> est\xE1 asociada al paciente:\n                                                <table class=\"table table-bordered\">\n                                                    <tr>\n                                                        <th>Nombres:</th>\n                                                        <th>Apellidos:</th>\n                                                        <th>Ubicaci\xF3n actual:</th>\n                                                    </tr>\n                                                    <tr>\n                                                        <td>").concat(response.user.profile.nombres, "</td>\n                                                        <td>").concat(response.user.profile.apellidos, "</td>\n                                                        <td>").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(response.episodesOpen.ubicacion_description) ? "" : response.episodesOpen.ubicacion_description, "</td>\n                                                    </tr>\n                                                </table>\n                                                <h4>\xBFQuieres reingresarlo? </h4>\n                                                Esta acci\xF3n crear\xE1 un nuevo episodio.\n                                            "),
                            allowOutsideClick: false,
                            icon: "warning",
                            showCancelButton: true,
                            confirmButtonColor: "#3085d6",
                            cancelButtonColor: "#d33",
                            confirmButtonText: "De acuerdo!",
                            cancelButtonText: "No!"
                          }).then(function (result) {
                            if (result.isConfirmed) {
                              clase.handle_ReingresoPaciente(response.user.profile);
                            } else {
                              setTimeout(function () {
                                _this10.form.cedula = "";

                                _this10.$refs.cedula.focus();
                              }, 110);
                            }
                          });
                        } else {
                          Swal.fire({
                            title: "¡Episodio activo!",
                            html: "\n                                                La c\xE9dula <b>".concat(_this10.form.cedula, "</b> est\xE1 asociada al paciente:\n                                                <table class=\"table table-bordered\">\n                                                    <tr>\n                                                        <th>Nombres:</th>\n                                                        <th>Apellidos:</th>\n                                                        <th>Ubicaci\xF3n actual:</th>\n                                                    </tr>\n                                                    <tr>\n                                                        <td>").concat(response.user.profile.nombres, "</td>\n                                                        <td>").concat(response.user.profile.apellidos, "</td>\n                                                        <td>").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(response.episodesOpen.ubicacion_description) ? "" : response.episodesOpen.ubicacion_description, "</td>\n                                                    </tr>\n                                                </table>\n\n                                            "),
                            allowOutsideClick: false,
                            icon: "warning",
                            showCancelButton: false,
                            confirmButtonColor: "#3085d6",
                            cancelButtonColor: "#d33",
                            confirmButtonText: "Ok!",
                            cancelButtonText: "Cancelar!"
                          });
                          /* .then(async (result) => {
                          if (result.isConfirmed) {
                              //this.areaSiglas =  [null,undefined,'undefined'].includes(localStorage.getItem('area')) ? 'tp': localStorage.getItem('area')
                              //this.selectorAreaName.textContent = this.items.find(x=>x.siglas === this.areaSiglas).name
                              this.loading.classList.remove("d-none")
                              pacientes_datos = await get( location.origin+"/vistas/" + this.areaSiglas )
                              EventBus.$emit('externalVarChanged', pacientes_datos);
                              //that.AppBtnTotales(pacientes_datos)
                              this.loading.classList.add("d-none")
                                  $("#exampleModal").modal("hide")
                            }
                          }); */
                        }
                      } else {//this.$refs.sms_cedula.innerHTML = "<i class='fas fa-check-circle text-success'></i><span class='text-success'> Cédula no registrada</span>"
                      }

                      return _context9.abrupt("return", false);

                    case 13:
                      _context9.prev = 13;
                      _context9.t0 = _context9["catch"](2);
                      console.log(_context9.t0);
                      _this10.loadingSearchCedula = false;

                    case 17:
                    case "end":
                      return _context9.stop();
                  }
                }
              }, _callee9, null, [[2, 13]]);
            }))();
          },

          /* async validateCedula() {
              let that = this
              if (this.form.cedula !== "") {
                  let formdata = new FormData();
                  formdata.append("nacionalidad", this.form.nacionalidad)
                  formdata.append("cedula", this.form.cedula)
                  formdata.append("_token", $("#_token").val())
                  let result = await get(location.origin + "/getPatient/" + this.form.cedula)
                  this.isValidateCedula=true
                  if (result.exist) {
                        this.cedulaExiste=true
                      alert("La cédula "+this.form.cedula+" ya se encuentra asociada a un paciente.")
                      this.form['cedula'] = this.originalCedula
                  }else{
                      this.cedulaExiste=false
                  }
                  console.log("cedula_encontrada->", result)
                    return result
              } else {
                  return {
                      exist: false
                  }
              }
          }, */
          vPrimerDigito: function vPrimerDigito(param) {
            Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["validarPrimerDigito"])(param);
          },
          handle_store: function handle_store() {
            var _this11 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee10() {
              var that, response;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee10$(_context10) {
                while (1) {
                  switch (_context10.prev = _context10.next) {
                    case 0:
                      that = _this11;

                      if (!that.validateForm()) {
                        _context10.next = 6;
                        break;
                      }

                      _context10.next = 4;
                      return that.store();

                    case 4:
                      response = _context10.sent;

                      if (response.success) {
                        that.items[index_episodio].nombres = response.data.nombres;
                        that.items[index_episodio].apellidos = response.data.apellidos;
                        that.items[index_episodio].cedula = response.data.cedula;
                        that.items[index_episodio].email = response.data.email;
                        that.items[index_episodio].genero = response.data.genero;
                        that.items[index_episodio].sexo = response.data.genero;
                        that.items[index_episodio].imagen = response.data.new_imagen;
                        that.items[index_episodio].nacionalidad = response.data.nacionalidad;
                        that.items[index_episodio].telefono_movil = response.data.telefono_movil;
                        that.items[index_episodio].telefono_hogar = response.data.telefono_hogar;
                        that.items[index_episodio].paciente = "".concat(response.data.nombres, " ").concat(response.apellidos);
                        console.log(that.items); // Escuchar cambios en el bus de eventos

                        EventBus.$on("externalVarChanged", function (newValue) {
                          that.items = newValue;
                        });
                        alert("Datos Actualizados. Los datos fueron actualizados correctamente.");
                        that.closeModal();
                      } else {
                        alert("Error. Ocurrió un error al guardar los datos.");
                      }

                    case 6:
                    case "end":
                      return _context10.stop();
                  }
                }
              }, _callee10);
            }))();
          },
          handle_create: function handle_create() {
            var _this12 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee11() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee11$(_context11) {
                while (1) {
                  switch (_context11.prev = _context11.next) {
                    case 0:
                      that = _this12;

                      _this12.is_a_child();

                      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_movil", _this12.telefonoConfig1, function (itiNumber, isValidNumber) {
                        that.form.telefono_movil = itiNumber;
                        var text = isValidNumber ? "<span class='text-success'><i class='far fa-check-circle'></i> Formato correcto</span>" : "<span class='text-danger'><i class='fas fa-exclamation-circle'></i> Formato incorrecto</span>";
                        $("#help_telefono_movil").html(text);
                      });
                      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_hogar", _this12.telefonoConfig2, function (itiNumber, isValidNumber) {
                        that.form.telefono_hogar = itiNumber;
                        var text = isValidNumber ? "<span class='text-success'><i class='far fa-check-circle'></i> Formato correcto</span>" : "<span class='text-danger'><i class='fas fa-exclamation-circle'></i> Formato incorrecto</span>";
                        $("#help_telefono_hogar").html(text);
                      });
                      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_movil_familiar", _this12.telefonoConfig3, function (itiNumber, isValidNumber) {
                        that.form.telefono_movil_familiar = itiNumber;
                        var text = isValidNumber ? "<span class='text-success'><i class='far fa-check-circle'></i> Formato correcto</span>" : "<span class='text-danger'><i class='fas fa-exclamation-circle'></i> Formato incorrecto</span>";
                        $("#help_telefono_movil_familiar").html(text);
                      });

                    case 5:
                    case "end":
                      return _context11.stop();
                  }
                }
              }, _callee11);
            }))();
          },
          closeModal: function closeModal() {
            /* this.$store.commit('updateDinamicComponent', {
                is_dismounted: true,
                customComponent: null,
                patient_data: null,
                index: null
            }); */
            $("#modal-patient-item").modal("hide");
          }
        },
        mounted: function mounted() {
          this.handle_create(); //console.log(this.dataPaciente);

          /* let that = this
          let { editDataPaciente } = this.$store.state
           */
          //console.log(this.$refs.cedula);
        }
      });
    }
  }, {
    key: "handle_ReingresoPaciente",
    value: function handle_ReingresoPaciente(profile) {
      console.log(profile);
      $("#modal-patient-item").modal("show");
      document.querySelector("#modal-patient-item .modal-body-2").innerHTML =
      /*html */
      "\n            <div id=\"AppReingresoPaciente\" class=\"flex-fill p-1 d-flex flex-column overflow-auto\">\n\n                <div class=\"flex-fill mt-1 d-flex flex-column rounded overflow-auto\">\n                    <div class=\"container\">\n                        <h3 class=\"text-primary\">Reingreso del Paciente</h3>\n                        <div class=\"row\">\n                            <div class=\"col-12 d-flex flex-column\">\n                                <div class=\"flex-fill mb-1 p-1 card\">\n                                    <div class=\"d-flex align-items-center\">\n                                        <img onerror=\"this.src='/image/system/icono-paciente-blanco.svg';\"\n                                            :src=\"dataPaciente.imagen\"\n                                            data-tippy-content=\"Imagen del paciente\" alt=\"...\"\n                                            class=\"tooltip-primary border border-primary rounded-circle m-1 d-block\"\n                                            style=\"width: 1.5cm; height: 1.5cm;\"\n                                        >\n                                        <div data-tippy-content=\"Nombre del paciente\"\n                                            class=\"tooltip-primary ml-1 text-primary flex-grow-1 h4 mb-0\">\n                                            {{ dataPaciente.nombres }} {{ dataPaciente.apellidos }}\n                                            <small class=\"text-light\" style=\"font-size: 0.7em;\">#{{ dataPaciente.user_id }}</small>\n                                        </div>\n                                    </div>\n                                    <div class=\"d-flex align-items-center\">\n                                        <div data-tippy-content=\"C\xE9dula del paciente\" class=\"tooltip-primary flex-fill pl-1\">\n                                            <b class=\"text-primary\" style=\"font-size: 0.8rem;\">C\xE9dula</b>\n                                            <div class=\"text-secondary\">\n                                                {{ dataPaciente.nacionalidad + dataPaciente.cedula }}\n                                            </div>\n                                        </div>\n                                        <div data-tippy-content=\"Edad\"\n                                            class=\"tooltip-primary flex-fill pl-1 border-left border-right\">\n                                            <b class=\"text-primary\" style=\"font-size: 0.8rem;\">Edad</b>\n                                            <div class=\"text-secondary\">{{ getEdad() }}</div>\n                                        </div>\n                                        <div data-tippy-content=\"Sexo\" class=\"tooltip-primary flex-fill pl-1\">\n                                            <b class=\"text-primary\" style=\"font-size: 0.8rem;\">Sexo</b>\n                                            <div class=\"text-secondary text-uppercase\">{{ dataPaciente.genero }}</div>\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                            <div class=\"col-12 col-sm-6\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"\">Fecha de ingreso:</label>\n                                    <input ref=\"ingreso\" v-model=\"ingreso\" type=\"date\" class=\"form-control bg-gray-footer-parodi\">\n                                </div>\n\n                            </div>\n                            <div class=\"col-12 col-sm-6\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"cat_user_ubicacion_id\">Nueva\n                                        Ubicaci\xF3n:</label>\n                                    <select\n                                        @change=\"updateCatUbicacion1()\"\n                                        ref=\"cat_user_ubicacion_id\"\n                                        name=\"cat_user_ubicacion_id\"\n                                        class=\"form-control bg-gray-footer-parodi\"\n                                    >\n                                        <option value=\"245\">Sala de Espera (SE)</option>\n                                        <option v-for=\"(item, index) in getCatUbicacionAreas\" :key=\"index\" :value=\"item.id\">{{\n                                            item.description }}</option>\n                                    </select>\n                                </div>\n                            </div>\n                            <div :class=\"['col-12', { 'd-none': catUbicacionTemp2.length === 0 }]\">\n                                <div class=\"form-group\">\n                                    <select\n                                        @change=\"updateCatUbicacion2()\"\n                                        ref=\"cat_user_ubicacion_id_2\"\n                                        class=\"form-control bg-gray-footer-parodi\"\n                                    >\n                                        <option value=\"\">Seleccione</option>\n                                        <option\n                                            v-for=\"(item, index) in getCatUbicacionAreas2\"\n                                            :key=\"index\"\n                                            :value=\"item.id\">\n                                            {{item.coments }}\n                                        </option>\n                                    </select>\n                                </div>\n                            </div>\n                            <div :class=\"['col-12', { 'd-none': catUbicacionTemp3.length === 0 }]\">\n                                <div class=\"form-group\">\n                                    <select\n                                        @change=\"updateCatUbicacion3()\"\n                                        ref=\"cat_user_ubicacion_id_3\"\n                                        class=\"form-control bg-gray-footer-parodi\"\n                                    >\n                                        <option value=\"\">Seleccione</option>\n                                        <option\n                                            v-for=\"(item, index) in getCatUbicacionAreas3\"\n                                            :key=\"index\"\n                                            :value=\"item.id\">\n                                            {{item.coments }}\n                                        </option>\n                                    </select>\n                                </div>\n                            </div>\n                            <div :class=\"['col-12', { 'd-none': catUbicacionTemp4.length === 0 }]\">\n                                <div class=\"form-group\">\n                                    <select\n                                        @change=\"updateCatUbicacion4()\"\n                                        ref=\"cat_user_ubicacion_id_4\"\n                                        class=\"form-control bg-gray-footer-parodi\"\n                                    >\n                                        <option value=\"\">Seleccione</option>\n                                        <option\n                                            v-for=\"(item, index) in getCatUbicacionAreas4\"\n                                            :key=\"index\" :value=\"item.id\"\n                                        >\n                                            {{item.coments }}\n                                        </option>\n                                    </select>\n                                </div>\n                            </div>\n\n\n                        </div>\n                    </div>\n                </div>\n                <div class=\"rounded d-flex mt-1\">\n                    <button @click=\"submit()\" id=\"submit\" class=\"flex-fill btn btn-success mr-1\">\n                        Reingresar paciente\n                    </button>\n                    <button class=\"flex-fill btn btn-primary\" data-dismiss=\"modal\" aria-label=\"Close\">\n                        Cancelar\n                    </button>\n\n                </div>\n\n            </div>\n        ";
      var app = new Vue({
        el: "#AppReingresoPaciente",
        name: "FormReingresoPaciente",
        props: {
          /*   index_row: Number,
              dataPaciente: Object */
        },
        components: {//CintilloModal,
        },
        data: function data() {
          return {
            pacientes: pacientes_datos,
            loading: document.getElementById("loadingScreen"),
            user_id_paciente: null,
            ingreso: Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaAMD"])(),
            dataPaciente: [],
            areaSiglas: "",
            cat_user_ubicacion_id: 245,
            catUbicacionTemp2: [],
            catUbicacionTemp3: [],
            catUbicacionTemp4: []
          };
        },
        methods: {
          getEdad: function getEdad() {
            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["calcularEdad"])(this.dataPaciente.fnacimiento);
          },
          getAreaData: function getAreaData() {
            var _this13 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee12() {
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee12$(_context12) {
                while (1) {
                  switch (_context12.prev = _context12.next) {
                    case 0:
                      _this13.areaSiglas = [null, undefined, "undefined"].includes(localStorage.getItem("area")) ? "tp" : localStorage.getItem("area"); //this.selectorAreaName.textContent = this.items.find(x=>x.siglas === this.areaSiglas).name

                      _this13.loading.classList.remove("d-none");

                      _context12.next = 4;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/vistas/" + _this13.areaSiglas);

                    case 4:
                      pacientes_datos = _context12.sent;
                      EventBus.$emit("externalVarChanged", pacientes_datos); //that.AppBtnTotales(pacientes_datos)

                      _this13.loading.classList.add("d-none");

                    case 7:
                    case "end":
                      return _context12.stop();
                  }
                }
              }, _callee12);
            }))();
          },
          submit: function submit() {
            var _this14 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee13() {
              var formdata;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee13$(_context13) {
                while (1) {
                  switch (_context13.prev = _context13.next) {
                    case 0:
                      formdata = new FormData();
                      formdata.append("cat_user_ubicacion_id", _this14.cat_user_ubicacion_id);
                      formdata.append("ingreso", _this14.ingreso);
                      formdata.append("value", "Reingreso");
                      formdata.append("user_id", _this14.user_id_paciente);
                      formdata.append("user_id_paciente", _this14.user_id_paciente);
                      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                      formdata.append("_token", $("#_token").val());
                      _context13.next = 10;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_ubicacion/storeReingreso", formdata);

                    case 10:
                      _this14.getAreaData();

                      $("#modal-patient-item").modal("hide");

                    case 12:
                    case "end":
                      return _context13.stop();
                  }
                }
              }, _callee13);
            }))();
          },
          getPacientesActivos: function getPacientesActivos() {
            var _this15 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee14() {
              var _model;

              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee14$(_context14) {
                while (1) {
                  switch (_context14.prev = _context14.next) {
                    case 0:
                      _context14.prev = 0;
                      _context14.next = 3;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/vistas/" + _this15.$store.state.ruta);

                    case 3:
                      _model = _context14.sent;
                      $("#modal-patient-item").modal("hide");
                      _context14.next = 11;
                      break;

                    case 7:
                      _context14.prev = 7;
                      _context14.t0 = _context14["catch"](0);
                      console.error("Error al obtener los datos:", _context14.t0);
                      return _context14.abrupt("return", []);

                    case 11:
                    case "end":
                      return _context14.stop();
                  }
                }
              }, _callee14, null, [[0, 7]]);
            }))();
          },
          updateCatUbicacion1: function updateCatUbicacion1() {
            var _this16 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee15() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee15$(_context15) {
                while (1) {
                  switch (_context15.prev = _context15.next) {
                    case 0:
                      that = _this16;
                      that.cat_user_ubicacion_id = 245;
                      that["catUbicacionTemp2"] = [];
                      that["catUbicacionTemp3"] = [];
                      that["catUbicacionTemp4"] = [];

                      if ([null, ""].includes(that.$refs["cat_user_ubicacion_id"].value)) {
                        _context15.next = 10;
                        break;
                      }

                      that.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id"].value);
                      _context15.next = 9;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_ubicacion/show/" + that.cat_user_ubicacion_id);

                    case 9:
                      that["catUbicacionTemp2"] = _context15.sent.sort(function (a, b) {
                        return a.orden - b.orden;
                      });

                    case 10:
                      console.log({
                        "catUbicacionTemp2": that["catUbicacionTemp2"],
                        "catUbicacionTemp3": that["catUbicacionTemp3"],
                        "catUbicacionTemp4": that["catUbicacionTemp4"],
                        "cat_user_ubicacion_id": that.cat_user_ubicacion_id
                      });

                    case 11:
                    case "end":
                      return _context15.stop();
                  }
                }
              }, _callee15);
            }))();
          },
          updateCatUbicacion2: function updateCatUbicacion2() {
            var _this17 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee16() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee16$(_context16) {
                while (1) {
                  switch (_context16.prev = _context16.next) {
                    case 0:
                      that = _this17;
                      that.cat_user_ubicacion_id = 245;
                      that["catUbicacionTemp3"] = [];
                      that["catUbicacionTemp4"] = [];

                      if ([null, ""].includes(that.$refs["cat_user_ubicacion_id_2"].value)) {
                        _context16.next = 9;
                        break;
                      }

                      that.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_2"].value);
                      _context16.next = 8;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_ubicacion/show/" + that.cat_user_ubicacion_id);

                    case 8:
                      that["catUbicacionTemp3"] = _context16.sent.sort(function (a, b) {
                        return a.orden - b.orden;
                      });

                    case 9:
                      console.log({
                        "catUbicacionTemp2": that["catUbicacionTemp2"],
                        "catUbicacionTemp3": that["catUbicacionTemp3"],
                        "catUbicacionTemp4": that["catUbicacionTemp4"],
                        "cat_user_ubicacion_id": that.cat_user_ubicacion_id
                      });

                    case 10:
                    case "end":
                      return _context16.stop();
                  }
                }
              }, _callee16);
            }))();
          },
          updateCatUbicacion3: function updateCatUbicacion3() {
            var _this18 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee17() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee17$(_context17) {
                while (1) {
                  switch (_context17.prev = _context17.next) {
                    case 0:
                      that = _this18;
                      that.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_2"].value);
                      that["catUbicacionTemp4"] = [];

                      if ([null, ""].includes(that.$refs["cat_user_ubicacion_id_3"].value)) {
                        _context17.next = 8;
                        break;
                      }

                      that.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_3"].value);
                      _context17.next = 7;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_ubicacion/show/" + that.cat_user_ubicacion_id);

                    case 7:
                      that["catUbicacionTemp4"] = _context17.sent.sort(function (a, b) {
                        return a.orden - b.orden;
                      });

                    case 8:
                      console.log({
                        "catUbicacionTemp2": that["catUbicacionTemp2"],
                        "catUbicacionTemp3": that["catUbicacionTemp3"],
                        "catUbicacionTemp4": that["catUbicacionTemp4"],
                        "cat_user_ubicacion_id": that.cat_user_ubicacion_id
                      });

                    case 9:
                    case "end":
                      return _context17.stop();
                  }
                }
              }, _callee17);
            }))();
          },
          updateCatUbicacion4: function updateCatUbicacion4() {
            var _this19 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee18() {
              var that;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee18$(_context18) {
                while (1) {
                  switch (_context18.prev = _context18.next) {
                    case 0:
                      that = _this19;
                      that.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_3"].value);

                      if (![null, ""].includes(that.$refs["cat_user_ubicacion_id_4"].value)) {
                        that.cat_user_ubicacion_id = Number(that.$refs["cat_user_ubicacion_id_4"].value);
                      }

                      console.log({
                        "catUbicacionTemp2": that["catUbicacionTemp2"],
                        "catUbicacionTemp3": that["catUbicacionTemp3"],
                        "catUbicacionTemp4": that["catUbicacionTemp4"],
                        "cat_user_ubicacion_id": that.cat_user_ubicacion_id
                      });

                    case 4:
                    case "end":
                      return _context18.stop();
                  }
                }
              }, _callee18);
            }))();
          }
        },
        computed: {
          getCatUbicacionAreas: function getCatUbicacionAreas() {
            return cat_ubicaciones.filter(function (item) {
              return [2, 28, 41, 42, 70, 98, 126, 154, 182, 211, 223, 390, 391, 237, 230].includes(item["id"]);
            }).sort(function (a, b) {
              return a.orden - b.orden;
            });
          },
          getCatUbicacionAreas2: function getCatUbicacionAreas2() {
            return this.catUbicacionTemp2.sort(function (a, b) {
              return a.orden - b.orden;
            });
          },
          getCatUbicacionAreas3: function getCatUbicacionAreas3() {
            return this.catUbicacionTemp3.sort(function (a, b) {
              return a.orden - b.orden;
            });
          },
          getCatUbicacionAreas4: function getCatUbicacionAreas4() {
            return this.catUbicacionTemp4.sort(function (a, b) {
              return a.orden - b.orden;
            });
          }
        },
        mounted: function mounted() {
          this.dataPaciente = profile;
          this.user_id_paciente = profile.user_id;
        }
      });
    }
  }, {
    key: "index",
    value: function index(selector, user_id) {
      $("#modelId").modal("show");
      $(selector).html("\n\n        ");
    }
  }, {
    key: "create",
    value: function () {
      var _create = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee19(selector, user_id) {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee19$(_context19) {
          while (1) {
            switch (_context19.prev = _context19.next) {
              case 0:
                //CREATE PARA REGISTRO PACIENTE EXTERNO
                $(selector).html("\n            <div class=\"container\">\n                <div class=\"row mt-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Datos del paciente\n                        </div>\n                    </div>\n                </div>\n\n                <div id=\"user_create\" ></div>\n\n                <div id=\"user_profile_create\" ></div>\n                <input style=\"display:none\" type=\"file\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"imagen\" id=\"imagen\" aria-describedby=\"helpId\" placeholder=\"\">\n\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Direcci\xF3n\n                        </div>\n                    </div>\n                </div>\n                <div id=\"user_cuest_direccion_create\" ></div>\n\n                <div id=\"user_cuest_pruebaCovid_create\"></div>\n\n                <div id=\"user_cuest_ubicacion_create\"></div>\n\n                <div class=\"row mb-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 mt-1\">\n                        <button id=\"user_cuest_paciente_store\" class=\"btn btn-success btn-lg btn-block\">Continuar</button>\n                    </div>\n                </div>\n\n            </div>\n        "); //VISTAS

                user.create2("#user_create");
                userProfile.create("#user_profile_create");
                userCuestDireccion.create("#user_cuest_direccion_create"); //UserCuestPruebaCovid.create("#user_cuest_pruebaCovid_create")
                //UserCuestPruebaCovid.resultadoCuestionarioCovid("#paso2_recomendacion")

                this.bienvenidaPaciente();
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_movil"); //ACCIONES

                $("#user_cuest_paciente_store").on("click", function () {
                  if (user.validate2()) {
                    if (userProfile.validate()) {
                      if (userCuestDireccion.validate()) {
                        //if (UserCuestPruebaCovid.validate()) {
                        if (userCuestUbicacion.validate()) {
                          user.store().then(function (response) {
                            //console.log("User:", response)
                            var email = $("#email");
                            var cedula = $("#cedula");

                            if (response == "cedula_registrado") {
                              message = _Component_js__WEBPACK_IMPORTED_MODULE_9__["default"].alertMessage("cedula_registrado");
                              alert(message["message"]);
                              cedula.trigger("focus");
                              return false;
                            }

                            if (response == "email_registrado") {
                              message = Component.alertMessage("email_registrado");
                              alert(message["message"]);
                              email.trigger("focus");
                              return false;
                            }

                            return response.id;
                          }).then(function (response) {
                            if (response != false) {
                              userType.store(response).then(function (response) {
                                //console.log("UserType:", response)
                                return response.user_id;
                              }).then(function (response) {
                                userProfile.store(response).then(function (response) {
                                  //console.log("UserProfile:", response);
                                  return response.user_id;
                                }).then(function (response) {
                                  userCuestDireccion.store(response).then(function (response) {
                                    //console.log("UserCuestDireccion:", response)
                                    return response.user_id;
                                  }).then(function (response) {
                                    userCuestUbicacion.store(response).then(function (response) {
                                      //console.log("UserCuestUbicacion:", response)
                                      return response.user_id;
                                    });
                                    /* .then(response => {
                                            UserCuestPruebaCovid.store(response)
                                                .then(response => {
                                                    console.log("Resultado del Cuestionario:", response)
                                                })
                                        }) */
                                  });
                                });
                              });
                            }
                          });
                        } //}

                      }
                    }
                  }
                });

              case 7:
              case "end":
                return _context19.stop();
            }
          }
        }, _callee19, this);
      }));

      function create(_x, _x2) {
        return _create.apply(this, arguments);
      }

      return create;
    }()
  }, {
    key: "create2",
    value: function () {
      var _create2 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee20(selector, user_id) {
        var f, fechaIngreso;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee20$(_context20) {
          while (1) {
            switch (_context20.prev = _context20.next) {
              case 0:
                //CREATE PARA REGISTRO PACIENTE INTERNO
                f = new Date();
                fechaIngreso = f.getFullYear() + "-" + ("0" + (f.getMonth() + 1)).slice(-2) + "-" + ("0" + f.getDate()).slice(-2);
                $(selector).html("\n            <div class=\"container\">\n                <div class=\"row mt-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Datos del paciente\n                        </div>\n                    </div>\n                </div>\n\n                <input id=\"cat_user_type_id\" type=\"hidden\">\n                <div id=\"user_create\" ></div>\n\n                <div id=\"user_profile_create\" ></div>\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Direcci\xF3n\n                        </div>\n                    </div>\n                </div>\n                <div id=\"user_cuest_direccion_create\" ></div>\n\n                <div id=\"user_cuest_pruebaCovid_create\"></div>\n\n                <div id=\"user_cuest_direccion_create\"></div>\n\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n\n                        <table class=\"w-100\">\n                            <tr>\n                                <td>\n                                    <div  class=\"text-center\" >\n                                        <img onerror=\"this.src='/image/system/icono-paciente-blanco.svg'\" id=\"imagen_perfil\" style=\"width: 10%; height:10%\" src=\"/image/system/patient.svg\" class=\"imagen-perfil\" alt=\"Medic default\">\n                                        <br/>\n                                        <i id=\"ampliar\" class=\"heartbeat cursor-pointer mt-1 fas fa-search-plus text-primary mr-1\" style=\"font-size:1.3em;\"></i>\n                                        <i id=\"reducir\" class=\"heartbeat cursor-pointer mt-1 fas fa-search-minus text-primary\" style=\"font-size:1.3em;\"></i>\n                                    </div>\n                                </td>\n                                <td>\n                                    <div class=\"form-group text-center\">\n                                        <label class=\"label-text text-secondary\" for=\"imagen\">Foto <h6>solo archivos (.jpg o .png)</h6></label>\n                                        <input type=\"file\" accept=\"image/png, image/gif, image/jpeg\" oninput=\"validarArchivoImagen(this.id)\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"imagen\" id=\"imagen\" aria-describedby=\"helpId\" placeholder=\"\">\n                                        <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                    </div>\n                                </td>\n                            </tr>\n                        </table>\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <div class=\"form-group\">\n                            <label class=\"label-text text-secondary\" for=\"nombre\">Fecha de ingreso</label>\n                            <input type=\"date\" value=\"".concat(fechaIngreso, "\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"ingreso\" id=\"ingreso\" aria-describedby=\"helpId\" placeholder=\"\">\n                            <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                        </div>\n                    </div>\n                </div>\n\n                <div class=\"row mt-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Traslado\n                        </div>\n                    </div>\n                </div>\n                <div id=\"user_cuest_ubicacion_create\"></div>\n\n\n                <div data-toggle=\"collapse\" href=\"#collapseExample\" aria-expanded=\"false\" aria-controls=\"collapseExample\" class=\"row cursor-pointer mt-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Datos del Familiar <i class=\"fas fa-chevron-down\"></i>\n                        </div>\n                    </div>\n                </div>\n\n                <div class=\"collapse  mb-5\" id=\"collapseExample\">\n                    <div id=\"user_cuest_familiar_create\"></div>\n                </div>\n\n\n\n                <div class=\"row mt-5\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <button id=\"user_cuest_paciente_store\" style=\"font-size: 1.5em;\" class=\"btn btn-success btn-block\">Registrar paciente</button>\n                    </div>\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <button onclick=\"history.back()\" style=\"font-size: 1.5em;\" class=\"btn btn-primary mt-1 btn-block\">Cancelar</button>\n                    </div>\n                </div>\n\n            </div>\n        "));
                document.getElementById("cat_user_type_id").value = 1;
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])("imagen", ".imagen-perfil");
                $("#ampliar").on("click", function () {
                  $("#imagen_perfil").animate({
                    width: "80%"
                  }, 500);
                });
                $("#reducir").on("click", function () {
                  $("#imagen_perfil").animate({
                    width: "10%"
                  }, 500);
                }); //VISTAS

                user.create2("#user_create");
                userProfile.create("#user_profile_create");
                userCuestDireccion.create("#user_cuest_direccion_create");
                userCuestUbicacion.create2("#user_cuest_ubicacion_create");
                userFamily.create("#user_cuest_familiar_create");
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_movil");
                $("html, body").animate({
                  scrollTop: 0
                }, "slow"); //ACCIONES

                $("#user_cuest_paciente_store").on("click", function () {
                  if (user.validate2()) {
                    if (userProfile.validate()) {
                      if (userCuestDireccion.validate()) {
                        if (userCuestUbicacion.validate2()) {
                          // let email = $("#email");
                          //let cedula = $("#cedula");
                          userCuestPaciente.guardarNuevoPaciente();
                        }
                      }
                    }
                  }
                });

              case 15:
              case "end":
                return _context20.stop();
            }
          }
        }, _callee20);
      }));

      function create2(_x3, _x4) {
        return _create2.apply(this, arguments);
      }

      return create2;
    }()
  }, {
    key: "guardarNuevoPaciente",
    value: function () {
      var _guardarNuevoPaciente = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee21() {
        var user_id, cedula, model, cedulaExiste;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee21$(_context21) {
          while (1) {
            switch (_context21.prev = _context21.next) {
              case 0:
                user_id = null;
                cedula = null;
                _context21.next = 4;
                return userProfile.validateCedula();

              case 4:
                cedulaExiste = _context21.sent;

                if (!cedulaExiste.exist) {
                  _context21.next = 14;
                  break;
                }

                user_id = cedulaExiste.user.profile.user_id;
                cedula = cedulaExiste.user.profile.cedula; //if(cedulaExiste.episodesOpen.length === undefined){
                //document.querySelector("#content_1").style.visibility = 'hidden'

                $("#content_1").slideDown();
                $("#loading").slideUp("slow");
                Swal.fire({
                  title: "¡Paciente ya registrado!",
                  html: "\n                        La c\xE9dula <b>".concat(cedula, "</b> est\xE1 asociada al paciente:\n                        <table class=\"table table-bordered\">\n                            <tr>\n                                <th>Nombres:</th>\n                                <th>Apellidos:</th>\n                                <th>Ubicaci\xF3n actual:</th>\n                            </tr>\n                            <tr>\n                                <td>").concat(cedulaExiste.user.profile.nombres, "</td>\n                                <td>").concat(cedulaExiste.user.profile.apellidos, "</td>\n                                <td>").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(cedulaExiste.episodesOpen.ubicacion_description) ? "" : cedulaExiste.episodesOpen.ubicacion_description, "</td>\n                            </tr>\n                        </table>\n                        <h4>Para ingresarlo, debe escribir la c\xE9dula nuevamente, y pulsar en el bot\xF3n reingresar.</h4>\n                    "),
                  allowOutsideClick: false,
                  icon: "error",
                  showCancelButton: false,
                  confirmButtonColor: "#3085d6",
                  //cancelButtonColor: "#d33",
                  confirmButtonText: "De acuerdo!" //cancelButtonText: "No!"

                }).then(function (result) {
                  if (result.isConfirmed) {
                    location.href = "/medico/create_paciente";
                    document.querySelector("#content_1").style.visibility = "visible";
                    cedula.val("");
                    cedula.trigger("focus");
                  }
                });
                return _context21.abrupt("return", false);

              case 14:
                _context21.next = 16;
                return _User_js__WEBPACK_IMPORTED_MODULE_4__["default"].store();

              case 16:
                model = _context21.sent;

                if (!(model === "cedula_existe")) {
                  _context21.next = 20;
                  break;
                }

                Swal.fire({
                  title: "¡Paciente ya registrado!",
                  html: "\n                        La c\xE9dula <b>".concat(cedula, "</b> est\xE1 asociada al paciente:\n                        <table class=\"table table-bordered\">\n                            <tr>\n                                <th>Nombres:</th>\n                                <th>Apellidos:</th>\n                                <th>Ubicaci\xF3n actual:</th>\n                            </tr>\n                            <tr>\n                                <td>").concat(cedulaExiste.user.profile.nombres, "</td>\n                                <td>").concat(cedulaExiste.user.profile.apellidos, "</td>\n                                <td>").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])(cedulaExiste.episodesOpen.ubicacion_description) ? "" : cedulaExiste.episodesOpen.ubicacion_description, "</td>\n                            </tr>\n                        </table>\n                        <h4>Para ingresarlo, debe escribir la c\xE9dula nuevamente, y pulsar en el bot\xF3n reingresar.</h4>\n                    "),
                  allowOutsideClick: false,
                  icon: "error",
                  showCancelButton: false,
                  confirmButtonColor: "#3085d6",
                  //cancelButtonColor: "#d33",
                  confirmButtonText: "De acuerdo!" //cancelButtonText: "No!"

                }).then(function (result) {
                  if (result.isConfirmed) {
                    location.href = "/medico/create_paciente";
                    document.querySelector("#content_1").style.visibility = "visible";
                    cedula.val("");
                    cedula.trigger("focus");
                  }
                });
                return _context21.abrupt("return", false);

              case 20:
                $("#content_1").slideUp();
                $("#loading").slideDown("slow");
                user_id = model.id;
                _context21.next = 25;
                return userType.store(user_id);

              case 25:
                model = _context21.sent;
                _context21.next = 28;
                return userProfile.store(user_id);

              case 28:
                model = _context21.sent;
                _context21.next = 31;
                return userCuestDireccion.store(user_id);

              case 31:
                model = _context21.sent;
                _context21.next = 34;
                return userFamily.store(user_id);

              case 34:
                model = _context21.sent;
                ingreso = "Ingreso";
                _context21.next = 38;
                return userCuestUbicacion.store2(user_id);

              case 38:
                model = _context21.sent;
                message = _Component_js__WEBPACK_IMPORTED_MODULE_9__["default"].alertMessage("send_success");
                Toast.fire({
                  icon: message["image"],
                  title: message["title"],
                  text: message["message"],
                  didClose: function didClose() {
                    setTimeout(function () {
                      if (document.getElementById("cat_user_ubicacion_id").value == 388) {
                        window.location = "/telesalud/empresarial/index";
                      } else {
                        //window.location = "/medico";
                        console.log(location.origin + localStorage.getItem("current_route"));
                        window.location = location.origin + localStorage.getItem("current_route");
                      } //console.log('%cUserCuestPaciente.js line:298 object', 'color: #007acc;', document.getElementById('cat_user_ubicacion_id').value);

                    }, 110);
                  }
                });

              case 41:
              case "end":
                return _context21.stop();
            }
          }
        }, _callee21);
      }));

      function guardarNuevoPaciente() {
        return _guardarNuevoPaciente.apply(this, arguments);
      }

      return guardarNuevoPaciente;
    }()
  }, {
    key: "create3",
    value: function () {
      var _create3 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee23(selector, user_id) {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee23$(_context23) {
          while (1) {
            switch (_context23.prev = _context23.next) {
              case 0:
                //DESPISTAJE COVID
                //CREATE PARA REGISTRO PACIENTE EXTERNO
                $(selector).html("\n            <div class=\"container\">\n                <div class=\"row mt-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Datos del paciente\n                        </div>\n                    </div>\n                </div>\n\n                <div id=\"user_create\" ></div>\n\n                <div id=\"user_profile_create\" ></div>\n                <input style=\"display:none\" type=\"file\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"imagen\" id=\"imagen\" aria-describedby=\"helpId\" placeholder=\"\">\n\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Direcci\xF3n\n                        </div>\n                    </div>\n                </div>\n                <div id=\"user_cuest_direccion_create\" ></div>\n\n                <div id=\"user_cuest_pruebaCovid_create\"></div>\n\n                <div id=\"user_cuest_ubicacion_create\"></div>\n\n                <div class=\"row mb-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 mt-1\">\n                        <button id=\"user_cuest_paciente_store\" class=\"btn btn-success btn-lg btn-block\">Registrar</button>\n                    </div>\n                </div>\n\n            </div>\n        "); //VISTAS

                user.create2("#user_create");
                userProfile.create("#user_profile_create");
                userCuestDireccion.create("#user_cuest_direccion_create"); //UserCuestPruebaCovid.create("#user_cuest_pruebaCovid_create")
                //UserCuestPruebaCovid.resultadoCuestionarioCovid("#paso2_recomendacion")

                userCuestPaciente.bienvenidaPaciente2();
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_movil"); //ACCIONES

                $("#user_cuest_paciente_store").on("click", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee22() {
                  var _model2, _user_id;

                  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee22$(_context22) {
                    while (1) {
                      switch (_context22.prev = _context22.next) {
                        case 0:
                          if (!user.validate2()) {
                            _context22.next = 21;
                            break;
                          }

                          if (!userProfile.validate()) {
                            _context22.next = 21;
                            break;
                          }

                          if (!userCuestDireccion.validate()) {
                            _context22.next = 21;
                            break;
                          }

                          _context22.next = 5;
                          return _User_js__WEBPACK_IMPORTED_MODULE_4__["default"].store();

                        case 5:
                          _model2 = _context22.sent;
                          _user_id = _model2.id;
                          ingreso = "Ingreso";
                          _context22.next = 10;
                          return userType.store(_user_id);

                        case 10:
                          _model2 = _context22.sent;
                          _context22.next = 13;
                          return userProfile.store(_user_id);

                        case 13:
                          _model2 = _context22.sent;
                          _context22.next = 16;
                          return userCuestDireccion.store(_user_id);

                        case 16:
                          _model2 = _context22.sent;
                          _context22.next = 19;
                          return userCuestUbicacion.store2(_user_id);

                        case 19:
                          _model2 = _context22.sent;
                          modalMensaje({
                            "static": true,
                            title: "\n                                    <i >Gracias por completar su registro.</i>\n\n                                ",
                            content: "\n                                    Esto nos permitir\xE1 brindarle el mejor servicio posible.\n\n                                    <p>\n                                        El equipo de salud del Centro M\xE9dico Docente La Trinidad le agradece que nos haya escogido como el aliado de su Salud !\n                                    </p>\n                                ",
                            action: "\n                                    <button id=\"salir\" onclick=\"window.location = 'https://www.cmdlt.edu.ve/';\" data-dismiss=\"modal\" type=\"button\" class=\"btn btn-light btn-block btn-lg text-primary\">Finalizar</button>\n                            "
                          });

                        case 21:
                        case "end":
                          return _context22.stop();
                      }
                    }
                  }, _callee22);
                })));

              case 7:
              case "end":
                return _context23.stop();
            }
          }
        }, _callee23);
      }));

      function create3(_x5, _x6) {
        return _create3.apply(this, arguments);
      }

      return create3;
    }()
  }, {
    key: "store",
    value: function store(user_id) {
      // let message;
      var formdata = new FormData();
      var nacionalidad = $("#nacionalidad");
      var cedula = $("#cedula");
      var email = $("#email");
      _User_js__WEBPACK_IMPORTED_MODULE_4__["default"].store({
        email: emaio.val,
        cedula: nacionalidad.val() + "-" + cedula.val()
      });
      /****** */

      var nombres = $("#nombres");
      var apellidos = $("#apellidos");
      var genero = $("#genero");
      var fnacimiento = $("#fnacimiento");
      var telefono_movil = $("#telefono_movil");
      var estado_id = $("#estado_id");
      var municipio_id = $("#municipio_id");
      var ciudad = $("#ciudad");
      var domicilio = $("#domicilio");
      var temp_actual = $("#temp_actual");
      var oxi_actual = $("#oxi_actual");
      var created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])();
      var caso = $("#caso");
      formdata.append("nacionalidad", nacionalidad.val());
      formdata.append("cedula", cedula.val());
      formdata.append("email", email.val());
      formdata.append("created_at", created_at);
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_temperatura/store", formdata);
    }
  }, {
    key: "destroy",
    value: function destroy(id) {
      var formdata = new FormData();
      formdata.append("id", id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_temperatura/destroy", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var message = _Component_js__WEBPACK_IMPORTED_MODULE_9__["default"].alertMessage("destroy_row_cuestion");
      var r = confirm(message["message"]);

      if (r == true) {
        userCuestPaciente.destroy(row_id).then(function (response) {
          var temp = $("#value").val();

          if (response.length > 0) {
            userCuestPaciente.columnaValor("#col_6_".concat(user_id), {
              user_id: user_id,
              valor: response[0].value
            });
          } else {
            $("#modelId").modal("hide");
            userCuestPaciente.columnaValor("#col_6_".concat(user_id), {
              user_id: user_id,
              valor: null
            });
          }

          userCuestPaciente.index(".modal-body", user_id);
        });
      }
    }
  }, {
    key: "colorTemp",
    value: function colorTemp(temp) {
      //$(selector).removeClass(["text-secondary", "text-cyan", "text-success", "text-danger"])
      if (temp <= 30.4) {
        return "text-cyan";
      } else if (temp >= 30.5 && temp <= 37.5) {
        return "text-success";
      } else if (temp >= 37.6 && temp <= 37.9) {
        return "text-warning";
      } else if (temp >= 38) {
        return "text-danger";
      } else {}
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      $(selector).html("\n            <div onclick=\"UserCuestPaciente.index('.modal-body',".concat(data.user_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    TEMP\n                </div>\n                ").concat(data.valor == "" || data.valor == null ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + UserCuestPaciente.colorTemp(data.valor) + "'>" + data.valor + "°C</span>", "\n            </div>\n        "));
    }
  }, {
    key: "help",
    value: function help(selector, user_id) {
      $(selector).html("\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <h1 class=\"text-primary\">\n                        Manual operativo de usuario\n                    </h1>\n                </div>\n            </div>\n            <br>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <object id=\"pdfHolder\" data=\"/help/temperatura.pdf\" type=\"application/pdf\" style=\"width:100%;height:500px;\">\n                        <a title=\"Pulse para descargar\" href=\"/help/temperatura.pdf\" download ><img src=\"/image/system/icono_archivo2.svg\" ></a>\n                    </object>\n                </div>\n            </div>\n            <br>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <button id=\"regresar\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                </div>\n            </div>\n\n        ");
      $("#regresar").on("click", function () {
        userCuestPaciente.index(selector, user_id);
      });
    }
  }, {
    key: "handle_peso",
    value: function () {
      var _handle_peso = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee25(item) {
        var index, d, App, app;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee25$(_context25) {
          while (1) {
            switch (_context25.prev = _context25.next) {
              case 0:
                index = pacientes_datos.findIndex(function (x) {
                  return x["user_id"] === item.user_id;
                });
                d = document;
                App = d.querySelector("#app_peso_" + item.user_id);
                App.innerHTML =
                /*html*/
                "\n                <div class=\"d-flex flex-column\">\n                    <b class=\"text-primary\" style=\"font-size:0.8rem\">{{title}}</b>\n                    <div class=\"input-group\">\n                        <input  v-model=\"value\" @keyup.enter=\"store()\" type=\"text\" class=\"form-control\" placeholder=\"Peso\" aria-label=\"Peso\" aria-describedby=\"button-addon2\">\n                        <div class=\"input-group-append\">\n                            <button @click=\"store()\" class=\"btn btn-outline-success\" type=\"button\" id=\"button-addon2\">Guardar</button>\n                        </div>\n                    </div>\n\n                </div>\n            ";
                app = new Vue({
                  el: "#app_peso_" + item.user_id,
                  data: function data() {
                    return {
                      index: null,
                      title: "Peso",
                      value: "",
                      item: []
                    };
                  },
                  methods: {
                    have_item: function have_item() {
                      var peso = this.item.peso;

                      if (peso !== null) {
                        this.value = peso;
                      } else {
                        this.value = "";
                      }
                    },
                    store: function store() {
                      var _this20 = this;

                      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee24() {
                        var formdata, result;
                        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee24$(_context24) {
                          while (1) {
                            switch (_context24.prev = _context24.next) {
                              case 0:
                                if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(_this20.value)) {
                                  _context24.next = 15;
                                  break;
                                }

                                formdata = new FormData();
                                formdata.append("episodio_id", _this20.item.episodio_id);
                                formdata.append("value", _this20.value);
                                formdata.append("user_id_paciente", _this20.item.user_id);
                                formdata.append("sintoma_observacion", "");
                                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                                formdata.append("_token", $("#_token").val());
                                _context24.next = 10;
                                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_peso/store2", formdata);

                              case 10:
                                result = _context24.sent;
                                _this20.item.peso = _this20.value;
                                pacientes_datos[_this20.index] = _this20.item;

                                _this20.have_item();

                                Swal.fire("Peso agregado!", "El peso se registr\xF3 correctamente.", "success");

                              case 15:
                              case "end":
                                return _context24.stop();
                            }
                          }
                        }, _callee24);
                      }))();
                    }
                  },
                  mounted: function mounted() {
                    this.item = item;
                    this.index = index;
                    this.have_item();
                  }
                });

              case 5:
              case "end":
                return _context25.stop();
            }
          }
        }, _callee25);
      }));

      function handle_peso(_x7) {
        return _handle_peso.apply(this, arguments);
      }

      return handle_peso;
    }()
  }, {
    key: "handle_talla",
    value: function () {
      var _handle_talla = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee27(item) {
        var index, d, App, app;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee27$(_context27) {
          while (1) {
            switch (_context27.prev = _context27.next) {
              case 0:
                index = pacientes_datos.findIndex(function (x) {
                  return x["user_id"] === item.user_id;
                });
                d = document;
                App = d.querySelector("#app_talla_" + item.user_id);
                App.innerHTML =
                /*html*/
                "\n                <div class=\"d-flex flex-column\">\n                    <b class=\"text-primary\" style=\"font-size:0.8rem\">{{title}}</b>\n                    <div class=\"input-group\">\n                        <input  v-model=\"value\" @keyup.enter=\"store()\" type=\"text\" class=\"form-control\" placeholder=\"Talla\" aria-label=\"Peso\" aria-describedby=\"button-addon2\">\n                        <div class=\"input-group-append\">\n                            <button @click=\"store()\" class=\"btn btn-outline-success\" type=\"button\" id=\"button-addon2\">Guardar</button>\n                        </div>\n                    </div>\n\n                </div>\n            ";
                app = new Vue({
                  el: "#app_talla_" + item.user_id,
                  data: function data() {
                    return {
                      index: null,
                      title: "Talla",
                      value: "",
                      item: []
                    };
                  },
                  methods: {
                    have_item: function have_item() {
                      var talla = this.item.talla;

                      if (talla !== null) {
                        this.value = talla;
                      } else {
                        this.value = "";
                      }
                    },
                    store: function store() {
                      var _this21 = this;

                      return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee26() {
                        var formdata, result;
                        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee26$(_context26) {
                          while (1) {
                            switch (_context26.prev = _context26.next) {
                              case 0:
                                if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(_this21.value)) {
                                  _context26.next = 15;
                                  break;
                                }

                                formdata = new FormData();
                                formdata.append("episodio_id", _this21.item.episodio_id);
                                formdata.append("value", _this21.value);
                                formdata.append("user_id_paciente", _this21.item.user_id);
                                formdata.append("sintoma_observacion", "");
                                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                                formdata.append("_token", $("#_token").val());
                                _context26.next = 10;
                                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_talla/store", formdata);

                              case 10:
                                result = _context26.sent;
                                _this21.item.talla = _this21.value;
                                pacientes_datos[_this21.index] = _this21.item;

                                _this21.have_item();

                                Swal.fire("Talla agregada!", "La talla se registr\xF3 correctamente.", "success");

                              case 15:
                              case "end":
                                return _context26.stop();
                            }
                          }
                        }, _callee26);
                      }))();
                    }
                  },
                  mounted: function mounted() {
                    this.item = item;
                    this.index = index;
                    this.have_item();
                  }
                });

              case 5:
              case "end":
                return _context27.stop();
            }
          }
        }, _callee27);
      }));

      function handle_talla(_x8) {
        return _handle_talla.apply(this, arguments);
      }

      return handle_talla;
    }()
  }, {
    key: "infoPaciente",
    value: function () {
      var _infoPaciente = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee28(selector, user_id) {
        var index, model, imp_diagn, f, fecha, contImpDiagn, resultImpDiag, fechaIngreso, cintilloVisibility;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee28$(_context28) {
          while (1) {
            switch (_context28.prev = _context28.next) {
              case 0:
                index = pacientes_datos.findIndex(function (x) {
                  return x.user_id == user_id;
                });
                model = pacientes_datos.filter(function (x) {
                  return x.user_id == user_id;
                })[0];
                imp_diagn = "";
                f = null;
                fecha = null;
                contImpDiagn = 0;
                _context28.next = 8;
                return UserCuestPaciente.showImpDiagnostica(user_id);

              case 8:
                resultImpDiag = _context28.sent;

                //console.log(resultImpDiag);
                if (resultImpDiag.length > 0) {
                  resultImpDiag.forEach(function (row) {
                    f = new Date(row.created_at);
                    fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()) + ", " + f.getFullYear();
                    imp_diagn += "\n                        <li class=\"list-group-item p-1 bg-light text-gray\">\n                            <div class=\"d-flex flex-column justify-content-start\">\n                                ".concat(row.value, "\n                            </div>\n                            <div class=\"d-flex flex-colum justify-content-end\">\n                                <span class=\"badge badge-gray rounded-custom text-secondary\">\n                                <i class=\"far fa-clock\"></i> ").concat(row.medico, "\n                                <span class=\"text-primary\">|</span> ").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaCortaCustom1"])(row.created_at_default), " <span class=\"text-primary\">|</span> ").concat(row.hora, "</span>\n                            </div>\n\n                        </li>\n                    ");
                    contImpDiagn++;
                  });
                }

                if (contImpDiagn === 0) {
                  imp_diagn =
                  /*html */
                  "\n                <li class=\"list-group-item p-1 bg-light text-gray\">\n                <div class=\"text-center\">Sin Probabilidad Diagn\xF3stica</div>\n                <a data-index=\"".concat(index, "\" onclick=\" localStorage.setItem('currentIndex',5)\"  class='").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(model.episodio) ? "d-none" : "", " btn-cintillo-imp-diag btn btn-light btn-block'>Agregar</a>\n                </li>\n            ");
                }

                ingreso = "";

                if (model.hasOwnProperty("ingreso_episodio")) {
                  f = new Date(model.ingreso_episodio);
                  ingreso = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()) + ", " + f.getFullYear();
                }

                fechaIngreso = "";
                fechaIngreso = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaAMD2"])(model.ingreso_episodio);
                cintilloVisibility = localStorage.getItem("cintilloVisibility"); //console.log('cintilloVisibility',cintilloVisibility);

                document.getElementById("infoPaciente").innerHTML =
                /*html */
                "\n            <nav class=\"d-flex align-items-start\">\n                <button\n                    class=\"ml-2 handleCintilloPacienteVisibility btn btn-default\"\n                    type=\"button\"\n                    data-toggle=\"collapse\"\n                    data-target=\"#collapseExample\"\n                    aria-expanded=\"".concat(cintilloVisibility, "\"\n                    aria-controls=\"collapseExample\"\n                >\n                    <i  class=\"fas fa-ellipsis-v text-primary\"></i>\n                </button>\n                <div id=\"collapseExample\" class=\"").concat(cintilloVisibility === "true" ? "show" : "collapse", " flex-column overflow-auto shadow-sm container-fluid bg-light\">\n                    <div class=\"row flex-nowrap\">\n                        <div class=\"col-12 col-sm-6 col-md-3 col-lg-2 col-xl-3 px-1 d-flex flex-column\">\n                            <div class=\"flex-fill mb-1 p-1 card\">\n                                <div class=\"d-flex align-items-center\">\n                                    <img onerror=\"this.src='/image/system/icono-paciente-blanco.svg';\" src=\"").concat(model.imagen, "\" style=\"width:1.5cm;height:1.5cm\" data-tippy-content=\"Imagen del paciente\" class=\"tooltip-primary border border-primary rounded-circle m-1 d-block\" alt=\"...\">\n                                    <div data-tippy-content=\"Nombre del paciente\" class=\"tooltip-primary ml-1 text-primary flex-grow-1 h4 mb-0\">\n                                        ").concat(model.paciente, " <small class=\"text-light\" style=\"font-size:0.7em;\">#").concat(model.user_id, "</small >\n                                    </div>\n                                </div>\n                                <div class=\"d-flex align-items-center\">\n                                    <div data-tippy-content=\"C\xE9dula del paciente\" class=\"tooltip-primary flex-fill pl-1\">\n                                        <b class=\"text-primary\" style=\"font-size:0.8rem\">C\xE9dula</b>\n                                        <div class=\"text-secondary\">").concat(model.cedula, "</div>\n                                    </div>\n                                    <div data-tippy-content=\"Edad\" class=\"tooltip-primary flex-fill pl-1 border-left border-right\">\n                                        <b class=\"text-primary\" style=\"font-size:0.8rem\">Edad</b>\n                                        <div class=\"text-secondary\">").concat(model.edad, "</div>\n                                    </div>\n                                    <div data-tippy-content=\"Sexo\" class=\"tooltip-primary flex-fill pl-1\">\n                                        <b class=\"text-primary\" style=\"font-size:0.8rem\">Sexo</b>\n                                        <div class=\"text-secondary\">").concat(model.sexo.toUpperCase(), "</div>\n                                    </div>\n                                </div>\n                                <div class=\"d-flex align-items-center\">\n\n                                    <div id=\"app_peso_").concat(model.user_id, "\" class=\"flex-fill\">\n\n                                    </div>\n                                    <div id=\"app_talla_").concat(model.user_id, "\" class=\"flex-fill ml-1\">\n\n                                    </div>\n\n                                </div>\n                            </div>\n                        </div>\n                        <div class=\"col-6 col-sm-4 col-md-2 col-lg-2 col-xl-1 px-0 d-flex flex-column\">\n                            <div class=\"flex-fill mb-1 p-1 card \">\n                                <b class=\"text-primary\" style=\"font-size:0.8rem\">Fecha Ingreso</b>\n                                <div class=\"text-secondary \">\n                                    <input\n                                    class=\"cursor-pointer\"\n                                    onchange=\"UserCuestEpisodio.update3(this.id,").concat(model.episodio, ",").concat(user_id, ")\"\n                                    id=\"ingreso").concat(model.user_id, "\"\n                                    style=\"\n                                        width: 100%;\n                                        border: 1px solid var(--gray);\n                                        background-color: transparent;\n                                        color: var( --secondary);\n                                        text-align: center;\n                                        border-radius: 50px;\n                                    \"\n                                    type=\"date\"\n                                    value=\"").concat(fechaIngreso, "\">\n                                </div>\n                            </div>\n                            <div onclick=\"userCuestUbicacion.historialUbiEpisodio('.modal-body',").concat(model.user_id, ")\" data-tippy-content=\"Ubicaci\xF3n Actual | D\xEDas desde el ingreso\" class=\"tooltip-primary flex-fill mb-1 p-1 card \">\n                                <b class=\"text-primary\" style=\"font-size:0.8rem\">Ubicaci\xF3n actual</b>\n                                <div class=\"text-secondary\">\n                                ").concat(model.ubicacion, " <span class=\"badge badge-secondary\">").concat(model.dias, "</span>\n                                </div>\n                            </div>\n                            <div class=\"flex-fill mb-1 p-1 card \">\n                                <b class=\"text-primary\" style=\"font-size:0.8rem\">Tel\xE9fono contacto</b>\n                                <div class=\"text-secondary\">\n                                    <a  target=\"_blank\"\n                                        href=\"https://api.whatsapp.com/send?phone=").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(model.telefono_movil) ? model.telefono_movil.replace("+", "") : "", "\"\n                                        style=\"padding: 0% 3% 3% 3%;border: 0;line-height: 1.4;\"\n                                        class=\"btn btn-default btn-block\">\n                                        <div class=\"text-gray text-nowrap overflow-hidden\">\n                                            <i class=\"fab fa-whatsapp text-success\"></i> ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(model.telefono_movil) ? model.telefono_movil.replace("+", "") : "", "\n                                        </div>\n                                    </a>\n                                </div>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6 col-md-4 col-lg-4 col-xl-5 px-1 d-flex flex-column overflow-auto\">\n                            <div class=\"flex-fill d-flex flex-column overflow-auto mb-1 p-1 card\">\n                                <b class=\"text-primary\" style=\"font-size:0.8rem\">Probabilidad Diagn\xF3stica <span class=\"badge badge-secondary\">").concat(contImpDiagn, "</span></b>\n                                <div class=\"flex-fill d-flex align-items-start justify-content-center overflow-auto\" style=\"height:100px\">\n                                    <ul class=\"list-group w-100 list-group-flush ").concat(location.pathname == "/medico/create_paciente" ? "d-none" : "", "\">\n                                        ").concat(imp_diagn, "\n                                    </ul>\n                                </div>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6 col-md-4 col-xl-2 col-xl-3 px-1 d-flex flex-column\">\n                            <div id=\"AppResumeHealthTeam\" ></div>\n                           \n                        </div>\n                    </div>\n                </div>\n            </nav>\n\n        ");
                this.handle_peso(model);
                this.handle_talla(model);
                Object(_MedicosPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"])(model.episodio_id); // $("#fecha_ingreso").val(fechaIngreso);

                /* let doctoresPaciente = new MedicosPaciente({
                    interconsultations:model.interconsultation_request,
                    episode_id: model.episodio,
                    attrSelectorId: `doctoresPaciente`,
                    medic_teem_to_show: [1, 2, 3, 7, 9],
                });
                doctoresPaciente.deployMedicoPacienteList();
                  let enfermerosPaciente = new MedicosPaciente({
                    interconsultations:model.interconsultation_request,
                    episode_id: model.episodio,
                    attrSelectorId: `enfermerosPaciente`,
                    medic_teem_to_show: [10],
                });
                enfermerosPaciente.deployMedicoPacienteList(); */

                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["activarTooltip"])();

              case 21:
              case "end":
                return _context28.stop();
            }
          }
        }, _callee28, this);
      }));

      function infoPaciente(_x9, _x10) {
        return _infoPaciente.apply(this, arguments);
      }

      return infoPaciente;
    }()
  }, {
    key: "infoPacienteRESPALDO28022025",
    value: function () {
      var _infoPacienteRESPALDO = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee29(selector, user_id) {
        var index, model, imp_diagn, f, fecha, contImpDiagn, resultImpDiag, fechaIngreso, cintilloVisibility, doctoresPaciente, enfermerosPaciente;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee29$(_context29) {
          while (1) {
            switch (_context29.prev = _context29.next) {
              case 0:
                index = pacientes_datos.findIndex(function (x) {
                  return x.user_id == user_id;
                });
                model = pacientes_datos.filter(function (x) {
                  return x.user_id == user_id;
                })[0];
                imp_diagn = "";
                f = null;
                fecha = null;
                contImpDiagn = 0;
                _context29.next = 8;
                return UserCuestPaciente.showImpDiagnostica(user_id);

              case 8:
                resultImpDiag = _context29.sent;

                //console.log(resultImpDiag);
                if (resultImpDiag.length > 0) {
                  resultImpDiag.forEach(function (row) {
                    f = new Date(row.created_at);
                    fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()) + ", " + f.getFullYear();
                    imp_diagn += "\n                        <li class=\"list-group-item p-1 bg-light text-gray\">\n                            <div class=\"d-flex flex-column justify-content-start\">\n                                ".concat(row.value, "\n                            </div>\n                            <div class=\"d-flex flex-colum justify-content-end\">\n                                <span class=\"badge badge-gray rounded-custom text-secondary\">\n                                <i class=\"far fa-clock\"></i> ").concat(row.medico, "\n                                <span class=\"text-primary\">|</span> ").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaCortaCustom1"])(row.created_at_default), " <span class=\"text-primary\">|</span> ").concat(row.hora, "</span>\n                            </div>\n\n                        </li>\n                    ");
                    contImpDiagn++;
                  });
                }

                if (contImpDiagn === 0) {
                  imp_diagn =
                  /*html */
                  "\n                <li class=\"list-group-item p-1 bg-light text-gray\">\n                <div class=\"text-center\">Sin Probabilidad Diagn\xF3stica</div>\n                <a data-index=\"".concat(index, "\" onclick=\" localStorage.setItem('currentIndex',5)\"  class='").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(model.episodio) ? "d-none" : "", " btn-cintillo-imp-diag btn btn-light btn-block'>Agregar</a>\n                </li>\n            ");
                }

                ingreso = "";

                if (model.hasOwnProperty("ingreso_episodio")) {
                  f = new Date(model.ingreso_episodio);
                  ingreso = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()) + ", " + f.getFullYear();
                }

                fechaIngreso = "";
                fechaIngreso = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fechaAMD2"])(model.ingreso_episodio);
                cintilloVisibility = localStorage.getItem("cintilloVisibility"); //console.log('cintilloVisibility',cintilloVisibility);

                document.getElementById("infoPaciente").innerHTML =
                /*html */
                "\n            <nav class=\"d-flex align-items-start\">\n                <button\n                    class=\"ml-2 handleCintilloPacienteVisibility btn btn-default\"\n                    type=\"button\"\n                    data-toggle=\"collapse\"\n                    data-target=\"#collapseExample\"\n                    aria-expanded=\"".concat(cintilloVisibility, "\"\n                    aria-controls=\"collapseExample\"\n                >\n                    <i  class=\"fas fa-ellipsis-v text-primary\"></i>\n                </button>\n                <div id=\"collapseExample\" class=\"").concat(cintilloVisibility === "true" ? "show" : "collapse", " flex-column overflow-auto shadow-sm container-fluid bg-light\">\n                    <div class=\"row flex-nowrap\">\n                        <div class=\"col-12 col-sm-6 col-md-3 col-lg-2 col-xl-3 px-1 d-flex flex-column\">\n                            <div class=\"flex-fill mb-1 p-1 card\">\n                                <div class=\"d-flex align-items-center\">\n                                    <img onerror=\"this.src='/image/system/icono-paciente-blanco.svg';\" src=\"").concat(model.imagen, "\" style=\"width:1.5cm;height:1.5cm\" data-tippy-content=\"Imagen del paciente\" class=\"tooltip-primary border border-primary rounded-circle m-1 d-block\" alt=\"...\">\n                                    <div data-tippy-content=\"Nombre del paciente\" class=\"tooltip-primary ml-1 text-primary flex-grow-1 h4 mb-0\">\n                                        ").concat(model.paciente, " <small class=\"text-light\" style=\"font-size:0.7em;\">#").concat(model.user_id, "</small >\n                                    </div>\n                                </div>\n                                <div class=\"d-flex align-items-center\">\n                                    <div data-tippy-content=\"C\xE9dula del paciente\" class=\"tooltip-primary flex-fill pl-1\">\n                                        <b class=\"text-primary\" style=\"font-size:0.8rem\">C\xE9dula</b>\n                                        <div class=\"text-secondary\">").concat(model.cedula, "</div>\n                                    </div>\n                                    <div data-tippy-content=\"Edad\" class=\"tooltip-primary flex-fill pl-1 border-left border-right\">\n                                        <b class=\"text-primary\" style=\"font-size:0.8rem\">Edad</b>\n                                        <div class=\"text-secondary\">").concat(model.edad, "</div>\n                                    </div>\n                                    <div data-tippy-content=\"Sexo\" class=\"tooltip-primary flex-fill pl-1\">\n                                        <b class=\"text-primary\" style=\"font-size:0.8rem\">Sexo</b>\n                                        <div class=\"text-secondary\">").concat(model.sexo.toUpperCase(), "</div>\n                                    </div>\n                                </div>\n                                <div class=\"d-flex align-items-center\">\n\n                                    <div id=\"app_peso_").concat(model.user_id, "\" class=\"flex-fill\">\n\n                                    </div>\n                                    <div id=\"app_talla_").concat(model.user_id, "\" class=\"flex-fill ml-1\">\n\n                                    </div>\n\n                                </div>\n                            </div>\n                        </div>\n                        <div class=\"col-6 col-sm-4 col-md-2 col-lg-2 col-xl-1 px-0 d-flex flex-column\">\n                            <div class=\"flex-fill mb-1 p-1 card \">\n                                <b class=\"text-primary\" style=\"font-size:0.8rem\">Fecha Ingreso</b>\n                                <div class=\"text-secondary \">\n                                    <input\n                                    class=\"cursor-pointer\"\n                                    onchange=\"UserCuestEpisodio.update3(this.id,").concat(model.episodio, ",").concat(user_id, ")\"\n                                    id=\"ingreso").concat(model.user_id, "\"\n                                    style=\"\n                                        width: 100%;\n                                        border: 1px solid var(--gray);\n                                        background-color: transparent;\n                                        color: var( --secondary);\n                                        text-align: center;\n                                        border-radius: 50px;\n                                    \"\n                                    type=\"date\"\n                                    value=\"").concat(fechaIngreso, "\">\n                                </div>\n                            </div>\n                            <div onclick=\"userCuestUbicacion.historialUbiEpisodio('.modal-body',").concat(model.user_id, ")\" data-tippy-content=\"Ubicaci\xF3n Actual | D\xEDas desde el ingreso\" class=\"tooltip-primary flex-fill mb-1 p-1 card \">\n                                <b class=\"text-primary\" style=\"font-size:0.8rem\">Ubicaci\xF3n actual</b>\n                                <div class=\"text-secondary\">\n                                ").concat(model.ubicacion, " <span class=\"badge badge-secondary\">").concat(model.dias, "</span>\n                                </div>\n                            </div>\n                            <div class=\"flex-fill mb-1 p-1 card \">\n                                <b class=\"text-primary\" style=\"font-size:0.8rem\">Tel\xE9fono contacto</b>\n                                <div class=\"text-secondary\">\n                                    <a  target=\"_blank\"\n                                        href=\"https://api.whatsapp.com/send?phone=").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(model.telefono_movil) ? model.telefono_movil.replace("+", "") : "", "\"\n                                        style=\"padding: 0% 3% 3% 3%;border: 0;line-height: 1.4;\"\n                                        class=\"btn btn-default btn-block\">\n                                        <div class=\"text-gray text-nowrap overflow-hidden\">\n                                            <i class=\"fab fa-whatsapp text-success\"></i> ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(model.telefono_movil) ? model.telefono_movil.replace("+", "") : "", "\n                                        </div>\n                                    </a>\n                                </div>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6 col-md-4 col-lg-4 col-xl-5 px-1 d-flex flex-column overflow-auto\">\n                            <div class=\"flex-fill d-flex flex-column overflow-auto mb-1 p-1 card\">\n                                <b class=\"text-primary\" style=\"font-size:0.8rem\">Probabilidad Diagn\xF3stica <span class=\"badge badge-secondary\">").concat(contImpDiagn, "</span></b>\n                                <div class=\"flex-fill d-flex align-items-start justify-content-center overflow-auto\" style=\"height:100px\">\n                                    <ul class=\"list-group w-100 list-group-flush ").concat(location.pathname == "/medico/create_paciente" ? "d-none" : "", "\">\n                                        ").concat(imp_diagn, "\n                                    </ul>\n                                </div>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6 col-md-4 col-xl-2 col-xl-3 px-1 d-flex flex-column\">\n                            <div class=\"flex-fill mb-1 p-1 card\">\n                                <ul class=\"nav nav-tabs p-0 m-0 flex-nowrap\" id=\"myTab\" role=\"tablist\">\n                                    <li class=\"nav-item p-0 m-0\" role=\"presentation\">\n                                        <a class=\"nav-link p-0 m-0 px-2 active\" id=\"equipoEspecialistas-tab\" data-toggle=\"tab\" href=\"#equipoEspecialistas\" role=\"tab\" aria-controls=\"equipoEspecialistas\" aria-selected=\"true\">\n                                            <b class=\"text-center text-primary\" style=\"font-size:0.8em;\">M\xE9dicos</b>\n                                        </a>\n                                    </li>\n                                    <li class=\"nav-item p-0 m-0\" role=\"presentation\">\n                                        <a class=\"nav-link p-0 px-2 m-0\" id=\"equipoEnfermeria-tab\" data-toggle=\"tab\" href=\"#equipoEnfermeria\" role=\"tab\" aria-controls=\"equipoEnfermeria\" aria-selected=\"false\">\n                                            <b class=\"text-center text-warning\" style=\"font-size:0.8em;\">Enfermer\xEDa</b>\n                                        </a>\n                                    </li>\n                                </ul>\n                                <div class=\"tab-content ").concat(location.pathname == "/medico/create_paciente" ? "d-none" : "", "\" id=\"myTabContent\">\n                                    <div class=\"tab-pane fade show active\" id=\"equipoEspecialistas\" role=\"tabpanel\" aria-labelledby=\"equipoEspecialistas-tab\">\n                                        <div id=\"doctoresPaciente\" class=\"p-0 pb-1\" scope=\"row\">\n\n                                        </div>\n                                    </div>\n                                    <div class=\"tab-pane fade\" id=\"equipoEnfermeria\" role=\"tabpanel\" aria-labelledby=\"equipoEnfermeria-tab\">\n                                        <div id=\"enfermerosPaciente\" class=\"p-0 pb-1\" scope=\"row\">\n\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n                    </div>\n                </div>\n            </nav>\n\n        ");
                this.handle_peso(model);
                this.handle_talla(model); // $("#fecha_ingreso").val(fechaIngreso);

                doctoresPaciente = new _MedicosPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"]({
                  interconsultations: model.interconsultation_request,
                  episode_id: model.episodio,
                  attrSelectorId: "doctoresPaciente",
                  medic_teem_to_show: [1, 2, 3, 7, 9]
                });
                doctoresPaciente.deployMedicoPacienteList();
                enfermerosPaciente = new _MedicosPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"]({
                  interconsultations: model.interconsultation_request,
                  episode_id: model.episodio,
                  attrSelectorId: "enfermerosPaciente",
                  medic_teem_to_show: [10]
                });
                enfermerosPaciente.deployMedicoPacienteList();
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["activarTooltip"])();

              case 24:
              case "end":
                return _context29.stop();
            }
          }
        }, _callee29, this);
      }));

      function infoPacienteRESPALDO28022025(_x11, _x12) {
        return _infoPacienteRESPALDO.apply(this, arguments);
      }

      return infoPacienteRESPALDO28022025;
    }()
  }, {
    key: "infoPacienteHistorial",
    value: function () {
      var _infoPacienteHistorial = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee30(user_id) {
        var $header, imp_diagn, f, fecha, contImpDiagn, $navInfoPaciente, paciente, ingreso_episodio, $info, configMedicos;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee30$(_context30) {
          while (1) {
            switch (_context30.prev = _context30.next) {
              case 0:
                $header = $modal.querySelector("#modal-header-info-user");
                imp_diagn = "";
                f = null;
                fecha = null;
                contImpDiagn = 0;
                $navInfoPaciente = byId("nav_info_paciente").content;
                paciente = first(pacientes_datos.filter(function (x) {
                  return x.user_id == user_id;
                }));
                ingreso_episodio = "";

                if (paciente.hasOwnProperty("ingreso_episodio")) {
                  f = new Date(paciente.ingreso_episodio);
                  ingreso_episodio = f.getFullYear() + "-" + ("0" + (f.getMonth() + 1)).slice(-2) + "-" + ("0" + f.getDate()).slice(-2);
                }

                $info = $navInfoPaciente.querySelectorAll("div"); //console.log($info)

                $info[4].querySelector("img").setAttribute("src", paciente.imagen);
                $info[7].querySelector("h4").textContent = paciente.paciente;
                $info[11].textContent = paciente.cedula;
                $info[14].textContent = paciente.edad;
                $info[17].textContent = paciente.sexo;
                $info[18].querySelector("input").value = ingreso_episodio;
                $info[19].querySelector("span").textContent = paciente.dias;
                $info[21].textContent = paciente.ubicacion;
                $info[22].querySelector("a").setAttribute("href", "https://api.whatsapp.com/send?phone=".concat(paciente.telefono_movil.replace("+", "")));
                $info[22].querySelector("a").innerHTML = "<i class=\"fab fa-whatsapp text-success\"></i> ".concat(paciente.telefono_movil.replace("+", ""));

                if (paciente.hasOwnProperty("resultados")) {
                  paciente.resultados.forEach(function (row) {
                    if (row.description === "Probabilidad diagnóstica") {
                      f = new Date(row.created_at);
                      fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(parseInt(f.getMonth())) + ", " + f.getFullYear();
                      imp_diagn += "\n                            <li class=\"list-group-item p-1 bg-light text-gray\">\n                                <div class=\"d-flex flex-column justify-content-start\">\n                                    ".concat(row.value, "\n                                </div>\n                                <div class=\"d-flex flex-colum justify-content-end\">\n                                    <span class=\"badge badge-gray rounded-custom text-secondary\"><i class=\"far fa-clock\"></i> ").concat(row.medico, " <span class=\"text-primary\">|</span> ").concat(fecha, " <span class=\"text-primary\">|</span> ").concat(row.hora, "</span>\n                                </div>\n\n                            </li>\n                        ");
                      contImpDiagn++;
                    }
                  });
                }

                if (contImpDiagn === 0) {
                  imp_diagn = "\n                    <li class=\"list-group-item p-1 bg-light text-gray\">\n                    <div class=\"text-center\">Sin Probabilidad Diagn\xF3stica</div>\n                    <a data-user_id_paciente=\"".concat(user_id, "\"  class='").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(model.episodio) ? "d-none" : "", " btn btn-light btn-block'>Agregar</a>\n                    </li>\n                ");
                }

                $info[23].querySelector("#impresion_diagnostica ul").innerHTML = imp_diagn;
                $header.innerHTML = "";
                $header.append($navInfoPaciente);

                if (!paciente.hasOwnProperty("medico_paciente")) {
                  paciente.medico_paciente = [];
                }

                configMedicos = [{
                  selector: "#lista_tratante",
                  grupo_medico_id: 1,
                  color: "success",
                  name: "Tratante",
                  medicosByPaciente: filtroMedico(paciente.medico_paciente, 1),
                  user_id: user_id
                }, {
                  selector: "#lista_interconsultante",
                  grupo_medico_id: 2,
                  color: "info",
                  name: "Interconsultante",
                  medicosByPaciente: filtroMedico(paciente.medico_paciente, 2),
                  user_id: user_id
                }, {
                  selector: "#lista_felow",
                  grupo_medico_id: 3,
                  color: "orange",
                  name: "Fellow",
                  medicosByPaciente: filtroMedico(paciente.medico_paciente, 3),
                  user_id: user_id
                }, {
                  selector: "#lista_residente",
                  grupo_medico_id: 4,
                  color: "secondary",
                  name: "Residente",
                  medicosByPaciente: filtroMedico(paciente.medico_paciente, 4),
                  user_id: user_id
                }, {
                  selector: "#lista_ramh",
                  grupo_medico_id: 5,
                  color: "purple",
                  name: "RAMH",
                  medicosByPaciente: filtroMedico(paciente.medico_paciente, 5),
                  user_id: user_id
                }];
                configMedicos.forEach(function (x) {
                  UserCuestMedicoPaciente.listadoMedicoPaciente(x);
                });
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["activarTooltip"])();

              case 29:
              case "end":
                return _context30.stop();
            }
          }
        }, _callee30);
      }));

      function infoPacienteHistorial(_x13) {
        return _infoPacienteHistorial.apply(this, arguments);
      }

      return infoPacienteHistorial;
    }()
  }, {
    key: "bienvenidaPaciente",
    value: function bienvenidaPaciente() {
      $("#messageModal").modal("show");
      $(".modal-body").html("\n            <div class=\"bg-primary p-3\">\n                <h3 class=\"display-4\" style=\"font-size: 2.5em;\">\n                    <i style=\"font-size: 0.8em;\">Bienvenido al</i><br>\n                    Programa de Atenci\xF3n Domiciliaria<br>\n                    <span style=\"overflow-wrap: normal;font-weight:400\">COVID-19</span>\n                </h3>\n                <p class=\"lead\" style=\"font-size: 1.4em;font-style: italic;\">\n                    Monitoreo a distancia para apoyarte en tu recuperaci\xF3n\n                    de s\xEDntomas relacionados al <br>COVID-19.<br>\n                    <br>\n                    Por favor, completa la informaci\xF3n, del siguiente cuestionario.<br><br>\n                    Te daremos las recomendaciones de qu\xE9 hacer al momento de completar la informaci\xF3n solicitada.<br>\n                    <br>\n                </p>\n                <small class=\"lead text-justify\" style=\"font-size: 1em;\">\n                    Este cuestionario no sustituye el examen m\xE9dico;\n                    sin embargo, permite evaluar tu riesgo y ofrecerte las recomendaciones\n                    acorde a tus respuestas.\n                    <br><br>\n                </small>\n\n\n\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 mt-2\">\n                        <button id=\"empezarcuestionario\" data-dismiss=\"modal\" type=\"button\" class=\"btn btn-light btn-block btn-lg text-primary\">De acuerdo, comenzar</button>\n                    </div>\n                    <!--\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 mt-2\">\n                        <button id=\"yacontestada\" type=\"button\" class=\"btn btn-danger btn-block btn-lg\">No, requiero atenci\xF3n de emergencia</button>\n                    </div>\n                    -->\n                </div>\n\n            </div>\n        ");
    }
  }, {
    key: "bienvenidaPaciente2",
    value: function bienvenidaPaciente2() {
      $("#messageModal").modal("show");
      $(".modal-body").html("\n            <div class=\"bg-primary p-3\">\n                <h3 class=\"display-4\" style=\"font-size: 2.5em;\">\n                    <i style=\"font-size: 0.8em;\">\n                        Bienvenido al<br>\n                        <span style=\"font-size: 0.8em;\">Programa de Atenci\xF3n Domiciliaria:</span>\n                    </i>\n\n                    <br>\n                    <span style=\"overflow-wrap: normal;font-weight:400\">Despistaje + Seguimiento COVID</span>\n                </h3>\n                <p class=\"lead\" style=\"font-size: 1.4em;font-style: italic;\">\n                    Monitoreo a distancia para apoyarte en tu recuperaci\xF3n\n                    de s\xEDntomas relacionados al <br>COVID-19.\n                    <br>\n                    Por favor, completa la informaci\xF3n, del siguiente cuestionario.<br>\n                </p>\n\n\n\n\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 \">\n                        <button id=\"empezarcuestionario\" data-dismiss=\"modal\" type=\"button\" class=\"btn btn-light btn-block btn-lg text-primary\">De acuerdo, comenzar</button>\n                    </div>\n                    <!--\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 mt-2\">\n                        <button id=\"yacontestada\" type=\"button\" class=\"btn btn-danger btn-block btn-lg\">No, requiero atenci\xF3n de emergencia</button>\n                    </div>\n                    -->\n                </div>\n\n            </div>\n        ");
    }
    /* formPaciente() {
      } */

  }, {
    key: "edit",
    value: function edit(selector, user_id) {
      var index_episodio = pacientes_datos.findIndex(function (paciente) {
        return paciente.user_id === user_id;
      });
      var episodio = pacientes_datos[index_episodio];
      $("#modal-patient-item").modal("show");
      document.querySelector("#modal-patient-item .modal-body-2").innerHTML =
      /*html */
      "\n\n            <div id='AppEditPacient' class=\"fade-in flex-fill d-flex flex-column  overflow-auto\">\n                <div class=\"container p-0\">\n                    <h3 class=\"text-primary\">\n                        Datos del Paciente\n                    </h3>\n                </div>\n                <div class=\"container\">\n                    <div class=\"row\">\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"cedula\">Documento de identidad</label>\n                                <table class=\"w-100\">\n                                    <tbody>\n                                        <tr>\n                                            <td style=\"width: 30%\">\n                                                <select v-model=\"form.nacionalidad\"\n                                                    class=\"form-control bg-gray-footer-parodi\" name=\"nacionalidad\"\n                                                    id=\"nacionalidad\">\n                                                    <option value=\"V\">V</option>\n                                                    <option value=\"E\">E</option>\n                                                    <option value=\"P\">P</option>\n                                                    <option value=\"J\">J</option>\n                                                </select>\n                                            </td>\n                                            <td>\n                                                <input @change=\"updateField('cedula')\" v-model=\"form.cedula\" type=\"text\"\n                                                    max=\"9\" class=\"form-control bg-gray-footer-parodi\" name=\"cedula\"\n                                                    id=\"cedula\" ref=\"cedula\" required aria-describedby=\"helpId\">\n                                            </td>\n                                        </tr>\n                                    </tbody>\n                                </table>\n                                <small v-if=\"isValidateCedula\" :class=\"['form-text notification',( cedulaExiste ? 'text-danger': 'text-success')]\">\n                                    <i class=\"fas fa-check-circle\"></i>\n                                    <span>C\xE9dula {{cedulaExiste ? ' no disponible para registrar': 'no registrada'}}</span>\n                                </small>\n                            </div>\n                        </div>\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n\n                            <div id=\"emailAlternativo\" class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"email_alternativo\">Correo electr\xF3nico\n                                    <span v-if=\"is_child\" class=\"font-weight-bold\">(del representante)</span></label>\n                                <input @change=\"updateField('email')\" v-model=\"form.email\" type=\"email\"\n                                    class=\"form-control bg-gray-footer-parodi\" name=\"email\" id=\"email\" ref=\"email\"\n                                    required>\n                                <small id=\"sms_email_alternativo\" class=\"form-text text-danger notification\"></small>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"nombre\">Nombres</label>\n                                <input v-model=\"form.nombres\" type=\"text\" class=\"form-control bg-gray-footer-parodi\"\n                                    name=\"nombres\" id=\"nombres\" aria-describedby=\"helpId\" ref=\"nombres\" required>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"apellido\">Apellidos</label>\n                                <input v-model=\"form.apellidos\" type=\"text\" class=\"form-control bg-gray-footer-parodi\"\n                                    name=\"apellidos\" id=\"apellidos\" aria-describedby=\"helpId\" ref=\"apellidos\" required>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"genero\">G\xE9nero</label>\n                                <select v-model=\"form.genero\" class=\"form-control bg-gray-footer-parodi\" name=\"genero\"\n                                    id=\"genero\" aria-describedby=\"helpId\" ref=\"genero\" required>\n                                    <option value=\"m\">Masculino</option>\n                                    <option value=\"f\">Femenino</option>\n                                </select>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"fnacimiento\">Fecha de\n                                    nacimiento</label>\n                                <input v-model=\"form.fnacimiento\" type=\"date\" class=\"form-control bg-gray-footer-parodi\"\n                                    name=\"fnacimiento\" id=\"fnacimiento\" aria-describedby=\"helpId\" ref=\"fnacimiento\"\n                                    required>\n                                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"telefono_movil\">Tel\xE9fono\n                                    Contacto</label>\n                                <input v-model=\"form.telefono_movil\" id=\"telefono_movil\"\n                                    @change=\"updateField('telefono_movil')\" ref=\"telefono_movil\" required type=\"text\"\n                                    class=\"form-control bg-gray-footer-parodi\" @keyup=\"vPrimerDigito('telefono_movil')\">\n                                <small id=\"help_telefono_movil\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                        <div class=\"col-12 col-sm-6\">\n                            <div class=\"form-group\">\n                                <label class=\"label-text text-secondary\" for=\"telefono_hogar\">Tel\xE9fono\n                                    Secundario</label>\n                                <input v-model=\"form.telefono_hogar\" id=\"telefono_hogar\"\n                                    @change=\"updateField('telefono_hogar')\" ref=\"telefono_hogar\" type=\"text\"\n                                    class=\"form-control bg-gray-footer-parodi\" @keyup=\"vPrimerDigito('telefono_hogar')\">\n                                <small id=\"help_telefono_hogar\" class=\"form-text text-muted notification\"></small>\n                            </div>\n                        </div>\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                            <div class=\"h3\">\n                                Direcci\xF3n\n                            </div>\n                        </div>\n                    </div>\n                    <div id=\"user_cuest_direccion_create\">\n\n                        <div class=\"row\">\n                            <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"cat_estado_id\">Estado</label>\n                                    <select v-model=\"form.cat_estado_id\" class=\"form-control bg-gray-footer-parodi\"\n                                        name=\"cat_estado_id\" id=\"cat_estado_id\" aria-describedby=\"helpId\"\n                                        ref=\"cat_estado_id\" required>\n                                        <option value=\"null\">Seleccione</option>\n                                        <option v-for=\"(item, index) in cat_estados\" :key=\"index\" :value=\"item.id\">\n                                            {{ item.description }}</option>\n                                    </select>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                            <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"cat_municipio_id\">Municipio</label>\n                                    <select v-model=\"form.cat_municipio_id\" class=\"form-control bg-gray-footer-parodi\"\n                                        name=\"cat_municipio_id\" id=\"cat_municipio_id\" aria-describedby=\"helpId\"\n                                        ref=\"cat_municipio_id\" required>\n                                        <option value=\"\">Seleccione</option>\n                                        <option v-for=\"(item, index) in getCatMunicipio\" :key=\"index\" :value=\"item.id\">\n                                            {{ item.description }}</option>\n                                    </select>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                            <div class=\"col-xs-4 col-sm-4 col-md-4 col-lg-4 col-xl-4\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"description\">Ciudad</label>\n                                    <input v-model=\"form.description\" type=\"text\"\n                                        class=\"form-control bg-gray-footer-parodi\" name=\"description\" id=\"description\"\n                                        aria-describedby=\"helpId\" ref=\"description\" required>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                        </div>\n                        <div class=\"row\">\n                            <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                                <div class=\"form-group\">\n                                    <label class=\"label-text text-secondary\" for=\"domicilio\">Domicilio</label>\n                                    <textarea v-model=\"form.domicilio\" class=\"form-control bg-gray-footer-parodi\"\n                                        name=\"domicilio\" id=\"domicilio\" rows=\"2\" ref=\"domicilio\" required></textarea>\n                                    <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                                </div>\n                            </div>\n                        </div>\n\n                    </div>\n                    <div class=\"row\">\n                        <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                            <div class=\"text-center\">\n\n                                <img\n                                    onerror=\"this.src='/image/system/icono-paciente-blanco.svg'\"\n                                    id=\"imagen_perfil\"\n                                    style=\"width: 10%; height:10%\"\n                                    :src=\"form.imagen\"\n                                    class=\"imagen-perfil\"\n                                    alt=\"Medic default\"\n                                >\n\n                                <br>\n                                <i id=\"ampliar\"\n                                    class=\"heartbeat cursor-pointer mt-1 fas fa-search-plus text-primary mr-1\"\n                                    style=\"font-size:1.3em;\"></i>\n                                <i id=\"reducir\" class=\"heartbeat cursor-pointer mt-1 fas fa-search-minus text-primary\"\n                                    style=\"font-size:1.3em;\"></i>\n                            </div>\n                            <div class=\"form-group text-center\">\n                                <label class=\"label-text text-secondary\" for=\"imagen\">Subir Documento de idendidad del\n                                    Paciente</label>\n                                <input\n                                    @change=\"validarArchivoImagen('imagen')\"\n                                    type=\"file\"\n                                    class=\"form-control bg-gray-footer-parodi\"\n                                    name=\"imagen\"\n                                    id=\"imagen\"\n                                    aria-describedby=\"helpId\"\n                                >\n                                <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                            </div>\n                        </div>\n                    </div>\n                </div>\n\n                <div class=\"container\">\n                    <div class=\"row\">\n                        <div class=\"col-6\">\n                            <button @click=\"handle_store()\" id=\"user_cuest_model_update\"\n                                class=\"font-weight-bold btn btn-success w-100\">Guardar</button>\n                        </div>\n                        <div class=\"col-6\">\n                            <button id=\"regresar\" @click=\"closeModal()\" aria-label=\"Close\"\n                                class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n                        </div>\n                    </div>\n                </div>\n            </div>\n        ";
      var app = new Vue({
        el: "#AppEditPacient",
        data: function data() {
          return {
            items: pacientes_datos,
            is_child: false,
            cat_estados: Array.from(catUserEstado.getAll()),
            telefonoConfig1: null,
            telefonoConfig2: null,
            dataPaciente: null,
            isValidateCedula: false,
            cedulaExiste: true,
            form: {
              id: null,
              email: null,
              email_alternativo: null,
              nacionalidad: null,
              cedula: null,
              telefono_movil: null,
              new_imagen: null,
              imagen: null,
              nombres: null,
              apellidos: null,
              genero: null,
              fnacimiento: null,
              telefono_hogar: null,
              cat_user_type_id: null,
              cat_estado_id: null,
              cat_municipio_id: null,
              description: null,
              domicilio: null
            }
          };
        },
        props: {//patient_data: Object,
          //index: Number,
        },
        computed: {
          getCatMunicipio: function getCatMunicipio() {
            var _this22 = this;

            return Array.from(catUserMunicipio.getAll()).filter(function (item) {
              return item["cat_estado_id"] === _this22.form.cat_estado_id;
            });
          }
        },
        methods: {
          validateForm: function validateForm() {
            var formNotValidate = true;
            var inputName = "cedula";
            var input = this.$refs[inputName];

            if ([null, ""].includes(this.form[inputName]) && formNotValidate) {
              alert("El campo cédula no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "email";
            input = this.$refs[inputName];

            if ([null, ""].includes(this.form[inputName]) && formNotValidate) {
              alert("El campo Correo electrónico no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "nombres";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Nombres no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "apellidos";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Apellidos no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "genero";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Género no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "fnacimiento";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Fecha de Nacimiento no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "telefono_movil";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Teléfono Contacto no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "cat_estado_id";
            input = this.$refs[inputName];
            console.log(this.form[inputName]);

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Estado no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "cat_municipio_id";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Municipio no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "description";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Ciudad no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            inputName = "domicilio";
            input = this.$refs[inputName];

            if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(this.form[inputName]) && formNotValidate) {
              alert("El campo Domicilio no puede estar vacio.");
              input.focus();
              formNotValidate = false;
              return false;
            }

            if (formNotValidate) {
              return true;
            } else {
              return false;
            }
          },
          store: function store() {
            var _this23 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee31() {
              var formdata, key, element;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee31$(_context31) {
                while (1) {
                  switch (_context31.prev = _context31.next) {
                    case 0:
                      _this23.form.new_imagen = document.getElementById("imagen").files[0];
                      formdata = new FormData();

                      for (key in _this23.form) {
                        if (Object.hasOwnProperty.call(_this23.form, key)) {
                          element = _this23.form[key];
                          formdata.append(key, element);
                        }
                      }

                      formdata.append("user_cuest_episodio_id", episodio.episodio_id);
                      formdata.append("user_id_paciente", episodio.user_id);
                      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                      formdata.append("_token", $("#_token").val());
                      _context31.next = 9;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_paciente/update", formdata);

                    case 9:
                      return _context31.abrupt("return", _context31.sent);

                    case 10:
                    case "end":
                      return _context31.stop();
                  }
                }
              }, _callee31);
            }))();
          },
          imagePreview: function imagePreview(archivo, selector) {
            var showImageName = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
            // We create the object of the FileReader class
            var reader = new FileReader(); // We read the uploaded file and pass it to our fileReader.

            reader.readAsDataURL(archivo);
            var file = archivo; //We look for the following element where we will show the name of the uploaded file.

            var name = file.name,
                size = file.size,
                type = file.type;

            if (showImageName) {//e.target.nextElementSibling.textContent= name
            } // We tell it that when it is ready, run the internal code.


            reader.onload = function (load) {
              document.getElementById(selector).setAttribute("src", reader.result);
            };
          },
          validarArchivoImagen: function validarArchivoImagen(id) {
            var archivoInput = document.getElementById(id);
            var archivo = archivoInput.files[0];
            console.log(archivo.name);

            if (archivo) {
              var extension = archivo.name.split(".").pop().toLowerCase();

              if (!(extension === "jpg" || extension === "jpeg" || extension === "png")) {
                Swal.fire({
                  icon: "error",
                  title: "Hubo un error...",
                  text: "Solo se permiten archivos .jpg o .png."
                });
                archivoInput.value = ""; // Limpiar el campo de entrada
              }

              this.imagePreview(archivo, "imagen_perfil");
            }
          },
          updateField: function updateField(fieldName) {
            var _this24 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee32() {
              var result, _result2, inputName, input;

              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee32$(_context32) {
                while (1) {
                  switch (_context32.prev = _context32.next) {
                    case 0:
                      if (!(fieldName === "cedula")) {
                        _context32.next = 5;
                        break;
                      }

                      _context32.next = 3;
                      return _this24.validateCedula();

                    case 3:
                      result = _context32.sent;

                      if (!result.exist) {
                        _this24.is_a_child();
                      }

                    case 5:
                      if (!(fieldName === "email")) {
                        _context32.next = 11;
                        break;
                      }

                      _context32.next = 8;
                      return _this24.validateEmail();

                    case 8:
                      _result2 = _context32.sent;

                      if (!_result2.exist) {
                        inputName = "email";
                        input = _this24.$refs[inputName];
                        alert(_this24.form.email + " ya se encuentra asociado a otro paciente.");
                        _this24.form.email = _this24.originalEmail;
                        input.focus();
                      }

                      return _context32.abrupt("return", false);

                    case 11:
                      if (!(fieldName === "telefono_movil")) {
                        _context32.next = 14;
                        break;
                      }

                      _this24.form.telefono_movil = _this24.telefonoConfig1.getNumber();
                      return _context32.abrupt("return", false);

                    case 14:
                      if (!(fieldName === "telefono_hogar")) {
                        _context32.next = 17;
                        break;
                      }

                      _this24.form.telefono_hogar = _this24.telefonoConfig2.getNumber();
                      return _context32.abrupt("return", false);

                    case 17:
                    case "end":
                      return _context32.stop();
                  }
                }
              }, _callee32);
            }))();
          },
          is_a_child: function is_a_child() {
            if (this.form.cedula !== null) {
              var numeroValido = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["validarNumeroConDosDecimales"])(this.form.cedula);

              if (numeroValido) {
                this.is_child = true;
              } else {
                this.is_child = false;
              }
            }
          },
          validateEmail: function validateEmail() {
            var _this25 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee33() {
              var formdata, result;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee33$(_context33) {
                while (1) {
                  switch (_context33.prev = _context33.next) {
                    case 0:
                      if (!(_this25.form.cedula !== "")) {
                        _context33.next = 12;
                        break;
                      }

                      formdata = new FormData();
                      formdata.append("nacionalidad", _this25.form.nacionalidad);
                      formdata.append("cedula", _this25.originalCedula);
                      formdata.append("email", _this25.form.email);
                      formdata.append("_token", $("#_token").val());
                      _context33.next = 8;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/emailExist", formdata);

                    case 8:
                      result = _context33.sent;
                      return _context33.abrupt("return", result);

                    case 12:
                      return _context33.abrupt("return", {
                        exist: false
                      });

                    case 13:
                    case "end":
                      return _context33.stop();
                  }
                }
              }, _callee33);
            }))();
          },
          getDataPaciente: function getDataPaciente() {
            var _this26 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee34() {
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee34$(_context34) {
                while (1) {
                  switch (_context34.prev = _context34.next) {
                    case 0:
                      _context34.next = 2;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])("user_paciente/getdatapaciente/".concat(user_id));

                    case 2:
                      _this26.dataPaciente = _context34.sent;

                    case 3:
                    case "end":
                      return _context34.stop();
                  }
                }
              }, _callee34);
            }))();
          },
          validateCedula: function validateCedula() {
            var _this27 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee35() {
              var that, formdata, result;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee35$(_context35) {
                while (1) {
                  switch (_context35.prev = _context35.next) {
                    case 0:
                      that = _this27;

                      if (!(_this27.form.cedula !== "")) {
                        _context35.next = 15;
                        break;
                      }

                      formdata = new FormData();
                      formdata.append("nacionalidad", _this27.form.nacionalidad);
                      formdata.append("cedula", _this27.form.cedula);
                      formdata.append("_token", $("#_token").val());
                      _context35.next = 8;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/getPatient/" + _this27.form.cedula);

                    case 8:
                      result = _context35.sent;
                      _this27.isValidateCedula = true;

                      if (result.exist) {
                        _this27.cedulaExiste = true;
                        alert("La cédula " + _this27.form.cedula + " ya se encuentra asociada a un paciente.");
                        _this27.form["cedula"] = _this27.originalCedula;
                      } else {
                        _this27.cedulaExiste = false;
                      }

                      console.log("cedula_encontrada->", result);
                      return _context35.abrupt("return", result);

                    case 15:
                      return _context35.abrupt("return", {
                        exist: false
                      });

                    case 16:
                    case "end":
                      return _context35.stop();
                  }
                }
              }, _callee35);
            }))();
          },
          vPrimerDigito: function vPrimerDigito(param) {
            Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["validarPrimerDigito"])(param);
          },
          handle_store: function handle_store() {
            var _this28 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee36() {
              var that, response;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee36$(_context36) {
                while (1) {
                  switch (_context36.prev = _context36.next) {
                    case 0:
                      that = _this28;

                      if (!that.validateForm()) {
                        _context36.next = 6;
                        break;
                      }

                      _context36.next = 4;
                      return that.store();

                    case 4:
                      response = _context36.sent;

                      if (response.success) {
                        //console.log("----->",that.form);
                        that.items[index_episodio].nombres = that.form.nombres;
                        that.items[index_episodio].apellidos = that.form.apellidos;
                        that.items[index_episodio].cedula = that.form.cedula;
                        that.items[index_episodio].email = that.form.email;
                        that.items[index_episodio].genero = that.form.genero;
                        that.items[index_episodio].sexo = that.form.genero;

                        if (response.data.new_imagen === null) {
                          that.items[index_episodio].imagen = that.form.imagen;
                        } else {
                          that.items[index_episodio].imagen = response.data.new_imagen;
                        }

                        that.items[index_episodio].nacionalidad = that.form.nacionalidad;
                        that.items[index_episodio].telefono_movil = that.form.telefono_movil;
                        that.items[index_episodio].telefono_hogar = that.form.telefono_hogar;
                        that.items[index_episodio].paciente = "".concat(that.form.nombres, " ").concat(that.form.apellidos); //console.log(that.items[index_episodio]);
                        // Escuchar cambios en el bus de eventos

                        EventBus.$on("externalVarChanged", function (newValue) {
                          that.items = newValue;
                        }); //alert("Datos Actualizados. Los datos fueron actualizados correctamente.")

                        that.closeModal();
                      } else {
                        alert("Error. Ocurrió un error al guardar los datos.");
                      }

                    case 6:
                    case "end":
                      return _context36.stop();
                  }
                }
              }, _callee36);
            }))();
          },
          handle_edit: function handle_edit() {
            var _this29 = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee37() {
              var key, element;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee37$(_context37) {
                while (1) {
                  switch (_context37.prev = _context37.next) {
                    case 0:
                      _context37.next = 2;
                      return _this29.getDataPaciente();

                    case 2:
                      for (key in _this29.dataPaciente) {
                        if (Object.hasOwnProperty.call(_this29.dataPaciente, key)) {
                          element = _this29.dataPaciente[key];

                          if (key == "cedula") {
                            _this29.originalCedula = element;
                          }

                          if (key == "email") {
                            _this29.originalEmail = element;
                          }

                          _this29.form[key] = element;
                        }
                      }

                      if (_this29.form.cat_estado_id === null) {
                        _this29.form.cat_estado_id = 1;
                      }

                      if (_this29.form.cat_municipio_id === null) {
                        _this29.form.cat_municipio_id = 1;
                      }

                      if (_this29.form.description === null) {
                        _this29.form.description = "No Indicado";
                      }

                      if (_this29.form.domicilio === null) {
                        _this29.form.domicilio = "No Indicado";
                      }

                      console.log(_this29.form);

                      _this29.is_a_child();

                      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_movil", function (iti, input) {
                        that.telefonoConfig1 = iti; // Manejar el evento countrychange

                        input.addEventListener("countrychange", function () {
                          var selectedCountryData = iti.getSelectedCountryData(); //console.log("País seleccionado:", selectedCountryData.name);
                          //console.log("Código de país seleccionado:", selectedCountryData.dialCode);

                          that.updateField("telefono_movil");
                        });
                      });
                      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_hogar", function (iti, input) {
                        that.telefonoConfig2 = iti; // Manejar el evento countrychange

                        input.addEventListener("countrychange", function () {
                          var selectedCountryData = iti.getSelectedCountryData(); //console.log("País seleccionado:", selectedCountryData.name);
                          //console.log("Código de país seleccionado:", selectedCountryData.dialCode);

                          that.updateField("telefono_hogar");
                        });
                      });

                    case 11:
                    case "end":
                      return _context37.stop();
                  }
                }
              }, _callee37);
            }))();
          },
          closeModal: function closeModal() {
            /* this.$store.commit('updateDinamicComponent', {
                is_dismounted: true,
                customComponent: null,
                patient_data: null,
                index: null
            }); */
            $("#modal-patient-item").modal("hide");
          }
        },
        mounted: function mounted() {
          this.handle_edit(); ///console.log(this.dataPaciente);

          /* let that = this
          let { editDataPaciente } = this.$store.state
           */
        }
      });
    }
  }, {
    key: "editDEPRECATED20240704",
    value: function editDEPRECATED20240704(selector, user_id) {
      $("#modal-patient-item").modal("show"); //CREATE PARA REGISTRO PACIENTE INTERNO

      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n\n            <div class=\"container\">\n                <div class=\"row mt-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Datos del paciente\n                        </div>\n                    </div>\n                </div>\n\n                <div id=\"user_create\" ></div>\n                <!--<div class=\"p-3 mb-2 bg-light text-dark text-center\">\n                    Para actualizar el <b>Correo Electr\xF3nico</b> o el <b>Documento de identidad</b>, comunicarse con el administrador del sistema.\n                </div>-->\n                <div id=\"user_profile_create\" ></div>\n                <input id=\"cat_user_type_id\" type=\"hidden\">\n\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Direcci\xF3n\n                        </div>\n                    </div>\n                </div>\n                <div id=\"user_cuest_direccion_create\" ></div>\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <div  class=\"text-center\" >\n                            <img onerror=\"this.src='/image/system/icono-paciente-blanco.svg'\"  id=\"imagen_perfil\" style=\"width: 10%; height:10%\" src=\"/image/user/foto_perfil/doc_id.svg\" class=\"imagen-perfil\" alt=\"Medic default\">\n                            <br/>\n                            <i id=\"ampliar\" class=\"heartbeat cursor-pointer mt-1 fas fa-search-plus text-primary mr-1\" style=\"font-size:1.3em;\"></i>\n                            <i id=\"reducir\" class=\"heartbeat cursor-pointer mt-1 fas fa-search-minus text-primary\" style=\"font-size:1.3em;\"></i>\n                        </div>\n                        <div class=\"form-group text-center\">\n                            <label class=\"label-text text-secondary\" for=\"imagen\">Subir Documento de identidad del Paciente</label>\n                            <input type=\"file\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"imagen\" id=\"imagen\" aria-describedby=\"helpId\" placeholder=\"\">\n                            <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                        </div>\n                    </div>\n                </div>\n\n                <div class=\"row\">\n                    <div class=\"col-6\">\n                        <button id=\"user_cuest_model_update\" class=\"font-weight-bold btn btn-success w-100\">Guardar</button>\n                    </div>\n                    <div class=\"col-6\">\n                        <button id=\"regresar\"  data-dismiss=\"modal\" aria-label=\"Close\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n                    </div>\n                </div>\n            </div>\n        ");
      document.getElementById("cat_user_type_id").value = 1;
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])("imagen", ".imagen-perfil");
      $("#ampliar").on("click", function () {
        $("#imagen_perfil").animate({
          width: "80%"
        }, 500);
      });
      $("#reducir").on("click", function () {
        $("#imagen_perfil").animate({
          width: "10%"
        }, 500);
      });

      function validarNumeroConDosDecimales(inputValue) {
        var regex = /^\d+\.\d{2}$/;
        return regex.test(inputValue);
      } //VISTAS


      user.create2("#user_create");
      userProfile.create("#user_profile_create");
      userCuestDireccion.create("#user_cuest_direccion_create"); //$("#nacionalidad,#cedula, #email").css("border", 0);

      var tempCedula = "";
      var tempEmail = "";
      user.show(user_id).then(function (response) {
        console.log("User/show: ", response);
        tempEmail = response.email;
        document.getElementById("email").dataset.user_id = user_id;
        document.getElementById("email").dataset.email = response.email;
        $("#email").val(response.email); //.attr("readonly", true)

        $("#email_alternativo").val(response.email_alternativo); //.attr("readonly", true)

        return response.id;
      }).then(function (response) {
        userProfile.show(response).then(function (response) {
          console.log("UserProfile/show: ", response);
          tempCedula = response.cedula;
          $("#nacionalidad").val(response.nacionalidad); //.attr("disabled", true)

          $("#cedula").val(response.cedula); //.attr("readonly", true)

          var numeroValido = validarNumeroConDosDecimales(response.cedula);

          if (numeroValido) {
            document.querySelector("#correoAdulto").classList.add("d-none");
            document.querySelector("#emailAlternativo").classList.remove("d-none");
          } else {
            document.querySelector("#correoAdulto").classList.remove("d-none");
            document.querySelector("#emailAlternativo").classList.add("d-none");
          } //$("#cedula").data("cedula",response.cedula)


          document.getElementById("cedula").dataset.cedula = response.cedula;
          $("#nombres").val(response.nombres);
          $("#apellidos").val(response.apellidos);
          $("#fnacimiento").val(response.fnacimiento);
          $("#genero").val(response.genero);
          $("#telefono_movil").val(response.telefono_movil);
          $(".imagen-perfil").attr("src", response.imagen == "patient.svg" ? "/image/user/foto_perfil/doc_id.svg" : response.imagen);
          return response.user_id;
        }).then(function (response) {
          userCuestDireccion.show(response).then(function (response) {
            console.log("UserCuestDireccion/show: ", response);
            $("#cat_estado_id").val(response.cat_estado_id);
            $("#description").val(response.description);
            $("#domicilio").val(response.domicilio);
            var municipio_id = response.cat_municipio_id;
            catUserMunicipio.show($("#cat_estado_id").val()).then(function (response) {
              $("#cat_municipio_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(response, municipio_id));
              return response;
            }).then(function (response) {
              Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["telefonoConfig"])("#telefono_movil");
              var text = iti.isValidNumber() ? "<span class='text-success'><i class='far fa-check-circle'></i> Formato correcto</span>" : "<span class='text-danger'><i class='fas fa-exclamation-circle'></i> Formato incorrecto</span>";
              $("#help_telefonomovil").html(text); // $("#messageModal").modal('hide')
              //loadingModal("hide")
            });
            return response.user_id;
          });
        });
      });
      $("#user_cuest_model_update").on("click", function () {
        if (userser.validate2()) {
          if (userProfile.validate()) {
            if (userCuestDireccion.validate()) {
              if (userCuestUbicacion.validate2()) {
                var _model3 = user.update();

                _model3.then(function (response) {
                  var model = userProfile.update(user_id);
                  model.then(function (response) {
                    //console.log("UserProfile:", response);
                    pacientes_datos.map(function (x) {
                      //console.log(x.user_id)
                      if (x.user_id == response.user_id) {
                        x.telefono_movil = response.telefono_movil;
                      }

                      return x;
                    });
                    return response.user_id;
                  }).then(function (response) {
                    model = userCuestDireccion.update(response);
                    model.then(function (response) {
                      console.log("UserCuestDireccion:", response);
                      return response.user_id;
                    }).then(function (response) {
                      //alert("Información actualizada")
                      message = Component.alertMessage("update_success");
                      Toast.fire({
                        icon: message["image"],
                        title: message["title"],
                        text: message["message"],
                        didClose: function didClose() {
                          setTimeout(function () {
                            $("#modelId").modal("hide");
                          }, 110);
                        }
                      });
                    });
                  });
                });
              }
            }
          }
        }
      });
    }
  }, {
    key: "show1",
    value: function show1(selector, user_id) {
      $("#modelId").modal("show");
      $(selector).html("\n            <div class=\"container\">\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg12 \">\n                        <div class=\"table-responsive-md\">\n                            <table class=\"table table-bordered\">\n                                <tbody>\n                                    <tr>\n                                        <td style=\"color:var(--primary)!important\" colspan=\"5\" class=\"text-center bg-gray text-uppercaser text-primary font-weight-bold text-uppercase\">Informaci\xF3n del paciente</td>\n                                    </tr>\n                                    <tr>\n                                        <td style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">Nombres</td>\n                                        <td style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">Apellidos</td>\n                                        <td style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">C\xE9dula</td>\n                                        <td style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">Edad</td>\n                                    </tr>\n                                    <tr>\n                                        <td class=\"text-secondary text-center\">Ranses Alejandro</td>\n                                        <td class=\"text-secondary text-center\">Jimenez dominguez</td>\n                                        <td class=\"text-secondary text-center\">22.014.778</td>\n                                        <td class=\"text-secondary text-center\">22</td>\n\n                                    </tr>\n                                    <tr>\n                                        <td colspan=\"2\" style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">Sexo</td>\n                                        <td colspan=\"2\" style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">Direcci\xF3n</td>\n                                    </tr>\n                                    <tr>\n                                        <td colspan=\"2\" class=\"text-secondary text-center\">M</td>\n                                        <td colspan=\"2\" class=\"text-secondary text-center\">Distrito capital, Municipio libertador, Caracas</td>\n                                    </tr>\n                                    <tr>\n                                        <td colspan=\"2\" style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">S\xEDntomas</td>\n                                        <td colspan=\"2\" style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">Antecedentes</td>\n                                    </tr>\n                                    <tr>\n                                        <td colspan=\"2\" class=\"text-secondary text-center\">Temperatura mayor a 38\xB0</td>\n                                        <td colspan=\"2\" class=\"text-secondary text-center\">Enfermedad cardiovascular</td>\n                                    </tr>\n                                    <tr>\n                                        <td colspan=\"2\" style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">Temperatura</td>\n                                        <td colspan=\"2\" style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light\">Oximetr\xEDa</td>\n                                    </tr>\n                                    <tr>\n                                        <td colspan=\"2\" class=\"text-secondary text-center text-danger font-weight-bold\">38\xB0C</td>\n                                        <td colspan=\"2\" class=\"text-secondary text-center text-danger font-weight-bold\">90%</td>\n                                    </tr>\n                                    <tr>\n                                        <td colspan=\"4\" style=\"color:var(--primary)!important\" class=\"text-center text-primary font-weight-bold bg-light h3\">OBSERVACIONES</td>\n                                    </tr>\n                                    <tr>\n                                        <td colspan=\"4\" class=\"text-secondary text-center h3\">\n                                            <div class=\"form-group\">\n                                            <textarea class=\"form-control\" name=\"\" id=\"\" rows=\"3\"></textarea>\n                                            </div>\n                                        </td>\n                                    </tr>\n                                </tbody>\n                            </table>\n                        </div>\n                    </div>\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg12 \">\n                        <button class=\"btn btn-success btn-block btn-lg\">Guardar</button>\n                    </div>\n                </div>\n            </div>\n        ");
    }
  }], [{
    key: "showImpDiagnostica",
    value: function () {
      var _showImpDiagnostica = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee38(user_id) {
        var formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee38$(_context38) {
          while (1) {
            switch (_context38.prev = _context38.next) {
              case 0:
                formdata = new FormData();
                formdata.append("_token", $("#_token").val());
                return _context38.abrupt("return", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_impresion_diagnostica/show/" + user_id, formdata));

              case 3:
              case "end":
                return _context38.stop();
            }
          }
        }, _callee38);
      }));

      function showImpDiagnostica(_x14) {
        return _showImpDiagnostica.apply(this, arguments);
      }

      return showImpDiagnostica;
    }()
  }]);

  return UserCuestPaciente;
}();



/***/ }),

/***/ "./public/class_js/UserCuestPendiente.js":
/*!***********************************************!*\
  !*** ./public/class_js/UserCuestPendiente.js ***!
  \***********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestPendiente; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
/* harmony import */ var _UserCuestHistoria_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./UserCuestHistoria.js */ "./public/class_js/UserCuestHistoria.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }




var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"]();
var userCuestHistoria = new _UserCuestHistoria_js__WEBPACK_IMPORTED_MODULE_3__["default"]();

var UserCuestPendiente = /*#__PURE__*/function () {
  function UserCuestPendiente() {
    _classCallCheck(this, UserCuestPendiente);
  }

  _createClass(UserCuestPendiente, [{
    key: "index",
    value: function index(selector, user_id) {
      var index = pacientes_datos.findIndex(function (x) {
        return x.user_id === user_id;
      });
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div id=\"AppPending\" class=\"flex-fill container-fluid d-flex flex-column\">\n                <div class=\"d-flex flex-wrap align-items-center my-2\">\n                <div class=\"col-12 col-sm-6\">\n                    <h3 class=\"text-primary mb-0\">{{!create ? '':'Nuevo '}}Pendiente{{!create ? 's':''}}</h3>\n                </div>\n                <div class=\"col-12 col-sm-6 text-sm-right text-left\">\n                    <button \n                    @click=\"create = !create\" style=\"font-size:1.5rem\" \n                    v-if=\"!create\"\n                    class=\"btn btn-default text-primary mb-0\"\n                    >\n                    Nuevo pendiente <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                    </button>\n                </div>\n                \n                </div>\n                <div v-if=\"!create\" class=\"flex-fill d-flex flex-column overflow-auto\">\n                <div class=\"flex-fill d-flex overflow-auto\">\n                    <div class=\"col-12 col-sm-6 d-flex flex-column overflow-auto\">\n                    <div class=\"p-1 mb-1 flex-fill d-flex flex-column overflow-auto\"\n                        style=\"background-color:#f3f3f3;border-radius:1rem\">\n                        <div class=\"d-flex align-items-center\">\n                        <h3 class=\"text-primary\">Por hacer</h3>\n                        <span class=\"ml-1 badge badge-danger\">\n                            {{listItems.filter(item=>Number(item.active)).length}}\n                        </span>\n                        </div>\n                        <div class=\"flex-fill d-flex flex-column overflow-auto\">\n                        <div v-if=\"listItems.filter(item=>Number(item.active)).length > 0\">\n                            <div \n                            v-for=\"(item,index) in listItems.filter(item=>Number(item.active))\" \n                            :key=\"item.id\" \n                            @click=\"selectItem(item.id)\"\n                            :class=\"['card cursor-pointer shadow-sm d-flex p-1 mb-1',{'active':itemsMarks.includes(Number(item.id))}]\"\n                            >\n                            <div class=\"d-flex align-items-center justify-content-between\">\n                                <div class=\"d-flex align-items-center\">\n                                <input\n                                    :checked=\"itemsMarks.includes(Number(item.id))\"\n                                    style=\"width:1rem; height:1rem\"\n                                    type=\"checkbox\"\n                                    class=\"mx-1 mt-1 justify-content-start\"\n                                    >\n                                <h5 class=\"mb-0\">{{item.title}}</h5>\n                                </div>\n                                <h6 class=\"mb-0 text-nowrap\">{{item.dias > 0 ? 'Hace '+item.dias+' dia' + (item.dias > 1 ? 's':'') : 'Hoy'}}</h6>\n                            </div>\n                            <div class=\"ml-4 d-flex\">\n                                {{item.description}}\n                            </div>\n                            <div class=\"d-flex justify-content-end align-items-center\">\n                                <img\n                                    onerror=\"this.src='/image/system/medic.svg';\"\n                                    :src=\"item.imagen\"\n                                    style=\"border-radius:1rem;width:1rem; height:1rem;margin-right:0.2rem\" alt=\"User Image\"\n                                > {{item.medico}}\n                            </div>\n                            </div>\n                        </div>\n                        <div v-else class=\"p-3 text-center text-secondary\">\n                            Nada pendiente\n                        </div>\n                        </div>\n                    </div>\n                    </div>\n                    <div class=\"col-12 col-sm-6 d-flex flex-column overflow-auto\">\n                    <div class=\"p-1 mb-1 flex-fill d-flex flex-column overflow-auto\"\n                        style=\"background-color:#f3f3f3;border-radius:1rem\">\n                        <div class=\"d-flex align-items-center\">\n                        <h3 class=\"text-primary\">Completados</h3>\n                        <span class=\"ml-1 badge badge-success\">\n                            {{listItems.filter(item=>!Number(item.active)).length}}\n                        </span>\n                        </div>\n                        <div class=\"flex-fill d-flex flex-column overflow-auto\">\n                        <div v-if=\"listItems.filter(item=>!Number(item.active)).length > 0\">\n                            <div \n                            v-for=\"(item,index) in listItems.filter(item=>!Number(item.active))\" \n                            :key=\"item.id\" \n                            \n                            :class=\"['card shadow-sm d-flex p-1 mb-1']\"\n                            >\n                            <div class=\"d-flex align-items-center justify-content-between\">\n                            \n                                \n                                <h5 class=\"mb-0\">{{item.title}}</h5>\n                                \n                                <h6 class=\"mb-0 text-nowrap\">{{item.dias > 0 ? 'Hace '+item.dias+' dia' + (item.dias > 1 ? 's':'') : 'Hoy'}}</h6>\n                            </div>\n                            <div class=\"d-flex\">\n                                {{item.description}}\n                            </div>\n                            <div class=\"d-flex justify-content-end align-items-center\">\n                                <img\n                                    onerror=\"this.src='/image/system/medic.svg';\"\n                                    :src=\"item.imagen\"\n                                    style=\"border-radius:1rem;width:1rem; height:1rem;margin-right:0.2rem\" alt=\"User Image\"\n                                > {{item.medico}}\n                            </div>\n                            </div>\n                        </div>\n                        <div v-else class=\"p-3 text-center text-secondary\">\n                            Nada Completado\n                        </div>\n                        </div>\n                    </div>\n                    </div>\n                </div>\n                <div class=\"d-flex mb-1\">\n                    <div class=\"col-6\">\n                    <button \n                        @click=\"updateItems\" \n                        :disabled=\"!itemsMarks.length > 0\" \n                        class=\"btn btn-success w-100 mb-0\"\n                    >\n                        Completar pendientes\n                    </button>\n                    </div>\n                    <div class=\"col-6\">\n                    <button data-dismiss=\"modal\" aria-label=\"Close\" class=\"btn btn-primary w-100 mb-0\">Cerrar</button>\n                    </div>\n                </div>\n                </div>\n                <div v-else class=\"flex-fill d-flex flex-column overflow-auto\">\n                <div>\n                    <input \n                        type=\"text\" \n                        class=\"form-control form-control-lg bg-gray-footer-parodi\" \n                        name=\"value\" \n                        v-model=\"title\"\n                        aria-describedby=\"helpId\" \n                        placeholder=\"Titulo\"\n                    >\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"flex-fill mb-1\">\n                    <textarea \n                        class=\"form-control bg-gray-footer-parodi \" \n                        name=\"coments\"  \n                        v-model=\"description\"\n                        style=\" width: 100%;height: 100%;resize: none;\" \n                        placeholder=\"Observaci\xF3n\"\n                    ></textarea>\n                </div>\n                <div class=\"d-flex mb-1\">\n                    <div class=\"col-6\">\n                    <button \n                        @click=\"storeItems\" \n                        :disabled=\"saving\" \n                        class=\"btn btn-success w-100 mb-0\"\n                    >\n                        Agregar\n                    </button>\n                    </div>\n                    <div class=\"col-6\">\n                    <button @click=\"create = !create\" class=\"btn btn-primary w-100 mb-0\">Regresar</button>\n                    </div>\n                </div>\n                </div>\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      var app = new Vue({
        el: '#AppPending',
        components: {},
        data: function data() {
          return {
            user_id: null,
            index: null,
            episodios: pacientes_datos,
            title: '',
            description: '',
            create: false,
            saving: false,
            listItems: [],
            itemsMarks: []
          };
        },
        methods: {
          selectItem: function selectItem(itemId) {
            var item = this.itemsMarks.find(function (x) {
              return x === Number(itemId);
            });

            if (item) {
              this.itemsMarks = this.itemsMarks.filter(function (x) {
                return x !== Number(itemId);
              });
            } else {
              this.itemsMarks.push(Number(itemId));
            }
          },
          addItem: function addItem() {},
          showItems: function showItems() {
            var _this = this;

            return _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee() {
              var formdata;
              return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
                while (1) {
                  switch (_context.prev = _context.next) {
                    case 0:
                      formdata = new FormData();
                      formdata.append('user_id', user_id);
                      formdata.append('_token', $('#_token').val());
                      _context.next = 5;
                      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + '/user_cuest_pendiente/show', formdata);

                    case 5:
                      _this.listItems = _context.sent;

                    case 6:
                    case "end":
                      return _context.stop();
                  }
                }
              }, _callee);
            }))();
          },
          updateItems: function updateItems(user_id) {
            var _this2 = this;

            var that = this;
            Swal.fire({
              icon: 'warning',
              title: '¿Desea completar los pendientes seleccionados?',
              showCancelButton: true,
              confirmButtonText: "Si",
              cancelButtonText: "Cancelar"
            }).then(function (result) {
              if (result.isConfirmed) {
                var formdata = new FormData();
                formdata.append('user_id', that.user_id);
                formdata.append('model', JSON.stringify(that.itemsMarks));
                formdata.append('_token', $('#_token').val());
                formdata.append('created_at', Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + '/user_cuest_pendiente/update', formdata).then(function (response) {
                  that.listItems = response;
                  that.itemsMarks = [];
                  _this2.episodios[that.index].t_pendiente = that.listItems.filter(function (item) {
                    return Number(item.active);
                  }).length;
                  EventBus.$on('externalVarChanged', function (newValue) {
                    that.episodios = newValue;
                  });
                });
              }
            });
          },
          validate: function validate() {
            if (!this.title) {
              Swal.fire({
                icon: "warning",
                title: "Campo obligatorio",
                text: "El campo 'Titulo' es obligatorio.",
                showCancelButton: true,
                confirmButtonText: "Si",
                cancelButtonText: "Cancelar"
              });
              return false;
            }
            /* if(!this.description){
                Swal.fire({
                    icon: "warning",
                    title: "Campo obligatorio",
                    text: "El campo 'Observación' es obligatorio.",
                    showCancelButton: true,
                    confirmButtonText: `Si`,
                    cancelButtonText: `Cancelar`,
                })
                return false;
            } */


            return true;
          },
          storeItems: function storeItems() {
            var _this3 = this;

            var that = this;

            if (that.validate()) {
              that.saving = true;
              var formdata = new FormData();
              formdata.append('user_id', that.user_id);
              formdata.append('value', that.title);
              formdata.append('coments', that.description);
              formdata.append('_token', $('#_token').val());
              formdata.append('created_at', Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
              Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + '/user_cuest_pendiente/store', formdata).then(function (response) {
                that.saving = false;
                that.listItems = response;
                that.itemsMarks = [];
                that.title = '';
                that.description = '';
                that.create = false;
                _this3.episodios[that.index].t_pendiente = that.listItems.filter(function (item) {
                  return Number(item.active);
                }).length;
                EventBus.$on('externalVarChanged', function (newValue) {
                  that.episodios = newValue;
                });
              });
            }
          }
        },
        created: function created() {
          var that = this; // Escuchar cambios en el bus de eventos

          EventBus.$on('externalVarChanged', function (newValue) {
            that.items = newValue;
          });
        },
        mounted: function mounted() {
          this.user_id = user_id;
          this.index = index;
          this.showItems();
        }
      });
    }
  }, {
    key: "create",
    value: function create(selector, user_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div>\n                <h1 class=\"text-primary\">\n                    Nuevo Pendientes\n                </h1>\n            </div>\n            <div>\n                <input type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"value\" id=\"value\" aria-describedby=\"helpId\" placeholder=\"Titulo\">\n                <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"coments\" id=\"coments\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n\n\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"mr-1 btn btn-success w-100\">Agregar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n            </div>\n\n        ");
      var that = this;
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        that.index(selector, user_id);
      });
      $("#user_cuest_model_store").on("click", function () {
        that.store(user_id).then(function (response) {
          var count = 0;
          $.each(response, function (indexInArray, valueOfElement) {
            if (valueOfElement.active == 1) {
              count++;
            }
          });
          $("#total_pendientes_" + user_id).show().html(count);
          userCuestHistoria.getHistorial(user_id).then(function (response) {
            if (Object.keys(response).length > 0) {
              pacientes_datos.map(function (x) {
                if (x.user_id == user_id) {
                  x.resultados = response;
                }
              });
            } else {
              pacientes_datos.map(function (x) {
                if (x.user_id == user_id) {
                  x.resultados = [];
                }
              });
            }

            that.index('.modal-body', user_id);
          });
        });
      });
      $("#info").on("click", function () {
        that.help(selector, user_id);
      });
    }
  }, {
    key: "store2",
    value: function store2(user_id) {
      var message;
      var formdata = new FormData();
      var value = "Pendiente";
      var coments = $("#coments_" + user_id);
      var privado = $("#privado_" + user_id).val();

      if (coments.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        coments.trigger('focus');
        return false;
      }

      formdata.append("user_id", user_id);
      formdata.append("value", value);
      formdata.append("privado", privado);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("coments", coments.val());
      formdata.append("_token", $("#_token").val());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_pendiente/store2", formdata).then(function (response1) {
        coments.val('');
        userCuestHistoria.getHistorial(user_id).then(function (response) {
          if (Object.keys(response).length > 0) {
            pacientes_datos.map(function (x) {
              if (x.user_id == user_id) {
                x.resultados = response;
              }
            });
          } else {
            pacientes_datos.map(function (x) {
              if (x.user_id == user_id) {
                x.resultados = [];
              }
            });
          }

          var ultimo = first(response1);
          var f = new Date(ultimo.created_at_default);
          var fecha = f.getDate() + " " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()).substr(0, 3) + ", " + f.getFullYear();
          $("#listado_pendiente_" + user_id).prepend(listGoup({
            eventCompletar: "this.update2(".concat(user_id, ",").concat(ultimo.id, ",'pendiente')"),
            completar: true,
            tagTitle: true,
            title: ultimo.value,
            tagDescription: true,
            description: ultimo.coments,
            destroy: false,
            tagTime: true,
            created_at: fecha,
            hora: ultimo.hora,
            registromedico: "".concat(ultimo.medico, " | ").concat(fecha, " | ").concat(ultimo.hora),
            id: ultimo.id,
            model: "pendiente",
            eventEliminar: "UserCuestPlan.destroy2(".concat(user_id, ",").concat(ultimo.id, ")")
          }));
        });
      });
    }
  }, {
    key: "store",
    value: function store(user_id) {
      console.log(user_id);
      var message;
      var formdata = new FormData();
      var value = $("#value");
      var coments = $("#coments");

      if (value.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        value.trigger('focus');
        return false;
      }

      if (coments.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        coments.trigger('focus');
        return false;
      }

      formdata.append("user_id", user_id);
      formdata.append("value", value.val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("coments", coments.val());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_pendiente/store", formdata);
    }
  }, {
    key: "update",
    value: function update(user_id) {
      var message;
      var formdata = new FormData();
      var value = JSON.stringify($('[name="model[]"]').serializeArray());
      var a = $('[name="model[]"]').serializeArray();

      if (a.length == 0) {
        message = Component.alertMessage("input_checkbox_empty");
        alert(message['message']);
        return false;
      }

      formdata.append("user_id", user_id);
      formdata.append("model", value);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_pendiente/update", formdata);
    }
  }, {
    key: "update2",
    value: function update2(user_id_paciente, user_cuest_pendiente_id, model) {
      //entrega de guardias
      var message = Component.alertMessage('update_row_cuestion');
      Swal.fire({
        icon: message['image'],
        title: message['message'],
        showDenyButton: true,
        showCancelButton: false,
        confirmButtonText: "Si"
      }).then(function (result) {
        if (result.isConfirmed) {
          var formdata = new FormData();
          formdata.append("id", user_cuest_pendiente_id);
          formdata.append("_token", $("#_token").val());
          formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
          Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_pendiente/update2", formdata).then(function (response) {
            userCuestHistoria.getHistorial(user_id_paciente).then(function (response) {
              if (Object.keys(response).length > 0) {
                pacientes_datos.map(function (x) {
                  if (x.user_id == user_id_paciente) {
                    x.resultados = response;
                  }
                });
              } else {
                pacientes_datos.map(function (x) {
                  if (x.user_id == user_id_paciente) {
                    x.resultados = [];
                  }
                });
              }

              showHide("#row_".concat(user_cuest_pendiente_id, "_").concat(model));
            });
          });
        }
      });
    }
  }, {
    key: "show",
    value: function show(user_id) {
      var _this4 = this;

      var a = confirm("¿Desea completar los pendientes seleccionados?");

      if (a == true) {
        this.update(user_id).then(function (response) {
          if (Object.keys(response).length > 0) {
            var count = 0;
            $.each(response, function (indexInArray, valueOfElement) {
              if (valueOfElement.active == 1) {
                $("#total_pendientes_" + user_id).show().html(count++);
              } else {
                $("#total_pendientes_" + user_id).css("display", "none");
              }
            });
          } else {
            $("#total_pendientes_" + user_id).css("display", "none");
          }

          _this4.index(".modal-body", user_id);
        });
      }
    }
  }, {
    key: "destroy",
    value: function destroy(id) {
      var formdata = new FormData();
      formdata.append("id", id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_pendiente/destroy", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var _this5 = this;

      var message = Component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        this.destroy(row_id).then(function (response) {
          if (Object.keys(response).length > 0) {
            var count = 0;
            $.each(response, function (indexInArray, valueOfElement) {
              if (valueOfElement.active == 1) {
                $("#total_pendientes_" + user_id).show().html(count++);
              } else {
                $("#total_pendientes_" + user_id).css("display", "none");
              }
            });
          } else {
            $("#total_pendientes_" + user_id).css("display", "none");
          }

          _this5.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "colorGl",
    value: function colorGl(value) {
      if (value < 60) {
        return "text-danger";
      }

      if (value >= 60 && value <= 69.9) {
        return "text-warning";
      }

      if (value >= 70 && value <= 100) {
        return "text-success";
      }

      if (value >= 100.1 && value <= 125) {
        return "text-warning";
      }

      if (value > 125) {
        return "text-danger";
      }
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      $(selector).html("\n            <div onclick=\"this.index('.modal-body',".concat(data.user_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    GL\n                </div>\n                ").concat(data.valor == "" || data.valor == null ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + this.colorGl(data.valor) + "'>" + data.valor + " mg/dL</span>", "\n            </div>\n        "));
    }
  }]);

  return UserCuestPendiente;
}();



/***/ }),

/***/ "./public/class_js/UserCuestTa.js":
/*!****************************************!*\
  !*** ./public/class_js/UserCuestTa.js ***!
  \****************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestTa; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__["default"]();

var UserCuestTa = /*#__PURE__*/function () {
  function UserCuestTa() {
    _classCallCheck(this, UserCuestTa);
  }

  _createClass(UserCuestTa, null, [{
    key: "index",
    value: function index(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                    Tensi\xF3n Arterial\n                </h1>\n                <span id=\"user_cuest_model_create\" class=\"text-shadow-drop-tl text-primary mr-2\" style=\"float:right;font-size: 2rem !important; cursor:pointer;\">\n                    Nuevo valor\n                    <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                </span>\n            </div>\n            <div class=\"flex-fill overflow-auto mb-1\">\n                <table class=\"table table-bordered mb-3\">\n                    <tr>\n                        <th class=\"text-primary text-center\">Fecha</th>\n                        <th class=\"text-primary text-center\">Valor</th>\n                        <th class=\"text-primary text-center\">Observaci\xF3n</th>\n                        <th class=\"text-primary text-center\">Acci\xF3n</th>\n                    </tr>\n                    <tbody id=\"filas\">\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center\">\n                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                    <span class=\"sr-only\">Loading...</span>\n                                </div>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n            </div>\n\n            <div class=\"d-flex\">\n                <a class='btn btn-primary btn-block' data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</a>\n\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_ta/show", formdata).then(function (response) {
        var filas = "";
        var tempRow = "";
        var dia = "";
        var count = 0;
        var m = "";
        var d = "";

        if (response.length > 0) {
          $(response).each(function (index, element) {
            d = new Date(element.created_at);

            if (dia != d.getDate()) {
              dia = d.getDate();
              count++;
              m = count;
            } else {
              m = "";
            }

            tempRow = "\n                            <tr id=\"temp_".concat(element.id, "\">\n                                <td class=\"text-center\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["jsUcfirst"])(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["meses"])(d.getMonth())) + "/" + d.getDate() + " a las " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["formatAMPM"])(d), "</td>\n                                <td class=\"text-center font-weight-bold ").concat(UserCuestTa.colorTa(element.value, element.value2), "\" id=\"modal-temp-").concat(element.id, "\">\n                                    ").concat(element.value, "/").concat(element.value2, " mmHg</td>\n                                <td class=\"text-center\">\n                                    ").concat(element.coments != null ? element.coments : "", "\n                                </td>\n\n                                <td align=\"center\">\n                                <!-- <button title=\"Eliminar valor\" onclick=\"UserCuestTa.eliminar(").concat(element.id, ",").concat(user_id, ")\" class=\"delete-row btn btn-danger\" data-option=\"").concat(element.id, "\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button> -->\n                                </td>\n                            </tr>\n                        ");
            filas = filas + tempRow;
          });
        } else {
          filas = "\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center text-primary\">Sin registros</td>\n                        </tr>\n                    ";
        }

        $("#filas").html(filas);
      });
      $("#index_table").slideDown('slow');
      $("#user_cuest_model_create").on('click', function () {
        UserCuestTa.create(".modal-body", user_id, episodio_id);
      });
      /*     $("#info").on("click", function() {
              UserCuestTa.help(selector, user_id);
          }); */
    }
  }, {
    key: "create",
    value: function create(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex \">\n                <h1 class=\"h1 text-primary\">\n                    Tensi\xF3n Arterial\n                </h1>\n            </div>\n            <div class=\"d-flex flex-wrap mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0 mr-1\">\n                    <input type=\"number\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"value\" id=\"value\" aria-describedby=\"helpId\" placeholder=\"Sist\xF3lica\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0\">\n                    <input type=\"number\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"value2\" id=\"value2\" aria-describedby=\"helpId\" placeholder=\"Diast\xF3lica\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 p-0\">\n                    <input type=\"datetime-local\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"sintoma_observacion\" id=\"sintoma_observacion\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"mr-1 font-weight-bold btn btn-success w-100\">Agregar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        UserCuestTa.index(selector, user_id, episodio_id);
      });
      $("#user_cuest_model_store").on("click", function () {
        UserCuestTa.store(user_id, episodio_id).then(function (response) {
          var value = $("#value").val() + "/" + $("#value2").val();
          UserCuestTa.columnaValor("#col_9_".concat(user_id), {
            'user_id': user_id,
            'valor': value,
            'episodio_id': episodio_id
          });
          UserCuestTa.index('.modal-body', user_id, episodio_id);
        });
      });
      $("#info").on("click", function () {
        UserCuestTa.help(selector, user_id);
      });
    }
  }, {
    key: "store",
    value: function store(user_id, episodio_id) {
      var message;
      var formdata = new FormData();
      var value = $("#value");
      var value2 = $("#value2");
      var created_at = $("#created_at");
      var sintoma_observacion = $("#sintoma_observacion");

      if (value.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        value.focus();
        return false;
      }

      if (value2.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        value2.focus();
        return false;
      }

      if (created_at.val() == "") {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])();
      } else {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps2"])(created_at.val());
      }

      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("value", value.val());
      formdata.append("value2", value2.val());
      formdata.append("created_at", created_at);
      formdata.append("sintoma_observacion", sintoma_observacion.val());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_ta/store", formdata);
    }
  }, {
    key: "store3",
    value: function store3(episodio_id, user_id, value, value2) {
      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("value", value);
      formdata.append("value2", value2);
      formdata.append("user_id", user_id);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_ta/store", formdata);
    }
  }, {
    key: "destroy",
    value: function destroy(id) {
      var formdata = new FormData();
      formdata.append("id", id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_ta/destroy", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var message = Component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        UserCuestTa.destroy(row_id).then(function (response) {
          console.log(response.length);

          if (response.length > 0) {
            UserCuestTa.columnaValor("#col_9_".concat(user_id), {
              'user_id': user_id,
              'valor': response[0]['value'],
              'valor2': response[0]['value2']
            });
          } else {
            $("#modelId").modal("hide");
            UserCuestTa.columnaValor("#col_9_".concat(user_id), {
              'user_id': user_id,
              'valor': null,
              'valor2': null
            });
          }

          UserCuestTa.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "colorTa",
    value: function colorTa(value1, value2) {
      if (value1 <= 120 && value2 <= 80) {
        return "text-success";
      }

      if (value1 <= 120 && value2 <= 85) {
        return "text-success";
      }

      if (value1 >= 120 && value1 < 130 && value2 <= 80) {
        return "text-warning";
      }

      if (value1 >= 120 && value1 < 130 && value2 <= 90) {
        return "text-warning";
      }

      if (value1 >= 130 && value1 < 140 && value2 >= 80 && value2 <= 90) {
        return "text-warning";
      }

      if (value1 >= 130 && value1 < 140 && value2 < 80) {
        return "text-warning";
      }

      if (value1 >= 140 || value2 >= 90) {
        return "text-danger";
      }

      if (value1 >= 180 || value2 >= 120) {
        return "text-danger";
      }
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      var cadena = data.valor.split("/");
      $(selector).html("\n            <div onclick=\"UserCuestTa.index('.modal-body',".concat(data.user_id, ",").concat(data.episodio_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    T.A\n                </div>\n                ").concat(cadena[0] == null || cadena[1] == null || cadena[0] == "" || cadena[1] == "" ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + UserCuestTa.colorTa(cadena[0], cadena[1]) + "'>" + data.valor + " mmHg</span>", "\n            </div>\n        "));
    }
  }]);

  return UserCuestTa;
}();



/***/ }),

/***/ "./public/class_js/UserCuestTemperatura.js":
/*!*************************************************!*\
  !*** ./public/class_js/UserCuestTemperatura.js ***!
  \*************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestTemperatura; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__["default"]();

var UserCuestTemperatura = /*#__PURE__*/function () {
  function UserCuestTemperatura() {
    _classCallCheck(this, UserCuestTemperatura);
  }

  _createClass(UserCuestTemperatura, null, [{
    key: "index",
    value: function index(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                    Temperatura\n                </h1>\n                <span id=\"user_cuest_model_create\" class=\"text-shadow-drop-tl text-primary mr-2\" style=\"float:right;font-size: 2rem !important; cursor:pointer;\">\n                    Nuevo valor\n                    <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                </span>\n            </div>\n            <div class=\"flex-fill overflow-auto mb-1\">\n                <table class=\"table table-bordered mb-3\">\n                    <tr>\n                        <th class=\"text-primary text-center\">Fecha</th>\n                        <th class=\"text-primary text-center\">Valor</th>\n                        <th class=\"text-primary text-center\">Observaci\xF3n</th>\n                        <th class=\"text-primary text-center\">Acci\xF3n</th>\n                    </tr>\n                    <tbody id=\"filas\">\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center\">\n                                <div class=\"spinner-border text-primary\" role=\"status\">\n                                    <span class=\"sr-only\">Loading...</span>\n                                </div>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n            </div>\n\n            <div class=\"d-flex\">\n                <a class='btn btn-primary btn-block' data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</a>\n\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      /** */

      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_temperatura/index", formdata).then(function (response) {
        //UserCuestTemperatura.grafico(response)
        var filas = "";
        var tempRow = "";
        var dia = "";
        var count = 0;
        var m = "";
        var d = "";

        if (response.length > 0) {
          $(response).each(function (index, element) {
            d = new Date(element.created_at);

            if (dia != d.getDate()) {
              dia = d.getDate();
              count++;
              m = count;
            } else {
              m = "";
            }

            tempRow = "\n                            <tr id=\"temp_".concat(element.id, "\">\n                                <td class=\"text-center\">").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["jsUcfirst"])(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["meses"])(d.getMonth())) + "/" + d.getDate() + " a las " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["formatAMPM"])(d), "</td>\n                                <td class=\"text-center font-weight-bold ").concat(UserCuestTemperatura.colorTemp(element.value), "\" id=\"modal-temp-").concat(element.id, "\">").concat(element.value, "\xB0C</td>\n                                <td class=\"text-center\">\n                                    ").concat(element.coments != null ? element.coments : "", "\n                                </td>\n                                <td align=\"center\">\n                                    <!-- <button title=\"Eliminar valor\" onclick=\"UserCuestTemperatura.eliminar(").concat(element.id, ",").concat(user_id, ")\" class=\"delete-row btn btn-danger\" data-option=\"").concat(element.id, "\"><i class=\"fa fa-minus\" aria-hidden=\"true\"></i></button>-->\n                                </td>\n                            </tr>\n                        ");
            filas = filas + tempRow;
          });
        } else {
          filas = "\n                        <tr>\n                            <td colspan=\"5\" class=\"text-center text-primary\">Sin registros</td>\n                        </tr>\n                    ";
        }

        $("#filas").html(filas);
      });
      $("#user_cuest_model_create").on('click', function () {
        UserCuestTemperatura.create(".modal-body", user_id, episodio_id);
      });
      /*  $("#info").on("click", function() {
           UserCuestTemperatura.help(selector, user_id);
       }); */
    }
  }, {
    key: "create",
    value: function create(selector, user_id, episodio_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex \">\n                <h1 class=\"h1 text-primary\">\n                    Temperatura\n                </h1>\n            </div>\n            <div class=\"d-flex mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0 mr-1\">\n                    <input type=\"number\" class=\"form-control bg-gray-footer-parodi\" name=\"value\" id=\"value\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 p-0\">\n                    <input type=\"datetime-local\" class=\"form-control bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n            </div>\n            <div class=\"flex-fill mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \" name=\"sintoma_observacion\" id=\"sintoma_observacion\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"mr-1 font-weight-bold btn btn-success w-100\">Agregar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100\">Regresar</button>\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        UserCuestTemperatura.index(selector, user_id, episodio_id);
      });
      $("#user_cuest_model_store").on("click", function () {
        UserCuestTemperatura.store(user_id, episodio_id).then(function (response) {
          var valor = $("#value").val();
          UserCuestTemperatura.columnaValor("#col_6_".concat(user_id), {
            'user_id': user_id,
            'valor': valor,
            'episodio_id': episodio_id
          });
          UserCuestTemperatura.index('.modal-body', user_id, episodio_id);
        });
      });
      /* $("#info").on("click", function() {
          UserCuestTemperatura.help(selector, user_id);
      }); */
    }
  }, {
    key: "store",
    value: function store(user_id, episodio_id) {
      var message;
      var formdata = new FormData();
      var value = $("#value");
      var created_at = $("#created_at");
      var sintoma_observacion = $("#sintoma_observacion");

      if (value.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        value.focus();
        return false;
      }

      if (created_at.val() == "") {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])();
      } else {
        created_at = Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps2"])(created_at.val());
      }

      formdata.append("user_id", user_id);
      formdata.append("episodio_id", episodio_id);
      formdata.append("cat_user_cuestionario_84", value.val());
      formdata.append("created_at", created_at);
      formdata.append("sintoma_observacion", sintoma_observacion.val());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_temperatura/store", formdata);
    }
  }, {
    key: "store2",
    value: function store2(user_id) {
      var message;
      var formdata = new FormData();
      var value = $("#model_temp");
      formdata.append("user_id", user_id);
      formdata.append("cat_user_cuestionario_84", value.val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_temperatura/store", formdata);
    }
  }, {
    key: "destroy",
    value: function destroy(id) {
      var formdata = new FormData();
      formdata.append("id", id);
      formdata.append("_token", $("#_token").val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_temperatura/destroy", formdata);
    }
  }, {
    key: "store3",
    value: function store3(episodio_id, user_id, value) {
      var formdata = new FormData();
      formdata.append("episodio_id", episodio_id);
      formdata.append("user_id", user_id);
      formdata.append("cat_user_cuestionario_84", value);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_cuest_temperatura/store", formdata);
    }
  }, {
    key: "eliminar",
    value: function eliminar(row_id, user_id) {
      var message = Component.alertMessage('destroy_row_cuestion');
      var r = confirm(message['message']);

      if (r == true) {
        UserCuestTemperatura.destroy(row_id).then(function (response) {
          var temp = $("#value").val();

          if (response.length > 0) {
            UserCuestTemperatura.columnaValor("#col_6_".concat(user_id), {
              'user_id': user_id,
              'valor': response[0].value
            });
          } else {
            $("#modelId").modal("hide");
            UserCuestTemperatura.columnaValor("#col_6_".concat(user_id), {
              'user_id': user_id,
              'valor': null
            });
          }

          UserCuestTemperatura.index('.modal-body', user_id);
        });
      }
    }
  }, {
    key: "colorTemp",
    value: function colorTemp(temp) {
      //$(selector).removeClass(["text-secondary", "text-cyan", "text-success", "text-danger"])
      if (temp <= 30.4) {
        return "text-cyan";
      } else if (temp >= 30.5 && temp <= 37.5) {
        return "text-success";
      } else if (temp >= 37.6 && temp <= 37.9) {
        return "text-warning";
      } else if (temp >= 38) {
        return "text-danger";
      } else {}
    }
  }, {
    key: "columnaValor",
    value: function columnaValor(selector, data) {
      $(selector).html("\n            <div onclick=\"UserCuestTemperatura.index('.modal-body',".concat(data.user_id, ",").concat(data.episodio_id, ")\" class=\"text-center text-secondary font-weight-bold cursor-pointer\">\n                <div class=\"text-center text-gray w-100 title-columna\">\n                    TEMP\n                </div>\n                ").concat(data.valor == "" || data.valor == null ? "<a class='btn btn-light btn-block' data-tippy-content='Pulse para añadir nuevo valor'>Añadir</a>" : "<span class='" + UserCuestTemperatura.colorTemp(data.valor) + "'>" + data.valor + "°C</span>", "\n            </div>\n        "));
    }
  }, {
    key: "help",
    value: function help(selector, user_id) {
      $(selector).html("\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <h1 class=\"text-primary\">\n                        Manual operativo de usuario\n                    </h1>\n                </div>\n            </div>\n            <br>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <object id=\"pdfHolder\" data=\"/help/temperatura.pdf\" type=\"application/pdf\" style=\"width:100%;height:500px;\">\n                        <a title=\"Pulse para descargar\" href=\"/help/temperatura.pdf\" download ><img src=\"".concat(location.origin, "/image/system/icono_archivo2.svg\" ></a>\n                    </object>\n                </div>\n            </div>\n            <br>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <button id=\"regresar\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                </div>\n            </div>\n\n        "));
      $("#regresar").on("click", function () {
        UserCuestTemperatura.index(selector, user_id);
      });
    }
  }, {
    key: "grafico",
    value: function grafico(data) {
      /*
      var colorTemp = 1;
      var bordeGrafico = "#2FA600"
      var fondoGrafico = "#2fa60078"
      if (colorTemp == 1) {
          //verde
          bordeGrafico = "green"
          fondoGrafico = "#2fa60078";
      }
      if (colorTemp == 2) {
          //amarillo
          bordeGrafico = "orange"
          fondoGrafico = "#b1860778";
      }
      if (colorTemp == 3) {
          //rojo
          bordeGrafico = "red"
          fondoGrafico = "#dc354578";
      }
      //qui empieza los datos del grafico
      //ejes
      //x = dia
      //y = temp
        let dia = [];
      let temp = [];
      let i = data.length;
      $.each(data, function(indexInArray, valueOfElement) {
          dia[indexInArray] = indexInArray + 1;
          temp.push({ x: indexInArray, y: valueOfElement['value'] })
              //console.log(valueOfElement['value'])
      });
      var data_set = temp; //[{ x: 1, y: 50 }, { x: 2, y: 3 }, { x: 3, y: 2 }, { x: 4, y: 1 }, { x: 5, y: 8 }, { x: 6, y: 8 }, { x: 7, y: 2 }, { x: 8, y: 2 }, { x: 9, y: 3 }, { x: 10, y: 5 }, { x: 11, y: 11 }, { x: 12, y: 1 }, { x: 12, y: 1 }, { x: 12, y: 1 }, { x: 12, y: 1 }, { x: 12, y: 1 }, { x: 40, y: 1 }];
      var dias = dia;
        var lines = [],
          id = 0;
      var linesOn = false;
        var data = {
          labels: dias,
          datasets: [{
              label: "Temperatura: ", //texto de tooltip
              backgroundColor: fondoGrafico, //fondo de linea
              borderColor: bordeGrafico, //color de linea
              borderWidth: 1, //amcho de linea
              hoverBackgroundColor: "red",
              hoverBorderColor: "purple",
              data: data_set,
          }]
      };
        var option = {
          responsive: true,
          legend: false,
            onHover: function(evt) {
              var item = myLineChart.getElementAtEvent(evt);
              if (item.length) {
                  //console.log(">item", item);
                  //console.log(">data", item[0]._index, data.datasets[0].data[item[0]._index]);
              }
          },
          onClick: function(evt) {
              //var el = myLineChart.getElementAtEvent(evt);
              //console.log("onClick", el, evt);
          },
          annotation: {
              drawTime: "afterDraw",
              events: ['click', 'mouseenter', 'mouseleave'],
              annotations: lines
          },
          scales: {
                yAxes: [{
                  id: 'y-axis',
                  type: 'linear',
              }],
          }
        };
        addLine(38, "red", "De cuidado");
      addLine(30, "green", "Estable");
        var myLineChart = Chart.Line('myChart', {
          data: data,
          options: option
      });
        //console.log(myLineChart.annotation.elements.line1);
      myLineChart.annotation.elements.line1.hidden = true;
      myLineChart.update();
        function addLine(value, color, text) {
          id++;
          var ln = {
              id: "line" + id,
              type: "line",
              mode: "horizontal",
              scaleID: "y-axis",
              value: value,
              borderWidth: 1,
              backgroundColor: color,
              borderColor: color,
              label: {
                  content: text,
                  enabled: true,
                  position: "right",
                  backgroundColor: 'rgba(0,0,0,0.3)',
              },
              onMouseenter: function(e) {
                  //console.log("onMouseenter", e, this);
                  this.options.borderColor = color;
                  this.options.label.backgroundColor = color;
                  myLineChart.update();
              },
              onMouseleave: function(e) {
                  //console.log("onMouseleave", e);
                  this.options.borderColor = color;
                  this.options.label.backgroundColor = color;
                  myLineChart.update();
              },
          };
          lines.push(ln);
      }
      */
    }
  }]);

  return UserCuestTemperatura;
}();



/***/ }),

/***/ "./public/class_js/UserCuestUbicacion.js":
/*!***********************************************!*\
  !*** ./public/class_js/UserCuestUbicacion.js ***!
  \***********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserCuestUbicacion; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
/* harmony import */ var _UserCuestImpresionDiagnostica_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./UserCuestImpresionDiagnostica.js */ "./public/class_js/UserCuestImpresionDiagnostica.js");
/* harmony import */ var _CatUserUbicacion_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./CatUserUbicacion.js */ "./public/class_js/CatUserUbicacion.js");
/* harmony import */ var _UserCuestEpisodio_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./UserCuestEpisodio.js */ "./public/class_js/UserCuestEpisodio.js");
/* harmony import */ var _UserCuestHistoria_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./UserCuestHistoria.js */ "./public/class_js/UserCuestHistoria.js");
/* harmony import */ var _Component_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./Component.js */ "./public/class_js/Component.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }








var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"]();
var catUserUbicacion = new _CatUserUbicacion_js__WEBPACK_IMPORTED_MODULE_4__["default"]();
var userCuestHistoria = new _UserCuestHistoria_js__WEBPACK_IMPORTED_MODULE_6__["default"](); //let userCuestImpresionDiagnostica = new UserCuestImpresionDiagnostica()

var UserCuestUbicacion = /*#__PURE__*/function () {
  function UserCuestUbicacion() {
    _classCallCheck(this, UserCuestUbicacion);
  }

  _createClass(UserCuestUbicacion, [{
    key: "create3",
    value: function create3(selector, user_id) {
      //MODAL INGRESO
      $("#modelId").modal("show");
      $(selector).html("\n            <div id=\"infoPaciente\" style=\"position: absolute;width: 100%;top:0;left:0;z-index: 200000;\"></div>\n            <div  class=\"row pt-6\" style=\" margin-top: 7.5rem !important;\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 text-primary\">\n                    Traslado\n                </div>\n            </div>\n            <div id=\"form\" class=\"row\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray text-center\">\n                        Ubicaci\xF3n Actual\n                    </div>\n                    <input type=\"text\" style=\"background-color:var(--gray) !important\" readonly class=\"form-control text-center form-control-lg bg-gray-footer-parodi\" name=\"ubicacion_actual\" id=\"ubicacion_actual\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray text-center\">\n                        Nueva Ubicaci\xF3n\n                    </div>\n                    <select id=\"cat_user_ubicacion_id\" name=\"cat_user_ubicacion_id\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <select id=\"subUbicacion1\" style=\"display:none;\" name=\"subUbicacion1\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <select id=\"subUbicacion2\" style=\"display:none;\" name=\"subUbicacion2\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <button id=\"user_cuest_ubicacion_store\" class=\"font-weight-bold btn btn-success mb-1 btn-block btn-lg\">Guardar</button>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <button id=\"regresar\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\"  data-dismiss=\"modal\" aria-label=\"Close\">Cancelar</button>\n                </div>\n            </div>\n            </div>\n\n        ");
      _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_2__["default"].infoPaciente("#infoPaciente", user_id);
      this.show(user_id).then(function (response) {
        //console.log("UserCuestUbicacion", response)
        if (response.name != null) {
          $("#ubicacion_actual").val(response.name);
        } else {
          $("#ubicacion_actual").val(response.description + " | " + response.coments);
        }
      });
      catUserUbicacion.getIndex().then(function (response) {
        console.log("catUserUbicacion:", response);
        var temp = [];
        $.each(response, function (indexInArray, valueOfElement) {
          if (valueOfElement['id'] !== 246) {
            temp[indexInArray] = valueOfElement;
          }
        });
        $("#cat_user_ubicacion_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(temp));
      });
      $("#cat_user_ubicacion_id").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion1").show();
            $("#subUbicacion2").hide();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion1").html(data);
          } else {
            $("#subUbicacion1").hide();
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#subUbicacion1").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion2").show();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion2").html(data);
          } else {
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#user_cuest_ubicacion_store").on("click", function () {
        ingreso = "Ingreso";
        UserCuestUbicacion.store2(user_id).then(function (response) {
          console.log("UserCuestUbicacion:", response);
          $("#row_" + user_id).slideUp("slow");
          $(".row_" + user_id).slideUp("slow");
          $("#modelId").modal("hide");
        });
      });
    }
  }, {
    key: "historialEgreso",
    value: function historialEgreso(selector, user_id) {
      //MODAL TRASLADO
      $("#modelId").modal("show");
      $(selector).html("\n            <div id=\"infoPaciente\" style=\"position: absolute;width: 100%;top:0;left:0;z-index: 200000;\"></div>\n            <div  class=\"row pt-6\" style=\" margin-top: 7.5rem !important;\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 text-primary\">\n                    Historial de Egresos\n                </div>\n            </div>\n            <div id=\"form\" class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <div id=\"historial_model\">\n\n                    </div>\n                </div>\n\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <button id=\"regresar\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\"  data-dismiss=\"modal\" aria-label=\"Close\">Cancelar</button>\n                </div>\n            </div>\n        ");
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["loading"])("#historial_model");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      UserCuestUbicacion.gethistorialAltas(user_id).then(function (response) {
        var result = "<table class='table'>";
        result += "\n                <thead>\n                    <tr>\n                        <th scope=\"col\" class=\"text-primary\">\xC1rea</th>\n                        <th scope=\"col\" class=\"text-primary\">Fecha ingreso</th>\n                        <th scope=\"col\" class=\"text-primary\">Fecha traslado</th>\n                        <th scope=\"col\" class=\"text-primary\">\xBFClasificado?</th>\n                        <th scope=\"col\" class=\"text-primary\">D\xEDas en \xE1rea</th>\n                    </tr>\n                </thead>\n                <tbody>\n            ";

        if (Object.keys(response).length > 0) {
          $.each(response, function (indexInArray, valueOfElement) {
            result += "\n                            <tr>\n                                <th class=\"text-gray\" scope=\"row\">".concat(valueOfElement.area, "</th>\n                                <td class=\"text-gray\">").concat(valueOfElement.fecha_ingreso, "</td>\n                                <td class=\"text-gray\">").concat(valueOfElement.fecha_traslado, "</td>\n                                <td class=\"text-gray\">No</td>\n                                <td class=\"text-gray\">").concat(valueOfElement.dias_en_area, "</td>\n                            </tr>\n                        ");
          });
        } else {
          result += "<a href='#' class='list-group-item list-group-item-action'>Sin ubicaciones</a>";
        }

        result += "</tbody></table>";
        $("#historial_model").html(result);
      });
      this.show(user_id).then(function (response) {
        //console.log("UserCuestUbicacion", response)
        if (response.name != null) {
          $("#ubicacion_actual").val(response.name);
        } else {
          $("#ubicacion_actual").val(response.description + " | " + response.coments);
        }
      });
      catUserUbicacion.getIndex().then(function (response) {
        console.log("catUserUbicacion:", response);
        $("#cat_user_ubicacion_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(response));
      });
      $("#cat_user_ubicacion_id").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion1").show();
            $("#subUbicacion2").hide();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion1").html(data);
          } else {
            $("#subUbicacion1").hide();
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#subUbicacion1").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion2").show();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion2").html(data);
          } else {
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#user_cuest_ubicacion_store").on("click", function () {
        ingreso = "Traslado";
        UserCuestUbicacion.store2(user_id).then(function (response) {
          console.log("UserCuestUbicacion:", response);
          $("#row_" + user_id).slideUp("slow");
          $(".row_" + user_id).slideUp("slow");
          $("#modelId").modal("hide");
        });
      });
    }
  }, {
    key: "create2",
    value: function create2(selector) {
      //se usa en el registro de paciente interno
      $(selector).html("\n            <div id=\"form\" class=\"row mb-3\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray\">\n                        Ubicaci\xF3n Actual\n                    </div>\n                    <input type=\"text\" style=\"background-color:var(--gray) !important\" readonly class=\"form-control text-center form-control-lg bg-gray-footer-parodi\" name=\"ubicacion_actual\" id=\"ubicacion_actual\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray\">\n                        Nueva Ubicaci\xF3n\n                    </div>\n                    <select id=\"cat_user_ubicacion_id\" name=\"cat_user_ubicacion_id\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <select id=\"subUbicacion1\" style=\"display:none;\" name=\"subUbicacion1\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <select id=\"subUbicacion2\" style=\"display:none;\" name=\"subUbicacion2\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </select>\n                </div>\n            </div>\n        ");
      $("#ubicacion_actual").val("Sala de Espera");
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/cat_user_ubicacion/index").then(function (response) {
        var data = "";
        console.log(response);
        $.each(response, function (indexInArray, valueOfElement) {
          var temp = []; //console.log(valueOfElement)

          if (![246, 387].includes(valueOfElement['id'])) {
            data += "\n                            <option value=\"".concat(valueOfElement.id, "\">\n                                ").concat(valueOfElement.description, "\n                            </option>\n                        ");
          }
        });
        $("#cat_user_ubicacion_id").html(data);
        $("#cat_user_ubicacion_id").append("\n                    <option value=\"388\">\n                        Telesalud Empresarial\n                    </option>\n                ");
      });
      $("#cat_user_ubicacion_id").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion1").show();
            $("#subUbicacion2").hide();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                    <option value=\"".concat(valueOfElement.id, "\">\n                                        ").concat(valueOfElement.coments, "\n                                    </option>\n                                ");
            });
            $("#subUbicacion1").html(data);
          } else {
            $("#subUbicacion1").hide();
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#subUbicacion1").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion2").show();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                    <option value=\"".concat(valueOfElement.id, "\">\n                                        ").concat(valueOfElement.coments, "\n                                    </option>\n                                ");
            });
            $("#subUbicacion2").html(data);
          } else {
            $("#subUbicacion2").hide();
          }
        });
      });
    }
  }, {
    key: "store",
    value: function store(user_id) {
      var formdata = new FormData();
      formdata.append("cat_user_ubicacion_id", $("#contacto_cmdlt").val());
      formdata.append("prioridad_lista", $("#prioridad_lista").val());
      formdata.append("user_id_paciente", user_id);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_ubicacion/store2", formdata);
    }
  }, {
    key: "gethistorialAltas",
    value: function gethistorialAltas(user_id) {
      var formdata = new FormData();
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_ubicacion/historialAltas/" + user_id, formdata);
    }
  }, {
    key: "validate",
    value: function validate() {
      var prioridad_lista = $("#prioridad_lista");
      var contacto_cmdlt = $("#contacto_cmdlt");

      if (prioridad_lista.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_7__["default"].alertMessage("input_select_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              prioridad_lista.trigger("focus");
            }, 110);
          }
        }); //alert(message['message'])
        //prioridad_lista.trigger("focus")

        return false;
      }

      if (contacto_cmdlt.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_7__["default"].alertMessage("input_select_empty"); //alert(message['message'])
        //contacto_cmdlt.trigger("focus")

        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              contacto_cmdlt.trigger("focus");
            }, 110);
          }
        });
        return false;
      }

      return true;
    }
  }, {
    key: "validate2",
    value: function validate2() {
      var cat_user_ubicacion_id = $("#cat_user_ubicacion_id");

      if (cat_user_ubicacion_id.val() == "") {
        message = _Component_js__WEBPACK_IMPORTED_MODULE_7__["default"].alertMessage("input_select_empty"); //alert(message['message'])
        //cat_user_ubicacion_id.trigger("focus")

        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              cat_user_ubicacion_id.trigger("focus");
            }, 110);
          }
        });
        return false;
      }

      return true;
    }
  }, {
    key: "menuEgreso",
    value: function menuEgreso(selector) {
      $("#messageModal").modal("show");
      $(selector).html("\n            <div id=\"form_pad\">\n                <div id=\"menuPatiencare\" class=\"list-group\">\n                    <span class=\"text-primary font-weight-bold menu-pad-title\"\">\n                        Men\xFA Egresos\n                    </span>\n                    <a id=\"\" href=\"#\"  data-area=\"egreso\" data-titlearea=\"Egresos - Alta\" class=\"menu-egreso list-group-item list-group-item-action\">Alta</a>\n                    <a id=\"\" href=\"#\"  data-area=\"traslado\" data-titlearea=\"Egresos - Traslado a otra Instituci\xF3n\" class=\"menu-egreso list-group-item list-group-item-action\">Traslado a otra Instituci\xF3n</a>\n                    <a id=\"\" href=\"#\"  data-area=\"contraopinion\" data-titlearea=\"Egresos - Contraopini\xF3n m\xE9dica\" class=\"menu-egreso list-group-item list-group-item-action\">Contraopini\xF3n m\xE9dica</a>\n                    <a id=\"\" href=\"#\"  data-area=\"fallecido\" data-titlearea=\"Egresos - Fallecido\" class=\"menu-egreso list-group-item list-group-item-action\">Fallecido</a>\n                    <a id=\"\" href=\"#\"  data-area=\"otro\" data-titlearea=\"Egresos - Otro\" class=\"menu-egreso list-group-item list-group-item-action\">Otro</a>\n\n                </div>\n            </div>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 pl-3 pr-3 pt-1 pb-1\">\n                    <button id=\"regresar\"  data-dismiss=\"modal\" aria-label=\"Close\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                </div>\n            </div>\n        ");
      $(".menu-egreso").on("click", function () {
        $('#btnNotificaciones').empty();
        _Component_js__WEBPACK_IMPORTED_MODULE_7__["default"].menu($(this).data("area"), $(this).data("titlearea"));
      });
    }
  }], [{
    key: "create",
    value: function create(selector, user_id) {
      //MODAL TRASLADO
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <h1 class=\"h1 text-primary\">\n                Traslado\n            </h1>\n            <div class=\"mb-auto row overflow-auto mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray text-center\">\n                        Ubicaci\xF3n Actual\n                    </div>\n                    <input type=\"text\" style=\"background-color:var(--gray) !important\" readonly class=\"form-control text-center form-control-lg bg-gray-footer-parodi\" name=\"ubicacion_actual\" id=\"ubicacion_actual\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray text-center\">\n                        Nueva Ubicaci\xF3n\n                    </div>\n                    <select id=\"cat_user_ubicacion_id\" name=\"cat_user_ubicacion_id\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <select id=\"subUbicacion1\" style=\"display:none;\" name=\"subUbicacion1\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <select id=\"subUbicacion2\" style=\"display:none;\" name=\"subUbicacion2\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </select>\n                </div>\n            </div>\n\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_ubicacion_store\" class=\"font-weight-bold btn btn-success mr-1 w-100 mb-1\">Guardar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100 mb-1\"  data-dismiss=\"modal\" aria-label=\"Close\">Cancelar</button>\n\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      this.show(user_id).then(function (response) {
        //console.log("UserCuestUbicacion", response)
        if (response.name != null) {
          $("#ubicacion_actual").val(response.name);
        } else {
          $("#ubicacion_actual").val(response.description + " | " + response.coments);
        }
      });
      catUserUbicacion.getIndex().then(function (response) {
        //console.log("catUserUbicacion:", response)
        var temp = [];
        $.each(response, function (indexInArray, valueOfElement) {
          if (valueOfElement['id'] !== 246) {
            temp[indexInArray] = valueOfElement;
          }
        });
        $("#cat_user_ubicacion_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(temp));
      });
      $("#cat_user_ubicacion_id").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion1").show();
            $("#subUbicacion2").hide();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion1").html(data);
          } else {
            $("#subUbicacion1").hide();
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#subUbicacion1").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion2").show();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion2").html(data);
          } else {
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#user_cuest_ubicacion_store").on("click", function () {
        ingreso = "Traslado";
        UserCuestUbicacion.store2(user_id).then(function (response) {
          console.log("UserCuestUbicacion:", response);
          $("#row_" + user_id).slideUp("slow");
          $(".row_" + user_id).slideUp("slow");
          $("#modal-patient-item").modal("hide"); // get(location.origin+"/getIdUltimasUbicaciones/"+user_id).then(res => {
          //     console.log(`Objeto res --> ${res}`)
          //     console.log('ID DE LA FILA DEL PACIENTE --> row-paciente-' + user_id + '-' + res[1].cat_user_ubicacion_id)
          //     // ocultar el paciente de la lista luego del traslado
          //     document.getElementById('row-paciente-' + user_id + '-' + res[1].cat_user_ubicacion_id).style.display = 'none'
          // })

          var userIdToRemove = user_id;
          var indexToRemove = pacientes_datos.findIndex(function (user) {
            return user.user_id === userIdToRemove;
          });

          if (indexToRemove !== -1) {
            pacientes_datos.splice(indexToRemove, 1);
          }
        });
      });
    }
  }, {
    key: "trasladoAmbulanciaIndex",
    value: function () {
      var _trasladoAmbulanciaIndex = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(episodio_id, user_id) {
        var _this = this;

        var rows, model, f, fecha;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                rows = "\n                <tr>\n                    <td colspan=\"5\" class=\"text-center text-primary\">No posee traslados</td>\n                </tr>\n                ";
                _context.next = 3;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])("/user/traslado_ambulancia/show/" + user_id);

              case 3:
                model = _context.sent;

                //console.log(model)
                if (model.length > 0) {
                  f = null;
                  fecha = null;
                  rows = "";
                  model.forEach(function (x) {
                    var fecha_traslado = x['fecha_traslado'].split(" ")[0];
                    var hora = x['fecha_traslado'].split(" ")[1];
                    f = new Date(fecha_traslado);
                    fecha = f.getDate() + " de " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()) + " " + f.getFullYear();
                    rows += "\n                            <tr>\n                                <td>\n                                    ".concat(fecha, "\n                                </td>\n                                <td>\n                                    ").concat(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["horaAMPM"])(hora), "\n                                </td>\n                                <td>\n                                    ").concat(x['origen_traslado'], "\n                                </td>\n                                <td>\n                                    ").concat(x['destino_traslado'], "\n                                </td>\n                                <td>\n                                    ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(x['observacion']) ? x['observacion'] : '', "\n                                </td>\n                            </tr>\n                        ");
                  });
                } //MODAL TRASLADO


                $("#modal-patient-item").modal("show");
                $("#modal-patient-item .modal-body-2").html(
                /*html */
                "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                    Traslados en ambulancia\n                </h1>\n                <button id=\"user_cuest_model_create\" class=\"btn btn-default text-primary\" style=\"float:right;font-size: 2rem !important; cursor:pointer;\">\n                    Nuevo traslado\n                    <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                </button>\n            </div>\n            <div class=\"flex-fill d-flex flex-column overflow-auto mb-1\">\n\n                <div class=\"flex-fill\">\n                    <table class=\"table table-bordered mb-3\">\n                        <tbody><tr>\n                            <th class=\"text-primary bg-white text-center sticky-top\">Fecha</th>\n                            <th class=\"text-primary bg-white text-center sticky-top\">Hora</th>\n                            <th class=\"text-primary bg-white text-center sticky-top\">Origen</th>\n                            <th class=\"text-primary bg-white text-center sticky-top\">Destino</th>\n                            <th class=\"text-primary bg-white text-center sticky-top\">Observaci\xF3n</th>\n                        </tr>\n                        </tbody>\n                            ".concat(rows, "\n                        </tbody>\n                    </table>\n                </div>\n            </div>\n\n            <div class=\"d-flex\">\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100 mb-1\"  data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</button>\n\n            </div>\n\n        "));
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["loading"])("#historial_model");
                userCuestPaciente.infoPaciente("#infoPaciente", user_id);
                document.getElementById("user_cuest_model_create").addEventListener("click", function () {
                  _this.trasladoAmbulanciaCreate(episodio_id, user_id);
                });

              case 10:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function trasladoAmbulanciaIndex(_x, _x2) {
        return _trasladoAmbulanciaIndex.apply(this, arguments);
      }

      return trasladoAmbulanciaIndex;
    }()
  }, {
    key: "trasladoAmbulanciaCreate",
    value: function trasladoAmbulanciaCreate(episodio_id, user_id) {
      var _this2 = this;

      var that = this; //MODAL TRASLADO

      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <div class=\"d-flex justify-content-between\">\n                <h1 class=\"h1 text-primary\">\n                    Traslados en ambulancia\n                </h1>\n            </div>\n            <div class=\"row\">\n                <div class=\"col-6\">\n                    <label for=\"fecha\">Fecha del Traslado:</label>\n                    <input type=\"date\" id=\"fecha_traslado\" name=\"fecha\" class=\"form-control bg-gray-footer-parodi\">\n                </div>\n                <div class=\"col-6\">\n                    <label for=\"hora\">Hora del Traslado:</label>\n                    <input type=\"time\" id=\"hora_traslado\" name=\"hora\" class=\"form-control bg-gray-footer-parodi\">\n                </div>\n                <div class=\"col-6\">\n                    <label for=\"origen\">Origen del Traslado:</label>\n                    <input type=\"text\" id=\"origen_traslado\" name=\"origen\" class=\"form-control bg-gray-footer-parodi\">\n                </div>\n                <div class=\"col-6\">\n                    <label for=\"destino\">Destino del Traslado:</label>\n                    <input type=\"text\" id=\"destino_traslado\" name=\"destino\" class=\"form-control bg-gray-footer-parodi\">\n                </div>\n\n            </div>\n\n            <div class=\"flex-fill d-flex flex-column  overflow-auto mb-1\">\n                <div class=\"flex-fill mt-1\">\n                    <textarea class=\"form-control bg-gray-footer-parodi \" name=\"observacion\" id=\"observacion\" rows=\"2\" style=\" width: 100%;height: 100%;resize: none;\" placeholder=\"Observaci\xF3n\"></textarea>\n                </div>\n            </div>\n\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"font-weight-bold btn btn-success w-100 mr-1 mb-1\" >Guardar</button>\n\n                <button id=\"user_cuest_model_index\" class=\"font-weight-bold btn btn-primary w-100 mb-1\">Regresar</button>\n\n            </div>\n\n        ");
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["loading"])("#historial_model");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      document.getElementById("user_cuest_model_index").addEventListener("click", function () {
        _this2.trasladoAmbulanciaIndex(episodio_id, user_id);
      });
      document.getElementById("user_cuest_model_store").addEventListener("click", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3() {
        var formNotValidate, formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
          while (1) {
            switch (_context3.prev = _context3.next) {
              case 0:
                formNotValidate = true;
                ["fecha_traslado", "hora_traslado", "origen_traslado", "destino_traslado", "observacion"].forEach(function (x) {
                  var input = document.getElementById(x);

                  if (Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(input.value) && formNotValidate) {
                    alert("El campo no puede estar vacio.");
                    formNotValidate = false;
                    input.focus();
                    return false;
                  }
                });

                if (!formNotValidate) {
                  _context3.next = 17;
                  break;
                }

                formdata = new FormData();
                formdata.append("fecha_traslado", document.getElementById("fecha_traslado").value);
                formdata.append("hora_traslado", document.getElementById("hora_traslado").value);
                formdata.append("origen_traslado", document.getElementById("origen_traslado").value);
                formdata.append("destino_traslado", document.getElementById("destino_traslado").value);
                formdata.append("observacion", document.getElementById("observacion").value);
                formdata.append("user_cuest_episodio_id", episodio_id);
                formdata.append("user_id_paciente", user_id);
                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                formdata.append("_token", $("#_token").val());
                console.log(formdata);
                _context3.next = 16;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user/traslado_ambulancia/store", formdata);

              case 16:
                Swal.fire({
                  title: "<strong>Registro completado</strong>",
                  icon: "success",
                  showCloseButton: true,
                  confirmButtonText: 'Ok!',
                  timerProgressBar: true
                }).then( /*#__PURE__*/function () {
                  var _ref2 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2(result) {
                    var fechaTimestamp, hora, f, fecha, btn_traslado_ambulancia, btn_traslado_ambulancia_icon;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
                      while (1) {
                        switch (_context2.prev = _context2.next) {
                          case 0:
                            fechaTimestamp = document.getElementById("fecha_traslado").value;
                            hora = document.getElementById("hora_traslado").value;
                            f = new Date(fechaTimestamp);
                            fecha = f.getDate() + " de " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["meses"])(f.getMonth()) + " " + f.getFullYear() + ", a las " + Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["horaAMPM"])(hora);
                            btn_traslado_ambulancia = document.querySelector("#btn_traslado_ambulancia_".concat(episodio_id));
                            btn_traslado_ambulancia.dataset.tippyContent = "Traslado reciente: ".concat(fecha);
                            btn_traslado_ambulancia.classList.replace("tooltip-gray", "tooltip-danger");
                            btn_traslado_ambulancia_icon = btn_traslado_ambulancia.querySelector("i");
                            btn_traslado_ambulancia_icon.classList.replace("text-gray", "text-danger");
                            that.trasladoAmbulanciaIndex(episodio_id, user_id);

                          case 10:
                          case "end":
                            return _context2.stop();
                        }
                      }
                    }, _callee2);
                  }));

                  return function (_x3) {
                    return _ref2.apply(this, arguments);
                  };
                }());

              case 17:
              case "end":
                return _context3.stop();
            }
          }
        }, _callee3);
      })));
    }
  }, {
    key: "historialUbiEpisodio",
    value: function historialUbiEpisodio(selector, user_id) {
      //MODAL TRASLADO
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <h1 class=\"h1 text-primary\">\n                Traslado\n            </h1>\n            <div class=\"flex-fill row overflow-auto mb-1\">\n                <div class=\"col-12\" id=\"historial_model\">\n\n                </div>\n            </div>\n\n            <div class=\"d-flex\">\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100 mb-1\"  data-dismiss=\"modal\" aria-label=\"Close\">Cerrar</button>\n\n            </div>\n\n        ");
      Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["loading"])("#historial_model");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      UserCuestUbicacion.gethistorialUbiEpisodio(user_id).then(function (response) {
        var result = "<table class='table'>";
        result += "\n                <thead>\n                    <tr>\n                        <th scope=\"col\" class=\"text-primary\">\xC1rea</th>\n                        <th scope=\"col\" class=\"text-primary\">Fecha ingreso</th>\n                        <th scope=\"col\" class=\"text-primary\">Fecha traslado</th>\n                        <th scope=\"col\" class=\"text-primary\">D\xEDas en \xE1rea</th>\n                        <th scope=\"col\" class=\"text-primary\">Reubicado por</th>\n                    </tr>\n                </thead>\n                <tbody>\n            ";

        if (Object.keys(response).length > 0) {
          $.each(response, function (indexInArray, valueOfElement) {
            result += "\n                        <tr>\n                            <th class=\"text-gray\" scope=\"row\">".concat(valueOfElement.area, "</th>\n                            <td class=\"text-gray\">").concat(valueOfElement.fecha_ingreso, "</td>\n                            <td class=\"text-gray\">").concat(valueOfElement.fecha_traslado, "</td>\n                            <td class=\"text-gray\">").concat(valueOfElement.dias_en_area, "</td>\n                            <td class=\"text-gray\">").concat(valueOfElement.creado_por, "</td>\n                        </tr>\n                    ");
          });
        } else {
          result += "<a href='#' class='list-group-item list-group-item-action'>Sin ubicaciones</a>";
        }

        result += "</tbody></table>";
        $("#historial_model").html(result);
      });
      UserCuestUbicacion.show(user_id).then(function (response) {
        //console.log("UserCuestUbicacion", response)
        if (response.name != null) {
          $("#ubicacion_actual").val(response.name);
        } else {
          $("#ubicacion_actual").val(response.description + " | " + response.coments);
        }
      });
      catUserUbicacion.getIndex().then(function (response) {
        //console.log("catUserUbicacion:", response)
        $("#cat_user_ubicacion_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(response));
      });
      $("#cat_user_ubicacion_id").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion1").show();
            $("#subUbicacion2").hide();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion1").html(data);
          } else {
            $("#subUbicacion1").hide();
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#subUbicacion1").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion2").show();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion2").html(data);
          } else {
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#user_cuest_ubicacion_store").on("click", function () {
        ingreso = "Traslado";
        UserCuestUbicacion.store2(user_id).then(function (response) {
          console.log("UserCuestUbicacion:", response);
          $("#row_" + user_id).slideUp("slow");
          $(".row_" + user_id).slideUp("slow");
          $("#modelId").modal("hide");
        }); // ocultar el paciente luego del traslado

        var userIdToRemove = user_id;
        var indexToRemove = pacientes_datos.findIndex(function (user) {
          return user.user_id === userIdToRemove;
        });

        if (indexToRemove !== -1) {
          pacientes_datos.splice(indexToRemove, 1);
        }
      });
    }
  }, {
    key: "show",
    value: function show(user_id) {
      var formdata = new FormData();
      formdata.append("user_id", user_id);
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])("/user_cuest_ubicacion/show", formdata);
    }
  }, {
    key: "gethistorialUbiEpisodio",
    value: function gethistorialUbiEpisodio(user_id) {
      var formdata = new FormData();
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_ubicacion/historialUbiEpisodio/" + user_id, formdata);
    }
  }, {
    key: "store2",
    value: function () {
      var _store = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4(user_id) {
        var formdata, fechaIngreso;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
          while (1) {
            switch (_context4.prev = _context4.next) {
              case 0:
                $("#row_" + user_id).slideUp("slow");
                formdata = new FormData();
                formdata.append("cat_user_ubicacion_id", $("#cat_user_ubicacion_id").val());
                formdata.append("subUbicacion1", $("#subUbicacion1").val() != undefined ? $("#subUbicacion1").val() : null);
                formdata.append("subUbicacion2", $("#subUbicacion2").val() != undefined ? $("#subUbicacion2").val() : null);

                if (!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_undefined"])($("#coment").val())) {
                  formdata.append("coment", $("#coment").val());
                }

                fechaIngreso = $("#ingreso").val(); //console.log(fechaIngreso)

                if (ingreso != undefined) {
                  console.log("Ingreso: ", ingreso);
                  formdata.append("value", ingreso);

                  if (fechaIngreso !== "") {
                    formdata.append("ingreso", fechaIngreso);
                  }
                }

                formdata.append("user_id", user_id);
                formdata.append("user_id_paciente", user_id);
                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                formdata.append("_token", $("#_token").val());
                console.log(formdata);
                _context4.next = 15;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_cuest_ubicacion/store", formdata);

              case 15:
                return _context4.abrupt("return", _context4.sent);

              case 16:
              case "end":
                return _context4.stop();
            }
          }
        }, _callee4);
      }));

      function store2(_x4) {
        return _store.apply(this, arguments);
      }

      return store2;
    }()
  }, {
    key: "egreso",
    value: function egreso(selector, user_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <h1 class=\"h1 text-primary\">\n                Egreso\n            </h1>\n            <div class=\"row overflow-auto mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray text-center\">\n                        Ubicaci\xF3n Actual\n                    </div>\n                    <input type=\"hidden\" id=\"cat_user_ubicacion_id_actual\">\n                    <input type=\"text\" style=\"background-color:var(--gray) !important\" readonly class=\"form-control text-center form-control-lg bg-gray-footer-parodi\" name=\"ubicacion_actual\" id=\"ubicacion_actual\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray text-center\">\n                        Destino alta\n                    </div>\n                    <select id=\"cat_user_ubicacion_id\" name=\"cat_user_ubicacion_id\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                        <option value='246'>Domicilio</option>\n                        <option value='251'>Traslado a otra Instituci\xF3n</option>\n                        <option value='247'>Contraopini\xF3n M\xE9dica</option>\n                        <option value='248'>Fallecido</option>\n                        <option value='249'>Otra</option>\n                    </select>\n                </div>\n\n            </div>\n            <div class=\"h3 text-gray text-center\">\n                Diagn\xF3stico de Egreso\n            </div>\n            <div class=\"flex-fill overflow-auto mb-1\">\n                <textarea class=\"form-control bg-gray-footer-parodi \"  placeholder=\"Escriba el diagn\xF3stico\" name=\"coment\" id=\"coment\" rows=\"3\" style=\" width: 100%;height: 100%;resize: none;\" ></textarea>\n            </div>\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_model_store\" class=\"font-weight-bold btn btn-success mr-1 w-100 mb-1\">Guardar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100 mb-1\"  data-dismiss=\"modal\" aria-label=\"Close\">Cancelar</button>\n\n            </div>\n\n        ");
      /*  $("#modelId").modal("show")
       $(selector).html(`
           <div id="infoPaciente" style="position: absolute;width: 100%;top:0;left:0;z-index: 200000;"></div>
           <div  class="row pt-6" style=" margin-top: 7.5rem !important;">
               <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 text-primary">
                   Egreso
               </div>
           </div>
           <div id="form" class="row">
               <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1">
                   <div class="h3 text-gray text-center">
                       Ubicación Actual
                   </div>
                   <input type="hidden" id="cat_user_ubicacion_id_actual">
                   <input type="text" style="background-color:var(--gray) !important" readonly class="form-control text-center form-control-lg bg-gray-footer-parodi" name="ubicacion_actual" id="ubicacion_actual" aria-describedby="helpId" placeholder="Nuevo valor">
               </div>
               <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1">
                   <div class="h3 text-gray text-center">
                       Destino alta
                   </div>
                   <select id="cat_user_ubicacion_id" name="cat_user_ubicacion_id" class="form-control form-control-lg bg-gray-footer-parodi">
                       <option value='246'>Domicilio</option>
                       <option value='251'>Traslado a otra Institución</option>
                       <option value='247'>Contraopinión Médica</option>
                       <option value='248'>Fallecido</option>
                       <option value='249'>Otra</option>
                   </select>
               </div>
               <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1">
                   <div class="h3 text-gray text-center">
                       Diagnóstico de Egreso
                   </div>
                   <div class="form-group">
                       <textarea class="form-control form-control-lg bg-gray-footer-parodi" placeholder="Escriba el diagnóstico" name="coment" id="coment" rows="3"></textarea>
                   </div>
               </div>
             </div>
           <div class="botones-bottom-modal">
               <button id="user_cuest_model_store" class="font-weight-bold btn btn-success mb-1 btn-block btn-lg">Guardar</button>
               <button id="regresar" class="font-weight-bold btn btn-primary mb-1 btn-block btn-lg"  data-dismiss="modal" aria-label="Close">Cancelar</button>
             </div>
       `); */

      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      this.show(user_id).then(function (response) {
        //console.log("UserCuestUbicacion", response)
        if (response.name != null) {
          $("#ubicacion_actual").val(response.name);
          $("#cat_user_ubicacion_id_actual").val(response.cat_user_ubicacion_id);
        } else {
          $("#cat_user_ubicacion_id_actual").val(response.cat_user_ubicacion_id);
          $("#ubicacion_actual").val(response.description + " | " + response.coments);
        }
      });
      $("#user_cuest_model_store").on("click", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee8() {
        var coment;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee8$(_context8) {
          while (1) {
            switch (_context8.prev = _context8.next) {
              case 0:
                coment = $('#coment');

                if (!(coment.val() == "")) {
                  _context8.next = 5;
                  break;
                }

                message = _Component_js__WEBPACK_IMPORTED_MODULE_7__["default"].alertMessage("input_text_empty");
                Swal.fire({
                  icon: message['image'],
                  title: message['title'],
                  text: message['message'],
                  didClose: function didClose() {
                    setTimeout(function () {
                      return coment.trigger("focus");
                    }, 110);
                  }
                });
                return _context8.abrupt("return", false);

              case 5:
                ingreso = "Egreso";
                _UserCuestEpisodio_js__WEBPACK_IMPORTED_MODULE_5__["default"].store({
                  user_id: user_id
                }).then( /*#__PURE__*/function () {
                  var _ref4 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee7(response) {
                    var patient_data, index;
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee7$(_context7) {
                      while (1) {
                        switch (_context7.prev = _context7.next) {
                          case 0:
                            if (String(response) == 'imp_diagn_not_found') {
                              Swal.fire({
                                icon: message['image'],
                                title: message['title'],
                                text: "Debe agregar una Probabilidad diagnóstica para generar un Informe de Egreso válido."
                              });
                              patient_data = pacientes_datos.find(function (x) {
                                return x.user_id === user_id;
                              });
                              index = pacientes_datos.findIndex(function (x) {
                                return x.user_id === user_id;
                              });
                              userCuestHistoria.index(patient_data, index); // UserCuestImpresionDiagnostica.create('.modal-body',user_id)
                            } else {
                              UserCuestUbicacion.store2(user_id).then( /*#__PURE__*/function () {
                                var _ref5 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6(response) {
                                  var user, paciente, correo, telefono_movil, userIdToRemove, indexToRemove, enlace;
                                  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
                                    while (1) {
                                      switch (_context6.prev = _context6.next) {
                                        case 0:
                                          user = pacientes_datos.find(function (x) {
                                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["equalsInt"])(x['user_id'], user_id);
                                          });
                                          paciente = user['paciente'];
                                          correo = user['email'];
                                          telefono_movil = "+584149320905"; //user['telefono_movil'];
                                          // ocultar el paciente luego del alta médica

                                          userIdToRemove = user_id;
                                          indexToRemove = pacientes_datos.findIndex(function (user) {
                                            return user.user_id === userIdToRemove;
                                          });

                                          if (indexToRemove !== -1) {
                                            pacientes_datos.splice(indexToRemove, 1);
                                          } //ENVIO DE ENCUESTA


                                          _context6.next = 9;
                                          return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/discharged/store/" + user_id);

                                        case 9:
                                          enlace = "";
                                          $("#row_" + user_id).slideUp("slow");
                                          $(".row_" + user_id).slideUp("slow");
                                          $("#modal-patient-item").modal("hide");
                                          /* switch (response['egreso_de']) {
                                              case 'pad':
                                                  return false
                                              break;
                                              case 'emergencia':
                                                  return false;
                                              break;
                                              case 'hospitalizacion':
                                                  enlace = `
                                                      https://api.whatsapp.com/send/?phone=${telefono_movil}&text=*Hola, ${paciente}*,%0ARecibe un afectuoso saludo de parte del equipo médico del Centro Médico Docente La Trinidad. Gracias, por preferirnos y considerarnos el aliado de su salud.%0ATus impresiones y opiniones son de mucha importancia para mejorar nuestros servicios, por lo que nos sentiríamos muy agradecidos de que nos puedas dedicar un momento de tu valioso tiempo, para responder una breve encuesta y compartirnos, cómo fue tu experiencia con nosotros. %0A*Has clic en el siguiente enlace para empezar*:%0A https://patientcare.cmdlt.pstelemed.com/encuesta/hospitalizacion/nueva
                                                  `
                                              break;
                                                default:
                                                  return false
                                              break;
                                          } */

                                          return _context6.abrupt("return", false);

                                        case 15:
                                        case "end":
                                          return _context6.stop();
                                      }
                                    }
                                  }, _callee6);
                                }));

                                return function (_x6) {
                                  return _ref5.apply(this, arguments);
                                };
                              }());
                            }

                          case 1:
                          case "end":
                            return _context7.stop();
                        }
                      }
                    }, _callee7);
                  }));

                  return function (_x5) {
                    return _ref4.apply(this, arguments);
                  };
                }());

              case 7:
              case "end":
                return _context8.stop();
            }
          }
        }, _callee8);
      })));
    }
  }, {
    key: "reingreso",
    value: function reingreso(selector, user_id) {
      $("#modal-patient-item").modal("show");
      $("#modal-patient-item .modal-body-2").html(
      /*html */
      "\n            <div id=\"infoPaciente\"></div>\n            <h1 class=\"h1 text-primary\">\n                Reingreso\n            </h1>\n            <div class=\"row mb-auto overflow-auto mb-1\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray text-center\">\n                        Ubicaci\xF3n Actual\n                    </div>\n                    <input type=\"text\" style=\"background-color:var(--gray) !important\" readonly class=\"form-control text-center form-control-lg bg-gray-footer-parodi\" name=\"ubicacion_actual\" id=\"ubicacion_actual\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1\">\n                    <div class=\"h3 text-gray text-center\">\n                        Nueva Ubicaci\xF3n\n                    </div>\n                    <select id=\"cat_user_ubicacion_id\" name=\"cat_user_ubicacion_id\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <select id=\"subUbicacion1\" style=\"display:none;\" name=\"subUbicacion1\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </select>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1\">\n                    <select id=\"subUbicacion2\" style=\"display:none;\" name=\"subUbicacion2\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </select>\n                </div>\n\n            </div>\n\n            <div class=\"d-flex\">\n                <button id=\"user_cuest_ubicacion_store\" class=\"font-weight-bold btn btn-success mr-1 w-100 mb-1\">Guardar</button>\n                <button id=\"regresar\" class=\"font-weight-bold btn btn-primary w-100 mb-1\"  data-dismiss=\"modal\" aria-label=\"Close\">Cancelar</button>\n\n            </div>\n\n        ");
      userCuestPaciente.infoPaciente("#infoPaciente", user_id);
      this.show(user_id).then(function (response) {
        //console.log("UserCuestUbicacion", response)
        if (response.name != null) {
          $("#ubicacion_actual").val(response.name);
        } else {
          $("#ubicacion_actual").val(response.description + " | " + response.coments);
        }
      });
      catUserUbicacion.getIndex().then(function (response) {
        //console.log("catUserUbicacion:", response)
        $("#cat_user_ubicacion_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(response));
      });
      $("#cat_user_ubicacion_id").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion1").show();
            $("#subUbicacion2").hide();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion1").html(data);
          } else {
            $("#subUbicacion1").hide();
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#subUbicacion1").on("change", function () {
        catUserUbicacion.show($(this).val()).then(function (response) {
          if (response.length > 0) {
            $("#subUbicacion2").show();
            var data = "<option value=''>Seleccione</option>";
            $.each(response, function (indexInArray, valueOfElement) {
              data += "\n                                <option value=\"".concat(valueOfElement.id, "\">\n                                    ").concat(valueOfElement.coments, "\n                                </option>\n                            ");
            });
            $("#subUbicacion2").html(data);
          } else {
            $("#subUbicacion2").hide();
          }
        });
      });
      $("#user_cuest_ubicacion_store").on("click", function () {
        ingreso = "Reingreso";
        UserCuestUbicacion.store2(user_id).then(function (response) {
          console.log("UserCuestUbicacion:", response);
          $("#row_" + user_id).slideUp("slow");
          $(".row_" + user_id).slideUp("slow");
          $("#modal-patient-item").modal("hide");
          window.location = "/medico";
        });
      }); //MODAL REINGRESO

      /*  $("#modelId").modal("show")
       $(selector).html(`
           <div id="infoPaciente" style="position: absolute;width: 100%;top:0;left:0;z-index: 200000;"></div>
           <div  class="row pt-6" style=" margin-top: 7.5rem !important;">
               <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 text-primary">
                   Reingreso
               </div>
           </div>
           <div id="form" class="row">
               <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1">
                   <div class="h3 text-gray text-center">
                       Ubicación Actual
                   </div>
                   <input type="text" style="background-color:var(--gray) !important" readonly class="form-control text-center form-control-lg bg-gray-footer-parodi" name="ubicacion_actual" id="ubicacion_actual" aria-describedby="helpId" placeholder="Nuevo valor">
               </div>
               <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6 mt-1">
                   <div class="h3 text-gray text-center">
                       Nueva Ubicación
                   </div>
                   <select id="cat_user_ubicacion_id" name="cat_user_ubicacion_id" class="form-control form-control-lg bg-gray-footer-parodi">
                     </select>
               </div>
               <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1">
                   <select id="subUbicacion1" style="display:none;" name="subUbicacion1" class="form-control form-control-lg bg-gray-footer-parodi">
                   </select>
               </div>
               <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 mt-1">
                   <select id="subUbicacion2" style="display:none;" name="subUbicacion2" class="form-control form-control-lg bg-gray-footer-parodi">
                   </select>
               </div>
             </div>
             <div class="botones-bottom-modal">
               <button id="user_cuest_ubicacion_store" class="font-weight-bold btn btn-success mb-1 btn-block btn-lg">Guardar</button>
               <button id="regresar" class="font-weight-bold btn btn-primary mb-1 btn-block btn-lg">Regresar</button>
             </div>
       `); */

      /**/
    }
  }]);

  return UserCuestUbicacion;
}();



/***/ }),

/***/ "./public/class_js/UserEquipoSalud.js":
/*!********************************************!*\
  !*** ./public/class_js/UserEquipoSalud.js ***!
  \********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserEquipoSalud; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var UserEquipoSalud = /*#__PURE__*/function () {
  function UserEquipoSalud() {
    _classCallCheck(this, UserEquipoSalud);
  }

  _createClass(UserEquipoSalud, null, [{
    key: "store",
    value: function store(user_id) {
      var formdata = new FormData();
      formdata.append("cat_user_equipo_salud_id", $("#cat_user_equipo_salud_id").val());
      formdata.append("user_id", user_id);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_equipo_salud/store", formdata);
    }
  }, {
    key: "update",
    value: function update(user_id) {
      var formdata = new FormData();
      formdata.append("cat_user_equipo_salud_id", $("#cat_user_equipo_salud_id").val());
      formdata.append("user_id", user_id);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_equipo_salud/update", formdata);
    }
  }, {
    key: "show",
    value: function () {
      var _show = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(user_id) {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                _context.next = 2;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/user_equipo_salud/show/" + user_id);

              case 2:
                return _context.abrupt("return", _context.sent);

              case 3:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function show(_x) {
        return _show.apply(this, arguments);
      }

      return show;
    }()
  }, {
    key: "menuEquipoEnArea",
    value: function menuEquipoEnArea(_ref) {
      var _ref$selector = _ref.selector,
          selector = _ref$selector === void 0 ? '.modal-body' : _ref$selector,
          _ref$area = _ref.area,
          area = _ref$area === void 0 ? 'tp' : _ref$area;
      $("#modelId").modal("show");
      $(selector).html("\n            <div>\n                <h1 class=\"text-primary\">\n                    Equipo m\xE9dico en el \xE1rea\n                </h1>\n            </div>\n            <div id=\"lista_especialistas_by_area\" style=\"max-height: 60vh;overflow: auto;\" class=\"row row-cols-1 row-cols-md-3\">\n                <div class=\"d-flex m-4 justify-content-center text-primary\">\n                    Cargando...\n                    <div class=\"spinner-border\" role=\"status\">\n                        <span class=\"sr-only\"></span>\n                    </div>\n                </div>\n            </div>\n\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 pl-3 pr-3 pt-1 pb-1\">\n                    <button id=\"regresar\"  data-dismiss=\"modal\" aria-label=\"Close\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                </div>\n            </div>\n        ");
      UserEquipoSalud.medicosByArea({
        area: area
      }).then(function (response) {
        $("#lista_especialistas_by_area").empty();

        if (Object.keys(response).length > 0) {
          $.each(response, function (indexInArray, valueOfElement) {
            $("#lista_especialistas_by_area").append("\n                        <div class=\"col mb-4\">\n                            <div data-id=\"".concat(valueOfElement['user_id'], "\" data-medico=\"").concat(valueOfElement['medico'], "\" class=\"card card-especialista mb-3\">\n                                <div class=\"row no-gutters py-2\">\n                                    <div class=\"col-md-2 border-right\">\n                                        <div class=\"text-center mt-1\">\n                                            <img onerror=\"reemplaza_imagen(this)\" src=\"").concat(location.origin).concat(valueOfElement['imagen'], "\" class=\"card-img rounded-circle\" style=\"width:3em;height:3em;\" alt=\"Foto M\xE9dico\">\n                                        </div>\n                                    </div>\n                                    <div class=\"col-md-10\">\n                                        <div class=\"card-body p-1\">\n                                        <div class=\"overflow-hidden\" style=\"max-width:100%;\">\n                                            <div class=\"card-title font-weight-bold text-secondary\"  style=\"font-size: 1.3em;white-space: nowrap;\">\n                                                ").concat(valueOfElement['medico'], "\n                                            </div>\n                                        </div>\n\n                                            <div class=\"card-text m-1\">\n                                                <div class=\"text-primary icon_esp\"  data-tippy-content=\"Especialidad\">\n                                                    <img src=\"").concat(location.origin, "/image/system/especialidades/").concat(valueOfElement['especialidad_imagen'], ".svg\" class=\"card-img rounded-circle\" style=\"width:1.5em;height:1.5em;\" alt=\"Especialidad\">\n                                                    ").concat(valueOfElement['especialidad'], "\n                                                </div>\n                                                <div class=\"mt-1 icon_esp\"  data-tippy-content=\"Pacientes siendo tratados por ").concat(valueOfElement['medico'], "\">\n                                                    <span style=\"font-size: 1em;\" class=\"badge badge-primary\">").concat(valueOfElement['cantidad_pacientes'], "</span>\n                                                    <span class=\"text-primary font-weight-bold\">Paciente").concat(valueOfElement['cantidad_pacientes'] > 1 ? 's' : '', "</span>\n                                                </div>\n                                            </div>\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n                    "));
          });
          $(".card-especialista").on("click", function () {
            Component.viewByMedico({
              'medico': $(this).data("medico"),
              'medico_id': $(this).data("id")
            }); //UserCuestPad.menuAccion('m_covid', $(this).data("area"), $(this).data("titlearea"))
          });
        } else {
          $("#lista_especialistas_by_area").html("\n                    <div class=\"text-center p-3 text-primary\">\n                        No se encontraron M\xE9dicos activos en esta \xE1rea.\n                    </div>\n                ");
        }

        tippy(".icon_esp", {
          theme: "tooltip-cmdlt-primary",
          zIndex: 200000
        });
      });
    }
  }, {
    key: "menuEquipoEnEspecialidad",
    value: function menuEquipoEnEspecialidad(_ref2) {
      var _ref2$selector = _ref2.selector,
          selector = _ref2$selector === void 0 ? '.modal-body' : _ref2$selector,
          _ref2$cat_especialida = _ref2.cat_especialidad_id,
          cat_especialidad_id = _ref2$cat_especialida === void 0 ? 'null' : _ref2$cat_especialida;
      $("#modelId").modal("show");
      $(selector).html("\n            <div>\n                <h1 class=\"text-primary\">\n                    Equipo m\xE9dico en la Especialidad\n                </h1>\n            </div>\n            <div id=\"lista_especialistas_by_area\" style=\"max-height: 60vh;overflow: auto;\" class=\"row row-cols-1 row-cols-md-3\">\n                <div class=\"d-flex m-4 justify-content-center text-primary\">\n                    Cargando...\n                    <div class=\"spinner-border\" role=\"status\">\n                        <span class=\"sr-only\"></span>\n                    </div>\n                </div>\n            </div>\n\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 pl-3 pr-3 pt-1 pb-1\">\n                    <button id=\"regresar\"  data-dismiss=\"modal\" aria-label=\"Close\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                </div>\n            </div>\n        ");
      UserEquipoSalud.medicosByEspecialidad({
        cat_especialidad_id: cat_especialidad_id
      }).then(function (response) {
        $("#lista_especialistas_by_area").empty();

        if (Object.keys(response).length > 0) {
          $.each(response, function (indexInArray, valueOfElement) {
            $("#lista_especialistas_by_area").append("\n                        <div class=\"col mb-4\">\n                            <div data-id=\"".concat(valueOfElement['user_id'], "\" data-medico=\"").concat(valueOfElement['medico'], "\" class=\"card card-especialista mb-3\">\n                                <div class=\"row no-gutters py-2\">\n                                    <div class=\"col-md-2 border-right\">\n                                        <div class=\"text-center mt-1\">\n                                            <img onerror=\"reemplaza_imagen(this)\" src=\"").concat(location.origin).concat(valueOfElement['imagen'], "\" class=\"card-img rounded-circle\" style=\"width:3em;height:3em;\" alt=\"Foto M\xE9dico\">\n                                        </div>\n                                    </div>\n                                    <div class=\"col-md-10\">\n                                        <div class=\"card-body p-1\">\n                                        <div class=\"overflow-hidden\" style=\"max-width:100%;\">\n                                            <div class=\"card-title font-weight-bold text-secondary\"  style=\"font-size: 1.3em;white-space: nowrap;\">\n                                                ").concat(valueOfElement['medico'], "\n                                            </div>\n                                        </div>\n\n                                            <div class=\"card-text m-1\">\n                                                <div class=\"text-primary icon_esp\"  data-tippy-content=\"Especialidad\">\n                                                    <img src=\"").concat(location.origin, "/image/system/especialidades/").concat(valueOfElement['especialidad_imagen'], ".svg\" class=\"card-img rounded-circle\" style=\"width:1.5em;height:1.5em;\" alt=\"Especialidad\">\n                                                    ").concat(valueOfElement['especialidad'], "\n                                                </div>\n                                                <div class=\"mt-1 icon_esp\"  data-tippy-content=\"Pacientes siendo tratados por ").concat(valueOfElement['medico'], "\">\n                                                    <span style=\"font-size: 1em;\" class=\"badge badge-primary\">").concat(valueOfElement['cantidad_pacientes'], "</span>\n                                                    <span class=\"text-primary font-weight-bold\">Paciente").concat(valueOfElement['cantidad_pacientes'] > 1 ? 's' : '', "</span>\n                                                </div>\n                                            </div>\n                                        </div>\n                                    </div>\n                                </div>\n                            </div>\n                        </div>\n                    "));
          });
          $(".card-especialista").on("click", function () {
            Component.viewByMedico({
              'medico': $(this).data("medico"),
              'medico_id': $(this).data("id")
            }); //UserCuestPad.menuAccion('m_covid', $(this).data("area"), $(this).data("titlearea"))
          });
        } else {
          $("#lista_especialistas_by_area").html("\n                    <div class=\"text-center p-3 text-primary\">\n                        No se encontraron M\xE9dicos activos en esta \xE1rea.\n                    </div>\n                ");
        }

        tippy(".icon_esp", {
          theme: "tooltip-cmdlt-primary",
          zIndex: 200000
        });
      });
    }
  }, {
    key: "medicosByArea",
    value: function medicosByArea(_ref3) {
      var _ref3$area = _ref3.area,
          area = _ref3$area === void 0 ? 'tp' : _ref3$area,
          _ref3$searchBy = _ref3.searchBy,
          searchBy = _ref3$searchBy === void 0 ? 'medico' : _ref3$searchBy;
      var formdata = new FormData();
      formdata.append("area", area);
      formdata.append("searchBy", searchBy);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_equipo_salud/medicosByArea", formdata);
    }
  }, {
    key: "medicosByEspecialidad",
    value: function medicosByEspecialidad(_ref4) {
      var _ref4$cat_especialida = _ref4.cat_especialidad_id,
          cat_especialidad_id = _ref4$cat_especialida === void 0 ? 'null' : _ref4$cat_especialida;
      var formdata = new FormData();
      formdata.append("cat_especialidad_id", cat_especialidad_id);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_equipo_salud/medicosByEspecialidad", formdata);
    }
  }]);

  return UserEquipoSalud;
}();



/***/ }),

/***/ "./public/class_js/UserEspecialidad.js":
/*!*********************************************!*\
  !*** ./public/class_js/UserEspecialidad.js ***!
  \*********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserEspecialidad; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var UserEspecialidad = /*#__PURE__*/function () {
  function UserEspecialidad() {
    _classCallCheck(this, UserEspecialidad);
  }

  _createClass(UserEspecialidad, null, [{
    key: "store",
    value: function store(user_id) {
      if ($("#cat_user_especialidad_id").val() == "") {
        $("#cat_user_especialidad_id").val(1);
      }

      var formdata = new FormData();
      formdata.append("cat_user_especialidad_id", $("#cat_user_especialidad_id").val());
      formdata.append("user_id", user_id);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_especialidad/store", formdata);
    }
  }, {
    key: "update",
    value: function update(user_id) {
      var formdata = new FormData();
      formdata.append("cat_user_especialidad_id", $("#cat_user_especialidad_id").val());
      formdata.append("user_id", user_id);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_especialidad/update", formdata);
    }
  }, {
    key: "show",
    value: function () {
      var _show = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(user_id) {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                _context.next = 2;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/user_especialidad/show/" + user_id);

              case 2:
                return _context.abrupt("return", _context.sent);

              case 3:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function show(_x) {
        return _show.apply(this, arguments);
      }

      return show;
    }()
  }, {
    key: "index_medico_especialidad",
    value: function index_medico_especialidad() {
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/user_especialidad/index_medico_especialidad");
    }
  }]);

  return UserEspecialidad;
}();



/***/ }),

/***/ "./public/class_js/UserFamily.js":
/*!***************************************!*\
  !*** ./public/class_js/UserFamily.js ***!
  \***************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserFamily; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }

var UserFamily = /*#__PURE__*/function () {
  function UserFamily() {
    _classCallCheck(this, UserFamily);
  }

  _createClass(UserFamily, [{
    key: "create",
    value: function create(selector) {
      $(selector).html("\n\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"familiar_email\">Correo Electr\xF3nico</label>\n                        <input type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"familiar_email\" id=\"familiar_email\" aria-describedby=\"helpId\" placeholder=\"\">\n\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"familiar_nombre\">Nombres</label>\n                        <input type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"familiar_nombre\" id=\"familiar_nombre\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"familiar_cedula\">Documento de identidad</label>\n                        <input type=\"number\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"familiar_cedula\" id=\"familiar_cedula\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"familiar_genero\">G\xE9nero</label>\n                        <select class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"familiar_genero\" id=\"familiar_genero\" aria-describedby=\"helpId\" placeholder=\"\">\n                            <option value=\"\">Seleccione</option>\n                            <option value=\"m\">Masculino</option>\n                            <option value=\"f\">Femenino</option>\n                        </select>\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"familiar_apellido\">Apellidos</label>\n                        <input type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"familiar_apellido\" id=\"familiar_apellido\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"cat_user_family_id\">V\xEDnculo</label>\n                        <select class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"cat_user_family_id\" id=\"cat_user_family_id\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <option value=\"\">Seleccione</option>\n                            <option value=\"1\">Abuelo(a)</option>\n                            <option value=\"2\">Amigo(a)</option>\n                            <option value=\"3\">Esposo(a)</option>\n                            <option value=\"4\">Hermano(a)</option>\n                            <option value=\"5\">Hijo(a)</option>\n                            <option value=\"6\">Madre</option>\n                            <option value=\"7\">Padre</option>\n                            <option value=\"8\">Pareja</option>\n                            <option value=\"9\">Sobrino(a)</option>\n                        </select>\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"familiar_telefono_movil\">Tel\xE9fono Contacto</label>\n                        <input type=\"number\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"familiar_telefono_movil\" id=\"familiar_telefono_movil\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n            </div>\n        ");
    }
  }, {
    key: "store",
    value: function () {
      var _store = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(user_id) {
        var familiar_nombre, familiar_apellido, familiar_email, familiar_cedula, cat_user_family_id, familiar_genero, familiar_telefono_movil, formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                familiar_nombre = $("#familiar_nombre");
                familiar_apellido = $("#familiar_apellido");
                familiar_email = $("#familiar_email");
                familiar_cedula = $("#familiar_cedula");
                cat_user_family_id = $("#cat_user_family_id");
                familiar_genero = $("#familiar_genero");
                familiar_telefono_movil = $("#familiar_telefono_movil");
                formdata = new FormData();
                formdata.append("familiar_nombre", familiar_nombre.val());
                formdata.append("familiar_apellido", familiar_apellido.val());
                formdata.append("familiar_email", familiar_email.val());
                formdata.append("familiar_cedula", familiar_cedula.val());
                formdata.append("cat_user_family_id", cat_user_family_id.val());
                formdata.append("familiar_genero", familiar_genero.val());
                formdata.append("familiar_telefono_movil", familiar_telefono_movil.val());
                formdata.append("user_id", user_id);
                formdata.append("created_at", timestamps());
                formdata.append("_token", $("#_token").val());
                _context.next = 20;
                return post(location.origin + "/user_family/store", formdata);

              case 20:
                return _context.abrupt("return", _context.sent);

              case 21:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function store(_x) {
        return _store.apply(this, arguments);
      }

      return store;
    }()
  }, {
    key: "validate",
    value: function validate() {
      var familiar_nombre = $("#familiar_nombre");
      var familiar_apellido = $("#familiar_apellido");
      var familiar_email = $("#familiar_email");
      var familiar_cedula = $("#familiar_cedula");
      var familiar_cat_user_family_id = $("#familiar_cat_user_family_id");
      var familiar_genero = $("#familiar_genero");
      var familiar_telefono_movil = $("#familiar_telefono_movil");

      if (familiar_nombre.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        familiar_nombre.trigger("focus");
        return false;
      }

      if (familiar_apellido.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        familiar_apellido.trigger("focus");
        return false;
      }

      if (familiar_cedula.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        familiar_cedula.trigger("focus");
        return false;
      }

      if (familiar_cat_user_family_id.val() == "") {
        message = Component.alertMessage("input_select_empty");
        alert(message['message']);
        familiar_cat_user_family_id.trigger("focus");
        return false;
      }

      if (familiar_telefono_movil.val() == "") {
        message = Component.alertMessage("input_text_empty");
        alert(message['message']);
        familiar_telefono_movil.trigger("focus");
        return false;
      }

      return true;
    }
  }]);

  return UserFamily;
}();



/***/ }),

/***/ "./public/class_js/UserMedico.js":
/*!***************************************!*\
  !*** ./public/class_js/UserMedico.js ***!
  \***************************************/
/*! exports provided: telefonoConfig, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "telefonoConfig", function() { return telefonoConfig; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserMedico; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _Component_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Component.js */ "./public/class_js/Component.js");
/* harmony import */ var _User_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./User.js */ "./public/class_js/User.js");
/* harmony import */ var _UserProfile_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./UserProfile.js */ "./public/class_js/UserProfile.js");
/* harmony import */ var _UserType_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./UserType.js */ "./public/class_js/UserType.js");
/* harmony import */ var _CatUserEspecialidad_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./CatUserEspecialidad.js */ "./public/class_js/CatUserEspecialidad.js");
/* harmony import */ var _UserEquipoSalud_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./UserEquipoSalud.js */ "./public/class_js/UserEquipoSalud.js");
/* harmony import */ var _UserEspecialidad_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./UserEspecialidad.js */ "./public/class_js/UserEspecialidad.js");
/* harmony import */ var _UserMedicoPosicion_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./UserMedicoPosicion.js */ "./public/class_js/UserMedicoPosicion.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }









 //let user = new User()

var userProfile = new _UserProfile_js__WEBPACK_IMPORTED_MODULE_4__["default"]();
var userType = new _UserType_js__WEBPACK_IMPORTED_MODULE_5__["default"]();
var message;
var iti;
var telefonoConfig = function telefonoConfig(selectorInput, customhandleChange) {
  var input = document.querySelector(selectorInput);
  iti = window.intlTelInput(input, {
    autoHideDialCode: true,
    nationalMode: false,
    preferredCountries: ['ve'],
    separateDialCode: true,
    utilsScript: "/plugin/intl-tel-input/build/js/utils.js"
  });
  /*
  let handleChange = function() {
  customhandleChange(iti.getNumber(),iti.isValidNumber())
  };
  input.addEventListener('change', handleChange);
  input.addEventListener('keyup', handleChange);*/
};

var UserMedico = /*#__PURE__*/function () {
  function UserMedico() {
    _classCallCheck(this, UserMedico);
  }

  _createClass(UserMedico, [{
    key: "getMedicosByCargo",
    value: function () {
      var _getMedicosByCargo = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee() {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                _context.next = 2;
                return UserMedico.getMedicos();

              case 2:
                medicos_datos = _context.sent;

              case 3:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function getMedicosByCargo() {
        return _getMedicosByCargo.apply(this, arguments);
      }

      return getMedicosByCargo;
    }()
  }], [{
    key: "index",
    value: function () {
      var _index = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4(selector, user_id) {
        var model;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
          while (1) {
            switch (_context4.prev = _context4.next) {
              case 0:
                _context4.next = 2;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/medico/getMedicos");

              case 2:
                model = _context4.sent;
                $(selector).html("\n            <div class=\"row mt-5\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <div>\n                        <h1 class=\"text-primary\">\n                            Equipo M\xE9dico\n                        </h1>\n                    </div>\n\n                    <span id=\"user_cuest_model_create\" onclick=\"window.location='/medico/create'\" class=\"float-right text-shadow-drop-tl text-primary text-right\" style=\"font-size: 2rem !important; cursor:pointer;\">\n                        Nuevo Usuario\n                        <i class=\"fa fa-plus\" aria-hidden=\"true\"></i>\n                    </span>\n\n                    <div class=\"table-responsive mb-3\">\n                        <table id=\"index_table_medicos\" class=\"table table-bordered mb-3\">\n                            <thead>\n\n                                <tr>\n                                    <th class=\"text-primary sticky-top text-center\">Foto</th>\n                                    <th class=\"text-primary sticky-top text-center\">M\xE9dico</th>\n                                    <th class=\"text-primary sticky-top text-center\">Sexo</th>\n                                    <th class=\"text-primary sticky-top text-center\">C\xE9dula</th>\n                                    <th class=\"text-primary sticky-top text-center\">Tel\xE9fonos</th>\n                                    <th class=\"text-primary sticky-top text-center\">Correo</th>\n                                    <th class=\"text-primary sticky-top text-center\">Equipo Salud</th>\n                                    <th class=\"text-primary sticky-top text-center\">Especialidad</th>\n                                    <th class=\"text-primary sticky-top text-center\">Posici\xF3n/Cargo</th>\n                                    <!--<th class=\"text-primary text-center\">Ingreso</th>-->\n                                    <th class=\"text-primary sticky-top text-center\" nowrap>Acci\xF3n</th>\n                                </tr>\n                            </thead>\n                            <tbody id=\"filas\">\n                                <tr>\n                                    <td colspan=\"10\" class=\"text-center\">\n                                        <div class=\"spinner-border text-primary\" role=\"status\">\n                                            <span class=\"sr-only\">Loading...</span>\n                                        </div>\n                                    </td>\n                                </tr>\n                            </tbody>\n                        </table>\n                    </div>\n                </div>\n            </div>\n        ");

                if (!(Object.keys(model).length > 0)) {
                  _context4.next = 9;
                  break;
                }

                $("#index_table_medicos tbody").empty();
                model.map(function (valueOfElement) {
                  // console.log(`Valor del elemento ${valueOfElement}`)
                  //cl(valueOfElement['prefijo'])onclick="EditImage(${valueOfElement['user_id']},'${valueOfElement['imagen']}')"
                  $("#index_table_medicos tbody").append("\n                    <tr id=\"row_".concat(valueOfElement['user_id'], "\">\n                        <td>\n                            <img\n                                data-user_id=\"").concat(valueOfElement['user_id'], "\"\n                                data-urlimg=\"").concat(valueOfElement['imagen'], "\"\n                                id=\"image-perfil_").concat(valueOfElement['user_id'], "\" onerror=\"this.src = '/image/system/medic.svg'\"\n                                src=\"").concat(valueOfElement['imagen'], "\"\n                                style=\"cursor:pointer; width: 1.5cm; height:1.5cm; object-fit: cover;\"\n                                class=\"image-perfil rounded-circle mx-auto d-block\" alt=\"Medic default\"'\n                            >\n                            <span class=\"text-white\">\n                                #").concat(valueOfElement['user_id'], "\n                            </span>\n\n                        </td>\n                        <td>\n                            ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(valueOfElement['prefijo']) ? valueOfElement['prefijo'] + ' ' : '', "\n\n                            <div class=\"d-flex flex-column\">\n                                <h5 class=\"mb-0 text-secondary\">\n                                    ").concat(valueOfElement['medico'], "\n                                </h5>\n                                <div class=\"d-flex\">\n                                    <div class=\"flex-fill\">\n                                        <b style=\"font-size:0.6rem;font-weight:500;\" class=\"text-info\">Firma</b>\n                                        <img onerror=\"this.style.display='none'\" src=\"").concat(['image/user/firma/firma_patientcare_default.png', null].includes(valueOfElement['firma']) ? '#' : '/' + valueOfElement['firma'], "\" style=\"height:50px\" >\n\n                                    </div>\n                                    <div class=\"flex-fill\">\n                                        <b style=\"font-size:0.6rem;font-weight:500;\" class=\"text-info\">Sello</b>\n                                        <img onerror=\"this.style.display='none'\" src=\"").concat(['image/user/sello/sello_patientcare_default.png', null].includes(valueOfElement['sello']) ? '#' : '/' + valueOfElement['sello'], "\" style=\"height:50px\" >\n                                    </div>\n                                </div>\n                            </div>\n                        </td>\n                        <td class=\"text-center\">\n                            ").concat(valueOfElement['genero'], "\n                        </td>\n                        <td class=\"text-right\">\n                            ").concat(valueOfElement['cedula'], "\n                        </td>\n                        <td>\n                            <div class=\"d-flex flex-column\">\n                                <div>\n                                    <b class=\"text-info\">Movil:</b>\n                                    <a target=\"_blank\" href=\"https://api.whatsapp.com/send?phone=").concat(['null', null].includes(valueOfElement['telefono_movil']) ? '#' : valueOfElement['telefono_movil'].replace("+", ""), "\" style=\"padding: 0% 3% 3% 3%;border: 0;line-height: 1.4;\" class=\"btn text-nowrap btn-default btn-block\">\n                                        <i class=\"fab fa-whatsapp text-success\"></i> ").concat(['null', null].includes(valueOfElement['telefono_movil']) ? '' : valueOfElement['telefono_movil'].replace("+", ""), "\n                                    </a>\n                                </div>\n                                <div>\n                                    <b class=\"text-orange\">Aster\xEDsco:</b> ").concat(['null', null].includes(valueOfElement['extension_telefonica']) ? '' : valueOfElement['extension_telefonica'], "\n                                </div>\n                            </div>\n\n                        </td>\n                        <td>\n                            <div>\n                            ").concat(valueOfElement['email'], "\n                            </div>\n                        </td>\n                        <td>\n                            ").concat(valueOfElement['equipo_salud'], "\n                        </td>\n                        <td>\n                            ").concat(['Sin Especialidad', null].includes(valueOfElement['especialidad']) ? '' : valueOfElement['especialidad'], "\n                        </td>\n                        <td>\n                            ").concat(valueOfElement['posicion'], "\n                        </td>\n                        <td nowrap>\n                            <button id=\"edit").concat(valueOfElement['user_id'], "\" onclick=\"location.href='/medico/edit/").concat(valueOfElement['user_id'], "'\" class=\"btn btn-default\"><i class=\"fas fa-user-edit text-primary\"></i></button>\n                            <button \n                                id=\"destroy").concat(valueOfElement['user_id'], "\" \n                                class=\"btn btn-default handleDelete\"\n                                data-user_id=\"").concat(valueOfElement['user_id'], "\" \n                            >\n                                <i data-user_id=\"").concat(valueOfElement['user_id'], "\"  class=\"handleDelete fas fa-user-minus text-danger\"></i>\n                            </button>\n                            <button \n                                class=\"btn btn-default handleRole\" \n                                type=\"button\"\n                                data-user_id=\"").concat(valueOfElement['user_id'], "\" \n                                data-role=\"4\" \n                            >\n                                <i id=\"role_").concat(valueOfElement['user_id'], "_4\" \n                                    data-user_id=\"").concat(valueOfElement['user_id'], "\" \n                                    data-role=\"4\" \n                                    class=\"fas fa-user-shield handleRole text-").concat(valueOfElement['user_roles_id'].split(",").includes('4') ? 'success' : 'gray', " \"\n                                ></i>\n                            </button>\n                        </td>\n                    </tr>\n                "));
                });
                _context4.next = 10;
                break;

              case 9:
                throw new Error(model);

              case 10:
                $('#index_table_medicos').DataTable({
                  "order": [[1, "asc"]]
                });
                document.addEventListener("click", /*#__PURE__*/function () {
                  var _ref = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3(e) {
                    var _user_id, role_id, r, _index2, formdata, temp_roles, message, _user_id2;

                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
                      while (1) {
                        switch (_context3.prev = _context3.next) {
                          case 0:
                            if (!e.target.matches(".handleRole")) {
                              _context3.next = 14;
                              break;
                            }

                            document.querySelector("#loadingScreen").classList.remove("d-none");
                            _user_id = Number(e.target.dataset.user_id);
                            role_id = Number(e.target.dataset.role);
                            r = model.find(function (med) {
                              return Number(med.user_id) === _user_id;
                            })['user_roles_id'].split(",").map(function (x) {
                              return Number(x);
                            });
                            _index2 = model.findIndex(function (med) {
                              return Number(med.user_id) === _user_id;
                            });
                            formdata = new FormData();
                            formdata.append("user_id", _user_id);
                            formdata.append("role_id", role_id);
                            formdata.append("_token", $("#_token").val());
                            _context3.next = 12;
                            return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/medico/updateRol", formdata);

                          case 12:
                            if (r.includes(role_id)) {
                              temp_roles = r.filter(function (x) {
                                return x !== role_id;
                              });
                              model[_index2]['user_roles_id'] = temp_roles.join(",");
                              $("#role_" + _user_id + "_" + role_id).removeClass("text-success").addClass("text-gray");
                            } else {
                              model[_index2]['user_roles_id'] = model[_index2]['user_roles_id'] + "," + role_id;
                              $("#role_" + _user_id + "_" + role_id).removeClass("text-gray").addClass("text-success");
                            }

                            document.querySelector("#loadingScreen").classList.add("d-none");

                          case 14:
                            if (e.target.matches(".handleDelete")) {
                              message = _Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('destroy_row_cuestion');
                              _user_id2 = Number(e.target.dataset.user_id);
                              Swal.fire({
                                icon: message['image'],
                                title: message['message'],
                                showDenyButton: true,
                                showCancelButton: false,
                                confirmButtonText: "Si"
                              }).then( /*#__PURE__*/function () {
                                var _ref2 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2(result) {
                                  var _formdata;

                                  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
                                    while (1) {
                                      switch (_context2.prev = _context2.next) {
                                        case 0:
                                          if (!result.isConfirmed) {
                                            _context2.next = 11;
                                            break;
                                          }

                                          _formdata = new FormData();

                                          _formdata.append("user_id", _user_id2);

                                          _formdata.append("cat_user_type_id", 2);

                                          _formdata.append("new_cat_user_type_id", 5);

                                          _formdata.append("_token", $("#_token").val());

                                          _context2.next = 8;
                                          return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_type/update", _formdata);

                                        case 8:
                                          $("#row_" + _user_id2).slideUp("slow");
                                          _context2.next = 12;
                                          break;

                                        case 11:
                                          if (result.isDenied) {//Swal.fire('Changes are not saved', '', 'info')
                                          }

                                        case 12:
                                        case "end":
                                          return _context2.stop();
                                      }
                                    }
                                  }, _callee2);
                                }));

                                return function (_x4) {
                                  return _ref2.apply(this, arguments);
                                };
                              }());
                            }

                          case 15:
                          case "end":
                            return _context3.stop();
                        }
                      }
                    }, _callee3);
                  }));

                  return function (_x3) {
                    return _ref.apply(this, arguments);
                  };
                }());

              case 12:
              case "end":
                return _context4.stop();
            }
          }
        }, _callee4);
      }));

      function index(_x, _x2) {
        return _index.apply(this, arguments);
      }

      return index;
    }()
  }, {
    key: "medicoTratante",
    value: function medicoTratante() {
      $("#modelId").modal("show");
      $(selector).html("\n            <div id=\"infoPaciente\" style=\"position: absolute;width: 100%;top:0;z-index: 200000;\"></div>\n            <div class=\"row mt-5\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 h1 text-primary\">\n                    M\xE9dico Tratante\n                </div>\n            </div>\n            <div id=\"form\" class=\"row\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <input type=\"number\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"value\" id=\"value\" aria-describedby=\"helpId\" placeholder=\"Nuevo valor\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <input type=\"datetime-local\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"created_at\" id=\"created_at\" aria-describedby=\"helpId\">\n                    <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <textarea class=\"form-control form-control-lg bg-gray-footer-parodi mb-3\" name=\"sintoma_observacion\" id=\"sintoma_observacion\" rows=\"2\" placeholder=\"Observaci\xF3n\"></textarea>\n                </div>\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <button id=\"user_cuest_model_store\" class=\"font-weight-bold btn btn-success mb-1 btn-block btn-lg\">Agregar</button>\n                </div>\n\n\n            </div>\n            </div>\n            <div class=\"row\">\n                <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                    <button id=\"regresar\" class=\"font-weight-bold btn btn-primary mb-1 btn-block btn-lg\">Regresar</button>\n                </div>\n            </div>\n        ");
      UserCuestPaciente.infoPaciente("#infoPaciente", user_id);
      $("#regresar").on("click", function () {
        UserCuestGl.index(selector, user_id);
      });
      $("#user_cuest_model_store").on("click", function () {
        UserCuestGl.store(user_id).then(function (response) {
          var valor = $("#value").val();
          UserCuestGl.columnaValor("#col_10_".concat(user_id), {
            'user_id': user_id,
            'valor': valor
          });
          UserCuestGl.index('.modal-body', user_id);
        });
      });
      $("#info").on("click", function () {
        UserCuestGl.help(selector, user_id);
      });
    }
  }, {
    key: "store",
    value: function () {
      var _store = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee5() {
        var model, _user_id3;

        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee5$(_context5) {
          while (1) {
            switch (_context5.prev = _context5.next) {
              case 0:
                _context5.next = 2;
                return _User_js__WEBPACK_IMPORTED_MODULE_3__["default"].store();

              case 2:
                model = _context5.sent;

                if (!(Object.keys(model).length > 0)) {
                  _context5.next = 21;
                  break;
                }

                _user_id3 = model.id;
                _context5.next = 7;
                return userType.store2({
                  'user_id': _user_id3,
                  'cat_user_type_id': 2
                });

              case 7:
                _context5.next = 9;
                return userType.store2({
                  'user_id': _user_id3,
                  'cat_user_type_id': 1
                });

              case 9:
                _context5.next = 11;
                return userProfile.store(_user_id3, iti);

              case 11:
                _context5.next = 13;
                return _UserEquipoSalud_js__WEBPACK_IMPORTED_MODULE_7__["default"].store(_user_id3);

              case 13:
                _context5.next = 15;
                return _UserEspecialidad_js__WEBPACK_IMPORTED_MODULE_8__["default"].store(_user_id3);

              case 15:
                _context5.next = 17;
                return _UserMedicoPosicion_js__WEBPACK_IMPORTED_MODULE_9__["default"].store(_user_id3);

              case 17:
                message = _Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("send_success");
                Swal.fire({
                  icon: message['image'],
                  title: message['title'],
                  text: message['message'],
                  didClose: function didClose() {
                    setTimeout(function () {
                      return window.location = "/medico/index_medicos";
                    }, 110);
                  }
                });
                _context5.next = 22;
                break;

              case 21:
                throw new Error(model);

              case 22:
              case "end":
                return _context5.stop();
            }
          }
        }, _callee5);
      }));

      function store() {
        return _store.apply(this, arguments);
      }

      return store;
    }()
  }, {
    key: "create",
    value: function create(selector) {
      UserMedico.create_form(selector);
      setTimeout(function () {
        Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])('imagen', ".image-perfil");
        Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])('sello', ".image-sello");
        Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])('firma', ".image-firma");
        document.querySelector("#cat_user_type_id").value = 2;
        console.log(document.querySelector(".extension_telefonica"));
        telefonoConfig("#telefono_movil");
        $("#user_medico_store").on("click", function () {
          if (_User_js__WEBPACK_IMPORTED_MODULE_3__["default"].validate()) {
            if (_UserProfile_js__WEBPACK_IMPORTED_MODULE_4__["default"].validate()) {
              UserMedico.store();
            }
          }
        });
      }, 1000);
    }
  }, {
    key: "edit",
    value: function () {
      var _edit = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee7(selector, user_id) {
        var response, emailCurrent;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee7$(_context7) {
          while (1) {
            switch (_context7.prev = _context7.next) {
              case 0:
                _context7.next = 2;
                return UserMedico.create_form(selector);

              case 2:
                $("#contenido_model").css("display", "none");
                $("#cargando_model").show();
                document.querySelector("#cat_user_type_id").value = 2;
                document.querySelector("#email").dataset.user_id = user_id;
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])('imagen', ".image-perfil");
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])('sello', ".image-sello");
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])('firma', ".image-firma");
                telefonoConfig("#telefono_movil");
                $("#user_medico_store").html("Actualizar usuario");
                $("#nacionalidad,#cedula, #email").css("border", 0);
                console.log("iti", iti);
                /* try { */

                _context7.next = 15;
                return _User_js__WEBPACK_IMPORTED_MODULE_3__["default"].show(user_id);

              case 15:
                response = _context7.sent;
                console.log("user->", response);
                emailCurrent = response.email;
                $("#email").val(emailCurrent);
                _context7.next = 21;
                return _UserProfile_js__WEBPACK_IMPORTED_MODULE_4__["default"].show(user_id);

              case 21:
                response = _context7.sent;
                console.log("user_profile->", response);

                if (!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_empty"])(iti)) {
                  iti.setNumber(response.telefono_movil);
                } else {
                  $("#telefono_movil").val(response.telefono_movil);
                }

                if (["/medico/create"].includes(location.pathname) || location.pathname.indexOf('medico/edit')) {
                  document.querySelector(".extension_telefonica").classList.remove("d-none");
                }

                if (response !== undefined) {
                  $("#nacionalidad").val(response.nacionalidad).attr("disabled", true);
                  $("#cedula").val(response.cedula); //.attr("readonly", true)

                  $("#nombres").val(response.nombres);
                  $("#apellidos").val(response.apellidos);
                  $("#fnacimiento").val(response.fnacimiento);
                  $("#extension_telefonica").val(response.extension_telefonica);
                  $("#genero").val(response.genero);
                  $("#image-perfil").attr('src', response.imagen);
                  $("#image-sello").attr('src', "/" + response.sello);
                  $("#image-firma").attr('src', "/" + response.firma);
                  $("#temp_imagen").val(response.imagen);
                  $("#temp_sello").val(response.sello);
                  $("#temp_firma").val(response.firma);
                  $("#prefijo").val(response.prefijo);
                  $("#colegio_medico").val(response.colegio_medico);
                  $("#mpps").val(response.mpps);
                }

                _context7.next = 28;
                return _UserEspecialidad_js__WEBPACK_IMPORTED_MODULE_8__["default"].show(user_id);

              case 28:
                response = _context7.sent;

                if (response !== undefined) {
                  if (response.cat_user_especialidad_id == undefined) {
                    $("#cat_user_especialidad_id").val(1);
                  } else {
                    $("#cat_user_especialidad_id").val(response.cat_user_especialidad_id);
                  }
                }

                _context7.next = 32;
                return _UserEquipoSalud_js__WEBPACK_IMPORTED_MODULE_7__["default"].show(user_id);

              case 32:
                response = _context7.sent;

                if (response !== undefined) {
                  if (response.cat_user_equipo_salud_id == undefined) {
                    $("#cat_user_equipo_salud_id").val(1);
                  } else {
                    $("#cat_user_equipo_salud_id").val(response.cat_user_equipo_salud_id);
                  }
                }

                _context7.next = 36;
                return _UserMedicoPosicion_js__WEBPACK_IMPORTED_MODULE_9__["default"].show(user_id);

              case 36:
                response = _context7.sent;

                if (response !== undefined) {
                  $("#cat_medico_posicion_id").val(response.cat_user_medico_posicion_id);
                }

                $("#user_medico_store").on("click", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee6() {
                  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee6$(_context6) {
                    while (1) {
                      switch (_context6.prev = _context6.next) {
                        case 0:
                          _context6.next = 2;
                          return UserMedico.update(user_id);

                        case 2:
                        case "end":
                          return _context6.stop();
                      }
                    }
                  }, _callee6);
                })));
                $("#cargando_model,#contenido_model").slideToggle();

              case 40:
              case "end":
                return _context7.stop();
            }
          }
        }, _callee7);
      }));

      function edit(_x5, _x6) {
        return _edit.apply(this, arguments);
      }

      return edit;
    }()
  }, {
    key: "create_externo",
    value: function () {
      var _create_externo = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee9(selector) {
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee9$(_context9) {
          while (1) {
            switch (_context9.prev = _context9.next) {
              case 0:
                UserMedico.create_form(selector);
                Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["fotoTemporal"])('imagen', ".image-perfil");
                $("#user_medico_store").on("click", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee8() {
                  var model, _user_id4;

                  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee8$(_context8) {
                    while (1) {
                      switch (_context8.prev = _context8.next) {
                        case 0:
                          if (!_User_js__WEBPACK_IMPORTED_MODULE_3__["default"].validate()) {
                            _context8.next = 31;
                            break;
                          }

                          if (!_UserProfile_js__WEBPACK_IMPORTED_MODULE_4__["default"].validate()) {
                            _context8.next = 31;
                            break;
                          }

                          _context8.prev = 2;
                          $("#cargando_model,#contenido_model").slideToggle();
                          _context8.next = 6;
                          return _User_js__WEBPACK_IMPORTED_MODULE_3__["default"].store();

                        case 6:
                          model = _context8.sent;

                          if (!(Object.keys(model).length > 0)) {
                            _context8.next = 23;
                            break;
                          }

                          _user_id4 = model.id;
                          _context8.next = 11;
                          return _UserType_js__WEBPACK_IMPORTED_MODULE_5__["default"].store2({
                            'user_id': _user_id4,
                            'cat_user_type_id': 2
                          });

                        case 11:
                          _context8.next = 13;
                          return _UserProfile_js__WEBPACK_IMPORTED_MODULE_4__["default"].store(_user_id4);

                        case 13:
                          _context8.next = 15;
                          return _UserEquipoSalud_js__WEBPACK_IMPORTED_MODULE_7__["default"].store(_user_id4);

                        case 15:
                          _context8.next = 17;
                          return _UserEspecialidad_js__WEBPACK_IMPORTED_MODULE_8__["default"].store(_user_id4);

                        case 17:
                          _context8.next = 19;
                          return _UserMedicoPosicion_js__WEBPACK_IMPORTED_MODULE_9__["default"].store(_user_id4);

                        case 19:
                          message = _Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("send_success");
                          Swal.fire({
                            icon: message['image'],
                            title: message['title'],
                            text: message['message'],
                            didClose: function didClose() {
                              setTimeout(function () {
                                return window.location = "/";
                              }, 110);
                            }
                          });
                          _context8.next = 24;
                          break;

                        case 23:
                          throw new Error(model);

                        case 24:
                          _context8.next = 31;
                          break;

                        case 26:
                          _context8.prev = 26;
                          _context8.t0 = _context8["catch"](2);
                          $("#cargando_model,#contenido_model").slideToggle();
                          message = _Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("error");
                          Swal.fire({
                            icon: message['imagen'],
                            title: message['title'],
                            text: message['message'],
                            didClose: function didClose() {
                              setTimeout(function () {
                                return console.log(_context8.t0);
                              }, 110);
                            }
                          });

                        case 31:
                        case "end":
                          return _context8.stop();
                      }
                    }
                  }, _callee8, null, [[2, 26]]);
                })));

              case 3:
              case "end":
                return _context9.stop();
            }
          }
        }, _callee9);
      }));

      function create_externo(_x7) {
        return _create_externo.apply(this, arguments);
      }

      return create_externo;
    }()
  }, {
    key: "show",
    value: function show(selector) {}
  }, {
    key: "update",
    value: function () {
      var _update = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee10(user_id) {
        var model;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee10$(_context10) {
          while (1) {
            switch (_context10.prev = _context10.next) {
              case 0:
                if (!_User_js__WEBPACK_IMPORTED_MODULE_3__["default"].validate()) {
                  _context10.next = 33;
                  break;
                }

                if (!_UserProfile_js__WEBPACK_IMPORTED_MODULE_4__["default"].validate()) {
                  _context10.next = 33;
                  break;
                }

                _context10.prev = 2;
                $("#cargando_model,#contenido_model").slideToggle();
                _context10.next = 6;
                return _UserProfile_js__WEBPACK_IMPORTED_MODULE_4__["default"].update(user_id, iti);

              case 6:
                model = _context10.sent;
                console.log("1->", model);
                _context10.next = 10;
                return _UserEquipoSalud_js__WEBPACK_IMPORTED_MODULE_7__["default"].update(user_id);

              case 10:
                model = _context10.sent;
                console.log("2->", model);
                _context10.next = 14;
                return _UserEspecialidad_js__WEBPACK_IMPORTED_MODULE_8__["default"].update(user_id);

              case 14:
                model = _context10.sent;
                console.log("3->", model);
                _context10.next = 18;
                return _UserMedicoPosicion_js__WEBPACK_IMPORTED_MODULE_9__["default"].update(user_id);

              case 18:
                model = _context10.sent;
                console.log("4->", model);
                _context10.next = 22;
                return _User_js__WEBPACK_IMPORTED_MODULE_3__["default"].update();

              case 22:
                model = _context10.sent;
                console.log("5->", model);
                message = _Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("send_success");
                Swal.fire({
                  icon: message['image'],
                  title: message['title'],
                  text: message['message'],
                  didClose: function didClose() {
                    setTimeout(function () {
                      return window.location = "/medico/index_medicos";
                    }, 110);
                  }
                });
                _context10.next = 33;
                break;

              case 28:
                _context10.prev = 28;
                _context10.t0 = _context10["catch"](2);
                $("#cargando_model,#contenido_model").slideToggle();
                message = _Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("error");
                Swal.fire({
                  icon: message['imagen'],
                  title: message['title'],
                  text: message['message'],
                  didClose: function didClose() {
                    setTimeout(function () {
                      return console.log(_context10.t0);
                    }, 110);
                  }
                });

              case 33:
              case "end":
                return _context10.stop();
            }
          }
        }, _callee10, null, [[2, 28]]);
      }));

      function update(_x8) {
        return _update.apply(this, arguments);
      }

      return update;
    }()
  }, {
    key: "destroy",
    value: function destroy(selector) {}
  }, {
    key: "eliminar",
    value: function eliminar(user_id) {
      var message = _Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('destroy_row_cuestion');
      Swal.fire({
        icon: message['image'],
        title: message['message'],
        showDenyButton: true,
        showCancelButton: false,
        confirmButtonText: "Si"
      }).then(function (result) {
        if (result.isConfirmed) {
          _UserType_js__WEBPACK_IMPORTED_MODULE_5__["default"].update(user_id, 2, 5).then(function (response) {
            $("#row_" + user_id).slideUp("slow");
          });
        } else if (result.isDenied) {//Swal.fire('Changes are not saved', '', 'info')
        }
      });
    }
  }, {
    key: "create_form",
    value: function () {
      var _create_form = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee12(selector) {
        var formdata, cat_user_medico_posicion;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee12$(_context12) {
          while (1) {
            switch (_context12.prev = _context12.next) {
              case 0:
                $(selector).html(
                /*html */
                "\n            <div id=\"cargando_model\" style=\"display:none;\" class=\"container\">\n                <div class=\"row mt-5\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <div class=\"d-flex m-4 justify-content-center text-primary\">\n                            Cargando...\n                            <div class=\"spinner-border\" role=\"status\">\n                                <span class=\"sr-only\"></span>\n                            </div>\n                        </div>\n                    </div>\n                </div>\n            </div>\n            <div id=\"contenido_model\" class=\"container\">\n                <div class=\"row mt-3\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-primary\">\n                        <div class=\"h3\">\n                            Nuevo Usuario\n                        </div>\n                    </div>\n                </div>\n\n                <div class=\"row\">\n                    <div class=\"col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12\">\n                        <div>\n                            <img id=\"image-perfil\" onerror=\"this.src = '/image/system/medic.svg'\" src=\"".concat(location.origin, "/image/user/foto_perfil/medic.svg\" style=\"width: 2cm;height:2cm\" class=\"image-perfil rounded-circle mx-auto d-block\" alt=\"Medic default\">\n                        </div>\n                        <div class=\"form-group\">\n                            <label class=\"label-text text-secondary\" for=\"imagen\">Subir Foto <h6>solo archivos (.jpg o .png)</h6></label>\n                            <input type=\"file\" accept=\"image/png, image/gif, image/jpeg\"\n\n                            class=\"validarArchivoImagen form-control form-control-lg bg-gray-footer-parodi name=\"imagen\" id=\"imagen\" >\n                            <input type=\"hidden\" name=\"temp_imagen\" id=\"temp_imagen\" >\n                            <input type=\"hidden\" name=\"temp_firma\" id=\"temp_firma\" >\n                            <input type=\"hidden\" name=\"temp_sello\" id=\"temp_sello\" >\n                            <input type=\"hidden\" name=\"cat_user_type_id\" id=\"cat_user_type_id\">\n                            <small id=\"helpId1\" class=\"form-text text-muted\"></small>\n                        </div>\n                    </div>\n                </div>\n                <div id=\"user_create\"></div>\n                <div id=\"user_profile\"></div>\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <label class=\"label-text text-secondary\" for=\"cat_especialidad_id\">Miembro del equipo de salud</label>\n                        <select name=\"cat_user_equipo_salud_id\" id=\"cat_user_equipo_salud_id\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                            <option value=\"1\">M\xE9dico(a)</option>\n                            <option value=\"2\">Enfermero(a)</option>\n                            <option value=\"3\">Estudiante de medicina</option>\n                            <option value=\"4\">Estudiante de enfermer\xEDa</option>\n                            <option value=\"5\">Atenci\xF3n al paciente</option>\n                            <option value=\"6\">Instrumentista</option>\n                            <option value=\"7\">C. Anestesia</option>\n                            <option value=\"8\">C. Cirug\xEDa</option>\n                            <option value=\"9\">Hoteler\xEDa</option>\n                            <option value=\"10\">Ingeniero(a)</option>\n                            <option value=\"12\">Admisi\xF3n</option>\n                        </select>\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <label class=\"label-text text-secondary\" for=\"cat_especialidad_id\">Especialidad</label>\n                        <select name=\"cat_user_especialidad_id\" id=\"cat_user_especialidad_id\" class=\"form-control form-control-lg bg-gray-footer-parodi\"></select>\n                    </div>\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <label class=\"label-text text-secondary\" for=\"cat_medico_posicion_id\">Posici\xF3n o cargo</label>\n                        <select name=\"cat_medico_posicion_id\" id=\"cat_medico_posicion_id\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n\n                        </select>\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <label class=\"label-text text-secondary\" for=\"prefijo\">Prefijo</label>\n                        <select name=\"prefijo\" id=\"prefijo\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                            <option aria-label=\"Doctor\" value=\"Dr.\">Dr.</option>\n                            <option aria-label=\"Doctora\" value=\"Dra.\">Dra.</option>\n                            <option aria-label=\"Licenciado\" value=\"Licdo.\">Licdo.</option>\n                            <option aria-label=\"Licenciada\" value=\"Licda.\">Licda.</option>\n                            <option aria-label=\"Bachiller\" value=\"Br.\">Br.</option>\n                            <option aria-label=\"Enfermer\xEDa\" value=\"Enf.\">Enf.</option>\n                            <option aria-label=\"Ingeniero\" value=\"Ing.\">Ing.</option>\n                        </select>\n                    </div>\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <img id=\"image-firma\" onerror=\"this.src = '/image/user/firma/firma_patientcare_default.png'\"  src=\"").concat(location.origin, "/image/user/firma/firma_patientcare_default.png\" style=\"width: 200px;height:110px;\" class=\"mt-3 image-firma rounded mx-auto d-block\" alt=\"Medic default\">\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <img id=\"image-sello\" onerror=\"this.src = '/image/user/sello/sello_patientcare_default.png'\"  src=\"").concat(location.origin, "/image/user/sello/sello_patientcare_default.png\" style=\"width: 200px;height:110px;\" class=\"mt-3 image-sello rounded mx-auto d-block\" alt=\"Medic default\">\n                    </div>\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <label class=\"label-text text-secondary\" for=\"firma\">Subir Firma</label>\n                        <input name=\"firma\" id=\"firma\" type=\"file\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <label class=\"label-text text-secondary\" for=\"sello\">Subir Sello</label>\n                        <input name=\"sello\" id=\"sello\" type=\"file\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </div>\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <label class=\"label-text text-secondary\" for=\"colegio_medicos\">Colegio de m\xE9dicos N\xB0</label>\n                        <input name=\"colegio_medico\" id=\"colegio_medico\" type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <label class=\"label-text text-secondary\" for=\"mpps\">MPPS N\xB0</label>\n                        <input name=\"mpps\" id=\"mpps\" type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\">\n                    </div>\n\n                </div>\n                <div class=\"row\">\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <button onclick=\"history.back()\" class=\"regresar btn btn-lg btn-default bg-secondary mt-3 btn-block\">\n                            Regresar\n                        </button>\n                    </div>\n                    <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                        <button id=\"user_medico_store\" type=\"submit\" class=\"btn btn-lg btn-primary mt-3 btn-block\">Registrar usuario</button>\n                    </div>\n                </div>\n            </div>\n        "));
                document.addEventListener("click", /*#__PURE__*/function () {
                  var _ref5 = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee11(e) {
                    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee11$(_context11) {
                      while (1) {
                        switch (_context11.prev = _context11.next) {
                          case 0:
                            if (e.target.matches(".validarArchivoImagen")) {
                              //that.show(user_id)
                              Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["validarArchivoImagen"])("imagen"); //EditImage(e.target.dataset.user_id, e.target.dataset.urlImg)
                            }

                          case 1:
                          case "end":
                            return _context11.stop();
                        }
                      }
                    }, _callee11);
                  }));

                  return function (_x10) {
                    return _ref5.apply(this, arguments);
                  };
                }());
                formdata = new FormData();
                formdata.append("_token", $("#_token").val());
                _context12.next = 6;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/cat_user_medico_posicion/getAll", formdata);

              case 6:
                cat_user_medico_posicion = _context12.sent;
                cat_user_medico_posicion.forEach(function (row) {
                  $("#cat_medico_posicion_id").append("\n                    <option value=\"".concat(row.id, "\">").concat(row.description, "</option>"));
                });
                _User_js__WEBPACK_IMPORTED_MODULE_3__["default"].create2("#user_create");
                _UserProfile_js__WEBPACK_IMPORTED_MODULE_4__["default"].create("#user_profile");
                _CatUserEspecialidad_js__WEBPACK_IMPORTED_MODULE_6__["default"].getIndex().then(function (response) {
                  $("#cat_user_especialidad_id").html(Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["select"])(response));
                  telefonoConfig("#telefono_movil");
                });
                return _context12.abrupt("return", 0);

              case 12:
              case "end":
                return _context12.stop();
            }
          }
        }, _callee12);
      }));

      function create_form(_x9) {
        return _create_form.apply(this, arguments);
      }

      return create_form;
    }()
  }, {
    key: "deleteRow",
    value: function deleteRow(_ref6) {
      var tipoMedico = _ref6.tipoMedico,
          user_cuest_medico_paciente_id = _ref6.user_cuest_medico_paciente_id,
          user_id = _ref6.user_id,
          color = _ref6.color,
          idTipoMedico = _ref6.idTipoMedico;
      loading("#listado_medicos_".concat(tipoMedico));
      UserCuestMedicoPaciente.eliminar({
        tipoMedico: tipoMedico,
        user_cuest_medico_paciente_id: user_cuest_medico_paciente_id,
        user_id: user_id,
        color: color,
        idTipoMedico: idTipoMedico
      });
    }
  }, {
    key: "getGroup",
    value: function getGroup(response) {
      // UserMedico.getMedicos()
      //.then(response => {
      var especialidad = "";
      var group = "";
      /*
          group +=`<optgroup>`;
          group +=`<option value="">Escoja un médico</option>`;
          group +=`</optgroup>`*/

      var key = false;
      $.each(response, function (indexInArray, valueOfElement) {
        if (especialidad !== valueOfElement.especialidad && !key) {
          especialidad = valueOfElement.especialidad;
          group += "<optgroup class='text-primary' label=\"".concat(especialidad, "\">");
          key = true;
        }

        if (key) {
          group += "<option data-position_id=\"".concat(valueOfElement.posicion_id, "\"  class='text-secondary' value=\"").concat(valueOfElement.user_id, "\">\n                    ").concat(!Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["is_null"])(valueOfElement['prefijo']) ? valueOfElement['prefijo'] + ' ' : '').concat(valueOfElement.medico, "\n                    </option>");
        }

        if (response[indexInArray + 1] !== undefined) {
          if (response[indexInArray + 1].especialidad !== especialidad) {
            group += "</optgroup>";
            key = false;
          }
        }
      }); //$(`#med_${tipoMedico}`).append(group);
      // })

      /*
      $('.mySelect2').select2({
          dropdownParent: $('#modelId'),
          placeholder:"Escoja Médico"
      });*/

      return group;
    }
  }, {
    key: "validate",
    value: function validate() {
      if ($("#nombre").val() == "") {
        alert(_Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('input_text_empty')['message']);
        $("#nombre").trigger("focus");
        return false;
      }

      if ($("#apellido").val() == "") {
        alert(_Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('input_text_empty')['message']);
        $("#apellido").trigger("focus");
        return false;
      }

      if ($("#cedula").val() == "") {
        alert(_Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('input_text_empty')['message']);
        $("#cedula").trigger("focus");
        return false;
      }

      if ($("#fnacimiento").val() == "") {
        alert(_Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('input_text_empty')['message']);
        $("#fnacimiento").trigger("focus");
        return false;
      }

      if ($("#genero").val() == "") {
        alert(_Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('input_select_empty')['message']);
        $("#genero").trigger("focus");
        return false;
      }

      if ($("#telefono_movil").val() == "") {
        alert(_Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('input_text_empty')['message']);
        $("#telefono_movil").trigger("focus");
        return false;
      }

      if ($("#cat_especialidad_user_id").val() == "") {
        alert(_Component_js__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage('input_select_empty')['message']);
        $("#cat_especialidad_user_id").trigger("focus");
        return false;
      }

      return true;
    }
  }, {
    key: "getMedicos",
    value: function getMedicos() {
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/medico/getMedicos");
    }
  }]);

  return UserMedico;
}();



/***/ }),

/***/ "./public/class_js/UserMedicoPosicion.js":
/*!***********************************************!*\
  !*** ./public/class_js/UserMedicoPosicion.js ***!
  \***********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserMedicoPosicion; });
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var UserMedicoPosicion = /*#__PURE__*/function () {
  function UserMedicoPosicion() {
    _classCallCheck(this, UserMedicoPosicion);
  }

  _createClass(UserMedicoPosicion, null, [{
    key: "store",
    value: function store(user_id) {
      var cat_medico_posicion_id = $("#cat_medico_posicion_id");
      var formdata = new FormData();
      formdata.append("user_id_medico", user_id);
      formdata.append("cat_user_medico_posicion_id", cat_medico_posicion_id.val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_medico_posicion/store", formdata);
    }
  }, {
    key: "update",
    value: function update(user_id) {
      var cat_medico_posicion_id = $("#cat_medico_posicion_id");
      var formdata = new FormData();
      formdata.append("user_id_medico", user_id);
      formdata.append("cat_user_medico_posicion_id", cat_medico_posicion_id.val());
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_medico_posicion/update", formdata);
    }
  }, {
    key: "show",
    value: function show(user_id) {
      var formdata = new FormData();
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_0__["post"])(location.origin + "/user_medico_posicion/show/" + user_id, formdata);
    }
  }]);

  return UserMedicoPosicion;
}();



/***/ }),

/***/ "./public/class_js/UserProfile.js":
/*!****************************************!*\
  !*** ./public/class_js/UserProfile.js ***!
  \****************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserProfile; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");
/* harmony import */ var _Component__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Component */ "./public/class_js/Component.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var message;

var UserProfile = /*#__PURE__*/function () {
  function UserProfile() {
    _classCallCheck(this, UserProfile);
  }

  _createClass(UserProfile, [{
    key: "store",
    value: function () {
      var _store = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(user_id, iti) {
        var cedula, nacionalidad, nombres, apellidos, fnacimiento, genero, prefijo, tipo_sangre, colegio_medico, mpps, extension_telefonica, sello, firma, cat_user_type_id, telefono_movil, imagen, formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                cedula = $("#cedula");
                nacionalidad = $("#nacionalidad");
                nombres = $("#nombres");
                apellidos = $("#apellidos");
                fnacimiento = $("#fnacimiento");
                genero = $("#genero");
                prefijo = $("#prefijo");
                tipo_sangre = $("#tipo_sangre");
                colegio_medico = $("#colegio_medico");
                mpps = $("#mpps");
                extension_telefonica = $("#extension_telefonica");
                sello = "image/user/sello/sello_patientcare_default.png";
                firma = "image/user/firma/firma_patientcare_default.png";
                cat_user_type_id = document.querySelector("#cat_user_type_id").value;
                console.log(iti);
                telefono_movil = iti.getNumber(intlTelInputUtils.numberFormat.E164);
                imagen = document.getElementById("imagen").files[0];
                formdata = new FormData();
                formdata.append("nombres", nombres.val());
                formdata.append("apellidos", apellidos.val());
                formdata.append("cedula", cedula.val());
                formdata.append("extension_telefonica", extension_telefonica.val());
                formdata.append("nacionalidad", nacionalidad.val());
                formdata.append("mpps", mpps.val());
                formdata.append("fnacimiento", fnacimiento.val());
                formdata.append("genero", genero.val());
                formdata.append("prefijo", prefijo.val());
                formdata.append("telefono_movil", telefono_movil);
                formdata.append("imagen", imagen); //console.log( document.getElementById(`sello`) )

                if (cat_user_type_id == 2) {
                  sello = document.getElementById("sello").files[0];
                  formdata.append("sello", sello);
                }

                if (cat_user_type_id == 2) {
                  firma = document.getElementById("firma").files[0];
                  formdata.append("firma", firma);
                }
                /* formdata.append("sello", sello)
                formdata.append("firma", firma) */


                formdata.append("colegio_medico", colegio_medico.val());
                formdata.append("user_id", user_id);
                formdata.append("tipo_sangre", tipo_sangre.val());
                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                formdata.append("_token", $("#_token").val());
                _context.next = 38;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_profile/store", formdata);

              case 38:
                return _context.abrupt("return", _context.sent);

              case 39:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function store(_x, _x2) {
        return _store.apply(this, arguments);
      }

      return store;
    }()
  }, {
    key: "getCedula",
    value: function getCedula(cedula) {
      var formdata = new FormData();
      formdata.append("user_id", cedula);
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_profile/getCedula", formdata);
    }
  }, {
    key: "validateCedula",
    value: function () {
      var _validateCedula = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2() {
        var nacionalidad, cedula, formdata, result;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
          while (1) {
            switch (_context2.prev = _context2.next) {
              case 0:
                nacionalidad = $("#nacionalidad");
                cedula = $("#cedula");
                formdata = new FormData();
                formdata.append("nacionalidad", nacionalidad.val());
                formdata.append("cedula", cedula.val());
                formdata.append("_token", $("#_token").val()); //CatUserEspecialidad.jsreturn await post( location.origin+"/user_profile/cedulaExiste", formdata)

                _context2.next = 8;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["get"])(location.origin + "/getPatient/" + cedula.val());

              case 8:
                result = _context2.sent;
                console.log("cedula_encontrada->", result);
                return _context2.abrupt("return", result);

              case 11:
              case "end":
                return _context2.stop();
            }
          }
        }, _callee2);
      }));

      function validateCedula() {
        return _validateCedula.apply(this, arguments);
      }

      return validateCedula;
    }()
  }], [{
    key: "update",
    value: function () {
      var _update = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3(user_id, iti) {
        var nombres, apellidos, fnacimiento, genero, prefijo, tipo_sangre, colegio_medico, mpps, cedula, nacionalidad, telefono_movil, extension_telefonica, temp_imagen, temp_firma, temp_sello, imagen, sello, firma, formdata, cat_user_type_id;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
          while (1) {
            switch (_context3.prev = _context3.next) {
              case 0:
                nombres = $("#nombres");
                apellidos = $("#apellidos");
                fnacimiento = $("#fnacimiento");
                genero = $("#genero");
                prefijo = $("#prefijo");
                tipo_sangre = $("#tipo_sangre");
                colegio_medico = $("#colegio_medico");
                mpps = $("#mpps");
                cedula = $("#cedula");
                nacionalidad = $("#nacionalidad");
                telefono_movil = iti.getNumber(intlTelInputUtils.numberFormat.E164);
                extension_telefonica = $("#extension_telefonica");
                temp_imagen = $("#temp_imagen");
                temp_firma = $("#temp_firma");
                temp_sello = $("#temp_sello");
                imagen = document.getElementById("imagen").files[0];
                sello = "image/user/sello/sello_patientcare_default.png";
                firma = "image/user/firma/firma_patientcare_default.png";
                formdata = new FormData();
                formdata.append("nombres", nombres.val());
                formdata.append("apellidos", apellidos.val());
                formdata.append("cedula", cedula.val());
                formdata.append("nacionalidad", nacionalidad.val());
                formdata.append("extension_telefonica", extension_telefonica.val());
                formdata.append("fnacimiento", fnacimiento.val());
                formdata.append("genero", genero.val());
                formdata.append("mpps", mpps.val());
                formdata.append("telefono_movil", telefono_movil);
                formdata.append("prefijo", prefijo.val());
                formdata.append("imagen", imagen);
                cat_user_type_id = document.querySelector("#cat_user_type_id").value; //cl(cat_user_type_id)

                if (cat_user_type_id == 2) {
                  sello = document.getElementById("sello").files[0];
                  formdata.append("sello", sello);
                }

                if (cat_user_type_id == 2) {
                  firma = document.getElementById("firma").files[0];
                  formdata.append("firma", firma); //console.log(firma)
                }

                formdata.append("colegio_medico", colegio_medico.val());
                formdata.append("temp_imagen", temp_imagen.val());
                formdata.append("temp_firma", temp_firma.val());
                formdata.append("temp_sello", temp_sello.val());
                formdata.append("tipo_sangre", tipo_sangre.val());
                formdata.append("user_id", user_id);
                formdata.append("updated_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                formdata.append("_token", $("#_token").val());
                _context3.next = 43;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_profile/update", formdata);

              case 43:
                return _context3.abrupt("return", _context3.sent);

              case 44:
              case "end":
                return _context3.stop();
            }
          }
        }, _callee3);
      }));

      function update(_x3, _x4) {
        return _update.apply(this, arguments);
      }

      return update;
    }()
  }, {
    key: "validate",
    value: function validate() {
      var nacionalidad = $("#nacionalidad");
      var cedula = $("#cedula");
      var nombres = $("#nombres");
      var apellidos = $("#apellidos");
      var fnacimiento = $("#fnacimiento");
      var genero = $("#genero");
      var telefono_movil = $("#telefono_movil");

      if (nacionalidad.val() == "") {
        message = _Component__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("input_select_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              nacionalidad.trigger("focus");
            }, 110);
          }
        }); //alert(message['message'])
        //nacionalidad.trigger("focus")

        return false;
      }

      if (cedula.val() == "") {
        message = _Component__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("input_text_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              cedula.trigger("focus");
            }, 110);
          }
        }); //alert(message['message'])
        //cedula.trigger("focus")

        return false;
      }

      if (nombres.val() == "") {
        message = _Component__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("input_text_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              nombres.trigger("focus");
            }, 110);
          }
        }); //alert(message['message'])
        //nombres.trigger("focus")

        return false;
      }

      if (apellidos.val() == "") {
        message = _Component__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("input_text_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              apellidos.trigger("focus");
            }, 110);
          }
        }); //alert(message['message'])
        //apellidos.trigger("focus")

        return false;
      }

      if (genero.val() == "") {
        message = _Component__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("input_select_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              genero.trigger("focus");
            }, 110);
          }
        }); //alert(message['message'])
        //genero.trigger("focus")

        return false;
      }

      if (fnacimiento.val() == "") {
        message = _Component__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("input_text_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              fnacimiento.trigger("focus");
            }, 110);
          }
        }); //alert(message['message'])
        //fnacimiento.trigger("focus")

        return false;
      }

      if (telefono_movil.val() == "") {
        message = _Component__WEBPACK_IMPORTED_MODULE_2__["default"].alertMessage("input_text_empty");
        Swal.fire({
          icon: message['image'],
          title: message['title'],
          text: message['message'],
          didClose: function didClose() {
            setTimeout(function () {
              telefono_movil.trigger("focus");
            }, 110);
          }
        }); //alert(message['message'])
        //telefono_movil.trigger("focus")

        return false;
      }

      return true;
    }
  }, {
    key: "create",
    value: function create(selector) {
      //cosas por arreglar:
      //onkeyup="validarPrimerDigito('telefono_movil')"
      $(selector).html("\n            <div class=\"row\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"nombre\">Nombres</label>\n                        <input type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"nombres\" id=\"nombres\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"apellido\">Apellidos</label>\n                        <input  type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"apellidos\" id=\"apellidos\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n            </div>\n            <div class=\"row\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"genero\">G\xE9nero</label>\n                        <select  class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"genero\" id=\"genero\" aria-describedby=\"helpId\" placeholder=\"\">\n                            <option value=\"\">Seleccione</option>\n                            <option value=\"m\">Masculino</option>\n                            <option value=\"f\">Femenino</option>\n                        </select>\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"fnacimiento\">Fecha de nacimiento</label>\n                        <input type=\"date\"  class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"fnacimiento\" id=\"fnacimiento\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n            </div>\n            <div class=\"row\">\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group\">\n                        <label class=\"label-text text-secondary\" for=\"telefono_movil\">Tel\xE9fono Contacto</label>\n                        <input type=\"tel\"  class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"telefono_movil\" id=\"telefono_movil\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"help_telefonomovil\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group extension_telefonica d-none\">\n                        <label class=\"label-text text-secondary\" for=\"extension_telefonica\">Aster\xEDsco</label>\n                        <input type=\"text\" class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"extension_telefonica\" id=\"extension_telefonica\" aria-describedby=\"helpId\" placeholder=\"\">\n                        <small id=\"help_extension_telefonica\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n                <div class=\"col-xs-6 col-sm-6 col-md-6 col-lg-6 col-xl-6\">\n                    <div class=\"form-group d-none\">\n                        <label class=\"label-text text-secondary\" for=\"tipo_sangre\">Grupo Sangu\xEDneo</label>\n                        <select  class=\"form-control form-control-lg bg-gray-footer-parodi\" name=\"tipo_sangre\" id=\"tipo_sangre\" aria-describedby=\"helpId\" placeholder=\"\">\n                            <option value=\"Lo desconozco\">Lo desconozco</option>\n                            <option value=\"O+\">O+</option>\n                            <option value=\"O-\">O-</option>\n                            <option value=\"A+\">A+</option>\n                            <option value=\"A-\">A-</option>\n                            <option value=\"B+\">B+</option>\n                            <option value=\"B-\">B-</option>\n                            <option value=\"AB+\">AB+</option>\n                            <option value=\"AB-\">AB-</option>\n                        </select>\n                        <small id=\"helpId1\" class=\"form-text text-muted notification\"></small>\n                    </div>\n                </div>\n            </div>\n        ");
    }
  }, {
    key: "show",
    value: function () {
      var _show = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee4(user_id) {
        var formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee4$(_context4) {
          while (1) {
            switch (_context4.prev = _context4.next) {
              case 0:
                formdata = new FormData();
                formdata.append("user_id", user_id);
                formdata.append("_token", $("#_token").val());
                _context4.next = 5;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_profile/show", formdata);

              case 5:
                return _context4.abrupt("return", _context4.sent);

              case 6:
              case "end":
                return _context4.stop();
            }
          }
        }, _callee4);
      }));

      function show(_x5) {
        return _show.apply(this, arguments);
      }

      return show;
    }()
  }]);

  return UserProfile;
}();



/***/ }),

/***/ "./public/class_js/UserType.js":
/*!*************************************!*\
  !*** ./public/class_js/UserType.js ***!
  \*************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return UserType; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }



var UserType = /*#__PURE__*/function () {
  function UserType() {
    _classCallCheck(this, UserType);
  }

  _createClass(UserType, [{
    key: "store",
    value: function () {
      var _store = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(user_id) {
        var formdata;
        return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
          while (1) {
            switch (_context.prev = _context.next) {
              case 0:
                formdata = new FormData();
                formdata.append("cat_user_type_id", 1);
                formdata.append("user_id", user_id);
                formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
                formdata.append("_token", $("#_token").val());
                _context.next = 7;
                return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_type/store", formdata);

              case 7:
                return _context.abrupt("return", _context.sent);

              case 8:
              case "end":
                return _context.stop();
            }
          }
        }, _callee);
      }));

      function store(_x) {
        return _store.apply(this, arguments);
      }

      return store;
    }()
  }, {
    key: "store2",
    value: function store2(data) {
      var formdata = new FormData();
      formdata.append("cat_user_type_id", data.cat_user_type_id);
      formdata.append("user_id", data.user_id);
      formdata.append("created_at", Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["timestamps"])());
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_type/store", formdata);
    }
  }, {
    key: "update",
    value: function update(user_id, cat_user_type_id, new_cat_user_type_id) {
      var formdata = new FormData();
      formdata.append("user_id", user_id);
      formdata.append("cat_user_type_id", cat_user_type_id);
      formdata.append("new_cat_user_type_id", new_cat_user_type_id);
      formdata.append("_token", $("#_token").val());
      return Object(_helpers_helpers_js__WEBPACK_IMPORTED_MODULE_1__["post"])(location.origin + "/user_type/update", formdata);
    }
  }]);

  return UserType;
}();



/***/ }),

/***/ "./public/class_js/app_registro_paciente_interno.js":
/*!**********************************************************!*\
  !*** ./public/class_js/app_registro_paciente_interno.js ***!
  \**********************************************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./UserCuestPaciente.js */ "./public/class_js/UserCuestPaciente.js");
/* harmony import */ var _UserMedico_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./UserMedico.js */ "./public/class_js/UserMedico.js");
/* harmony import */ var _helpers_helpers_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../helpers/helpers.js */ "./public/helpers/helpers.js");


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }



 //INSTANCIAS DE CLASE

var userCuestPaciente = new _UserCuestPaciente_js__WEBPACK_IMPORTED_MODULE_1__["default"](); //let component = new UserMedico()

/************* */

d.addEventListener("DOMContentLoaded", /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee() {
  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
    while (1) {
      switch (_context.prev = _context.next) {
        case 0:
          //userCuestPaciente.create2("#content_1")
          _UserMedico_js__WEBPACK_IMPORTED_MODULE_2__["default"].getMedicosByCargo();

        case 1:
        case "end":
          return _context.stop();
      }
    }
  }, _callee);
})));
/************* */

/***/ }),

/***/ "./public/helpers/helpers.js":
/*!***********************************!*\
  !*** ./public/helpers/helpers.js ***!
  \***********************************/
/*! exports provided: loading, validarArchivoImagen, getFileExtension2, reemplaza_imagen, fechaCortaCustom1, formatAMPM, post, validarNumeroConDosDecimales, telefonoConfig, fotoTemporal, horaAMPM, clone, validarPrimerDigito, imagePreview, is_null, is_empty, equalsInt, getFileExtension1, getRouteParams, timestamps, timestamps2, activarTooltip, is_undefined, first, last, byId, calcularEdad, fechaAMD, fechaAMD2, $qs, $qsa, select, meses, fechaCortaCustom2, fechaDMA, get, ucwords, alertMessage, emptyItem, jsUcfirst, jsUclast, fileType, removeAccents, $select, $selectCustom, recalcularAltoPagina, store_field, selectOptions, dia_semana, fechaUserCita, mes, item_historial_cita, item_historial_cita2, active_form, cargandoModal, get_index, get_one, get_all, get_one_by_index, add_row, delete_row, update_row, add_row_by_index */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "loading", function() { return loading; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "validarArchivoImagen", function() { return validarArchivoImagen; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getFileExtension2", function() { return getFileExtension2; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "reemplaza_imagen", function() { return reemplaza_imagen; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "fechaCortaCustom1", function() { return fechaCortaCustom1; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "formatAMPM", function() { return formatAMPM; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "post", function() { return post; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "validarNumeroConDosDecimales", function() { return validarNumeroConDosDecimales; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "telefonoConfig", function() { return telefonoConfig; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "fotoTemporal", function() { return fotoTemporal; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "horaAMPM", function() { return horaAMPM; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "clone", function() { return clone; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "validarPrimerDigito", function() { return validarPrimerDigito; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "imagePreview", function() { return imagePreview; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "is_null", function() { return is_null; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "is_empty", function() { return is_empty; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "equalsInt", function() { return equalsInt; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getFileExtension1", function() { return getFileExtension1; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getRouteParams", function() { return getRouteParams; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "timestamps", function() { return timestamps; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "timestamps2", function() { return timestamps2; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "activarTooltip", function() { return activarTooltip; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "is_undefined", function() { return is_undefined; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "first", function() { return first; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "last", function() { return last; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "byId", function() { return byId; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "calcularEdad", function() { return calcularEdad; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "fechaAMD", function() { return fechaAMD; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "fechaAMD2", function() { return fechaAMD2; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "$qs", function() { return $qs; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "$qsa", function() { return $qsa; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "select", function() { return select; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "meses", function() { return meses; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "fechaCortaCustom2", function() { return fechaCortaCustom2; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "fechaDMA", function() { return fechaDMA; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "get", function() { return get; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ucwords", function() { return ucwords; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "alertMessage", function() { return alertMessage; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "emptyItem", function() { return emptyItem; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "jsUcfirst", function() { return jsUcfirst; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "jsUclast", function() { return jsUclast; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "fileType", function() { return fileType; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "removeAccents", function() { return removeAccents; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "$select", function() { return $select; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "$selectCustom", function() { return $selectCustom; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "recalcularAltoPagina", function() { return recalcularAltoPagina; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "store_field", function() { return store_field; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "selectOptions", function() { return selectOptions; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "dia_semana", function() { return dia_semana; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "fechaUserCita", function() { return fechaUserCita; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "mes", function() { return mes; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "item_historial_cita", function() { return item_historial_cita; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "item_historial_cita2", function() { return item_historial_cita2; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "active_form", function() { return active_form; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "cargandoModal", function() { return cargandoModal; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "get_index", function() { return get_index; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "get_one", function() { return get_one; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "get_all", function() { return get_all; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "get_one_by_index", function() { return get_one_by_index; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "add_row", function() { return add_row; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "delete_row", function() { return delete_row; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "update_row", function() { return update_row; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "add_row_by_index", function() { return add_row_by_index; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "./node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);


function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

var d = document;
var loading = function loading() {
  return "\n            <div class=\"scale-up-hor-left my-1 d-flex justify-content-center\">\n                <div class=\"spinner-border text-primary\" role=\"status\">\n                    <span class=\"sr-only\">Loading...</span>\n                </div>\n            </div>\n        ";
};
var validarArchivoImagen = function validarArchivoImagen(id) {
  var archivoInput = document.getElementById(id);
  var archivo = archivoInput.files[0];

  if (archivo) {
    var extension = archivo.name.split('.').pop().toLowerCase();

    if (!(extension === 'jpg' || extension === 'jpeg' || extension === 'png')) {
      Swal.fire({
        icon: "error",
        title: "Hubo un error...",
        text: "Solo se permiten archivos .jpg o .png."
      });
      archivoInput.value = ''; // Limpiar el campo de entrada
    }
  }
};
var getFileExtension2 = function getFileExtension2(filename) {
  return filename.substring(filename.lastIndexOf('.') + 1, filename.length) || filename;
};
var reemplaza_imagen = function reemplaza_imagen(imagen) {
  imagen.onerror = "";
  imagen.src = "/image/system/medic.svg";
  return true;
};
var fechaCortaCustom1 = function fechaCortaCustom1(param) {
  var hoy = new Date(param);
  return hoy.getDate() + " " + meses(hoy.getMonth()).slice(0, 3).toUpperCase() + ", " + hoy.getFullYear();
};
var formatAMPM = function formatAMPM(date) {
  var hours = date.getHours();
  var minutes = date.getMinutes();
  var seconds = date.getSeconds();
  var ampm = hours >= 12 ? 'pm' : 'am';
  hours = hours % 12;
  hours = hours ? hours : 12; // the hour '0' should be '12'

  minutes = minutes < 10 ? '0' + minutes : minutes;
  var strTime = hours + ':' + minutes + ':' + seconds + ' ' + ampm;
  return strTime;
};
function post(_x, _x2) {
  return _post.apply(this, arguments);
}

function _post() {
  _post = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee2(url, data) {
    var response;
    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee2$(_context2) {
      while (1) {
        switch (_context2.prev = _context2.next) {
          case 0:
            _context2.prev = 0;
            _context2.next = 3;
            return fetch(url, {
              method: 'POST',
              body: data
            });

          case 3:
            response = _context2.sent;
            _context2.next = 6;
            return response.json();

          case 6:
            return _context2.abrupt("return", _context2.sent);

          case 9:
            _context2.prev = 9;
            _context2.t0 = _context2["catch"](0);
            console.error(_context2.t0.message);

          case 12:
          case "end":
            return _context2.stop();
        }
      }
    }, _callee2, null, [[0, 9]]);
  }));
  return _post.apply(this, arguments);
}

var validarNumeroConDosDecimales = function validarNumeroConDosDecimales(inputValue) {
  var regex = /^\d+\.\d{2}$/;
  return regex.test(inputValue);
};
var telefonoConfig = function telefonoConfig(selectorInput, iti, customhandleChange) {
  var input = document.querySelector(selectorInput);
  iti = window.intlTelInput(input, {
    autoHideDialCode: true,
    nationalMode: false,
    preferredCountries: ['ve'],
    separateDialCode: true,
    utilsScript: "/plugin/intl-tel-input/build/js/utils.js"
  });
  /*
  let handleChange = function() {
  customhandleChange(iti.getNumber(),iti.isValidNumber())
  };
  input.addEventListener('change', handleChange);
  input.addEventListener('keyup', handleChange);*/
};
var fotoTemporal = function fotoTemporal(image_id, selector) {
  document.getElementById(image_id).onchange = function (e) {
    // Creamos el objeto de la clase FileReader
    var reader = new FileReader(); // Leemos el archivo subido y se lo pasamos a nuestro fileReader

    reader.readAsDataURL(e.target.files[0]); // Le decimos que cuando este listo ejecute el código interno

    reader.onload = function () {
      $(selector).attr("src", reader.result);
      /*
      let preview = document.getElementById('preview'),
              image = document.createElement('img');
        image.src = reader.result;
        preview.innerHTML = '';
      preview.append(image);*/
    };
  };
};
var horaAMPM = function horaAMPM(param) {
  var m = "AM";
  var p = param.split(":");
  var hora = p[0];

  if (parseInt(p[0]) > 12) {
    m = "PM";

    switch (p[0]) {
      case "13":
        hora = "1";
        break;

      case "14":
        hora = "2";
        break;

      case "15":
        hora = "3";
        break;

      case "16":
        hora = "4";
        break;

      case "17":
        hora = "5";
        break;

      case "18":
        hora = "6";
        break;

      case "19":
        hora = "7";
        break;

      case "20":
        hora = "8";
        break;

      case "21":
        hora = "9";
        break;

      case "22":
        hora = "10";
        break;

      case "23":
        hora = "11";
        break;

      case "00":
        hora = "12";
        break;
    }
  }

  return "".concat(hora.toString().padStart(2, '0'), ":").concat(p[1].toString().padStart(2, '0'), " ").concat(m);
};
var clone = function clone(selector) {
  return document.getElementById(selector).content.cloneNode(true);
};
var validarPrimerDigito = function validarPrimerDigito(value) {
  //console.log($("#"+value).val().length)
  if ($("#" + value).val().length == 1) {
    if ($("#" + value).val() == 0) {
      $("#" + value).val('');
    }
  }
};
var imagePreview = function imagePreview(e, selector) {
  var showImageName = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  // We create the object of the FileReader class
  var reader = new FileReader(); // We read the uploaded file and pass it to our fileReader.

  reader.readAsDataURL(e.target.files[0]);
  var file = e.target.files[0]; //We look for the following element where we will show the name of the uploaded file.

  var name = file.name,
      size = file.size,
      type = file.type;

  if (showImageName) {
    e.target.nextElementSibling.textContent = name;
  } // We tell it that when it is ready, run the internal code.


  reader.onload = function (load) {
    document.getElementById(selector).setAttribute("src", reader.result);
  };
};
var is_null = function is_null(params) {
  return params === null || params === "null" ? true : false;
};
var is_empty = function is_empty(params) {
  return params === "" ? true : false;
};
var equalsInt = function equalsInt(param1, param2) {
  return parseInt(param1) === parseInt(param2) ? true : false;
};
var getFileExtension1 = function getFileExtension1(filename) {
  return /[.]/.exec(filename) ? /[^.]+$/.exec(filename) : undefined;
};
var getRouteParams = function getRouteParams() {
  // Crear un objeto URLSearchParams a partir de la parte de consulta de la URL
  var params = new URLSearchParams(window.location.search); // Crear un objeto vacío para almacenar los parámetros

  var queryParams = {}; // Iterar sobre los parámetros y agregarlos al objeto

  params.forEach(function (value, key) {
    queryParams[key] = value;
  }); // Imprimir los parámetros obtenidos

  console.log('Parámetros de consulta:', queryParams); // Puedes acceder a un parámetro específico usando su clave
  // Por ejemplo, si tienes un parámetro 'nombre' en la URL, puedes obtener su valor así:

  return queryParams;
};
var timestamps = function timestamps() {
  var hoy = new Date();
  return hoy.getFullYear() + "-" + (hoy.getMonth() + 1) + "-" + hoy.getDate() + " " + hoy.getHours() + ':' + hoy.getMinutes() + ':' + hoy.getSeconds();
};
var timestamps2 = function timestamps2(date) {
  var hoy = new Date(date);
  return hoy.getFullYear() + "-" + (hoy.getMonth() + 1) + "-" + hoy.getDate() + " " + hoy.getHours() + ':' + hoy.getMinutes() + ':' + hoy.getSeconds();
};
var activarTooltip = function activarTooltip() {
  var array = ['primary', 'danger', 'success', 'info', 'warning', 'secondary', 'cyan', 'gray', 'purple', 'orange', 'pink'];

  for (var index = 0; index <= array.length; index++) {
    var element = array[index];
    tippy('.tooltip-' + element, {
      allowHTML: true,
      theme: 'tooltip-cmdlt-' + element,
      zIndex: 200000
    });
  }
};
var is_undefined = function is_undefined(params) {
  return params === undefined || params === "undefined" ? true : false;
};
var first = function first(arr) {
  return arr.length > 0 ? arr[0] : null;
};
var last = function last(arr) {
  return arr.length > 0 ? arr[arr.length - 1] : null;
};
var byId = function byId(selector) {
  return document.getElementById(selector);
};
var calcularEdad = function calcularEdad(fnacimiento) {
  var fechaNacimiento = new Date(fnacimiento);
  var fechaActual = new Date();
  var diferencia = fechaActual - fechaNacimiento;
  var edadEnMilisegundos = new Date(diferencia); // Extraer el año de la fecha de nacimiento

  var edad = Math.abs(edadEnMilisegundos.getUTCFullYear() - 1970); //console.log(`Edad --> ${edad} años`);
  // alert(edad)

  return String(edad) === "NaN" ? 0 : edad;
};
var fechaAMD = function fechaAMD() {
  var hoy = new Date(); //console.log(hoy)

  return hoy.getFullYear() + "-" + String(hoy.getMonth() + 1).padStart(2, '0') + "-" + String(hoy.getDate()).padStart(2, '0');
};
var fechaAMD2 = function fechaAMD2(date) {
  var hoy = new Date(date); //console.log(hoy)

  return hoy.getFullYear() + "-" + String(hoy.getMonth() + 1).padStart(2, '0') + "-" + String(hoy.getDate()).padStart(2, '0');
};
var $qs = function $qs(selector) {
  return d.querySelector(selector);
};
var $qsa = function $qsa(selector) {
  return d.querySelectorAll(selector);
};
var select = function select(model, param) {
  var id = param != undefined ? param : "";
  var selected = '';
  var data = "<option value=''>Seleccione</option>";
  $.each(model, function (indexInArray, valueOfElement) {
    if (valueOfElement.id == id) {
      //console.log(valueOfElement.id+"=="+id)
      selected = 'selected';
    } else {
      selected = '';
    }

    data += "\n                <option ".concat(selected, " value=\"").concat(valueOfElement.id, "\">\n                    ").concat(valueOfElement.description, "\n                </option>\n            ");
  });
  return data;
};
var meses = function meses(p) {
  var mes = ["Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio", "Julio", "Agosto", "Septiembre", "Octubre", "Noviembre", "Diciembre", "Enero"];
  return mes[p];
};
var fechaCortaCustom2 = function fechaCortaCustom2(param) {
  var hoy = new Date(param);
  return hoy.getDate() + " " + meses(hoy.getMonth()).slice(0, 3).toUpperCase() + ", " + hoy.getFullYear() + " <span class=\"text-primary\">|</span> " + formatAMPM(hoy);
};
var fechaDMA = function fechaDMA(param) {
  var divider = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "/";
  var hoy = new Date(param); //console.log(hoy)

  return hoy.getDate() + divider + (hoy.getMonth() + 1) + divider + hoy.getFullYear();
};
function get(_x3) {
  return _get.apply(this, arguments);
}

function _get() {
  _get = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee3(url) {
    var response;
    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee3$(_context3) {
      while (1) {
        switch (_context3.prev = _context3.next) {
          case 0:
            _context3.prev = 0;
            _context3.next = 3;
            return fetch(url, {
              method: 'GET',
              headers: {
                'Content-Type': 'application/json'
              }
            });

          case 3:
            response = _context3.sent;
            _context3.next = 6;
            return response.json();

          case 6:
            return _context3.abrupt("return", _context3.sent);

          case 9:
            _context3.prev = 9;
            _context3.t0 = _context3["catch"](0);
            console.error(_context3.t0.message);

          case 12:
          case "end":
            return _context3.stop();
        }
      }
    }, _callee3, null, [[0, 9]]);
  }));
  return _get.apply(this, arguments);
}

function ucwords(string) {
  return string.charAt(0).toUpperCase();
}
function alertMessage(caso) {
  switch (caso) {
    case 'expire_sesion':
      return {
        'title': 'Atención',
        'message': 'Su sesión ha finalizado por inactividad.',
        'image': 'success'
      };
      break;

    case 'input_text_empty':
      return {
        'title': 'Atención',
        'message': 'El campo no puede estar vacio.',
        'image': 'warning'
      };
      break;

    case 'input_select_empty':
      return {
        'title': 'Atención',
        'message': 'Debe seleccionar una opción válida.',
        'image': 'warning'
      };
      break;

    case 'input_datetime_empty':
      return {
        'title': 'Atención',
        'message': 'Debe seleccionar una fecha y hora válida.',
        'image': 'warning'
      };
      break;

    case 'input_date_empty':
      return {
        'title': 'Atención',
        'message': 'Debe seleccionar una fecha válida.',
        'image': 'warning'
      };
      break;

    case 'input_checkbox_empty':
      return {
        'title': 'Atención',
        'message': 'Debe seleccionar al menos una opción.',
        'image': 'warning'
      };
      break;

    case 'destroy_row_cuestion':
      return {
        'title': 'Atención',
        'message': '¿Seguro que desea eliminar este registro?',
        'image': 'warning'
      };
      break;

    case 'update_row_cuestion':
      return {
        'title': 'Atención',
        'message': '¿Seguro que desea actualizar este registro?',
        'image': 'warning'
      };
      break;

    case 'user_no_valid':
      return {
        'title': 'Atención',
        'message': 'Ingrese un Documento de identidad ó un Correo Electrónico para continuar',
        'image': 'warning'
      };
      break;

    case 'close_cie11':
      return {
        'title': 'Atención',
        'message': '¿Desea cerrar el clasificador CIE11?',
        'image': 'warning'
      };
      break;

    case 'cedula_registrado':
      return {
        'title': 'Atención',
        'message': 'El Documento de identidad ya se encuentra asociado a un paciente. Ingrese uno distinto.',
        'image': 'warning'
      };
      break;

    case 'email_registrado':
      return {
        'title': 'Atención',
        'message': 'El Correo Electrónico ya se encuentra asociado a un paciente. Ingrese uno distinto.',
        'image': 'warning'
      };
      break;

    case 'send_success':
      return {
        'title': 'Completado',
        'message': 'Registro creado exitosamente',
        'image': 'success'
      };
      break;

    case 'error':
      return {
        'title': 'Error',
        'message': 'Ha ocurrido un error',
        'image': 'error'
      };
      break;

    case 'update_success':
      return {
        'title': 'Completado',
        'message': 'Registro actualizado exitosamente',
        'image': 'success'
      };
      break;

    case 'cedula_asignada':
      return {
        'title': 'Atención',
        'message': ' ya se encuentra asignado a un paciente, ¿desea reingresarlo?.',
        'image': 'warning'
      };
      break;

    case 'cedula_asignada2':
      return {
        'title': 'Atención',
        'message': ' ya se encuentra asignada a un paciente registrado.',
        'image': 'warning'
      };
      break;

    default:
      console.log("error_menssage");
      break;
  }
}
var emptyItem = function emptyItem() {
  var message = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'Sin Registros';
  var positions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  return "\n                <tr>\n                    <td colspan=\"".concat(positions, "\"class=\"text-center text-primary\">\n                        ").concat(message, "\n                    </td>\n                </tr>\n            ");
};
var jsUcfirst = function jsUcfirst(string) {
  return string.charAt(0).toUpperCase() + string.slice(1);
};
var jsUclast = function jsUclast(string) {
  return string.substring(0, string.length - 1);
};
var fileType = function fileType($html, file_type) {
  if (["png", "jpg", "jpeg", "bmp"].includes(file_type)) {
    $html.classList.add("fas", "fa-file-image", "text-gray");
  }

  if (["mp3", "ogg", "wav"].includes(file_type)) {
    $html.classList.add("fas", "fa-file-audio", "text-gray");
  }

  if (["doc", "docx", "txt"].includes(file_type)) {
    $html.classList.add("fas", "fa-file-word", "text-gray");
  }

  if (["ppt", "pptx"].includes(file_type)) {
    $html.classList.add("fas", "fa-file-powerpoint", "text-gray");
  }

  if (["xls", "xlsx"].includes(file_type)) {
    $html.classList.add("fas", "fa-file-excel", "text-gray");
  }

  if (["pdf"].includes(file_type)) {
    $html.classList.add("fas", "fa-file-pdf", "text-gray");
  }

  if (["gif"].includes(file_type)) {
    $html.classList.add("fas", "fa-film", "text-gray");
  }

  if (!["gif", "gif", "pdf", "xls", "xlsx", "ppt", "pptx", "doc", "docx", "txt", "png", "jpg", "jpeg", "bmp", "mp3", "ogg", "wav"].includes(file_type)) {
    $html.classList.add("fas", "fa-question-circle", "text-danger");
  }
};
var removeAccents = function removeAccents(cadena) {
  var acentos = {
    'á': 'a',
    'é': 'e',
    'í': 'i',
    'ó': 'o',
    'ú': 'u',
    'Á': 'A',
    'É': 'E',
    'Í': 'I',
    'Ó': 'O',
    'Ú': 'U'
  };
  return cadena.split('').map(function (letra) {
    return acentos[letra] || letra;
  }).join('').toString();
};
var $select = function $select(data) {
  var selectoption = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  var $fragment = document.createDocumentFragment();
  var $option;

  if (selectoption) {
    $option = document.createElement("option");
    $option.value = "";
    $option.textContent = "Seleccione";
    $fragment.appendChild($option);
  }

  data.forEach(function (x) {
    $option = document.createElement("option");
    $option.value = x.id;
    $option.textContent = x.description;
    $fragment.appendChild($option);
  });
  return $fragment;
};
var $selectCustom = function $selectCustom(data) {
  var attr = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
  var selectoption = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
  var $fragment = document.createDocumentFragment();
  var $option;

  if (selectoption) {
    $option = document.createElement("option");
    $option.value = "";
    $option.textContent = "Seleccione";
    $fragment.appendChild($option);
  }

  data.forEach(function (x) {
    $option = document.createElement("option");
    $option.value = x[attr[0]];
    $option.textContent = x[attr[1]];
    $fragment.appendChild($option);
  });
  return $fragment;
};
var recalcularAltoPagina = function recalcularAltoPagina() {
  var alto_pantalla = screen.height;
  Array.from(["#tab_preconsulta", "#tab_consulta", "#tab_citas"]).forEach(function (tab) {
    //d.querySelector(tab).style.border="1px solid red"
    // d.querySelector(tab).style.height=alto_pantalla - 425 + "px"
    d.querySelector(tab).style.height = alto_pantalla - 425 + "px";
  });
};
var store_field = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(name, value, user_id, route) {
    var formdata;
    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {
      while (1) {
        switch (_context.prev = _context.next) {
          case 0:
            formdata = new FormData();
            formdata.append("field", name);
            formdata.append("value", value);
            formdata.append("user_id", user_id);
            formdata.append("_token", document.querySelector("#_token").value);
            _context.next = 7;
            return post(route, formdata);

          case 7:
            return _context.abrupt("return", _context.sent);

          case 8:
          case "end":
            return _context.stop();
        }
      }
    }, _callee);
  }));

  return function store_field(_x4, _x5, _x6, _x7) {
    return _ref.apply(this, arguments);
  };
}();
var selectOptions = function selectOptions(model, param) {
  var fields = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : ['id', 'description'];
  var id = param != undefined ? param : "";
  var selected = ''; //<option value=''>Seleccione</option>

  var data = "";
  model.forEach(function (option) {
    if (equalsInt(option[fields[0]], id)) {
      //console.log(valueOfElement.id+"=="+id)
      selected = 'selected';
    } else {
      selected = '';
    }

    data += "\n                    <option ".concat(selected, " value=\"").concat(option[fields[0]], "\">\n                        ").concat(option[fields[1]], "\n                    </option>\n                ");
  });
  return data;
};
var dia_semana = function dia_semana(date) {
  var abr = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  var maysc = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  var fecha = new Date(date);
  var response = ""; //console.log(date)
  //console.log("dia_semana->",parseInt( fecha.getDay() ))

  switch (parseInt(fecha.getDay())) {
    case 0:
      response = "Domingo";
      break;

    case 1:
      response = "Lunes";
      break;

    case 2:
      response = "Martes";
      break;

    case 3:
      response = "Miércoles";
      break;

    case 4:
      response = "Jueves";
      break;

    case 5:
      response = "Viernes";
      break;

    case 6:
      response = "Sábado";
      break;
  }

  if (abr) {
    response = response.slice(0, 3);
  }

  if (maysc) {
    response = response.toUpperCase();
  }

  return response;
};
var fechaUserCita = function fechaUserCita(cita) {
  //console.log(param)
  var fullHora = cita.hour.split(":"); //2022-03-24 00:12:26

  var anio = cita.year;
  var mes = parseInt(cita.month);
  var dia = cita.day;
  var hora = fullHora[0];
  var minutos = fullHora[1];
  var segundos = fullHora[2];
  var horario = "AM";

  if (parseInt(hora) > 12 && parseInt(hora) <= 23) {
    horario = "PM";
  }

  switch (hora) {
    case "13":
      hora = 1;
      break;

    case "14":
      hora = 2;
      break;

    case "15":
      hora = 3;
      break;

    case "16":
      hora = 4;
      break;

    case "17":
      hora = 5;
      break;

    case "18":
      hora = 6;
      break;

    case "19":
      hora = 7;
      break;

    case "20":
      hora = 8;
      break;

    case "21":
      hora = 9;
      break;

    case "22":
      hora = 10;
      break;

    case "23":
      hora = 11;
      break;

    case "00":
      hora = 12;
      break;
  } //console.log(`${anio}-${mes}-${dia} ${hora}:${minutos}:${is_undefined(segundos)?'00':segundos}`)


  return "".concat(anio, "-").concat(mes, "-").concat(dia, " ").concat(hora, ":").concat(minutos, ":").concat(is_undefined(segundos) ? '00' : segundos);
};
var mes = function mes(date) {
  var abr = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  var maysc = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  var fecha = new Date(date);
  var response = "";

  switch (parseInt(fecha.getMonth())) {
    case 0:
      response = "Enero";
      break;

    case 1:
      response = "Febrero";
      break;

    case 2:
      response = "Marzo";
      break;

    case 3:
      response = "Abril";
      break;

    case 4:
      response = "Mayo";
      break;

    case 5:
      response = "Junio";
      break;

    case 6:
      response = "Julio";
      break;

    case 7:
      response = "Agosto";
      break;

    case 8:
      response = "Septiembre";
      break;

    case 9:
      response = "Octubre";
      break;

    case 10:
      response = "Noviembre";
      break;

    case 11:
      response = "Diciembre";
      break;
  }

  if (abr) {
    response = response.slice(0, 3);
  }

  if (maysc) {
    response = response.toUpperCase();
  }

  return response;
};
var item_historial_cita = function item_historial_cita(cita, centro_salud) {
  //console.log(centro_salud)
  var $item = document.getElementById('artefacto_0031').content.cloneNode(true);
  $item.querySelector(".header-historia-day").textContent = dia_semana(fechaUserCita(cita), true, true);
  $item.querySelector(".header-historia-day-month").textContent = parseInt(cita.day);
  $item.querySelector(".header-historia-month").textContent = mes(fechaUserCita(cita), true, true);
  $item.querySelector(".header-historia-year").textContent = cita.year;
  $item.querySelector(".header-historia-doctor").textContent = cita.medico;
  $item.querySelector(".header-historia-especiality").textContent = cita.especialidad_nombre;
  $item.querySelector(".header-historia-address").textContent = first(centro_salud.filter(function (x) {
    return equalsInt(x.id, cita.centro_salud_id);
  })).description;
  $item.querySelector("li").dataset.cat_user_cita_status_id = 6;
  $item.querySelector("li").dataset.user_cita_id = cita.user_cita_id;
  return $item;
};
var item_historial_cita2 = function item_historial_cita2(cita, centro_salud) {
  //console.log("🚀 ~ file: helpers.js ~ line 396 ~ cita", cita)
  //console.log(centro_salud)
  var $item = document.getElementById('artefacto_0034').content.cloneNode(true);
  $item.querySelector(".header-historia-day").textContent = dia_semana(fechaUserCita(cita), false, true);
  $item.querySelector(".header-historia-day-month").textContent = parseInt(cita.day);
  $item.querySelector(".header-historia-month").textContent = mes(fechaUserCita(cita), false, true);
  $item.querySelector(".header-historia-year").textContent = cita.year;
  $item.querySelector(".header-historia-doctor").textContent = cita.medico;
  $item.querySelector(".header-historia-especiality").textContent = cita.especialidad_nombre;
  $item.querySelector(".header-historia-address").textContent = first(centro_salud.filter(function (x) {
    return equalsInt(x.id, cita.centro_salud_id);
  })).description;
  $item.querySelector("tr").dataset.cat_user_cita_status_id = 7;
  $item.querySelector("tr").dataset.user_cita_id = cita.user_cita_id;
  $item.querySelector("tr").dataset.user_id_paciente = cita.user_id_paciente;
  var reason_for_consultation = cita.reason_for_consultation,
      user_motivo_consulta = cita.user_motivo_consulta;
  var motivo = "";

  if (!is_null(reason_for_consultation)) {
    motivo = reason_for_consultation;
  }

  if (!is_null(user_motivo_consulta)) {
    motivo = user_motivo_consulta.value;
  }

  console.log(motivo);

  if (!is_null(motivo) && !is_empty(motivo) && !is_undefined(motivo)) {
    $item.querySelector(".header-historia-motivo_consulta").textContent = motivo;
  } else {
    $item.querySelector(".header-historia-motivo_consulta").textContent = "No indicado";
  }

  return $item;
};
var active_form = function active_form(tab) {
  var readonly = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;

  if (readonly) {
    d.querySelectorAll("".concat(tab, " textarea")).forEach(function (x) {
      return x.readOnly = true;
    });
    d.querySelectorAll("".concat(tab, " textarea")).forEach(function (x) {
      return x.classList.add("bg-gray");
    });
    d.querySelectorAll("".concat(tab, " input")).forEach(function (x) {
      return x.readOnly = true;
    });
    d.querySelectorAll("".concat(tab, " input")).forEach(function (x) {
      return x.classList.add("bg-gray");
    });
    d.querySelectorAll("".concat(tab, " select")).forEach(function (x) {
      return x.disabled = true;
    });
    d.querySelectorAll("".concat(tab, " select")).forEach(function (x) {
      return x.classList.add("bg-gray");
    });
  } else {
    d.querySelectorAll("".concat(tab, " textarea")).forEach(function (x) {
      return x.readOnly = false;
    });
    d.querySelectorAll("".concat(tab, " textarea")).forEach(function (x) {
      return x.classList.remove("bg-gray");
    });
    d.querySelectorAll("".concat(tab, " input")).forEach(function (x) {
      return x.readOnly = false;
    });
    d.querySelectorAll("".concat(tab, " input")).forEach(function (x) {
      return x.classList.remove("bg-gray");
    });
    d.querySelectorAll("".concat(tab, " select")).forEach(function (x) {
      return x.disabled = false;
    });
    d.querySelectorAll("".concat(tab, " select")).forEach(function (x) {
      return x.classList.remove("bg-gray");
    });
  }
};
var cargandoModal = function cargandoModal() {
  var message = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "Espere un momento por favor...";
  return (
    /*html */
    "\n            <div class=\"bg-primary p-3\">\n                <div class=\"d-flex justify-content-between text-white\">\n                    <span class=\"float-left font-weight-bold\">\n                        ".concat(message, "\n                    </span>\n                    <span class=\"spinner-border float-right\" role=\"status\">\n                        <span class=\"sr-only\"></span>\n                    </span>\n                </div>\n            </div>\n        ")
  );
};
var get_index = function get_index(attr, key, value) {
  return useState[attr].findIndex(function (index) {
    return equalsInt(index[key], value);
  });
};
var get_one = function get_one(attr, key, value) {
  return useState[attr].filter(function (index) {
    return equalsInt(index[key], value);
  });
};
var get_all = function get_all(attr, key, value) {
  return useState[attr].filter(function (index) {
    return equalsInt(index[key], value);
  });
};
var get_one_by_index = function get_one_by_index(index, attr, key, value) {
  return useState[index][attr].filter(function (index) {
    return equalsInt(index[key], value);
  });
};
var add_row = function add_row(attr, value) {
  var position = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : "first";

  if (position === "first") {
    useState[attr].unshift(value);
  }

  if (position === "last") {
    useState[attr].push(value);
  }
};
var delete_row = function delete_row(_ref2) {
  var attr = _ref2.attr,
      key = _ref2.key,
      value = _ref2.value;
  var resultset = useState[attr].filter(function (row) {
    if (parseInt(row[key]) !== parseInt(value)) {
      return row;
    }
  });
  useState[attr] = resultset;
};
var update_row = function update_row(_ref3) {
  var attr = _ref3.attr,
      key = _ref3.key,
      value = _ref3.value,
      resultset = _ref3.resultset;
  var index = get_index(attr, key, value);
  useState[attr][index] = resultset;
};
var add_row_by_index = function add_row_by_index(index, attr, value) {
  var position = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : "first";

  if (position === "first") {
    useState[index][attr].unshift(value);
  }

  if (position === "last") {
    useState[index][attr].push(value);
  }
};

/***/ }),

/***/ 3:
/*!****************************************************************!*\
  !*** multi ./public/class_js/app_registro_paciente_interno.js ***!
  \****************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(/*! C:\Users\PC\Documents\GitHub\patientcare\public\class_js\app_registro_paciente_interno.js */"./public/class_js/app_registro_paciente_interno.js");


/***/ })

/******/ });